- 2026-09-23: The earlier Univer `0.25.1` hold is replaced by the fully aligned `1.0.0` cohort (`@univerjs/core`, `@univerjs/themes`, `@univerjs/presets`, `@univerjs/preset-docs-core`, and `@univerjs/preset-sheets-core`). The document editor and Markdown converter now require the `1.0.0` paragraph/section ID and document-save APIs. Keep all five manifest pins and lockfile resolutions aligned; the earlier failure came from updating only core/themes to `0.25.2`.
