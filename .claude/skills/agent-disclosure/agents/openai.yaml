interface:
  display_name: "Agent Disclosure"
  short_description: "Register fixed AI jobs in the top Agents menu"
  default_prompt: "Use $agent-disclosure to register only existing fixed jobs in the top Agents menu, record approved helper context contracts separately, preserve native and resident agent isolation, never add visible page content, and verify the result live."
