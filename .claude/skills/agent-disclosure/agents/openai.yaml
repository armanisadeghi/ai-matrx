interface:
  display_name: "Agent Disclosure"
  short_description: "Name fixed AI workers and open them in place"
  default_prompt: "Use $agent-disclosure to identify fixed AI workers, exempt agent-authoring and universal-host surfaces, add the PageAgents and manifest role disclosures where required, and verify each surface live."
