---
name: message-actions-overlay-system
description: MERGED into the overlay-system skill (Wave 4 doc consolidation, 2026-07). Do not add content here — read `.claude/skills/overlay-system/SKILL.md` (its "Message actions (chat)" section) instead.
---

# message-actions-overlay-system — merged

This skill was merged into **`overlay-system`** (`.claude/skills/overlay-system/SKILL.md`). The chat message action bar (`AssistantActionBar` + `messageActionRegistry`) dispatches through the one overlay system; `messageActionsSlice` is instance tracking only.
