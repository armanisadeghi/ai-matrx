/** Canonical route for the signed-in user's always-open Launchpad. */
export const USER_LAUNCHPAD_PATH = "/launchpad";

export const LAUNCHPAD_REFRESH_INTERVAL_MS = 60 * 60 * 1_000;
export const LAUNCHPAD_EDITING_RETRY_MS = 5 * 60 * 1_000;
