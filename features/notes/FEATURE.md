# FEATURE.md — `notes`

**Status:** `active` — production, actively maintained
**Tier:** `1`
**Last updated:** `2026-07-15`

> User-facing README at [`README.md`](./README.md). This doc is the agent-facing architecture view.

---

## Purpose

Comprehensive notes system: rich-text editing (WYSIWYG + markdown split view), folder organization, auto-labeling, real-time sync via Supabase Realtime, permissions-backed sharing, and a programmatic API that lets any feature in the app save content to notes. Notes is one of the most-integrated surfaces — it's both a standalone feature and a substrate other features target.

---

## Entry points

**Routes**
- `app/(authenticated)/notes/` — main surface (list, folder tree, editor)

**Feature code** (`features/notes/`)
- `actions/` — thunk-style actions for CRUD
- `components/` — editor, folder tree, list, share UI
- `constants/` — shared constants
- `context/` — `NotesInstanceContext` only (the legacy `NotesContext` provider — with its second realtime channel — is deleted; do not recreate it)
- `hooks/` — note CRUD, labels, folders, realtime subscription
- `redux/` — slice + selectors
- `route/` — route-level helpers
- `service/` — Supabase DB calls
- `components/cleanup/` — **Content cleanup** UI: `NoteCleanupButton` → `CleanupOptionsPopover` → `CleanupReviewDialog` (per-operation `CleanupChangeCard`s with Now/After examples). The engine is a reusable primitive at [`lib/content-cleanup/`](../../lib/content-cleanup/) (`segment` → `operations` → `clean` → `review` → `debug`), not notes-specific.
- `index.ts` — public barrel

**Floating window** (`features/window-panels/windows/notes/NotesWindow.tsx`) — thin composition root: owns the per-instance lifecycle (`registerInstance` + notes-list/scopes fetch) and maps independent, prop-drill-free units (each takes only `instanceId`, reads Redux) onto `WindowPanel` slots — `sidebar`=`NoteSidebar`, `actionsRight`=`NoteViewControls` (view-mode menu + versions toggle), `footer`=`NoteStatsFooter` (save status + content metrics only), body=`NotesWindowView` (tab bar + presence + editor + chrome `NoteMetadataBar` + split). Chrome lives in slots/body; no reinvented header, no `sidebarExpandsWindow` rect mutation. Version-history open state is per-instance (`historyOpen` on `NotesInstance`; `setInstanceHistoryOpen` / `selectInstanceHistoryOpen`).

**Realtime** — uses Supabase Postgres Changes (RLS-authorized) for multi-client sync of note content.

---

## Data model

DB tables (verify in Supabase; names representative):
- `notes` — note rows: `id`, `user_id`, `organization_id`, `project_id`, `folder_id`, `title`, `content` (rich/markdown payload), `labels[]`, timestamps
- `note_folders` — tree structure: parent references
- `note_labels` — auto-labeling metadata

Key types live in `features/notes/` — import from the feature barrel, not internal paths.

---

## Key flows

### Flow 1 — Create / edit a note

1. User opens editor → `notes` slice hydrates the target row via `service/`
2. Edits dispatch granular actions (title, content, labels) — small updates, never full-object replacement
3. Debounced autosave → Supabase update
4. Realtime broadcasts the change to other subscribed clients

### Flow 2 — Folder organization

1. Drag-drop or menu action → thunk updates `folder_id` on the note row
2. Folder tree slice re-derives view

### Flow 3 — Auto-labeling

1. On save (or a dedicated trigger), an agent or server function extracts labels from content
2. Labels stored on the note row / `note_labels` table
3. UI surfaces filter + color-code by label

### Flow 4 — Save-from-anywhere programmatic API

1. External feature (e.g. agents) calls the notes programmatic API with content + optional folder / labels
2. Note row created; user sees it appear in the Notes surface with realtime
3. This is a major cross-feature integration point — agents often write notes as side effects

### Flow 5 — Sharing

1. Note owner opens share modal (from `features/sharing/`)
2. Grants user / org permission → row in `iam.permissions`
3. Grantee's `/notes` sidebar shows it under **"Shared with me"** (`fetchSharedNotesList` → `get_notes_shared_with_me` RPC: direct + org grants, effective level, owner email). Owner-list queries stay `created_by`-filtered; shared records are flagged `_sharedWithMe` and excluded from folder groupings by `selectAllNotesList`.
4. Opening a shared note runs the **edit gate**: `useNoteAccess(noteId)` (hooks/useNoteAccess.ts — composes record ownership, the shared grant level, and the canonical `get_resource_access` RPC). Viewer level → read-only editor + banner + "Duplicate to edit" (desktop `NoteContentEditor` + mobile `MobileNoteEditor`).

### Flow 6 — Content cleanup

1. `NoteCleanupButton` opens an opt-in popover: the engine detects **protected regions** (code, JSON, tables, front-matter, inline code, HTML), shows per-operation toggles with live change counts, and warns when protected content exists.
2. Run → `CleanupReviewDialog`: one plain-language card per **type** of change ("Removed spaces at the end of lines") with real Now→After examples (changed characters shown as visible markers), an Apply/Skip switch each; protected-regions inspector + debug panel + "Copy for AI" XML below.
3. Apply re-runs the engine with **only the accepted operations** (`cleanContent`) and writes via `updateNoteContent` + `saveNote` (auto-versions); Cancel changes nothing — that's the "go back".

---

## Invariants & gotchas

- **Small, granular updates only.** Never replace the entire note object in Redux — follow the project's small-update rule.
- **No silent lost writes.** RLS filters a non-editor's UPDATE/soft-delete to **0 rows with no SQL error** (our `.select().single()` turns it into PGRST116). Every note write path MUST verify rows-affected or single-row result and scream on failure via `utils/writeErrors.ts` (`noteSaveErrorMessage` + `toastNoteWriteBlocked`); the metadata bar shows a red "Save failed". Gate editors proactively with `useNoteAccess` — never render an editable surface for a view-only sharee.
- **Atomic autosave concurrency.** Canonical saves (`autoSaveMiddleware`, `saveNote`) UPDATE with `.eq("updated_at", local)` — `_touch_row` only mutates NEW, so the predicate is valid. 0 rows → conflict (or RLS deny after probe). Do **not** reintroduce a separate SELECT-then-UPDATE TOCTOU check. Mid-save keystrokes: pass `savedSnapshot` to `markNoteSaved` so only matching fields clear dirty; leftover dirty re-triggers via `notes/requestAutoSave`.
- **Echo suppression is timestamp-monotonic FIRST, content-aware second.** The realtime echo of your own UPDATE arrives **50–500ms after** the REST response already stored the fresh `updated_at` — any suppression keyed only on an in-flight-save flag misses it. `isOwnEcho` (realtimeMiddleware) drops any payload whose `updated_at` is not strictly newer than local state (equal timestamps also require content+label match so a same-millisecond collaborator write lands); `applyServerNoteUpsert` (slice) enforces the same monotonic guard on every merge path. While `_savingNoteIds`, the content-aware match still applies: divergent collaborator payloads must reach the upsert. **Never remove either guard; never go back to flag-only suppression.**
- **Realtime is RLS-authorized.** Use Postgres Changes here (not Broadcast) so non-owners only see notes they have access to.
- **The save-from-anywhere API is a public contract.** Agents and other features depend on it; don't break the signature silently.
- **Cleanup never edits protected regions.** `lib/content-cleanup/` masks code / JSON / tables / front-matter / inline-code / HTML out before any whitespace or typography op, then restores them verbatim. The engine is pure and surface-agnostic — reuse it, never fork it, for any paste-cleanup need.
- **Rich text editor:** Notes currently uses its own editor — the legacy `features/rich-text-editor/` is deprecated (per CLAUDE.md) and must not be re-adopted. Verify the current editor before modifying content rendering.
- **The `/notes` route uses inline rich-document actions.** The header intentionally contains only view-mode controls; it does not mount a `RichDocumentActionSurface`. Do not pass an `actionsSurfaceId` from `NotesView` unless the matching consumer is mounted. With no remote ID, preview mode renders the full inline bar and split mode renders its compact inline icon menu.
- **Scope columns** (`user_id`, `organization_id`, `project_id`) follow the project's multi-scope convention — see [`features/scopes/FEATURE.md`](../scopes/FEATURE.md).
- **Folder tree operations are transactional.** Moving a folder with children must update all descendants' paths — don't optimize away the reparenting walk.

### Freeze-loop doctrine — read before touching notes realtime, Redux, or editor code

`/notes` has locked up entire browsers ~10 times in 6 months. Every incident was one of these four mechanisms; **each has a canonical guard in the code — never weaken or reintroduce:**

1. **Self-echo → false conflict.** Covered by the timestamp-monotonic echo rule above. The failure shape: unsuppressed echo of save N arrives while the user typed toward N+1 → `upsertNoteFromServer` flags `_error="conflict"` → conflict UI does heavy work → next save clears it → its echo re-sets it, forever.
2. **Heavy work per render.** `analyzeDiff` is O(lines²); it is memoized as `conflictAnalysis` in `NoteContentEditor` — **never call it (or any O(content) work) inline in JSX.** Same rule for O(all-notes) builds: `useNotesSurfaceScope` memoizes its notes-map projection; `contextData` uses the memoized `surfaceContextData`.
3. **Per-item dispatch loops.** List hydration dispatches **one** `upsertNotesFromServer` batch. A dispatch-per-note loop re-runs every sorted list selector per note — O(N²·log N) on route entry. Event-driven refetches (`focus`/`visibilitychange`) are min-interval gated (30s); realtime reconnect backoff resets only after 30s healthy (a flapping channel must not fetch full lists at the 1s floor).
4. **Effect-identity churn.** Callbacks passed into hooks are ref-held (`useAutoSave` — inline `onSaveSuccess`/`onSaveError` arrows once recreated `forceSave` every render, whose effect-cleanup call made one save attempt per render and an unbounded retry loop on failing saves). `urlActive` is ref-read in `NotesView`'s deep-link effect — `NoteTabBar`'s `history.replaceState` feeds back through `useSearchParams`, so putting it in deps re-dispatches per tab action. Selector factories stay `cached()`.

**Conflict resolution rule:** go through `resolveNoteConflict` (adopts the server `updated_at`, clears the error, keeps dirty so autosave persists) — **never `markNoteSaved({id})` without a snapshot**: it wipes all dirty fields, the queued save bails on `!_dirty`, nothing is written, and the stale `updated_at` re-fires the conflict on the next keystroke.

---

## Related features

- **Depends on:** `features/sharing/` (permissions), Supabase Realtime, the app's scope system
- **Depended on by:** agents (save-to-notes), any feature offering a "save to notes" action
- **Cross-links:** [`features/sharing/FEATURE.md`](../sharing/FEATURE.md), [`features/scopes/FEATURE.md`](../scopes/FEATURE.md), [`features/agents/FEATURE.md`](../agents/FEATURE.md)

---

## Change log

- `2026-07-15` — **Browser-freeze regression fixed + freeze-loop doctrine added.** Root cause (commit `584345434`, Jul 10): `markNoteSaved` cleared `_savingNoteIds` on the REST response while the realtime echo lands 50–500ms later, and the old `_pendingDispatchIds` grace window lost its producers — so every autosave's own echo arrived unsuppressed, flagged a false `conflict` on the dirty record, and `NoteConflictWindow`'s `analyzeDiff` (O(lines²) LCS) ran inline on every keystroke render: main-thread saturation, tab/browser lockup, self-sustaining (each save's echo re-armed it). Fixes: (1) timestamp-monotonic echo guard in `isOwnEcho` + the same guard in `applyServerNoteUpsert` (also kills out-of-order-echo content reverts / sticky conflicts); (2) `conflictAnalysis` memoized; (3) new `resolveNoteConflict` reducer — "Keep mine" was a silent no-op (`markNoteSaved` wiped dirty before the save ran) and "Accept remote" never healed `updated_at`; both handlers rebuilt on it; (4) list hydration batched into one `upsertNotesFromServer` dispatch (was one dispatch per note = O(N²·log N) on entry) and the focus/visibility shared-list refetch min-interval gated (30s); (5) reconnect backoff resets only after 30s healthy (flapping channel = full-list fetch storm at the 1s floor); (6) `NotesView` deep-link effect reads `urlActive` via ref (tab actions re-added closed tabs + refetched); `NoteTabBar` URL writer skips until the instance registers (was transiently wiping `?tabs=` on mount); (7) `useAutoSave` holds callbacks in refs (inline arrows recreated `forceSave` per render → save-per-render + unbounded retry loop on failing saves, reachable via Quick Notes); (8) server metadata merges preserve local `metadata.lastEditorMode` (realtime events wiped it → default-mode effect re-dispatched + remounted the split editor mid-typing); (9) `useNotesSurfaceScope` notes-map projection + `NoteContentEditor` `contextData` memoized (O(all-notes) per keystroke render); (10) dead-and-dangerous code deleted: `NotesContext.tsx` (dormant second realtime channel on `workbench.notes`), `useWorkspaceNotesRedux`, the producer-less `_pendingDispatchIds` family, `selectFilteredNotes` (unbounded selector-cache growth per keystroke), `selectNoteDirtyFields` (fresh `Set` per call), `selectSavingNoteIds` (read the wrong field). Doctrine recorded under **Freeze-loop doctrine** in Invariants.
- `2026-07-14` — **Restored `/notes` rich-document actions (D47).** `NotesView` no longer sends its active editor to a nonexistent `note-detail-*` remote surface. Preview mode now renders the canonical inline action bar and MatrxSplit renders its compact inline action menu; the deliberately decluttered page header and tab-owned note controls remain unchanged. Updated the rich-document remote-surface doctrine so future consumers only publish remotely when a matching surface is mounted.
- `2026-07-13` — **Quick-save core on shared content-refine primitives + window footer.** `QuickNoteSaveCore` now renders `RefinableContentEditor` (`components/content-refine/`) instead of its own copy of the view-toggle/strip-thinking/copy/trim toolbar + TrimRow sliders (duplicate deleted), and portals its footer actions (Cancel/Save + post-save New tab · Window · Go to note · Done) into the `WindowPanel` `footerRight` slot via a new `footerHost` prop — `QuickNoteSaveWindow` provides the slot (same pattern as `TaskQuickCreateWindow`). Dialog/Popover/Overlay consumers keep the inline footer (no `footerHost`). `useQuickNoteSave`: the existing-note selection is stored keyed by folder+mode (derivation-invalidated; the setState-in-effect reset is gone); behavior change: after a save with `showPostSaveActions=false` the footer renders nothing (previously kept showing Cancel/Save).
- `2026-07-12` — **Sidebar: active-note highlight fixed for light mode + bulk-action bar moved to the top.** (1) The active row used `ItemRow`'s default `bg-accent`, which is `zinc-150` in light mode (nearly white on the sidebar) — the active note was invisible in light mode while fine in dark. Active rows (both `NoteSidebarRow` and the inline Shared-with-me rows) now use a **primary tint** (`bg-primary/10 dark:bg-primary/20`) + `font-medium` + a **2px primary left accent bar** drawn as an inset box-shadow (no layout shift) — clearly visible and consistent in both themes. (2) `NoteSidebarBulkBar` previously rendered only once ≥1 note was checked AND was docked at the very **bottom** of the sidebar, where it was pushed off-screen and unreachable. It now renders the moment selection mode is entered, docked at the **top** just below the toolbar, with all actions **disabled until a note is selected** (so the layout never shifts). Added a **select-all/clear toggle**, a live "N selected"/"Select notes" label, and replaced every native `title` with real Radix `SimpleTooltip`s. The bottom New Note / New Folder bar stays put in selection mode.
- `2026-07-12` — **MobileNoteEditor read-only notes get the NON-editable menu.** Review fix: v3's Cut/Paste mutate via `getTextarea`/`onTextReplace` regardless of the textarea's `readOnly` attribute, so the `EditableContextMenu` added earlier today let a read-only viewer dirty `localContent` and arm a doomed autosave. Plain mode now branches: `readOnly` → `NonEditableContextMenu` (copy/export/AI only), writable → `EditableContextMenu` unchanged.
- `2026-07-12` — **Sidebar right-click is the universal v3 menu; last hand-rolled menu deleted.** `NoteSidebarRow` wraps `ItemRow` in `NonEditableContextMenu`, per-note scoped (`contentSource {type:"note"}` + `entity` → Copy-as/Export/Convert/Attach/Share; Open/Duplicate/Knowledge/Move/Delete via `buildNoteContextSections` in `noteMenuRegistry.tsx` — same handlers as the kebab, which keeps the full `ItemMenu` config incl. inline Rename; `ItemRow` gained `disableContextMenu` so right-click belongs to ONE system). Folder headers wrap in `NonEditableContextMenu` with New Note / Rename / Delete All as `extraSections` — the hand-rolled portal menu and `NoteSidebar`'s `contextMenuPortalTarget` prop (incl. `NotesWindow`'s body-portal plumbing) are gone. **Delete All Notes now confirms first** (was an unconfirmed bulk soft-delete). `MobileNoteEditor`'s plain-mode textarea wraps in `EditableContextMenu` (long-press bottom sheet; it previously mounted no menu at all). Legacy `NotesSidebar`/`NotesLayout` render only in the dev-only `/demos/notes-salvage` route — left untouched.
- `2026-07-12` — **Editable context menus carry the note content source (D33).** `NoteContentEditor` and `NoteEditor`'s `EditableContextMenu` mounts never passed `contentSource`, so the shared rich-document Export actions resolved the save-less `raw` adapter — Export → HTML preview opened with no Save button and note save-back was unreachable from edit mode. Both now pass `{ type: "note", noteId }` (held to raw when read-only / phantom), routing html-preview Save through `noteAdapter.edit` → `NotesAPI.update` via the overlay callback registry. Verified live: edit-mode Export → HTML preview → Save persists across reload.
- `2026-07-12` — **Sidebar: collapsible paginated "Recent" section + cleaner toolbar.** In default mode the recent notes were poured loose into the top of the list. They now live in a collapsible **Recent** section (Clock icon, count badge, open by default) that sits **above** "Shared with me" (previously Shared was pinned at the very top, which read as more important than the user's own recents). Recent is paginated: it shows `RECENT_PAGE_SIZE` (10) and grows by that step via **Show N more**, with a **Show less** reset. The old inline default-mode recents strip is removed; `defaultModeRecent` (fixed slice) became `recentSorted` (full sorted list, sliced in render). Toolbar controls were inconsistent (some labeled, some bare, native `title` only): they're now two clearly distinct kinds — **selector chips** (Group, Sort) with a filled `bg-muted/40` showing the current value, and **plain icon actions** (sort-order via `ArrowDownWideNarrow`/`ArrowUpWideNarrow` instead of a bare ↓/↑ glyph, Select, Expand-all) — all uniform `h-6`, and **every control wrapped in a real `SimpleTooltip`** (Radix) rather than a native `title`.
- `2026-07-12` — **Active note is now reliably revealed + centered in the sidebar.** The two separate `NoteSidebar` effects (auto-expand folder, auto-scroll) are replaced by ONE effect keyed only on `activeTabId` + `groupBy` (not `allNotes`, so autosave/realtime churn never re-scrolls the list). On an active-tab change it (1) expands the folder holding the note in folder **and default** modes — default mode previously skipped this, leaving a note buried in a collapsed folder — or opens the "Shared with me" section when the active note lives there; (2) polls a few `requestAnimationFrame`s until the row paints (a just-triggered folder expansion renders a frame later, which the old scroll effect ran too early to see) then smooth-scrolls it to the **vertical center of the list container only** (never ancestors/page). The scroll target is clamped to `[0, maxScroll]`, so a row too near the top stays *above* center and never below it. `allNotes`/`sharedNotes` are read through refs to keep the dep set stable. Highlighting was already correct (`ItemRow active={isActive}`, driven by the URL-synced active tab).
- `2026-07-11` — **`NoteContextStatusIcon` fixed — the tab-strip context icon was silently ignoring its own sizing.** It sat on `ContextStatusButton` (fixed 40px `TapTargetButton` geometry) and tried to shrink it to the 24px tab-strip footprint via `size="xs" buttonClassName="h-6 w-6 border-0 p-0"` — `size` is a no-op on that component and `buttonClassName` *replaces* the status-color class instead of adding to it, so the override never reached the outer button at all. Rebuilt directly on `ContextAssignmentPopover` with a small local trigger matching `NoteTabItem`'s `actionBtnClass` sibling buttons exactly (24px, no border, colored shield icon). Root cause + sibling fix in `PdfStudioPagesMeta`: `features/scopes/FEATURE.md` 2026-07-11.
- `2026-07-10` — **Header Surface Agents chrome: live scope.** `NoteContentEditor` mounts `SurfaceRuntimeProvider` (`matrx-user/notes`) so the shell Agents button can Run with the same `getApplicationScope` as the context menu / ProTextarea.
- `2026-07-10` — **Autosave data-loss holes closed.** (1) Atomic concurrency: UPDATE uses `.eq("updated_at", local)` + `maybeSingle` (0 rows → conflict vs RLS probe) — the old TOCTOU pre-check is gone; `_touch_row` only mutates NEW so the predicate is valid. (2) Mid-save dirty: `markNoteSaved({ savedSnapshot })` only clears fields that still match what was written; `requestAutoSave` re-debounces leftovers. (3) Echo: while `_savingNoteIds`, suppress only when remote content/label match local — divergent collaborator writes reach `upsertNoteFromServer`. (4) Shared-list: monotonic fetch seq ignores stale RPC responses; dirty `_sharedWithMe` notes are not pruned on revoke. Legacy `updateNote` / `useAutoSave` comments corrected; `updateNote` accepts optional `expectedUpdatedAt`.
- `2026-07-10` — **URL active tab fixed + active-first tabs.** `/notes/[id]` no longer steals focus from `?active=` on reload (route effect only activates the path id when there is no `active` param, or on client-side `[id]` changes). `?tabs=` is always written **active-first**; `setInstanceActiveTab` moves the focused note to the front of `openTabs`. Util: `utils/tabUrlOrder.ts`.
- `2026-07-10` — **Version History: wider + content-first.** Side panel default ~720px (was ~520); window secondary panel 520/400. `NoteDiffViewer` default tab is **Content** — full-bleed `TextDiff` side-by-side of the note body; All / Changes / Summary / JSON remain for structured fields (folder, tags, …).
- `2026-07-10` — **Shared-note realtime + reconnect.** `workbench.notes` → `REPLICA IDENTITY FULL`; subscription drops `created_by=` filter so RLS delivers owned + shared-with-me events. CHANNEL_ERROR / TIMED_OUT reconnect with backoff + list catch-up. Mobile `NotesView` forwards `singleNote` into `MobileNotesView`.
- `2026-07-10` — **Notes realtime fixed (data-loss).** Root cause was twofold: (1) `workbench.notes` was never in the `supabase_realtime` publication after the schema move — subscription got zero events; (2) `notesRealtimeMiddleware` existed but was never registered in `lib/redux/store.ts`. Applied `realtime_publication_workbench_notes.sql`, wired the middleware, tightened echo suppression (`_savingNoteIds` + `_pendingDispatchIds` + sync-engine `isPendingEcho`), and pass full row fields (incl. `organization_id`) so INSERT upserts aren't skipped. Follow-up: `markNoteSaved` / `markNoteSaveError` now clear `_savingNoteIds` (was permanent suppression after first autosave).
- `2026-07-10` — **Mobile Trash + live metrics + legacy retirement + window URL isolation.** Mobile list gets Trash (restore / empty / delete forever). `NoteStatsFooter` reads live keystrokes via `noteLiveContent` (not debounced Redux). Utilities + Quick Notes now mount canonical `NotesView` (`hidePageHeader`, `syncUrl: false`) instead of legacy `NotesLayout`/`NoteEditor`. Floating `NoteTabBar` defaults `syncUrl={false}` so windows don't fight over `?tabs=`.
- `2026-07-10` — **`removeNote` clears every instance's tabs** (`openTabs` / `activeTabId` / `splitNoteId`) so hard-delete + empty-trash don't leave dangling "Note not found" panes. `permanentlyDeleteNote` requires rows-affected; legacy `NoteEditor` ProTextarea also sets `enableTextStats={false}`.
- `2026-07-10` — **Notes window layout fix + chrome/stats compartmentalization.** Floating mid-pane `chars · whitespace · words · lines · paragraphs` was ProTextarea's pinned `ProTextFieldStatsBar` (default on) stacked above a thick WindowPanel `footerVariant="bar"` wrapping a self-chromed `NoteMetadataBar` — three footers. Fix: (1) notes `ProTextarea` sets `enableTextStats={false}` (metrics never live in the editor item); (2) new `NoteStatsFooter` owns save status + `PlainTextMetricsBar` and is the WindowPanel `/notes` footer; (3) `NoteMetadataBar` is chrome-only (folder/context/tags), slimmed, rendered above the stats footer in the body; (4) Notes window passes `embedded` so `pb-[85dvh]` doesn't balloon a 680px panel. Also: History → Versions labeling; Trash empty/permanent-delete + deleted dates; mobile Shared-with-me moved to top; default editor mode persisted on first open.
- `2026-07-10` — **Layout basics: MatrxSplit default, header chrome, Shared/Trash placement.** (1) Notes without a saved `lastEditorMode` now default to MatrxSplit (`split`) when the viewport is ≥900px (`usePreferredDefaultEditorMode`); narrower stays on plain. (2) `/notes` header split: center keeps view-mode pills only; sidebar toggle is a left `PanelLeftTapButton`; History / Clean up / Refresh moved to `#shell-header-right` via `PageHeaderRightPortal` + `TapTargetButtonGroup` (`NoteCleanupButton` gained `asTapGroup`). (3) **"Shared with me"** is a top-of-list virtual folder (indigo `Users` icon) instead of a muted footer strip; **Trash** (rose icon, restore) lives at the bottom of the same scrollable list so soft-delete recovery is discoverable — both were previously buried under the New Note bar.
- `2026-07-10` — **Notes sidebar upgraded to the `ItemRow`/`ItemMenu` "..." system + bulk selection.** `NoteSidebar`'s three duplicated inline `<button>` row blocks (recent flat list, default-mode recent, grouped folder list) and its hand-rolled right-click-only note context menu are replaced by `NoteSidebarRow.tsx` — built on the same `ItemRow` primitive the chat sidebar uses for conversations (hover kebab + right-click, both driven by one `ItemMenuConfig`, inline double-click rename). The menu (`components/note-actions/noteMenuRegistry.tsx` → `buildNoteMenu`) offers Rename / Duplicate / Share / Add to knowledge base / Export as Markdown / Move to Folder (submenu) / Delete, modeled on `conversationActionRegistry.tsx`. Share resolves ownership on demand (only when clicked, not per row) via `isResourceOwner` and opens the shared `shareModal` overlay — no per-row `useIsOwner` fetch storm, no bespoke share modal wiring in the sidebar. **Delete skips the confirmation dialog for empty notes** — `isNoteContentEmpty()` (new helper in `noteUtils.ts`, also used by `findEmptyNewNote`/`findEmptyNewNoteInFolder`/`generateUniqueLabel`) decides instantly; a note with real content still goes through the global imperative `confirm()`. `useNoteDelete` (tab bar + mobile editor) gained the same `content` prop and skip-rule, so the tab's delete button and the mobile editor's delete both benefit for free. **Bulk selection mode**: a toolbar toggle (`ListChecks`) turns on checkboxes (rendered as a sibling of `ItemRow`, not inside its `leading` slot — `ItemRow`'s primary element is itself a button, so a nested interactive checkbox would be invalid button-in-button HTML); once ≥1 note is selected, `NoteSidebarBulkBar.tsx` replaces the "New Note / New Folder" footer with Move / Add to knowledge / Export / Share (single-selection only) / Delete, each bounded-concurrency fanned out (`utils/concurrency.ts#runWithConcurrency`, 4-way parallel, mirrors the Files feature's `BulkActionsBar` pattern) and a single `confirm()` for the whole batch on delete (skipped if every selected note is empty). Markdown export reuses one path for 1 vs 2+ notes (`utils/exportNotesMarkdown.ts` — single `.md` blob, or a `JSZip` bundle with collision-safe filenames). New reusable primitive: `features/rag/api/ingest.ts#ingestSource(sourceKind, sourceId)` — `ingestFile` is now a thin `cld_file`-scoped wrapper around it; bulk "Add to knowledge" calls it directly per note (no per-stage progress UI needed for a background bulk op).
- `2026-07-09` — **Sidebar folder counts fixed when active org ≠ personal org.** After the org retrofit, notes were stamped onto the user's personal organization while most users keep a company org as active/default. The sidebar filtered strictly on `activeOrgId`, so every folder showed **0** even though notes still existed (and appeared in FolderQuickPick, which reads the unfiltered list). Fix: `noteMatchesActiveOrgContext` — folder counts / lists include notes from the **active org OR the personal org**; other company orgs stay hidden. Folder headers are now derived from that same filtered set (was `allNotes`, which left empty folders for excluded orgs). Same filter on mobile `MobileNotesList`.
- `2026-07-09` — **KgSuggestionsChip restored to the note context surface.** The chip (`features/kg-suggestions/components/KgSuggestionsChip.tsx`) was lost when `NoteContextPicker` was replaced by `NoteContextSection` — per-note KG → scope-item fill suggestions (the visible NER payoff for notes) had no note-UI surface. It now renders beneath the `ContextAssignmentField` inside `NoteContextSection` (so it appears in the metadata-bar panel, `NoteInfoPanel`, and the mobile `NoteEditorDock` for free); self-hides at count 0. The 2026-06-02 entry's `NoteContextPicker` placement is historical.
- `2026-07-08` — **Shared-with-me reaches mobile; split-mode read-only made real.** `MobileNotesList` renders the "Shared with me" section (owner email + view/edit badge) and its "Shared only" filter now filters to shared notes (was a dead stub returning `[]`); `MobileNotesView` resolves selection across shared records and full-fetches content on open (RPC rows are content-less), gating the editor on readiness. `MatrxSplit` gained a **`readOnly` prop** — a swallowed `onChange` alone let viewers type into the split editor, the text painting into the DOM and silently going nowhere; the prop sets native `readOnly` on the textarea and disables the full-screen editor entry, and `NoteEditorCore` split mode passes it through. Adversarial review closed four more holes: preview mode's full-screen editor is disabled for viewers (its saves applied a LOCAL overlay that displayed as saved while `onContentChange` was a no-op — block edits now toast `NOTE_READONLY_SAVE_MESSAGE` instead); desktop gained the same content-readiness gate as mobile (an editor-level sharee typing into the empty pre-fetch editor could overwrite the owner's entire note on save); `deleteNote` thunk now blocks non-admin sharees (soft delete is an UPDATE, so RLS *permits* editor sharees — the 0-row check only catches viewers); plain-mode `ProTextarea` uses `readOnly` not `disabled` (viewers keep select/copy). Mobile: dock folder/tags mutators hidden for viewers, cleanup button hidden, failed autosave parks until the next edit (was a 2s infinite retry loop), "Shared only" filter skips the folder filter (shared rows carry the owner's folders). Removed the `__sharedNotesDebug` window global.
- `2026-07-07` — **Shared notes are first-class + silent lost-writes killed.** (1) New `get_notes_shared_with_me()` RPC (migration `get_notes_shared_with_me_rpc.sql`; direct + org grants, active/unexpired, effective max level, owner email) → `fetchSharedNotesList` thunk → records flagged `_sharedWithMe`/`_sharedMeta` → sidebar **"Shared with me"** section (desktop `NoteSidebar` + `NotesWindow`; excluded from owner folder groupings via `selectAllNotesList`). (2) **Edit gate** `hooks/useNoteAccess.ts` — ownership/grant-level/`get_resource_access` composition; viewer-level sharees get a read-only editor + amber banner + "Duplicate to edit" in `NoteContentEditor` (TUI modes degrade to preview) and `MobileNoteEditor`. (3) **Loud write failures** (`utils/writeErrors.ts`): RLS-filtered saves (PGRST116/42501 → friendly message + deduped toast) in `saveNote`, `autoSaveMiddleware` (INSERT+UPDATE), `useAutoSave` (incl. unmount flush); `deleteNote` (thunk + service) now checks rows-affected and refuses to remove the note locally on a 0-row delete; `NoteMetadataBar` shows a red "Save failed" (with the error as tooltip) instead of a benign "Unsaved". Root cause: viewer UPDATEs are RLS-filtered to 0 rows with NO error, and the list query is owner-only by design — shared notes were invisible and their edits were silently discarded.
- `2026-07-07` — **No-login link sharing + DM-on-share; legacy accept-flow removed.** Notes are the first consumer of the canonical no-login share-link system (see [`features/sharing/FEATURE.md`](../sharing/FEATURE.md) 2026-07-07): the Share modal's Public tab now has an "Anyone with the link" panel that mints a token opening the note at `/s/[token]` with **zero sign-in** (anon `resolve_share_token` RPC, column-allowlisted to safe note fields, rendered read-only via `react-markdown`). Sharing a note with a user now also sends them an in-app DM with a clickable resource card. The legacy `/notes/share/[id]` "accept a shared note" route + `acceptSharedNote` were **deleted** — grants are immediate via RLS (the note just appears in the grantee's list) and the DM is the notification; the accept step was redundant.
- `2026-07-07` — **Note sharing fixed + de-duplicated.** Sharing a note was fully broken: the DB share RPCs threw `42703 column "user_id" does not exist` because `workbench.notes` is canonical (`created_by`/`visibility`) while its `shareable_resource_registry` row still said `user_id`/`is_public` (root cause + fix in [`features/sharing/FEATURE.md`](../sharing/FEATURE.md) 2026-07-07). Separately, the legacy `NotesLayout`/`QuickNotesSheet` surfaces used a bespoke `ShareNoteDialog` whose "share with user" was a dead "Email lookup coming soon" stub (it only displayed status). **Deleted `ShareNoteDialog.tsx`**; added `NoteShareModal.tsx` (thin adapter supplying `isOwner` via `useIsOwner` over the canonical `features/sharing/ShareModal` — the same UI the live `/notes` tab/editor already use). All note-share surfaces now go through the one canonical ShareModal (user search + email lookup + org + public). Do NOT reintroduce a bespoke note share dialog.
- `2026-07-05` — Added official default folder **Chat Saves** (`MessageSquareText`, sky) for assistant-message captures. Quick Note Save accepts optional `defaultNoteName` (chat flow: `{conversation title} Message {n}`).
- `2026-07-03` — **`createFolder` is now truly atomic — it had no unique constraint behind its get-or-create and silently minted duplicate folders under concurrent Quick-Saves.** Migration `note_folders_created_by_name_unique.sql` (applied + ledger-recorded + live-verified): deduped the one existing `(created_by, name)` dup (40→39; nesting unused, notes reference folders by `folder_name` string not id, so nothing orphaned) and added a FULL unique index `note_folders_created_by_name_unique (created_by, name)`. `createFolder` now `upsert(..., { onConflict: "created_by,name", ignoreDuplicates: true })` → `ON CONFLICT DO NOTHING` (never a 23505/409); on conflict it returns the existing live row. Because the FULL index spans soft-deleted rows that RLS hides from the `authenticated` client (so a soft-deleted folder would block same-name recreate with no way to see/revive it), `deleteFolderNotes` now **hard-deletes** the folder *record* (a name-keyed picker-registry row — notes reference folders by `folder_name` string, and `ensureFolderMaterialized` recreates on demand; the notes themselves stay soft-deleted / recoverable). Part of the app-wide non-atomic get-or-create sweep (see `features/transcript-studio/FEATURE.md` 2026-07-03). *(The initial version tried to revive soft-deleted folder rows client-side — an adversarial review proved RLS defeats that; hard-delete is the fix.)*
- `2026-06-27` — **No more homeless notes.** New notes are no longer created with `organization_id = null`: `createNote` (the single service chokepoint for all ~10 callers) now defaults a missing org to the user's **personal organization** via the canonical `ensure_personal_organization` RPC (mirrors `projectService.createProject`); RLS `std_insert` already permits it (`has_org_access`). For notes that are already homeless, the `NoteSidebar` "N notes without an organization" banner is now actionable: alongside the Show/Hide toggle it has an **"Add to my organization"** button → `confirm(...)` popup → `assignHomelessNotesToPersonalOrg()` (one RLS-safe bulk `UPDATE … WHERE organization_id IS NULL`) → `fetchNotesList()` refresh + success toast. The banner's old behavior (a click that only toggled a filter, with a "use its context shield" tooltip that led nowhere) is gone.
- `2026-06-24` — **Left notes sidebar is now collapsible + resizable** (react-resizable-panels v4, the canonical pattern). `NotesView`'s fixed `w-[280px]` sidebar is now a `ResizablePanel` in a horizontal `ResizablePanelGroup` (`id="notes-shell"`): drag-resize (220px default — narrower than before — min 180px, max 42%), collapse to 0 via a header `PanelLeft`/`PanelLeftClose` toggle, and the split persists to the `panels:notes-shell` cookie read server-side in `app/(core)/notes/layout.tsx` (`sidebarLayout` prop → flash-free first paint). The library owns the size; only the collapsed boolean is mirrored (for the toggle icon + to hide the drag handle when collapsed, preventing drag-reopen). Desktop only (mobile keeps `MobileNotesView`); when the sidebar is config-hidden (`singleNote` / `showSidebar=false`) the main area renders full-width with no group. Also: the tab context-status icon now matches the other action buttons (same size, no border, color kept).
- `2026-06-24` — **Light-mode modal contrast fix (cross-feature).** Hand-rolled modal surfaces using the translucent glass (`matrx-glass-core` / `matrx-glass-thin-border` / `var(--matrx-glass-bg)`) rendered dark over the dark modal scrim in BOTH themes, so `text-foreground` was unreadable in light mode (reported on the notes delete confirm). Swapped these modal surfaces to the canonical opaque `bg-background border` (matching `components/ui/dialog`/`alert-dialog`): `NoteTabItem` delete `AlertDialog`, `CanvasShareSheet` (Dialog + Drawer), `ResearchFilterBar` mobile filter Drawer. Glass is correct for window/dock chrome (over the app background) — only modal content over a dark scrim was affected; audit found no others.
- `2026-06-24` — **Notes → knowledge base (RAG), + two salvaged behaviors.** A note can now be indexed into the knowledge base and tested in place — reusing the file/PDF pattern, no backend/schema (the `/rag/ingest/stream` endpoint already accepts `source_kind:"note"`). **`NoteKnowledgePanel`** (new) hosts it in a pop-out resizable side panel via the **`noteKnowledgePanel`** overlay (`SidePanelSurface`, opener `overlays/openers/noteKnowledgePanel.tsx`): index/re-index with `ProcessForRagButton sourceKind="note"`, and once indexed it embeds the canonical RAG viewer (`LibraryPreviewPage embedded` — chunks, raw/cleaned text, per-document **Test search**) + a Full-viewer link (`/rag/viewer/<processed_document_id>`). Entry points: the tab **"…" menu** (`NoteTabItem`, with a live "indexed" dot) and the **mobile dock "More" sheet** (`NoteEditorDock`). `useNoteIngestStatus` extended to return `documentId`. Once indexed, notes are also searchable at `/rag/search` (Notes filter). **Also:** mobile **"New folder"** in `MobileNoteToolbar` (reuses `CreateFolderDialog` + `createFolder`, assigns the current note); and **drag-edge auto-scroll** ported back into `NoteSidebar` (`handleListAutoScroll` on the list container — auto-scrolls while dragging a note near the top/bottom edge).
- `2026-06-24` — **Dead-component sweep (16 files deleted).** Removed `NoteToolbar`; the abandoned `shell/` RSC rewrite (7: `NotesShell`, `NoteViewShell`, `NoteEditorPlaceholder`, `NotesMainArea`, `NotesTabBar`, `NotesSidebar`, `NotesSidebarClient`); the bespoke context-menu chain now fully superseded by `UnifiedAgentContextMenu` (`NoteContextMenu`, `NoteContextMenuContent`, `noteContextMenuBridge`, `useNoteContextMenuGroups`, plus its only consumers `NoteEditorWithChrome` + `actions/WindowNotesBody`); and dead mobile bits `MobileActionsMenu` + `MobileFolderSelector`. All had zero live consumers (`tsc` clean after). **Salvaged capabilities live on via surviving primitives** — see `(dev)/demos/notes-salvage`: (1) **RAG/knowledge-base indexing for a note** (`ProcessForRagButton` + `useNoteIngestStatus`, the one thing the canonical `/notes` lacks — wire into the tab "…" menu); (2) sidebar drag-edge auto-scroll; (3) mobile "New Folder" creation; (4) `NoteViewShell`'s declarative single/split-frame idea. **Still open:** the legacy `NotesLayout` / `NoteEditor` / `NoteTabs` / `NotesSidebar` stack (live only via the Utilities + Quick-Notes overlays) is a second notes UI; canonical `NotesView`/`NoteContentEditor` is a strict superset — migrating those overlays would retire it.
- `2026-06-23` — **Notes tab controls consolidated; header decluttered.** The live `/notes` header (`NotesView` `PageHeader`) now holds **only** the centered view-mode pills (+ History / Clean up / Refresh); the stranded `NoteReferenceCopyButton` and the `RichDocumentActionSurface` "…" bar were removed from it (they pushed the pills off-center). All per-note actions live on the tab (`NoteTabItem`): inline = **copy content · share · context · mic · … · close**; the **"…" dropdown** (one shared item list with the right-click menu) holds Save, Copy reference (bookmark), Duplicate, About this note (was the standalone Info icon), Export, Close tab/others/all, Delete. Removed the duplicate inline Info / Save / Delete icons.
- `2026-06-23` — **`/notes` editor migrated to the canonical context menu.** `NoteContentEditor` (the live `/notes` route's editor) now mounts `UnifiedAgentContextMenu` (dynamic, `{ ssr: false }`, with a flex-reserving `loading` fallback so the editor body never collapses during the brief chunk load) in place of the bespoke `NoteContextMenu` — so its dynamically-built items behave identically to `/chat` and it shows the `matrx-user/notes · C1V1` footer. `createNotesEditorExtraSections(config)` now takes the surface's REAL handlers (save / duplicate / export / share-link / copy / move-to-folder submenu / close-tab×3 / delete, plus super-admin "Permanently Delete" behind a confirm) instead of toast stubs; the host passes its live callbacks. `NoteContentEditor` also mounts `useNoteUndoRedo` — which installs the capture-phase Cmd+Z / Ctrl+Z interceptor that keeps undo synced with Redux (previously mounted inside the bespoke menu) — and passes undo/redo + `contextData` to the menu (so the menu's Compare-without-selection and admin Inspect-Context aren't empty). Bespoke `NoteContextMenu` / `NoteContextMenuContent` / `useNoteContextMenuGroups` / `noteContextMenuBridge` remain only for the not-yet-migrated note surfaces (mobile editor, `NoteEditorWithChrome`).
- `2026-06-23` — **Content cleanup added.** `NoteCleanupButton` (live `/notes`, Notes window, mobile) opens an opt-in popover — per-operation toggles with live counts, protected-content summary, power-user warning — then `CleanupReviewDialog`: **one plain-language card per type of change** (`CleanupChangeCard`) with real Now→After examples (changed characters shown as visible markers, never whole-line highlight), an Apply/Skip switch each, plus `ProtectedRegionsInspector` + `CleanupDebugPanel` + Copy-for-AI XML. Apply re-runs `cleanContent` with the accepted operations → `updateNoteContent` + `saveNote` (auto-versions). Engine is a new reusable primitive at `lib/content-cleanup/`: `segment` (protected-region detector — code/JSON/tables/front-matter/inline-code/HTML) → `operations` (registry; each op exposes precise `edits()`) → `clean` (masking orchestrator) → `review` (per-operation cards + examples) → `debug` (XML). Whitespace/typography ops only ever touch unprotected text. Deletes the dead `utils/textCleanup.ts` (code-unsafe, zero callers).
- `2026-06-23` — Notes editor wired to the live agent surface. `NoteEditorCore`/`NoteContentEditor` (live `/notes` route) and the legacy `NoteEditor` now render `ProTextarea` for the note body (ref forwarded straight to the textarea — no manual mutation) and `ProInput` for the title, each carrying `surfaceName: "matrx-user/notes"` + a plain `getApplicationScope`; the four `UnifiedAgentContextMenu` mounts pass `getApplicationScope` / `contextData` / `extraSections`, and the markdown-preview keeps its presentational menu. The body's "…" menu now lists the surface's bound agents and runs them with full scope.
- `2026-06-22` — Target context-menu wiring extracted: `buildNotesEditorContextData`, `NOTES_EDITOR_CONTEXT_MENU_PROPS`, `createNotesEditorExtraSections`. `useNotesSurfaceScope` delegates to the shared builder. Demos use this as the canonical notes shape (surfaceName + full scope + extraSections) ahead of migrating `NoteEditor` off legacy `NoteContextMenu`.
- `2026-06-23` — **`NotesWindow` can deep-link to a note.** `useOpenNotesWindow` / `OpenNotesWindowOptions`, `NotesWindowProps`, and the `OverlayController` notesWindow block gained `initialNoteId`. When set, the window opens that note as the active tab on mount (`addInstanceTab` + `setInstanceActiveTab` + `fetchNoteContent`) instead of just showing the list. Reusable opener primitive — first consumer is the `note` tool-call renderer's "Open in Notes" action (`features/tool-call-visualization/renderers/note/`).
- `2026-06-22` — **Notes floating window rebuilt as a slot-delegating composition root** (Windows Panel System Overhaul, Phase 1). `NotesWindow.tsx` now owns only the instance lifecycle + list/scopes fetch and maps independent units onto `WindowPanel` slots (`sidebar`=`NoteSidebar`, `actionsRight`=`NoteViewControls`, `footer`=`NoteMetadataBar`, body=`NotesWindowView`); passes explicit `width`/`height`/`position` + canonical `bodyClassName`; **dropped `sidebarExpandsWindow`** (it mutated the window rect on sidebar toggle). New prop-drill-free units `NoteViewControls.tsx` (view-mode menu + history toggle) and `NotePresenceBanner.tsx`. `NotesWindowView.tsx` is now a dumb body with a **window-relative** version-history pane (`ResizablePanel` desktop / `Drawer` mobile), replacing `NoteVersionHistory`'s `MatrxDynamicPanelHost` (sized to `window.innerWidth`, overflowed when nested). New per-instance slice state `historyOpen` + `setInstanceHistoryOpen` + `selectInstanceHistoryOpen` (was local React state). `OverlayController` notesWindow `windowInstanceId` falls back to `inst.instanceId` so multiple windows get distinct instances.
- `2026-06-22` — `NotePickerPopover` search now shows **Folders** and **Notes** sections together (folder name or contained-note match + flat note picks), instead of hiding folder matches during search. (`NoteVersionHistoryPanel`) with `NoteDiffViewer`, per-version compare shortcuts, and a link to `/notes/[id]/diff` for the full-page view. Replaces timeline-only `DiffHistory`. MatrxDynamic right-panel header gets `pr-10` so controls clear the shell avatar.
- `2026-06-19` — `NoteContentAdapter` (version diff) now renders **word-level** intra-line highlighting. It was line-level only (a one-word edit tinted the whole line, and a removed line never aligned with its replacement). The renderer now runs the canonical `computeTextDiff` engine (`components/diff/text/engine`) to align removed+added into MODIFIED pairs and emit word segments, while keeping the existing collapse-unchanged, ignore-whitespace toggle, and stats/summary UX intact. `analyzeDiff` still backs the stats line + `toSummaryText`.
- `2026-06-12` — Find/replace **scroll-to-active-match** reliability fixes. The active match now scrolls into view in every editor mode and on every entry path, not just when the match index changes mid-search. (1) `NoteFindMatchOverlayRedux` / `NotePreviewFindHighlightRedux` (`NoteContentEditor.tsx`) seed `prevActiveRef` with a `-1` sentinel so a fresh mount — reopening the bar, or switching editor mode with find open — counts as a change and scrolls the already-active match into view instead of leaving the viewport stranded. (2) Both find surfaces are now `key={editorMode}` so they remount cleanly on a mode switch and re-bind to the new DOM container (a stable ref can't tell the effect its `.current` element swapped). (3) `usePreviewFindHighlight` waits for the rendered markdown to actually appear (MutationObserver + poll backstop) before highlighting/scrolling, distinguishing "rendered doc with no matches" (settle) from "content not mounted yet" (keep watching) via `container.textContent`. (4) New `refreshNonce` option + a component-owned bounded readiness ticker (`~150ms`, ~18s cap, stops the instant the container has rendered text) re-runs the highlighter on a cold first switch into preview — where React renders a Suspense fallback then swaps in a new scroll-container element once the lazy markdown chunk resolves, leaving the original ref pointing at an empty node.
- `2026-06-11` — Notes tab strip + folder dropdown fixes: `NoteTabBar` splits the strip into a non-scrolling **pinned cluster** (new-note `+` button + the active tab) and a **scrolling region** for the remaining tabs, so the current tab's label is never scrolled out of view and other tabs start where it ends instead of sliding behind it (the earlier `sticky` approach let siblings overlap). `NoteMetadataBar`'s folder dropdown is now portaled to `document.body` with fixed positioning (anchored to the trigger rect, `z-[10000]`) — it was being clipped by the metadata row's `overflow-hidden` + upward (`bottom-full`) open, which read as "contents don't render." Closes on outside-click/Escape; scroll/resize *reposition* it rather than closing (the trigger lives in a fixed bottom bar).
- `2026-06-10` — Added `NotePickerPopover` (lazy `NotesAPI.listItems` on open, collapsible folder tree + search, Drawer on mobile) and `fetchNoteListItems` / `NotesAPI.listItems` for lightweight picker queries without note content. Consumed by transcription cleanup context blocks.
- `2026-06-03` — Added a single **Note Info** window panel that consolidates per-note metadata in one place: content stats (words, characters, characters-no-spaces, lines, paragraphs, reading time), timeline (created/updated/version), folder (editable), the full hierarchy context picker (reuses `NoteContextPicker`), tags, and copyable identifiers. New shared util `utils/noteStats.ts` (`computeNoteStats` / `formatStatNumber`) is the single source of truth for content metrics — consumed by `NoteMetadataBar`, the editor chrome status bar, and the info panel, memoized on `content` so it never recalculates on unrelated re-renders. `NoteMetadataBar` now shows clear `N words · N chars` (was a bare `Nw`). The note **tab** (`NoteTabItem`) drops its separate context (Network) and folder icons in favor of one **Info** icon that opens the window (folder + context now live inside it). Window wiring: `features/window-panels/windows/notes/NoteInfoWindow.tsx` + overlay `noteInfoWindow` (registry metadata, overlay-ids, catalogue, opener, `OverlayController` block). Ephemeral, singleton, `mobilePresentation: "drawer"`.
- `2026-06-02` — Phase F (kg-suggestions): added `<KgSuggestionsChip filter={{ sourceKind: "note", sourceId }} />` to `NoteContextPicker` beneath the scope tagger. Surfaces pending KG → scope-item fill suggestions for the note; hidden at count 0. No notes-slice changes. See `features/kg-suggestions/FEATURE.md`.
- `2026-05-15` — Added a Refresh button to the desktop notes header that mirrors the route-start fetches: `fetchNotesList()`, plus `fetchScopeTypes` / `fetchScopes` when an org is active, plus a forced refetch of every currently-open tab. Introduces `refreshNoteContent` thunk in `redux/thunks.ts` — a force-fetch sibling of `fetchNoteContent` that bypasses the `_fetchStatus === "full"` short-circuit but skips dirty notes to protect unsaved local edits.
- `2026-05-15` — Notes now emits the `matrx-user/notes` surface scope when launching an agent shortcut from the context menu. Adds `hooks/useNotesSurfaceScope.ts` (the scope builder) and `utils/markdown-headings.ts` (heading-aware section slicing). The surface manifest at `features/surfaces/manifests/notes-editor.manifest.ts` declares 19 surface-specific values covering active-note metadata, selection/scope mirror, workspace context, and editor pane state.
- `2026-04-25` — Removed `@/features/notes` barrel imports; consumers use `components/NotesLayout`, `service/notesApi`, `actions/CategoryNotesModal`, `types` (no new barrel file).
- `2026-04-22` — claude: initial FEATURE.md extracted from README.md.

---

> **Keep-docs-live:** changes to the DB schema, realtime pattern, or the save-from-anywhere programmatic API must update this doc. Keep `README.md` focused on user-facing guidance; architecture notes go here.
