# RAG Feature

Single home for everything Retrieval-Augmented Generation in this app — the doc library and processing pipeline, data stores, the document viewer, search, plus the typed API clients and React hooks every RAG surface depends on.

**Routes that live here:**

- `/rag` — landing dashboard (`components/RagHomePage.tsx`)
- `/rag/library` — processed-document library (`components/library/LibraryPage.tsx`)
- `/rag/library/[id]/preview` and `/rag/viewer/[id]` — preview / full viewer (`components/library/LibraryPreviewPage.tsx`, `components/documents/DocumentViewer.tsx`)
- `/rag/data-stores` — manage data stores and bind documents to them (`components/data-stores/DataStoresPage.tsx`)
- `/rag/repositories` — code repositories you can index for RAG (`components/RepositoriesPage.tsx`)
- `/rag/search` — multi-tab Search Lab: clean search, agent simulation, **agent chat (canonical managed agent on the `matrx-user/rag-search` surface)**, diagnostics (`components/search/RagSearchExperience.tsx`)

---

## Layout

```
features/rag/
├── README.md                          ← this file
│
├── api/                               ← typed wire clients (mirror Pydantic shapes)
│   ├── document.ts                    GET  /document/*
│   ├── ingest.ts                      POST /rag/ingest{,/stream}
│   ├── search.ts                      POST /rag/search
│   ├── stages.ts                      POST /rag/library/{id}/{extract,clean,chunk,embed,run-all}
│   └── derivations.ts                 derivation rollup/resume + rebuild-preview/confirmation handshake + cancel
│
├── hooks/                             ← all RAG-related React hooks
│   ├── useDataStores.ts               data-stores CRUD + member management
│   ├── useDocument.ts                 single-document fetch (chunks/lineage/pages)
│   ├── useFileIngest.ts               kick a file through /rag/ingest with live progress
│   ├── useKnowledgeAssetRunner.ts     per-op derivation runner (rollup + run/runAll/cancel, live counts)
│   ├── useLibrary.ts                  library list + summary (auto-polling)
│   ├── useProcessingRunner.ts         multi-job runner for stage + pipeline jobs
│   ├── useRagSearch.ts                /rag/search with debounce + filters
│   ├── useStageAction.ts              run a single stage (extract/clean/chunk/embed)
│   └── useStagesStatus.ts             per-doc stage-status pills
│
├── types/                             ← per-domain wire types (Pydantic mirrors)
│   ├── library.ts                     DocStatus, LibraryDocSummary, LibraryDocDetail, …
│   ├── data-stores.ts                 DataStore, DataStoreMember, …
│   ├── data-stores-ext.ts             enums (DATA_STORE_KINDS, SOURCE_KINDS, …)
│   └── documents.ts                   DocumentDetail, ChunkRow, LineageTree, PageDetail, …
│
├── animations/                        ← standalone HTML animation prototypes
│   ├── rag_pipeline_overview_animation.html
│   ├── retrieval-animation.html
│   ├── upload-pipeline-animation.html
│   └── step_{1..6}_*_animation.html   reference visuals for the 6-stage pipeline
│
└── components/                        ← UI, sub-foldered when an area is large
    ├── RagHomePage.tsx                /rag landing page
    ├── RepositoriesPage.tsx           /rag/repositories landing page
    ├── ProcessForRagButton.tsx        cross-cutting: button used by files + notes toolbars
    │
    ├── library/                       large area — own README inside
    │   ├── LibraryPage.tsx            /rag/library
    │   ├── LibraryPreviewPage.tsx     full-screen single-doc preview
    │   ├── LibraryDocDetailSheet.tsx  per-doc side sheet (read + run stages inline)
    │   ├── ProcessingProgressSheet.tsx multi-job progress sheet
    │   ├── ProcessingJobView.tsx      reusable single-job visualization (used in both sheet & detail-tab)
    │   ├── ProcessingProgressDialog.tsx legacy single-job dialog (kept for IngestProgressDialog)
    │   ├── IngestProgressDialog.tsx   files-side ingest dialog (uses useFileIngest)
    │   ├── ActiveJobsStrip.tsx        compact in-page strip for concurrent jobs
    │   ├── AnimatedKpiCard.tsx        animated count-up KPI tiles
    │   ├── KnowledgeAssetPanel.tsx    Knowledge Asset Builder — derivation rollup + per-op build/rebuild/cancel + live activity (mounted in PdfStudioInspector "Assets" + LibraryDocDetailSheet "Knowledge Asset" tab)
    │   ├── knowledgeAssetStatus.ts    durable-output + latest-run resolver: Build vs Resume vs Rebuild
    │   ├── StageAnimations.tsx        per-stage hero animations (extract/clean/chunk/embed)
    │   ├── StageStatusPills.tsx       4 stage-state pills (run-all + per-stage actions)
    │   ├── StatusBadge.tsx            doc-status pill
    │   ├── QuickSearchDialog.tsx      modal vector-search prompt
    │   └── README.md                  ← in-depth library architecture notes
    │
    ├── data-stores/
    │   ├── DataStoresPage.tsx         /rag/data-stores
    │   ├── DataStoreBindPanel.tsx     bind a doc to one or many data stores (used by DocumentViewer + PdfStudioInspector + PdfExtractorWorkspace)
    │   ├── RichMemberTable.tsx        members of a data store with rich source metadata
    │   └── CldFilePicker.tsx          cloud-files picker scoped for member adds
    │
    ├── documents/                     4-pane document viewer
    │   ├── DocumentViewer.tsx         resizable 4-pane shell
    │   ├── LineageBreadcrumbs.tsx     binary + processing lineage chips
    │   └── panes/
    │       ├── PdfPane.tsx            renders the original PDF page-by-page
    │       ├── CleanedMarkdownPane.tsx cleaned markdown body for the page
    │       ├── ChunksPane.tsx         virtualized chunks list (selectable)
    │       └── RawTextPane.tsx        raw extracted text per page
    │
    ├── search/
    │   ├── RagSearchExperience.tsx    /rag/search — four-tab Search Lab (search, agent sim, agent chat, diagnostics)
    │   └── RagSearchHits.tsx          embeddable hit list (used by file context menu, omnibox, etc.)
    │
    └── agent-context/
        └── buildRagSearchContextData.ts  canonical `contextData` + menu props for the `matrx-user/rag-search` surface (Search tab box + results)
```

---

## What stays outside this feature (and why)

The following live in `features/files/` even though they have "rag" in the name — they are **files-table chrome that reads `cloudFiles.ragStatus` from the cloud-files Redux slice**, not the rag feature:

- `features/files/redux/rag-thunks.ts` — `prefetchRagStatusesForFiles()` mutates the cloudFiles slice.
- `features/files/components/surfaces/desktop/RagStatusCell.tsx` — renders per-file rag status in the file table.
- `features/files/components/surfaces/desktop/RagFilterPicker.tsx` — column header filter for rag status.

Splitting them into `features/rag/` would split the cloudFiles slice across two features. They consume the moved API clients (`@/features/rag/api/ingest`) but the state and rendering stay in files.

---

## Conventions

- **Imports inside `features/rag/`** are absolute (`@/features/rag/...`). Do not introduce new `../` relative imports across sub-areas — they break when files move.
- **Imports across sub-areas inside `components/`** (e.g. `data-stores/RichMemberTable` reaching for `library/StatusBadge`) are fine — these are sister surfaces in the same feature.
- **Redux state for per-file rag status** lives in the `cloudFiles` slice (`features/files/redux/`). The rag feature itself has no slice today; it composes auth + cloud-files state via existing selectors (`selectUserId`, `selectRagStatusForFile`, etc.).
- **Backend contract** for new endpoints is documented in [`features/files/for_python/REQUESTS.md`](../files/for_python/REQUESTS.md) — that file pre-dates this consolidation and has not been moved (the Python team's bookmark stays stable).

---

## Shared Knowledge Resources

Curated, system-owned RAG content (canonical example: the **AMA Guides 5th Edition**) ingested **once** and published **read-only** to many tenants by audience — without each tenant re-processing it, and without letting them mutate it.

**The read/write asymmetry (why read-only is free):** these stores are owned by a dedicated **"Matrx Library" org** (`system_orgs.key='library'`). WRITE/manage is gated by data-store ownership (`can_access_data_store` = creator/org-member) — no tenant qualifies, so every mutation 403s with no new code. READ is granted by a single OR-branch in the RAG visibility clause (`matrx_rag.search._build_visibility_clause`) admitting a chunk when its source's data_store has a grant reaching the caller.

**Three-tier audience = one primitive** (`rag.data_store_grants`): `global` | `industry` (→ orgs in that [industry](../industries/FEATURE.md)) | `organization` (a direct grant or self-service opt-in on a `discoverable` store). Grants attach to the container, so they cover every member source's chunks.

**Open path = platform reachability (not FE exceptions):** industry/org/global grants on a library `data_store` cascade through `platform.associations` → `platform.reachability` → `iam.has_access` / `public.has_access_as`:

| Edge                                        | Conveys |
| ------------------------------------------- | ------- |
| `file → data_store` (`library_member`)      | viewer  |
| `note → data_store` (`library_member`)      | viewer  |
| `transcript → data_store` (`library_member`)| viewer  |
| `code_file → data_store` (`library_member`) | viewer  |
| `processed_document → file` (`source_file`) | viewer  |
| `file → file` (`page_image`)                | viewer  |

**Member kinds are registry-driven (P4, 2026-07-23):** `rag.sync_data_store_member_association` writes a `library_member` edge for ANY `data_store_members.source_kind` whose entity token has an ACTIVE `<token> → data_store` rule in `platform.association_types` (`rag.member_source_entity_token` maps `cld_file → file`; every other kind must BE a token). Registering the rule is the whole act of making a new kind cascade; a member with no rule is a LOUD trigger WARNING, never a silent skip. **Deliberately NOT library-shareable today:** `project` and `task` (containers with their own cascades — conveying viewer on a whole project/task through a store grant is a conveyance decision reserved for Arman) and `research` / `scraped` (no `platform.entity_types` token; registering token + conveyance is the same reserved decision). To enable one: register the token (if missing) + the `association_types` rule, then re-run the backfill in `migrations/library_cascade_generalize_member_kinds.sql` — nothing else.

**Orphan members (D-F rule):** deleting a file (soft or hard) soft-deletes its live `data_store_members` rows (trigger `trg_files_soft_delete_store_members`, loud WARNING, provenance marker in `notes`); the member trigger then removes the edge. Restoring a file does NOT auto-restore membership — re-adding is an explicit curator act.

**Archived docs (D-D rule):** `can_read_processed_document` lets the OWNER and CURATORS keep read on an archived doc (they are the ones who unarchive it); org members, grant audiences, and kernel viewers lose read while archived — symmetric with `can_curate_library_document`, which stays archived-blind.

Service-role file download asks `has_access_as(p_user=…)` (JWT `has_access` is NULL under the service key). Docproc SELECT RLS stays additive via `can_read_processed_document`. **Viewer only** — grants never confer editor/admin. Curator library list (`rag_library_list`) stays owner/curator-only; grant browse is catalog/search.

Migrations: [`library_store_file_reachability_cascade.sql`](../../migrations/library_store_file_reachability_cascade.sql) + [`library_reachability_cascade_hardening.sql`](../../migrations/library_reachability_cascade_hardening.sql).

**Ingest profile:** library content is ingested with **NER + scope-association OFF** (`ingest_source(run_ner=False)`) — cross-tenant content has no tenant scopes/entities to extract, and the agent fan-out would be wasted cost.

**FE surfaces:**

- `hooks/useDataStoreGrants.ts` — publish / revoke / list grants **direct to Supabase** (`rag.library_grant_publish` / `_revoke`, `rag.fn_list_data_store_grants`). The old `/rag/data-stores/{id}/grants` HTTP path is dead for the FE (migration `0162`).
- `components/data-stores/DataStorePublishPanel.tsx` — audience-tab publish dialog (mirrors `ShareModal`; super-admin only). Opened from `DataStoresPage` for `kind:"library"` stores.
- `DataStoresPage` / `RichMemberTable` — a `'granted'` store shows a "Shared library · read-only" badge and hides Edit / Delete / Add-member / remove / drag-drop. `access` / `readOnly` ride on the data-store list/detail responses.
- Tenant catalog (browse `discoverable` + subscribe) — `LibraryCatalogPane` + `hooks/useLibraryCatalog.ts`, mounted as a **section on `/rag`** (`RagHomePage`), reading `rag.fn_list_library_catalog` direct + `rag.library_subscribe`/`_unsubscribe`. **This pane is currently the ONLY way a grant reader can find granted content** — `files_listing_owner_grant_only.sql` (2026-07-20) deliberately bars reachability-conveyed rows from every file tree, search, and picker. A dedicated `/rag/library-catalog` route is still unbuilt.

**Backend (aidream):** the ACL branch + `can_read_data_store` + grants-aware `list_data_stores`/`get_data_store` + the `run_ner` profile + `services/rag/library_grants.py`. DB: migrations `0116`–`0119`, then `0162`/`0163` (the `rag.fn_*` direct-read family that replaced the FE's HTTP hops). The `/rag/data-stores/{id}/grants` + `/rag/library-catalog` HTTP endpoints still exist for non-Supabase clients (extension/external), but **the FE no longer calls them** — do not "restore" them into FE code.

**Guardrail:** library publishing is the ownership-asymmetry model — READ via `rag.data_store_grants`, WRITE via store ownership. `data_store` is in `shareable_resource_registry` only so Relationship Manager / reachability recognize it as a conveying container (`rls_uses_has_permission=false`, `is_link_shareable=false`). Do **NOT** wire `ShareButton` / `useSharing` / `iam.permissions` for data stores — use `DataStorePublishPanel`. See [`features/sharing/FEATURE.md`](../sharing/FEATURE.md).

---

## Change log

- **2026-08-10 (data stores are agent-WRITABLE)** — `/rag/data-stores` gained the write half of its surface (`matrx-user/rag-data-stores`, 3 ask-policy targets; see [`features/surfaces/FEATURE.md`](../surfaces/FEATURE.md) and the `surface-write-targets` skill). `CreateStoreInline` registers `new_store_draft` — one composite object of name / kind / description staged into the create form, opening the form first when it is collapsed, because a closed form owns no state the user could review. `StoreDetailPanel` registers `store_name` and `store_description` as entity targets routed through the canonical `useDataStoreDetail.updateStore`; both refuse on a `granted` shared library, mirroring the ownership-asymmetry the server already enforces. `kind` is writable only at CREATION: `updateStore` deliberately does not patch it, so an existing store has no canonical path for it — and note that the Edit form's `kind` select has always been silently dropped by that same rule (pre-existing, untouched here). Nothing that curates or publishes is writable: adding/uploading a source, removing a member, deleting a store, and audience grants stay human. Handlers validate against `DATA_STORE_KINDS` from [`types/data-stores-ext.ts`](types/data-stores-ext.ts) — the same constant the `<select>` renders — and throw on a bad shape so the agent reads a real error. Live-verified end-to-end with a Badass Agent run. **Follow-up (same day):** a saved edit now refreshes the LEFT column too (`onStoreUpdated` → `list.refresh()`, wired into both entity handlers and the Edit form's Save). `useDataStoreDetail.updateStore` only refreshes the detail hook, so the list — fetched by a separate `useDataStores` instance — kept rendering the pre-save name and description until a reload. Pre-existing for the Edit form; the `store_name` target made it visible, because an agent rename left the row it renamed looking untouched.
- **2026-08-09 (mobile: data stores + hit card)** — `/rag/data-stores` is on `MobilePanelShell`: the raw `w-80` + `flex-1` split left a ~55px detail sliver on a 375px viewport, so below `md` the store list is a "Stores" bottom drawer and the detail panel owns the screen. Selecting/creating a store only writes `?store_id`, which is not a route change, so `StoreListRow` / `CreateStoreInline` dismiss the drawer with `useMobilePanelClose()`. `RagHitCard`'s expanded header now wraps below `sm` with the score + reference-icon strip + actions on their own right-aligned row — the fixed-width trailing cluster used to squeeze the title/snippet column to one character per line on a phone. New fixture page `/demos/rag-hit-card` renders both card variants without a live backend; use it for hit-card layout work.
- **2026-08-08 (`rag.citation` channel deleted — one citation UI)** — The per-hit `rag.citation` / `rag.citation.summary` stream events were a parallel citation channel with ZERO consumers anywhere: `ragSearch()`'s `onCitation` callback was never wired into any UI (only the terminal `rag.search.result` is read), and the aidream cross-doc `tag` variant streamed to an endpoint no FE code calls (that flow lives in the `knowledge_compare` agent shortcut). Superseded by the unified citation system — `document_search`/`knowledge_search` tool results emit citable `search_result` blocks, so RAG answer citations are provider-native (`citation` stream event + `TextPart.citations` + MessageSourcesRow). Deleted on both sides: FE `RagSearchCitationEvent` + `onCitation` ([api/search.ts](api/search.ts)); aidream emitters (`matrx_rag/search.py`, `library_streams.py` cross-doc), both `RagCitation` models + `RagCitationSummary`, and the schema-snapshot entries. `citationHrefFor` and `RagSearchHit` (REST hit shapes) are untouched. Ruling recorded in [`docs/handoffs/citations-system.md`](../../docs/handoffs/citations-system.md).
- **2026-07-24 (document API contract path correction)** — Updated the unified-document typed client from the legacy compatibility namespace `/api/document/*` to the canonical OpenAPI paths `/document/*` after aidream removed its invalid internal `/api` router mount. Response types continue to derive directly from generated Pydantic/OpenAPI schemas with no casts or duplicate wire declarations.
- **2026-07-23 (P4 — cascade generalization + guardrails)** — Member→edge sync is now registry-driven (`migrations/library_cascade_generalize_member_kinds.sql`): `note`/`transcript`/`code_file` members convey viewer (proven live end-to-end with a disposable store: entitled reader RLS row 1 / editor false / control 0, edges torn down on delete); `project`/`task`/`research`/`scraped` documented above as deliberately not-shareable (loud trigger WARNING, never silent). `files.*` babies (`analysis`, `analysis_result`, `pages`, `page_annotations`, `entities`, `file_rag_jobs`, `structure` — RLS newly enabled) got additive grant-aware SELECT; the four page-extraction policies collapsed onto one `public.can_read_extraction_job(job_id)`; `docproc.derive_runs`' over-wide FOR ALL policy split so grant readers can no longer write (`migrations/library_grant_read_file_babies.sql`). D-D closed (`library_archived_read_curate_symmetry.sql`: owner/curator keep read on archived, audiences lose it). D-F closed (`library_member_orphan_hygiene.sql`: 2 orphans soft-deleted with markers + files.files trigger enforcement). D-H closed (`web_crawl_artifact_live_reconcile.sql`: live `'personal'` body restated, 3 dead-on-disk functions dropped, supersession map recorded). D-B closed in aidream (spend gates on `page_extraction.py` / `retry.py` / `runs_db.py` rewritten onto kernel predicates per Decision 4 — owner/curator/super-admin, ANY-admin bypass removed; ungated cancel endpoint also fixed; **aidream deploy pending**). New guards: `pnpm check:access-matrix` (42-probe grid, GREEN) + `pnpm check:access-drift` (4 guards; surfaced 38 pre-existing dead policies → FOUND_DEFECTS) — see `scripts/access-matrix/FEATURE.md`.
- **2026-07-23 (P2 — Shared Knowledge admin console shipped)** — New super-admin issuance cockpit at `/administration/shared-knowledge` (`features/admin/shared-knowledge/`: server loader over the org directory + EVERY `kind='library'` store incl. inactive/undiscoverable, client tabs Industries / Stores & grants / Ingest / Access explorer). `DataStorePublishPanel` gained the missing **organization** audience tab (`organizationOptions` prop for the admin directory, `getUserOrganizations` fallback; industries now refetched on every open so a just-created industry is publishable) + `onChanged`. `useDataStoreGrants` exports one-shot `fetchDataStoreGrants` (explorer batches it per store); its gate is Decision 2 (super-admin OR store owner) — verified refused for a non-admin at DB level. New `api/library-ingest.ts` for P1's `POST /rag/library/stores/{id}/ingest` (501 → labeled "pipeline not yet live", never swallowed); ingest UI picks/uploads ONLY through the canonical `openFilePicker`/`useFileUpload`. `/rag/admin` FeatureAdminMap added (drift-clean, includes P3's `/rag/library-catalog`). All grant mutations remain on `rag.library_grant_publish`/`_revoke` — no new mutation path.
- **2026-07-23 (P3 — Discovery & opt-in shipped)** — `/rag/library-catalog` is now a real list-view route (`components/library-catalog/LibraryCatalogPage.tsx` + `EntitlementChip`; hub nav gained "Catalog"): search/filter, per-caller entitlement chip (Subscribed / via `<industry>` / Available to everyone / Not entitled, from `fn_list_library_catalog`'s settled `entitled_via` columns — `useLibraryCatalog` typed for them + optional org override), detail view with "Why you have access" provenance, subscribe/unsubscribe, and a read-only member table with preview links; the `/rag` pane is a 4-item teaser linking there. **Provenance everywhere:** new `hooks/useLibraryProvenance.ts` (ONE fetch layer over `library_grant_provenance`/`_batch` + a `data_store_members` file→store resolver) feeds "Shared library · via …" chips on `RagHitCard` (new typed `RagHitView.libraryProvenance`, batch-resolved by RagSearchHits / the search lab / knowledge-search chat cards — never per-card), the Source Inspector toolbar, `LibraryPreviewPage`'s status band (where the `/files/f/[id]` grant-reader redirect lands), and the DataStores granted badge (now "Shared library · via <industry>"). **Entitled empty state:** `/rag` home leads with "Shared libraries you can read right now" when the caller has zero personal documents but entitled catalog rows. **D-E closed:** rag catalog/store list hooks documented in `scripts/access-guards/allowlist.json` (bareRlsList) — catalog is deliberately a global discoverable list; scoping lives inside the SECURITY DEFINER RPCs. Verified live (entitled admin browser pass + DB-level control probes: control sees the row with a null chip, zero provenance, zero members). Discovered (pre-existing, not fixed here): `rag.fn_data_store_members_rich` raises "data store not found" for grant readers, so the DataStores rich member table errors on granted stores.
- **2026-07-23 (Shared Knowledge re-audit + container-visibility fix)** — Full re-verification of the access cascade after a 13-day pause and ~890 commits: the spine is **intact and prod-proven** (entitled reader gets file bytes 200/3.86 MB, pages, chunks, extractions; non-entitled denied; editor never conveyed), and it survived the `has_access_for` kernel refactor (aidream `0159`/`0160` — `iam.has_access`/`has_access_as` are now thin wrappers over ONE resolver, killing the twin-divergence risk), the `private`→`personal` rename, Wave A soft-delete, and the `0162`/`0163` direct-Supabase conversion. **Fixed:** `iam.has_access('data_store',…,'viewer')` returned true while `rag.data_stores` RLS returned zero rows — the container was invisible though everything inside it was readable; additive SELECT on `data_stores` + `data_store_members` delegating to the judge (`migrations/data_stores_grant_reader_select.sql`, applied + ledgered, verified 0→1 entitled / 0 control / editor false). **Doc truth-up:** grants + catalog hooks are direct-to-Supabase, not HTTP. **Newly recorded risk:** `files_listing_owner_grant_only.sql` (07-20) and `list_library_documents`' owner/curator scope mean granted content appears in **no** file tree, search, picker, or library list — the `/rag` catalog pane is the only discovery path until the catalog route ships. **Security (D86, same-day):** the `public.industry_*` RPC family let a caller-supplied `p_actor` beat the JWT (`COALESCE(p_actor, auth.uid())`) and all five were anon-EXECUTE-granted — an anonymous caller passing a known super-admin uuid could perform super-admin writes, including `industry_curator_grant` → `iam.industry_curators` → curate (write) on library documents. Fixed to session-identity-wins + anon revoked (`migrations/industry_rpc_actor_spoof_fix.sql`); no prior exploitation in the audit log. Remaining work + the open decisions: [`docs/handoffs/shared-knowledge-access.md`](../../docs/handoffs/shared-knowledge-access.md).
- **2026-07-18 (knowledge tool family)** — The backend consolidated twelve RAG/document tools into six (`knowledge_search` · `knowledge_browse` · `knowledge_compare` · `document_search` · `document_content` · `verify`). Frontend follow-through: `RagSearchExperience`'s hardcoded `RAG_AGENT_TOOL_IDS` list dropped the four tools that became soft-deleted `tool.definition` rows (`rag_search_data_store`, `rag_list_data_stores`, `rag_get_data_store`, `rag_get_chunk`) — ids are stable across a RENAME but not across an ABSORPTION, so those four were arming dead rows; the four survivors kept their ids under new names. Agent-facing hint strings that name tools in prose (`features/scopes/registry/entityContentAdapters.ts`, `features/war-room/service/warRoomAgentContext.ts`, `war-room-master-tools/tools/tool-defs.ts`) now teach `knowledge_search`. Tool-call renderers moved to `renderers/knowledge-search/` + `renderers/knowledge-browse/` (see that feature's `FEATURE.md`).

- **2026-07-17 (Wave A soft-delete + library Trash)** — Library deletion is now a real trash lifecycle (spec: `docs/handoffs/wave-a-softdelete-spec.md`). The three delete RPCs (`rag.fn_delete_library_document` / `fn_bulk_delete_library_documents` / `fn_delete_library_document_and_source`) soft-delete (doc + `rag.kg_chunks.deleted_at` + doc-keyed memberships; embeddings survive); canonical extracts (initial_extract/legacy_import) are blocked per-doc (delete via the file); "Delete file" trashes the WHOLE family atomically via the `files.files` cascade trigger and requires file ownership. New owner-only Trash: `LibraryTrashSheet` (Trash button in the library header) over `rag.fn_list_library_trash` / `fn_restore_library_document` / `fn_purge_library_document` — purge is the ONLY hard delete and only works from the trash (DB BEFORE DELETE guard enforces it). Family rows restore via `restore_file`. `rag_library_summary_totals` excludes trashed docs/chunks. Delete dialogs/toasts say trash/restorable and surface `skipped_canonical`.
- **2026-07-16 (uncaged physical-PDF search preview)** — The expanded `/rag/search` physical-page reference no longer uses the generic 50/50 retrieved-text/reference comparison or its nested fixed-height card. When **Physical PDF page(s)** is selected, the page viewer is a direct, full-width evidence canvas (`min(72dvh, 64rem)`, 34rem minimum) beneath the compact reference selector, retaining the viewer's own zoom and source-page controls. Other reference representations (clean/raw text, images, tables, verification, and derivations) retain the existing comparison layout.
- **2026-07-15 (source-grounded Review & Repair workspace)** — Page-anchored cloud-file hits on `/rag/search` now expose a dedicated wrench action in the canonical result header. It opens a focused three-column workspace: retrieved agent-visible text, a physical-PDF packet containing the hit span plus one page before/after, and a targeted repair launcher. Desktop uses the v4 resizable-panel contract (`27% / 46% / 27%`, stable IDs, 8% side minimums, 25% PDF minimum); mobile swaps to one stacked, single-scroll flow. The scope sidebar is removed while reviewing so the PDF remains legible. The selected-page derivative viewer is now shared by normal result references and repair mode, preserves explicit source-page ↔ derivative-page labeling, and keeps identical file/page requests promise-cached. The repair launcher only accepts agents with a real Document variable, wires native `pdf_page` attachments plus page/filename/instruction inputs, creates one durable text-output extraction dataset anchored to the review packet, streams its output, and links directly to `/knowledge/extractions/{jobId}` for editing and management. Completed extraction output follows the existing automatic RAG indexing contract; a separate approval-gated supersession lifecycle is not implied by this UI.
- **2026-07-15 (colored source glyphs + selectable content export)** — Canonical RAG result headers now reuse the Files feature's extension-aware `FileIcon` (PDF red, image emerald, media/code/data colors) at a larger 22px two-row scale; non-file sources retain their established semantic glyph colors (including amber notes). Retrieved passages and every page-reference preview now expose format-aware one-click copy (plain text/Markdown, TSV tables, pretty JSON extractions, and honest file/image references). The branded Copy-for-AI action now opens `ragAiCopyWindow`, a non-blocking desktop WindowPanel / mobile drawer based on the aidream admin composer's pure-builder pattern: source/file/document/chunk/parent/field/page identifiers are always included, while retrieved/clean/raw text, physical document refs, tables, images + vision analysis, custom/agent content, derived passages, verification, ranking, and metadata are independently selectable. The live XML-ish preview reports chars/bytes/estimated tokens and offers honest per-string/per-list caps; quick presets cover identifiers-only, essentials, and everything currently available. Pure payload coverage lives in `ragAiCopy.test.ts`.
- **2026-07-15 (resume-first knowledge assets + server-enforced rebuild handshake)** — Knowledge Asset cards now resolve completion from persisted resume keys plus the latest `derive_runs` row: interrupted/partial output shows **Resume**, including synthetic Q&A where many chunks can belong to one completed section; live server runs hydrate after reload; and the bulk CTA becomes **Resume remaining**, skipping representations already complete. Resume calls remain `reset=false`, so figure pages/sections already persisted are skipped. Resume-state reads now fail closed instead of silently treating a DB error as “nothing done” and re-spending from zero. Rebuild is no longer a one-click query flag: the UI first POSTs `derive/{kind}/rebuild-preview`, the server returns exact completed/remaining/output counts, full-vs-resume estimates, and a five-minute capability token; the destructive dialog surfaces those facts; only then does request two send `reset=true` with that token. The backend rejects missing/expired/wrong-user/wrong-doc/wrong-kind tokens and confirmations whose output snapshot changed after inspection.
- **2026-07-15 (physical-page truth previews + agent validation)** — Expanded PDF RAG hits now default their right-hand reference pane to a lazy selected-page PDF: the frontend sends the hit's canonical one-or-more `page_numbers` to `POST /files/{file_id}/pdf-pages`, then renders the returned derivative file instead of loading the full source PDF. Identical file/page requests share an in-memory promise and the backend's revision-aware derivative cache. The full source remains a separate explicit action. The server tool-merge funnel automatically adds the canonical `document_content` companion whenever an agent has a RAG search tool, so agents reason from processed RAG text first and selectively attach exact physical pages for OCR/scan/high-stakes validation.
- **2026-07-14 (knowledge asset resume UX)** — Partial costly derivations (`section_summary`, `synthetic_qa`, `page_image_caption`) now show **Continue** alongside **Rebuild**. Continue POSTs without `reset=true` (server skips already-persisted sections); Rebuild still clears and re-spends on everything.
- **2026-07-15 (search-result identity and representation contract)** — Canonical RAG hit cards expose a compact two-line source summary (human filename, shortened source UUID, full-value hover, full-ID copy, configurable Copy for AI); the distinct result/chunk UUID and copy action live at the bottom of the expanded card. Copy for AI always emits XML-ish source/retrieval identity and can add user-selected content through the composer; raw metadata is recursively stripped of content-bearing fields before it becomes an optional provenance section. Search/diagnostic/tool hits sharing a `(source_kind, source_id)` hydrate one canonical sibling filename instead of falling back to a UUID fragment; UUID-like values are never accepted as display names. Table/derived hits with `first_page`/`last_page` but no `page_number` retain their page provenance and exact-page open link. The header's non-control surface toggles the disclosure; UUID/copy actions, window open, new-tab open, the chevron, and the fixed representation strip remain independent. The strip always reserves the same five positions — **Document**, **Clean text**, **Image**, **Table**, **Custom content** — with a restrained primary tint when available and muted gray when absent. Document opens the exact-page source window; the other available icons expand the result and select that real page-reference preview. “Custom” is reserved for user/agent/unique derived content and intentionally excludes coarse/fine/normal passages, raw text, chunks, and verification. Result type is separately humanized (`Table`, `Coarse passage`, etc.) from source type (`File`).
- **2026-07-15 (library org-identity hardening)** — `rag.fn_list_library_catalog(p_organization_id)` and `public.rag_source_has_library_grant(..., p_org_id)` now reject caller-selected organizations the signed-in user cannot access; service-role workflows retain cross-org operation. This closes the authenticated cross-org subscription/grant oracle while preserving the no-org catalog view.
- **2026-07-14 (library detail interaction contract)** — `/rag/library` document details now keep all text selectable across the shared resizable-panel shell. The header exposes canonical human Copy + XML-ish Copy for AI for the full document payload; page text and segment bodies expose the same pair at granular scope. Document/source/parent/data-store/segment UUIDs use `MatrxUuidCell` (short head/tail, full hover value, adjacent full-ID copy). Source-file IDs open the canonical File Preview window, retain a real `/files/f/<id>` new-tab link, and expose the shared file right-click menu.
- **2026-07-14 (RAG PDF results open at the cited page)** — The `/rag/search` expanded result shelf's **PDF page** action now resolves either half of the `file_id ↔ processed_document_id` bridge and opens the canonical File Preview window with `{ fileId, pageNumber }`. The PDF renderer lands on the hit's 1-based page and remains navigable; unresolved bridge identities retain the existing exact-page Source Inspector fallback.
- **2026-07-14 (search result comparison workspace)** — `/rag/search` highlighting now recognizes adjacent meaningful query terms as a chain even when their order differs from the query (`pain guidelines` is a two-word chain for a query containing both terms); exact multi-word phrases still win first and standalone stop words remain excluded. Search results gained synchronized **Expand all / Collapse all** controls beneath the search field. An expanded document hit is now a permanent 50/50 comparison: retrieved chunk on the left, one page reference preview on the right, with every available view promoted into the reference header. The default preview and header order share the exported `REFERENCE_PREVIEW_PRIORITY` list (`table → rendered page/figure → image analysis → fine passages → clean text fallback`), so changing the preference is a one-list edit.
- **2026-07-14 (page-scoped references on search hits)** — Expanded PDF results on `/rag/search` now mount a lazy `RagPageReferences` shelf anchored by the hit's `file_id ↔ processed_document_id ↔ page_number` bridge. The shelf exposes raw/clean page text, verification state, rendered page image, page-filtered table rows, synthetic Q&A, image analysis, section summaries, coarse/fine derived passages, and custom page-extraction rows. The original PDF page is explicit **Open on demand**: no PDF bytes are requested merely by expanding a result; opening it uses the existing exact-page progressive/range viewer. Derivative `/chunks` reads now pass the backend's existing `page_number` filter, and collapsed results do not mount or fetch their page resources.
- **2026-07-14 (collapsible, query-aware search results)** — `/rag/search` expanded hit cards are now independent disclosures: the first result opens by default, the rest start collapsed, and a dedicated disclosure control toggles the body while identity/source controls remain independent. Result snippets now highlight longest exact query-word sequences first on an amber→green→cyan/blue scale; shorter overlaps cannot replace stronger phrase matches, standalone stop words are ignored, and stop words remain eligible inside meaningful multi-word phrases. Search term coverage now uses the same Unicode-aware stop-word filtering.
- **2026-07-11 (page_image association direction)** — Retargeted page-image edges from the dead `file→processed_document` rule (non-shareable container) to `file→file` (page render → source PDF, role=`page_image`). `processed_document→file` (`source_file`) remains the only file↔processed_document association. Migration: `page_image_assoc_retarget_to_source_file.sql`.
- **2026-07-10 (Shared Knowledge open path via reachability)** — `data_store_grants` now cascade through the platform judge: `file→data_store`, `processed_document→file`, page-image `file→file` (all Conveys **viewer**); `iam.has_access` / `has_access_as` honor store grants; `entity_row_access_attrs` maps `owner_id` tables (processed docs) so the reachability loop is not aborted; sync triggers drop OLD edges on identity UPDATE; `has_access_as` is **service_role-only**. Aidream: `PermissionsManager` + `/api/document` ACL call the same grant/judge path. Source Inspector PDF + page text + page images work for industry-grant readers without FE ACL exceptions. Migrations applied + ledgered. **Live-proven on prod** (grant user: file download 200, `/rag/library/{doc}/page/1` 200, direct RLS page read returns rows; DB judge: viewer=true / editor=false / stranger=false). Same-day follow-up: `page_extraction_library_grant_read.sql` (applied + ledgered) — extraction jobs/runs/page-runs/results get additive grant-aware SELECT (via `iam.has_access('file'…)` OR `can_read_processed_document`), so the Source Inspector Extractions tab works for grant readers too.
- **2026-07-09 (ONE canonical RAG hit card, everywhere)** — Killed the three divergent RAG-hit renderers (the beautiful chat `RagSourceCard`, the plainer `/rag/search` `RichHitCard`, the minimal `RagSearchHits` rows) in favour of a single `components/hit-card/RagHitCard` with two variants that share one beautiful core (glossy `kindGlyph` · 2-line title · color-coded `scoreTier` badge · a shared `HitBreakdown` = relevance bar + rank chips + entity Mentions + "entity only" warning): **compact** (chat + omnibox — the breakdown + full chunk + open actions live in a hover peek) and **expanded** (the search lab — full chunk inline + always-visible breakdown + chunk_id copy). Every hit type adapts to one `RagHitView` (`adapters.ts` for `RagSearchHit`/`DiagnoseHit`; the tool's `NormalizedHit` at its callsite). `scoreTier` + `kindGlyph` moved here as the canonical home (tool-viz re-imports). `/rag/search`'s old flat gray `ScoreBar`s + monospace `CLD_FILE` header + `fileNameOf`/`pageNumberOf` helpers deleted. `tsc` clean; **live-verified** both variants via headless screenshots — chat peek (Strong-match bar, `vector #2 · lexical #5 · rerank 0.82`, Mentions, Inspect/Open) and `/rag/search` (glyph header, colored rank chips, chunk_id).
- **2026-07-06 (kill the Python-as-DB-proxy hop on `/rag/library`)** — The library visibility surface was routing React → Python → Supabase → Python → React for pure aggregate reads (`GET /rag/library`, `GET /rag/library/summary/totals`). Per CLAUDE.md, a direct call that returns the same rows is the canonical path — Python is only for processing (LLM/OCR) or S3 file bytes/signing, never a plain DB read. Two `SECURITY DEFINER` RPCs now replicate the FastAPI queries **1:1** ([`migrations/rag_library_direct_read_rpcs.sql`](../../migrations/rag_library_direct_read_rpcs.sql), applied + ledgered + live-verified): `public.rag_library_list(p_limit,p_offset,p_search,p_status_filter,p_source_kind)` (correlated-subquery counts + the owner/curator visibility clause + status-derive CASE, returns `{documents,total,limit,offset}` jsonb) and `public.rag_library_summary_totals(p_organization_id)` (top-level-doc rollup + legacy voyage_code_3 count + data-store count). Both keyed on `auth.uid()`, granted to `authenticated` only. Live in `public` (always PostgREST-exposed) + SECURITY DEFINER so schema exposure of `docproc`/`rag` is irrelevant, and the embedded visibility clause IS the RLS-parity the 2026-07-01 note flagged as a blocker. FE: [`hooks/useLibrary.ts`](hooks/useLibrary.ts) `useLibrary` + `useLibrarySummary` now call `supabase.rpc(...)` (summary passes `selectEffectiveOrganizationId`); [`hooks/useProcessingRunner.ts`](hooks/useProcessingRunner.ts) source-id lookup swapped off a broken relative `fetch("/rag/library?…")` (hit the Next origin, never the backend) to the same RPC. `useLibraryDoc` (doc-detail page/chunk previews) still hits the HTTP endpoint — a heavier read left for a follow-up. `tsc` clean; RPCs verified live against prod data.
- **2026-07-16 (viewer host-driven search + assets deep link)** — `LibraryPreviewPage` new props: `externalSearch: {query, nonce}` (host-submitted in-document search — runs the same flow as the internal bar incl. jump-to-first-matched-page; empty query clears), `hideSearchBar` (hosts that surface search in their own chrome, e.g. the chat drawer's PDF pill title), `initialAssetsOpen` + `?assets=1` on `/rag/library/[id]/preview` (deep link from the file menu's new "Knowledge assets" entry). The PDF extractor studio reuses `useDocumentSearch` + `DocumentSearchSummary`/`DocumentSearchResultsList` for its Segments-pane results view — one search engine, three surfaces.
- **2026-07-16 (viewer segments rail chrome)** — `LibraryPreviewPage` middle rail: replaced the ugly `TabsList` ("Segments (this page)") with a full-width 2-way split switch — **Segments (P.N)** | **Results (n)** — and locked all three column headers to a shared `h-9` band so Pages / Segments / page-text headers align. `PageContentHeader` height matched; fixed latent `SEGMENT_ORDER` ReferenceError.
- **2026-07-16 (viewer search bar → ProInput)** — `DocumentSearchBar` uses full canonical `ProInput`: search `startIcon`, clear, voice mic, and built-in send submit (`onSubmit` + `submitOnEnter` + `isSubmitting`). Dropped the external fat Search button and the wrapping `<form>` so mic tap buttons can't accidentally submit.
- **2026-07-06 (stream-everything: `/rag/search` streams)** — Backend deleted the blocking `SearchResponseOut` JSON body; `POST /rag/search` now returns the canonical NDJSON stream (`rag.citation` per hit as retrieval fuses → `rag.citation.summary` → terminal `rag.search.result` = the old body). `ragSearch()` ([api/search.ts](api/search.ts)) drains the stream via the shared `postNdjson` (`lib/python-client.ts`) and resolves with the terminal payload — every consumer (useRagSearch, RagSearchExperience) unchanged; new optional `onCitation` callback delivers hits live (not yet wired into any UI). RAG events ride `data.kind` (dotted), NOT the typed `data.type` registry — same convention as ingest. Ingest unaffected: both FE callers already consume the retained `/rag/ingest/stream` alias (the bare `/rag/ingest` is canonical for NEW callers only). `/rag/search-lab/*` and `/rag/library/*/test-search` are NOT converted.
- **2026-07-02 (Shared-Knowledge open-path ACL fix — search-vs-open divergence)** — RAG search surfaced content published via `rag.data_store_grants` (Shared Knowledge Resources: global/org/industry audiences), but the **read/open** surfaces ignored those grants, so an entitled reader could search granted content yet every citation 404'd. Root cause: the library read endpoints (`GET /rag/library/{id}` + `/page`, `/chunks`, `/test-search` in aidream `rag.py`) gated on `owner_id OR can_curate_library_document` — `can_curate` is a **write** check (super-admin OR industry-_curator_), with no read-via-grant path; and `cld_file` citations route to `/files/f/<id>`, whose ACL has no data-store-grant branch either. Fix: (1) those 4 read endpoints now use `owner_id OR public.can_read_processed_document(...)` — the canonical read predicate (owner/org/curate/data-store-grant) that already existed (do NOT create a new one); (2) new `SECURITY DEFINER` resolver `public.readable_processed_doc_for_file(p_file)` ([`migrations/readable_processed_doc_for_file.sql`](../../migrations/readable_processed_doc_for_file.sql), applied live) returns the file's `canonical_processed_document_id` **iff** the caller can read it; (3) [`app/(core)/files/f/[fileId]/page.tsx`](<../../app/(core)/files/f/[fileId]/page.tsx>) redirects a denied-but-entitled reader to the read-only `/rag/viewer/<doc>` instead of `notFound()`. Also hardened a cross-tenant hole in `matrx_rag/search.py::_build_visibility_clause`: the data-store-grant branch trusted the caller-supplied active org (`$1`) without a membership check — now `EXISTS(iam.organization_member …)`-gated (om3/om4). **NOT a data leak** — the industry grant was intentional; the open path just didn't honor it. **aidream `rag.py` + `search.py` deploy-gated**; DB functions live + verified. Latent follow-up: `rag.kg_chunks.organization_id` can go stale vs the source file's org (search filters chunk-org, open filters file-org) — audit + backfill tracked in feedback.
- **2026-07-02 (model upgrade: voyage-4-large + rerank-v4.0-pro; filtered-ANN recall fix)** — Researched-best models, pre-rollout timing (13k chunks ≈ 4.5M tokens, inside Voyage's free tier). aidream `582c29782` + FE `522ac376e`: prose embeddings `openai:text-embedding-3-small` → **`voyage:voyage-4-large`** (new `rag.embeddings_voyage_4_large_1024`, migration 0136 applied + ledgered, RLS parity; **all 13,004 live chunks backfilled, 0 missing**, idempotent `scripts/backfill_voyage4_embeddings.py`); reranker → **`rerank-v4.0-pro`**; fixed latent `_embed_voyage` model hardcode. **Filtered-ANN recall collapse fixed** (owner of 7,221 chunks got `vector_candidates=2` → 100): `SET LOCAL hnsw.iterative_scan='relaxed_order'` per KNN via the new `QueryBuilder.compile()` matrx-orm primitive — the only GUC carrier that survives Supavisor transaction pooling (ALTER DATABASE/ROLE permission-blocked, startup-packet dropped; both probed live). Full new stack verified live locally against prod data. **Deploy aidream + re-run the backfill post-deploy** — see [`docs/handoffs/SEARCH_SYSTEM_HANDOFF.md`](../../docs/handoffs/SEARCH_SYSTEM_HANDOFF.md) pending list.
- **2026-07-01 (Cohere confidence floor + handoff)** — Cohere `rerank-v3.5` confirmed in active use (retrieve-then-rerank on OpenAI embeddings — two models by design, not a conflict). aidream `c76f6b105`: `RERANK_CONFIDENCE_FLOOR` (an all-low rerank window keeps the fused RRF order instead of letting noise override it) + `rerank_status` (`applied|low_confidence|failed|off`) surfaced end-to-end (SearchResponse → HTTP + stream → FE type); the results line shows "rerank skipped: low confidence" and the rerank toggle names Cohere again. **DB fact update: the `rag` schema IS PostgREST-exposed now** (supersedes the 2026-06-10 server-only invariant); direct-read migration is possible but gated on RLS parity with the Python visibility clause. **Full state, done-list, pending-list: [`docs/handoffs/SEARCH_SYSTEM_HANDOFF.md`](../../docs/handoffs/SEARCH_SYSTEM_HANDOFF.md).**
- **2026-07-01 (citations open in windows, everywhere)** — Every citation now opens in a **non-blocking window** instead of forcing a page navigation, unified behind one primitive: `components/source-inspector/useOpenCitation.ts` routes by `source_kind` — `cld_file`/`library_doc` → **Source Inspector**, `note` → **Notes window** (`initialNoteId`), `transcript` → **Transcript Studio** (`activeSessionId`), `scraped` → **Scraper window** (`url`); kinds without a window (e.g. `code_file`, which needs file objects a citation lacks) fall back to a new tab. `shouldOpenInNewTab` preserves native new-tab on modifier/middle click. Wired into every citation surface without losing prior behaviour: the `rag_search` tool cards (`RagSourceCard` — all kinds now, not just PDFs), the shared `RagSearchHits` (default click → window; `onHitClick` still wins; `<a href>` kept for new-tab), the `/rag/search` page (`RagSearchExperience` rows), and the KG citation links (`KgInspector` mentions, `KgGraphSidePanel` evidence). `tsc` clean + eslint clean; **live-verified** each route (note → Notes window, scraped → Scraper window with URL pre-filled, cld_file → Source Inspector on page 393 with the matched chunk ringed).
- **2026-06-27 (search quality + legibility + hardening)** — Rescue pass on `/rag/search` ranking and trust, spanning aidream + frontend. **Ranking (aidream `matrx-rag/search.py`):** the entity-recall lane no longer lets a 3-word title outrank real content — a 40-char content gate + `count DESC, max(length) DESC` tiebreak; MMR dedup is **granularity-aware** (same-source + different `chunk_kind`/`derivation_kind` collapse at 0.45; distinct sections stay at 0.72); embedding / multi-query / HyDE calls **degrade instead of crashing** (skip the model → lexical+entity carry the search); blank queries short-circuit (no zero-vector noise). **Deploy-gated** — live search reflects these only after aidream deploys. **Legibility (Search tab):** an **"entity match only"** warning badge + Entity-rank bar + matched-entity chips on `RichHitCard`; a **scope-summary line** under the box (resolved store / org / scope-tags / kind, spelling out that an empty org filter = ALL your orgs); **per-term coverage chips** ("indemnification ✕ 0", scoped honestly to "in these results"); the sidebar HyDE / multi-query / MMR knobs now drive the plain Search request (were Agent-tab-only). **Honesty:** OpenAI-embeddings-only deployment — the "Rerank with Cohere" control + copy are de-Cohere'd ("Rerank results", only when a reranker is configured); `QuickSearchDialog` no longer calls its lexical search "what an agent would retrieve". **Robustness:** `/rag/viewer/[id]?page=` forwards to the preview (`LibraryPreviewPage` `initialPageNumber`) so a citation lands on the hit's page; the local `citationHrefFor` was completed to mirror `api/search.ts` (every kind, `&page=`, `encodeURIComponent`); `seqRef` race guards on `runSearch`, the diagnose stream, and the agent-tool panel; invalid `?tab=` falls back to `search`; `?store_id=` is restored from the URL; null-snippet / null-score guards. Verified by the full `matrx-rag` suite (210) + full type-check + two adversarial review rounds (fix-forwards committed).
- **2026-07-27 (surface completeness)** — `matrx-user/rag-search` taken to `readiness: "verified"`. The manifest gained `urlPattern`, an `intro`, four curated groups (Query / Retrieval scope / Pipeline settings / Results) and grew 11 → 27 surface-specific values. The five result values the emitter had been shipping as UNDECLARED runtime extras (`result_count`, `total_candidates`, `search_results`, `source_ids`, `result_scores`) are now declared, plus `executed_query`, `query_term_coverage`, `available_data_stores`, `active_organization_id`, `active_scope_ids`, `expand_entity_clusters`, `result_limit`, `top_score`, `result_source_kinds`, `latency_ms`, `embedding_model`, `reranker_model`, `rerank_status`. `buildRagSearchContextData` emits every one (term coverage reuses the page's own `getHighlightTerms`, so the agent sees exactly the "Terms in results" readout the user sees); `search_results` and `available_data_stores` are `autoContext: false` — the readable passages already ride in the baseline `content`. The Search tab now mounts a `SurfaceRuntimeProvider` so the header Agents chrome gets the live query + results, and the Agent Chat tab's launch scope carries the working-context org/scopes, the store list, and the entity-cluster toggle.
- **2026-06-27 (source inspector)** — New `components/source-inspector/` — the citation landing surface for a retrieved hit (opened from the `rag_search` tool cards; reusable by any citation surface). Given `{source_kind, source_id, page_number(s), chunk_id}` it opens the `sourceInspectorWindow` overlay → `SourceInspectorPane`: the real PDF rendered **at the exact cited page** (composes `features/pdf` `PdfPreview`'s controlled `pageNumber`) beside synced tabs that follow the page — **Match** (the matched chunk floated + ringed among its page siblings via the canonical `ChunkList`/`ChunksOnPage`, now taking `highlightChunkId`), **Clean text**, **Raw text** (focused single-page read `usePageBundle` — one `processed_document_pages` row, NOT the whole-doc load), **Extractions** (lazy `ExtractionsPane`). Identity resolved through the PDF bridge (`usePdfSurfaceLinks`); non-PDF/text-only sources degrade to the page image + text. Opened via `useOpenSourceInspectorWindow` (`features/overlays/openers/sourceInspectorWindow.tsx`). Live-verified against a real processed PDF (landed on page 12/35, real chunk highlighted, page-12 raw/clean shown), light + dark.
- **2026-06-27 (viewer search)** — `/rag/viewer/[id]` (`LibraryPreviewPage`) in-document search rebuilt and lifted to the viewer level (`hooks/useDocumentSearch.ts`). One query now drives, in lockstep: a top **search bar** (`components/library/DocumentSearch.tsx` → `DocumentSearchBar`), a **summary banner** (`DocumentSearchSummary` — match/page counts + clickable page chips that jump + highlighted best snippet), **in-place highlighting** of every literal match in the page-text pane with a per-page **match stepper** (prev/next + `n/total`, auto-scroll), and the right-rail **Results** tab (`DocumentSearchResultsList`, replaces the deleted self-contained "Test search" panel). On submit, the viewer auto-jumps to the first matched page. Cross-page reach uses server lexical hits (`…/test-search`, `page_numbers`); highlighting is literal (`computeMatches`). New generic primitive `components/text/HighlightedText.tsx` (reuses the notes `computeMatches` engine) — the canonical "render text with matched terms marked" component. The bar links out to full AI search via **`/rag/search?q=`**, which now **auto-runs** on a `?q=` deep link (`RagSearchExperience` SearchTab). Highlighting also reaches the embedded `/files` Knowledge tab.
- **2026-06-24** — Fixed Document Library detail side panel getting stuck on "Loading document…" / blank content after resize: `MatrxDynamicPanel` no longer feeds live drag sizes back into `defaultSize` (that remounted panel children and aborted detail fetches); fullscreen uses `groupRef.setLayout()` instead. `useLibraryDoc` aborts in-flight fetches, resets on close, serializes responses, and surfaces errors with Retry; re-clicking the selected row refetches.
- **2026-06-23 (knowledge assets)** — **Knowledge Asset Builder** added: an observable, controllable surface that builds the six premium derivations over an extracted doc. New `api/derivations.ts` (`fetchDerivations` / `runDeriveStream` / `cancelDeriveRun` — clones the `stages.ts` NDJSON stream shape; treats the backend's `phase:"error" + extra.cancelled` as a cooperative cancel, not a failure, and captures `run_id` from the `started` event for cancel). New `hooks/useKnowledgeAssetRunner.ts` (per-kind `OpState`, `Map<kind, AbortController>`, sequential `runAll` in canonical order `page_verification → table_row → multigranularity → page_image_caption → section_summary → synthetic_qa`; refreshes the rollup after each result so KPI counts climb live). New `components/library/KnowledgeAssetPanel.tsx` (responsive `AnimatedKpiCard`-style representation rollup + per-card Build/Rebuild/Cancel with inline progress + a rich "Live activity" strip per running op). Cancel aborts the fetch AND POSTs the cancel endpoint. Mounted in `PdfStudioInspector` ("Assets" section) and `LibraryDocDetailSheet` ("Knowledge Asset" tab). Backend live in aidream (`rag.py` derive endpoints + `services/knowledge_derivations.py`).
- **2026-06-23 (surface wiring)** — `/rag/search` **Search tab** fully agent-wired to the `matrx-user/rag-search` surface. New pure `agent-context/buildRagSearchContextData.ts` maps live search state → `createRagSearchScope` (baselines `selection`/`content`/`context` + every manifest custom value: `query`, `data_store_id`, `data_store_name`, `source_kinds`, `admin_bypass_acl`, `rerank`, `multi_query`, `use_hyde`; plus additive result extras — `search_results`, `source_ids`, `result_scores`, `result_count`). `UnifiedAgentContextMenu` (modern `placementMode`, `content-block` hidden) now mounts on BOTH the editable search box (right-click + `onTextReplace`) and the presentational results panel (`isEditable={false}`). The search `<Input>` became `ProInput` (`startIcon`, `clearable`). No manifest/`ui_surface_value` change — all emitted values were already declared.
- **2026-06-22 (agent chat)** — `/rag/search` **Agent Chat** tab rebuilt on the canonical managed-agent system. The hand-rolled chat (bespoke message state + custom tool-call rendering streaming from `ragAgentChatStream` / `/rag/search-lab/agent/chat`) was replaced with `useAgentLauncher` + `AgentConversationColumn` (same stack as `/chat` and the Projects "Use AI" tab). New registered surface `matrx-user/rag-search` (`features/surfaces/manifests/rag-search.manifest.ts` + `ui_surface`/`ui_surface_value` rows) hands the agent the page's retrieval scope (`data_store_id`, `data_store_name`, `source_kinds`, `admin_bypass_acl`, `rerank`, `multi_query`, `use_hyde`) via `runtime.surfaceName` + `applicationScope`. The RAG tool family (`rag_search`, `rag_search_data_store`, `rag_search_cross_doc`, `rag_list_data_stores`, `rag_get_data_store`, `rag_list_sources`, `rag_get_chunk`, `rag_verify_answer`) is armed on the conversation via `addedTools`. `SourceFeature` gained `"rag-search"`. `ragAgentChatStream` in `api/search-lab.ts` is now unused by this page.
- **2026-06-22** — `/rag/search` Search Lab wires Surface-A working context: `ActiveContextPanel` in the scope sidebar (desktop + mobile drawer), `ActiveScopeChips` in the tab header, and `buildRagSearchContext` / `useRagSearchContext` pass `organization_id` + `scope_ids` into `POST /rag/search` (and `useRagSearch`). Project/task selections are shown in the picker but are **not** accepted by the RAG search API today. (see section above): `rag.data_store_grants` audience model (global / industry / opt-in), read-only-by-ownership-asymmetry, NER-off library ingest profile, `DataStorePublishPanel` + read-only treatment in `DataStoresPage`/`RichMemberTable`, `useDataStoreGrants`, grants/catalog endpoints, `data-stores-ext.ts` `"library"` kind. Paired with the new `features/industries/` taxonomy. Backend ACL branch + read-widening + `services/rag/library_grants.py`; DB migrations `0116`–`0119`.
- **2026-06-21** — User-facing RAG vocabulary: "chunks" → **Segments** / **Knowledge Segments**, pipeline **chunk** stage → **segment**, in-progress **Chunking** → **Segmenting**. Canonical labels live in `constants/vocabulary.ts`; DB/API `chunk*` fields unchanged.
- **2026-06-09** — claude: **removed two `AnimatePresence` exit-driven view swaps that can leave stale content layered over live content under React Compiler** (`reactCompiler: true` — same class of bug just fixed in `AgentConversationColumn` for `/chat/new`). `StageAnimations.tsx` (`StageHero`) swapped two `absolute inset-0` panels (gradient backdrop + per-stage hero animation) on `activeStage` via `AnimatePresence mode="sync"` + `exit` — a stalled exit stacks stale stage panels; now plain keyed `motion.div`s (enter-only fade, instant unmount). `RagSearchExperience.tsx` results panel used `AnimatePresence mode="wait"` + `FADE_IN_UP` (which carries `exit`) — worse under `mode="wait"`: a stalled exit blocks the NEXT query's results from ever mounting; now a plain `key={response.query}` conditional with enter-only animation. Benign list-map `AnimatePresence` usages (job rows, chunk particles, hit cards) left as-is.
- **2026-05-06** — Feature created via consolidation. Absorbed `features/library/`, `features/data-stores/`, `features/documents/`, and `features/rag-search-ui/` into a single feature; pulled the rag-shaped pieces out of `features/files/` (api: ingest/search; hooks: useFileIngest/useRagSearch; components: ProcessForRagButton/RagSearchHits). Files-table chrome that reads `cloudFiles.ragStatus` (rag-thunks, RagStatusCell, RagFilterPicker) intentionally stayed in `features/files/`. All routes (`/rag`, `/rag/library`, `/rag/data-stores`, `/rag/search`, `/rag/repositories`, `/rag/viewer/[id]`) compile and return 200.
- **2026-05-28** — `/rag/search` rebuilt as the multi-tab Search Lab (`components/search/RagSearchExperience.tsx`); old single-pane `RagSearchPage.tsx` removed. Added `api/search-lab.ts` typed client for the new `/rag/search-lab/{diagnose,expand,inventory,agent/chat}` endpoints. Search + Agent Simulation tabs now render skeleton loaders + framer-motion entrance animations for parity with the library surfaces. `NoteToolbar`'s "Process for RAG" button surfaces a `View in library` toast on completion.
- **2026-05-28 (later)** — Agent Simulation Pipeline counts switched from inline `Stat` helpers to `AnimatedKpiCard` (count-up tween + tonal glow + pulse on increase) so the "numbers on a panel" pattern is unified app-wide. `AnimatedKpiCard.value` extended to accept `string | number | undefined` — strings render as-is (no count-up), letting the Reranker tile show the model name or "off" through the same primitive.
- **2026-05-28 (final)** — `/rag/search-lab/diagnose` now has a streaming sibling at `/rag/search-lab/diagnose/stream`. Agent Simulation consumes the stream via `ragDiagnoseStream` and progressively fills each panel as events land: query expansion → Visible chunks KPI animates 0 → N → After fusion KPI animates → hits arrive with stagger → final elapsed_ms + notes + JSON inspectors. Same `DiagnoseResponse` shape on the FE so the existing render code keeps working; the difference is _when_ the fields are populated.
- **2026-05-28 (agents)** — `citationHrefFor()` in `api/search.ts` extended to handle `library_doc` (`/rag/viewer/<id>?chunk=…`), `transcript` (`/transcription/studio?session=<id>`), and `scraped` (`/scraper?url=<source_id>`). The new `rag_search` tool-call renderer (`features/tool-call-visualization/renderers/rag-search/`) consumes the canonical helper directly so chat citations now route the same way as `/rag/search` hits.
- **2026-05-28 (mobile)** — `/rag/search` is now fully responsive (`components/search/RagSearchExperience.tsx`). Container switched from hardcoded `h-[calc(100vh-3rem)]` to `h-dvh … md:h-page`; tab strip becomes a horizontal scroll-snap row on phones (icons + labels fit across the viewport — no Drawer hop); the scope sidebar collapses into a bottom `<Drawer>` triggered by a `PanelLeftOpen` icon in the header, rendering the SAME `ScopeSidebar` component (new `variant: "desktop" | "drawer"` prop, no shrunk-down cousin); every input/textarea/number-input in all four tabs gained `text-base` to prevent iOS auto-zoom; the Agent Chat composer footer got `pb-safe` for home-indicator safety; the rich hit-card header chips now wrap so nothing overflows at 390px. Existing skeleton loaders and framer-motion entrance animations preserved.
