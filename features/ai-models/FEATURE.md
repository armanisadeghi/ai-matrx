# FEATURE.md — `ai-models`

**Status:** `stable`
**Tier:** `2`
**Last updated:** `2026-07-11`

---

## Purpose

The catalog and configuration surface for every LLM available to the product — model identity (id, maker, deprecated flag), capabilities, and (via the resolution layer) controls schema + declarative runtime constraints. Pricing lives on `ai.offering` (not the model row). Agents (and legacy prompts/builtins) reference rows from this registry by ID; the Agent Builder uses it as a picker.

---

## Entry points

**Imports:** There is no root `index.ts` barrel — import from `components/…`, `service.ts`, `types.ts`, `hooks/…`, `redux/…`, `audit/…`, and `server/…` as needed.

**Routes**
- `app/(admin)/administration/ai-models/page.tsx` — admin model registry (table + detail panel + tab presets)
- `app/(admin)/administration/ai-models/audit/page.tsx` — `ModelAuditDashboard` (data-quality rules across models)
- `app/(admin)/administration/ai-models/deprecated-audit/page.tsx` — deprecated-reference cleanup
- `app/(admin)/administration/ai-models/provider-sync/page.tsx` — pull live model lists from provider APIs
- `app/(admin)/administration/ai-models/providers/page.tsx` — `ProvidersContainer`, full CRUD on `ai.provider` (name, slug, links, logo, visibility)
- `app/(admin)/administration/ai-models/endpoints/page.tsx` — `EndpointsApisContainer`, full CRUD on `ai.endpoint` (vendor, base URL, auth, priority) + `ai.api` (translator key, transport, rules, request defaults)
- `app/(admin)/administration/ai-models/offerings/page.tsx` — `OfferingsContainer`, full CRUD on `ai.offering` (Manage tab) + a Coverage tab reporting `ai.model_offering` and the models with zero offerings
- `app/(admin)/administration/ai-models/settings/page.tsx` — `SettingsContainer`, full CRUD on `ai.setting` (the canonical settings vocabulary)
- `app/(admin)/administration/ai-models/aliases/page.tsx` — `AliasesContainer`, full CRUD on `ai.model_alias` (alias, kind, target model, notes)

All four routes are registered in `features/admin/constants/admin-categories.ts` under the "AI Models" category alongside Registry/Audit/Deprecated/Provider Sync.

**API endpoints**
- `GET /api/ai-models` — cached (12h s-maxage, 24h SWR) list of active models for client/SSR readers
- `POST/GET /api/ai-models/provider-sync` — fetch-and-cache model lists from Anthropic / OpenAI / Groq APIs into `ai.provider.provider_models_cache`
- `POST /api/ai-models/revalidate` — cache-tag invalidation

**Hooks** (`features/ai-models/hooks/useModels.ts`)
- `useModels()` — active-model options (lightweight, for dropdowns); triggers `fetchModelOptions` thunk
- `useModelOptions()` — `{ value, label, provider }` options for dropdowns
- `useAllModelOptions()` — active + deprecated (admin tooling)
- `useDeprecatedModels()` / `useAllModels()` — full lists
- `useModelFull(id)` — gates on `_fetchType === 'full'`, returns `undefined` until the full row is loaded; auto-dispatches `fetchModelById`
- `useModelById(id)` — any fetch level (options or full)
- `useModelFetchType(id)` — readiness check for conditional rendering
- `useTabUrlState()` — URL-persisted tab/filter presets for the admin table

**Services**
- `features/ai-models/service.ts` — `aiModelService`: client-side CRUD (`fetchAll`, `create`, `update`, `remove`, `bulkPatchField`, `patchField`), provider-cache ops, usage lookup (`fetchUsage` across `agent.definition`/`agent.template`), and deprecation-migration helpers (`replaceModelIn*`). `public.prompts`/`prompt_builtins` are graveyarded — `fetchUsage`/`replaceModelInPrompts` treat that leg as a no-op (0 rows, intentional). Also: `fetchAllProviders`/`createProvider`/`updateProvider`/`deleteProvider`, `fetchEndpoints`/`createEndpoint`/`updateEndpoint`/`deleteEndpoint`, `fetchApis`/`createApi`/`updateApi`/`deleteApi`, `fetchOfferings`/`createOffering`/`updateOffering`/`deleteOffering`/`fetchModelOfferingView`, `fetchSettings`/`createSetting`/`updateSetting`/`deleteSetting`, `fetchAliases`/`createAlias`/`updateAlias`/`deleteAlias`, and `fetchModelConfig(modelId)` (the resolved `ai.model_config` row) — full CRUD for the catalog's other tables/views.
- `features/ai-models/catalogReload.ts` — `reloadAiCatalog()` thunk → `POST /admin/ai-catalog/reload` via `callApi`. EVERY rule-editing save (api rules, offering override) dispatches it; the aidream brain caches the catalog and stays stale otherwise.
- `features/ai-models/server/ai-models-server.ts` — `fetchAIModels()` (React-cached server reader for SSR shells)

**Redux slice**
- `features/ai-models/redux/modelRegistrySlice.ts` — `modelRegistry` slice: normalized `entities` + `activeIds`/`deprecatedIds`, `fetchScope`, per-record `_fetchType` (`'options' | 'full'`). Thunks: `fetchModelOptions`, `fetchModelById`. Action: `hydrateModels` (SSR). Memoized selectors including factory `makeSelectModelById` for multi-ID subscribers.

---

## Data model

> 🔴 **READ SURFACE = THE VIEWS.** The 2026-07-10 reshape landed: the dissolved `ai.model_definition` columns (`provider`/`endpoints`/`pricing`/`capabilities_pre_canonical`) are **dropped**, `model_provider` was **renamed `provider_id`**, and **`ai.service` no longer exists** — it split into **`ai.endpoint`** (one row per serving vendor) + **`ai.api`** (one row per wire contract; `translator_key` = the old `wire_format` vocabulary). The legacy `api_class`/`model_class`/`controls`/`constraints` columns are **DROPPED** (ai_034, 2026-07-10; per-row values snapshotted into `metadata->'legacy'`, which app code must NEVER read). The clean read surfaces are the DB views **`ai.model_public`** (users; maker + `cost_rating`/`speed_rating`, points-not-dollars, NO vendor/wire-format columns) and **`ai.model_admin`** (admins; + endpoint/api/offering internals). The app attaches a resolved **`maker`** (`ai.provider.name`) to every model row in the fetch/service/SSR layers; **read `maker`, never `provider`.** Users must NEVER see serving vendors (Groq, Together, Cerebras, …) or wire formats — admin-only surfaces may.

**Database tables** (Supabase, `ai` schema — renamed/canonicalized 2026-07-02 "AI-catalog reshape", was flat `public.ai_*`)
- `ai.model_definition` (was `ai.model`) — master registry row. Live columns include `id`, `name`, `common_name`, `provider_id` (FK → `ai.provider`; was `model_provider`), `context_window`, `max_tokens`, `is_deprecated`, `is_primary`, `is_premium`, curated `cost_rating` + `speed_rating` (1-6 smallints; 6 renders as "5+"), retry plumbing `retry_fallback_id` + `retry_max_attempts`, two self-FK columns `mid_fallback_id` + `guest_fallback_id` (see Tier fallbacks below), and the JSONB `capabilities` blob. The legacy `api_class`/`model_class`/`controls`/`constraints` columns are dropped (ai_034) — resolved controls/constraints come from `ai.model_config`.
- `ai.model_public` (view) — user read surface: `id, name, common_name, capabilities, context_window, max_tokens, is_primary, is_premium, mid_fallback_id, guest_fallback_id, release_date, description, maker, cost_rating, speed_rating, usage_basis, token_billed, points_per_million_input/output`. NO `model_class`, NO `service_name`/vendor — the serving-vendor leak is fixed. Excludes deprecated/deleted; backs the picker options fetch (`fetchModelOptions`).
- `ai.model_admin` (view) — admin read surface with postgres-ONLY grants (real serving-vendor names are secret). Client code MUST read it via the `public.admin_model_catalog()` SECURITY DEFINER RPC (gated by `is_super_admin()`; `migrations/ai_model_admin_catalog_rpc.sql`) — never `.from("model_admin")`, never grant it to `authenticated`. Columns: everything in `model_public` + `is_deprecated`, `offering_id`, `provider_model_id`, `provider_id`, real `pricing`, `endpoint_id`, `endpoint_internal_name`, `endpoint_display_name`, `vendor`, `endpoint_has_base_url`, `api_id`, `api_name`, `translator_key`, `transport`, `retry_fallback_id`, `retry_max_attempts`, `cost_rating`, `speed_rating`.
- `ai.provider` — provider catalog (Anthropic, OpenAI, Groq, …) + `provider_models_cache` JSONB (last live fetch from provider API). `provider.name` is the resolved `maker`.
- `ai.endpoint` — ONE row per serving vendor: `vendor` (unique), `internal_name`, `display_name`, `base_url`, `auth_ref` jsonb, `byok_secret_key`, `priority`, `is_active`, `notes`.
- `ai.api` — one row per wire contract: `name` (unique), `display_name`, `translator_key` (unique; the old `wire_format` values), `transport`, `rules` jsonb (`{"params":{...},"constraints":[...]}` envelope), `request_defaults` jsonb, `description`.
- `ai.model_alias` — alias registry: `alias` (unique), `model_id`, `kind` (`alias`|`deprecated`|`latest`), `notes`. Admin CRUD at `/administration/ai-models/aliases`.
- **`ai.model_config` (view) + `ai.resolve_model_config(model_id)` (fn)** — THE resolution layer (Phase D, migration `ai_030`): per active model, computes the FE-facing `controls` (NormalizedControls-compatible per-key defs) and `constraints` (ModelConstraint[] DSL) for the PREFERRED offering (lowest priority, available, active endpoint) from `ai.api.rules.params` ⊕ `ai.offering.override.params` (per-key merge, override wins per field; `supported:false` keys excluded) × `ai.setting` (type, canonical range/values, defaults), plus capability-derived UI gates (`tools`/`image_urls`/`file_urls`/`youtube_videos`/`internal_*`/`response_format`). `max_output_tokens.max` is capped by `model_definition.max_tokens` (per-model ceilings live THERE, not in overrides). Masked like `model_public` — no vendor/translator data. This is what `fetchModelById` reads; the legacy `model_definition.controls`/`constraints` columns are dropped (ai_034). Parity gate report: `docs/db_rebuild/proposals/ai_model_config_parity.md` (zero regressions).
- `ai.voices` — TTS voice catalog (provider-agnostic), canonicalized alongside the reshape; consumed by `features/podcasts/generator/voiceCatalog.ts`, not this feature.
- Referenced by (read side): `agent.definition.model_id` / `agent.template.model_id` (+ `model_tiers.primary_model_id`). The old `public.prompts`/`prompt_builtins`/`agx_agent`/`agx_agent_templates` were migrated into `agent.definition`/`agent.template` and are now `graveyard.*` — no live reads against them.

**Catalog admin CRUD (reshaped 2026-07-10):**
- `ai.endpoint` + `ai.api` — managed together at `/administration/ai-models/endpoints` (`EndpointsApisContainer`, a tab per entity with table + edit panel). The old `/administration/ai-models/services` route and `ServicesContainer`/`ServiceTable`/`ServiceForm` are deleted.
- `ai.offering` — `model_id × endpoint_id × api_id` = the actual callable unit; **the sole home of pricing** (per-offering tiers) + availability/capability overrides over `model_definition`, plus the enveloped **`override`** jsonb (`{"params":{...},"constraints":[...]}`; was flat `controls_override`). Managed at `/administration/ai-models/offerings` (`OfferingsContainer`, Manage tab; the form edits `override.params` and passes `override.constraints` through untouched). **Model-level pricing editing was removed** — the model detail Pricing tab is read-only (`OfferingPricingReadOnly`); there is exactly ONE way to edit pricing (here).
- `ai.setting` — canonical settings vocabulary (temperature, reasoning_effort, …). Managed at `/administration/ai-models/settings` (`SettingsContainer`/`SettingTable`/`SettingForm`). **Still not wired as the render source for the model Constraints/Controls UI** — that architectural switch remains a separate, unstarted initiative.
- `ai.model_offering` (view) — user-facing offering rows with points-based pricing (`points_per_million_input/output/cached_input`). NO vendor identity — the serving vendor is never exposed here; instead each row carries **`served_via`** = the endpoint's PUBLIC Matrx brand (`ai.endpoint.display_name`: "Matrx Lightning"=cerebras, "Matrx Fast"=groq, "Matrx Standard"=together, "Matrx Voice"/"Matrx Extract"/"Matrx Hub"/"Matrx Media"/"Matrx Test"; anthropic/openai/google/xai keep their own names) + **`served_via_endpoint_id`** (opaque uuid — the value a future chat-wire endpoint hint would echo to aidream's `resolve_call_profile(endpoint_hint=...)`; the wire field does not exist yet). One model row + N offerings = N serving TIERS (priority-ordered, fastest first) — `useModelCatalog` groups them into `CatalogModel.tiers` and the `ModelListDropdown` detail card renders a "Served via" brand-chip row (default tier highlighted). Surfaced read-only in the Offerings page's Coverage tab, alongside a computed gap list of models with zero offerings.
- `ai.model_definition.provider_id` (FK → `ai.provider.id`) is the **sole** way to set a model's maker — the model's brand is read as the resolved **`maker`** (`ai.provider.name`), never free text.
- All four new CRUD screens home newly-created rows in the global system org via `resolveSystemOrgId()` (`lib/organizations/systemOrg.ts`) — **do not** rely on the `_stamp_org_default` trigger for these tables; it defaults to the *creating user's personal org*, not the system org, which would mis-home new catalog rows relative to the existing system-owned seed data.

**Key types** (`features/ai-models/types.ts`)
- `AiModel` — `AiModelRow` with JSONB `capabilities` narrowed, plus a resolved **`maker: string \| null`** (not a stored column — attached by the fetch/service layer from `ai.provider.name`). Read `maker` for all display/filter/group. No `controls`/`constraints`/`model_class`/`api_class` — those columns are dropped; resolved controls/constraints live only on the registry slice's `'full'` records (from `ai.model_config`).
- `AiEndpoint` / `AiApi` — `ai.endpoint` / `ai.api` rows with Json fields narrowed; `AiApi.rules` and `AiOffering.override` share the **`RulesEnvelope`** shape (`{ params: ControlsSchema; constraints: ModelConstraint[] }`). `AiModelAliasRow` covers `ai.model_alias`.
- `AiProvider` — provider row with `provider_models_cache: ProviderModelsCache | null`.
- `PricingTier` — `{ max_tokens, input_price, output_price, cached_input_price, usage_basis?, note? }`. `usage_basis` is the **billing unit** the prices map to (`null`/absent = standard $/1M-token billing; other values: per-image, per-second, per-character, …). The full taxonomy + per-basis price labels + validation live in `features/ai-models/usageBasis.ts`, a **mirror of the matrx-ai server SSOT** (`matrx_ai/config/usage_config.py::USAGE_BASIS_SPECS`).
- `ControlsSchema = Record<string, ControlParam>` — per-field param definitions (type, min/max, default, allowed, enum, required).
- `ModelConstraint` — discriminated union:
  - `UnconditionalConstraint`: `{ id, rule: 'required'|'fixed'|'min'|'max'|'one_of'|'forbidden', field, value?, severity, message }`
  - `ConditionalConstraint`: `{ id, when: FieldCondition, require: FieldCondition, severity, message }` where `FieldCondition = { field, op: ConditionOp, value? }` and `ConditionOp = eq|neq|gt|gte|lt|lte|in|not_in|exists|not_exists`
  - `isConditionalConstraint(c)` discriminates by presence of `when`+`require` vs `rule`+`field`
- `AIModelRecord` (registry slice) — `AIModelRow & { _fetchType: 'options' | 'full'; maker; controls; constraints }`. `controls`/`constraints` are NOT table columns — they're resolved from `ai.model_config` and populated only on `'full'` records. Status only upgrades; a `'full'` record is never downgraded.
- Audit types (`audit/auditTypes.ts`): `CapabilityKey` (20 canonical capability keys — `text_input`, `function_calling`, `streaming`, `vision`, `structured_output`, …), `AuditRuleConfig`, `ModelAuditResult`.

---

## Key flows

### (a) Adding / editing a model in the registry

1. Admin opens `/administration/ai-models/`. `AiModelsContainer` calls `aiModelService.fetchAll()` + `fetchProviders()`.
2. Clicks "+ new" → `AiModelDetailPanel` opens in create mode with an empty `AiModelForm`.
3. On save: `aiModelService.create(insert)` inserts into `ai.model_definition`. The new row is prepended in component state and becomes the selected record.
4. Edit path uses `aiModelService.update(id, patch)` — returns the updated row (with resolved `maker`); `handleSaved` replaces in list. JSON fields (`controls`, `constraints`, `capabilities`) are edited via dedicated sub-editors in tabs. The **Pricing** tab is read-only (`OfferingPricingReadOnly`, sourced from the model's `ai.offering` rows) — pricing is edited in the Offerings page. There is no `endpoints`/`pricing` editor here.
5. Cache invalidation: `POST /api/ai-models/revalidate` clears the SSR cache tag; the client Redux registry is refreshed on next `fetchModelOptions` / `fetchModelById`.

### (b) Editing a model's controls / constraints (Phase D — rules, not columns)

1. In `AiModelDetailPanel`, the **Controls** and **Constraints** tabs mount `ModelRulesEditor` (`components/ModelRulesEditor.tsx`). There is NO editor for `model_definition.controls`/`constraints` anymore — those columns are dead (`ControlsEditor.tsx` deleted; raw-JSON pastes strip both keys).
2. The editor shows: an offering selector (defaults to the preferred one), the READ-ONLY resolved output from `ai.model_config`, and editors for the two real sources — the family envelope `ai.api.rules` (`{params, constraints}`, saved via `aiModelService.updateApi`) and the per-model envelope `ai.offering.override` (saved via `updateOffering`). Constraints mode uses the structured `ConstraintsEditor` (spec: `CONSTRAINTS-EDITOR-SPEC.md`) against each source's constraints array.
3. Every successful save dispatches `reloadAiCatalog()` (POST `/admin/ai-catalog/reload`) and re-reads the resolved view. The same reload fires from the Endpoints & APIs page (api rules save) and the Offerings page (override save).
4. Server-side validation against agent settings at call time treats the resolved constraints array as the source of truth.

### (c) Model selection in the Agent Builder

1. Builder renders `AgentModelConfiguration`, which mounts the canonical `ModelListDropdown` (`features/ai-models/components/lab/ModelListDropdown.tsx`) — the Picker-Lab off/user/admin switcher is GONE; the user variant IS the picker, and super admins get an in-place Admin toggle inside the dropdown's bottom bar. `AgentSettingsCore` (settings modal) still mounts `SmartModelSelect` (`features/ai-models/components/smart/SmartModelSelect.tsx`).
2. `SmartModelSelect` calls `useModels()` → `fetchModelOptions` thunk populates `modelRegistry` with `_fetchType:'options'` records. Dropdown renders from `selectModelOptions`.
3. On pick, `onValueChange(modelId)` dispatches `setAgentField({ field: 'modelId', value: modelId })` on the `agentDefinition` slice. The agent row stores the ID in `agent.definition.model_id`; converters map it back as `modelId` (`features/agents/redux/agent-definition/converters.ts`).
4. For controls/context_window/max_tokens/etc., `SmartModelConfigs` calls `useModelFull(modelId)` — triggers `fetchModelById` and returns the `'full'` record only once loaded.
5. At invocation time, the Builder ships the full agent definition (including `modelId`) to `POST /prompts`; Runner/Chat/Shortcut/App ship only the agent ID, and the server resolves the model row.
6. **Service pinning (optional):** in `ModelListDropdown`'s detail card the Service chips are pin toggles when the consumer passes `pinnedOfferingId`/`onOfferingPinChange` — pin an exact `ai.offering` uuid, or "Auto" (no pin) to let the server pick the preferred offering by priority. `AgentModelConfiguration` persists the pin as `settings.offering_id` alongside every other agent setting. See the 2026-07-11 change-log entry for the full contract.

### (d) Audit — who changed what / data quality

1. `/administration/ai-models/audit` mounts `ModelAuditDashboard`. It pulls `aiModelService.fetchAll()`, filters deprecated out by default, runs `runAudit(models, rules)` against `DEFAULT_AUDIT_RULES`.
2. Tabs (`overview`, `core_fields`, `capabilities`, `configurations`) show per-category failures. `AuditRulesConfig` lets admins tune thresholds live (client-only state). (There is no model-level `pricing` audit — pricing lives on `ai.offering`, validated server-side by `validate_model_pricing`.)
3. Inline fixes: each row has a quick-edit that calls `aiModelService.patchField(id, field, value)` or `bulkPatchField(patches)`; local `models` state is updated via `handleModelUpdated`.
4. Deprecated-reference cleanup: `DeprecatedModelsAudit` uses `aiModelService.fetchUsage(id)` to find every builtin/agent/template still pointing at a deprecated model (against `agent.definition`/`agent.template` — `prompts`/`prompt_builtins` are graveyarded no-ops), then `replaceModelInBuiltins` / `replaceModelInAgents` / `replaceModelInAgentTemplates` to rewrite `model_id` (and merge `settings->model_id`) in one pass.
5. Provider sync: `/administration/ai-models/provider-sync` → `POST /api/ai-models/provider-sync` fetches live model lists from provider APIs and caches in `ai.provider.provider_models_cache`; admins diff against registry to spot new or removed models. (Note: historical row-level change audit is not stored in-app — the dashboard is data-quality audit, not who-changed-what.)

---

## Invariants & gotchas

- **A model is not callable until it has an `ai.offering`.** `ai.model_definition` is identity + capabilities; routing is `ai.offering` (model × endpoint × api × `provider_model_id`). Pickers (`fetchModelOptions`, SSR `fetchAIModels`) MUST filter through `ai.model_offering` — a bare model row looks "set up" in admin but `resolve_call_profile` raises. Creating a model without an offering is an incomplete create. (2026-07-09: grok-4.5 failed every call for exactly this.)
- **The `ai.model_definition` table is the single source of truth for model IDs, capabilities, pricing, and constraints.** Never hard-code provider model strings (`"claude-3-5-sonnet-…"`, `"gpt-4o"`) at call sites. Resolve via `useModelFull` or the server reader.
- **Agents reference models by `model_id` only.** `agent.definition.model_id` is a UUID pointing at `ai.model_definition.id` — never a provider string. Converters (`features/agents/redux/agent-definition/converters.ts`) preserve this on both read and write.
- **Constraints are advisory at the agent level and enforced server-side at call time.** The Builder/Runner UI MAY surface constraint violations as warnings but MUST NOT block save. The LLM-call layer runs `ModelConstraint[]` evaluation against the assembled request and rejects/downgrades per `severity`.
- **Registry records have two data levels.** `_fetchType: 'options'` only guarantees `id`, `name`, `common_name`, `maker`, `cost_rating`, `speed_rating` (sourced from `ai.model_public`; `model_class` is gone from the view and from `AIModelRecord`). Anything else (`controls`, `context_window`, `constraints`, `capabilities`) requires `_fetchType: 'full'` — sourced from **`ai.model_config`** (the resolution view), NOT `model_definition`. Always gate on `useModelFull()` or `selectModelFullyLoaded()` before reading those fields. Read `maker` for brand, never `provider`.
- **Rating rendering is centralized.** `costRatingTier()` / `speedRatingLabel()` in `features/ai-models/format.ts` are the only renderers for `cost_rating`/`speed_rating` (1-6; 6 = "5+"). `lab/modelDisplay.ts` is deleted; hardcoded maker/price maps are forbidden.
- **Every rule edit must reload the backend catalog.** `ai.api.rules` / `ai.offering.override` are cached in the aidream brain — a DB write without `reloadAiCatalog()` leaves the live server translating with stale rules.
- **Status never downgrades.** Once a record is `'full'`, subsequent `fetchModelOptions` calls will not overwrite it back to `'options'`. The slice has explicit guards — do not bypass them.
- **`/api/ai-models` is CDN-cached for 12h** (`s-maxage=43200, stale-while-revalidate=86400`). Changes to the registry require `POST /api/ai-models/revalidate` to propagate to SSR consumers; in-app Redux reads use the client thunks and see changes immediately.
- **Deprecating a model is destructive at the reference layer.** Flip `is_deprecated=true` in `ai.model_definition`, then run the deprecated-audit migration helpers — do not rely on downstream features to catch stale references. `fetchUsage` is the canonical list of everything to migrate.
- **`replaceModelIn*` helpers patch both column and `settings->model_id`.** Some legacy rows store the model in either place. Always let the helper fetch-then-patch; never write raw SQL that ignores the JSONB path.
- **Provider-cache and registry are separate.** `ai.provider.provider_models_cache` is live provider data — not the canonical list. Adding a model to the registry is always an explicit create against `ai.model_definition`.
- **A media/audio model's `pricing.usage_basis` MUST match how its provider reports usage — wrong/absent basis silently mis-bills.** This is the bug class behind a gpt-image model billing $30/image (missing basis → synthetic per-image charge against a $/1M-token price), TTS billing $0 (units never populated), and ElevenLabs billing 1,000,000× too little ($/character entered as $/1M-character). The pricing editor (`ModelPricingEditor`) now requires a basis per tier, labels each price field with its real unit, and surfaces inline errors via `validatePricingTiers` (`usageBasis.ts`) — the same checks the server runs in `scripts/validate_model_pricing.py` (loud drift guard) and `matrx_ai.config.usage_config.validate_model_pricing`. "Is this a media model" is derived from the canonical `capabilities` shape (`isMediaModel`), NOT from the dead `api_class` column. **Billing NEVER reads a model name.** A media model may legitimately carry no basis only when its provider bills real tokens — that is the explicit DB fact `ai.offering.token_billed` (migration `ai_012`, also exposed on the `ai.model_offering` view), passed into `validatePricingTiers` as `PricedModel.tokenBilled`. The rule is pure data and fail-closed: *media output AND `usage_basis IS NULL` AND NOT `token_billed` ⇒ pricing bug*. Unknown ⇒ `false` ⇒ the validator screams. Pricing (with `token_billed`) is edited in the offering editor (`OfferingForm`, "Token-billed" switch); the model detail Pricing tab only *displays* the model's offerings read-only (`aiModelService.fetchOfferingsForModel`). Never re-introduce a model-name regex or hardcoded list. **When adding a media model, pick the basis; never leave it on "Token" for a non-token-billed media model.**

---

## Tier fallbacks — `mid_fallback_id` + `guest_fallback_id`

Two self-FKs on `ai.model_definition` let admins declare what the aidream backend should substitute when the caller is past a tier:

- **`mid_fallback_id`** — picked when an authenticated user is past their soft limit. Today this is plumbed but not enforced; the column captures the intent so the eventual quota gate can read it without further schema changes.
- **`guest_fallback_id`** — picked when the caller is unauthenticated (`X-Fingerprint-ID` header from the matrx-extend Chrome extension or any future surface). Aidream's `swap_model_for_auth_tier` runs inside `prepare_agent_run` and silently rewrites `config.model`. The original is stashed on the run's `ctx.metadata['original_model']`.

Both default to `NULL`, which means "no swap; keep the agent's declared model". Set them for premium models that shouldn't run for free (Opus → Haiku for guests, Opus → Sonnet for paying users at limit; GPT-5/4o → gpt-4.1-mini for guests; Gemini Pro → Gemini 3 Flash Preview, etc.).

**Editing**: every model row in `AiModelDetailPanel` has Mid-tier Fallback + Guest Fallback Selects under the Flags section. The dropdowns are grouped by provider, exclude the current model (no self-references) and deprecated rows. Choose "— no swap —" to clear the field.

**Migration**: aidream `db/migrations/0045_guest_mode_and_model_tiers.sql` adds the columns + a `cx_user_usage_summary` table + an `AFTER UPDATE OF completed_at` trigger on `cx_user_request` that maintains rolling 6h/24h usage windows per user (so future enforcement is O(1)).

**TS regen**: until `pnpm db:generate` refreshes `types/database.types.ts`, the two columns are added to the augmented `AiModel` type in `features/ai-models/types.ts` and the detail panel's save path casts through `unknown`. Drop those casts after the next regen.

---

## Related features

- **Depends on:** `utils/supabase/*` (client, server, admin/script clients), `types/database.types` (generated Supabase types)
- **Depended on by:** `features/agents/` (Builder model picker, settings, runtime resolution) via `agent.definition`/`agent.template`. The legacy `features/prompts/`/`features/prompt-builtins/` consumers are gone — those tables are graveyarded.
- **Cross-links:**
  - [`features/agents/FEATURE.md`](../agents/FEATURE.md) — consumer of the registry
  - [`features/agents/docs/AGENT_BUILDER.md`](../agents/docs/AGENT_BUILDER.md) — Builder flow that surfaces `SmartModelSelect`
  - [`features/ai-models/CONSTRAINTS-EDITOR-SPEC.md`](./CONSTRAINTS-EDITOR-SPEC.md) — full spec for the constraints tab

---

## Current work / migration state

Phase D (2026-07-10) is DONE: the resolution layer (`ai.resolve_model_config` + `ai.model_config`) is live, the settings engine reads it (`fetchModelById`), admin rule editing targets `ai.api.rules` / `ai.offering.override` with a backend catalog reload on every save, ratings render in pickers, and `ai.model_alias` / `retry_fallback_id` / `retry_max_attempts` / ratings have admin UI. Phase C is ALSO DONE (ai_034, same day): `model_definition.controls`/`constraints`/`api_class`/`model_class` are dropped from the live DB (values snapshotted per-row into `metadata->'legacy'` — recoverable, never read by app code).

---

## Change log

- `2026-07-11` — **Picker promoted to canonical + admin total-transparency variant.** (1) The Picker-Lab off/user/admin switcher is DELETED from `AgentModelConfiguration` — `ModelListDropdown` (user variant) IS the builder's model picker; `SmartModelSelect` no longer renders there. The `variant` prop is gone: the dropdown gates internally via `selectIsSuperAdmin` and shows super admins an "Admin" toggle (Shield, bottom count bar) that switches user ⇄ admin IN PLACE; the DB stays the real gate (`admin_model_catalog()` raises 42501 for non-admins). (2) **Admin variant = total transparency.** `useModelCatalog("admin")` now ALSO calls `public.admin_model_offerings()` (the RPC alone is one-row-per-model) and attaches `CatalogModel.admin.offerings` (`AdminOffering[]`: real `provider_model_id`, vendor, api name, translator, transport, parsed $ pricing bands from `ai.offering.pricing` — the ARRAY shape `[{max_tokens,input_price,output_price,cached_input_price}]`, `parsePricingBands`/`pricingOutputCost` handle it — priority, availability). The popover widens (400+360+400) with a third "Offerings — real vendor / wire data" column rendering EXPLICIT labeled rows per offering (Model:/Vendor:/API:/Translator:/Transport:/Endpoint:/Priority:/Input $/Output $/Cached $/Billing/Offering id); mobile appends the same section below the detail card; the old cramped inline admin blob in the detail card is gone. Admin tiers now come from ALL offerings (not just the preferred one), so GPT OSS 120B shows 3 services in admin too. (3) **Everything displayed is filterable/sortable in admin**: new filter groups Deprecated / Availability / Premium (tri-state) + Vendor + API (real names), new sorts Vendor / API / Deprecated / Availability / Premium + real-$ price sort; admin search also matches vendor/api/provider_model_id; leaving admin mode clears admin-only filters/sorts loudly-safe (they'd otherwise filter invisibly). (4) **User filter layout**: Input + Output modality groups moved to the TOP of the filter panel; Maker + Service moved to the BOTTOM. (5) Modality chips render straight from stored `capabilities` (no hardcoding — data fixes to e.g. whisper/xai-realtime input types flow through) and NEVER wrap: detail column widened to 360px, `flex-nowrap whitespace-nowrap` on modality rows. (6) **POINTS — the user currency — displayed everywhere** (was displayed nowhere): list row gains a Points column (compact, per-1M output; full in·out tooltip; list column 448px), the detail card shows a prominent "Points / 1M tokens — X in · Y out" row, and admin offering blocks show POINTS IN/OUT/CACHED alongside the real $. Sources: `ai.model_public.points_per_million_input/output` (model-level), `ai.model_offering.points_per_million_*` (per tier/offering; admin model-level = preferred offering's points). (7) **Admin offerings read hardened behind an RPC**: the brief direct `ai.offering`/`ai.endpoint`/`ai.api` client reads are GONE (any authenticated user could read vendor names — that RLS is being locked down); the ONE read path is `public.admin_model_offerings()` — SECURITY DEFINER over the postgres-only view `ai.model_offering_admin` (offering × endpoint × api × model_offering-points join; migration `migrations/ai_model_offerings_admin_rpc.sql`, applied + ledgered `source='matrx-frontend'`; EXECUTE to authenticated only; non-admin calls AND direct view reads raise 42501). Never grant the view; never read those tables directly from the client.
- `2026-07-11` — **Service pinning (offering_id) shipped in the picker.** A catalog listing is the exact call (model × endpoint × api = one `ai.offering` uuid), and the server now accepts an optional `offering_id` in agent settings: set → the call is pinned to exactly that offering; unset → the server picks the preferred offering by priority ("Auto (preferred)"). `ModelListDropdown` gained optional `pinnedOfferingId` + `onOfferingPinChange` props — when provided, the detail card's Service chips become pin toggles: a leading **Auto** chip (selected by default), click a Service to pin it (also selects that model — the pin and the model always move together), click the pinned chip (or Auto) to unpin. Selected chip reuses the exact former "default tier" highlight (`border-primary/40 bg-primary/10`); without the props the chips stay display-only (first = preferred, highlighted). Selecting a model whose offerings don't include the current pin clears it loudly (`console.warn`). Persistence lives with the consumer: `AgentModelConfiguration` stores it as `settings.offering_id` on `agent.definition` via `setAgentSettings` (same save path as temperature; the key is REMOVED when unpinned, never null). Stale pins on model swap are cleared by the agent-settings reconciliation dialog (`features/agents/.../reconciliation/analyze.ts`, offerings sourced from `useModelCatalog` tiers), and `offering_id` is a recognized routing key in the validation engine (never flagged unrecognized/unsupported). See `features/agents/FEATURE.md`.
- `2026-07-11` — **"Service" concept shipped in the picker + admin catalog fixed (RPC).** (1) USER-FACING term for a serving tier is **"Service"** — single source `features/ai-models/constants/service-label.ts` (`SERVICE_LABEL`/`SERVICE_LABEL_PLURAL`/`serviceCountLabel()`); rename it there ONLY. `ModelListDropdown` gained: Maker + Service filter chip groups (options derived from the loaded catalog), sort-by-Service (preferred brand, more-Services-first within a brand), search matching Service names ("lightning" finds models), an "N services" availability chip on multi-tier rows (GPT OSS 120B shows "3 services"), and the detail-card heading now reads Service/Services. Values shown are always the branded `served_via` — never a real vendor. (2) **Admin variant fixed**: `ai.model_admin` has postgres-only grants (vendor names secret), so the old direct `.from("model_admin")` read 42501'd for EVERYONE incl. super admins. New `public.admin_model_catalog()` SECURITY DEFINER RPC gated by `is_super_admin()` (migration `migrations/ai_model_admin_catalog_rpc.sql`, applied + ledgered; EXECUTE granted to authenticated only, non-admin calls raise 42501) is the ONE admin read path; `useModelCatalog` admin variant calls it — never grant the view itself. (3) The Picker-Lab "admin" toggle in `AgentModelConfiguration` is gated by `selectIsSuperAdmin` (hidden for non-admins; variant force-downgrades to "user" if state disagrees).
- `2026-07-11` — **Brand restore + serving tiers surfaced (ai_040, aidream repo).** Live DB: `ai.endpoint.display_name` restored to the public Matrx brands erased in the endpoint/api split (from `graveyard.ai_service_legacy`); GPT-OSS duplicate listings merged (120B: groq+together+cerebras "Lightning" rows → ONE model with 3 offerings prio 90/100/110 fastest-first; 20B likewise; `ai.model_alias` kind=deprecated rows incl. "GPT OSS 120b Lightning"); `ai.model_offering` gained `served_via` + `served_via_endpoint_id`. FE: `useModelCatalog` fetches the tier columns and exposes `CatalogModel.tiers` (`CatalogTier`); `ModelListDropdown` detail card shows a "Served via" brand-chip row. Display-only — per-request tier SELECTION needs a chat-wire endpoint-hint field (server seam `resolve_call_profile(endpoint_hint)` exists but nothing plumbs it from `ChatRequest`/`AgentStartRequest`).

- `2026-07-10` — **Phase C: legacy-column drop (ai_034) — no-legacy retire.** Live DB dropped `ai.model_definition.api_class`/`model_class`/`controls`/`constraints` (values snapshotted per-row into `metadata->'legacy'`; recoverable, NEVER read by app code); `ai.model_admin` no longer exposes `model_class`; types regenerated. FE fallout removed with no shims: `AiModel` no longer omits/re-adds the dropped keys (only `capabilities` is narrowed) and `AiModelFormData` lost `model_class`; `AIModelRecord` now declares `controls`/`constraints` as explicit resolved fields (from `ai.model_config`, `'full'` records only — this is what keeps agent-settings/`useModelControls` consumers typed); the `model_class` concept is deleted from the admin UI end-to-end — `AiModelTable` (column + filter + search + ctrl/rules JSON badges), `AiModelFilterBar` (class select), `AiModelForm` (input + "same as name" logic), `AiModelDetailPanel` (form field, insert/update payload, save validation, JSON-import hydrate, `AI_MODEL_COLUMNS`), `DeprecatedModelsAudit` (column/filter/sort/search), `AddProviderModelDialog` (no longer writes `model_class` into the insert payload; dead strip-list removed), `ProviderSyncDashboard` (class sort/column + local-entry fields + controls pane), `serializeProviderSyncForAi`, `filterUtils`, `format.ts`, `useTabUrlState` (URL filter param), audits (`require_model_class` rule + `CoreFieldsAuditTab` class edit + `AuditRulesConfig` toggle). `ProviderModelEntry` data from `provider_models_cache` is untouched (provider-API shape, not the DB column).
- `2026-07-10` — **Phase D: resolution layer + rules-based admin editing + ratings/alias UI.** DB: `ai_030_model_config_view` (`ai.resolve_model_config(model_id)` plpgsql STABLE + `ai.model_config` view over active models; grants to anon+authenticated, masked like `model_public`), `ai_031`-`ai_033` parity backfills (family params like openai/xai `store`+`parallel_tool_calls`, gemini `reasoning_summary`, gpt-5 `verbosity`, generated media/audio overrides from legacy controls, TTS chat-param suppressions, clamp/default enrichment, anthropic family constraint) — parity gate: `docs/db_rebuild/proposals/ai_model_config_parity.md`, ZERO regressions. FE: `fetchModelById` → `ai.model_config` (one view read; legacy controls/constraints columns dead to the app); `ModelRulesEditor` replaces raw controls/constraints editing in `AiModelDetailPanel` (`ControlsEditor.tsx` DELETED); every rules/override save dispatches `reloadAiCatalog()` (`catalogReload.ts`, POST `/admin/ai-catalog/reload`) — also wired into `EndpointsApisContainer` + `OfferingsContainer`; pickers show stored ratings (`SpeedDots` now real in `ModelListDropdown` + detail card, `SmartModelSelect` shows `$-tier · spd n/5`; `costRatingTier`/`speedRatingLabel` moved to `format.ts`, `lab/modelDisplay.ts` deleted); `AiModelForm` gained Ratings (cost/speed 1-6) + Retry Fallback (`retry_fallback_id`/`retry_max_attempts`) sections; NEW `/administration/ai-models/aliases` (`AliasesContainer`) full `ai.model_alias` CRUD + admin-nav entry.
- `2026-07-10` — **ai-schema reshape repoint (service → endpoint + api).** Live DB reshape applied during downtime; frontend fully repointed. `ai.service` is GONE: split into `ai.endpoint` (one row per serving vendor) + `ai.api` (one row per wire contract; `translator_key` = old `wire_format`). `ai.offering.service_id` → `endpoint_id` + `api_id`; `controls_override` → enveloped `override` (`{params, constraints}` — `OfferingForm` edits `override.params`, constraints pass through). `model_provider` → `provider_id` everywhere (service layer, API routes, cx-dashboard, forms, audits, provider-sync). NEW: `ai.model_alias`, `cost_rating`/`speed_rating` (1-6; 6 renders "5+"), `retry_fallback_id`/`retry_max_attempts`. Views: `model_public` lost `model_class`/`service_name` and gained the ratings (picker/catalog now read stored `maker` + `costRatingTier()` — `resolveMaker()` and the `priceTier()` threshold bands are deleted from `lab/modelDisplay.ts`); `model_offering` lost `service_name`/`service_slug`; `model_admin` swapped `service_*` for endpoint/api columns. Admin surface: `/administration/ai-models/services` → `/administration/ai-models/endpoints` (`EndpointsApisContainer`; old Service components deleted). Registry: `AIModelRecord` no longer carries `model_class`; the options fetch selects `id, name, common_name, maker`. Dead-relations entry added for `ai.service`. Settings modal's `model_definition.controls` read path intentionally unchanged (later phase).
- `2026-07-09` — **Dissolved-column read cutover → the views.** Stopped every frontend read of the dropping `ai.model_definition` columns (`provider`/`endpoints`/`pricing`/`api_class`/`capabilities_pre_canonical`). All model-row consumers now read a resolved **`maker`** (`ai.provider.name` via the `model_provider` FK), attached in `aiModelService.fetchAll`/`create`/`update`, SSR `fetchAIModels`, `GET /api/ai-models`, and (for the picker) sourced directly from **`ai.model_public`** in `fetchModelOptions`. Removed the **model-level pricing editor** (detail Pricing tab is read-only `OfferingPricingReadOnly`; deleted `PricingAuditTab` + the whole `pricing` audit category) and the endpoints table column + JSON editor — pricing/endpoints live on `ai.offering`/`ai.service` now. `AiModel`/`AIModelRecord` types omit the five columns and carry `maker`; `AiModelFormData` lost `provider`/`pricing`. Views `ai.model_public` + `ai.model_admin` are the clean read surfaces.
- `2026-07-09` — **Orphan model trap closed.** `fetchModelOptions` + SSR `fetchAIModels` now require a row in `ai.model_offering` before a model appears in pickers. A bare `model_definition` row is identity only — without an `ai.offering` the catalog cannot route it (`resolve_call_profile` raises). Incident: grok-4.5 was created without an offering and failed every agent-battle call while looking "set up" in the UI.
- `2026-07-08` — **Settings-review step restored in the replacement audits (D25 residual).** When the prompts system died, `ModelSettingsDialog` was stubbed to a plain confirm in `DeprecatedModelsAudit` / `ModelUsageAudit`. Shared `components/ModelSettingsReviewDialog.tsx` — callback-driven (open/value/onChange/onApply/onCancel), rows come from the settings catalogue (`buildSettingsRows`) for the REPLACEMENT model's declared controls, every control renders via the shared `SettingControlInput` (same stack as the agent builder / `RunConfigOverrides`), no forked settings UI. Genuine-delta semantics: only keys adjusted away from the model's defaults are kept, so `replaceModelReferences` receives real overrides, never defaults-as-settings. Both audits use it ("Review" per-row in the deprecated audit; step 2 of Replace Model in the usage audit).
- `2026-07-05` — **Admin deprecated-model replace bypasses RLS.** `replaceModelIn*` helpers silently no-op'd when PostgREST UPDATE matched 0 rows (admin lacks editor on another user's agent). New super-admin route `POST /api/admin/ai-models/replace-references` + `features/ai-models/server/replace-model-references.ts` (admin client, verifies row counts, patches `model_tiers.default` + nested tier `model_id`). `aiModelService.replaceModelReferences()` is the canonical single call; usage/deprecated audits switched to it. `fetchUsage` OR filter fixed (`model_tiers->>default`, was stale `primary_model_id`). `iam.apply_rls`'s generated `std_insert` policy (used by every `'system'`-variant canonical table, including `ai.model_definition` itself) only allowed `organization_id IS NULL OR iam.has_org_access(organization_id)` — and `iam.system_orgs` has zero members by design, so NO admin could ever INSERT a new row explicitly homed in the system org; every existing system-catalog row (across this table and others) was seeded via service-role migration, never through the app. `iam.has_access` already had the correct bypass for UPDATE/DELETE/SELECT (system-org row + `is_super_admin()` ⇒ allowed) — `migrations/apply_rls_super_admin_system_insert.sql` adds the identical bypass to the INSERT check and re-applies RLS to `ai.provider/service/offering/setting/model_definition`. Purely additive; re-verified `iam.canonical_certify_ok` stays `true` on all five. Without this, every "Create" button below (including the pre-existing "+ new model" one) would 42501 the moment it tried to home a new row in the system org.
- `2026-07-03` — Admin UI audit found the reshape's relational layer was half-wired: `AiModelForm.tsx` still had a free-text Provider input living alongside the `model_provider` FK select (both independently editable, nothing kept them in sync — the root cause of visible drift), and `ai.service`/`ai.offering`/`ai.setting` had zero admin surface despite holding 23/188/106 real rows. Fixed: (1) `migrations/ai_catalog_provider_backfill.sql` added the 11 providers that existed only as free text and re-derived every `model_provider` FK from the free-text column, fixing 33 null and ~10 mis-assigned rows; (2) removed the free-text Provider input from `AiModelForm.tsx` — `provider` is now derived from the selected `model_provider`'s name on save, never hand-typed; (3) built full CRUD for all three previously-unmanaged tables: `/administration/ai-models/providers`, `/services`, `/offerings` (+ a Coverage tab reporting the `ai.model_offering` view and models with zero offerings), `/settings` — all registered in `features/admin/constants/admin-categories.ts`. New rows on all four screens are homed via `resolveSystemOrgId()`, not the `_stamp_org_default` trigger (see Data model note above for why).
- `2026-07-02` — AI-catalog reshape doc sync + drift cleanup. `ai.model` → `ai.model_definition` rename (plus new `ai.service`/`ai.offering`/`ai.setting` tables + `ai.model_offering` view) was already fully repointed in code across prior commits, sitting unpushed on local `main` — production was still on stale deployed code, throwing `PGRST205: ai.model not found`, not a code bug. Separately found and fixed a real drift: the checked-in `migrations/get_ssr_shell_data_rpc.sql` / `get_ssr_agent_shell_data_rpc.sql` still said `ai.model` while the live DB functions had already been patched to `ai.model_definition` out of band — synced both files to the live bodies, re-applied (no-op) via Supabase MCP, updated `_schema_migrations` ledger checksums. Deleted two dead code paths modeling the old flat shape: `lib/api/ai-models.ts` (unused duplicate fetch path) and `features/recipes/view-setup/` (dead sub-feature, hand-rolled `AISettings` type mirroring graveyarded `ai_settings`).
- `2026-06-30` — Provider sync: Copy for AI at row / provider (status-filtered dropdown) / page levels via `ProviderSyncCopyForAi`; in-code excluded-model registry (`constants/excluded-provider-models.ts`) marks legacy provider IDs as "Excluded" instead of "Not in DB".
- `2026-06-30` — Provider sync dashboard: sortable column headers per provider table; default sort is release date descending via `compareTimestamps` (newest models first).
- `2026-06-30` — Provider sync: dropped retired `anthropic-beta: models-2024-09-01` header; Anthropic `/v1/models` is GA and only needs `anthropic-version`. Added `usage_basis` (+ `note`) to `PricingTier`; new `features/ai-models/usageBasis.ts` (taxonomy + `validatePricingTiers`, mirrors the matrx-ai server SSOT). `ModelPricingEditor` now has a per-tier billing-basis dropdown, dynamic per-unit price labels, and inline validation. Closes the root cause of the media-billing bugs (freeform pricing JSONB with no guardrail). Server side: `usage_config.USAGE_BASIS_SPECS`, computed megapixel/second billing, and `scripts/validate_model_pricing.py` drift guard.
- `2026-05-17` — Added Tier Fallbacks section + form Selects in `AiModelDetailPanel` for `mid_fallback_id` and `guest_fallback_id` (aidream migration 0045). Powers the guest-mode model swap for the matrx-extend Chrome extension.
- `2026-04-25` — Removed `features/ai-models/index.ts`; admin routes and provider-sync use direct imports to `components/*` and `service.ts` (same exports as before).
- `2026-04-22` — claude: initial doc.

---

> **Keep-docs-live rule (CLAUDE.md):** after any substantive change to this feature — especially to the `ai.model_definition` schema, `ModelConstraint` shape, registry slice fetch contracts, or the constraints editor — update this file's Entry points / Data model / Invariants sections and append to the Change log. Stale FEATURE.md cascades across parallel agents working on Builder, audits, and model picking.
