# War Room — FEATURE.md

> Session-based multitasking command center. Desktop-first. A user opens saved
> **War Rooms**, each a **cockpit of threads**: a **Stage** mode (a live
> watchlist rail + one driven thread) and a **Grid** mode (the self-arranging
> bento gallery, all at once), toggled in the header. Every thread bundles a
> Task + Notes + Audio transcript + Files/Documents + an Agent (the real Scribe
> Agent+ panel) behind six tabs, is context-aware, and can be pinned, parked, or
> projected. **Read this before touching any `features/war-room/**` code.**

**Status:** Active — the cockpit is shipped (Stage⇄Grid, instrument projector, density dial, live meter, kind accent-rails, metric chips, parked-thread rail; Waves 1–8 + the UI bake-off consolidation). **Owner route group:** `(core)`.

---

## What it is

The answer to context-switching: a War Room is a cockpit, not a wall of equal
cards. **Stage mode** keeps every thread alive in a glanceable watchlist rail
(each row shows a live status word + an "is-alive" pulse glyph) beside the one
thread you're driving on a full-height Stage — click any rail row to snap it onto
the Stage with full working state. **Grid mode** is the bird's-eye bento gallery
of every thread at once, arranged and resized by the generic gallery engine
(video-call gallery math + bento at low counts), so 3 tiles look intentional and
12 still hold up. Each thread also carries a **Files** tab — real cloud files
(upload or attach existing) and real editable documents (`udt_documents`) linked
to the tile — and an **Agent** tab: the real Scribe **Agent+** panel (an
assistant conversation + voice in/out + a user-and-agent co-edited working
document), bound to the SAME `studio_sessions` row the Audio tab records into, so
the tile's recordings are the agent's transcript context. The header is mission
control: a **Stage⇄Grid** toggle, the
**instrument projector** (force every thread to one view — all-Tasks, all-Notes…),
a **Comfortable/Compact density dial**, and a live **active / parked / pinned**
meter. Threads pin/park (hide) like Meet participants; parked threads stay
readable as chips and restore-and-stage on click. Rooms are saved sessions you
return to. War Room is a **thin consumer** — it owns the layout engine, the
thread shell, and the session model; all substrate data lives in the existing
features (tasks, notes, transcription, scopes).

## Routes (`app/(core)/war-room/`)

| URL | Renders | Notes |
|---|---|---|
| `/war-room` | `WarRoomLanding` (`ModuleLanding`) | Marketing. Authed users redirect to `/all`. |
| `/war-room/all` | `WarRoomAllView` | Browse / create / delete saved rooms (the list "savior" page). |
| `/war-room/[id]` | `WarRoomShell` | The cockpit: mission-control header (title + live meter + Stage⇄Grid + projector + density dial + session context) over `StageView` (rail + driven thread) or `WarRoomGallery` (Grid). |
| `/war-room/admin` | `FeatureAdminPage` | Super-admin map of every route/component/slice/table. |

## Data model (3 tables, all `ctx_war_room_*`)

- **`ctx_war_room_sessions`** — the room. Owner + multi-scope + `context_scope_ids` (jsonb scope-id array = the session-level context default) + soft-delete.
- **`ctx_war_room_tiles`** — one row per tile. `task_id`/`note_id` FKs (nullable), `context_organization_id`/`context_scope_ids` (NULL = inherit session), `active_tab`, `is_pinned`, `is_hidden`, `position`, **`flavor`** (`thread`|`task`|`project`, default `thread`), **`project_id`** FK → `ctx_projects` (nullable, set-null; for project-flavor tiles — see invariant 9).
- **`ctx_war_room_tile_audio_sessions`** — link table: tile → N `studio_sessions` (`source='war_room'`), `is_active` marks the current one.
- **`ctx_war_room_tile_notes`** — link table: tile → N `notes`, `is_active` marks the current one (the active note also lives on `tile.note_id` for note↔task sync).
- **`ctx_war_room_tile_attachments`** — **polymorphic** link table: tile → N files/documents. `entity_type` ∈ `('user_file','document')` → `entity_id` points at `cld_files.id` or `udt_documents.id`. `UNIQUE(tile_id, entity_type, entity_id)`. The linked entity stays in its own feature; only the link is stored.

RLS follows the `studio_sessions` `check_resource_access(...)` pattern (migration `migrations/ctx_war_room_schema.sql`). Cross-user sharing is not wired yet (register in `shareable_resource_registry` when it is).

## State — the `warRoom` slice (`redux/`)

Stores **only linkage + tile UI state** (sessions/tiles registries, audio links, ephemeral UI). Task data stays in the agent-context tasks slice, note data in `notes`, transcript data in `transcriptStudio`. Selectors are per-key memoized (`redux/selectors.ts`); thunks (`redux/thunks.ts`) are the only writers.

## Room UI (`components/room/` + `components/tile/` + `hooks/`)

The cockpit is one canonical build (the consolidation of a 4-way ui bake-off — winner `reimagine` + grafts from `dense`/`refine`/`sharp`):

- **`WarRoomShell`** — the frame + mission-control header. Routes the body to `StageView` or `WarRoomGallery` by `mode`.
- **`roomViewContext`** — the ONLY home for ephemeral room view state (`mode` stage/grid · `projectedTab` · `density` · the staged thread). React context, **never Redux, never persisted** — these are view preferences, not session data. `DENSITY_LAYOUT` maps the dial to gallery floors; `resolveStagedId` clamps the Stage to a visible thread so it can't strand.
- **Stage mode:** `StageView` (rail + stage) → `RailTile` rows (read-only watchlist, `PulseGlyph` + status word) and `StageTile` (the hero focus pane, full working state). Parked threads fold into a collapsible rail section.
- **Grid mode:** `WarRoomGallery` (the gallery engine + density floors) → `WarRoomTile` (operable card: accent rail + metric chips + segmented tabs + projector; double-click → Stage). Parked threads dock in `HiddenTilesTray`.
- **Shared tile primitives:** `TileTabBar` (segmented, kind-colored switcher, rendered from `TILE_KIND_ORDER`), `TileTabContent` (the 5 real bodies — Task/Notes/Audio/Files/Agent — + combined view; the single source both modes render; the Agent tab is deliberately excluded from the combined "All" view, too rich to stack), `TileMetricChips`, `TileOptionsMenu`, `PulseGlyph`, `ParkedThreadChip`, `tileKind` (semantic accent map aligned with route favicon families: task→success/green, notes→warning/yellow, audio→secondary/purple, files→info/light blue, project stacked section→primary/dark blue, agent→secondary, all→success).
- **Hooks (compose the real slices read-only, written once):** `useTilePulse` (live status word/headline/preview + is-alive signal), `useTileMetrics` (chip readings), `useTileActions` (rename/pin/hide/expand/delete/stage resolver). Stage, Grid, and parked chips all consume these — no forked tile logic.

## Reused primitives (do NOT rebuild)

- Layout: generic `computeGalleryLayout` (`lib/layout/galleryLayout.ts`) + `useGalleryLayout` (`hooks/useGalleryLayout.ts`) — extracted, reusable by any tiled workspace.
- Landing: `ModuleLanding`. Text inputs: `ProTextarea`. Confirms: `confirm()`.
- Task tab: `EditableTaskTitle`, task thunks (`createTaskThunk`/`createSubtaskThunk`/…), `TaskAttachments`, and `TaskCommentPopover` (new shared primitive in `features/tasks/`).
- Notes tab: `ProTextarea` + the notes autosave middleware (`createNote` API).
- Audio tab: the **real transcription-cleanup pipeline** — `<CleanupPad variant="embedded">` (chunked/crash-safe record → transcribe → auto-clean) bound to the tile's `source='war_room'` `studio_sessions` row via `sessionId`/`urlSync={false}`; the tile owns session lifecycle (switcher + `addAudioSessionToTile`), the pad is the sole transcript writer. Expand via `useOpenTranscriptStudioWindow`.
- Files tab (`TileAttachmentsTab`): the **file handler** (`@/features/files`) — `requestUpload` (upload to `folderForWarRoomTile`) + `openFilePicker` (attach existing), rows rendered via `InlineMediaRef` (durable, never a raw `<img>`); and the **document service** (`@/features/data-tables/document-service`) — `createDocument` + `listAccessibleDocuments`, rows open `/documents/[id]`. Display details hydrate client-side (`useFile` for files, `getDocument` for docs). Never reimplement upload/pick/doc-edit.
- Agent tab (`TileAgentTab` → lazy `TileAgentPanel`): the **real Scribe Agent+ panel**, reused unchanged — `AssistantAgentBar` + `WorkingDocumentHeader` + `ExperimentalAgentScreen` (`features/transcript-studio/components/scribe/*`) over hooks `useStudioAssistant` / `useAutoVoiceResponse` / `useWorkingDocumentDraft`, all keyed by a `studio_sessions.id`. `TileAgentTab` resolves the tile's session exactly like Audio (`selectActiveAudioSessionId` → `ensureTileAudioSession`) and passes `tileId` through; the panel is code-split (`next/dynamic` ssr:false) and re-fetches the working document on each completed turn to cover the single-active-session realtime gap. **The panel also feeds the assistant the tile's own work as READ-ONLY context every turn** — `tile_task` (+ subtasks), `tile_notes`, `tile_files` — via `warRoomAgentContext.ts#buildTileAgentContextEntries`, merged in through `useStudioAssistant`'s optional `buildExtraEntries` (so the shared Scribe studio context is untouched). The transcript is not duplicated (already `session_cleaned`/`all_raw`). **The assistant can also EDIT the tile's entities** via the `war-room-tools` family (`features/agents/war-room-tools/`) — `war_room_update_task` / `war_room_add_subtask` / `war_room_toggle_subtask` / `war_room_update_note` / `war_room_update_tile` — armed on THIS conversation only (the panel calls `setClientTools` + `registerWarRoomToolBinding(conversationId, tileId)`; Scribe never mounts the panel, so its conversation stays read-only). Every write requires the user's approval (a confirm AskCard) before it applies. Expand via `useOpenTranscriptStudioWindow` (same session as Audio). Nothing reimplemented.
- Context: `EntityTargetPicker` (org) + `EntityScopeTagger` (controlled) via `WarRoomContextPicker`.

## Invariants (load-bearing)

1. **Context is a controlled selection the records carry.** War Room pickers persist org/scope ONLY onto `ctx_war_room_*` rows (`setSessionContextThunk` / `setTileContextOverrideThunk`). They **never** write `appContextSlice` (global active context) or `ctx_scope_assignments`. `selectTileEffectiveContext` resolves `tile override ?? session default`. See [`features/scopes/FEATURE.md`](../scopes/FEATURE.md).
2. **A tile is the container; its tabs are thin consumers.** A tile = `{task_id?, note_id?, audio sessions[], attachments[]}`. Tasks/notes/transcripts/files/documents are owned by their features and survive tile/room deletion — detaching a file/doc removes only the link.
3. **The "new" tile is a render concern**, not a DB row, until the user captures into it.
4. **Room view state is ephemeral.** Stage/Grid mode, the projector, the density dial, and the staged thread live ONLY in `roomViewContext` (React context) — never Redux, never persisted, never on the record. The projector overrides which tab is *shown*, never the tile's saved `active_tab`. Don't reach for a slice for any of these.
5. **Desktop-first.** The gallery math assumes a wide viewport.
6. **The agent edits the tile only through HITL-gated, tile-bound tools.** The `war-room-tools` family (`features/agents/war-room-tools/`) mirrors `features/agents/ui-first-tools/` and is the ONLY way the assistant mutates a tile. Each tool is offered as an **inline** spec (no server-side registry change), **armed per-conversation** by `TileAgentPanel` (`setClientTools` + `registerWarRoomToolBinding`) so it exists ONLY on a War Room tile's Agent conversation, and **resolves its target tile from the binding** — never a guess (no binding ⇒ the dispatcher refuses with `no_tile_bound`). Every write **requires explicit user approval** (a confirm AskCard, reusing the ui-first `pendingAsks` surface) before it runs; on decline the agent gets a non-error "declined" result. Handlers call the **real feature writers** (tasks thunks, `notesApi`, `renameTile`) so Redux + every open surface update live — never a war-room-only mirror. Read `features/agents/ui-first-tools/FEATURE.md` for the client-delegated tool pattern this clones.
7. **The MASTER and ROOM agents act across threads by NOTIFY-AND-WATCH, not approve-each.** The `war-room-master-tools` family (`features/agents/war-room-master-tools/`) is the cross-thread tool set, armed on TWO surfaces: the master agent (`/war-room/all`, full set: `war_room_read_thread`, `war_room_message_thread` (`fresh`|`fork`), `war_room_create_room`, `war_room_rename_room`) and the TIER-2 room agent (in the room shell, the SAME set **minus `war_room_create_room`** — a room agent reads/messages its threads + renames its room but doesn't spin up rooms). Deliberately **NOT** HITL-gated (the per-tile invariant 6 model is the opposite, on purpose): each runs immediately, and messaging a thread fires a toast + opens a live-watch `WindowPanel` (the shared `warRoomWatch` slice → `MasterWatchLayer`, mounted on BOTH surfaces) so the user **sees it stream and steps in**. Safety = Zod-validate args + **resolve the target** (`service/threadResolver.ts` resolves a `thread_id` = tile id → its agent `conversationId`; rooms by id) and refuse cleanly (`ok:false`) on unknown — never guess. The dispatcher + resolver are **room-agnostic**; what scopes the room agent to its room is its single-room ROSTER (`buildRoomAgentContext`), not the tools. Armed per-conversation by `useMasterAgent` / `useRoomAgent` (`setClientTools(...)`, cleared on unmount). `fresh` = `createManualInstance` seeded with `buildTileAgentContextEntries` + the message; `fork` = `forkConversationServer` the thread's existing chain. All from existing primitives — **NO DB, NO server change.**
8. **Three tiers of agent, one widening lens.** A War Room exposes the same agent stack at three scopes, each reusing the canonical `AgentConversationColumn` + a durable conversation + a READ-ONLY roster (never a parallel store): **Tier 1 — per-thread** (`TileAgentPanel` → `useStudioAssistant`, the tile's Agent+ conversation, armed with `war-room-tools` to EDIT that ONE thread, HITL-gated — invariant 6); **Tier 2 — per-room** (`RoomAgentPanel` → `useRoomAgent(sessionId)`, one durable conversation PER ROOM in `localStorage` `war-room:room-agent:<sessionId>`, context `buildRoomAgentContext` = `room_agent_role` + `war_room_threads` (just this room's roster), armed with the master tools MINUS create-room — invariant 7); **Tier 3 — master, all rooms** (`MasterAgentPanel` → `useMasterAgent`, one conversation PER USER, context `buildMasterAgentContext` = `master_role` + `war_room_overview` (every room → its threads), full master tools — invariant 7). Tier 2 is literally Tier 3 narrowed to one session: the context builders share the per-thread shape (`MasterThreadEntry`) and the two agents share the dispatcher/resolver. The widening is roster scope, not new machinery.
9. **A room and its threads never hold CONFLICTING projects.** A tile's `flavor` (`thread`|`task`|`project`) is its render/intent discriminator; a `project` tile binds to a `ctx_projects` row (`tile.project_id`) and its Task tab is that project's task list. A room can ALSO carry a project (`session.project_id`). The invariant: **if the room has a project, every tile is NULL (inherits) or equal to it; per-thread projects (different projects on different threads) are allowed ONLY when the room has none.** Adding/assigning a thread on a DIFFERENT project than the room's raises `ProjectConflictDialog` with the two (and only two) coherent resolutions — **per-thread** (`convertRoomToPerThreadThunk`: stamp the room's project onto every existing tile, THEN clear the room's, give this thread its own) or **keep-room** (this thread joins the room's project). The mirror, "make the whole room one project", is `absorbRoomIntoProjectThunk` (clear differing tile projects + set the room's). Tasks created in any tile auto-associate via the app-wide `ctx_tasks.project_id` (`createTileTask` stamps `selectEffectiveTileProjectId` = `tile.project_id ?? session.project_id`) — War Room is a VIEW onto the project↔task link, never a parallel store. Decision logic is `checkTileProjectConflict` (sync) + `setTileProjectThunk(resolution?)`; the cross-org "pick any of my projects" picker is `WarRoomProjectPicker` (flat over `useUserProjects`, because the canonical `EntityTargetPicker kind="project"` is org-gated). **NO server change** (one idempotent migration: `migrations/ctx_war_room_tiles_flavor_project.sql`).

## Doctrine

- **Build the platform, not the artifact.** The gallery engine and `TaskCommentPopover` were extracted as reusable primitives, not buried here. Before adding a tile capability, check whether the substrate feature already exposes it.
- New routes/panels/overlays/components → add to the `/war-room/admin` map config.
- After substantive changes, update this doc + the Change Log.

## Known deferral

The deeper **transcription working-state Redux migration** (Wave 0) — lifting `useCleanupSession`'s local edit/run state into `transcriptStudio` so the embedded Audio pad and its expand overlay share **live in-progress clean edits** — is deferred. The Audio tab now embeds the full `CleanupPad` pipeline, so the *committed* transcript and the persisted *cleaned* output are both shared via DB + the studio slice; only a clean edit that is still being typed in one view (before its debounce persists) is unsynced to the other. Tracked for when concurrent live editing of the same session across both views is needed.

## Verify

Dev-login → `/war-room/all`. Create a room; add threads. **Stage mode:** rail shows every thread with a live status word; click a row → it stages with full state; the rail "new thread" auto-stages; parked threads collapse under the rail and restore-and-stage on click. **Grid mode:** grid re-flows at 1/2/3/8/12; the **density dial** packs/loosens it; double-click a card → Stage. **Projector:** set the whole room to one view, then clear it (saved per-tile tabs untouched). Pin/park + restore (rail section + Grid tray). Task (name/subtask/attachment/comment, persists), Notes (Text/Matrx-Split/Preview + single-layer in the All view; type → autosave → reload), Audio (embedded `CleanupPad`: record on a real mic → transcript persists → auto-clean appears in the Clean pane immediately, all on the tile's `war_room` session; New Session adds another; reload restores both panes), Files (Upload a file or attach an existing one → it lists with an image preview / type icon + open link; New document opens the editor in a new tab and links it; Add document picks an existing `udt_documents` row; remove detaches the link only; reload restores all rows), Agent (the real Scribe Agent+ panel: pick/switch the assistant agent, open and edit the working document, talk to the agent by text or voice — record a turn and choose send / transcribe via the action sheet, auto-voice reads replies aloud; record into the SAME session in the Audio tab and the agent sees those recordings as context), Context (session org/scope → threads inherit → reload persists; never mutates global). Expand opens each tab's full UI. Editable session + thread titles.

## Change Log

- 2026-06-21 — Bundle leak fix: both war-room route surfaces were statically importing `WindowPanel` for their inline, non-modal agent windows — `WarRoomShell` (`/war-room/[id]`) and `WarRoomAllView` (`/war-room/all`). Because both components sit in their route's boot graph, this collapsed the 100+ lazy window-panel chunks into the route bundle and tripped the window-panels bundle-leak guard (`[WINDOW-PANELS BUNDLE LEAK]`). Fix: extracted each inline `WindowPanel` wrapper into its own component that owns the static `WindowPanel` import — `RoomAgentWindow` (`components/room/`, wraps `RoomAgentPanel`) and `MasterAgentWindow` (`components/master/`, wraps `MasterAgentPanel`) — and the shell/view now load THOSE via `next/dynamic(ssr:false)` instead of statically importing `WindowPanel` themselves. No behavior change (same window size/dock/props); the agent column was already lazy, now the window chrome is too. `MasterWatchLayer` was already lazy in both surfaces and needed no change.
- 2026-06-16 — Grid-tile header density: the thread TITLE now always wins the header space fight, even at 12 tiles (previously it collapsed to nothing). Three moves, **nothing removed — only relocated**: (1) the leading kind icon is gone (the accent rail + the view dropdown's own icon already convey kind) so the title leads (pin glyph still shows when pinned); (2) the six-segment `TileTabBar` collapsed into one compact dropdown — new `TileTabSelect` (current view's icon + label + chevron, every tab one click away in the menu), replacing both the inline switcher and the old narrow-cell second tab row; (3) the per-tile context control moved into the `⋯` menu — `TileOptionsMenu` gained optional `onOpenContext`/`contextActive`, and `TileContextOverride` gained a controlled (`open`/`onOpenChange`) + `hideTrigger` (anchor-only `PopoverAnchor`) mode so the menu opens the same full org/scope picker. Grid-mode only; the Stage tile keeps the labeled segmented `TileTabBar` + inline context button (it has the room). No DB/server/state change.
- 2026-06-16 — `WarRoomProjectPicker` (the project-flavor thread picker) gained inline **Create new project** + a **refresh** control. Create opens the new app-wide `createProjectWindow` overlay (WindowPanel chrome over the shared `ProjectFormCore`) via `useOpenCreateProjectWindow`; its `onCreated` callback auto-selects the new project for the thread (an optimistic label holds until the nav-tree refetch lands, then `projects.find` takes over). Refresh re-pulls the cross-org list via `useUserProjects().refresh()`. No DB/server change. See `features/overlays/FEATURE.md` for the overlay.
- 2026-06-16 — Bug fix: project-flavor tile Task tab (`TileProjectTaskList`) now renders SUBTASKS nested under each top-level task, matching the projects page (`ProjectTaskList`). Previously it loaded parents-only (`loadProjectTopLevelTasks` → `getTopLevelProjectTasks`, `parent_task_id IS NULL`) so subtasks were invisible until you opened a task's editor. New generic tasks primitive `loadProjectTasks` thunk (over the existing `taskService.getProjectTasks` = ALL rows for a project) loads parents + children into the agent-context tasks slice; the list still reads top-level via `selectTopLevelTasksByProjectId`, and each row now reads its children via the existing `selectSubtasksByParent` and renders them as indented rows (`CornerDownRight`, `pl-7`, smaller text), respecting the `showCompleted` toggle. Subtask rows open in the same detail pane / floating window as parents. No DB/server change.
- 2026-06-16 — TIER-2 ROOM AGENT (one agent per room, sees ALL threads in THAT room — the master narrowed to a single session; completes the 3-tier agent model, new invariant 8). The room header (`WarRoomShell`) gained a **"Room Agent"** button (Lucide `Bot`) that opens a lazy (`next/dynamic` ssr:false) `RoomAgentPanel` inside an inline, **non-modal**, draggable `WindowPanel` (docked bottom-right, 460×620, id `war-room-room-agent-<sessionId>`) — local React state owns open/closed so the cockpit stays interactive. The panel REUSES the canonical `AgentConversationColumn` unchanged on `surfaceKey="war-room-room-agent"`. `features/war-room/hooks/useRoomAgent.ts` (clone of `useMasterAgent`, keyed PER ROOM) owns **one durable conversation per room** — id persisted in `localStorage` `war-room:room-agent:<sessionId>`, so each room has its own agent and switching rooms switches agents; same resolve recipe (reuse in-memory instance, else `createManualInstance({ conversationId })` + `loadConversation`, else mint + persist; `AUDIO_ASSISTANT_AGENT_ID` for v1), same in-flight dedupe (keyed by sessionId), same no-empty-push guard. It pushes READ-ONLY single-room context via `features/war-room/service/roomAgentContext.ts#buildRoomAgentContext(sessionId)` — `room_agent_role` (framing: you oversee THIS one room) + `war_room_threads` (a compact roster of just this room's threads, the SAME per-thread shape `MasterThreadEntry` the master uses), re-pushing when this room's tile set changes. The builder reuses the master builder's exact read path scoped by sessionId (`getSession` + `listTiles` + `list*ForTiles` + targeted `ctx_tasks`/`notes`/`studio_sessions.assistant_conversation_id` reads); owner-scoped via RLS; values carry no `mutable`/`source` ⇒ `ctx_get` only. **Tools:** ARMS the `war-room-master-tools` family MINUS `war_room_create_room` (read_thread + message_thread + rename_room — a room agent reads/messages its threads + renames its room but doesn't create rooms); the dispatcher (`dispatchWarRoomMasterTool`) + `threadResolver` are room-agnostic and work as-is, so the single-room roster is what scopes the agent to its room. **Live-watch:** the shared `warRoomWatch` slice + `MasterWatchLayer` (the same renderer) are now mounted in `WarRoomShell` too (lazy), so messaging a thread pops a live-watch window in the room as well; `closeAllWatches` on room-leave mirrors `/all` (only one room surface mounts at a time, so they never contend). Admin map + invariants 7/8 updated. **NO DB, NO server change** — assembled entirely from existing primitives.
- 2026-06-16 — War Room task lists (`TileProjectTaskList`, `SubtaskRail`) now honor the shared tasks `showCompleted` preference (`tasksUi.showCompleted`, default `false`): completed items are hidden by default; the header eye toggle reveals them crossed out. Same setting as `/tasks` sidebar and Quick Tasks.
- 2026-06-15 — Phase 5 (flavor foundation + project association — the create flow, the invariant, the room control): tiles gained `flavor` (`thread`|`task`|`project`) + `project_id` (FK → `ctx_projects`, set-null) via the idempotent migration `migrations/ctx_war_room_tiles_flavor_project.sql` (applied + live-verified + `db-types` regenerated). **Create flow:** `QuickAddThread` now leads with a segmented flavor picker (Thread default — Enter still fast-creates a thread; Task; Project); choosing Project reveals `WarRoomProjectPicker`. **The invariant (see invariant 9):** a room (`session.project_id`, which already existed) and its threads never hold conflicting projects — assigning a thread a different project than the room's raises `ProjectConflictDialog` (per-thread vs keep-room). Foundation in `redux/`: selectors `selectTileFlavor`/`selectTileProjectId`/`selectSessionProjectId`/`selectEffectiveTileProjectId`/`selectSessionProjectMode`; thunks `checkTileProjectConflict` (sync decision), `setTileFlavorThunk`, `convertRoomToPerThreadThunk`, `setTileProjectThunk(resolution?)`, `setRoomProjectThunk`, `absorbRoomIntoProjectThunk`, `createRoomFromProject`; `createTileTask` now stamps the tile's effective project so tasks auto-associate app-wide. The slice needed NO new reducers (`tileUpserted`/`sessionUpserted` carry the full rows). **Surfaces:** `RoomProjectButton` (header — tie the whole room to a project / clear, absorbing conflicts via `confirm()`), `NewRoomFromProjectButton` (`/all` "From project" → `createRoomFromProject` seeds a project room + one project tile, navigates in), `TileFlavorBadge` (quiet header marker for task/project tiles). **`WarRoomProjectPicker`** is a new flat, cross-org project picker (over `useUserProjects`) — live verification caught that the canonical `EntityTargetPicker kind="project"` is org-gated ("Select an organization first"), which can't express "pick any of my projects", so all three flows use the flat picker. Verified at the data layer: a from-project room persisted `session.project_id` + a seeded tile with `flavor='project'` and the matching `project_id`, `active_tab='task'`. **NO server change.**
- 2026-06-15 — Phase 5 (project-flavor tile Task tab): a `project`-flavored tile's Task tab now shows the PROJECT's task LIST (browse + fast-create + open) instead of the single-anchor `TaskEditor` that thread/task tiles use. `TileTaskTab` became a thin flavor-router (reads `selectTileFlavor`; `project` → new `TileProjectTaskList`, else the unchanged `TileTaskTabAnchored` = the old body). `TileProjectTaskList` resolves the project via `selectEffectiveTileProjectId` (graceful "No project linked" empty state when null), shows the project name (`selectProjectById`) + completion count as a header, a rapid Enter-to-add input (`createTaskThunk({ title, projectId })`), and a compact list (checkbox `toggleTaskCompleteThunk` + priority dot + title). Clicking a row opens the REAL `TaskEditor` in a detail pane beside the list on wide tiles (@[34rem]) / over it on narrow; "⋯ → Open in window" reuses `SubtaskWindow` (a generic TaskEditor-in-a-window). Three MINIMAL generic primitives added to the **tasks** feature (not war-room): `taskService.getTopLevelProjectTasks(projectId)` (ctx_tasks where `project_id` = $1 AND `parent_task_id IS NULL`, oldest-first — ctx_tasks hard-deletes, so no `is_deleted` filter), a `loadProjectTopLevelTasks` thunk loading them into the agent-context tasks slice at thin-list level via `upsertTaskWithLevel`, and a memoized `selectTopLevelTasksByProjectId` selector. No DB/server change.
- 2026-06-15 — Phase 4 messaging tools + live-watch + notify (master agent acts): the `war-room-master-tools` family (`features/agents/war-room-master-tools/`, cloned from `war-room-tools`) gives the master agent 4 cross-room tools — `war_room_read_thread` (read a thread agent's recent chain), `war_room_message_thread` (`mode:"fresh"|"fork"`), `war_room_create_room`, `war_room_rename_room`. **NOTIFY-AND-WATCH, not approve-each** (see invariant 7): tools run immediately; `war_room_message_thread` opens a live-watch window + toast. **Resolution:** `service/threadResolver.ts` maps a roster `thread_id` (= tile id) → the thread agent's `conversationId` (active audio session → `studio_sessions.assistant_conversation_id`, the same chain `masterAgentContext` builds), backed by a new `getTile(id)` single-tile reader in the war-room service. **fresh** = `createManualInstance({agentId: AUDIO_ASSISTANT_AGENT_ID, apiEndpointMode:"agent", allowChat:true})` → (load the tile's room if cold) → `setContextEntries(buildTileAgentContextEntries)` → `setUserInputText` → `executeInstance`; **fork** = `forkConversationServer` the thread's chain → `setUserInputText` → `executeInstance` on the fork. `executeInstance` awaits the full stream, so the handler reads the reply back from `selectConversationMessages` for a concise tool result. **Watch layer:** new ephemeral `warRoomWatch` slice (`openConversationIds` + `openWatch`/`closeWatch`) + `MasterWatchLayer` (mounted in `WarRoomAllView`, lazy) renders one inline non-modal `WindowPanel` per open id with `<AgentConversationColumn hideInput/>`, guard-hydrating cold convos via `loadConversation`. **Wiring:** additive inline-spec branch in `build-tool-injection.ts` (`isWarRoomMasterToolName` → `getWarRoomMasterInlineToolDef`) + routing branch in `surface-delegated-tool-call.thunk.ts` (→ `dispatchWarRoomMasterTool`, runs immediately, no `paused`); `useMasterAgent` arms the tools on the master conversation. **NO DB, NO server change** — assembled entirely from existing primitives; fulfills the "seam for the next build" the Phase 4 foundation entry below flagged.

- 2026-06-15 — Phase 4 foundation (Master agent over all rooms): `/war-room/all` gained a **"Master Agent"** header button that opens a lazy (`next/dynamic` ssr:false) `MasterAgentPanel` inside an inline, **non-modal**, draggable `WindowPanel` (docked bottom-right, 460×620) — local React state owns open/closed, so the rooms list stays visible + interactive. The panel REUSES the canonical `AgentConversationColumn` (composer + streaming) unchanged on `surfaceKey="war-room-master"`. `features/war-room/hooks/useMasterAgent.ts` owns **one durable conversation per user**: the id is persisted in `localStorage` (`war-room:master-conversation:<userId>`); on mount it reuses the in-memory instance, else recreates it via `createManualInstance({ conversationId })` + `loadConversation` to rehydrate, else mints + persists a fresh one (reusing `AUDIO_ASSISTANT_AGENT_ID` for v1 — a dedicated master agent/prompt is future polish; orchestrator behavior comes from the context + the tools later). It pushes READ-ONLY cross-room context via `features/war-room/service/masterAgentContext.ts#buildMasterAgentContext` with the SAME no-empty-push guard as `useStudioAssistant`, re-pushing when the room set changes. The builder returns `master_role` (framing) + `war_room_overview` — a compact **roster** (every room → its threads with `{ threadTitle, conversationId|null, status?, taskTitle?, noteSnippet?, hasAudio, fileCount }`), NOT full transcripts. Because Redux only holds the active room, the builder fetches its own data (war-room service `listSessions`/`listTiles`/`list*ForTiles` + targeted reads of `ctx_tasks` titles, `notes` snippets, and `studio_sessions.assistant_conversation_id` — queried directly since `studioService` excludes `source='war_room'`); owner-scoped via RLS; values carry no `mutable`/`source` ⇒ `ctx_get` only. **NO DB, NO server change** — assembled entirely from existing primitives. **Seam for the next build** (messaging tools + watch/notify): each thread's `conversationId` in `war_room_overview` is the handle a tool reads/messages; the master's own conversationId comes from `useMasterAgent`; tools bind per-conversation exactly like `TileAgentPanel` arms `war-room-tools` (`setClientTools` + a binding-registry entry).
- 2026-06-15 — Phase 3b (Agent EDITS the tile's entities): the `war-room-tools` family (`features/agents/war-room-tools/`) — a client-delegated tool family cloned from `features/agents/ui-first-tools/` — gives the tile's Agent+ assistant 5 WRITE tools: `war_room_update_task` (title/status/description/priority/due), `war_room_add_subtask`, `war_room_toggle_subtask`, `war_room_update_note` (replace/append the active note), `war_room_update_tile` (rename). Each = a Zod schema (`tools/schemas.ts`) + a handler (`handlers/*.handler.ts`) calling the **real** writers — `updateTaskFieldThunk`/`createSubtaskThunk`/`toggleTaskCompleteThunk` (tasks), `notesApi.update` + `upsertNoteFromServer` (notes), `renameTile` (war-room) — so Redux + the live tile UI update with no new mirror. **Attachment:** the tools are offered as **INLINE** specs (`tools/tool-defs.ts`; the names aren't in the server registry, so inline = zero backend change) — `build-tool-injection.ts` splits any armed `instanceClientTools` name that `isWarRoomToolName` into an inline spec, the rest stay `registered`. `TileAgentPanel` arms them on the tile's studio-assistant conversation ONLY (`setClientTools(WAR_ROOM_TOOL_NAMES)`) and binds the tile (`registerWarRoomToolBinding(conversationId, tileId)` — a module-level map mirroring `ask-resolver-registry`), clearing both on unmount; Scribe never mounts the panel, so its conversation is untouched. **Routing:** an additive `isWarRoomToolName` branch in `surface-delegated-tool-call.thunk.ts` → `dispatchWarRoomTool` (mirrors `dispatchUiFirstTool`): resolve the bound tile → validate args → flip to `paused` → **request approval** (a `confirm` AskCard via the reused ui-first `pendingAsks` + `ask-resolver-registry`, rendered by the `<PendingAsksZone>` already in the panel's conversation column) → on approve run the handler + `submitToolResult`; on decline/cancel submit a non-error `declined` result; "Other…" returns the user's typed redirection. No new UI, no server change, no war-room UX components touched.
- 2026-06-15 — Phase 3b (Agent SEES the tile's entities): the Agent+ panel now feeds its assistant three **read-only** context objects every turn — `tile_task` (title/status/priority/due/description **+ subtasks**), `tile_notes` (the active note's content + label), `tile_files` (a concise name+kind list of attachments) — so the agent can `ctx_get` the tile's own work, not just the working document. Priority emphasis in the descriptions: Tasks → Notes → Files. The audio transcript is deliberately NOT duplicated (it already arrives as `session_cleaned`/`all_raw`). Built as `features/war-room/service/warRoomAgentContext.ts#buildTileAgentContextEntries` (reads the real `tasks`/`notes`/`attachmentsByTile` slices; dispatches `loadTileSubtasks` so subtasks are complete; values carry no `mutable`/`source` ⇒ the server exposes only `ctx_get`, never `ctx_patch` — editing tools come later). Wired without polluting the shared Scribe studio context: `buildAssistantContextEntries` gained an OPTIONAL `extraEntries` param (default `[]` ⇒ Scribe byte-for-byte unchanged) and `useStudioAssistant` an OPTIONAL `{ buildExtraEntries }` it merges in (keeping the no-empty-push guard); `setContextEntries` merges by key, so the panel's co-mounted no-extras hooks never drop the `tile_*` keys. `TileAgentTab` now passes `tileId` → `TileAgentPanel`, which keys `buildExtraEntries` on the tile's task/note/file data so edits reach the agent without waiting for a recording.
- 2026-06-14 — Waves 1–5: schema + slice + landing + `/all`; gallery engine + tile system + pin/hide; Task/Notes/Audio/Combined tabs (all functional); session + per-tile context-awareness; expand overlays; this doc + `/war-room/admin` map.
- 2026-06-14 — Wave 6 (ui bake-off consolidation): the canonical room UI became a cockpit — **Stage⇄Grid** modes, a live watchlist rail (`StageView`/`RailTile`/`StageTile`), the **instrument projector**, a **Comfortable/Compact density dial**, live metric chips + accent rails on tiles, and a parked-thread chip treatment. New ephemeral `roomViewContext`; new `useTilePulse`/`useTileMetrics`/`useTileActions` hooks and shared tile primitives (`TileTabContent`/`TileMetricChips`/`TileOptionsMenu`/`PulseGlyph`/`tileKind`). Removed `TileFrame` (its chrome is now inlined in the richer tiles) and the four `_bakeoff/` variants + `/war-room/bakeoff/*` routes. Parity preserved: editable titles, options menu, icon tab switcher, all four tab bodies, note modes, context controls, pin/park/restore, expand overlays, real loading/empty/not-found.
- 2026-06-14 — Wave 7 (whole-feature subagent verification): a verification pass over the entire feature (routes, slice, thunks, selectors, tile system) confirming the cockpit's flows end-to-end.
- 2026-06-14 — Wave 8 (UX refinements): editable session + thread titles; the "All" (combined) view became one single-layer scroll column (no nested/double editors); a 3-mode notes switcher — **Text / Matrx-Split / Preview**; tab icons moved inline into the tile header (the segmented `TileTabBar`), reclaiming vertical space.
- 2026-06-14 — Audio tab → real pipeline: replaced the fake recorder (raw single-segment write, no cleanup) with the embedded `CleanupPad` (record → transcribe → auto-clean, "the clean version is immediately available"). Made `CleanupPad` embeddable via OPTIONAL props (`sessionId`, `urlSync`, `variant="embedded"`, `sections`, `showNewSession`) and `useCleanupSession({ sessionId, urlSync })` — the `/transcripts/cleanup` page is byte-for-byte unchanged (all defaults = today). The tile binds the pad to its `war_room` session and keeps owning lifecycle (switcher + New Session); deleted the now-dead `saveTileTranscript` thunk (the pad is the sole writer). Cleanup auto-runs with the sidebar hidden because the Clean agent resolves from the surface-role default, not the dropdown.
- 2026-06-15 — Phase 3 (Agent tab): a sixth tile tab that ports the real Scribe **Agent+** collaboration panel into the tile — `AssistantAgentBar` + `WorkingDocumentHeader` (+ `FocusedDocumentEditor`) + `ExperimentalAgentScreen` (+ `RecordActionSheet`), reused **unchanged** from `features/transcript-studio/components/scribe/*` over hooks `useStudioAssistant`/`useAutoVoiceResponse`/`useWorkingDocumentDraft` (no chat/doc/voice reimplemented). New `TileAgentTab` (resolves the tile's studio session via `selectActiveAudioSessionId` → `ensureTileAudioSession`, same as Audio, so the tile's recordings are the agent's transcript context) + lazy (`next/dynamic` ssr:false) `TileAgentPanel` (composes the three Scribe pieces sans the mobile page shell; re-fetches the working document on each completed turn to cover the single-active-session realtime writeback gap in multi-tile rooms). `tileKind` gains an `agent` (secondary accent, `Sparkles`) entry; `TILE_KIND_ORDER` picks it up so `TileTabBar` + the instrument projector render it automatically; `useTileActions` expand opens the full Scribe studio for the shared session. Deliberately excluded from the combined "All" view. DB: `migrations/ctx_war_room_tiles_active_tab_agent.sql` widens the `active_tab` CHECK to add `'agent'`.
- 2026-06-18 — **Copy for AI on project tile:** `TileProjectTab` overview adds `ProjectCopyForAiButton` — exports full project context (tasks, subtasks, comments, notes) via shared `aiExportService`.
- 2026-06-15 — Phase 2 (Files & Documents tab): a fifth tile tab backed by the new polymorphic `ctx_war_room_tile_attachments` link table (`entity_type` ∈ `user_file`/`document`). Files reuse `@/features/files` (`requestUpload` → `folderForWarRoomTile`, `openFilePicker`, `InlineMediaRef`); documents reuse `@/features/data-tables/document-service` (`createDocument`/`listAccessibleDocuments`, open `/documents/[id]`). New `TileAttachmentsTab` (+ a compact form for the All view); slice `attachmentsByTile` map with `attachmentsLoadedForTile`/`attachmentUpserted`/`attachmentRemoved`, `selectAttachmentsForTile`, service `listTileAttachments`/`attachToTile`/`detachFromTile`/`listAttachmentsForTiles` (batched into `loadWarRoomSession`), thunks `loadTileAttachments`/`attachFileToTile`/`attachDocumentToTile`/`detachTileAttachment`. `tileKind` gains a `files` (muted accent) entry; `TILE_KIND_ORDER` picks it up so `TileTabBar` renders it automatically. No upload/pick/doc-edit reimplemented.
- 2026-06-14 — UI bake-off consolidation (cockpit cleanup): consolidated the cockpit on the bake-off winner — Stage⇄Grid, the instrument projector, the density dial, the live active/parked/pinned meter, kind accent-rails, metric chips, and the parked-thread rail. Audit cleanup: removed the dead Redux UI/save-state cluster (`tileSaveState`, `ui`, `SaveStatus`, their reducers + selectors, `selectActiveSession*`, `selectWarRoomSlice`); trimmed unused `TileActions.unhide`/`setTab`; single-sourced the gallery tuning constants into `DENSITY_LAYOUT.comfortable`; honest tab semantics (`aria-pressed` over a disconnected `role="tablist"`) + keyboard focus rings + `aria-label`s on icon-only context triggers.
- 2026-06-17 — Combined "All" view section boundaries: removed the duplicate sticky label row + interior tints; each stacked block is now only a 2px top accent + full-height left rail (`combinedSectionKind`, flavor-aware project→primary). `tileKind` accents realigned to app route colors (task/green, notes/yellow, audio/purple, files/light blue). `TileAudioTab` gained `compact` (drops the "Audio" label in All; session controls stay). Rail layout switched to flex column then `border-l-[3px]` on section/tile wrappers (opaque bodies no longer clip the spine). Task tab: `TaskEditor` `footerAppend` merges the subtasks toggle into the single bottom bar. Agent tab: tile cards use `border-l-[3px]` + `sectionBorder` for full-height spine; grid tiles pass `compact` into Scribe Agent+ (`ExperimentalAgentScreen` 36px controls vs 64px, tighter chrome).
- 2026-06-17 — Audio tab compact toolbar (grid + combined): merged four non-scrolling header bands into one `h-7` row — session dropdown (`1/3`) · `+` · icon-only Controls/Custom · compact mic · save-only. `CleanupPad` gained `compact` + `embeddedHeaderSlot`; grid dedicated audio tab now passes `compact` via `tileLayout="grid"`. Pane headers shrink to `h-7` in compact embedded mode.
- 2026-06-17 — Task tab compact flush layout: `TaskEditor` gained `compact` — drops `px-4` body gutter, full-bleed property cards, `px-0` embedded chrome; wired from grid tiles + combined "All" via `TileTaskTab compact`.
- 2026-06-17 — Task property rows (compact): Assignee/Context/Priority/Due become fixed `h-7` inline rows (`pl-1.5` · icon · label · value), no dividers or card chrome; pickers switch to dense/borderless variants.
- 2026-06-17 — Task description → `ProTextarea` with `floatingLabel="Description"` (drops section header) + voice dictation; auto-grow bounds tighter in compact tiles.
- 2026-06-17 — Task label chips: compact text toggles (`rounded-md`, `gap-1`, `px-2 py-1`) with leading Tag icon only — no separate "Labels" header row.
- 2026-06-17 — Subtasks densified: `h-7` rows, `pl-1.5` section label, Enter chains focus in add input (`TaskEditor` + `SubtaskRail`).
- 2026-06-17 — Section headers (`SectionHeader`, Attachments, Associated resources, Advanced) gain `pl-1.5`; comments → `ProTextarea` with `floatingLabel="Comment"`. `OrgResourceRoleSection` role titles (Sources, Outputs, …) aligned with `pl-1.5`.
- 2026-06-17 — Notes tab single toolbar: `TileNotesToolbar` merges session + editor mode chrome into one `h-7` row (icon · Text/Matrx Split/Preview · + New); drops double header.
- 2026-06-17 — Blue-on-blue accent fix: removed `bg-primary/10` + `text-primary` pairings across war-room chrome (header icons, agent toggles, flavor badge, pulse glyph, instrument projector) and embedded `CleanupPad` chips — icons use `text-primary` only; selected controls use `text-primary border border-primary/70`.
- 2026-06-17 — **Project tab drill-down + editable project fields:** project-flavored tiles use a project-first tab (`TileProjectTab`) with inline `ProjectInlineEditors` (name, description, status, priority, dates) and a task list below. Clicking any task drills into the shared `TileEmbeddedTaskView` (`TaskEditor` + breadcrumb back); subtasks drill the same way. Task-flavored tiles use the same drill stack for subtasks (`onOpenLinkedTask` on `TaskEditor`) instead of only opening the overlay. Primitives: `useTaskDrillStack`, `TileDrillHeader`, `TileEmbeddedTaskView`.
