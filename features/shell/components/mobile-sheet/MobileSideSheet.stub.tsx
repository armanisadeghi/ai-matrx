/** Build A/B stub — real MobileSideSheet aliased away when FORCE_EXCLUDE_SIDEMENU. */
export default function MobileSideSheetStub(_props: {
  isAuthenticated?: boolean;
  pathname?: string;
}) {
  return null;
}
