/** Build A/B stub — real Sidebar is aliased away when FORCE_EXCLUDE_SIDEMENU. */
export default function SidebarStub(_props: {
  pathname?: string;
  isAuthenticated?: boolean;
}) {
  return null;
}
