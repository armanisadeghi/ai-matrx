/**
 * Visual contract for rows rendered by a Large Route menu. Keeping these next
 * to the registry prevents route-owned menus from drifting away from the shell
 * nav as new consumers are registered.
 */
export const ROUTE_MENU_NAV_ITEM_CLASS =
  "shell-nav-item shell-nav-stable shell-tactile-subtle";
export const ROUTE_MENU_ICON_SIZE = 18;
export const ROUTE_MENU_ICON_STROKE_WIDTH = 1.75;
