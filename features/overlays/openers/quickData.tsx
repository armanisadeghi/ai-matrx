"use client";

/**
 * Opener for the `quickData` overlay.
 *
 * - `useOpenQuickDataSheet()` — imperative hook. Call to open with typed options;
 *   returns a handle with a `close()` method.
 * - `<QuickDataSheetController />` — declarative wrapper. Mount to open,
 *   unmount to close. Equivalent ergonomics to rendering a normal component.
 *
 * Generated by scripts/generate-overlay-controller.ts (`openers` mode).
 * Hand-edit freely after the initial seed; the script will not re-emit a
 * file that already exists unless invoked with --force.
 */

import { useCallback, useEffect } from "react";
import { useAppDispatch } from "@/lib/redux/hooks";
import { closeOverlay, openOverlay } from "@/lib/redux/slices/overlaySlice";

const OVERLAY_ID = "quickData" as const;

export interface OpenQuickDataSheetOptions {
  className?: string;
  initialTableId?: string | null;
}

export interface QuickDataSheetHandle {
  close: () => void;
}

export function useOpenQuickDataSheet() {
  const dispatch = useAppDispatch();
  return useCallback(
    (opts: OpenQuickDataSheetOptions = {}): QuickDataSheetHandle => {
      dispatch(
        openOverlay({
          overlayId: OVERLAY_ID,
          data: {
            className: opts.className,
            initialTableId: opts.initialTableId,
          },
        }),
      );
      return {
        close: () => dispatch(closeOverlay({ overlayId: OVERLAY_ID })),
      };
    },
    [dispatch],
  );
}

/**
 * Declarative form. Renders nothing visible; opens the overlay on mount,
 * closes it on unmount. Use this when a caller wants to express overlay
 * state declaratively (the way they'd render a normal component).
 */
export function QuickDataSheetController(props: OpenQuickDataSheetOptions): null {
  const open = useOpenQuickDataSheet();
  useEffect(() => {
    const handle = open(props);
    return () => handle.close();
  }, [open, props.className, props.initialTableId]);
  return null;
}
