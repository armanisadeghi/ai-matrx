/** Shared menu identity for every `matrx-user/html-page` mount point. */
export const HTML_PAGE_CONTEXT_MENU_PROPS = {
  sourceFeature: "cms" as const,
  surfaceName: "matrx-user/html-page" as const,
};
