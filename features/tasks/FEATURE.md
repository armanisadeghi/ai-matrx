# FEATURE.md — `tasks` + `projects`

**Status:** `active` — both features in production
**Tier:** `2`
**Last updated:** `2026-08-07`

> Combined doc. **Projects and Tasks are first-class _containers_** (like orgs and scopes): nearly every resource table carries both a `project_id` and a `task_id` column, so "what belongs to this project/task" is a direct FK query — the same shape as the org workspace's `organization_id`. Tasks nest under projects (`project_id`) and under each other (`parent_task_id`). They share the org-scoped architecture documented in [`features/scopes/FEATURE.md`](../scopes/FEATURE.md).

---

## Purpose

Org-scoped project management. Projects group work within an organization; tasks are todos with metadata (status, assignment, timestamps, attached conversations, attached transcripts, etc.) belonging to a project. The scope chain `org → project → task` is a central axis of the [scope system](../scopes/FEATURE.md).

---

## Entry points

**Routes (canonical, top-level — un-nested from orgs)**

- `app/(core)/projects/page.tsx` — **ProjectsHub** launcher; `?org=<slug|id>` / `?scope=<id>` filtered views. Self-fetches `ctx_projects` (RLS-filtered), not nav-tree-dependent.
- `app/(core)/projects/[projectId]/page.tsx` — **ProjectWorkspace** (resolves by slug or UUID): hero + nested task list + associated resources + scopes + advanced references.
- `app/(core)/projects/[projectId]/settings/page.tsx` — **ProjectManage** (single-page sectioned, no tabs): General / Scopes / Members / Invitations / Danger.
- `app/(core)/tasks/page.tsx` — `TasksDesktopShell` (3-pane); `app/(core)/tasks/[id]/page.tsx` — `TaskEditor`.
- **Legacy redirects:** `app/(core)/organizations/[orgId]/projects/**` → `/projects?org=` and `/projects/[id]`; `(transitional)/settings/projects` → `/projects`. `(transitional)/projects/**` removed.

**Feature code — `features/projects/`**

- `service.ts` (canonical CRUD + members + invitations + `getProjectReferences`), `hooks.ts`, `types.ts`
- `components/ProjectWorkspace.tsx`, `ProjectsHub.tsx`, `ProjectManage.tsx`, `ProjectTaskList.tsx` (new), plus `GeneralSettings`/`MemberManagement`/`InvitationManager`/`DangerZone`/`ProjectReferencesPanel`/`ProjectFormSheet`/`CreateProjectModal` (reused)
- `README.md` — user-facing guide

**Feature code — `features/tasks/`**

- `components/` (incl. `TaskEditor.tsx`, `TasksDesktopShell.tsx`, `TaskAssociatedResources.tsx` (new)), `hooks/`, `services/taskService.ts`, `utils/`, `types/`
- `redux/` — `taskUiSlice`, `selectors`, `thunks`, `taskAssociationsSlice` (M2M engine: `associateWithTask`/`dissociateFromTask` — UI panel pending)
- `widgets/` — task widgets
- `agent-context/buildTasksContextData.ts` — surface wiring for `matrx-user/tasks` (manifest `features/surfaces/manifests/tasks.manifest.ts`). Pure `buildTasksContextData(...)` maps the active task → `createTasksScope(...)`; `TASKS_CONTEXT_MENU_PROPS`; `createTasksExtraSections(...)`. Consumed by `TaskEditor.tsx` (v3 `EditableContextMenu` on the description editor, `NonEditableContextMenu` on the read-only comment thread; `ProTextarea` description carries `surfaceName` + `getApplicationScope`).

**The container primitive (shared, in `features/organizations/`)**

- `hooks/useContainerInventory.ts` — counts catalogue resources for any container by `{column: organization_id|project_id|task_id, value}`. `useOrgResourceInventory` delegates to it.
- `components/ContainerResourceSheet.tsx` — lists a kind's FK-linked items with peek/open (reused by project workspace + task editor).
- `resource-catalogue.ts` + `OrgResourceRoleSection.tsx` (now `onContribute` optional) — role-grouped tiles.

---

## Data model

**DB tables** (Supabase, project `txzxabzwovsujtloxrus`):

- `ctx_projects` — `id`, `organization_id` (FK → organizations; non-null after the personal-org backfill), `name`, `slug`, `description`, `created_by`, `settings`, timestamps. **No `is_personal` column** — a project is "personal" iff its owning org's `organizations.is_personal` is true (derive via the org, or read RPC-derived `NavProject.is_personal`).
- `workspace.tasks` (formerly `ctx_tasks`; reached via `workspaceDb(supabase)`) — `id`, `title`, `description`, `project_id`, `parent_task_id` (subtasks), `status`, `priority` (enum), `due_date`, `assignee_id`, `organization_id`, `visibility`, `settings`, plus the 2026-08 upgrade columns below.

### Task system (2026-08 world-class upgrade)

- **Lifecycle** — `status ∈ inbox | planned | active | completed | cancelled | dismissed` (DB CHECK; legacy `incomplete` still accepted from old clients and normalized on read). Canonical vocabulary + `normalizeTaskStatus`/`isClosedStatus` + `TASK_STATUS_META` live in `features/tasks/constants/status.ts` — NEVER compare raw status literals at a callsite. `completed_at` is stamped/cleared by `taskService.updateTask`. Editor control: `TaskStatusPicker` (pill/segmented) + read-only `TaskStatusChip`.
- **Provenance** — `origin` (`user|agent|system`), `source_type`, `source_id`, `source_url` (deep link), `source_label`, `dedupe_key` (unique per org among live rows). Rendered by `TaskProvenanceChip` in the editor. **System-created tasks go through the idempotent primitive**: `taskService.upsertSystemTask` → `public.wsp_upsert_system_task` RPC (create-once per dedupe key; never re-opens a task the user resolved) and `taskService.resolveSystemTask` → `wsp_resolve_system_task` (feature completes/cancels its own task when the underlying work resolves). Any feature may call these — that is THE way to put work in front of a user.
- **Time controls** — `start_date`, `due_time`, `timezone`, `recurrence_rule` (RRULE subset incl. `BYMONTHDAY` (1–31 or -1 = last day), `features/tasks/utils/recurrence.ts`; editor: `TaskRecurrencePicker`; jest suite `utils/__tests__/recurrence.test.ts`), `reminders` jsonb (`TaskReminder[]`). **Completing a recurring task rolls `due_date` forward and reopens as `planned`** (Todoist semantics) — `taskService.completeTask` is the canonical completion path; the toggle thunk reconciles with the server row. On the first roll of a MONTHLY/YEARLY rule, `completeTask` stamps the original day-of-month anchor into the rule (`ensureMonthDayAnchor` → `BYMONTHDAY`) so month-end never drifts after a short-month clamp (Jan 31 → Feb 28 → Mar 31, not Mar 28).
- **Per-user notification state** — `workspace.task_user_state` (`seen_at`/`acknowledged_at`/`snoozed_until`/`dismissed_at`/`pinned_at`, PK `(task_id,user_id)`, RLS own-rows). Client chokepoint `features/tasks/services/taskUserStateService.ts`; hydrated into `taskUiSlice.userState` by `loadTaskUserStateThunk` on /tasks mount. Snoozed tasks vanish from attention views until expiry; pinned tasks float to the top. UI: `TaskSnoozeButton`. Snooze expiry (and every other "now" derivation — overdue, Today/Upcoming windows) re-evaluates within ~60s via the `tasksUi.nowMinute` selector input, ticked by `useNowMinuteTick` (mounted in `TasksDesktopShell`) — never by capturing `new Date()` inside a memoized selector.
- **Smart views** — Inbox / Today / Upcoming / Overdue / Assigned to me / Created by me / Completed, declared ONCE in `features/tasks/constants/smartViews.ts` (predicate registry) and consumed by the sidebar Views section (live counts via `selectSmartViewCounts`, org-context-scoped) and the `selectFilteredTasks` pipeline (`taskUiSlice.smartView` axis). Selecting a view widens to all projects; drilling into a project resets the view.
- **Reminder delivery** — Vercel cron (`vercel.json` → `/api/cron/due-date-reminders`, daily 15:00 UTC, `CRON_SECRET`-gated) emails due-today/overdue/upcoming via `lib/email/notificationService.sendDueDateReminderEmail`, respecting per-user snooze/dismiss and a 3-emails-per-user-per-run cap.
- **Hydration** — `get_user_full_context` emits the new fields at thin-list level and includes closed tasks from the last 90 days (the Completed view is client-side).
- **Project membership + invites** → canonical `iam.memberships` / `iam.invitations`, reached only via `membershipsService` (`mbr_*` RPCs) / `invitationsService` (`inv_*` RPCs) in `features/organizations/service/`. The legacy `ctx_project_members` / `ctx_project_invitations` junctions are no longer read by app code (2026-06-25 cutover).
- **Comments** → `platform.comments` (canonical, threaded; the `cmt_*` RPCs via `features/comments/service/commentsService.ts`). The legacy `ctx_task_comments` junction is no longer read by app code.
- **Attachments** → a `user_file → task` edge in `platform.associations` (`associationsService` to write; the `get_task_associations` `files` array to read). The legacy `ctx_task_attachments` junction is no longer read by app code.
- **Assignments** → the primary assignee lives on `ctx_tasks.assignee_id` (no junction; the legacy `ctx_task_assignments` table is unused).
- **Generic task↔entity M2M** → canonical `platform.associations`. Reads use `get_task_associations` (denormalized preview bundle) / `get_tasks_for_entity`; writes use the generic `assoc_*` primitives via the `associationsService` chokepoint. **`associate_with_task` + `create_task_with_association` were GRAVEYARDED 2026-06-29** (dropped — `migrations/graveyard_buggy_task_assoc_rpcs.sql`): both hand-rolled a 4-column `ON CONFLICT` that matched no unique index (the real key is the 5-tuple incl. `role`) and threw `42P10`. `taskAssociationsSlice`'s `associateWithTask` thunk now calls `associationsService.add` (entity→task); `createTaskWithAssociation` inserts the task via the task service then wires edges through the chokepoint (task creation no longer aborts on an edge failure — edge errors are loud, not fatal). `dissociate_from_task` / `create_tasks_bulk` unchanged. The legacy `ctx_task_associations` table is retired to the `graveyard` schema.
- `getProjectReferences(projectId)` RPC — every table FK-referencing a project (`{schemaName, tableName, columnName, rowCount}`)

> All six legacy `ctx_*` junctions (`ctx_scope_assignments`, `ctx_task_associations`, `ctx_project_members`, `ctx_project_invitations`, `ctx_task_comments`, `ctx_task_attachments`, `ctx_task_assignments`) have been moved to the `graveyard` schema; no app code or live RPC reads them. Final `DROP` is pending explicit sign-off.

**Container insight:** nearly every resource table carries both `project_id` and `task_id`. FK-association is therefore universal and read via `useContainerInventory`. Projects/tasks are also scopeable (scope tags via `platform.associations`, entity types `project`/`task`) and counted in the org resource catalogue.

---

## Key flows

### Flow 1 — Create a project in an org

1. User (with org permission) creates a project → row inserted with `organization_id`.
2. The project insert trigger atomically grants the creator the canonical `iam.memberships` owner role; the client must not follow the insert with `mbr_add(owner)` because owner establishment is bootstrap-only.
3. Further members are added via project invitations.

### Flow 2 — Create a task

1. User in a project creates a task → row inserted with `project_id`.
2. Task inherits org scope via join.
3. Conversations / transcripts / notes attached to the task stamp `task_id` on their own rows.

### Flow 3 — Task widgets

1. Task detail renders registered widgets (`features/tasks/widgets/`).
2. Widgets are mini-components that read/write against the task row or related data.
3. Extensible — new widgets register in the widget system without touching core task rendering.

### Flow 4 — Agent invocation scoped to a task

1. User runs an agent / shortcut from within a task context.
2. `appContext.taskId` is set; `assembleRequest` carries `scope.task_id`.
3. Conversation stamped with `task_id`; broker lookups resolve at task level first.

### Flow 5 — Invite a user to a project

1. Project owner / admin sends an invitation to an email.
2. The canonical invitation service writes `iam.invitations`; email is sent via `features/email/`.
3. Acceptance atomically adds the canonical `iam.memberships` row.

---

## Invariants & gotchas

- **Every task has a project; every project has an org.** Personal projects belong to the user's real personal organization.
- **Scope inheritance is explicit, not derived at the client.** Always carry the full scope chain on requests.
- **RLS enforces access.** UI-level filtering is for UX only.
- **Widgets render additively** — a failing widget must not break task detail.
- **Conversations attached to tasks carry the full scope chain** on `cx_conversation`. Don't re-stamp.
- **Project slugs / URL identifiers** — verify uniqueness scoping (per-org or global) before building routes that depend on them.

---

## Related features

## Notifications — channels and placement (deliberate decision)

Three channels, all frontend-side because ALL delivery infra (Resend email in `lib/email/`, DM inserts, preference tables) lives here; aidream has no notification service. Revisit (move firing onto aidream's `sch_*` spine) only if that changes or reminders need sub-daily granularity.

1. **Email** — `lib/email/notificationService.ts`, preference-gated (`users.user_email_preferences.task_notifications`).
2. **In-app DM** (canonical in-app channel) — `lib/services/system-dm.ts` `sendDm()` (sender = user or the Matrx System bot `system@aimatrx.com`); action chips via `features/messaging/actions/messageActionRegistry.tsx` `task_reminder` kind (Open / Complete — recurrence-aware / Snooze 1d, all inline). Senders: task assignment (`app/api/notifications/task-assigned`), the reminder cron (ONE volume-aware DM per user per run: single task → actionable chips; several → digest + `open_link` to /tasks).
3. **Reminder cron** — `app/api/cron/due-date-reminders/route.ts`, `vercel.json` daily 15:00 UTC, main deployment only, fail-closed on missing `CRON_SECRET` (set on Vercel prod 2026-08-07), snooze/dismiss-aware (`task_user_state`), 3-emails-per-user cap.

Forward work order: [docs/handoffs/tasks-world-class.md](../../docs/handoffs/tasks-world-class.md).

## Change log

- `2026-08-09` — **Page-layer assist chips on /tasks.** `tasks-assists-producer.ts` performs a deterministic overdue-pileup sweep and renders the canonical `AssistStrip` through `TasksAssistStrip`, using the swappable `tasks.triage_assistant` slot after task user-state has loaded so snoozes are respected.
- `2026-08-09` — **D129 lifecycle gaps closed**: `taskUiSlice.operatingTaskId` single slot → `operatingTaskIds` set (`addOperatingTaskId`/`removeOperatingTaskId`, `selectIsTaskOperating`) so concurrent per-task ops never clobber each other's in-flight guard; `nowMinute` ~60s tick input (`useNowMinuteTick`) into `selectFilteredTasks`/`selectSmartViewCounts`/`buildSmartViewContext` so snooze expiry and date windows resurface without an unrelated store change; monthly/yearly recurrence keeps its month-end anchor via `BYMONTHDAY` stamping (`ensureMonthDayAnchor`) + honor in `nextOccurrence`, with a jest suite.
- `2026-08-08` — **Task editor is agent-writable.** `matrx-user/tasks` declares 8 ask-policy `writeTargets` (title/description/status/priority/due-date/labels stage into the `patchTaskEdit` draft; `add_subtasks`/`save_task` run the canonical thunks); handlers on `TaskEditorBody`'s SurfaceRuntimeProvider validate-and-throw. An agent on the surface gets the `apply_surface_write` tool automatically; every write is confirmed in place. Live-verified. Recipe: `surface-write-targets` skill.
- `2026-08-07` — **DM notification integration**: shared `system-dm.ts` primitive (feedback notifier refactored onto it), `task_reminder` action chips verified live (Snooze writes `task_user_state`), assignment DMs, volume-aware cron digest DMs from the Matrx System bot. Placement decision documented above.
- `2026-08-07` — **World-class task system upgrade** (branch `feat/tasks-world-class`): full lifecycle vocabulary + backfill, provenance + idempotent system-task RPCs, time controls (start/due-time/recurrence/reminders), `workspace.task_user_state` (snooze/ack/pin), smart views with live counts, editor Status/Start/Repeat rows + provenance chip + snooze, reminder cron actually scheduled (was orphaned), `updateTaskLabels` settings-clobber fixed. See "Task system (2026-08 world-class upgrade)" above.
- `2026-07-28` — **Task descriptions are spacious and manually resizable.** The canonical full task editor now opens its description field at 240px and preserves the browser's vertical resize handle. Compact embedded task tiles retain their bounded auto-grow behavior. The shared `ProTextarea` primitive now merges caller styles with its `minHeight` / `maxHeight` contract instead of letting a caller's unrelated inline style silently erase those bounds.
- `2026-07-21` — **Project creation follows the database-owned membership bootstrap contract.** Removed the stale post-insert `mbr_add(owner)` call from canonical `createProject`; the project insert trigger already creates the creator's `iam.memberships` owner row atomically, and the hardened RPC correctly rejects a second owner-establishment attempt. Slug availability now uses zero-or-one-row `maybeSingle()` semantics with loud, conservative error handling instead of producing `PGRST116` for an available slug.
- `2026-07-19` — **Project invitation mutations no longer mount a hidden second list query.** `useProjectInvitationOperations` is mutation-only; `Project InvitationManager` owns the visible query and performs one refresh after a successful invite/cancel/resend. This mirrors the organization fix and removes duplicate manager-only `inv_list` traffic.
- `2026-07-10` — **Header Surface Agents chrome: live scope.** `TaskEditorBody` mounts `SurfaceRuntimeProvider` (`matrx-user/tasks`) so the shell Agents button Runs with the same description-selection scope as the context menu.

- **Depends on:** `features/organizations/` (parent scope), `features/scope-system/`, `features/sharing/` (share grants orthogonal to membership), `features/invitations/` (and its `project_invitations` extension)
- **Depended on by:** `features/agents/` (task-scoped invocations), `features/transcripts/` (task-attached transcripts), `features/notes/` (project-scoped notes)
- **Cross-links:** [`../scopes/FEATURE.md`](../scopes/FEATURE.md), [`../organizations/FEATURE.md`](../organizations/FEATURE.md)

---

- `2026-08-09` — claude: **`ProjectsHub`'s Cards/Table toggle moved onto `useListViewPrefs`** (`surfaceKey` `projects-hub`). The local `ViewMode` union, the `projects-view` localStorage key, and its `useState`/`useEffect` pair are deleted; default is unchanged (cards) and the choice syncs across devices via `userPreferences`.
- `2026-08-09` — claude: **`TaskListPane`'s List/Table toggle moved onto `useListViewPrefs`** (`surfaceKey` `tasks-list-pane`). The local `ListViewMode` union, the `tasks-list-view` localStorage key, and its `useState`/`useEffect` pair are deleted; the grouped list is now the canonical `rows` view and table is `table`, so the choice syncs across devices via `userPreferences`. Default is unchanged (grouped list).
- `2026-07-06` — **Quick Tasks window panel: project optional + hierarchy hydration + sidebar/detail UX.** `QuickTasksWorkspace` no longer blocks task creation or listing on a selected project — tasks can be created with `project_id = null`; opening the window calls `useNavTree()`/`useEnsureHierarchyLoaded()` to fetch hierarchy when idle, defaults to all tasks (restores `/tasks` view state on close), and filters by the window's org picker instead of requiring a project. Sidebar default width 300px (was 200). `CompactTaskItem` gained `layout="stacked"` (2-line title clamp, full-width meta row below checkbox row) — Quick Tasks window only; `card` layout unchanged elsewhere. `TaskDetailsPanel` gained `titleLayout="stacked"` + `buildTaskTitleLabel()` — toolbar row (back, checkbox, truncated label, actions) then full title in `text-xs` on its own row; used by Quick Tasks window + sheet. `associate_with_task` + `create_task_with_association` were DROPPED (`migrations/graveyard_buggy_task_assoc_rpcs.sql`, applied + ledgered) — both hand-rolled a 4-column `ON CONFLICT` matching no unique index (the real key is the 5-tuple incl. `role`), throwing `42P10` on every entity-branch write. `taskAssociationsSlice.associateWithTask` now calls `associationsService.add`; `createTaskWithAssociation` inserts the task via the task service then wires entity + scope edges through the chokepoint (a failed edge no longer aborts task creation; edge errors are loud, not fatal). `get_task_associations` retained as the denormalized read. See `features/scopes/FEATURE.md` (2026-06-29) for the hardened handler + generated `EntityTypeToken` vocabulary, and FOUND_DEFECTS D27 for the phantom-token holes this surfaced (`user_file`/`cx_message`/… in the task read filters).
- `2026-06-25` — claude: **Tasks vertical cut over to the canonical DB surface (comments + attachments + assignments).** Comments left the legacy `ctx_task_comments` junction for the unified `platform.comments` primitive: new sole chokepoint `features/comments/service/commentsService.ts` (`cmt_list`/`cmt_add`/`cmt_edit`/`cmt_delete`, returns `ScopesRpcResult`, never throws) + clean `Comment` type (`features/comments/types.ts`) with nested `author {email, displayName, avatarUrl}`. `taskService.getTaskComments`/`createTaskComment` now call `commentsService` (entity_type='task'; org resolves from the task in the RPC) and return `Comment[]`; every consumer (`TaskCommentPopover`, `TaskEditorBody`, `TaskDetailsPanel`, `TaskDetailPage`, `aiExportService`, `serializeProjectTaskForAi`) updated to the camelCase shape (`body`/`createdAt`/`createdBy`/`author`). Attachments left `ctx_task_attachments` (0 rows) for the canonical association edge — a file→task link is now `associationsService.add({sourceType:'user_file', targetType:'task'})` and reads come from the existing `get_task_associations` `files` array; `deleteTaskAttachment(taskId, fileId)` detaches via `associationsService.remove`. Assignments: the dead `ctx_task_assignments` type alias was removed (primary assignee stays on `ctx_tasks.assignee_id`; no junction). Dead row aliases `DatabaseTaskComment`/`DatabaseTaskAttachment`/`DatabaseTaskAssignment` deleted from `types/database.ts`. War Room admin map description repointed to `platform.comments`. Zero `ctx_task_comments|ctx_task_attachments|ctx_task_assignments` references remain in app code.
- `2026-06-26` — codex: removed the Personal pseudo-org sentinel from project/task project ownership. Legacy `features/tasks/services/projectService.ts` now resolves `ensure_personal_organization` and writes that real org id for default personal projects; project hooks now keep personal projects under their owning org id.
- `2026-06-23` — **Tasks wired as the `matrx-user/tasks` agent surface.** New `features/tasks/agent-context/buildTasksContextData.ts` — pure `buildTasksContextData(...)` maps the active task's live editor state → `createTasksScope(...)` using the manifest's exact value names (baselines `selection`/`content`/`context` from the description editor; customs `active_task_id`/`title`/`description`/`priority`/`status`/`due_date`/`subtasks`/`active_project_id`/`active_project_name`). List-level manifest values (`task_list`/`project_list`/`task_count`/`search_query`) are intentionally NOT emitted by the single-task editor (it doesn't own the list). Also exports `TASKS_CONTEXT_MENU_PROPS` + `createTasksExtraSections(...)`. `TaskEditor.tsx`: the description `ProTextarea` now carries `surfaceName="matrx-user/tasks"` + `getApplicationScope` (lists bound task-agents in its "…" menu); it's wrapped in `UnifiedAgentContextMenu` (editable) with text-replace/insert callbacks + Save/Complete/Delete `extraSections`; the read-only comment thread is wrapped in a presentational `UnifiedAgentContextMenu` (`isEditable={false}`). Both plain title `<input>`s swapped to `<ProInput>`. `sourceFeature` reuses `"task-create"` (no dedicated `"tasks"` member in the `SourceFeature` union yet — follow-up). No manifest change (all emitted names already declared).
- `2026-06-21` — **Task file attachments fixed (cloud-files canonical).** `get_task_associations` was joining legacy `user_files` while uploads store `cld_files.id` under entity_type `user_file` — upload + `associate_with_task` succeeded but the Files section stayed empty. Migration `ctx_get_task_associations_cld_files.sql` resolves `cld_files` (with `user_files` fallback). `TaskAttachmentsPanel` now uses `useFileUpload().uploadMany` (canonical handler + upload guard) with attach error toasts; legacy `TaskAttachments` replaced by the panel on `TaskDetailsPanel` / `TaskDetailPage`. `useFileUpload.uploadMany` now forwards `folderPath` to the upload thunk. Nav-tree hydration stores tasks at thin-list level without `created_at` / `user_id`; `useEnsureTaskLoaded` now upgrades via `fetchTask` instead of returning early when a thin row exists. `TaskEditor` calls the hook on every surface (`/tasks/[id]`, window overlay, War Room tiles) so Advanced shows real values with a spinner while full-data fetches. New `TaskCreatePanel` (`features/tasks/widgets/quick-create/TaskCreatePanel.tsx`) wraps the canonical `TaskQuickCreateCore` (Manual) + `AgentRunWrapper` (Use AI) via the shared `CreateWithAiTabs` primitive (`features/agents/components/smart/CreateWithAiTabs.tsx` — both tabs stay mounted, AI lazy-mounted then retained, constant `min-h-[460px]` body floor, so switching never resizes/flashes/remounts). `TaskQuickCreateWindow` now renders the panel; new full-page route `/tasks/new`. On AI run completion the panel dispatches `loadProjectsWithTasks({ force: true })` so the list picks up the agent-created task. Added `task-create` to the `SourceFeature` union. **AI tab is gated OFF** until `TASK_CREATE_AGENT_ID` (in `TaskCreatePanel.tsx`) is set to a real agent UUID — until then the window/route render exactly the bare manual core (no behavior change); it's also hidden when a `source` is present (source-linked capture stays manual-only).
- `2026-06-17` — claude: **Tasks list/table dual view on `/tasks`.** `TaskListPane` gained a List/Table toggle (persisted `tasks-list-view`); default stays the grouped list. New `TasksTableView` — sortable/filterable flat table (status, task+labels, project, priority, due, updated) with zebra rows, column filters, default sort `updated desc`. `TaskRecord` + `selectProjects` mapping now carry `updated_at` when loaded.
- `2026-07-05` — claude: **Task quick-create footer moved to window chrome.** `TaskQuickCreateCore` gained `footerHost?: HTMLElement | null`: window-level actions (Cancel / Create & attach / post-save New tab · Window · Go to task · Done) portal into the `WindowPanel` `footerRight` slot as compact h-7 buttons when the host provides one (`TaskQuickCreateWindow` does), falling back inline otherwise. Also replaced the scope-sync `setState`-in-effect with derived `effectiveScopeIds` (follows app context until first touch). Menu entry renamed "Create Task" with the /tasks route-badge icon (see `ASSISTANT_MESSAGE_ACTIONS_REVIEW.md` menu reorg).
- `2026-07-05` — claude: **"Create task from message" brought to the save-as-note gold standard.** (1) One seed helper, `features/agents/components/messages-display/message-options/buildTaskSeedFromMessage.ts`, used by BOTH the live menu action and the post-auth resume path: task title from the conversation label via shared `buildConversationMessageTitle` (`features/agents/utils/conversation-message-title.ts`) — never raw message text (markdown-cleaned first-line fallback only for unlabeled conversations); conversation association labeled with the real conversation title (was a message-content preview); message association label is a `cleanMarkdown`ed single-line preview. (2) `TaskQuickCreateCore` calls `useEnsureHierarchyLoaded()` on mount — the Project dropdown hydrates when the window opens from chat/anywhere (previously read possibly-empty Redux; boot fetch was removed 2026-05-16; siblings `TaskTapButton`/`AssociateTaskButton` already fetched). (3) Description textarea replaced by the shared content-refine primitive (`components/content-refine/useRefinableContent` + `RefinableContentEditor`): strip-thinking, start/end trim sliders, plain/split/preview modes, char count — same toolkit as the quick note save window; save persists the refined `workingContent`. (4) `TaskQuickCreateWindow` resized from fixed 720×560 to `90vw × 85dvh` (24px viewport pad), matching `QuickNoteSaveWindow`.
- `2026-06-17` — claude: **`taskEditorWindow` overlay primitive** — multi-instance `WindowPanel` + `TaskEditor` registered in OverlayController; opener `useOpenTaskEditorWindow({ taskId })` (stable instance `task-${taskId}`). `useEnsureTaskLoaded` hydrates the agent-context slice when opening from surfaces that don't preload Redux. Wired: `ProjectTaskList`, `TileProjectTaskList`, `SubtaskRail`/`TileTaskTab`, `TaskEditor` subtask rows.
- `2026-06-27` — claude: **Verified canonical cutover complete; docs reconciled.** Confirmed live: the 6 task-association RPCs (`get_task_associations` / `get_tasks_for_entity` / `associate_with_task` / `dissociate_from_task` / `create_task_with_association` / `create_tasks_bulk`) all read/write `platform.associations` (no legacy reads), and all 7 legacy `ctx_*` junctions (`ctx_scope_assignments`, `ctx_task_associations`, `ctx_project_members`, `ctx_project_invitations`, `ctx_task_comments`, `ctx_task_attachments`, `ctx_task_assignments`) are now in the `graveyard` schema with zero app-code or live-RPC readers. `db-types` regenerated, `type-check` green, migration ledger clean. Final `DROP graveyard.*` + dead `_mirror_assoc` trigger removal awaits explicit sign-off.
- `2026-06-17` — claude: **`ProjectTaskList` quick-add Enter chains fields.** Title → Priority → Due → (Description when Advanced) → create; cursor returns to title for the next task. Subtask rows stay open after Enter (type → Enter → type). Quick-add row is always visible. **⌘/Ctrl+Enter** submits from any field for title-only rapid capture. `TaskPriorityPicker` / `TaskDueDatePicker` gained optional `triggerRef` + `onTriggerKeyDown` for pill keyboard chains.
- `2026-06-16` — claude: **Task list grouping UX.** `TaskListPane` shows `BY {GROUP}` banner when grouped; each group is a bordered card with muted header. Shared labels in `constants/groupBy.ts`.
- `2026-06-16` — claude: **Tasks context sidebar UX.** `TasksContextSidebar` — View (group/sort/show completed) moved to top; every section collapsible with active selection shown when collapsed.
- `2026-06-19` — claude: **Sidebar "Add Project" + reusable nav-action primitive.** The main app sidebar's Projects flyout (`features/shell/components/sidebar/NavFlyoutGroup.tsx`) gained an **Add Project** entry that opens the `createProjectWindow` overlay in place (no routing) via `useOpenCreateProjectWindow`. Built as the first of a general pattern: nav entries can carry a declarative `action?: ShellNavActionId` (`features/shell/constants/nav-data.ts`) whose handler is registered in `features/shell/navigation/navActions.ts` (`useNavActions`, typed `Record<ShellNavActionId, () => void>` so the id union and the registry can't drift). Action-aware surfaces render a `<button>` that runs the handler; surfaces that don't yet understand actions fall back to the entry's `href` (`/projects/new`) — progressive enhancement, adding an action never breaks a surface. `button.shell-nav-flyout-item` reset added in `styles/shell.css`.
- `2026-06-19` — claude: **Create a project (+ tasks + subtasks) from JSON — one RPC, one write path.** New `create_project_from_json(p_payload jsonb, p_organization_id uuid)` RPC (`migrations/create_project_from_json.sql`, SECURITY INVOKER so RLS + `auth.uid()` apply exactly like the direct client inserts it replaces; the existing `trg_ctx_projects_add_creator_membership` still grants owner membership) writes the whole tree in one transaction from the agent payload shape (`name`/`slug`/`description`/`start_date`→`start_date`/`end_date`→`target_date`/`tasks[].subtasks[]`). Slug is derived + de-duplicated within scope; task/subtask array order is preserved via per-row `created_at` offsets (the project task list sorts by `created_at ASC`). The backend agent flow and the new UI both call this one RPC. Client side: `features/projects/importJson.ts` (`validateProjectJson` pure validator + `createProjectFromJson` RPC wrapper) and `ProjectImportJsonPanel.tsx` (owner picker + JSON textarea + **Validate**/**Create**). Surfaced as a third **Paste JSON** tab in the create-project window: `CreateWithAiTabs` gained a generic `extraTabs` prop (lazy-mounted + retained like the AI tab, no layout shift), consumed by `ProjectCreatePanel` (`enableJsonImport`, default on). `OrgSelector` + `OrgContext` now exported from `ProjectFormCore`.
- `2026-06-22` — **Copy for AI on project/task list surfaces.** `ProjectCopyForAiButton` added to `ProjectsHub` (cards + table), `ProjectCard`; `TaskCopyForAiButton` on `ProjectTaskList` rows and War Room `TileProjectTaskList` (header + per-task ⋯). War Room tiles use shared `TileCopyForAiButton`.
- `2026-06-19` — claude: **Copy for AI payload slimmed.** Project/task exports now emit one semantic xml document (no duplicate summary + JSON dump); empty comments/attachments/notes omitted; inherited FKs and null DB noise stripped.
- `2026-06-18` — claude: **Copy for AI on project/task surfaces.** Shared export pipeline (`features/tasks/services/aiExportService.ts`) fetches full project/task trees (nested subtasks, comments, attachments, linked notes) and wraps them in the agent-copy xml-ish payload via `ProjectCopyForAiButton` / `TaskCopyForAiButton`. Wired on War Room project tiles, `/projects/[id]` workspace + settings, and all `TaskEditor` surfaces (including `/tasks/[id]` header).
- `2026-06-14` — claude: **Fixed spurious "duplicate" error on project creation (double-write).** The DB trigger `trg_ctx_projects_add_creator_membership` (AFTER INSERT on `ctx_projects`, SECURITY DEFINER, `ON CONFLICT (project_id, user_id) DO NOTHING`) already writes the owner `ctx_project_members` row, but `features/projects/service.ts → createProject` _also_ manually inserted that row — the second write violated the `(project_id, user_id)` unique constraint and made `createProject` return `{success:false, error: "duplicate…"}` even though the project + membership were fully created. Removed the manual member insert; the trigger is now the single source of truth (matching the legacy `features/tasks/services/projectService.ts → createProject`, which never inserted it). This surfaced both as "create project fails" and "create project from the tasks sidebar fails" (`createProjectThunk` and `hierarchyService.createProject` both route through the canonical service). **Tasks themselves were never affected** — `ctx_tasks` has only a PK on a server-generated `id`, so task inserts cannot collide.
- `2026-06-08` — claude: **Priority + due-date editing extracted into shared task primitives (doctrine: build the platform).** Two inline editors that had been re-implemented in two shapes are now ONE primitive each, consumed everywhere: `features/tasks/components/TaskPriorityPicker.tsx` (`variant="pill"` compact Popover for the project task table, `variant="segmented"` inline control for the full editor; exports the `TaskPriority` type + `TASK_PRIORITY_META` styling vocab so read-only chips stay in lockstep) and `features/tasks/components/TaskDueDatePicker.tsx` (`variant="pill"` for the table, `variant="field"` full-width form field replacing `TaskEditor`'s raw `<input type="date">` — every surface now gets the Calendar-popover + Clear + no-TZ-shift behavior). `ProjectTaskList` (project task table) and `TaskEditor` both consume them; the local `PriorityPicker` / `DueDatePicker` / `PrioritySegmented` / `PRIORITY_PILL` / `PRIORITY_STYLES` copies are deleted. Priority colors unified app-wide to red/amber/sky (editor's `low` was green). The `yyyy-mm-dd` no-TZ-shift date helpers (`parseDateOnly` / `toDateOnly` / `formatDateOnly` / `isDateOnlyOverdue`) now live in `@/utils/dateOnly` — the three duplicate copies (both task pickers + `features/projects/components/ProjectInlineEditors.tsx`) collapsed into the one shared util.
- `2026-06-07` — claude: **Projects are now real PM containers + view==edit, no filter traps.** (1) Added `ctx_projects` columns `status` (planning/active/paused/completed/archived), `priority`, `start_date`, `target_date` — projects are no longer title-only. (2) New `ProjectInlineEditors` (`InlineProjectName`, `InlineProjectDescription`, `ProjectMetaRow`) — name/description/status/priority/dates/**organization** all edit IN PLACE and autosave via `updateProject` (no Edit/Save toggle, no separate edit page). Used on the workspace hero AND the Manage › General card (which is now directly editable). `UpdateProjectOptions` + service handle the new fields. (3) **Filter trap fixed:** `ProjectsHub` shows a visible, removable filter banner whenever `?org=`/`?scope=` is active (chip + X + "Show all projects" → `/projects`); `DangerZone` post-delete redirect now always goes to plain `/projects` (it used to send you to `/organizations/<slug>/projects`, which redirects to `/projects?org=` and silently trapped you in a filter).
- `2026-06-07` — claude: **`ProjectTaskList` made fully inline-editable (Linear/Things style).** Every task field is now editable from the project-workspace table — not just the name on add. Title is click-to-edit (`InlineTitle`: Enter/blur saves, Escape cancels); Priority is an inline `Popover` picker (None/Low/Med/High colored pill, None→null); Due is an inline `Popover` + `Calendar` with a Clear option (overdue shown red). All edits go through one optimistic `patchField` → `updateTask` with revert + `toast.error`, and work on parent rows AND subtasks. Quick-add row now sets name + priority + due before adding and has an **Advanced** chevron revealing a Description textarea (`createTask` supports `description`). New trailing actions column with an "Open" affordance that navigates to `/tasks/[id]` (the full `TaskEditor` is Redux-coupled to `selectSelectedTaskId` and can't render standalone from a prop, so navigation is used rather than a half-wired drawer). Self-fetch / complete-toggle / nested subtasks / collapsible Done / `onCountsChange` all preserved.
- `2026-06-06` — claude: **Project Manage › General redesigned + Organization made editable.** `GeneralSettings` is now a clean definition-row read view with inline edit (no nested header / double padding). Organization is a first-class **editable** field — an org picker over the user's orgs (incl. their personal org) that persists via `updateProject({ organizationId })` (added `organizationId` to `UpdateProjectOptions`; the service sets `organization_id`, never null). Slug + created stay read-only with reasons. Also widened the hub table's Organization column.
- `2026-06-06` — claude: **`ctx_projects.is_personal` dropped → personal-ness is org-derived.** The DB column no longer exists; a project is "personal" iff its owning org's `organizations.is_personal` is true (every project now has a non-null org after backfill). Removed `is_personal` from all `ctx_projects` `.select(...)` / insert payloads across `features/agent-context/redux/projectsSlice.ts`, `features/agent-context/service/hierarchyService.ts` (+ `HierarchyProject` shape), `features/projects/service.ts` (`createProject` insert, `getPersonalProjects` filter now joins `organizations(is_personal)`, `transformProjectFromDb` reads org join), `features/projects/components/{ProjectsHub,ProjectWorkspace,ProjectFormSheet}.tsx`, `app/(core)/invitations/project/accept/[token]/page.tsx`, `features/tasks/services/projectService.ts`. `ProjectsHub`/`ProjectWorkspace` now compute the "Personal" badge from the org's `isPersonal` (no longer `!organizationId`). `NavProject.is_personal` / `NavOrganization.is_personal` (RPC-derived) and `organizations.is_personal` / `ctx_templates.is_personal` are unchanged.
- `2026-06-06` — claude: **UX refinements.** (1) New `AssignedScopesDisplay` (read-only `Scope Type: Scope` + Organization line, resolving `ctx_scope_assignments → ctx_scopes → ctx_scope_types`) replaces the misused `EntityScopeTagger` on the project workspace — it now shows only _assigned_ scopes by type, never the full available list. `EntityScopeTagger` remains the editor (Manage › Scopes). (2) Project workspace Tasks are now a table (Task/Priority/Due, subtasks indented). (3) Projects hub gained a Cards/Table dual view (sortable, searchable, full-width table) backed by one batched task-count query. Superseded by 2026-06-26: "Personal" is now keyed only by the owning org's `organizations.is_personal`.
- `2026-06-06` — claude: **reimagined Projects + enhanced Tasks as containers.** New `useContainerInventory` primitive (org delegates). New canonical top-level **ProjectWorkspace** (`/projects/[id]` — hero, nested ProjectTaskList with subtasks + quick-add, role-grouped associated resources via `project_id`, scope tagging, ProjectReferencesPanel), **ProjectsHub** (`/projects` with `?org=`/`?scope=` filters + live task previews), and **ProjectManage** (`/projects/[id]/settings`, single-page sectioned — no tabs). Task editor gained an **Associated resources** section (FK by `task_id`). Legacy org-nested + transitional project routes now redirect; `createProjectThunk` migrated to the canonical `features/projects/service.ts`. **Pending:** a UI panel for `ctx_task_associations` (M2M linked items) — the redux engine exists; only the panel is unbuilt. Full `features/tasks/services/projectService.ts` dedup still pending (used by `useTaskManager`/`ImportTasksModal` for `getProjectsWithTasks`/`ensureDefaultProject`).
- `2026-04-25` — Stopped using `features/projects/index.ts` and `features/tasks/redux/index.ts` as import entry points: call sites import from `service.ts` / `types.ts` / `hooks.ts` / `components/*` (projects) and from `taskUiSlice` / `selectors` / `thunks` / `taskAssociationsSlice` / `quickTasksWindowSlice` (tasks). Root `index.ts` files remain for re-exports only.
- `2026-04-22` — claude: initial combined FEATURE.md for tasks + projects.

---

> **Keep-docs-live:** schema changes to `projects` or `tasks`, widget system changes, or scope-column changes must update this doc and cross-check `features/scopes/FEATURE.md`.
