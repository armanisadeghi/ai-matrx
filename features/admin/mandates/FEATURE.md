# Mandates admin console

Cross-repo system-of-record: `/Users/armanisadeghi/code/common-docs/systems/mandates/FEATURE.md` — read it before touching this feature in ANY repo.

Cross-repo system-of-record: `/Users/armanisadeghi/code/common-docs/systems/agent-variable-binding/FEATURE.md` — read it before touching this feature in ANY repo. It governs how a mandate's variables actually reach the agent (the mapping vocabulary, the scenario matrix, and why the UI must read live code truth instead of the `contract` column).

Route: `/administration/agents/mandates` (`app/(admin)/administration/agents/mandates/page.tsx`). Code: `service.ts` (direct supabase reads/writes on `agent.mandate` / `agent.mandate_binding`; super-admin writes ride RLS via `has_access` editor on system-org rows — no bespoke RPC) + `MandatesConsole.tsx`.

The USER/ORG-facing override surface (browse mandates, provenance, write `mandate_binding` overrides) is `/agents/mandates` — `features/agents/mandates/FEATURE.md`. This console stays admin-only (pins, health, bench).

## The two laws (Arman's ruling, 2026-08-08 — violations are defects, fix on sight)

1. **THE SYSTEM-AGENT LAW.** A mandate DEFAULT may only reference a **system agent** (`agent_type='builtin'`, system-org owned, no user). A personal/shared/org agent pinned as a mandate default breaks every user the mandate serves — it fails the moment ownership, visibility, or archival shifts, and it fails on some page far from where it was pinned. The console picker therefore offers ONLY system agents; any row whose default drifts to a non-builtin shows a destructive **"NOT a system agent — fix this pin"** badge; aidream's `sync_declared_mandates` screams the same on every boot. Promote an agent to system via `agx_duplicate_agent(p_as_system => true)`, then pin the promoted copy. (User/org OVERRIDE bindings are the opposite case — those are _supposed_ to be the principal's own agents.)
   A super-admin promotion may copy a private source on the platform's behalf;
   the normal personal-copy path still requires viewer access. This split is
   enforced by `agx_duplicate_agent` / `agx_duplicate_version` and recorded in
   `migrations/agx_system_promotion_superadmin.sql`.
2. **THE CANONICAL-SELECTION LAW.** Anywhere agents are listed for selection, use the canonical agent listing system — the Redux agent-definition slice (`fetchAgentsListFull` + the purpose-fit selector: `selectBuiltinAgents` here, `selectActiveAgents` for user-facing pickers) or the scoped server RPCs (`agx_list_scoped` with true scopes). **A raw `.from("definition")` query dumped alphabetically is the recurring disease this repo keeps re-catching**: it blends mine/shared/org/public/system into one meaningless list, ignores scopes, and treats an administrator like a user. Never write one. The console consumed exactly this bug at birth (fixed 2026-08-08, same day).

## Owner test bench (Wave 2, 2026-08-09)

The bench is organized around the decisions an owner actually makes:

1. **Is the new version better?** Name any number of comparison columns and choose the mandate's pinned version, latest version, an arbitrary saved version, another system agent, or the currently resolved agent with inherited/custom/no settings overrides. Blank overrides inherit; explicit `{}` means no overrides. **Run all exemplars** sends one generated-contract `callApi` batch to `POST /mandates/{mandate_key}/tests`. Every exemplar renders labeled Reference / Baseline / candidate columns side by side. Agent failures are red result cards with an `error`, not rejected-call toasts.
2. **Make this result the reference.** Every successful result has a one-click action that updates the exemplar's `reference_output` / `reference_artifact` directly in Supabase and stamps that persisted result as promoted.
3. **Test as principal.** The canonical admin user and organization directories let an owner reproduce user-only, org-only, or combined binding resolution. Named principals retain their real record doors.

Manual exemplars accept both variables JSON and `user_input`. The server persists a capped history (100 entries) in the existing `agent.mandate_exemplar.metadata.test_bench_results`; the frontend parses that open JSONB defensively. Owner verdict notes and reference promotion are direct, optimistic-concurrency Supabase updates because they are pure record CRUD. Python owns only agent execution and the result append. API request/response aliases come exclusively from `types/python-generated/api-types.ts`; there is no local response mirror or backend `fetch`. Image outputs still render via `InlineMediaRef` (file_id recovered with `fileIdFromUserFilesUrl`, never a raw expiring URL).

**COLD MANDATES: "Try it now" is the way in (`TryItNowPanel.tsx`, 2026-08-15).** A batch needs stored test cases, and most live mandates have none — so for them the batch bench had nothing to run and the console's whole comparison story was unreachable. The panel runs ONE candidate on inputs typed right now (`POST /mandates/{mandate_key}/test` with inline `variables`/`user_input`, no exemplar; the server persists nothing), and **Save as test case** writes the inputs AND that run — as the reference — into one new `mandate_exemplar` insert, with the result embedded in `metadata.test_bench_results` in the server's own `MandateTestResult` shape (carrying its new `exemplar_id` and a `promoted_to_reference_at` stamp), so it reads identically to a promoted batch result. Two rules:

- **The form is SCAFFOLDED, never a JSON textarea**: fields are the union of the mandate contract's `required_variables` and the resolved agent's `variable_definitions` (either side alone silently misses inputs the run needs), each rendered by the canonical `VariableInputComponent` — so picklist/media/slider variables get their real control. A contract-required field left blank refuses the run. The user-message field appears only when code truth says a call site passes `user_input` (`MandateDetailPanel` passes `codeTruth.passes_user_input`), or when the mandate declares no variables at all.
- **No progress UI.** That endpoint returns one completed result and exposes no `requestId`, so there is no stream to render — the button carries a busy state and nothing else. When it learns to stream, this panel adopts the stream and renders the canonical `LiveRunWindow`; it must never grow a hand-rolled progress bar instead.

**A batch is a COMPARISON.** The server requires at least one candidate beside the baseline, so "Run test cases" is disabled (with the reason) until a comparison is added — clicking it with none reached the API and returned a raw `body.candidates` validation error (fixed 2026-08-15). To run just the current setup, use Try it now.

The batch response is non-streaming and includes every selected candidate/exemplar cell. Its `callApi` request therefore uses a ten-minute connection deadline and no total deadline; the original 90-second deadline was proven too short by the live four-exemplar comparison and would hide already-persisted results behind a client timeout.

## Console shape (2026-08-08 rebuild; drawer rebuilt 2026-08-12)

The list is the canonical `MatrxDataTable` (`components/official/matrx-data-table`) — every column sorts + filters, global search, Copy/Copy-for-AI (row + this view), pagination, UUID cell on `id`. The canonical `<feature>.<mandate>` key is split at its first dot into separate **Feature** (searchable select) and **Mandate** columns; later dots remain part of Mandate, while copy/surface/detail payloads retain the full `mandate_key` and UUID identity. Derived `MandateRow` (`mandate-health.ts`) owns ONE worst-first **Health** model: `code ↔ agent drift` → code-truth import failure → unresolved pin → non-system agent → archived agent → `code ↔ contract drift` → version drift → ok. Rows use that same order, so a breaking code/agent mismatch is on the first page without opening a drawer. Row click → side-panel workbench; the WindowPanel Edit tab reuses the same body.

**The drawer is FACTS-FIRST, THEN ISSUE-DRIVEN (`MandateDetailPanel.tsx`).** Structure: labeled mandate facts → live code facts + per-variable server verdicts → health remedy → three collapsible sections (**Change pinned agent** / **Test this mandate** / **User & org bindings**). The code facts come from typed `GET /mandates/code-truth`; per-variable labels come from typed `POST /mandates/{mandate_key}/variable-verdicts`, evaluated against the agent that actually runs. Never infer either from `mandate.contract`.

**The code-truth read allows 60 seconds to receive headers.** A cold server may
still be preloading its process-stable source inventory. Failure remains visible
in the amber stale-data banner and canonical `callApi` diagnostic; the console
uses `console.warn`, never a duplicate captured `console.error`.

- **A code/agent mismatch ships the complete choice, not a scolding:** map onto an existing agent variable when one exists; use a real agent default when the server reports one; spill to `user_input` with a permanent caution; open the agent builder to declare the variable; choose another agent; or copy the full source-aware fix brief. Unavailable options stay visible and say why.
- **The rebind guard consumes the same live truth.** Every `computeRebindImpact` call receives `codeSuppliedVariables`; `buildRebindFixBrief` receives the runner class, source line, bound declarations, user-input fact, and every discovered call site.

- **Version drift is the main event**: an amber split view (Running now vN → Newest vM, names from the saved-version list), inline field-level `AgentDiffViewer` via `fetchVersionSnapshotDefinition` (the same `agx_get_version_snapshot` + converter the version pages use), one-click **Update to vM** / **Track latest automatically**, and **Test old vs new first**, which opens the bench pre-armed (baseline `Current — pinned vN`, candidate `Latest version`) and scrolls to it.
- **The drawer owns its scroll**: `SidePanelSurface` (and the window body) hand children an `overflow-hidden` cell — the root of `MandateDetail` is `h-full min-h-0 overflow-y-auto`. Dropping that wrapper is the exact "everything cut off at the bottom" defect the rebuild was ordered over.
- **No user-visible "exemplar"**: visible copy says **test case** everywhere (the DB table, API fields, and the surface-manifest value names keep `exemplar` — they are contracts). No raw `JSON.stringify` one-liners: contract variables render as clickable chips in the bench composer, binding `config_overrides` as wrapped pretty JSON.
- The **Test this mandate** section stays MOUNTED while folded (`Section keepMounted`) — the bench registers the `mandate_exemplar_draft` surface write handler and publishes the composer snapshot; unmounting it would break the agent-writable seam.
- `MandateEditor`/`MandateTestBench`/`MandateOverridePanel` seed local state from props, so `MandateDetail` keys them by mandate id — dropping the key regresses to stale cross-mandate state (bug found 2026-08-08).

File map: `service.ts` (direct DB console bundle + typed aidream code-truth/verdict calls), `TryItNowPanel.tsx` (ad-hoc run + save-as-test-case), `mandate-health.ts` (MandateRow/buildRow/ONE health order), `rebind-impact.ts` + `variable-verdict-presentation.tsx` (shared impact/fix brief/verdict labels), `mandate-actions.tsx` (shared remedies), `MandateDetailPanel.tsx` (drawer), `MandateTestBench.tsx` (bench), `MandatesConsole.tsx` (table + surface plumbing).

## THE DOOR LAW on this console (2026-08-08)

Doctrine: `/Users/armanisadeghi/code/common-docs/policies/no-dead-ends.md`; recipe: the `no-dead-ends` skill. This console is the worked reference, because it was the surface that provoked the ruling — it named agents it would not open, and complained about a pin it would not fix.

- **Every agent is reachable.** The Agent column renders `EntityRef` (`components/official/entity-ref/EntityRef.tsx`) — open / new tab / peek — with an `href` override, because system agents live under `/administration/agents/system-agents/agents/<id>` and personal agents under `/agents/<id>`. Pin cells carry a version-history door (`…/latest`; `/v/<number>` is one saved version, never the collection).
- **Lineage is shown, not withheld.** `selectAgentLineageIndex` derives parent / children / **systemTwin** from the agent slice (zero extra queries). The twin may be a PARENT (personal copy of a system agent) or a CHILD (system agent promoted from a personal one) — both directions are load-bearing and covered by `features/agents/redux/agent-definition/__tests__/lineage-selectors.test.ts`.
- **The complaint ships with the fix.** A `not a system agent` row renders the twin (linked) plus **Rebind to system twin** (rebinds to the twin tracking latest — a pin to a personal agent's version has no meaningful version to carry over). No twin → **Create system twin…**, which opens the existing Linked Agent Sync window rather than a second bespoke flow.
- **The drawer leads with what you HAVE.** `MandateAgentIdentityCard` (currently-running agent, type badge, pin vs latest, drift, lineage chips, Versions / Linked Agent Sync / Rebind) sits above the picker. It used to open straight into a rebind picker, so an admin could not see the agent they were about to replace.
- **`unresolved pin` is its own health state.** A pin whose agent row the caller cannot read (another user's personal agent under RLS, or a deleted record) is NEVER `ok`. Reporting green for data you could not read is the exact dead end this console exists to prevent — it hid two live personal-agent pins (`podcast.deep_research`, `podcast.image_v2`) behind a green badge for any admin who wasn't their owner.

## Surface declaration

The page is the `matrx-admin/mandates` surface (`features/surfaces/manifests/mandates.manifest.ts`, readiness `partial`; route mapped in `features/surfaces/utils/route-to-surface.ts`; `ui.ui_surface` + values synced live 2026-08-08). `MandatesConsole` mounts `<SurfaceRuntimeProvider>` and builds the Run-time scope via `createMandatesScope` — mandate list summary, health roll-up, system-agent picker count, and the selected mandate's id/detail/health/overrides. The bench's stored exemplars and its "+ Exemplar" composer ARE now in the scope (`selected_mandate_exemplars`, `mandate_exemplar_draft`), published upward from `MandateTestBench` through `bench-draft.ts`; candidate-run state (comparison columns, batch results, verdict notes) still stays in `MandateTestBench` local state — lifting it promotes readiness further.

The surface is also AGENT-WRITABLE, with exactly two targets — `select_mandate` (`ui`, handled on the console's own provider, which owns `selectedId`) and `mandate_exemplar_draft` (`draft`, registered by `MandateTestBench` via `useSurfaceWriteHandlers`). Both are `applyPolicy: "ask"`. Read the JUDGMENT BAR block at the top of the manifest before adding a third: rebind, enable/disable, the per-principal overrides, Run all, and every health/roll-up value are deliberately NOT writable, and each has its reason written down there.

## Change Log

- 2026-08-18 — Gave code truth a 60-second connection window and removed its
  duplicate console-error capture; aidream now preloads the immutable source
  inventory.

- 2026-08-17 — **Retired-slot producer guard.** The admin and user Mandate services read `agent.mandate` / `agent.mandate_binding`, call `/mandates/*`, and permanently redirect the two retired `/agents/slots` URLs. `retired-slot-producer.test.ts` prevents the removed tables or API prefix from returning to runtime code; the live PostgREST failures all predate the table-rename commit.
- 2026-08-17 — **Renamed to Mandates.** `AgentSlotsConsole` → `MandatesConsole`, `SlotDetailPanel` → `MandateDetailPanel`, `SlotTestBench` → `MandateTestBench`, and every database/API contract now uses Mandate vocabulary. The code-truth fixture moves in lockstep with its generated OpenAPI type so the runtime guard cannot silently reject the server's real shape.
- 2026-08-16 — Split the registry's canonical `<feature>.<mandate>` key into independently sortable/filterable Feature and Mandate columns. Feature uses a searchable select; full `mandate_key` and UUID identities remain in copy, surface, and detail payloads. Shared `splitMandateKey` preserves later dots and dotless legacy keys without information loss.
- 2026-08-15 — **Cold mandates can be benched: "Try it now" + Save as test case (`TryItNowPanel.tsx`).** Most live mandates have zero exemplars, so the batch bench had literally nothing to run for them. The bench now runs one candidate on inputs typed in a scaffolded form (contract required variables ∪ the resolved agent's variable definitions, each through the canonical `VariableInputComponent`; user message only when code truth says a call site passes one) against the existing `POST /mandates/{mandate_key}/test`, and turns any result into the mandate's first real test case in one insert — inputs kept, that run promoted to the reference, the run itself written into `metadata.test_bench_results` in the server's own shape. Same trip fixed the batch button firing a raw 422 when no comparison was added. Browser-verified end to end on a genuinely cold mandate (`podcast.post_prep_summarization`: run → save → the batch then ran 1 test case through 2 setups) and on `knowledge.qa_generator`; DB-verified the created exemplar's variables, reference output/artifact, captured agent/model, and promoted stamp. Honest finding kept, not hidden: `podcast.post_prep_summarization` returns prose while its contract declares a structured kind, so its first result reads "wrong structure" — that is the output-contract gap the mandates handoff item 3 tracks, now visible in the bench.
- 2026-08-15 — **Code-truth acceptance surface shipped.** The console consumes typed `GET /mandates/code-truth`, promotes code↔agent/code↔contract/import failures into the existing worst-first health model, sorts them first, and shows code vs agent variables in the unopened row. The facts-first drawer consumes the typed server verdict endpoint, labels every variable without JSON fields, offers the full scenario-matrix choice with real doors/copy actions, and reuses the rebind impact presentation + source-aware `buildRebindFixBrief`. The guarded rebind seam now receives live `codeSuppliedVariables`. `podcast.deep_research` remains deliberately broken and is the browser acceptance case.

- 2026-08-12 — **Drawer rebuilt as an issue-driven workbench (Arman's order, in anger).** The old drawer opened into a resolution ribbon, an always-visible rebind picker, remedies that didn't match the state, "exemplar" jargon, and a bottom that cut off (no scroll container — `SidePanelSurface` children own their scroll and nothing did). New `MandateDetailPanel.tsx`: per-health status banner first (drift = split view vN→vM + inline `AgentDiffViewer` + one-click update/track-latest + bench pre-armed for old-vs-new; non-system = twin/promote in place; unresolved = server identity + remedies; archived = replacement path; ok = one green line), agent card, three collapsible sections. Visible copy de-jargoned (test case, plain-English candidate labels), bench defaults simplified (no phantom second candidate), contract variables are clickable chips, config JSON wraps. Console split into `mandate-health.ts` / `mandate-actions.tsx` / `MandateDetailPanel.tsx`. Verified live in the browser across ok / drift / unresolved states, drift diff loading both snapshots, section fold/unfold, drawer scroll, and mobile bottom-sheet width.

- 2026-08-12 — **The console is agent-writable, with exactly two targets — and the bench is finally in the surface scope.** `matrx-admin/mandates` declares `select_mandate` (`ui`, `ask`) and `mandate_exemplar_draft` (`draft`, `ask`, a partial `{label?, variables?, user_input?}`). The exemplar is the one genuinely AUTHORED artifact on this page: an agent reads the mandate's contract and composes a realistic test input in seconds, the admin still presses **Save exemplar**, and **Run all** stays a human press because it spends real model budget. Everything else is deliberately refused, with the reasoning written into the manifest docblock — health and the roll-ups are MEASURED (writing them would fabricate the state of the system), rebind and the per-principal bindings are agent identity by UUID over live production capacity with no agent catalog on the surface to pick from, enable/disable touches live capacity, and verdict notes are the admin's judgment of record. To give the write target a read twin, `MandateTestBench` now publishes its stored exemplars and its "+ Exemplar" composer upward through the new `bench-draft.ts` (a module-level snapshot, same idiom as the `onMandateCacheInvalidated` bus), and the console emits three new values: `selected_mandate_contract` (via the shared `parseMandateContract`), `selected_mandate_exemplars`, `mandate_exemplar_draft`. Two guards exist because the writeback seam resolves handlers BEFORE the confirm dialog is answered: `select_mandate` validates against live refs assigned from the wrapper's `ref` callback and refuses while an unsaved exemplar draft is staged (opening another mandate remounts the workbench and would discard it), and each bench checks `readMandateBenchOwner()` against its own registration id so a write can never land in a composer that has since unmounted. Live-verified end to end with real Badass Agent runs — both dialogs carrying the manifest prose, Apply staging a contract-complete exemplar, a decline the agent accepted without retrying, and zero `surface-writeback` captures on the clean load.

- 2026-08-10 — **W3 review fixes.** (a) A rebind made from the Linked Agent Sync window now refreshes the console: `updateMandateDefinition` fires the mandates invalidation bus (`invalidateMandateCache(mandate_key)`) after every successful definition write, and `MandatesConsole` subscribes via `onMandateCacheInvalidated` → reload — no callbacks through overlay/Redux, and every other rebind surface gets the same effect for free. (b) `fetchPinnedAgentIdentity` no longer drops the version pin: it sends `agent_id` AND `agent_version_id` when both are set, and the identity route resolves version context whenever `agent_version_id` is present, so a version-pinned unresolved pin keeps its pinned-version badge.

- 2026-08-10 — **W3-twin: unresolved pins identified + remedied, one-click promote, mandate-aware compare.** (a) The drawer's `unresolved pin` branch now asks the server WHO the pin is via the new super-admin route `GET /api/admin/mandates/agent-identity` (`app/api/admin/mandates/agent-identity/route.ts` — session super-admin gate + `createAdminClient`, the same sanctioned pattern as `/api/admin/agent-builtins/by-source`; no RPC or aidream endpoint exposes out-of-scope agent identity, all are ownership-scoped by design) and renders the identity WITH its door (owner email, archived/deleted, pinned version) plus in-branch remedies: **Rebind to system twin** when the server finds one, or one-click **Create system twin + rebind**. (b) `CreateSystemTwinButton` replaces the multi-step window detour everywhere the console offered "Create system twin…": one click runs `agx_duplicate_agent(p_as_system=true)` (via the `duplicateAgent` thunk) then immediately rebinds the mandate to the new twin (tracks latest), with busy state and a doored toast; a partial failure (twin created, rebind failed) is reported loudly with the twin's door. The Linked Agent Sync window stays reachable as "Advanced…". (c) "Compare with system twin…" now passes mandate context (`mandateId`/`mandateKey`/`mandateLabel`, serializable overlay data) through the opener → `AgentConvertSystemWindow` → `AgentSyncBody`'s new OPTIONAL `mandateKey`/`mandateLabel`/`onRebindToSystem` props: the linked-pair view shows a "This is what mandate X runs" banner with **Rebind mandate to system side** in place (the window constructs the callback itself over `updateMandateDefinition` — no callbacks through Redux). Every other `AgentSyncBody` caller is unchanged.

- 2026-08-09 — **Wave 2 owner bench rebuild:** multi-candidate/all-exemplar batch comparisons with explicit Baseline columns; pinned/latest/arbitrary-version/different-agent/current ± override choices; persisted history and verdict notes; one-click reference promotion; manual `user_input`; user/org principal simulation; failed runs inline. The aidream call now uses `callApi` plus generated OpenAPI types, while note/reference CRUD stays direct to `agent.mandate_exemplar`. Live browser verification found and fixed the original 90-second response deadline; the production four-exemplar comparison now completes under the bench's ten-minute connection budget.

- 2026-08-09 — Replaced the three permanently expanded agent catalogues in the mandate workbench (default pin, test candidate, and principal override) with the canonical `AgentListDropdown`. Each trigger now shows its selected agent or default state; opening it preserves the existing search, ownership scopes, filters, previews, record doors, and mobile drawer.

- 2026-08-09 — The mandate picker now enforces the System-Agent Law at all three visible boundaries: only the **System** ownership tab exists, a legacy personal pin is not repeated as a selectable "Current agent," and Save rejects any id outside the loaded builtin-agent set. The shared `AgentListInlinePicker` gained reusable ownership-tab visibility, system-label, pinned-row, and per-record-route controls instead of a one-off mandate picker fork.

- 2026-08-09 — **Compare with system twin is now a real comparison.** The shared Linked Agent Sync window loads both complete definitions, states the synchronized-behavior verdict separately from expected personal profile and unsynced local-record differences, renders the canonical structured `AgentDiffViewer`, and shows an honest relationship rail (derived-side-created, last-reconciled, current heads) with open/peek/new-tab and Versions doors for both agents. It deliberately does not invent sync events the database does not store. Both overwrite directions stay disabled until the comparison loads; publishing a personal copy to the shared system baseline requires confirmation.

- 2026-08-09 — Hardened comparison safety: snapshots load directly without hydrating the Agent Builder Redux record, so opening Compare cannot clear dirty fields or undo history. Pull and push send the two reviewed `updated_at` values to `agx_sync_linked_agents_reviewed`; the database locks both records and rejects stale reviews before copying anything. Reusing the singleton window for another agent remounts its body, clearing the prior pair's profile checkbox and confirmation state.

- 2026-08-09 — Fixed both Versions doors in the Pin cell and selected-mandate identity card. They now reuse the canonical shell-aware `getAgentModeHref("versions", …)` route helper and open `/latest`; `/v` is only a single-version route (`/v/<number>`) and never a versions collection.

- 2026-08-08 — THE DOOR LAW pass (Arman's ruling): `EntityRef` on every agent reference, version-history door on pins, lineage/system-twin resolution + one-click **Rebind to system twin**, identity card above the drawer's picker, and the new `unresolved pin` health state (`MandatesHealthSummary.unresolved_pin` added to the surface scope). Health column widened; `ok` no longer masks unreadable pins.

- 2026-08-08 — Bindings are now EDITABLE in the console: `MandateDetail` embeds the shared `MandateOverridePanel` (`features/agents/mandates/components/`) — user + admin-org bindings incl. settings-only `config_overrides`, written via the aidream bind endpoint (`PUT/DELETE /mandates/{mandate_key}/binding`, 422 contract verdicts shown verbatim); the read-only "All overrides" roll-up stays below it.
- 2026-08-08 — One-value-per-column pass (Arman): Label its own column; IO kinds split into separate Input / Output columns. Never re-merge values into a compound column.
- 2026-08-08 — Console rebuilt on MatrxDataTable (was a hand-rolled `<table>` with no sort/filter/copy); expanded-row editor moved to the side-panel/window workbench; per-mandate remount keys added.
- 2026-08-07 — Created: mandate list, pin-vs-latest drift badges, rebind editor, enable toggle, override display.
- 2026-08-08 — Picker rewired from a raw `agent.definition` query to `selectBuiltinAgents` (system agents only); non-system-pin badge added; both laws documented here + in the SoR; aidream sync gained the boot-time scream.
- 2026-08-08 — Test bench shipped: mandate_exemplar table + auto-capture + candidate test endpoint + side-by-side console UI; `run_mandate` now structurally validates `output_kind` (loud, non-fatal).
- 2026-08-08 — Repo-wide eradication sweep completed: research admin wiring/templates rewired to `selectBuiltinAgents`, org agents page to `agx_list_scoped`, global shortcuts gated to system agents; ESLint `matrx/no-raw-agent-list-query` (error) now bans raw `agent.definition` list queries outside the canonical services.
