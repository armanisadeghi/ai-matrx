# PDF — the document domain feature

**Status:** Active consolidation (2026-06). One canonical home for every PDF concept; the studios are surfaces, not features.
**The rule that created this feature:** `features/pdf-extractor`, `features/pdf-demo`, and the PDF parts of `features/file-analysis` were never features — they are *surfaces* of this one domain. New PDF capability lands HERE; surfaces compose it. Never add a sibling `features/pdf-*`.

## Canonical parts (use these; never re-create)

| Need | Use | Path |
|---|---|---|
| Call any `/utilities/pdf/*` endpoint | `usePdfClient` (typed; 422-aware errors; blob/JSON content-type guard) | `api/client.ts` |
| Build a request source | `buildPdfSource` / `buildSecondSource` — **`media:{file_id}`, never `cld_id`** (the keystone bug class) | `utils/source.ts` |
| Parse "1,3-5" page ranges | `parsePagesInput` | `utils/pages.ts` |
| Render a PDF | `PdfDocumentRenderer` (THE viewer) / `PdfPreview` wrapper | `components/viewer/` |
| Region overlays on a page | annotation layer (all 4 rotations verified) | `components/viewer/annotation-layer/` |
| Jump between surfaces | `PdfSurfaceSwitcher` + registry (add a surface = one registry entry, every menu updates) | `components/PdfSurfaceSwitcher.tsx`, `surfaces/registry.ts` |
| Switcher + document identity in ONE glass pill | `PdfNamedSurfaceSwitcher` — one `TapTargetButtonGroup` containing: registry-sourced PDF icon + truncated text-[11px] filename (click-to-edit via `EditableLabel` when `onRename` given; edit mode never shrinks — holds measured width, expands to w-64) + the switcher + the full `/files` `···`/right-click menus (self-hydrating via `useEnsureCloudFile`) + opt-in macOS-style search (`onSearchChange`: overlays the field across the pill's content — idle content goes `visibility:hidden` so the width literally cannot change; X clears then closes, Esc closes). Demo: `/demos/pdf-named-switcher` | `components/PdfNamedSurfaceSwitcher.tsx` |
| Resolve fileId ↔ processedDocumentId | `usePdfSurfaceLinks` (bridge-backed, cached) | `hooks/usePdfSurfaceLinks.ts` |
| Download a result blob | `useDownloadBlob` (tracked URLs, unmount revocation) | `hooks/useDownloadBlob.ts` |
| Persist a derived PDF with lineage | `saveDerivative` (fileHandler upload + `processed_documents` row) | `services/saveDerivative.ts` |
| Studio preset catalog UI | `PdfPresetPicker` (catalog from `GET studio/presets`; ids are backend-owned) | `components/PdfPresetPicker.tsx` |
| Compress with metadata | `usePdfOptimize` | `hooks/usePdfOptimize.ts` |
| Tokened inline URL for a cld PDF | `usePdfRemoteSource` | `hooks/usePdfRemoteSource.ts` |
| Phone-scanner session (immediate-upload durability, resume, reorder, crops) | `useScanSession` + `createScanPdf`/`detectDocument` | `scanner/useScanSession.ts`, `scanner/api.ts` |
| 4-corner perspective crop overlay | `QuadEditor` (natural-pixel quads, post-EXIF-transpose space) | `scanner/components/QuadEditor.tsx` |

Old import paths under `features/files/**` and `features/pdf-demo/**` are transitional re-export shims — replace opportunistically, never add new consumers.

## Data model — the bridge (2026-06-11)

One physical PDF = `cld_files` row. Two derived families, now sharing identity:
- **Extractor:** `processed_documents` (clean_content, lineage via `parent_processed_id`) → `processed_document_pages` (raw/clean text, blocks/words, `is_continuation`, sections — the fidelity-rich canonical page table).
- **Analysis:** `file_pages` (user decisions: exclude/rotate/override, thumbnails) → `file_analysis_result`, `file_page_annotations`.
- **The bridge:** `cld_files.canonical_processed_document_id` (backfilled 27/27; trigger-maintained: new docs fill empty bridges, new pages auto-link `file_pages.processed_document_page_id`, new file_pages self-resolve). **One read perspective:** `public.pdf_unified_pages` (security-invoker view).
- **Redaction compliance:** every redaction writes `pdf_redaction_audits` (applied + wired 2026-06-11); reversible spans in `redaction_mapping` (client-held AES keys); `pdf.pdf_redaction_key_escrow` is org recovery, LIVE 2026-07-08 (closes D31): the browser wraps each session key with the escrow KMS RSA public key (never raw — `features/file-analysis/redact/escrow.ts`); recovery is the audited aidream `POST /redact/escrow/recover` (requires `MATRX_REDACTION_KMS_KEY_ID` in the aidream env; 503 loudly until set).

## Surfaces (consumers of this feature)

**Admin map:** `/tools/pdf-extractor/admin` (`app/(core)/tools/pdf-extractor/admin/page.tsx`) — the `FeatureAdminMap` index of every route, panel, canonical part, endpoint, slice, and demo. Add a PDF resource → add it there.

`/tools/pdf-extractor[/[id]]` (extractor studio + window-panel) · `/tools/scanner` (phone scanner — camera/library/files → crop → one PDF → extractor pipeline; entry: "Scan with your camera" in `PdfStudioUpload`) · `/files/f/[id]` (viewer tabs) · `/files/f/[id]/studio` (Analysis Studio; mobile = stacked layout) · `/demos/pdf-processing/*` (27 live API test pages) · RAG `PdfPane` · **Source Inspector** (`features/rag/components/source-inspector/`, overlay `sourceInspectorWindow` — a RAG/citation surface that renders `PdfPreview` at the cited page beside the page's raw/clean extraction + matched chunk; composes pdf parts, never forks them) · code-editor preview · public-chat optimize prompt. All reachable from each other via `PdfSurfaceSwitcher`.

## Invariants

- Sources go through `buildPdfSource` — raw `media`/`cld_id` literals are the bug class that broke the platform (Pydantic silently drops unknown fields; `extra="allow"`).
- Pages are 1-based everywhere on the wire.
- `persist_output=true` switches binary endpoints to a JSON envelope — `usePdfClient.postPdfBlob` guards content-type so an envelope/error can't masquerade as a PDF.
- Derivative ops NEVER mutate the source file; UIs must say so (ConfirmDialog + note pattern, see ManipulationPanel/PagesPanel).
- In-place page ops (`rotate/exclude/include` via file_analysis router) are metadata-only on `file_pages`; callers refetch after success.
- Uploads only via `fileHandler`; no `direct object-store SDK` here.
- Backend preset/pattern/region ids are stable contract strings — never hardcode FE copies.

## Known gaps / roadmap (audit 2026-06-11, plan: `~/.claude/plans/feature-deep-dive-audit-rustling-hare.md`)

- W4 remainder: per-pattern `substitute_formats` overrides.
- Never-built roadmap (vision docs): figure/image extraction with RAG tokens, OCRmyPDF searchable-PDF, cloud-OCR + ML layout providers (`pdf/ml`, `cloud_ocr` stubs), `pdf/generate`.
- Scanner-produced PDFs (`Scans/`, `parent_file_id` set) get no `page1_url` grid thumbnail — the variant pipeline skips derivatives BY DESIGN; the grid falls back to live render.

## Change Log
- 2026-08-10 — **Analysis Studio is agent-writable (live-verified).** `matrx-user/analysis-studio` declares 3 `applyPolicy:"ask"` write targets on the open document's annotations: `annotation_label` and `annotation_extracted_text` (`mode:"entity"`) plus `studio_focus_annotation` (`mode:"ui"`). Handlers are built in [`analysis-studio-write-handlers.ts`](../file-analysis/studio/analysis-studio-write-handlers.ts) and registered on `StudioShell`'s existing `SurfaceRuntimeProvider` via `getWriteHandlers` — every content write goes through `useAnnotations(fileId).update`, the SAME function `PdfRegionContextMenu` writes `extracted_text` with and the canvas writes a dragged `bbox` with, so an agent write and a user click share one door to `PUT /files/{id}/annotations/{aid}`. The label/category vocabulary comes from the live `useLabelCatalog()` catalog rather than re-typed literals: a catalog label id carries its own category (a conflicting `label_category` is REJECTED, not coerced), and a custom label requires a category key the picker actually offers. **`entity`, not the preferred `draft`, because the studio has no staging buffer for an EXISTING annotation** — the label picker's draft state exists only while creating one — and since `useAnnotations.update` patches the shared cache OPTIMISTICALLY, each handler re-reads the row the server returns and throws when the value did not land, so a rejected write can never toast as success. Handlers also refuse loudly when no annotation is selected and no `annotation_id` was passed, when the id is not an active annotation on this document, and while another annotation write is in flight. **Deliberately human-only:** `bbox` and all geometry (an agent cannot see the rendered page — a "corrected" box silently re-points the region at different text; that is a pointer gesture, not authoring), `redact` (a disclosure decision, not a copy edit), delete / `status`, annotation and file identity, and `document_summary` (derived emitter output with no write path). `notes` / `normalized_value` are writable on the API body but have no editor for an existing annotation and no read twin on this surface — no evidence loop, no target. Verified with a real Badass Agent run on a throwaway 2-page PDF (annotations created through Draw → label picker → Save; file deleted afterwards): one message produced three per-target ask dialogs each carrying the manifest description verbatim, Apply persisted the cleaned `extracted_text` and re-labelled a region to the catalog's `billing_amount` with `label_category` auto-resolved to `financial`, "Keep as is" returned a clean decline the agent reported without an error and left the row untouched, a forced `{"label_category":"invoices"}` came back to the model as the handler's own throw listing the real vocabulary with nothing written, and a same-page request to move a bbox / toggle redaction / delete an annotation produced no tool call at all — the agent named the three targets it had and refused all three. `check:surface-drift` + `type-check` clean; DB mirror sync still pending.
- 2026-08-09 — **Region right-click was structurally dead and is fixed.** `AnnotatablePdfCanvas` forwarded the layer's optional `onRegionContextMenu` wrapped in an unconditional arrow, so `PdfAnnotationLayer` always took its handler branch (`preventDefault` + `stopPropagation`) and then called into `undefined`. Right-clicking a region did nothing — native menu suppressed, the v3 `PdfRegionContextMenu` never reached, and its extract-at-bbox / promote-to-entity / redact / delete actions unreachable on both `StudioShell` and `PdfEditTab` since they shipped. Now forwarded conditionally, preserving the layer's documented "omit the prop to opt out" contract.
- 2026-08-09 — **Annotation-layer visual defects fixed + region move/resize shipped** (handoff `pdf-features-fix-session.md` §1/§3, browser-verified on Bio-Chapter-9). (1) `annotation-layer/colors.ts` palette strengthened — strokes at saturated 600-weights, fills ~20% alpha (the 12% `structure` slate was confirmed near-invisible over content), region border 1.5px→2px. (2) **Region move/resize:** `PdfAnnotationLayer` gained `onRegionUpdate(id, bbox)` — in select mode the selected region drags to move and resizes via 8 handles (`RESIZE_HANDLES`), emitting the final PDF-points bbox once on pointer release; both studio surfaces (`StudioShell`, `PdfEditTab`) persist through `useAnnotations.update(id,{bbox})`. (3) **Label picker no longer clips at viewport bottom:** `PendingDraw` now carries the pointer-up `clientX/clientY`, `AnnotatablePdfCanvas` anchors `LabelPicker` there (was window-center), and the popover caps to `--radix-popover-content-available-height` with internal scroll + `collisionPadding`. (4) **Thumbnail strip placeholders:** `usePageThumbnail` no longer caches a failed render forever — bounded auto-retry with backoff + exposed `retry()`; `ThumbnailStrip` shows a pulse skeleton while rendering and a "retry preview" button after exhausted retries, never the old `p1/p2` text placeholder. Glyph-duplication in extracted text remains server-side (aidream `extract/bbox.py`), tracked in the handoff.
- 2026-07-21 — **Scanner camera migrated onto the media-capture runtime** (media-capture Phase 5). `scanner/components/CaptureView.tsx` rebuilt on `acquireCameraLease({facingMode:"environment", profile:"maximum-available"})` + `<CameraPreview framing="full-frame">` + `capturePhotoFromVideo` (canvas path only, JPEG q0.92 — output dims === stream `videoWidth×videoHeight`, the WYSIWYG contract; native `ImageCapture.takePhoto()` deliberately disabled here because it can return non-stream resolution). Contract change: `onCapture` now delivers a JPEG **Blob** (was a data URL); `useScanSession.addCapture(blob)` builds the `scan-NN.jpg` File and a tracked preview URL — all scanner preview URLs now go through `lib/media/object-url-registry` (revoked on remove/clear). Camera switching reads `deviceManager` cameras + lease respec; `notSupported`/`permissionDenied` states and the `<input capture="environment">` OS-camera fallback preserved (fallback file passes through byte-identical). Legacy `components/matrx/camera/` deleted. See `features/media-capture/FEATURE.md`.
- 2026-07-16 — **UUID-filename class fixed at the source (aidream `fd9226f27`).** Studio batch uploads wrote cloud files at `pdf-extractions/{uuid}.pdf`; `cld_files.file_name` = basename(path), so the FILE row was named `<uuid>.pdf`, and `useSyncStudioDocNames` faithfully mirrored that uuid onto `processed_documents.name` (user saw a uuid after refresh). Fix: per-upload FOLDER for collision-safety, real sanitized filename as basename (both aidream write paths); 19 files + 29 docs backfilled live from `metadata.filename`. The FE sync hook is correct and unchanged — never add a "looks like a uuid" guard; the data is the fix. **aidream deploy pending** — until then new studio uploads still get uuid file names.
- 2026-07-16 — **`PdfNamedSurfaceSwitcher` deployed to the extractor studio + chat drawer; doc-switcher + submit search.** New opt-in props: `documents`/`activeDocumentId`/`onSelectDocument` (the PDF icon becomes a document-switcher dropdown), `onSearchSubmit` (Enter — for server-backed search; either search prop alone enables the control; clear/close notify both), `current` now optional (non-surface hosts like the drawer). **Extractor studio:** `PdfStudioDocTitle` cloud branch = the pill (doc list from `usePdfStudioDocs`, select routes like the sidebar; live typing drives `findQuery` highlights; Enter runs the RAG `useDocumentSearch` — ranked hits + matched-page chips render in the Segments pane via `PdfStudioChunksPane` search mode, auto-surfaced with `ensurePaneVisible`; pane ✕ dismisses results only, pill X/Esc clears everything). **Chat drawer:** see `features/agents/components/context-items/FEATURE.md` (registry `Title` slot). **File menus:** `FileContextMenu` RAG section gained "Knowledge assets" (bridge-resolves the doc → `/rag/library/{id}/preview?assets=1`, new deep link) and "Knowledge search" (`/rag/search`). Layout hardening: `.matrx-tap-target` is now `flex-shrink:0` (globals.css), `TapTargetButtonGroup`'s content div is `min-w-0`, and the studio header grid uses `minmax(0,1fr)` side columns without `justify-self-start` — a start-aligned grid item sizes to content and was overflowing UNDER the centered page jumper (clicks hit the jumper instead of the covered Search button).
- 2026-07-16 — **`PdfNamedSurfaceSwitcher`: opt-in macOS-style search.** Pass `onSearchChange` → a search tap button joins the pill; activating it overlays `[icon][input][x]` absolutely across the content area while the idle content keeps layout at `visibility:hidden` — the pill width literally cannot change (no measurement, no lock). X clears-then-closes; Esc closes; closing emits `""`. Input is 16px on mobile (no iOS zoom), `text-[11px]` on desktop. Noted in passing: the global mobile overflow rule (`globals.css` `@media ≤768px * { max-width: 100% }`) overrides all `max-w-*` utilities on mobile, so the name cap only applies ≥768px — mobile pills clamp to their container instead (acceptable behavior, pre-existing global).
- 2026-07-16 — **`PdfNamedSurfaceSwitcher` v2 — everything inside ONE glass pill.** v1 put the name beside the tap group and lacked the file menu; now the pill contains icon + name + switcher + `···` (`FileContextMenu`) with right-click on the name (`FileRightClickMenu`), self-hydrating via `useEnsureCloudFile` (the `PdfStudioDocTitle` pattern, generalized). Name is text-[11px]; edit mode never shrinks — captures the display's rendered width as a floor and expands to w-64. `EditableLabel` extended: `onEditingChange` now also fires as a notification in uncontrolled modes.
- 2026-07-16 — **`PdfNamedSurfaceSwitcher` added** (`components/PdfNamedSurfaceSwitcher.tsx`): the switcher composed with document identity — registry-sourced PDF icon (`icon: "start"|"end"|"none"`), truncated text-xs filename with click-to-edit rename (`EditableLabel`, optional `onRename`), switcher in its own `TapTargetButtonGroup` (geometry untouched — no padding around the group). Live demo at `/demos/pdf-named-switcher` (real files, real `renameFile` thunk, truncation stress rows). Also corrected the stale px numbers in the `.matrx-tap-*` doc comment in `app/globals.css` (said 44/32/16 & 36/24; actual 40/28/14 & 32/20).
- 2026-07-11 — **`PdfStudioPagesMeta`'s Context indicator fixed — it was rendering as a broken oversized pill.** `PdfFileContextRow` forced `ContextStatusButton` (fixed-geometry `TapTargetButton`, meant for header chrome) into a compact metadata-list row via `size="xs" buttonClassName="w-full justify-start"` — both props are no-ops/broken on that component (`size` is discarded; `buttonClassName` *replaces* the status-color class rather than adding to it), so it rendered as a full-size floating pill instead of a row. Rebuilt as a `<dl>` row (same envelope as Pages/Characters/Status/Updated below it — label left, colored "None"/"N scopes" + shield icon right) directly on `ContextAssignmentPopover`, no `ContextStatusButton`. See `features/scopes/FEATURE.md` 2026-07-11 for the root cause and the sibling fix in `NoteContextStatusIcon`.
- 2026-07-11 — **Surface rename:** `matrx-user/pdf-extractor` → `matrx-user/pdf-extractor`; chunked child `extractor-chunker`; siblings `analysis-studio` + `scanner` registered.
- 2026-07-10 — **PDF Studio: unified sub-header rows, no border bleed.** Sidebar/inspector column wrappers no longer duplicate the `border-r`/`border-l` already drawn by the inner `<aside>` — removing the redundant outer-wrapper border stops it from cutting through the transparent shell header at y=0 (the inner border already starts correctly below the `pt-[var(--shell-header-h)]` offset). Every column's top row (sidebar Files/Pages toggle, inspector section-nav pill, every reader `PaneHeader` incl. chunks pane + legacy fallback) now shares one canonical envelope — `px-3 pt-2 pb-1.5` wrapping an `h-7` content row, no `border-b` — instead of each picking its own padding/height/border. Inspector's section nav also switched from a flat button row to the same bordered pill control (`border border-border bg-background p-0.5`) the sidebar's toggle uses.
- 2026-07-10 — **Header Surface Agents chrome: live scope.** `PdfStudioShell` mounts `SurfaceRuntimeProvider` (`matrx-user/pdf-extractor`) with full-doc + current-page scope (Widgets-tab defaults; no range picker in the header path yet).
- 2026-07-10 — **PDF Studio: full-width shell header, sidebars evicted.** `/tools/pdf-extractor[/[id]]` now wires a real `<PageHeader>` (new `PdfStudioHeaderControls`) instead of three column-owned chrome strips — the left/right sidebar collapse rows and the fat center `PdfStudioToolbar` (back + title + ~9 action buttons) are gone. Header (full shell width, matches `/tasks`): `PanelLeftTapButton` + back + `PdfStudioDocTitle` on the left, the page jumper centered, a `MoreHorizontalTapButton` overflow menu (pipeline / AI clean / knowledge assets / copy pages / find / refresh / download / share / open source) + `PanelRightTapButton` on the right — zero gap between tap buttons, default glass variant. Sidebar/inspector now collapse to width 0 (no chevron rail) and only reopen from the header toggles; each of the three body columns clears the glass header via `pt-[var(--shell-header-h)]` (tasks pattern). `PdfStudioToolbar.tsx` deleted — its page jumper moved into `PdfStudioHeaderControls`, its live pipeline/AI-clean status strip now renders inline in `PdfStudioShell` under the header, above the pane-visibility strip.
- 2026-07-10 — **PDF Widgets: Agent Settings opens with Surface tab.** Gear on
  bound-agent rows passes `surfaceName: matrx-user/pdf-extractor` into
  `agentSettingsWindow` so Info | Surface panes appear (Surface = locked
  bind/mapping editor for that agent on this surface).
- 2026-07-10 — **PDF Widgets: surface-bound agents + surface-first bind.** Inspector
  Widgets tab no longer hardcodes two shortcut UUIDs or links to `/agents/shortcuts`.
  It lists agents from `useSurfaceBoundAgents("matrx-user/pdf-extractor")` (incl. default
  contracts) and **Add custom agent** opens the new reusable `surfaceAgentBindWindow`
  (pick agent → scope → map values → bind). Run uses `launchAgent` with the full
  pdf-widgets application scope. Ask-this-document uses the first bound/default agent.
- 2026-07-10 — **Results-surface parity + real scan thumbnails (final design deltas).**
  Extractor toolbar: Download (authed blob; `downloadFile` now exported from the
  `@/features/files` public surface) + Share (canonical `shareModalWindow`) + Indexed pill;
  text-pane subtitles = "N words · M% confidence" (per-page `extraction_confidence`, raw pane
  only); inspector Widgets gained **Ask this document** (question input + 4 suggested prompts →
  Analyze Document shortcut with `runtime.userInput`). Scanner recents render real page-1
  thumbnails via `<MediaThumbnail>` (`RecentScanRow.fileId` from `source_id`). aidream:
  `run_pdf_from_images` dispatches `generate_thumbnail_for_file(allow_lineage=True)` at save
  (scanned masters legitimately carry `parent_file_id` lineage — the flag bypasses only that
  guard); 10 existing scans backfilled live (new CLI `aidream/cli/scan_thumbnail_backfill.py`).
  ⚠️ Root-cause fix ridealong: `thumbnail_source._render_pdf_first_page` read `.bytes`/
  `.mime_type` off `PdfPageRender` (has `.content`/`.format`) — EVERY pdf page-1 thumbnail
  platform-wide had silently fallen back to the mime icon. Residual: pre-fix PDF masters keep
  icon variants under the real keys (key-dedup skips them); healing needs `force_rerender`
  plumbed through — tracked in aidream FOUND_DEFECTS (Deferred).
- 2026-07-08 — **500-page scale items shipped (closes D32).** (1) `PdfStudioReader` text panes virtualized with `@tanstack/react-virtual` (the repo-standard windowing primitive) — only the visible window + overscan of `PageBlock`s mounts; active-page sync is now scroll-position-derived (the old IntersectionObserver can't observe unmounted rows), `pinPageToTop` = `scrollToIndex`, and inline-edit buffers lifted to pane-level `drafts` so an edit survives its row unmounting. (2) aidream `/pdf/clean-content/{doc_id}` routes docs with >200 per-page rows to the **resumable per-page clean** (`matrx_rag.stages.run_clean` — per-page `cleaned_text` rows are the durable checkpoints, retry skips finished pages); stream announces mode via `pdf_clean_started {mode, total_pages}`, FE (`streamPdfClean` `onCleanStarted` + `usePdfExtractor.cleanContent`) auto-retries per-page runs up to 2× on stream death and surfaces per-page progress in the cleaned pane (`streamingStatus`). ≤200pp keeps the whole-doc agent path. (3) aidream ZIP endpoints (`/pdf/split`, `/pdf/render-all`, `/pdf/studio/render` render_all) stream via new `matrx_utils.file_handling.zip_stream.stream_zip`; render-all renders lazily via `pdf.ops.iter_render_pages_to_image_bytes` (bounded queue, one render thread owns the Document) — peak memory O(one page). (4) **Reading-order viewer tab** ("Order") added to the Analysis Studio `InspectorRail` — `features/file-analysis/content/ReadingOrderContent.tsx` streams `extract-reading-order`, renders per-page ordered blocks with column chips, page header jumps the canvas. (5) Extractor uploads now dispatch `generate_thumbnail_for_file` from `_upload_raw_to_cld_files` (page-1 grid thumbnails were missing on 19/20 `pdf-extractions/` masters; files-router uploads were fine). aidream tests: `packages/matrx-utils/tests/test_zip_stream_and_iter_render.py`.
- 2026-07-08 — **Reversible-redaction key escrow wired (closes D31).** The browser now wraps each session key with the org escrow KMS RSA public key (WebCrypto RSA-OAEP SHA-256; raw key never transits the server) and inserts the ciphertext into `pdf.pdf_redaction_key_escrow` (`features/file-analysis/redact/escrow.ts` + `utils/supabase/pdfDb.ts`); recovery = "Recover via organization" in `RestoreDialog` → aidream `POST /redact/escrow/recover` (owner or org owner/admin, KMS Decrypt, every unwrap audit-logged to `platform.activity_log`); `KeyHandoff` copy reflects escrow status (loud red panel on escrow failure). Server: aidream `GET /redact/escrow/wrapping-key` + recover endpoint, env `MATRX_REDACTION_KMS_KEY_ID` (asymmetric RSA ENCRYPT_DECRYPT key; 503 loudly when unset).
- 2026-07-07 — **Design-parity wave** (design sources now in-repo:
  `docs/proposals/pdf-scanner-projects/design/`). Per-page rename (`ScanItem.label`,
  manifest-persisted, editable inputs on desktop Review grid/list), "View all" recents expand,
  50 MB per-file limit (loud reject + dropzone hint), and **enhance modes**
  (Auto/Grayscale/B&W chips in the shared CropSheet — `scanner/enhance.ts` composes the
  EXISTING image-ops registry via `POST /images/edit` (auto_levels / grayscale / levels;
  catalog `GET /images/ops`, ~45 ops — check it before proposing any new image op); each pick
  creates a non-destructive derivative whose id replaces the original at save; ops
  exif_transpose on load so quads stay valid). Save payload: `enhancedFileId ?? fileId`.
- 2026-07-07 — **Desktop scanner hardened** (adversarial review of the ship commit). `ScannerRouteClient` locks the skin ONCE per page load (device heuristic: width<768 or coarse-pointer<1024) — a reactive `useIsMobile` swap remounted the surface across the 768px line (landscape phones got desktop; rotating destroyed mid-upload/mid-save state). Window-level drop guard in `ScannerDesktop` (a drop outside the Home dropzone — e.g. on Review, where the first add auto-flips you — navigated the browser away; now any drop adds files). `useScanSaveFlow` gained a mounted guard (leaving mid-save no longer orphans a 4-min docproc poll or router.pushes from an unrelated page). `fetchRecentScans` now throws loudly on DB errors and filters `owner_id = uid` (the org-member RLS policy would put teammates' org-stamped scans in the personal list).
- 2026-07-07 — **Desktop scanner shipped** (design: "Photo-to-PDF Desktop", user's Claude Design file). `/tools/scanner` splits mobile/desktop in `ScannerRouteClient` (skin locked per page load — see hardening entry): mobile keeps the capture-first phone skin; desktop gets a sidebar + workspace shell (`features/pdf/scanner/components/desktop/` — Home with drag-drop zone / webcam + import cards / recent-scans grid; Review with editable title, Autosaved pill, Grid⇄List toggle, CAM/FILE source chips, dnd-kit list reorder, dashed Add-pages tile). The save/processing orchestration moved out of ScannerSurface into the shared **`useScanSaveFlow`** hook (stream consumption, live page ledger, docproc polling, verified-fetch navigation gate, context prompt, background boundary detection) — ONE engine, two skins. `ScanItem.source` ("camera"|"file") tracks provenance for the chips; `fetchRecentScans` reads docproc via `metadata->>via='/pdf/from-images'` (cheap `clean_content_completed_at` marker, never the body). Results deliberately stay on the extractor — the design's Preview/Text/Ask tabs map onto the extractor's panes + agent panel (one canonical surface). ⚠️ Dev-only gotcha hit twice while verifying: Turbopack's route cache drops `/tools/pdf-extractor/[id]` after mass file changes (git mv, 200+-file parallel sweeps) — restart `next dev` with a cleared cache; prod builds are unaffected.
- 2026-07-07 — **Scanner OCR quality + live processing + mobile extractor pass.** (1) `/pdf/from-images` pages are sized so the 300-DPI OCR render is pixel-exact with the embedded photo (was fixed Letter width — a ~4000px shot reached Tesseract at 2550px); JPEG q92; extraction inside from-images now streams the standard `pdf_extract_started`/`pdf_page_extracted` events. (2) Scanner ProcessingView gained a live per-page ledger (raw chars → AI section titles/kinds as the clean pipeline lands each page — polled via `cleaned_char_count > 0`; the old `cleaned_text is not null` check counted every page instantly because the column is NOT NULL DEFAULT ''). (3) `PdfStudioMobile`: root clears the fixed shell header (its own header/inspector button rendered underneath the shell chrome — inspector was untappable), text-pane pager no longer fights the IntersectionObserver (programmatic-scroll suppression), bottom bar gained a first-class **Agents** button opening the inspector. (4) Camera fill-variant `<video>` sizes via inline styles — an unlayered mobile CSS rule (`img,video{height:auto}`, globals.css ~1775) beats utility classes and clipped the live view bottom (view showed ~2/3 of the captured frame).
- 2026-07-06 — **PDF analyzer endpoints + scanner detect-document converted to NDJSON streams** (same stream-everything wave). The five analyzer demos (`extract-tables`, `classify-pages`, `extract-reading-order`, `detect-repeated-regions`, `strip-repeated-regions` under `(dev)/demos/pdf-processing/`) consume streams via new shared `drainPdfStream` + `<PdfStreamProgress>` (`features/pdf/api/streamDrain.tsx` — wraps `postNdjson`; onProgress for intermediate events, returns the terminal payload, throws on in-stream errors). ⚠️ strip's terminal `pdf_repeated_regions_strip_complete` is FLATTENED vs the old nested `regions.*` body. Scanner `detectDocument` (`scanner/api.ts`) drains the `/images/detect-document` stream and folds the flattened corner fields back into the legacy nested `Quad` — callers untouched. `process-with-ai`/`full-pipeline` terminal event retyped to `pdf_pipeline_result` (drops `pages`/`storage_uri`/`tables_path`; `streamPdf.ts` reads only `file_id` — unaffected).
- 2026-07-06 — **Analysis refresh + in-file search converted to NDJSON streams** (backend stream-everything mandate; old 202+poll / blocking-JSON contracts deleted from the OpenAPI spec). New shared primitive `postNdjson` in `lib/python-client.ts` — the ONE NDJSON client for canonical `{event,data}` envelope streams (typed via generated `stream-events.ts`); use it for any endpoint converted to streaming, never a blocking `postJson` wrapper. `refreshAnalysisStream` (`features/file-analysis/api/file-analysis.ts`) streams `file_analysis_started` → `file_detector_completed` × N → terminal `file_analysis_complete`; `AnalysisTab.handleRefresh` drives a live detector counter from the events and writes the terminal `head`+`results` (byte-compatible with the GET) straight into the shared cache — the blind `setTimeout` refetch is gone, and GET `/analysis` is never a poll target after a refresh (Realtime channel stays for passive viewers/upload-hook). `searchInFileStream` streams `file_search_page` hits live into `SearchPanel`, terminal `file_search_complete` replaces with the canonical set. A refresh that joins an in-flight analysis may emit only the terminal event — both consumers handle that.
- 2026-07-06 — **Scanner v2 polish wave** (12-item user feedback). Route promoted to `/tools/scanner` + Utilities sidebar entry (ScanLine). Camera: rear-camera default fixed (provider adopts the constraint-granted track's device instead of `devices[0]`; skips redundant re-inits), new `CameraView variant="fill"` + `fullFrameCapture` = strict WYSIWYG (whole sensor frame visible, photo = exactly that frame), Done button unwrapped, filmstrip tap → full-screen preview with Delete & retake, background boundary detection per shot. Crop editor rebuilt: measured-fit (whole image + all 4 handles visible on first render — handles live in a screen-space overlay outside the clipped layer), REAL rotation (canvas display copy + quad coordinate mapping; server contract stays crop-before-rotate in original space), pinch/wheel zoom + drag-pan + double-tap reset + post-drag auto-fit, "Try harder" fires the new relaxed detection mode (`detect-document mode="relaxed"`: Otsu region + eps sweep + min-area-rect fallback), 44px remove targets. Save UX rebuilt: full-screen `ProcessingView` (build/OCR from stream; AI-clean per-page progress + entities via 2s `docproc` polling — `scanner/processing.ts`), context assignment now opt-in from that view (no longer piled on the prompt), navigation gated on pipeline completion + verified clean-content fetch (3×2s retries, loud `console.error` misses) — killed the "no extracted text until refresh" bug.
- 2026-07-06 — **Feature admin map shipped** at `/tools/pdf-extractor/admin` (`FeatureAdminMap` via `<FeatureAdminPage>`): studio/scanner/Analysis-Studio routes, `pdfExtractorWindow` + `sourceInspectorWindow` panels, all canonical parts, the full `/utilities/pdf/*` + `/images/detect-document` endpoint surface, `pdfStudioSlice`, and the 27 `(dev)/demos/pdf-processing/*` test pages.
- 2026-07-06 — **Phone scanner shipped** (`features/pdf/scanner/`, route `app/(core)/tools/scanner/` (moved from `/tools/pdf-extractor/scan` 2026-07-06)). Camera (extended `CameraProvider` with `videoConstraints`/`photoQuality` props — 4K rear-camera ideal) / library / file picks; **every add uploads immediately** to hidden `system-files/scanner/{sessionId}` (zero data loss; localStorage manifest + Resume banner; Save blocked until all uploaded); dnd-kit reorder; crop = server boundary detection (**new** `POST /images/detect-document`, cv2) drawn as a draggable `QuadEditor` quad, stored as JSON and applied server-side at build time (**new** `perspective_crop` image op) — originals never mutated. Save = ONE streaming round trip (**new** `POST /utilities/pdf/from-images`): ordered items → one full-resolution PDF persisted as a USER file in `Scans/` (`derivation_kind="scanned"`, uniquified path, full source lineage) → extraction (`_persist_extraction`, OCR on textless pages) → `dispatch_on_upload` (clean/chunk/RAG) → `{file_id, doc_id}`; context assignment runs in parallel via the canonical `UploadContextPrompt`; lands on `/tools/pdf-extractor/{doc_id}`. Quad coordinate contract everywhere: post-EXIF-transpose pixels. On save failure after assembly, the PDF is still in `Scans/` — the error toast says so (never silent loss).
- 2026-07-06 — **`storage_uri`/`file_uri` eradicated from the PDF cluster.** Native storage locations (`s3://…`) are SERVER-ONLY (see `features/files/FEATURE.md`): no `processed_documents` read selects `storage_uri` anymore; `PdfDocument.source` / `StudioDocSummary.source` deleted with every consumer; derivative inserts (`features/pdf/services/saveDerivative.ts`, now also backing the reader's crop/reorder saves) no longer write `storage_uri` (the column is nullable; the backend resolves bytes via `source_kind='cld_file'` + `source_id`). The source wire (`features/pdf/utils/source.ts`, `streamPdf.ts`, `MediaRef` docs) sends only `media.file_id` / `media.url` — the `file_uri` member is gone; `buildPdfSource` resolves cld_file docs only (no-source docs get the existing re-upload empty state). "Open original / View Original / Source File" everywhere routes through `/files/f/{sourceId}`. `PdfStudioUrlViewer` deleted (caller-less once `doc.source` fell); `SyncedPdfTextView` iframes (which never worked for `s3://` rows) replaced with a "open in PDF Studio" note.
- 2026-07-06 — **Upload → new-doc routing made reliable.** Submitting a PDF from the studio (empty-state hero or `+ Add` drawer, desktop + mobile) sometimes never navigated to the new doc even though the server finished and the `processed_documents` row existed. Two root causes, both fixed in `features/pdf-extractor`: (1) the hand-off was gated on the `processed_documents` detail fetch succeeding — a read that can transiently return `null` right after the row is created blocked routing entirely; `usePdfExtractor.extractFiles` now fires a new `onFirstDocId(docId)` callback from **inside** the stream the instant the first file's `doc_id` lands (before/independent of the fetch) and resolves with all completed ids, and `PdfStudioUpload` routes off that instead of a fragile `tabs`-watching effect (deleted, along with its stale-closure session tracking). (2) `fetchProcessedDocument` cached `null` misses for the full 30s TTL, so the destination reader would 404 the just-uploaded doc for 30s; it now caches hits only, so a transient miss self-heals. NOTE: batch-extract emits `doc_id` only at each file's completion (no incremental page persistence), so the reader opens the moment the first file is done — true mid-extraction "live" streaming into the reader needs a server change (early `doc_id` + incremental page rows or realtime).
- 2026-07-06 — **Sidebar doc menu parity with `/files/f`.** Cloud-file-backed rows in the left rail now use the canonical `FileContextMenu` (rename, share, visibility, duplicate, PDF surfaces, RAG actions, delete file, etc.) plus extractor extras (**Copy extractor link**, **Remove from extractor**). Right-click uses `FileRowContextMenu`. Legacy/non-cloud docs keep the studio menu with rename added. Shared rename path: `renameStudioDocument` + `StudioDocRenameDialog`. When every per-page `cleaned_text` row is empty, the cleaned reader pane's banner now offers **Run AI Clean** (same action as the toolbar) instead of **Re-run pipeline** — pipeline is for raw re-extraction, not missing clean content. Raw pane still offers pipeline.
- 2026-07-02 — **AI output now renders through the canonical rich-document engine (thinking handled, markdown rendered).** The extractor's AI-generated content (whole-document clean + full-pipeline template output — live-streaming and final aggregate) was displayed in raw `<pre className="font-mono">` blocks, so the model's `<thinking>`/`<reasoning>` spilled as literal text and markdown didn't render. New single-purpose renderer `features/pdf-extractor/components/PdfAiContent.tsx` wraps `MarkdownStream` (the same engine that renders assistant chat messages), so AI output renders as markdown with the model's chain-of-thought collapsed into a `ThinkingTrace`. Wired into the live-stream + aggregate-cleaned sites of `PdfExtractorWorkspace` (`AiCleanView`), `PdfStudioReader` (`TextPane` streaming + cleaned-aggregate + legacy fallback), and `PdfStudioMobile` (streaming + cleaned aggregate). RAW extracted PDF text stays monospace (it is not AI output); per-page cleaned views and the copy-overlay preview stay monospace for now (they coexist with search-highlight / synced-scroll / inline-edit). Cleaned-aggregate search highlight is intentionally dropped in favor of proper rendering. Part of the app-wide "single canonical path for streamed content" work — see `features/agents/docs/STREAMING_SYSTEM.md` (2026-07-02).
- 2026-06-27 — **New consumer: the Source Inspector** (`features/rag/components/source-inspector/`). A RAG/citation surface that, given a hit (`file_id`/`processed_document_id` + `page_number` + `chunk_id`), takes the user to the EXACT page of the source and unifies the page-anchored content. Reuses, never forks: `PdfPreview` (controlled `pageNumber` lands on the cited page — single page or first-of-range, page nav), `usePdfSurfaceLinks` (file ↔ processed-document bridge), and a NEW focused single-page read `usePageBundle` (one `processed_document_pages` row by `(processed_document_id, page_number)` — deliberately NOT `useProcessedDocumentPages`, which pulls every page; a 600pp reference must not load 600 rows to show one). Extended the canonical `ChunkList` (`features/rag/.../ChunkList.tsx`) with `ChunksOnPage`'s `highlightChunkId` to mark the matched chunk. Verified live: viewer landed on `12/35`, raw/clean text matched the page. (Roadmap tie-in: chunk-bbox highlight via the annotation layer; figure/table extraction once that data model exists.)
- 2026-06-14 — **PDF-Studio toolbar slimmed; metadata moved to pages sidebar.** Toolbar row 2 (chips, context, updated) removed; new `PdfStudioPagesMeta` sits above the pages list (context picker first, then pages/chars/status/updated). Center toolbar keeps one row: back + title/actions, `PageJumperTapGroup` (`ChevronLeft/RightTapButton` + inline count), and action `TapTargetButtonGroup`. Added `ChevronRightTapButton`, `PartyPopperTapButton`, `ClipboardListTapButton`, `ExternalLinkTapButton`, `LayersTapButton`; `PdfSurfaceSwitcher` gains `triggerVariant="group"`. The old (title + id + provenance breadcrumb) block is gone, replaced by new `PdfStudioDocTitle` — the filename is **click-to-edit in place** (`EditableLabel`; commits rename `processed_documents.name` authoritatively + best-effort renames the backing cloud file via `renameFile` so `/files` stays in sync; optimistic with revert-on-error in `PdfStudioShell.handleRenameDoc`) and a `…` button surfaces a full action set. For cloud-file-backed docs the `…` (and right-click on the name) reuse the `/files` route menus verbatim (`FileContextMenu` / `FileRightClickMenu`: share, visibility, versions, duplicate, RAG actions, PDF surfaces, **Delete**), hydrated into the files store by new reusable hook `features/files/hooks/useEnsureCloudFile.ts`; non-cloud docs get the lighter `buildPdfDocMenu` (open / copy link / delete-from-studio). `PageJumper` lost the "Page" label, the number is no longer oversized (matches the count at `text-[11px]` on desktop, `text-base` only on mobile to dodge iOS zoom), and the prev/next chevrons are flush to the count (one connected control). `OverflowToolbar` icon-only buttons now carry a proper styled `Tooltip` (not a native `title`).
- 2026-06-13 — **PDF-Studio toolbar/file-list cleanup.** New reusable primitive `components/official/toolbar/OverflowToolbar.tsx` — a row of uniform compact buttons (same height/padding/icon size; `tone:"primary"` colors without enlarging; `hideLabel` = icon-only + tooltip for obvious actions) that collapses the actions that don't fit (measured via a hidden ghost row + ResizeObserver, collapsing from the end) into one `…` `ItemMenu`. `PdfStudioToolbar` actions (Pipeline/AI Clean/Copy Pages/Find/Source) now route through it (Pipeline no longer visually larger); metadata chips moved to row 2 to free row 1. File rows in `PdfStudioSidebar` gained a kebab + right-click menu (mirrors the chat sidebar's `ItemMenu` pattern) via new `pdfDocMenu.tsx` — Open in new tab / Open original file / Copy link / **Delete**. Delete archives the doc (`processed_documents.archived_at`, soft, recoverable) via new `usePdfStudioDocs.deleteDoc` (optimistic); the shell + mobile clear the active doc and route to `/tools/pdf-extractor` when the open doc is deleted.
- 2026-06-13 — **Recurring data-integrity system** for the file system + PDF document bridge. New generic framework `lib/integrity/` (declarative check registry + runner), super-admin API `app/api/admin/integrity/route.ts`, admin page `/administration/database/data-integrity`, and CI/cron CLI `pnpm check:data-integrity[:strict]`. Checks (SQL via `execute_admin_query`, scale-safe window-count + LIMIT sample): visible `unrecoverable://` files, empty `storage_uri` (server-side SQL only — `storage_uri` is BANNED on the client, see `features/files/FEATURE.md`), dangling folder/bridge/duplicate refs, orphaned + deleted-source `processed_documents`, plus an opt-in live S3 byte probe. Add invariants by appending to `lib/integrity/checks.ts` — both UI and CLI pick them up. First run found 2 visible-unrecoverable (the known dead-source class, incl. ACOEM) + 2 processed_docs-on-deleted-source; rest clean.
- 2026-06-12 — **"Source" button / mobile PDF tab no longer dead on cld_file docs.** Both opened the raw `processed_documents.storage_uri` (an `s3://…` URI for cld_file-backed docs) — `window.open("s3://…")` and an iframe `src="s3://…"` both yield a permanently blank tab/pane. PDF-Studio toolbar "Source" now routes cld_file docs to the in-app viewer `/files/f/{sourceId}` (auth-safe, progressive render), opens http(s) sources directly, and toasts on anything unopenable; the button hides entirely for `s3://`-only docs with no cld_file. `PdfStudioMobile` now renders cld_file sources through the shared `PdfCldFileViewer` (exported from `PdfStudioReader`) instead of the broken iframe. Desktop reader pane was already correct.
- 2026-06-11 — **Missing-source hardening.** `usePdfRemoteSource` now does a single PK health probe on `cld_files` and returns `sourceMissing` when the row is absent or soft-deleted (the failure class introduced by the 2026-05 AWS migration, which trashed original binaries while leaving extracted text intact). Both the app-wide `PdfPreview` and the PDF-Studio `cld_file` viewer render the new `PdfSourceUnavailable` panel instead of pdfjs's raw "Failed to fetch" card. PDF-Studio list (`usePdfStudioDocs`) and legacy workspace (`usePdfExtractor.loadHistory`) now exclude `archived_at` docs and the studio sidebar flags any remaining `sourceMissing` doc ("Original file removed · text only"). Data: the 10 dangling Studio docs for the affected owner were archived (reversible, text preserved) rather than deleted.
- 2026-06-11 — Feature created from the consolidation build: keystone `cld_id`→`file_id` fix; canonical client/source/pages/download/saveDerivative parts; viewer family moved in from features/files; surface switcher + registry; W2 bridge + unified view + redaction audits/escrow migrations; detect→redact one-flow; preset picker; detector prefs end-to-end; deep-link self-heal; mobile Analysis Studio; reliability floor (stream cancellation, abort/timeout, transactional clears, per-doc locks, resource bounds).
