# Shared menu sections — THE REGISTRY

**The rule (Arman, 2026-08-25):** when a surface shows a thing (a keyword, a page, a topic, a contact, …), the right-click actions for that thing come from ONE shared section builder, used everywhere the thing appears. A per-surface copy of another surface's actions is a defect.

## THE ADOPTION PROTOCOL — every agent wiring a menu runs this

1. **Look here first.** If the right-clicked identity already has a row below, use that builder. Never re-implement its items inline.
2. **No row, but the identity appears on 2+ surfaces?** Extract a shared builder (pattern: `useKeywordMenuSection`), register it here, and use it.
3. **No row, identity is truly page-local?** Inline `extraSections` on that pane is correct. Do not register one-offs.
4. **THE GROWTH STEP (the most important one).** When you adopt an existing section on a new surface, list every action a user would reasonably want for this identity _on this surface_. Almost always the section is missing some. Add them TO THE SHARED BUILDER — every prior consumer gains them. Never bolt a private sibling section next to the shared one for the same identity.
5. **THE CONSISTENCY STEP** — the mechanism is [`utils/availability.ts`](./utils/availability.ts), do not hand-roll it. An action the shared section offers that cannot work on this surface stays VISIBLE but `disabled`, with a `description` that names the surface where it works (the disabled-reason tooltip is the ONE sanctioned use of `description` — see THE DENSITY LAW in the skill). The menu stays the same everywhere; only availability changes. An action gated on data the row may lack (no library keyword, no page id) follows the same rule — disabled with the reason, never absent, never a silent no-op.

## The registry

| Identity                               | Builder                                                                   | File                                                                         | Consumers (keep current)                                                                                                                                                                               |
| -------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Keyword / query                        | `useKeywordMenuSection` + `useKeywordAssignSurfaces` + `keywordEntityRef` | `features/marketing/seo/keyword/keyword-actions.tsx`                         | KeywordWorkbench · ValueWorkbench · RanksWorkspace · GscDimensionTable · DigResultsTable · WatchlistTab · Insights (movers/shifts/ctr-gap/cannibalization/trends via `insight-row-menu`) · RunConsole (`RunDecisions`) · KeywordResearchWorkbench (library) · PageSearchConsoleCard · LocationDecomposition (traffic-by-location drilldown) · MatcherReviewBody (matched-keyword review, opened via MatcherReviewWindow and QuickAnswers via KeywordQuickAnswersWindow) |
| Marketing site / brand (`web.site`)    | `siteMenuSection` + `siteEntityRef`                                       | `features/marketing/seo/run-console/site-menu.tsx`                           | RunConsole (brand table) · SituationalRefreshConsole (brand table) · PlanSitesList (content-plan list)                                                                                                |
| Traffic class (a filter, not a record) | `classMenuSection`                                                        | `features/marketing/search-console/components/insights/insight-row-menu.tsx` | Insights Quality view                                                                                                                                                                                  |
| Value level (a filter, not a record)   | `levelMenuSection`                                                        | same file                                                                    | Insights Quality view                                                                                                                                                                                  |
| Site page (by `web.page` id / URL)     | `pageMenuSection`                                                         | same file                                                                    | Insights page-dimension tables · NewPagesTab (launch tracker) · PagesTable · DismissedPagesTable · SitemapDetail · SiteAnalysisTable (priority-queue findings, by their affected page) · SitePerformanceWorkspace (suggested/worst/most-improved/most-regressed tables, grown with a page-local "Run PageSpeed test" item) · AuthorityRouterWorkspace (evidence table)                                                                                                                                                                         |
| Page snapshot (`web.snapshot`)         | `snapshotMenuSection` + `snapshotEntityRef`                               | `features/marketing/components/pages/snapshot-actions.tsx`                  | SnapshotsTable · SnapshotCompare (before/after) · CrawlSnapshotsInspectionTable · CrawlReportWorkspace (snapshot-report table) — future adopter: SnapshotArtifacts                                                                                                                                                                |
| Discovered media asset (`SnapshotMediaAsset`, keyed by `src` — no table row) | `mediaAssetMenuSection` (plain function; no entity ref — no id of its own) | `features/marketing/components/media/media-asset-actions.tsx` | MarketingMediaAssetWindow (image body + inspector) — future adopters: SnapshotMediaGallery, CrawledMediaView, SiteMediaWorkspace, PageMediaCard (same `SnapshotMediaAsset` shape, no menu today) |
| Link edge (`web.link_edge`)            | `linkEdgeMenuSection` + `linkEdgeEntityRef`                               | `features/marketing/components/inspection/link-edge-actions.tsx`            | LinksInspectionTable (site graph + crawl link edges) · CrawlReportWorkspace (broken-links report)                                                                                                     |
| Run URL (`web.crawl_url`)              | `crawlUrlMenuSection` + `crawlUrlEntityRef`                               | `features/marketing/components/crawls/crawl-url-actions.tsx`                | CrawlUrlsTable (full ledger) · CrawlReportWorkspace (response-log report)                                                                                                                             |
| CRM row (party/list)                   | `useCrmRowMenu`                                                           | `features/crm/components/crm-row-actions.tsx`                                | CrmListPage · OutreachListsPage                                                                                                                                                                        |
| Workflow run                           | `useWorkflowRunMenuSection`                                               | `features/workflow-runtime/run-actions.tsx`                                  | RunsList · ReadoutView (stands in for the readout's owning run) · WorkflowRunWindow (floating run adoption) — future adopters: WorkflowBattlePage, EncoreRunPage, UsageHistoricalContext |
| Captured product item (`workbench.product_capture_item`) | `useCaptureItemMenuSection` | `features/product-capture/item-actions.tsx` | AllItemsTable — future adopters: ItemDetailView, ItemsSheet, pipeline/ItemWorkspace |
| Code file (`code_file`) | `useCodeFileMenuSection` + `codeFileEntityRef` | `features/code-files/file-menu.tsx` | CodeFileManagerWindow (file-browser list, grown with "Copy path") — future adopter: LibraryTreeNode (`/code` workspace file tree; still an inline `fileMenuSections` copy — migrate on next touch) |
| Embedded browser frame (cross-origin iframe — no entity, URL/title only) | `browserFrameMenuSection` | `features/window-panels/windows/iframe/browser-frame-menu.tsx` | BrowserFrameWindow · BrowserWorkbenchWindow (active tab, grown with page-local "Bookmark this tab") |
| Flashcard (front/back/index — the owning `fc_set` is the entity, cards have no row of their own) | `useFlashcardMenuSection` + `flashcardEntityRef` | `features/flashcards/components/flashcard-menu.tsx` | FlashcardItemWindow · FlashcardStudyWindow (grown with "Flip card") · FlashcardsBlockWindow (grid, grown with "Open full view") · FlashcardSubcardsWindow (nested grid) — future adopters: CanvasFlashcardsView, StudyDeck, `flashcards-set-parts` markdown block body (same front/back shape, no menu today) |
| CX dashboard record (request/conversation/iteration/tool call) | `useCxRowMenu` + `cxEntityRef`                            | `features/cx-dashboard/components/cx-row-actions.tsx`                        | CX overview (tool usage, page-local) · RequestsContent · RequestDetailContent (request/iterations/tool-calls) · ErrorsContent (problem requests/tool-call errors) · ConversationsContent               |
| Scheduled task (`scheduler.sch_task`)  | `useScheduledTaskMenuSection`                                             | `features/scheduling/components/shared/scheduling-menu-sections.tsx`        | Admin › Automation › Scheduling: Tasks · SeoOperationsClient (Automations panel, added `onRunNow` growth). `scanner-health/page.tsx` names the same identity via `EntityRef token="scheduled_task"` and should adopt this on next touch. |
| Scheduled run (`scheduler.sch_run`)    | `useScheduledRunMenuSection`                                              | same file                                                                    | Admin › Automation › Scheduling: Runs · Orphan leases                                                                                                                                                  |
| HR task inbox step (`hr.workflow_step`, `hr_wf_inbox` projection) | `hrTaskStepMenuSection` + `hrTaskStepEntityRef` | `features/hr/tasks/task-step-actions.tsx` | LeaveQueueSurface (§4.4 leave projection; decision items stay page-local) · HrTaskTable — future adopter: HrTaskInbox (same `HrInboxRow` shape, no menu today) |
| HR attendance exception (`hr.attendance_exception`) | `attendanceExceptionMenuSection` + `attendanceExceptionEntityRef` | `features/hr/time/shared/exception-actions.tsx` | ExceptionsQueue (route 31; grown with `onAcknowledge`) — future adopters: ExceptionsStrip, PeriodApprovalGrid, ExceptionDoor (same `AttendanceExceptionRow` shape, no menu today) |
| HR person / employee                   | `hrPersonMenuSection` + `hrPersonEntityRef` + adapters (`hrDirectoryRowTarget`, `leaveBalanceRowTarget`) — thin (Open profile only), for surfaces with just a name + href | `features/hr/people/hr-person-menu.tsx`                             | LeaveEnrollmentSurface (roster + candidates) · LeaveBalancesSurface (`onAdjustBalance` grown in) — future adopters: HrDirectoryCards, directoryColumns, EmploymentPicker, LeaveDecisionDialogs |
| HR employee — FULL verb set (the canonical `ItemMenuConfig`, one per SPEC-EMPLOYEES §1.3/§2.2) | `useHrEmployeeMenu` (builds `ItemMenuConfig`), rendered as a v3 menu via `rowWrapper` + `ItemContextMenu` (converts through `itemMenuConfigToExtraSections`) | `features/hr/people/directory/useHrEmployeeMenu.tsx` | HrDirectory (table, via `rowWrapper`) · HrDirectoryCardGrid (kebab) · OffboardEmployeeDialog — prefer THIS over `hrPersonMenuSection` wherever the host already has a full `HrEmployeeMenuSubject` (employeeId + employmentId + partyId + directReportCount); the thin section stays for surfaces that only have a name and a door |
| HR leave policy                        | `leavePolicyMenuSection`                                                  | `features/hr/leave/policies/leave-policy-menu.tsx`                          | LeavePolicyListSurface — future adopter: LeavePolicyEditorSurface (same policy, its own edit form)                                                     |
| HR punch (`hr.punch`)                  | `punchMenuSection`                                                        | `features/hr/time/shared/punch-menu.tsx`                                     | PunchRegister — future adopters: PunchChain, PunchCorrectionDialog. No new write path: every item delegates to a door the host already owns. |
| Referring domain (`seo.referring_domain_profile` / provider snapshot) | `useReferringDomainMenuSection` + `referringDomainEntityRef` | `features/marketing/components/backlinks/referring-domain-actions.tsx` | ReferringDomainIntelligenceTable (our view) · BacklinkDimensionTable kind="domain" (provider view) — same `/backlinks?view=domains` tab |
| Backlink prospect domain (link-gap or SERP, pre-CRM) | `useProspectDomainMenuSection` + `prospectDomainEntityRef` | `features/marketing/components/backlinks/prospect-actions.tsx` | BacklinkProspectsTab (competitor-link method) · SerpProspectsTab (SERP method) — same `/backlinks?view=prospects` tab |
| Admin user account (Supabase auth user — no entity token yet) | `adminUserMenuSection`                            | `features/admin/users/components/admin-user-menu-section.tsx`               | AccountsTableClient (roster) · UsageTableClient · UserAcquisitionTableClient · PreferencesTabClient (drift table) · OrganizationsAdminClient (member table) — mirrors the doors `AdminUserRef` already gives the row's name; future adopters: AnnouncementsTableClient/InvitationsTableClient (reviewer column, page-local today) |
| System announcement (`SystemAnnouncement`, no entity token yet) | `announcementMenuSection`                        | `features/admin/users/components/announcement-menu-section.tsx`             | AnnouncementsTableClient — future adopter: `app/(admin)/administration/users/feedback/components/AnnouncementTable.tsx` (same type, not yet adopted) |
| Relationship rule (`platform.relationship_rules` — composite key, no entity token) | `useRelationshipRuleMenu` | `features/admin/relationships/components/relationship-rule-actions.tsx` | RelationshipRulesClient (registry table) — future adopter: `EntityRelationshipOrbit` (orbit diagram card renders the same identity, read-only today) |
| Content plan node (`plan.node`) | `planNodeMenuSection` + `planNodeEntityRef` | `features/marketing/content-plan/components/plan-node-actions.tsx` | PlanNodesTable (table view) — future adopters: PlanTree, SiteMap, NodePanel (same `PlanNodeRow` shape, no menu of their own yet) |
| Competitor (`seo.competitor`) | `useCompetitorMenu` + `competitorEntityRef` | `features/marketing/competitors/competitor-actions.tsx` | GroundTruthQueue (ruling queue) · CompetitorAutopsyWorkspace (Competitors tab) |
| SEO collection run (`seo.collection_run`, every SEO command's run record) | `collectionRunEntityRef` (entity/content only — no row-actions hook yet) | `features/marketing/seo/run-console/collection-run-actions.tsx` | CompetitorAutopsyWorkspace (History tab) — future adopters: `CollectionRunView` (share page), `RunHistoryPanel` (merged execution list) |
| Pay-period timecard (`hr_pay_period_employment`) | `periodGridRowEntityRef` (entity/content only — no row-actions hook yet) | `features/hr/time/timesheet/period-grid-row-actions.tsx` | PeriodApprovalGrid (approval table) — future adopter: `BulkApproveDialog`'s eligible/excluded confirmation lists, a static preview with no menu of its own today |
| Agent (agent definition — the SUBJECT of an agent-native window, not a roster/Bind) | `useAgentMenuSection` + `agentEntityRef` | `features/agents/menu/agent-actions.tsx` | AgentMemoryWindow · AgentTestCasesWindow · AgentSkillsWindow · AgentConnectionsWindow · AgentFindUsagesWindow · AgentAdminFindUsagesWindow · AgentSettingsWindow · AgentShortcutQuickCreateWindow · AgentConvertSystemWindow · PromptPreviewWindow · SystemInstructionWindow · ScopeBatchImportWindow · AgentCreateAppWindow — future adopters: AgentContentWindow (inline "agent-advanced-editor" section, same items, not yet extracted), AgentDebugWindow (currently a bare "Copy agent ID" button), AgentViewContent, InstanceUIStateList, AgentRoleCard |
| Note (`note`) | `buildNoteContextSections` | `features/notes/components/note-actions/noteMenuRegistry.tsx` | NoteSidebarRow (sidebar list) · NoteTabItem (open tab) · NoteInfoWindow (info window, `move-new-folder` disabled — the Folder section already offers it, `onOpen` opens the canonical Notes window since the info window has no tab instance of its own) |
| Task (`task`) | `createTasksExtraSections` | `features/tasks/agent-context/buildTasksContextData.ts` | TaskListPane (list, grown with "Duplicate task") · TaskEditorBody (description field) · TaskEditorWindow (backstop over title band + body, same `controller` handlers TaskEditorBody uses) |
| Context item (`context_item`, a scope type's item definition) | `useContextItemMenuSection` + `contextItemEntityRef` | `features/scope-system/components/context-item-actions.tsx` | ContextItemsWindow (sidebar) — future adopters: ContextItemEditView, EditContextItemSheet, ContextItemsHub, ScopeItemDetail, ScopesList, OrgHomeScopeSection (same `ContextItem` shape, no menu of their own today) |
| UDT dataset table (`workbench.udt_datasets` / `dataset` entity token, the table AS A WHOLE — not a row/cell inside it) | `useDatasetTableMenuSection` + `datasetTableEntityRef` | `features/data-tables/dataset-table-actions.tsx` | UserTableWindow (full-size floating viewer) · QuickDataWindow's `QuickDataSheet` (table picker + inline preview) — future adopters: `DataTableDetailClient` (`/data/[id]`), `OrgResourceList`, `DatasetPeek` (same `udt_datasets` row, no menu of their own today) |

Registered-but-inline candidates (identity appears on 2+ surfaces, builder not yet extracted — extract on next touch): **SEO topic** (`TopicTreeWorkbench` inline; topics also appear on the value workbench and receipts), **value rule / service area** (`MeaningRulesWorkbench` inline; rules also surface on meaning-health and pack screens), **mandate** (`MandatesConsole` has its own rich inline menu; `SeoOperationsClient` Mandates panel now adds a minimal inline "Open in mandates console" item using the same `mandate` entity token rather than duplicating the console's actions — extracting a shared builder is future work).

## The consistency step, in code

A shared builder takes an `unavailable` map; the host declares only what it CANNOT do:

```ts
const section = useKeywordMenuSection({
  …,
  unavailable: {
    "kw-pages": unavailableHere("the Keyword Workbench"),
    "kw-intel": needs("a library keyword"),
  },
});
```

`withAvailability` keeps the row, its label, its icon and its POSITION, sets `disabled`, puts the reason in the tooltip, and neuters `onSelect`/`href` so it cannot fire even if a renderer ignores the flag. A submenu whose every child is dead is disabled too, so the user learns without opening it.

**Per-surface gating works today. Per-ROW gating works only when the host keeps the clicked row in STATE** (as `MeaningRulesWorkbench` does) — a `useRef` host cannot re-render, so a row-dependent `unavailable` map is computed from a stale row. Ref-based hosts must move the clicked row to state before gating per row.

## Registration contract

A registered builder:

- returns `ContextMenuExtraSection` (or `{section, node, entityRef}` when it owns assignment surfaces), takes a `getRow: () => Row | null` reading the pane's clicked-row state — never a captured row;
- ships the identity's `entityRef` helper so Attach To targets the row (`CONTEXT_MENU_ENTITY_KEY`);
- adds NO new write path — every item delegates to an existing RPC/opener/route;
- obeys THE DENSITY LAW: labels are a short verb phrase, no `description` except a disabled-reason.

**After any change here: update the Consumers column and the matching FEATURE.md in the same commit.**
