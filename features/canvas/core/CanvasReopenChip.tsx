"use client";

/**
 * @deprecated Replaced by `CanvasShellHeaderToggle` in the app shell header
 * (next to the avatar). Kept as a no-op so stale imports fail silently.
 */
export function CanvasReopenChip() {
  return null;
}
