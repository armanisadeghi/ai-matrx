# FEATURE.md — `agents`

**Status:** `migrating` (active rebuild — see `features/agents/migration/`)
**Tier:** `1` — core of the product
**Last updated:** `2026-08-09`

> This file is the **entry point** for the agents system. The system is large enough that it has its own `docs/` subdirectory with sub-feature docs. Start here, then jump to the relevant sub-doc.

---

## 🚨 THERE IS ONE AGENT PICKER. Building a second one is a defect.

**Any UI that lets a person choose an agent renders `AgentListDropdown` (trigger +
popover/drawer) or `AgentListInlinePicker` (embedded panel).** Both are shells over the
ONE core — `useAgentListCore` + `AgentListContent` — so every agent-selection surface in
the product gets the full set, always:

search · **Mine / Shared / All / System tabs with live counts** · sort · favorites ·
category filter · tag filter · reset · current-agent pinned on top · per-row origin badge
(`system` / `shared`) · detail peek · cmd-click to open the agent · footer count.

- **A list + a text box is NOT an agent picker.** That shape (`SearchableAgentSelect`)
  existed in 8 places, shipped none of the above, and was **deleted 2026-08-08**. Do not
  reintroduce it under any name. If a surface needs an agent, import the canonical picker.
- **No tab is allowed to be poorer than its neighbours.** The System tab used to offer
  sort only; it now carries the identical filter bar, with category/tag options drawn from
  the builtin population (`selectAllSystemAgentCategories` / `…Tags`).
- **User vs ADMIN variant — same component, two props.** `initialTab` picks the opening
  tab; `includeSystemInAll` is the admin reading of "All" (system agents blend into it,
  and the tab reads **System** instead of **Public**). Admin surfaces that manage system
  agents pass `initialTab="system" includeSystemInAll` — an admin owns the system agents
  AND their own, must reach both, and must never confuse them, which is what the per-row
  `system` badge is for.
- **Never hand a picker a pre-built `agents` array.** It reads the canonical Redux
  agent-definition slice itself. A caller-supplied list is how a surface silently ends up
  showing a partial set.

Files: `features/agents/components/agent-listings/` — `AgentListDropdown.tsx`,
`AgentListInlinePicker.tsx`, `useAgentListCore.ts`, `core/AgentList{Content,Tabs}.tsx`,
`core/AgentFilterBar.tsx`, `core/AgentRow.tsx`.

---

## Purpose

Agents are autonomous AI specialists. The AI Matrx Harness turns a raw model into one by providing persistent context, tool execution, orchestration, and multi-surface invocation. Everything in the product that does AI work is ultimately an agent invocation — Chat, Runner, Shortcuts, Agent Apps, Builder.

---

## The mental model in one page

The system runs in three stages with three consumer surfaces:

1. **Build** — `Agent Builder` (`/agents/[id]/build`) — engineers craft identity, instructions, model, settings, tools, variables, context slots. Every save creates a new version.
2. **Test** — `Agent Runner` (`/agents/[id]/run`) — same runtime as Chat with observability on; can pin any past version.
3. **Consume** — `Chat` (user conversations), `Agent Shortcuts` (click-to-fire invocations with auto-mapped variables), `Agent Apps` (custom UIs for workflows).

### Two invocation payloads — the key distinction

| Surface                            | Endpoint on first turn | Payload includes                                                             | Why                                                                                             |
| ---------------------------------- | ---------------------- | ---------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| **Builder**                        | `POST /ai/manual`      | **Live agent definition** flattened into the manual request (priming messages, model, settings, tools, variables) | Cache-independent raw test — builder must see exactly what the server will run, no hidden state |
| **Runner / Chat / Shortcut / App** | `POST /ai/agents/{id}` | **Agent ID + form values** (variables, context, user input)                  | Server owns the agent definition; client sends only what changes per call                       |

After the first turn, saved-agent surfaces collapse to `POST /ai/conversations/{conversationId}` (or `POST /ai/chat` for ephemeral). Builder tests remain on `POST /ai/manual`: the client carries their accumulated history and the server mints a fresh durable wire conversation for each stored request while Redux keeps one stable local test-panel key. See **AGENT_INVOCATION_LIFECYCLE.md**.

One-shot feature invocations use `features/agents/run/useRunAgent`. It delegates
to canonical `callAgentStart`/`callApi`, so auth, API versioning, diagnostics,
global org/project/task fallback, source attribution, and streaming share the
same request boundary as every other agent surface. Entity-local callers pass
their own scope (`organizationId`/`projectId`/`taskId`); explicit local identity
beats global active context for genuinely new work. When a run belongs to an
existing durable entity, callers also pass `contextAnchor` (stable resource
type/id only). Aidream reloads that row and its saved scope wins over both local
and global browser context. Never rebuild a bare `useBackendApi` agent body.

Admin/dev desktop targeting is an additive request overlay, not part of agent authoring. When `adminPreferences.desktopTargetInstanceId` is set by the admin Server environment tab, first-turn, continuation, resume, manual, and legacy `callApi` agent turn paths stamp `client.state["desktop-native"].target_instance_id` with the selected `public.app_instances.instance_id` (`/desktop-instances.id`); compatibility paths also include a root `target_instance_id`. Auto stores `null` and omits the target so aidream keeps default routing.

### Variables vs. context slots

- **Variables** are required inputs — missing them leaves the agent confused. Bound by name from `invocation.inputs.variables`.
- Choice-backed variables may opt into automatic random assignment with `customComponent.assignment.random=true`. Smart Agent Input then sends the exact object marker `{type:"auto_assign",strategy:"random"}`; it never chooses in the browser. The server selects from the definition's authoritative static options or Structured List with OS-backed cryptographic randomness.
- **Context slots** are optional, auto-filled from ambient sources (user profile, org, active project, scope mappings). Their absence is graceful.
- **Everything else** is fetched on demand via tool call, not injection.

### Agent Resources

**Agent Resources** are durable content attached to the agent itself. They are
neither prompt blocks nor run inputs: a canonical
`resource → agent` `platform.associations` edge with
`role="agent_resource"` makes the resource available on every invocation.

- Use **Agent Resources** for permanent knowledge such as a handbook, corpus,
  note, transcript, workbook, or data store.
- Use a **document variable** for a file selected independently on each run.
- Use a **document message block** only when the file must be sent in that exact
  authored message. Owned files persist as `file_id`; MIME is resolved by the
  server and is never an authoring requirement.
- Files enter the backend Document Evidence System: processed text is primary,
  while RAG, raw/clean representations, selected physical PDF pages, and
  verification tools are auto-injected.
- Sharing the agent conveys contextual `viewer` access to attached resources,
  capped by `platform.association_types`. Reachability does not make those
  resources globally discoverable. Adding requires `editor` access to both the
  agent and source resource, because attachment conveys access to other agent
  viewers. Removal requires agent `editor` access.
- In Builder, Resources is one compact top-row beside Variables and Context:
  horizontally scrolling canonical resource chips followed by **Add** and
  **Batch add**. Both actions reuse `ResourcePickerMenu`; batch mode keeps the
  picker open for repeated selections. File edges render through
  `FileResourceChip`, so click/hover/details behavior is identical to Stored
  Files everywhere else. Never mount the large `AssociationList` browser here.

### Versioning

Every Builder save = new `agent_definition` version. Runner + Chat default to the current pointer. **Shortcuts and Apps pin to a specific version** so embeds never break when the agent evolves. Drift is surfaced via **Find Usages & Drift** (below), never auto-resolved. See **AGENT_VERSIONING.md**.

The `/agents/all` gallery also recognizes a complete version UUID in its normal search field. It resolves the RLS-authorized `agent.definition_version` row to its parent agent and numeric version, then offers the matching version page. This is the canonical UI entry point when a caller has only a version ID.

### Linked agent sync (user agent ⇄ system twin)

A user agent and its system (`builtin`) twin are linked by `source_agent_id` on whichever side was derived — the link points **either direction**. The panel is `AgentSyncBody` ([components/admin/AgentSyncBody.tsx](components/admin/AgentSyncBody.tsx)) inside `AgentConvertSystemWindow` (overlay `agentConvertSystemWindow`, opened from `AgentOptionsMenu` and the Agent Slots console). Actions: Pull (system → my copy), Push (my copy → system, super-admin), Create my personal copy, Convert to system.

**The panel states a verdict before it offers an action.** A "last synced" timestamp is provenance, not an answer, and the panel used to show nothing else — it invited an overwrite without ever comparing the two agents.

- **ONE field set: [`sync/sync-fields.ts`](sync/sync-fields.ts) → `AGENT_SYNC_FIELDS`.** It mirrors exactly the `agent.definition` columns `agx_sync_linked_agents` copies, split **identity** (`name`, `description`, `category`, `tags` — only on `p_include_identity`) vs **behavior** (the other 14, always). Everything — the snapshot `select`, the verdict, the per-field summary, the button labels — derives from this list, so the verdict can never disagree with what Pull/Push writes. **Add a column to the RPC's `UPDATE` ⇒ add a row here in the same change.** Guard: `pnpm check:sync-fields` parses the SET clause out of the deployed function and screams on drift.
- **Verdict: `identical` | `differs` | `unknown`** — [`sync/compare.ts`](sync/compare.ts), a pure module (no React, no Redux, no network; 62 unit tests in [`sync/__tests__/compare.test.ts`](sync/__tests__/compare.test.ts) built on the real production pair). `unknown` means a side could not be read (RLS/deleted) and is **never** downgraded to `identical`.
- **It reuses `computeDiff`** (`components/diff/engine/`), not a second diff engine — but with **none of `AGENT_DIFF_OPTIONS`' ergonomic options**. One rule: *the RPC copies these columns verbatim, so any option that makes two different stored values look equal is a lie here.* `excludePaths` is keyed by bare key name at every depth (it would drop a nested `id`/`version` inside `settings`); `skipUnderscorePrefix` would hide a `__kind` difference; and **`identityKeys` is the dangerous one** — matching array items by `name`/`key` instead of position reported a reordered `variable_definitions` as *identical*, silently dropped items whose key collided, and leaked to any nested array merely spelled `variableDefinitions`. Since "identical" **disables both buttons**, each of those made a real difference unsyncable. Positional comparison over **raw column values** (not converter output) is the only form faithful to `SET col = v_from.col`. `AGENT_IDENTITY_KEYS` still belongs to `AgentDiffViewer`, which answers a different question.
- **A failed request is not a verdict.** A 500/timeout/offline is rendered as "the comparison request failed" with a Try again button — never folded into `unknown`, which would blame the user's permissions for a network blip.
- **The full-diff link is offered as a WIDER view, not the same list.** `/agents/compare` diffs converted `AgentDefinition` values with `AGENT_DIFF_OPTIONS` over all fields; the panel's chips remain the authority on what sync would write, and the link says so.
- **The verdict is direction- and option-aware** (`agentSyncImpact`). A pair can be identical for everything a default Pull copies while still differing in name/description — the real 2026-08-09 state of the canonical pair. So Pull and Push each report their own impact, and a direction with nothing to write is **disabled and labelled "Nothing to pull/push"** instead of inviting a no-op overwrite.
- **Doors:** the pair card renders each side through `EntityRef` (open / new tab / peek), and a differing verdict links to the full field-by-field diff at `/agents/compare?left=<userId>&right=<systemId>` — the existing `AgentComparisonPage`, which now preselects both sides from those params. No second diff view was built.
- **Known gap:** `ui_gates` and `matrx_actions` are real config columns the RPC does **not** copy, so sync silently leaves them divergent. `AGENT_SYNC_FIELDS` omits them on purpose (it describes the sync as it is). Tracked in [FOUND_DEFECTS.md](../../FOUND_DEFECTS.md).

### Find Usages & Drift

Find every place an agent is used and detect when a usage no longer matches the agent. **"Pinned to active" = `agx_version.version_number == agx_agent.version`** — a promoted older version is fine; numeric-latest is irrelevant.

- **Forward-looking usages only** (a usage can re-invoke the agent later): shortcuts, apps, prompt apps, scheduled tasks, surface bindings, SMS lines, workflow nodes, derived agents, comparisons, and **code-registered usages** (backend pins in `agx_usage_registry`). Conversations/runs are history — counted as muted context, never drift-checked. **Workflow-node scanning is dormant:** current `wf_definition` nodes bind a _model_, not an `agent_id`, so the `workflow_node` branch (matches `nodes[].data.config.agent_id`) finds nothing today — it's the forward hook for when agent-pinning nodes ship; confirm the real JSON path then.
- **Severity (worst→least), one source `components/usages/severity.ts`:** `breaking` (a stored variable is gone / required var unmet on a non-interactive usage / agent disabled) · `silent_breaking` (a stored context slot was renamed — still injected, but as default context, so its rules are silently ignored) · `warning` (stale pin) · `info` (pinned older but the variable+slot contract is unchanged).
- **Drift is computed against the _effective_ definition:** pinned usages compare to the pinned snapshot (the stale pin is its own finding); follow-active usages compare to live `agx_agent`.
- **Entry points:** agent options menu → "Find Usages" / "Find Usages (Admin)" (super-admin, `selectIsSuperAdmin`) windows; "Drift Report" menu link + severity-tinted `AgentsListHeader` button on `/agents/all` → `/reports/agent-drift` (weekly-scan-fed alerts, show-once via `viewed_at`).
- **Privacy (user variant):** own + org-managed usages in full detail; everyone else's collapse to aggregate counts (not a security wall — a "don't decide for others" wall, enforced in the RPC). Org usages the caller can't manage carry `org_manager_user_ids` for "Notify managers".
- **Remediation is one-click, never automatic:** `repin_active` (default — move the pin to the active version) or `follow_active`, single + bulk, gated to owner / org owner-admin / super admin. Stored-config keys are never auto-deleted.
- **Redux:** `features/agents/redux/usages/` (slice `agentUsages`); engine `components/usages/AgentUsagesEngine.tsx` (one component, mode `user | admin`) powers both windows + the report detail pane. **RPCs:** `agx_usage_scan` / `agx_usage_scan_admin` / `agx_usage_report` / `agx_usage_report_admin` / `agx_usage_update_to_active` / `agx_usage_update_all_to_active` / `agx_usage_history_counts` (migrations `agx_usage_001`–`004`). Backend: code-usage registry sync + weekly drift scan + DM notifier in aidream `services/agent_usage/`. See the **Agents admin map** at `/agents/admin`.
- **Alert access follows the agent token, never a project FK.** `agent.drift_alert` is a component of `agent.definition`; its RLS resolves through `iam.has_access('agent', agent_id, ...)`. Agent/project context is an optional `platform.associations` edge, so `platform.entity_relationships` must not declare an `agent.definition.project_id` containment path.

---

## Entry points

**Routes**

- `app/(authenticated)/agents/[id]/build/page.tsx` — Builder
- `app/(core)/agents/[id]/run/page.tsx` — Runner
- `app/(admin)/administration/agents/system-agents/agents/[id]/run/page.tsx` — System-agent admin runner; shares the runner shell/sidebar with admin-aware navigation
- `app/(authenticated)/chat/...` — Chat (🚧 not yet built; legacy at `features/cx-conversation/` + `features/cx-chat/`, deprecated stub at `app/(authenticated)/deprecated/chat/`)
- `app/(authenticated)/ai/agents/[id]/connections` — tool/integration config
- `app/(authenticated)/ai/shortcuts/` — shortcut admin
- `app/(core)/agents/sets/page.tsx` — Agent Sets list · `sets/[orchestratorId]/page.tsx` — Set Builder (React Flow canvas, code-split). See [`docs/AGENT_SETS.md`](./docs/AGENT_SETS.md)

**API endpoints**

- `POST /ai/agents/{id}` — first turn of a new conversation (agent mode)
- `POST /ai/agent-assignments` — start/resume a durable coordinated variable batch
- `GET /ai/agent-assignments/sessions/{id}` — read durable batch progress/results
- `POST /ai/agent-assignments/sessions/{id}/cancel` — cancel unfinished batch items
- `POST /ai/conversations/{conversationId}` — subsequent turns
- `POST /ai/chat` — ephemeral turns (no DB persistence)
- `POST /ai/manual` — Builder-mode raw request; live definition + client-held history on every turn
- `POST /ai/conversations/{id}/tool_results` — durable + widget tool result submission

**Key thunks** (`features/agents/redux/execution-system/thunks/`)

- `launch-conversation.thunk.ts` — single entry point every surface hands a `ConversationInvocation` to
- `launch-agent-execution.thunk.ts` — low-level launch delegate
- `execute-instance.thunk.ts` — body assembly, fetch, stream parsing (agent mode)
- `execute-chat-instance.thunk.ts` — same but for ephemeral/chat mode
- `resume-conversation.ts` — rehydrate after refresh
- `submit-tool-results.ts` — durable tool call result submission

**Services**

- `features/agents/services/mcp.service.ts` — MCP catalog + metadata-only connection upsert (`upsert_mcp_connection` carries NO tokens)
- `features/agents/services/mcp-connections.service.ts` — vault-backed MCP operations via aidream `/api/mcp-connections/*` (discover/invoke/refresh/credentials/disconnect); the browser never holds an MCP token (vault Phase 4 — see `features/secrets/FEATURE.md` § MCP connections)
- `features/agents/services/mcp-client/` — tool schema types only (the browser JSON-RPC client + token refresh were deleted in the vault cutover)
- `features/agents/services/mcp-oauth/` — MCP OAuth discovery/PKCE helpers for the Next.js start/callback boundary

---

## Data model (Redux — four layers)

All state lives under `features/agents/redux/`. The four layers:

### Layer 1 — Agent Source (static definitions)

| Redux key         | Slice               | Role                                                                                                  |
| ----------------- | ------------------- | ----------------------------------------------------------------------------------------------------- |
| `agentDefinition` | `agent-definition/` | Master registry — live + version snapshots, per-record fetch status, dirty tracking, field-level undo |
| `agentShortcut`   | `agent-shortcuts/`  | Stored launch buttons — agentId + scope mappings + display config                                     |
| `agentConsumers`  | `agent-consumers/`  | Per-UI filter/sort/search state for list views                                                        |

**Config storage (`agx_agent` / `agx_version`) — `settings` holds ONLY model params the server consumes.** Every other concern is a **dedicated column**, never stuffed in `settings`: `model_id` · **`tools` uuid[] + `custom_tools` jsonb** (tool *assignment* — authoritative; executor reads these) · **`tool_config`** (flags only: `auto_tools_disabled` / `excluded_tools` — **never** assigns tools) · `output_schema` · `context_slots` · `skill_config` · and:

- **`matrx_actions`** — Matrx Actions apply config (`{apply_policy|auto_apply|allow}`), read by aidream's output-directive dispatcher. Full rebrand of the retired `settings["output_apply"]` — that key exists **nowhere** (DB, FE, or backend). Edited via the **Matrx Actions** tab (`components/settings-management/matrx-actions/`): the tab picks the agent's action from the live registry (built-ins + action catalog), which wires the agent's `output_schema` to emit that `output_directive` envelope (`actionSchema.ts`), then sets the apply policy → `setAgentMatrxActions`. **`matrx_actions` is policy ONLY — the action the agent performs lives in `output_schema`** (the legacy `directive` field unified both; read-only here, being retired).
- **`ui_gates`** — FE-only model-gated UI flags (`file_urls`/`image_urls`/`youtube_videos`/`tools`-bool). **Never sent to the server**; gates what the chat/builder UI offers. The one source of keys + helpers is `lib/redux/slices/agent-settings/ui-gates.ts` — **no per-file `UI_CAPABILITY_KEYS` copies**. Edited via the **Input capabilities** editor (`settings-management/ui-gates/`) → `setAgentUiGates`.

A write-time sanitizer in `agent-definition/converters.ts` **strips** those keys from `settings` on every save (loud `console.warn` if it fires — a writer is mis-routing). `voice_id` / `realtime_tools` / `tts_voice` / `multi_speaker` / `audio_format` / `output_format` are real params and **stay** in `settings`. **Both tables move together** — `agx_version` runs most executions and loads through the same backend loader; every column add + backfill + the snapshot **triggers** + the **RPCs** (`agx_get_version_snapshot` / `agx_promote_version` / `agx_duplicate_agent` / `agx_duplicate_version` / `agx_get_execution_full`) hardcode the column list, so update all of them or a column silently won't version.

Model-control types from `ai.resolve_model_config` are authoritative. The
frontend may infer `number` versus `integer` only for legacy controls that omit
`type`; it must never overwrite an explicit `type: "integer"` because the
control lacks a default. Integer controls use the shared `NumberInput`, whose
slider step and emitted values are whole numbers.

Tool assignment is fail-safe at both write boundaries. The shared frontend
`sanitizeAgentToolIds` guard removes malformed UUIDs before PostgREST can cast
the `uuid[]`. The database `_sanitize_definition_tool_references` trigger then
removes well-formed IDs whose `tool.definition` row is missing, inactive, or
deleted, emitting a warning whenever it repairs a write. It runs before the
version snapshot trigger. Invalid tool input therefore creates/saves the agent
without that tool; it must never fail the surrounding route or preserve the bad
assignment in a new version.

### Layer 2 — App Context (external)

| Redux key    | Location                              | Role                                                                              |
| ------------ | ------------------------------------- | --------------------------------------------------------------------------------- |
| `appContext` | `lib/redux/slices/appContextSlice.ts` | Global scope: org, workspace, project, task IDs. Injected by `assembleRequest()`. |

### Layer 3 — Execution Instances (11 slices, `byInstanceId`)

Ephemeral runtime state. Core invariant: **`agentId` is read exactly ONCE — at instance creation.** After that the instance owns its data; if the definition changes mid-run the instance doesn't notice.

| Redux key                     |          In API body?          | Owns                                                                                                                                                                                                                                                                                 |
| ----------------------------- | :----------------------------: | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `executionInstances`          |               No               | Shell: agentId, origin, status lifecycle                                                                                                                                                                                                                                             |
| `instanceModelOverrides`      |    Yes (`config_overrides`)    | Model settings snapshot + user deltas                                                                                                                                                                                                                                                |
| `instanceVariableValues`      |       Yes (`variables`)        | Variable defs + resolved values (defaults → scope → user)                                                                                                                                                                                                                            |
| `instanceResources`           | Yes (merged into `user_input`) | Attached files/content with status                                                                                                                                                                                                                                                   |
| `instanceContext`             |        Yes (`context`)         | Context slot matches + ad-hoc entries                                                                                                                                                                                                                                                |
| `instanceUserInput`           |       Yes (`user_input`)       | Text + multimodal content blocks                                                                                                                                                                                                                                                     |
| `instanceClientTools`         |      Yes (`client_tools`)      | Client-side tool IDs registered for this instance                                                                                                                                                                                                                                    |
| `instanceWorkingDocument`     |  No (feeds `instanceContext`)  | Documents keyed by `workingDocKey(scope, kind)` — `working` (per-conversation, collaborative) + `scratch` (USER-GLOBAL pool at `sp:<docId>` scopes; agent read-only): enabled/content/title/binding/saving + `attachedScratchByConversation` (see **Working document & scratchpad**) |
| `instanceUIState`             |             **No**             | Display mode, panels, modals, visual state                                                                                                                                                                                                                                           |
| `instanceConversationHistory` |      No (server owns it)       | Turn history, conversation mode, server conversation ID                                                                                                                                                                                                                              |
| `activeRequests`              |               No               | Per-request stream state (chunks, status, content blocks, tool lifecycle)                                                                                                                                                                                                            |

### Layer 4 — Thunks + aggregate selectors

See `features/agents/redux/execution-system/` and `selectors/aggregate.selectors.ts`.
Multi-run surfaces that need one canonical composer use
`thunks/copy-instance-request-draft.thunk.ts` to copy the complete request draft
between initialized instances. It copies input, variables, resources, context,
run settings, and client tools while intentionally preserving the target's
model overrides.

---

## Sub-feature docs (read these for detail)

### New in this doc pass

- [`docs/AGENT_BUILDER.md`](./docs/AGENT_BUILDER.md) — authoring surface; ships full definition; cache-independent
- [`docs/AGENT_RUNNER.md`](./docs/AGENT_RUNNER.md) — observability-on test track; ID-only invocation
- [`docs/AGENT_VERSIONING.md`](./docs/AGENT_VERSIONING.md) — version semantics + pin-by-version for Shortcuts/Apps
- [`docs/AGENT_INVOCATION_LIFECYCLE.md`](./docs/AGENT_INVOCATION_LIFECYCLE.md) — endpoint routing, Builder vs Runner payloads, ephemeral branch
- [`docs/AGENT_ORCHESTRATION.md`](./docs/AGENT_ORCHESTRATION.md) — maxIterations, retries, self-correction, state persistence
- [`docs/AGENT_ASSIGNMENTS.md`](./docs/AGENT_ASSIGNMENTS.md) — secure random variables, coordinated batch plans, durable API sessions, and demo UI
- [`docs/AGENT_SETS.md`](./docs/AGENT_SETS.md) — Agent Sets (Orchestrators): agent→agent M2M on `platform.associations` (no `agent_set` table), drag-drop React Flow set builder — + [`docs/AGENT_SETS_ROADMAP.md`](./docs/AGENT_SETS_ROADMAP.md) (path to full runtime delegation)
- [`docs/STREAMING_SYSTEM.md`](./docs/STREAMING_SYSTEM.md) — canonical NDJSON streaming contract (anchor for the whole app)
- **Find Usages & Drift** — section above + the Agents admin map at `/agents/admin`; reports module at [`features/reports/FEATURE.md`](../reports/FEATURE.md)

### Existing (refresh as needed when editing)

- [`docs/AGENTS_OVERVIEW.MD`](./docs/AGENTS_OVERVIEW.MD) — four-layer architecture deep dive
- [`agent-system-mental-model.md`](./agent-system-mental-model.md) — long-form mental model
- [`docs/agent-rpcs-reference.md`](./docs/agent-rpcs-reference.md) — RPC surface (with `new-rpcs.md`)
- [`docs/STREAM_STATUS_LIFECYCLE.md`](./docs/STREAM_STATUS_LIFECYCLE.md) — stream event types + phases
- [`docs/DURABLE_TOOL_CALLS_CLIENT_INTEGRATION.md`](./docs/DURABLE_TOOL_CALLS_CLIENT_INTEGRATION.md) — durable tools contract
- [`docs/WIDGET_HANDLE_SYSTEM.md`](./docs/WIDGET_HANDLE_SYSTEM.md) — widget handle + client tools
- [`docs/WIDGET_HANDLE_AND_CLIENT_TOOLS-STATE.md`](./docs/WIDGET_HANDLE_AND_CLIENT_TOOLS-STATE.md) — state integration
- [`docs/agent-undo-redo.md`](./docs/agent-undo-redo.md) — field-level undo
- [`docs/agents-migration-status.md`](./docs/agents-migration-status.md) — live migration status
- [`conversation-invocation-reference.md`](./conversation-invocation-reference.md) — `ConversationInvocation` shape
- [`ROADMAP-agent-ecosystem-rebuild.md`](./ROADMAP-agent-ecosystem-rebuild.md) — roadmap

---

## Invariants & gotchas

- **Every conversation START sends three required fields: `conversation_id` (we mint it, always), `is_new`, `store`.** `store:false` is the ONLY thing that makes a run ephemeral — omitting the id and sending `is_new:false` is a 422 from the server and a throw from `buildAgentStartLifecycleFields`. The id is our correlation handle, so concurrent sends can be told apart. Continue calls (`/ai/conversations/{id}`) are exempt: the id is in the path. Server contract: `/Users/armanisadeghi/code/aidream/aidream/services/conversation_context/FEATURE.md` § "Starting a conversation".
- **Wire / stream / DB types are never hand-mirrored.** Python → `types/python-generated/api-types.ts` + `stream-events.ts`; DB → `types/database.types.ts`. Alias with `components["schemas"]["…"]`. Hitlist: `pnpm generate:type-drift-hitlists` → `docs/type-drift/generated/`. Invoke the **`type-safety`** skill before adding or "fixing" a type at a data boundary.
- **`BackendChannel` lives in `resolve-base-url.ts` only.** `RequestRouting.channel` must use that type — never re-declare the union (a stale copy already dropped `local-runtime`).
- **`agentId` is read once at instance creation.** Do not re-read during execution. If the agent definition changes mid-run, the instance must not notice.
- **Builder and Runner are not the same payload shape.** Builder ships the full definition; Runner ships only the ID. If you add a field to the agent definition, both paths must be updated — Runner needs server-side handling, Builder needs client-side bundling.
- **Shortcuts and Apps pin to version.** `useLatest: false` + `agentVersionId` is the frozen case. Breaking the version contract breaks every embedded invocation silently.
- **The stream is never paused for widget tools.** Fast + fire-and-forget. Non-widget delegated tools do pause.
- **Ephemeral conversations cannot call `/ai/conversations/{id}` on turn 2** — they must call `/ai/chat` with full accumulated history (there is no DB row to target).
- **Client never sees the system prompt or instructions.** Those are server-owned engineer secrets. The client only gets variable + context slot definitions on agent load.
- **Drift between a usage and the live agent is surfaced (Find Usages window + weekly scan + severity-tinted AgentsListHeader link), never auto-resolved.** Remediation is one-click, opt-in, permission-gated. See **Find Usages & Drift** above.
- **Agent/project linkage is association-only.** `agent.definition` has no `project_id`; creation/duplication RPCs must not write one, and list/search/access RPCs must not read or expose one. Resolve project context through `platform.associations`.
- **Never send while the mic is recording or finishing transcription.** Mid-voice submit drops the trailing audio and leaves the recorder running. Gate send (button + Enter) on `isRecording || isTranscribing` via `AgentMicrophoneButton.onRecordingStateChange` — wired in `SmartAgentInput*` and sibling composers.

---

## Related features

- **Depends on:** `features/agent-context/` (variable/context resolution; `features/brokers/` was deleted 2026-08-11 — it was never in the invocation path), `features/api-integrations/` (MCP + external tools), `features/artifacts/` (rendering output), `features/tool-call-visualization/` (tool UI)
- **Depended on by:** `features/agent-shortcuts/`, `features/agent-apps/`, `features/conversation/`, almost every user-facing surface
- **Cross-links:** `features/agents/migration/MASTER-PLAN.md`, [`features/scopes/FEATURE.md`](../scopes/FEATURE.md)

## Change Log

- `2026-08-12` — Surface-launched conversations retain the DB-registered launch surface. `launchAgentExecution` records it on the conversation, and tool injection prefers it over route detection so overlay agents resolve tools for the open overlay rather than the page beneath it. Plain chat conversations still fall back to their route.
- `2026-08-12` — claude: **the Agent Settings window is agent-writable, and the catalog-profile contract now has ONE definition instead of two.** `AgentSettingsWindow` mounts the first `SurfaceRuntimeProvider` for `matrx-user/agent-settings` (`useAgentSettingsSurface.ts`; the manifest was `readiness: "stub"` with no emitter anywhere, so this was `surface-authoring` before it was `surface-write-targets`), and `AgentSettingsForm` registers ONE ask-policy draft target, `settings_catalog_profile` — `{description?, category?, tags?}` staged into the same `draft` / `tagsInput` state the user's typing writes, so the Save button arms itself normally and the USER still presses it. **New shared module `features/agents/surface-catalog-profile.ts`**: `matrx-user/agent-advanced-editor` already shipped `editor_catalog_profile` over the same three `agent.definition` columns, so the contract (accepted keys, bounds, replace-vs-patch semantics, model-facing prose) is defined once and both manifests import it — the `lists` / `list-manager` precedent. The two keep separate target NAMES on purpose: they are floating windows that can be open at once on different agents, and `applySurfaceWrite` resolves deepest-first, so a shared name would stage into the wrong window. **This closed a real gap:** `editor_catalog_profile` validated with an ad-hoc `requireProseText` and enforced no length, tag-count, duplicate or comma rules; both surfaces now route through `agent-identity-metadata.ts`'s canonical validators and interpolate the real bounds into their descriptions. `useAgentBuilderWriteHandlers`'s four single-field targets are the third implementation of the same columns and are still unbounded — flagged, not touched, because they are a different target shape on a separately verified surface. `AgentSettingsForm` gained a `writeSurfaceName` opt-in prop because `AgentContentWindow` renders the SAME form on the advanced editor's Overview tab; ungated, both instances would register for this surface and the last mounted would win. Read twins come from the form's live draft via `agentSettingsDraftRegistry.ts` (the window owns the provider, the fields live two levels down in component state). Name, the Active/Public/Favorite/Archived switches, hierarchy scopes, RAG boost and the Surface pane's bindings all stay human-only. Live-verified end to end with real Badass Agent runs; see `features/surfaces/FEATURE.md` for the full evidence.
- `2026-08-12` — claude: **the Agents Hub gallery is agent-writable (1 composite target), and its emitter had drifted off its own route.** `matrx-user/agents` declares `catalog_filters` (`mode:"ui"` / `applyPolicy:"ask"`) — one partial object carrying all nine filter/search/sort fields, handled on `AgentsGrid`'s provider and dispatching the SAME `setAgentConsumerFilter` the search box, tabs and filter chips already fire. One composite rather than nine targets because they are one user thought, which also makes validation atomic (one bad value rejects the whole call, so a filter never half-applies) and removes write ordering as a hazard. Enums validate against what the UI renders — `SORT_OPTIONS` for the sort, the LIVE `selectAllAgentCategories`/`selectAllAgentTags` (read at call time, since the vocabulary grows as the list loads) plus `AGENT_NONE_SENTINEL` for the real Uncategorized chip; `ownership_tab` refuses `"system"` (no System tab here) and refuses `"shared"` when nothing is shared, since both would strand the user in a view with no control to leave it. Everything that MUTATES an agent stays human-only — `archived_filter` shows archived agents, it never archives one — and `peeked_agent_id` is unwritable by construction (`agent-peek-tracker` is a reporting register with no canonical opener). **Route-vs-mount drift found and documented:** `AgentsGrid` is the only mount for this surface and renders on `/agents/classic` ALONE, while `/agents/all` (where `/agents` redirects) is now the `lib/entity-list` `AgentBrowsePage` and mounts no runtime — so that route names the surface from the route map but offers no live values and no write tool. The manifest docblock claimed otherwise and now says this outright; adopting the emitter on `AgentBrowsePage` is left as a `surface-authoring` job. Live-verified with a real Badass Agent run on `/agents/classic` (four fields in one dialog and one call, decline clean, archive/favorite refused with no tool call, invalid tag returned the handler's throw verbatim with the live vocabulary listed back). Full evidence in the `features/surfaces/FEATURE.md` Change Log.
- `2026-08-12` — **`toggleCategory`/`toggleTag` drop all but the last change when looped (fixed at the hub, still live in `agent-sets`).** Both compute their next array from the `includedCats`/`includedTags` captured in the CURRENT render, so N calls in one tick all start from the same snapshot and the last patch wins — `MultiSelectChips`' "Clear filter" with three categories active removed exactly one. `useAgentConsumer` now exports canonical `setIncludedCats`/`setIncludedTags` (one dispatch of the final set) and `AgentsGrid` consumes them; `features/agents/agent-sets/components/AgentLibraryRail.tsx` and `CreateSetDialog.tsx` still route their whole-set writes through duplicate `applyArrayViaToggle` helpers and retain the bug. Left off-mission rather than swept.
- `2026-08-11` — claude: **Agent-set "Sync agent listings" streams instead of spinning — and the sweep's read of this flow was wrong.** `docs/handoffs/live-run-streaming-sweep.md` recorded `agent-sets/orchestrator/thunks.ts:153` as "launches EVERY set member in the background". It does not: that line is ONE Role Describer pass over the whole set, and the actual multi-member set RUN already streams (`SetRunPanel` embeds the canonical `AgentRunnerPage`; `useSetMemberRunStatus` lights each member on the canvas as it executes). The real offender was the describer — a multi-minute pass with up to three SILENT retries behind the button's spinner. `syncOrchestratorPrompt` now launches `displayMode: "direct"` with `hideReasoning: true` and floats the run in the canonical `LiveRunWindow`, one window per set (`agent-set-sync:${orchestratorId}`) so a re-sync and each retry re-bind that same window (retry attempts relabel it "retry N of 3"). `onConversationCreated` fires BEFORE the stream, which is what turns the window from "pending" into live output within a second of the click. The describer's instance is deliberately **kept** after the run so the finished description survives completion — destroying it there would blank the window exactly when the content lands — and the next sync releases it, so at most one is ever held. Live-verified on a real set: window opened, streamed, completed, both members got role titles written to their edges.
- `2026-08-11` — **The Agent Advanced Editor WINDOW is agent-writable — a second, deliberately different policy over the same `agent-definition` record.** `matrx-user/agent-advanced-editor` (mounted by `features/window-panels/windows/agents/AgentContentWindow.tsx`, which now carries the surface's FIRST provider) declares 4 `mode:"draft"` / `ask` targets: `editor_system_instruction`, `editor_append_system_instruction`, `editor_output_schema`, and the composite `editor_catalog_profile` (`{description?, category?, tags?}`). Handlers dispatch the SAME `withAgentSystemInstruction` + `setAgentMessages`, `setAgentOutputSchema` and `setAgentField` the window's own editors dispatch; the footer Save stays the user's press and is not a target. **Two things an agents-feature reader must know.** First, the target names carry an `editor_` prefix ON PURPOSE: `matrx-user/agent-builder` already owns `system_instruction` / `append_system_instruction`, this window floats over any route including `/agents/[id]/build`, and `applySurfaceWrite` resolves a bare name against the deepest surface that DECLARES it — so unprefixed names would silently hand the write to the builder behind the window, which frequently has a DIFFERENT agent open than the window's own picker does. Second, the two surfaces deliberately DISAGREE on two fields, documented rather than accidental: `agent_name` is writable in the builder but NOT here (in this window the name is navigation chrome — footer EntityRef, tab strip, sidebar row), and `outputSchema` is excluded by the builder but IS writable here, because an emitted shape is authored content rather than a reachable capability; it lands through the same `setAgentOutputSchema` the Output Schema tab's own Apply dispatches, is gated by `validateOutputSchema`, and cannot be cleared. Worth reconciling the two manifests in one direction; flagged there rather than silently diverged. Model, tools, custom tools, MCP servers, skills, variables, context slots, message templates, sharing and all governance stay human-only on both. Live-verified with real Badass Agent runs; full evidence in the `features/surfaces/FEATURE.md` Change Log.
- `2026-08-11` — claude: **the Agent Run page is agent-writable — and the targets it already had were dead code until now.** `matrx-user/agent-run` offers 3 `applyPolicy: "ask"` targets: `user_input_draft` (`mode:"draft"`, `{text, mode?: "replace" | "append"}`) dispatching the SAME `setUserInputText` `AgentTextarea` fires, so a staged draft joins the undo stack, obeys `input-draft-protection.ts`, and sends through the normal queue/steer flow; `variable_values` (`mode:"draft"`, a PARTIAL per-key patch) dispatching the same `setUserVariableValues` the variable inputs fire; and `conversation_title` (`mode:"entity"`) through the canonical `renameConversation` thunk. **The bug this change actually fixed:** `agent-run.manifest.ts` already declared all three targets and `AgentRunnerPage` already registered all three handlers via `getWriteHandlers`, but the exported `agentRunManifest` object never listed `writeTargets` — so `listAgentWritableTargets()` found nothing, no `apply_surface_write` tool was ever offered on a run page, and the feature was complete-looking dead code that passed both `type-check` and `check:surface-drift`. One line published it; the landed design was kept verbatim. Handlers live in `AgentRunnerPage.tsx` past the `isAgentRunSurface` gate, so the `/code` workspace built on the same component registers no provider and is offered nothing. `variable_values` re-reads `selectInstanceVariableDefinitions` at APPLY time (the run's instance can change between offer and write) and refuses an undeclared name with the real declared list, applying nothing — never a partial write. `conversation_title` gates on `waitForConversationPersisted(id, {timeoutMs: 0})` like chat's, because a fresh run holds a client-minted id and the rename would update zero rows while the sidebar showed the new name. **Nothing the run PRODUCED is writable** — `agent_response`, `agent_reasoning`, `all_messages`, `tool_calls`, `completion_stats`, `is_streaming`, `run_status` stay read-only, because this page is the instrument that MEASURES an agent and writing its output would forge the result the user came to read; the `agent_*` contract (instruction, model, tools, variable definitions) stays with `matrx-user/agent-builder`. Executing is not a target — it spends real tokens, so the agent stages and the human presses send. **No mid-stream guard, deliberately:** per Flow 6 a send during a run queues or steers and the composer stays live, so refusing while streaming would re-impose the hard lock removed on `2026-07-29`. Vocabulary (`CHAT_DRAFT_WRITE_MODES` / `CHAT_INPUT_DRAFT_MAX` / `CHAT_CONVERSATION_TITLE_MAX`) is imported from `chat.manifest.ts`, not re-typed — this composer IS the same `instanceUserInput` entry and the same `chat.conversation` row. Live-verified with real Badass Agent runs (first verification this surface has ever had); full evidence in the `features/surfaces/FEATURE.md` Change Log.
- `2026-08-10` — **The System Agents ADMIN console is agent-writable (4 entity targets).** `matrx-admin/system-agents` now declares `agent_description`, `agent_name`, `agent_category` and `agent_tags` as `mode:"entity"` / `applyPolicy:"ask"` write targets on the ONE open builtin agent, handled by the new `components/admin/SystemAgentWriteTargets.tsx`. Every handler goes through `saveAgentField` — the same thunk `AgentSettingsForm`'s Save dispatches per changed field — and refuses loudly first when no agent is open, when the record has not loaded, when it is a read-only version snapshot, or when the viewer is not a Matrx admin (`selectIsAdmin`, deliberately the same bar the `(admin)` route layout gates on, not the stricter `selectIsSuperAdmin`: refusing an agent a change the very same person can make by hand two clicks away is theatre, not safety). Handlers are mounted on the DETAIL page rather than the `[id]` layout because `/build` nests `matrx-user/agent-builder`, which owns these same four target names as reviewable draft targets and wins deepest-first — so the builder keeps the better draft path and the read-only routes get the entity one. Everything that decides what a platform-wide agent DOES or may REACH stays human-only (messages, variables, context slots, tools, custom tools, MCP servers, output schema, skills, model, model tiers, settings, UI gates), as do `is_active`/`is_public`/`is_archived` and all version provenance. Live-verified end to end on a throwaway builtin agent; full reasoning and evidence in the `features/surfaces/FEATURE.md` Change Log.
- `2026-08-10` — **`saveAgentField` failures are invisible in `AgentSettingsForm` (found, NOT fixed).** The thunk correctly rolls back and throws `pgErrorToError` on a failed update, but `handleSave` dispatches it bare — no `await`, no `.unwrap()`, no `catch`, no toast — and `createAsyncThunk` converts the throw into a `rejected` action nothing observes (the slice has no `extraReducers`; `AgentSaveStatus` reads no error selector; `setAgentError` writes `_error` and nothing surfaces it). A rejected save therefore snaps the field back with no explanation. Noted here because the new admin write handlers had to work around it — they `.unwrap()` and then re-read the record — and because the contrast is instructive: the header's `useAgentSaveAction` DOES `.unwrap()` `saveAgent` and toasts on failure. Fixing the form is out of scope for this surface-write change.
- `2026-08-10` — **The Scratchpad and the Working Document are agent-writable (surface write targets).** Both surfaces declare the SAME 4 `ask`/`entity` targets — `document_content`, `append_document_content`, `replace_selection`, `insert_at_cursor` — shared as `CONVERSATION_DOCUMENT_WRITE_TARGETS` from the manifest file that already shares their value set, because `WorkingDocumentEditor` renders both and one handler block services both. That editor previously mounted NO `SurfaceRuntimeProvider` (it only emitted scope through `UnifiedAgentContextMenu` at trigger time), so it now mounts the surfaces' first one, reusing the existing `getApplicationScope` as `getScope`. Every handler lands through the SAME `onChange` prop a keystroke goes through, so an agent edit joins the identical dirty flag, canonical-slice publish, 700ms autosave and version history as the user's own typing — no parallel write path. The scratchpad being writable here is not a contradiction of "the agent only reads it": that rule governs the CLOUD agent in the chat and is enforced by publishing the scratchpad as a read-only `user_scratchpad` context entry, whereas these targets are only ever offered to an agent the user runs from inside the document, behind a per-target confirm. Targets are `entity` rather than `draft` because there is no Save button — `useWorkingDocument.onChange` autosaves at 700ms and the panel says "Auto-saved" — so the dialog tells the truth ("This is saved immediately") and the descriptions name version history, not ⌘Z, as the recovery path (this editor mounts no undo stack). All handlers refuse loudly while `has_conflict` is set, because `useWorkingDocument`'s commit deliberately bails there and a staged write would sit unsaved while enlarging the merge the user is about to reconcile; they also refuse a view-only sharee. `document_version` stays unwritable — it is the optimistic-concurrency token. Live-verified with a real Badass Agent run on the scratchpad panel (restructure applied, selection-scoped rewrite applied, decline clean, undeclared target refused, invalid value returned the handler's own throw).
- `2026-08-10` — **Restored the Agent Builder write-target entry a merge dropped** (below). The code shipped in `68b1c007`; a later merge resolution reverted the doc lines only, so this file claimed the builder was not agent-writable while it was. No behavior change.
- `2026-08-09` — **Agent Builder is agent-writable (surface write targets).** The `matrx-user/agent-builder` surface declares 6 draft/`ask` write targets and `AgentBuilderClient` registers their handlers (`hooks/useAgentBuilderWriteHandlers.ts`) on both its providers — desktop and mobile — so a target is never live on one viewport and silently dead on the other. The system prompt is not a plain field (it lives in `messages`), so the rebuild rule moved into `utils/agent-system-instruction.ts` as `withAgentSystemInstruction`, and `SystemMessage.handleTextChange` now calls it too: one rule, one `setAgentMessages` path, whether the user types or an agent rewrites — non-text blocks always round-trip. `system_instruction` replaces, `append_system_instruction` adds without re-sending the prompt; `agent_description` / `agent_name` / `agent_category` / `agent_tags` go through `setAgentField`. Everything stages as unsaved dirty state — the user still presses Save (`useAgentAutoSave` only mirrors to localStorage) — and read-only/version records refuse outright. Model, tools, MCP servers, skills, variables, output schema and all governance remain human-only. Live-verified end-to-end with a real agent run on the builder.
- `2026-08-09` — **Linked Agent Sync now actually compares the two agents.** The panel showed a "last synced" date and offered Pull/Push without ever checking whether the pair differed — Arman's complaint: *"it's not actually doing a comparison, which is stupid… If they're identical, tell me so I don't waste my damn time."* Added the platform primitive `features/agents/sync/`: `sync-fields.ts` declares ONE field set mirroring exactly the columns `agx_sync_linked_agents` copies (identity vs behavior), and `compare.ts` is a pure, unit-tested core returning `identical | differs | unknown` plus per-field changes with counts and order-only detection. New `fetchAgentSyncComparison` thunk reads exactly those columns raw for both sides; an RLS-hidden side yields `unknown`, never `identical`. The panel now opens with the verdict, lists the differing fields by name, and links to the full diff at `/agents/compare?left=&right=` (`AgentComparisonPage` gained URL preselection — no second diff view). `agentSyncImpact` makes the actions honest: Pull and Push each report and disable on their own no-op, so the canonical pair (behavior identical, name/description divergent) correctly says "Nothing to pull" while Push still has work. Pair card moved onto `EntityRef` for open/new-tab/peek. Drift guard `pnpm check:sync-fields`. 30 unit tests on the real production pair shape.
- `2026-08-09` — **Sync panel: a deleted agent says so, and relatives stay reachable beside a live pair.** Two review findings. (1) `fetchLinkedCounterpart` filtered `deleted_at` on `source`/`derived` but not on `self`, so opening the panel on a soft-deleted agent showed a live-looking twin while the comparison — which does exclude deleted rows — reported `unknown` and blamed account access. `self` is now read unfiltered *and carries its `deletedAt`* (filtering it would collapse "this agent is deleted" into the false "isn't linked to a system agent"); the panel renders a dedicated deleted state that offers no sync and still links every linked relative. (2) `resolvePair` always computed `otherLinked`, but `LinkedRelatives` rendered only in the no-twin branches — so a third personal copy vanished exactly when a syncable pair existed. THE DOOR LAW does not switch off once a twin is found; the paired branch renders them too.
- `2026-08-09` — **A failed linkage read no longer outlives the read that succeeded.** `errorState` was keyed by `agentId` while the re-read was driven by `syncNonce`, so the tab-focus recheck (which bumps the nonce directly) could load linkage successfully and still leave the destructive error panel on screen, hiding the result. `errorState` is now keyed by ATTEMPT (`agentId|syncNonce`); `counterpartState` stays keyed by agent alone, deliberately, so a recheck never blanks good data back to a skeleton. `refreshAfterSync`'s explicit `setErrorState(null)` was **deleted** rather than duplicated — one key-driven mechanism, because the second clearing path is precisely the one the focus recheck forgot to call.
- `2026-07-28` — **Removed the final seven live RPC dependencies on `agent.definition.project_id`.** Agent creation from templates and both duplication paths no longer insert the retired column; access, compatibility list, full-list, and search routines no longer select or expose it. The manual `AgentListRow` mirror and regenerated Supabase contracts now match the association-only project model.
- `2026-07-28` — **Agent browsing and drift-alert access no longer depend on the retired `project_id` column.** The live `agx_list_scoped` return contract/read and stale `platform.entity_relationships` agent→project FK edge were removed together; scope counts/facets and direct drift-alert RLS reads now resolve against the canonical agent row while agent/project linkage remains association-backed.
- `2026-07-27` — **Conversation project context is association-backed.** Load and fork hydration no longer expect the forbidden physical `chat.conversation.project_id` FK; the backend restores optional project context from canonical `conversation → project` edges and the fork RPC copies those edges through `assoc_link`.
- `2026-07-27` — **Integer model controls remain integers end to end.** The shared model-control parser and the legacy hook now preserve explicit catalog types when a control has no default, fixing `max_output_tokens` being misclassified as a float and rendered with a `0.01` slider step. The shared `NumberInput` additionally forces integer sliders to whole-number steps/values, and `LLMParamsGrid` now reuses that primitive instead of maintaining a duplicate renderer.
- `2026-07-24` — **Agent set "Sync agent listings" — one AI pass describes every member's role + a clean, code-built listing.** Three fixes on `/agents/sets/[id]`. (1) **Reasoning leak → orchestrator prompt.** Headless `pollForCompletion` (`launch-agent-execution.thunk.ts`) joined ALL render blocks, so a describer's `thinking`/`reasoning` was written verbatim into `<available_agents>`; now derives via canonical `deriveAnswerText` (reasoning-excluded) — fixes every headless `launchAgentExecution` consumer. (2) **Sync redesigned to what it should do:** one click runs the new builtin **Agent Set Role Describer** (`a3e9d1c4…`, replaces the old "Agent Description Generator" listing) over the WHOLE set — each member fed its config + its CURRENT set role, so the AI **fills** blank Role title/gap, **fixes** wrong ones, and **confirms** valid ones — writing `{roleTitle,gap}` to the member EDGES via `saveMemberMeta` (only when changed). The `<available_agents>` block is then built **in code** (`buildAvailableAgentsBlock`) from the corrected role/gap + declared inputs/outputs: one clean `<agent id>` per member (Role / Fills / Inputs / Output), no duplicated id, no `<agents>` wrapper, no LLM prose. (3) **Header "Orchestrator" action opens a new tab** (its up-right-arrow icon now matches behavior) — `EntityModeHeader`/`EntityHeaderAction` gained a `newTab` flag honored on desktop (`target=_blank`) and mobile (`window.open`). Migration `agent_set_role_describer_builtin.sql`. Verified in-browser: blank member → "SEO Content Optimizer"; a deliberately-wrong "Cookie Baker" role → corrected to "Fact Checker"; edges + block confirmed in DB; new-tab confirmed. Type-check + `check:migrations` clean.
- `2026-07-24` — **Committed-row attachment gate + offline local-PC de-duplication.** A fresh chat's `record_reserved` event cleared `cacheOnly` before the backend's atomic turn commit, so `AttachedDocumentChips` called `conversation_files` against a UUID whose `chat.conversation` row did not exist yet (`42501 viewer access required`). `useConversationMaterialized` now treats the readable row as the only persistence proof and polls through the existing `waitForConversationPersisted` primitive; stream statuses are no longer mistaken for commits. Local-PC `410 device_offline` resolution now enters the existing bounded dead-target suppression window, preventing the gate and capability assembler from resolving/logging the same offline device twice in one send while preserving automatic retry after the cooldown.
- `2026-07-24` — **Set Builder header conformance (`/agents/sets/[orchestratorId]`).** The builder rendered a faux in-body header (`border-b` bar with title + `pr-14` action row) inside a `h-[calc(100vh-2.5rem)]` shell — the shell glass header overlapped the page and the double header subtraction cut the bottom off. Replaced with the shared `EntityModeHeader` template (back → `/agents/sets`, set-name dropdown over the user's other sets via `useAgentSetsList`, `Canvas | Grid` `RouteModeNav`, actions library-toggle / Run / Sync agent listings / Orchestrator / Set settings); body is `h-full` + `pt-[var(--shell-header-h)]` so the rail search and the canvas Arrange panel clear the glass. Canvas/Grid moved into the URL (`?view=grid`) so the href-driven mode nav owns it — `EntityModeHeader` gained an **`activeModeHref`** prop for modes that differ only by query string (pathname matching can't tell them apart), used by both the desktop nav and the mobile sheet. Mobile: everything collapses into the one `…` bottom sheet, and the 16rem library rail (which left no canvas on a phone) is now a slide-over below `md`, toggled from the header on every breakpoint. Verified in-browser at 1280 / 820 / 375 in light and dark.
- `2026-07-24` — **One-shot agent runs preserve request identity.**
  `useRunAgent` now delegates to canonical `callAgentStart`/`callApi` instead
  of assembling a scope-free `useBackendApi` request. All consumers inherit
  global org/project/task fallback, and entity-local callers can explicitly
  override it; source app/feature attribution travels on the same contract.
  Existing-entity callers can additionally send a stable `contextAnchor`;
  aidream RLS-loads the entity and owns the effective organization/project/task
  rather than trusting browser values. Regression tests cover both the anchor
  wire shape and omitted local fields.
- `2026-07-24` — **Added canonical multi-run request fan-out.** New
  `copyInstanceRequestDraft` composes the existing execution-slice actions to
  copy a Smart Agent Input draft—including multimodal message parts, uploaded
  files/resources, variable resource policies, context, run settings, and
  client tools—between initialized instances without overwriting target model
  overrides. `/agents/battle/model` now consumes this primitive instead of a
  text-only parallel input.
- `2026-07-23` — **Provisional and Builder conversations no longer call durable attachment RPCs.** `AttachedDocumentChips` keeps the association inventory idle while the execution instance is `cacheOnly`, because that UUID has no authorized `chat.conversation` row (and Builder/manual mode intentionally keeps a local Redux ID distinct from every server-minted wire conversation). Picking a stored document in that state now reuses the existing per-turn `instanceResources` path; server-confirmed conversations still use durable `file → conversation` edges and the viewer-aware `conversation_files` inventory.
- `2026-07-23` — **Invalid agent tools became non-fatal at every write boundary.** The manual starter no longer pins a retired tool UUID; shared frontend conversion strips malformed UUID syntax before PostgREST casts `uuid[]`; and the live `agent.definition` trigger now removes missing, inactive, or deleted tool references with a loud warning instead of raising `23503`. The sanitizer runs before version snapshots, covering manual creation, imports, saves, templates, duplicates, promotions, and server/admin writers. Also retired the focused table's forbidden project/task association-mirror triggers and project FK after confirming/backfilling canonical edges.
- `2026-07-23` — **Durable document chips now use viewer-aware attachment reads and explicit policy replacement.** Cross-org viewers of an explicitly shared conversation receive its canonical file attachments through `conversation_files`, while legacy processed-document edges remain visible through the general association inventory. Re-attaching a file no longer erases a concurrent `resource_policy`; only the policy editor and its rollback path opt into full metadata replacement. Transient inventory failures keep last-known chips visible and render an explicit retry control, so active server context never disappears silently from the composer.
- `2026-07-22` — **Two-tier server-side agent search (`agx_search`).** Paginated data REQUIRES a server search: filtering only what the client happened to load answers "no results" for agents that were merely never fetched — a wrong answer, not a missing feature. One RPC, two tiers: **tier 1** (default) matches name / description / category / tags / model / type / id / shared-by email; **tier 2** (`p_deep`, opt-in via the "Prompts" toggle) adds the agent's own `messages` prompt content. Tier 2 is a strict superset and a prompt hit scores **50** — below every tier-1 field — so deep results can only ever append BELOW the obvious matches, never bury a name match. SQL weights **mirror `features/agents/search/score.ts`; change one, change both** or the list reshuffles when the server responds. Results merge **additively** through the same `mergeAgentListRows` path as a list fetch (`searchAgentsServer` thunk) — nothing is replaced or evicted, local matches keep rendering throughout. Wired into `useAgentConsumer`, so **every** agent surface gets it, not just the gallery. Because the local scorer cannot see prompt content, `serverMatchedIds` on the consumer lets a server hit survive the local filter — without it the server would return tier-2 matches and the UI would filter them straight back out. `ORDER BY` ends in `id` (total order) so paging search results cannot drop rows either. Verified live: exact UUID → the agent; "mitochondria" → 0 tier-1, 2 tier-2, ids matching the DB exactly. `migrations/agx_search_two_tier.sql`.
- `2026-07-22` — **`agx_get_list` unstable pagination fixed (agents were missing from search).** The RPC ordered by `is_favorite DESC, updated_at DESC` — not a total order — so each `LIMIT/OFFSET` page re-sorted independently and rows were duplicated onto one page and skipped from another. Paging a 365-agent account 100-at-a-time yielded only **306 distinct ids**; the ~59 dropped agents never entered Redux, so the client-side search could not find them while `/agents/[id]/build` (a direct `agent.definition` read) worked normally. Fix: append `id` as a unique tiebreaker (`migrations/agx_get_list_stable_pagination.sql`) — **do not remove it**. Only bites above one page, so it scaled with agent count. Same defect class audited across all paginated RPCs; see FOUND_DEFECTS D82 for the unrelated findings that audit surfaced.
- `2026-07-22` — THE VIEW LAW: documented (not narrowed) `fetchAppsInitial` in `agent-apps/thunks.ts` as a deliberate blended view (mine + org/project-shared + public + admin-sees-all) — mine-scoping it would drop legitimate org/public apps from the apps home page; a real Mine/Org/Public tab split is future UX work, not this fix.
- `2026-08-09` — **Agent picker constraints are now first-class.** `AgentListInlinePicker` can restrict ownership tabs, label the builtin catalogue for an admin, suppress an out-of-scope globally active pinned row, and resolve each record's correct shell-aware door. Agent Slots consumes those controls as a genuinely system-only picker and independently rejects a non-builtin id at Save; a personal legacy default remains visible in the identity card but can no longer be selected or silently re-saved.

- `2026-08-09` — **Linked Agent Sync now compares instead of merely claiming a link — and the sync contract is complete.** `AgentSyncBody` loads two non-mutating saved snapshots and reuses the canonical diff engine/viewer, so opening Compare cannot mark an Agent Builder draft clean or clear its undo history. Its Relationship tab states whether synchronized behavior matches, separates normal personal profile differences and deliberately-local record state, shows an honest Git-like relationship rail using the milestones actually stored (the derived side's `created_at`, `source_snapshot_at`, current heads), and gives both agents their canonical record + Versions doors. Configuration Diff exposes the existing Changes / All / Summary / JSON views and discloses its operational-field exclusions. `compareAgentDefinitions` is the shared verdict primitive used by both the summary and viewer, with contract tests across every portable behavior field. The frontend definition now carries `ragAwarenessMode`, closing a false-match hole; live RPC `agx_sync_linked_agents` now copies `matrx_actions` + `ui_gates` as duplicate already did, so those visible differences actually reconcile. Interactive sync uses `agx_sync_linked_agents_reviewed`, which locks both records and rejects either saved timestamp changing after review. Sync actions wait for a loaded comparison, post-write refresh failure cannot misreport a committed overwrite as failed, and updating the shared system baseline requires confirmation.

- `2026-08-08` — **One agent picker, everywhere — `SearchableAgentSelect` annihilated.** Eight surfaces were still choosing agents through a bare list + text box with no tabs, counts, sort, favorites, or filters: the Agent Slots repin drawer and test bench (`/administration/agents/slots`), the slot override editor and consumer `SlotAgentPicker`, the global-scope shortcut form, the agent-app create form + its wrapper's "Select Your Agent", the New System App admin page, and a context-menu demo. All now render the canonical `AgentListInlinePicker`; the component and its `AgentOption` type are **deleted**, along with every `agents={...}` array a caller was hand-building (pickers read the Redux agent slice themselves). Three gaps in the canonical picker were closed in the same pass, so the promise is real rather than nominal: (1) the **System tab is no longer crippled** — `filterBuiltinTypeAgents` honours favorites/category/tags (not just search), `AgentFilterBar` lost its `systemTab` suppression, and new `selectAllSystemAgentCategories`/`selectAllSystemAgentTags` supply the builtin population's options; (2) an **ADMIN variant** — `initialTab` + `includeSystemInAll` (blends system agents into "All" and relabels that tab **System**, since to an admin these are their own agents), threaded through both shells and `useAgentListCore`; (3) **every row is badged** `system` or `shared`, so a blended list can never hide which is which. `AgentListDropdown` also gained a `consumerId` override for surfaces that want isolated filter state. Invariant written at the top of this file. Verified live in the slots drawer: tabs `Mine 14 / Shared 10 / All 24 / System 216`, full filter row, builtin categories listed, no console errors.
- `2026-07-19` — PlusAttachMenu manual-mode gate: `selectIsManualExecutionMode` disables the model picker (shows live builder model read-only) and Advanced Settings Window on Agent Builder test runs; agent-mode runs unchanged.
- `2026-07-18` — Unified file-family controls across Agent Builder defaults,
  runtime media variables, and durable chat attachments. The shared dynamic
  inventory supports suppression and up to three bounded inline promotions;
  stored chat files now always use canonical `file → conversation` edges.
- `2026-07-18` — Restored system-agent admin runner parity with the user runner: shared PageHeader controls, desktop/mobile Large Route sidebar activation, and admin-preserving run-history URLs.
- `2026-07-18` — Added direct typed APIs and `/demos/agent-assignments` for secure single-variable random assignment plus durable paired, independent-random, and Cartesian agent batches with progress, resume, cancellation, and Content IR-backed contracts.
- `2026-07-17` — Made conversation provenance release-safe: `SourceFeature` now has an exhaustive runtime registry, stored unknowns cross one loud branded boundary instead of `as never`, and `release.sh` blocks on a repository-wide literal `source_app` / `source_feature` audit.
- `2026-07-17` — Replaced the oversized Agent Resources association browser with the Variables-style compact row, canonical resource picker, batch-add mode, and Stored Files chips/details; confirmed the manager has one Builder mount point.
- `2026-07-17` — Fixed the shared Creator Panel Payload tab crash (`Cannot read properties of undefined (reading 'assistant')`): its memoized request-preview selector now includes `userPreferences`, matching `assembleRequest`'s USER-layer directive-policy dependency. Added focused regression coverage for default and explicit policies.

- 2026-07-17 — Added global version-ID resolution to `/agents/all` search, linking an accessible version snapshot to its parent agent’s historical-version page.

---

## Current work / migration state

Active rebuild governed by `features/agents/migration/MASTER-PLAN.md`. Phase-ordered plan (20 phases). Key context:

- Legacy prompts stack is still wired in some places — do not extend it. See `migration/INVENTORY.md` for the legacy ↔ agent map.
- Phases 16–19 are deletion phases; run last.
- RTK only for new state; extend `features/agents/redux/**`, never create parallel local state.
- Multi-scope (admin/user/org) from day one — Shortcuts, categories, content blocks must all support it.

---

## UI component conventions

### JSON display

Always use `JsonInspector` from `@/components/official-candidate/json-inspector/JsonInspector` for any JSON data display.
Do **not** use raw `<pre>` or custom JSON renderers — `JsonInspector` bundles five display modes (formatted, explorer, tree, JSON-tree, truncator) plus built-in copy, lazy-loading, and an optional edit tab. Give the wrapper a defined height (`h-64`, `h-[calc(100dvh-12rem)]`, etc.) because `JsonInspector` is `flex-col h-full` internally.

```tsx
import { JsonInspector } from "@/components/official-candidate/json-inspector/JsonInspector";

<div className="h-64">
  <JsonInspector data={someObject} label="My data" className="h-full" />
</div>;
```

---

## Conversation row context menu (cross-surface)

Every conversation-list surface renders rows with the shared **`ItemRow`** primitive (`@/components/official/item/ItemRow`) — full-width label with a deepening edge fade, hover/touch-revealed `⋯`, right-click, and inline rename. **Read [`components/official/item/README.md`](../../components/official/item/README.md) before changing row or menu behavior.** The menu is built declaratively from one registry and dispatches optimistic thunks against shared slices, so Rename / Pin / Archive / Delete from any surface updates every other surface listing the same conversation (no re-fetch).

### Anatomy

| Piece                                                       | Role                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `conversationActionRegistry.tsx#buildConversationMenu(ctx)` | Pure `(ctx) => ItemMenuConfig`. Sections: **Actions** (Rename, Pin/Unpin, Open in new tab, Copy link), **Manage** (Duplicate, Share…, Archive/Unarchive, Exclude/Include KG), **Danger** (Delete). Rename is an `intent: "rename"` entry — `ItemRow` turns it into inline edit; the row also passes a `rename` prop dispatching `renameConversation`. The menu closes itself (Radix) before any overlay/confirm opens; **no backdrop**. |
| `ItemRow`                                                   | Owns its own kebab + right-click menu (both forced `z-[9999]`, so they layer above floating windows), the fade, and inline rename. Per-row — there is **no singleton menu instance**.                                                                                                                                                                                                                                                   |

### Data + thunks

The menu is fully DB-backed:

- `cx_conversation.is_favorite` — boolean column, partial index on `(user_id, updated_at desc) where is_favorite = true and deleted_at is null`. RLS lets the owner write directly; no RPC needed.
- `get_agent_conversations` RPC returns `is_favorite`; `fetchGlobalConversations` selects it too. Mapping helpers (`mapRpcRowToConversationListItem`, `buildConversationListItemFromExecution`) populate `ConversationListItem.isFavorite` / `excludeFromKg`.
- Actions live in `features/agents/redux/conversation-list/conversation-row-actions.thunks.ts`:
  - `renameConversation` — optimistic across `conversationList`, `conversationHistory`, `messages`, `conversations` slices, direct Supabase update, reverts on failure.
  - `setConversationFavorite` / `setConversationArchived` / `setConversationExcludeFromKg` — same pattern, write `is_favorite` / `status` / `exclude_from_kg`.
  - `duplicateConversation` — wraps the **server** `forkConversationServer` with no selector + `Copy of <title>`.
- `softDeleteConversation` (in `execution-system/message-crud/`) dispatches `removeConversationFromScopes({ conversationId })` so every list drops the row without a re-fetch.

### Wired consumers (every conversation list)

`ConversationHistorySidebar.tsx` (chat/code/agent-apps) · `shell/AgentRunSidebarMenu.tsx` · `run/run-sidebar/AgentRunsSidebar.tsx` · `agent-widgets/AgentChatHistorySidebar.tsx` · `chat/ChatHistorySidebar.tsx` (`/chat`) · `window-panels/windows/agents/{ChatHistoryWindow,AgentRunHistoryWindow,AgentRunWindow,AgentContentHistoryPanel}.tsx` · `code-editor/agent-code-editor/components/parts/CodeEditorHistoryPanel.tsx` (DRAFT rows keep their own `Trash2`; real conversations get the menu).

### Add the menu to a new conversation list

Render an `<ItemRow>` per conversation: `menu={() => buildConversationMenu({ conversationId, title, isFavorite, isArchived, excludeFromKg, isOwner: true, href, surfaceKey?, dispatch })}` (pass the surface's canonical `href` for Open/Copy-link; set `surfaceKey` only where Duplicate should jump focus). Add `rename={{ value: title, emptyFallback, onCommit: (next) => dispatch(renameConversation({ conversationId, title: next })) }}` so the menu's Rename works. `trailing` holds indicators (streaming dot, a standalone star toggle); `size="sm"` for dense rows. The kebab, right-click, fade, and rename come for free.

---

## Conversation transcript: logical turn grouping

The agent backend reserves a **new `cx_message` per server iteration** (one for each thinking step / tool_call / final text emission inside a single agentic turn). So a tool-heavy answer becomes 10–100+ adjacent `assistant` rows in `messages.byId` — even though, conceptually, the user asked **one** question and got **one** answer that happened to include a multi-step trace.

Rendering each iteration as its own visual unit fragmented the answer: every iteration got its own `space-y-6` gap and its own AssistantActionBar, scattering controls across the transcript. The fix is a render-layer **logical turn grouping** that collapses contiguous assistant messages between two user messages into a single visual unit.

### Anatomy (`features/agents/components/messages-display/`)

| File                                  | Role                                                                                                                                                                                                                                                                                                            |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AgentConversationDisplay.tsx`        | Builds the existing per-entry `displayEntries[]` (streaming-bubble detection unchanged), then runs a grouping pass that emits `DisplayGroup[]` — `{ kind: "user", messageId }` or `{ kind: "assistant", members: AssistantTurnGroupMember[] }`. Outer `space-y-6` lives BETWEEN groups; inside groups is flush. |
| `assistant/AssistantTurnGroup.tsx`    | Renders N child `AgentAssistantMessage`s with `hideActionBar={true}` and emits **one** trailing `AssistantActionBar` anchored to the latest assistant `messageId` in the group. Owns the DOM-capture `<div>` so "Print" covers the whole turn.                                                                  |
| `assistant/AgentAssistantMessage.tsx` | Accepts `hideActionBar?: boolean`. When true: skips its internal `AssistantActionBar` _and_ skips wrapping itself in `captureRef` (parent owns capture). Content rendering (`MarkdownStream`, files strip, error UI) is unchanged.                                                                              |
| `assistant/AssistantActionBar.tsx`    | Accepts `groupMessageIds?: string[]`. When set with >1 ids: `Copy` and `Speak` aggregate `extractFlatText` across every member of the group (joined with blank lines). `Edit` / `Like` / `Delete` / overflow-menu stay anchored to the single `messageId` (the latest in the group — the "answer").             |

### Visual rules

- **Zero added chrome between sub-messages.** No card, no left rail, no iteration badges, no extra spacing. The natural content (tool cards, thinking blocks, text) already provides all the visual rhythm a multi-step turn needs.
- **One action bar per group**, anchored to the **last** assistant message. Intermediate iterations carry no bar at all.
- **Compact-density behaviour carries over for free.** `selectIsLatestAssistantMessage` returns true only for the conversation-wide latest assistant — which is the group bar's anchor only for the newest turn. Older groups' bars hover-reveal exactly as before.
- **Streaming gate is unchanged.** The group bar renders only when the group's last member has `isStreamActive === false` AND resolves to a real `messageId` — same gate as the pre-grouping per-message path.

### Action semantics on the group bar

| Action                         | Anchor                                                                                                               |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------- |
| Copy                           | Aggregated across every member (`groupMessageIds`)                                                                   |
| Speak (TTS)                    | Aggregated across every member                                                                                       |
| Print (full DOM capture → PDF) | The group's outer `<div>` — covers every iteration's DOM + files strip                                               |
| Like / Dislike                 | Latest `messageId` (the answer)                                                                                      |
| Edit (full-screen editor)      | Latest `messageId`                                                                                                   |
| Retry (atomic)                 | Latest `messageId` → `atomicRetry` walks back to the preceding user turn → naturally retries the whole logical turn  |
| Fork / Delete / overflow menu  | Latest `messageId` (single-iteration semantics preserved; future work may extend Delete to cascade across the group) |

### Single-iteration turns are unchanged

A turn with exactly one assistant `cx_message` becomes a one-member group. Visually and behaviourally identical to the pre-grouping render: same content, same bar, same single-message `Copy` / `Speak` text (aggregation is skipped when `groupMessageIds.length <= 1`).

### Cold deep-link windowing

`/chat/[conversationId]` cold loads and `/agents/[id]/run?conversationId=...` deep-links render the transcript bottom-first. `AgentConversationDisplay` derives display groups through `display-groups.ts`, then applies a visible group window from the messages slice. The first hydrated frame shows the latest user group + latest assistant group only, bottom-aligns that cold transcript with a moderate viewport spacer, and `AgentConversationColumn` pins the scroll container to its true bottom while `MarkdownStream` and the auto-revealed prior four groups settle above it. Runner deep-links pass the same bounded `loadConversation` limit and `deferColdMarkdown` flag as chat so the two page surfaces cannot drift.

The top sentinel is locked during this cold settle window so it cannot auto-trigger while the transcript is still shorter than the viewport. Once unlocked, upward scroll reveals two already-loaded groups at a time; only after the hidden loaded groups are exhausted does it dispatch `loadOlderMessages`. Live `requestId` streams and URL-promotion conversations skip the cold markdown/window path, so active streaming bubbles keep the existing stream contract.

---

## Working document (per-conversation) & scratchpad (user-global)

ONE primitive, two **kinds** (`kind: "working" | "scratch"`) with DIFFERENT scopes:

- **`working`** — the collaborative doc the user and agent co-author (agent reads AND writes via `ctx_patch`), attached to a conversation via a single `conversationId` prop. Generalised out of Scribe; Scribe consumes the same value shape.
- **`scratch`** — the user's **scratchpad**: ALWAYS user-editable, NEVER agent-writable **through this channel** (`mutable: false`, no writeback `source`; the agent may `ctx_get` it). That is a rule about the CLOUD agent out in the chat, and it is unchanged. It does NOT govern an agent the user runs from INSIDE the scratchpad editor: the `matrx-user/scratchpad` SURFACE declares write targets (see `features/surfaces/FEATURE.md`), which land through the editor's own `onChange` behind a per-target confirm — a different channel with a human in the loop, not a `ctx_patch` writeback. **USER-GLOBAL, not per-conversation**: a user-owned POOL of rows, exactly ONE **active** at all times (`userPreferences.scratchpad.activeId`, synced cross-device). Slice entries key on the synthetic scope **`sp:<documentId>`** (`scratchScopeId`), reusing all working-doc machinery; `destroyInstance` never evicts them. **Rendering the scratchpad readonly to the USER anywhere is a bug.**

**Scratchpad rules:** the active scratchpad floats on every page (Quick Actions → **`scratchpadPanel`** overlay → `ScratchpadQuickPanel`: switcher pick/new/delete over the shared panel) but is published into a conversation's context **ONLY when that conversation opted in** — the **per-conversation gate** (default OFF; documents are never force-attached to a chat, they can badly confuse the model when unrelated) — **and never when empty**. `setScratchpadGateThunk` is the one toggle (creates+activates a scratchpad first when the pool is empty); the gate persists as a scratch-kind association edge at the deterministic **phantom** id `reservedWorkingDocumentId(conversationId,"scratch")` (no backing row — a pure conversation-level boolean; hydrate paths must skip it when resolving real docs) and restores via `hydrateConversationDocumentsThunk`. Toggling never touches content, rows, or the active pointer. Additional scratchpads attach per-conversation as `user_scratchpad_<id8>` extras (association edges + `attachedScratchByConversation` — attaching IS their opt-in), same non-empty rule. Delete = soft delete (`softDeleteWorkingDocument`; lists filter `deleted_at` explicitly — authenticated RLS never gates it).

**The routing rule (kills the readonly-dump class):** `DOC_CONTEXT_KINDS` / `docKindForContextKey` (`utils/workingDocumentContext.ts`) is the SINGLE authority mapping context keys (`working_document`, `user_scratchpad`, `user_scratchpad_*`, future doc keys) → document kind. Every consumer resolves through it — `ContextSlotDetailSheet` (doc keys → editable `WorkingDocumentBody`/workspace, never the readonly `ValueRenderer`), `ConversationContextRail` (doc pills read the **editor slice**, never `instanceContext` — the publication is agent-facing only), `process-stream` gates. A new doc-like key is ONE map entry.

The working doc is **opt-in** (off by default); its on/off + any cross-conversation attach **persists across reloads** via `platform.associations` edges.

**Entry points**

- Slice/selectors/thunks — `redux/execution-system/instance-working-document/` (keyed `byKey` = `workingDocKey(scope, kind)`; `working` keeps the bare `conversationId` for back-compat, scratch scopes are `sp:<docId>`; conversation entries cleared on `destroyInstance`). Scratchpad thunks live in `scratchpad.thunks.ts` (`hydrateActiveScratchpadThunk` — adopt newest when no pointer, `createScratchpadThunk`, `setActiveScratchpadThunk`, `deleteScratchpadThunk`, `attach/detachScratchpadFromConversationThunk`, `hydrateAttachedScratchpadsThunk`).
- Hook — `hooks/useWorkingDocument.ts` (`useWorkingDocument(scope, kind?)` for state + actions; `useWorkingDocumentContextSync` — working publication + materialize/realtime effects for any scope; `useScratchpadContextSync(conversationId)` — publishes active + attached scratchpads, gated on the per-conversation opt-in AND non-empty content; `useConversationDocumentsBridge(conversationId)` hydrates working + scratch gate + active + attached and mounts both syncs — mounted in `RunControlsMenu` AND `ProTextareaAgentPanel`).
- Context value — `utils/workingDocumentContext.ts`: `buildWorkingDocumentContextValue` (the single `working_document` value shape, `mutable: true`; Scribe's `buildWorkingDocumentValue` delegates here) + `buildUserScratchpadContextValue` (the `user_scratchpad` value, `mutable: false`, `persist: "client"`, no `source` — agent-read-only) + `DOC_CONTEXT_KINDS`/`docKindForContextKey`/`scratchpadExtraContextKey`.
- Service — `cx-working-document.service.ts`: document CRUD by id (`getCxWorkingDocumentById`, `updateCxWorkingDocumentTitle/Content`, `commitWorkingDocumentContent`, `materializeWorkingDocument`, `listUserDocuments`, `listRecentUserDocuments`) + association-edge ops (`linkDocumentToConversation`, `unlinkDocumentFromConversation`, `setConversationDocumentEnabled`, `listConversationDocuments`, `listDocumentConversations`). Origin resolves `conversation_id` (legacy column) → `metadata.origin_conversation_id`, so new column-less docs still surface.
- UI — `components/working-document/WorkingDocumentPanel.tsx` is the ONE editor (header + **source-controls row**: rename, bind note [working], link, status — on every mount via `showSourceControls`; scratch shows "private — agent reads, never edits", no note binding, no enable toggle) + `DocumentLinkPicker.tsx` + `documents-workspace/DocumentsWorkspace.tsx` (+ `DocumentsListRail.tsx`) — the ONE multi-document workspace shell every open surface renders (Canvas, `workingDocumentPanel` sidebar, floating window, context drawer/slot sheet, run-controls Document tab) + `ScratchpadQuickPanel.tsx` (the global quick-access panel). `WorkingDocumentControls` is **deleted** — its chrome lives in the panel. Toolbar quick-access: `components/inputs/smart-input/ContextDocsMenu.tsx` (next to the `+`; BOTH rows are per-conversation **FEATURE-exposure** switches with FIXED titles — "Working document" / "Scratchpad", never a doc's given name; the scratch switch drives `setScratchpadGateThunk`; the Context section embeds the compact `ActiveContextTree` from `features/scopes`). **Per-document share (scratch):** `WorkingDocumentPanel` accepts `gateConversationId` — when the host knows the chat (DocumentsWorkspace scratch tabs, canvas payload `data.gateConversationId` from the rail pill), the scratch panel header shows a per-document "Share" switch: the ACTIVE scratchpad maps to the gate, any other scratchpad maps to attach/detach edges.
- Window — `features/window-panels/windows/WorkingDocumentWindow.tsx`, opener `features/overlays/openers/workingDocumentWindow.tsx` (overlay id `workingDocumentWindow`, multi-instance per `conversationId`, ephemeral).

**Storage — first-class entity + platform.associations (M2M).** `workbench.working_documents` is the **document**: a first-class, versioned, owner-scoped entity (token `working_document`; `is_versioned=true` + `_history` trigger → `history.row_versions`; entity-variant RLS = owner `created_by` + org). Chat↔doc links are **`platform.associations` edges** (`working_document` source → `conversation` target; `metadata {enabled, doc_kind}`), reached ONLY via the canonical `associationsService` — the bespoke `cx_conversation_documents` junction is **retired**. Many conversations ↔ many documents. Migrations: `migrations/working_document_canonicalize_step1*.sql` (canonicalize + move chat→workbench), `_step2_associations.sql` (junction→edges), `_step4_cleanup_empties.sql`.

**Opt-in + hydrate.** BOTH kinds start OFF per conversation. `hydrateConversationDocumentsThunk` restores the enabled WORKING doc from the conversation's association edges on mount AND the scratch **gate flag** (edge at the phantom deterministic id — flag only, never loads a doc; the bridge hydrates the active scratchpad + `hydrateAttachedScratchpadsThunk` restores attached extras, both skipping the gate id). Toggling persists via `setConversationDocumentEnabledThunk` (working: enable reserves an id, no write; disable persists `enabled=false` on the edge; scratch on a real conversation: persists the gate edge BOTH directions, no binding; sp-scopes never persist an edge).

**Materialize-on-write (the core invariant — kills the old data-loss class).** Enabling a document only RESERVES an id — NO DB row. Working docs reserve the **deterministic id** (`reservedWorkingDocumentId` = `uuidv5(${conversationId}:${kind})`, so every tab/session agrees BEFORE the row exists); scratchpads carry their id in the `sp:<docId>` scope. The durable row (+ the conversation edge, working only) is created on the **first byte of content by either party**: `materializeWorkingDocumentThunk` (user; sp-scopes skip the `cacheOnly` gate, the edge, and origin provenance) or the server writeback (agent), reflected via `reflectAgentMaterializedThunk`. An agent-editable doc is ALWAYS `persist:"auto"` — **`persist:"client"` is BANNED** (an unbound editable doc dropping the agent's edit was THE bug). `buildWorkingDocumentContextValue` carries the reserved id + a `materialize` spec + `base_version`; the server's `_writeback_working_document` UPSERTs at the reserved id (create-if-missing). Working materialize is gated on `!cacheOnly` (the edge can't target an unconfirmed conversation; content lives in Redux until then). **Every OTHER conversation-edge write follows the same gate via the pending-edge queue**: `persistOrQueueLink`/`queueEdgeOp` (thunks file) hold link/gate/unlink intents while the conversation is `cacheOnly` — an `assoc_add`/`assoc_remove` against a row-less conversation is a guaranteed `42501` — and `flushPendingDocumentEdgesThunk` drains them when `process-stream` dispatches `confirmServerSync`. Never call `linkDocumentToConversation`/`unlinkDocumentFromConversation` directly from a thunk without this gate. Binding kinds: `none | note | cx_working_document | studio_document`. **Linking** (`linkConversationDocumentThunk`): attach an existing document from another conversation (M2M). **Note binding** (working only): `bindWorkingDocumentToNoteThunk({mode})`; `unbindWorkingDocumentThunk` reverts to the conversation's own deterministic doc.

**Auto-naming.** An unnamed document auto-derives its title from content (H1 / first non-empty line, markdown markers stripped) via notes' `generateLabelFromContent` (`useAutoLabel`) — never blank, never forced. A manual name stops auto-generation; `setTitle` trims so a cleared field falls back to the auto-name (the old blank-name bug).

**Round-trip + concurrency.** Agent edits stream in LIVE via the **`context_delta`** event (D9 fix): the server emits the edit's content (compact splice or full text) the moment each `ctx_patch` lands — before the DB writeback — and `process-stream` applies it synchronously via `applyAgentWorkingDocDelta` (pure applier: `contextDelta.ts`; routes `source_kind:"studio_document"` to the transcript-studio slice for Scribe, everything else to `applyAgentWorkingDocContent`). A splice that doesn't match the local copy (`base_len` guard — e.g. the user typed mid-turn) is SKIPPED, never forced. Backstops (unchanged): Supabase realtime on `workbench.working_documents` filtered by `id` (a **ref-counted shared channel** — one connection + one dispatch per doc across all mounts) + the `context_persisted` `syncWorkingDocumentFromAgentThunk` re-read, which ALWAYS runs (latches the new row `version`, heals any skipped delta); `context_changed` skips its re-read only while this stream's deltas are applying cleanly. The user's content updates the slice on every keystroke and is **published to instanceContext IMMEDIATELY** (no debounce) so the agent receives the exact latest text at submit. **Optimistic concurrency:** the user's commit (`commitWorkingDocumentContent`) writes only if the row is still at `base_version`; a concurrent edit (typically the agent this turn) REFUSES the write and raises a **conflict** (`markWorkingDocConflict`) — the user's draft is preserved, a split diff (agent vs yours) + **Keep mine / Use agent's** reconciles it; both versions are in `history.row_versions`, so nothing is silently lost. The server writeback also version-checks and emits `context_conflict`. Realtime handler applies **only columns present in the payload** (title-only UPDATEs omit `content` under `REPLICA IDENTITY DEFAULT` — never treat missing as blank). `applyAgentWorkingDocContent` keeps the **BUG-B guard** (empty/omitted remote never wipes non-empty). Scratch has realtime too (cross-tab sync of the user's own edits) but no agent/stream write path — the agent can never write it.

**One UI component.** `WorkingDocumentPanel` is the single document surface (editor / agent-diff / version history / source controls / toolbar); every mount — `DocumentsWorkspace` tabs (incl. the run-controls Document tab), the window, canvas, the scratchpad quick panel — uses it, varied only by chrome props. No forked editor logic. The context drawer/slot sheet (`WorkingDocumentBody`, now `initialKind`-aware) renders `DocumentsWorkspace` (rail collapsed) and registers **no** TitleActions/Footer — the workspace owns that chrome (a registered null-rendering Footer still trips the drawer's `hasFooter` and leaves an empty bar).

**Multi-attach workspace.** `DocumentsWorkspace` = collapsible recent-docs rail + tabs over `WorkingDocumentPanel`. Base tabs (never closable) = the conversation's working + the user's ACTIVE scratchpad (`sp:<activeId>` — the same document on every surface; the workspace creates one if none exists). Any other document attaches as a closable tab: working docs key by their **ORIGIN** `(conversationId, kind)`, scratchpads by their `sp:<docId>` scope; `openWorkspaceDocumentThunk` loads content on open AND (with `attachTo`) persists the `working_document → conversation` edge (attached scratchpads also join the publication list); closing the tab = `detachWorkspaceDocumentThunk` (removes the edge + publication, keeps the doc); attached tabs restore via `listAttachedDocumentTabsThunk` (skips the conversation's own working doc AND the active scratchpad — already base tabs).

**Composer UI.** Chips are TINY by contract — icon + ONE word + rich tooltip; the composer is not a dashboard. The `ConversationContextRail` doc pills read the editor slice (working: `enabled`; scratch: gate ON or extras attached), **click = canvas visibility toggle** (open/switch/close on the stable `wd:<scope>:<kind>` canvas item shared with `ChatCanvasButton`), and carry a **floating X** = same as the switch (generic context-entry pills: click = detail sheet, X = `removeContextEntry`; lists/layers pills have no X). Attachment chips are `ResourceAttachmentTile variant="compact"` (same X + tooltip pattern). The `ContextDocsMenu` toolbar dot lights when working on, scratch gate on, OR active scope context set. **Stacked** chrome: dedicated Layers (`ContextDocsMenu` → dense `ActiveContextTree`) + `+` compact rows (model / doc switches / chips+Edit / Advanced) — never duplicate the tree in `+`. **Single-row / landing** (`foldToolbarExtras`): no Layers button; `+` folds `ContextDocsMenuBody` (docs + tree) + Enter/auto-clear toggles. The `+` `RunControlsMenu` keeps its Document/Context tabs (Document tab = `DocumentsWorkspace`; badge dot lights for working-enabled OR scratch-gate-on).

---

## Change log

- `2026-08-08` — **`SettingControlInput` dense pass + builder renderer convergence.** The shared per-control primitive ([`controls/SettingControlInput.tsx`](components/settings-management/controls/SettingControlInput.tsx)) got the global ui-dense restyle: enum triggers are borderless/transparent inline triggers (ModelListDropdown trigger style), inline `fontSize:16px` overrides replaced with `text-base sm:text-xs` (iOS no-zoom on mobile, true 12px on desktop), compact textareas on the 4px grid, JSON parse error at the `text-[11px]` floor. `AgentSettingsCore`'s internal `renderControlInput` (the deferred duplicate) now delegates to the primitive — only value defaulting (`value ?? control.default ?? min`) and the `response_format` unwrap (primitive emits `{type}`, `handleSettingChange` validates the string via `toResponseFormat`) stay at the callsite. One primitive now renders the builder settings modal, `RunConfigOverrides`, and `ModelSettingsReviewDialog`. Verified live on both surfaces (dark + light + 375px): override set/highlight/reset in Chat Options → Overrides; `response_format` round-trip in the builder modal ends "No unsaved changes".
- `2026-07-29` — **Adversarial hardening of the edge queue + sharing pass.** The queue now gates on PROVEN persistence, not `cacheOnly` (which flips at `record_reserved` — an announce, not a commit): `flushPendingDocumentEdgesThunk` runs for every newly confirmed conversation, marks itself in-flight synchronously (all edge intents queue during the announce→commit window and during drains, killing both the residual mid-stream 42501 and the detach-resurrection race), drains loop-until-empty, keeps the queue on failure for the next confirm, and surfaces failures via `markWorkingDocError` (never console-only). Guests short-circuit (nothing queues that can never flush). Re-enabling a materialized doc persists `enabled:true` (was reload-OFF); org-unresolvable persists are loud. Viewer-level sharees: the conveying edge needs EDITOR on the doc, so viewer attaches are session-only with an honest notice instead of a doomed write, and `WorkingDocumentPanel` renders read-only via `useAccess` (a viewer's RLS-refused save previously surfaced as an unresolvable fake "concurrent edit" conflict loop; also fixed the stuck `saving` flag in the keep-mine re-conflict branch). The picker's Shared scope now lists DIRECT `iam.permissions` grants (all registry spellings, active + unexpired) — never container-reachable docs (contextual-access rule) — which also fixes NULL-`created_by` rows vanishing from both scopes. `?attachDoc=` moved to a Suspense-wrapped `useSearchParams` watcher so query-only client navigations fire it. Policy findings filed: D118 (conveying-edge re-share amplification), D119 (editors can flip `visibility` at the DB layer). **Round 2 (verify pass):** the view-only gate moved INTO `useWorkingDocument` — the single write authority resolves `viewOnly` itself and no-ops every durable write (debounced commit, keep-mine, title persist, auto-label), closing the TUI-editor, version-history-Apply, and auto-label bypasses in one choke point; the panel/editor/view-controls only render read-only chrome (TUI modes coerce to preview under readOnly — they have no read-only rendering). `AttachDocDeepLink` waits for the conversations-slice record (an unknown conversation is treated as persisted by the queue — attaching pre-registration would fire a doomed direct write). Ephemeral (incognito) conversations skip edge queueing entirely (no row will ever exist to flush against).
- `2026-07-29` — **Working documents are shareable + consumable across users.** DB (applied + ledgered, `migrations/working_document_shareable.sql`): `workbench.working_documents.visibility` (canonical enum, default `personal` — identical to the access kernel's prior fallback, so zero behavior change), `entity_types.default_visibility='personal'`, and a `platform.shareable_resource_registry` row for `working_document` (RLS already enforced grants via `iam.has_access`). FE: `ShareButton` in the panel's source-controls row (materialized `cx_working_document` bindings only; ShareModal resolves ownership itself), `DocumentLinkPicker` gains **Mine / Shared with me** scopes, `listUserDocuments`/`listRecentUserDocuments` declare explicit scope (`created_by` filters — THE VIEW LAW; the rail stays personal now that sharing exists), `/s/[token]` renders `working_document` via the markdown renderer, and the registry share URL `/chat/new?attachDoc={id}` is consumed by `ChatRoomClient` on fresh routes (links the document into the new chat via the pending-edge queue, opens the panel, strips the param). Also closed pre-existing registry drift: 6 live rows (`content_ir_kind_instance`, `crm_campaign`, `party`, `seo_collection_run`, `seo_keyword`, `youtube_search`) mirrored into `utils/permissions/registry.ts` + snapshot; live `auto_ingest_batch` URL corrected to the real route; `content_ir_kind_instance`'s enum-in-boolean-slot registry row filed as D117.
- `2026-07-29` — **Document edges queue while the conversation is cache-only + linking an existing doc is a first-class entry point.** Attach/link, the scratch gate, disable-persist, and detach all fired `assoc_add`/`assoc_remove` against conversations with no `chat.conversation` row yet (nothing sent), guaranteeing `42501 editor access` failures (the live incident behind "[working-document] attach: link failed"). All edge writes now route through `persistOrQueueLink` (queue while `cacheOnly`, flush on `confirmServerSync` via `flushPendingDocumentEdgesThunk`); detach on a cache-only conversation cancels the queued link instead of calling the RPC. `linkConversationDocumentThunk` adopts the picked document into Redux BEFORE persisting the edge (edge failure degrades to a loud durability warning, never a blank doc), and `ContextDocsMenu` shows the document picker in the OFF state ("Start from existing…") so enabling the working document with an existing doc is one step — blank is no longer the only path.
- `2026-07-27` — **Failed persistence barriers cannot launch artifact writes.** A transport-complete stream may still terminate with `agent_error`; its persistence coordinator then rolls back IDs announced earlier by `record_reserved`. `processStream` now runs post-stream canvas materialization only when there is no terminal error, preventing a rolled-back assistant UUID from violating `fk_canvas_items_source_message`. The shared `chat.tool_trace` event constraint now also accepts the canonical executor's loud argument-recovery outcomes (`INFERRED`, `COERCED`, `VARIANT_RECOVERED`), so telemetry cannot abort the request it describes.
- `2026-07-22` — Added `// VIEW LAW:` comments to five redux thunks (`agent-apps/thunks.ts`, `conversation-history/thunks.ts`, `conversation-list.thunks.ts`, `execution-system/client-capabilities/desktop-presence.ts`, `execution-system/thunks/conversation-bundle.ts`, `tools/tools.thunks.ts`, `usages/usages.thunks.ts`) documenting each list read's existing scope (deliberate blended view, RLS container-scope, or message_id container-scope) inline at the query site, clearing THE VIEW LAW's bare-RLS guard findings (no behavior change).
- `2026-07-18` — **Builder artifact persistence uses the reserved message's server conversation.** Manual runs intentionally keep a stable local Redux conversation while `/ai/manual` mints a fresh durable wire conversation per request. Chat canvas upserts now omit the client conversation argument so `cx_canvas_upsert` resolves `chat.message.conversation_id`; materialization carries the returned server id into adapters and the discovery index. This removes the `canvas_items_conversation_id_fkey` 409 cascade without changing stream dispatch keys. Focused service/materialization regressions cover both boundaries. The Builder endpoint docs were corrected from the retired `/prompts` model to the live `/ai/manual` contract.
- `2026-07-18` — **System Agents admin hub/catalog parity.** The Administration dashboard's `Agents: System` category card now opens the canonical `/administration/agents/system-agents` parent page. That hub now exposes every route advertised by the admin catalog: the flat All Shortcuts view plus guided agent creation, manual agent creation, and system-app creation join the existing agent/lineage/shortcut/category/content-block/app management tiles. The general Administration route directory is catalog-grouped, so all system-agent list, creation, and dynamic detail routes appear together instead of being split by raw filesystem segments.

- `2026-07-18` — **Managed context-menu agents now open in `AgentFlexiblePanel`.** Context Menu v3's desktop and mobile bound-agent launch paths share one framework-owned `displayMode: "flexible-panel"` config, replacing `modal-full` while preserving each shortcut's explicit persisted display mode and the safe open-and-wait default.
- `2026-07-18` — **Coordinated assignment API and demo.** Added strict generated API and stream types, RTK-managed batch state, durable polling/resume/cancel behavior, and `/demos/agent-assignments` covering paired rows, random batches, Cartesian enumeration, and the normal single-random marker path. See `docs/AGENT_ASSIGNMENTS.md`.
- `2026-07-18` — **Random automatic assignment for choice variables.** Agent Builder can opt a static-option or Structured List variable into `assignment.random`; every surface using `VariableInputComponent` gains an “Assign randomly” control and sends the exact typed marker instead of preselecting in the browser. Manual Builder requests also carry live `variable_definitions`, so unsaved option changes are authoritative for tests. Human-readable displays render “Random on run.” Aidream validates opt-in and option source, chooses with `secrets.choice`, and routes Structured List results through the existing secret-safe picklist resolver.
- `2026-07-17` — **Smart Input can attach live Context Values.** `ResourcePickerMenu → Context Values` now hosts the canonical narrow Drill Deck, where organization/type/scope rows navigate and only context-item leaves select. Each selected `(scope_id, context_item_id)` cell becomes a compact Context value attachment backed by the canonical `context_value` Matrx reference fence; aidream resolves the live cell on every send rather than receiving a stale snapshot. This stays on the supported text/fence path instead of adopting the unfinished `input_context` structured block.
- `2026-07-17` — **`AgentFlexiblePanel` default size matches `AgentFullModal`.** Initial window is now `768×85vh` (was `500×800`) so `flexible-panel` launches feel equivalent to the old full modal; mins raised to `480×320`.
- `2026-07-15` — **Builder tool save looked like it stuck but tools vanished on reload.** Save correctly wrote `agent.definition.tools`; `dbRowToAgentDefinition` preferred stale `tool_config.tools` (often `[]`) and masked the column. Fixed: read assignment from `tools` / `custom_tools` only; `tool_config` is flags-only (`auto_tools_disabled`). Scrubbed dead `tool_config.tools` keys live (`migrations/agent_tool_config_strip_assignment_key.sql`).
- `2026-07-16` — **Launches no longer read the model catalog or authorize execution in the browser.** Removed the client runtime resolver from `launchAgentExecution`: it incorrectly used the current selectable-model view (`ai.model_config`) to authorize pinned historical versions, making any version on a deprecated model fail before its request reached aidream. The frontend now creates the normal execution instance and sends the agent/version identifier; aidream remains authoritative for model resolution, deprecation fallback, interaction validation, and execution transport. The voice surface manages its own browser-realtime session and is not launched through this generic thunk.
- `2026-07-15` — **Long agent descriptions take over the empty-state background.** `AgentEmptyMessageDisplay` (chat + agent run before the first message) now hides the icon and name when the description exceeds 1000 characters and renders description-only markdown in a left-aligned, scrollable layout.

- `2026-07-14` — **Runner conversation deep-links share the cold bottom-first loader.** `/agents/[id]/run?conversationId=...` now loads the target conversation with the same bounded initial page, transcript skeleton, cold markdown settle, bottom-pinned display window, and older-message lazy reveal used by `/chat/[conversationId]`. Verified in-browser on a long conversation with `scrollTop === maxTop`.
- `2026-07-14` — **Cold chat deep-links render at scroll bottom.** Existing `/chat/[conversationId]` loads now keep a transcript skeleton up until the DB bundle resolves, render the latest user + assistant groups first, bottom-align the cold transcript, pin the scroll container to `scrollHeight - clientHeight` while cold markdown and the prior four groups settle above it, and lazy-reveal older loaded groups two at a time before fetching older pages. Streaming/URL-promotion conversations bypass the cold path.
- `2026-07-14` — **`cx_fork_conversation` preserves artifact discovery indices.** The direct Supabase fork thunk (`forkConversation`) was failing on assistant messages containing multiple artifacts of the same `chat.artifact.artifact_type` because the RPC copied discovery rows without `artifact_index`, collapsing them into one NULLS-NOT-DISTINCT natural-key slot. Migration `20260714161847_fix_cx_fork_artifact_indices.sql` now remaps `source_system/source_id` to the forked message and copies `artifact_index`; verified against the reported production conversation in a rollback transaction.
- `2026-07-14` — **Admin desktop target override for delegated desktop tools.** Agent turn assembly now reads the admin-only `adminPreferences.desktopTargetInstanceId` override and stamps `client.state["desktop-native"].target_instance_id` only when an admin pins a registered desktop engine; Auto omits the target. Covered first turns, continuations, resumes, manual/dev runs, `useRunAgent`, and the legacy `callApi` agent paths so aidream can route delegated `chat.tool_call` work to one exact matrx-local instance.
- `2026-07-14` — **`desktop-native` capability: web turns can delegate to the user's matrx-local desktop.** New capability provider (`client-capabilities/desktop-native.provider.ts`) declares `desktop-native` + `client.state["desktop-native"]` (`{platform, engine_version, instance_id, tunnel_state}` — normalizes stored `"windows"` → `"win32"` to match aidream's `sys.platform` filter) on every turn AND resume (both flow through `buildToolInjection`), gated on live presence: a non-deleted `app_instances` row with `last_seen` inside 11 min (engine heartbeats every 5 min), read direct from Supabase with a module TTL cache (`client-capabilities/desktop-presence.ts`) — **never declared while no desktop is online** (a dead declared executor delegates calls into a 30-day-ledger void). `surfaceDelegatedToolCall` routes desktop tools (`local_*`) by OWNERSHIP: presence re-checked, then the `delegated` ledger row is left alone for the matrx-local engine to execute + resume headlessly (posting an error would steal the call); no desktop → loud `unsupported_client_tool` as before. UI: `DesktopPresenceIndicator` (laptop + emerald dot, `useDesktopPresence` — same cache as the provider) in both smart-input toolbars. Verified live: turn advertised `load_desktop_tools`, loaded `local_file`/`local_documents`, suspended with the row in `status='delegated'`.
- `2026-07-16` — **Media variable library pick uses the real Cloud Files picker.** `MediaVariableInput` no longer opens the thin `useFilePicker`/`PickerShell` (no search/filter). "Choose from Files" now hosts the same `FilesResourcePicker` as Smart Agent Input → Stored Files (search, type filter, sort, list/grid, recents, folder tree). Selection still persists canonical `file_id` only. Library pick is available for all media kinds (image/audio/video/document); image/audio/video open with a matching initial filter. `FilesResourcePicker` gained optional `initialFilter` — Smart Agent Input path unchanged.
- `2026-07-16` — **Smart-input compute picker.** The former passive desktop-presence dot is now a compact upward picker in both toolbar layouts. It fetches the unified compute-target inventory, disables only when no live target (and no stale binding to clear) exists, lists only ready local PCs / non-expired running sandboxes, and separates an actual verified binding from available options. Bound local PCs use a blue monitor; bound sandboxes use a green box; unbound availability has no status dot. Selection updates the surface seed and current conversation together; the Connected row provides one-click disconnect.
- `2026-07-13` — **Mobile header conformance** (`core-route-headers` skill campaign): re-enabled `AgentHeaderMobile` in `AgentHeader.tsx` (was commented out by an unrelated commit — below `lg` the whole header row vanished; the Switch-mode bottom sheet is back), gave `AgentBuilderMobile`'s Build/Test tab bar `pt-[var(--shell-header-h)]` so it clears the glass header, and fixed `/agents/all` mobile: the tab pills row + content now use `var(--shell-header-h)`-based top clearance instead of floating into the header (content still scrolls behind the glass).
- `2026-07-13` — **Drift report rollup shows only drifted agents.** `agx_usage_report` verified correct live (per-agent severity counts + pending-alert join present); the report vs AgentsListHeader-tint discrepancy was the FE rollup listing EVERY in-scope agent and labeling the total "agents with drift". `usages.selectors.ts` now filters (`userRowHasDrift`/`adminRowHasDrift`: severity buckets, stale pins, open alert, others-redflags) in `makeSelectReportSorted` + `makeSelectReportTotals`.
- `2026-07-12` — **Agent edits to scope slots now PERSIST (aidream `ctx_item` writeback, cross-repo).** The half of the bug below that was a real data-loss hole: a scope-bound slot could be marked agent-editable with `persist:"auto"`, but aidream had **no `ctx_item` writeback handler**, so every agent edit was dropped with a debug line. Fixed end-to-end. **aidream:** new `@register_writeback("ctx_item")` (`context_writeback.py`) writes through the `public.set_context_value` RPC — the one sanctioned mutation path for `ctx_context_item_values` (append-only new version, previous `is_current` flipped, `source_type='ai_enriched'` + `authored_by` stamped, caller's scope-write access re-checked, `reference` cells refused, `{ok:false}` envelope raised so a refusal surfaces as `context_persist_failed` instead of vanishing). The cell is `(context_item_id, scope_id)` and **the agent author cannot know `scope_id`** (it depends on the caller's active scope set) — so `scope_binding_resolution` now attaches the resolved one to the slot's source via the rich per-key form (new `ContextSource.scope_id`), and screams + refuses rather than guess a scope (guessing would overwrite a different client's/case's value). **FE:** "Save to the source" is the default for an agent-editable scope slot. Tests: 4 new in `test_scope_binding_resolution.py` (12 total, passing). **Needs an aidream deploy to take effect in prod.**
- `2026-07-12` — **Context slots: "Agent access" replaces "Mutable" (and batch could never set it).** In the **scope batch importer** the Mutable checkbox was `disabled` for any context item already bound to a slot — so the only instant you could mark a slot agent-editable was during its first add; reopening the tool to change it was impossible. It now edits access on **existing** slots too (rows stay checked+disabled in the _Context slot_ column so a re-run can't double-add, while **Agent access stays live**; submit patches the stored slot in place — "Save changes"). New primitive **`components/context-slots-management/AgentEditAccessControl.tsx`** — `AgentEditAccessControl` (radio rows w/ lock + pencil), `AgentEditAccessToggle` (dense table), `AgentEditAccessBadge` (detail sheet), `decodeAgentEditAccess` / `applyAgentEditAccess` / `agentEditAccessChanged`; read-only stores **no** `mutable`/`persist` keys at all. **The word "mutable" is gone from the UI** (users read `Read-only` / `Agent can edit`, and "Where the agent's edits go" instead of persist=never/auto/client); `PersistSegment` deleted, `buildContextSlotFromItem` takes `access`, not `mutable`. Tests: `utils/__tests__/agent-edit-access.test.ts` (12).
- `2026-07-12` — **block send while voice is recording/transcribing.** Mid-recording submit dropped trailing audio and left the mic running. `AgentMicrophoneButton` now forwards `onRecordingStateChange`; `SmartAgentInputStacked` / `SingleRow` / `NewChatLandingInput` / `CompactAssistantInput` disable send + Enter until idle.
- `2026-07-11` — **Offering pinning (`settings.offering_id`).** A catalog listing is the exact call (model × endpoint × api = one `ai.offering` uuid); the server accepts an optional `offering_id` in agent settings — set = pin the call to exactly that offering, unset = server picks the preferred offering by priority ("Auto (preferred)"). FE contract: (1) **Shape** — `offering_id?: string` on the hand-written `FeLlmParams` (`types/agent-api-types.ts`; NOT in generated `LLMParams` yet — remove when it graduates). Stored in the `agent.definition.settings` blob like temperature; when unpinned the key is **deleted**, never null. (2) **UI** — the Service chips in `ModelListDropdown`'s detail card are pin toggles when `AgentModelConfiguration` (builder model row, Picker-Lab variants) passes `pinnedOfferingId`/`onOfferingPinChange`; leading "Auto" chip selected by default, pinned chip highlighted, click-again unpins. Persist path: `setAgentSettings` → dirty → `saveAgent`/`saveAgentField` — the same path as every other setting. (3) **Validation** — `offering_id` is a ROUTING key: registered in `buildRecognizedKeys` (`settings-management/validation/resolve-config.ts`, `ROUTING_KEYS`) and exempted from `unsupported-by-model` (like `model_id`) — it is never a model control, so it must never be flagged. (4) **Model swap clears stale pins** — an offering belongs to exactly ONE model, so `analyzeModelChange` (`settings-management/reconciliation/analyze.ts`) takes the new model's offering uuids (from `useModelCatalog` tiers, wired in `AgentSettingsCore`) and rows a stale pin in the reconciliation dialog with suggested action "clear" (null offerings = catalog not loaded = check skipped, never guess-clear). `ModelListDropdown.handleSelect` also clears (with `console.warn`) when the picker itself switches models.
- `2026-07-11` — **Type-drift purge (request wire + agent-service).** Killed hand-mirrored OpenAPI duplicates that had already drifted: `RequestRouting.channel` → `BackendChannel` (restored missing `local-runtime`); `UserOverrides` / `AssembledAgentStartRequest` / `AssembledConversationRequest` / `ClientToolResult` → OpenAPI aliases (`Partial` + `MessagePart` refine for assembled bodies); deleted camelCase fake `ClientToolResult` + unused hand-written `AgentStartRequest` / `ConversationContinueRequest` / `ToolResults*`; `agentService.types.ts` → pure OpenAPI aliases (14 schemas); `ApplyPolicy` derived from OpenAPI. Agents actionable duplicates **35 → 14**; repo **288 → 268**. Remaining agents hitlist + escalations in chat.
- `2026-07-08` — **Find Usages + Drift production activation (closes FOUND_DEFECTS D3).** Drift DMs now send from the dedicated "Matrx System" bot auth user (`system@aimatrx.com`, `71b55cc0-f333-462f-8176-f558f866ea5d`; profile row in `users.profiles`) instead of Arman's personal super-admin account — aidream `services/agent_usage/drift_dm.py` default swapped, with a SEPARATE `platform_operator_user_id()` (env `MATRX_PLATFORM_OPERATOR_USER_ID`, default Arman) as the recipient fallback for ownerless/code-declared usages so the bot never DMs itself. Also fixed the post-schema-reorg DM send failure (`communication.dm_*` `organization_id` NOT NULL; the ORM path has no `auth.uid()` for `_stamp_org_default` — now stamped explicitly via `ensure_personal_organization`). Weekly cron verified live in prod (`scheduler.sch_task` "Agent drift weekly scan", Mondays 13:00 UTC; real runs incl. a 28-DM send on 2026-06-15); `/agent-usage/{scan,report,registry}` driven over real prod HTTP. FE: removed the stale `deprecated: "Stub"` flags on the two Find Usages window registry entries (`windowRegistryMetadata.ts`) — the windows are fully implemented and verified rendering live drift data in-browser.
- `2026-07-08` — **Agent doc edits stream live (`context_delta`, closes D9).** aidream's `ctx_write` now emits a `context_delta` event (minimal splice or full content) per `ctx_patch`/`create` BEFORE `context_changed`; `process-stream` applies it synchronously via the new `applyAgentWorkingDocDelta` plain thunk (`instance-working-document.thunks.ts`; pure applier in `contextDelta.ts` + tests) — Scribe's `studio_document` docs route to the new `studioDocumentContentChanged` reducer in the transcript-studio slice. `context_changed` skips its mid-turn re-read while deltas apply cleanly; `context_persisted` semantics unchanged (always re-reads: latches `version`, heals divergence). `base_len` guard + BUG-B guard mean a delta never clobbers a diverged/mid-typing copy. `context_conflict` is now registered/typed in the generated stream events (was untyped on the wire).
- `2026-07-07` — **Full-prompt preview is now a WindowPanel, not a Dialog.** The dry-run preview moved off the blocking `PromptPreviewModal` (deleted) onto the new **`promptPreviewWindow`** overlay (`features/window-panels/windows/agents/PromptPreviewWindow.tsx`, opener `useOpenPromptPreviewWindow`) — a draggable / resizable / minimizable `WindowPanel` (720×720) so a creator can keep the assembled prompt (context + tools + the auto-injected `## Available Matrx Actions` guidance, all visible inline in the returned `system_prompt`) open beside their work. The preview UI extracted into a host-agnostic content component **`PromptPreviewContent`** (owns the `requestPromptPreview` dry-run + loading/error/refresh/copy); `service.ts`/`types.ts` unchanged. Trigger rewired in `RunControlsTabPanel.tsx` creator tab (`openPromptPreview({ conversationId })` + `onClose()`, mirroring the Memory button) — local `previewOpen` state gone. Registered the canonical 6 ways (overlay-ids / catalogue / metadata `ephemeral`+`fullscreen` / controller lazy-import + isOpen + data + gated block / opener).
- `2026-07-16` — **Dense context tree in composer + Smart Input chrome parity.** `ContextDocsMenu` / folded `+` now host the promoted Surface-A `ActiveContextTree` (cascade-up). Single-row + `/chat/new` landing pass `foldToolbarExtras` so docs/context/Enter/auto-clear live in `+` without a Layers button; stacked keeps Layers and does not re-host the tree in `+`.
- `2026-07-06` — **Feature-exposure semantics + compact context tree + canvas per-doc Share.** Documents & Context rows are FEATURE switches with fixed titles ("Working document" / "Scratchpad" — never a doc's given name; which docs are shared is a per-document choice in the canvas/workspace). ContextDocsMenu's Context section swapped `ActiveContextPanel sectionHeight=200` + `ActiveContextLayersPanel` for the new compact `ActiveContextTree` (see `features/scopes/FEATURE.md`). `WorkingDocumentPanel` gained `gateConversationId` + a scratch per-document "Share" switch (active doc → gate thunk; others → attach/detach); threaded from DocumentsWorkspace scratch tabs and the canvas scratch payload (`data.gateConversationId`, set by the rail pill).
- `2026-07-06` — **Per-conversation scratchpad gate + 4-row `+` menu + tiny composer chips.** Killed the "scratchpad force-attached to every chat" regression: `useScratchpadContextSync` is now gated on the conversation's own scratch `enabled` flag (default OFF), persisted as a scratch-kind association edge at the phantom deterministic id (`setConversationDocumentEnabledThunk` scratch branch; restored by `hydrateConversationDocumentsThunk`; `hydrateAttachedScratchpadsThunk` skips the gate id). New `setScratchpadGateThunk` = the one toggle (creates+activates a scratchpad when the pool is empty); wired into `ContextDocsMenu`'s ScratchRow (was toggle-less) and the rebuilt `PlusAttachMenu` — now FOUR rows: model (always) / Working doc + Scratchpad switches / active context (`ActiveScopeChips` + edit) / Advanced settings (deep link fixed `context`→`settings`). Composer chips compacted to icon + ONE word + tooltip + floating X: `RailPill` rewritten (doc pill click = canvas visibility toggle on the stable `wd:<scope>:<kind>` item — no longer opens the detail sheet; X = the switch; generic entry X = `removeContextEntry`); `ResourceAttachmentTile` gained `variant="compact"` (used by `SmartAgentResourceChips`; messages keep the two-line tile). Run-controls Document badge now reads the scratch GATE (was scratch-content).
- `2026-07-05` — **Builder read-only mode for shared agents.** View-only shares (`accessLevel` viewer/public/none from `agx_get_access_level`) now show amber inset chrome on the builder left column (`AgentBuilderReadOnlyFrame`: floating "View only / Create my copy" chip, no layout shift) once access resolves (`selectAgentIsReadOnly`, `selectAgentAccessResolved`). Save attempts open a duplicate prompt instead of hitting RLS (`useAgentSaveAction`); autosave skips view-only records. Shared duplicate flow extracted to `useAgentDuplicateFlow`. Overview tab's Category field replaced the native `<datalist>` (broken inside WindowPanel overflow) with `AgentCategoryPicker` — searchable Popover over existing agent categories + custom create, portaled correctly via `useNestedPortalContainer`. Scratch decoupled from conversations: user-owned pool in `workbench.working_documents` (kind `scratch`), slice entries at `sp:<docId>` scopes, ONE active pointer in `userPreferences.scratchpad.activeId`; new `scratchpad.thunks.ts` (hydrate-active/create/set-active/soft-delete/attach/detach/hydrate-attached) + `useScratchpadContextSync` publishes the active scratchpad (`user_scratchpad`) + attached extras (`user_scratchpad_<id8>`) into each conversation — **never when empty**. New global quick-access **`scratchpadPanel`** overlay (`ScratchpadQuickPanel`: switcher + shared panel) in `QuickActionsMenu`. Routing unified on `DOC_CONTEXT_KINDS`/`docKindForContextKey`: `ContextSlotDetailSheet` routes ALL doc keys to the editable workspace (the scratch pill's readonly JSON dump is dead), rail pills read the editor slice, `process-stream` gates via the map. Run-controls Document tab = `DocumentsWorkspace` (badge = working-enabled OR scratch-with-content); `WorkingDocumentControls` DELETED (rename/bind/link/status folded into `WorkingDocumentPanel`'s source-controls row on every mount); version history + view controls enabled for scratch. Legacy per-conversation scratch edges deleted in DB (rows joined the pool).
- `2026-07-05` — **Agent Memory manager — prominent importance score + sidebar sorting.** `importanceScore()`/`importanceTier()` (`features/agents/components/memory/types.ts`) convert the stored 0–1 float to a 0–10 integer shown everywhere: a colored chip on every sidebar row (replacing the old dot), at the top of the single-memory composer and footer (next to the editable slider), and on every entry in the "All memories" document view. `useAgentMemories()` gained `sortMode`/`setSortMode` (`importance | updated | created | alphabetical`, default `importance` desc) with a `sortedMemories` memo; `AgentMemorySidebar` exposes it via a small dropdown ("Sort by…") above the list. `totalCount` (raw, unsorted length) was split out from the now-sorted `memories` array for the "N memories saved" footer/header counts.
- `2026-07-05` — **Agent Memory manager UX pass — content-first, no teasing.** Reworked the memory window (`features/agents/components/memory/`) after feedback that it was "teasing" the user with tiny previews instead of giving content directly. `useAgentMemories()` now hoists a single **draft** (title/content/importance/scope) shared by body and footer, so Save/Delete live in the **footer slot** (compact Importance slider + Save button) instead of floating inside the body. `AgentMemoryEditor.tsx` collapsed `NewMemoryForm` + `MemoryDetailEditor` into one `MemoryComposer`: title is a borderless heading-style `<input>`, content is a single `<textarea flex-1 min-h-0>` that fills essentially all remaining height (no min-height cap, no inner scroll). `AgentMemoryAllView` is now one continuous scrollable document (title + full content + divider, repeated) — no cards, no clamping, no cutoff. `AgentMemorySidebar` rows are single-line and dense: importance dot + title (masked with a CSS `mask-image` gradient fade at the right edge instead of a hard `truncate`/ellipsis) + scope abbreviation, with a hover-revealed `MoreHorizontal` dropdown (shadcn `DropdownMenu`) for Delete — content preview text and the double-padding wrapper are gone. `WindowPanel`'s `bodyClassName` is `p-0` so each body view owns its own `p-3`, filling the full width/height instead of a centered `max-w-xl` column.
- `2026-07-05` — **Working-document rename no longer blanks the canvas editor.** Root cause: `workbench.working_documents` uses `REPLICA IDENTITY DEFAULT`, so a title-only UPDATE (rename in `DocumentsListRail`) emits a partial Supabase realtime payload with no `content` column; the shared realtime handler treated that as an empty doc and wiped Redux. Fix: apply content only when `row.content` is present, sync title separately, tighten the BUG-B guard (`?? ""`), and have rail rename dispatch `setWorkingDocTitle` + refresh the open tab label (documents are tracked by id + origin `(conversationId, kind)`, never by title).
- `2026-07-05` — **Agent Memory manager.** New "Memory" tab in the Chat Options run controls (Brain icon, next to Sandbox) opens the **`agentMemoryWindow`** overlay (`features/window-panels/windows/agents/AgentMemoryWindow.tsx`, opener `useOpenAgentMemoryWindow`) — a WindowPanel composition root over `chat.agent_memory` letting a user browse, edit, add, and soft-delete the semantic long-term memories the agent has saved about them. Sidebar (`AgentMemorySidebar`) lists every memory plus an "All memories" entry (renders `AgentMemoryAllView`, a read-first card list); selecting one opens `MemoryDetailEditor` (title/content/importance slider/scope badge, save + `ConfirmDialog`-gated delete); "New memory" opens `NewMemoryForm` (title/content/importance/scope picker). All state is hoisted once in `useAgentMemories()` at the window root per the composition-root pattern. New service `features/agents/components/memory/service/agent-memory.service.ts` targets the REAL `chat.agent_memory` columns (`content`, `memory_type`, `scope`, `key`, `importance`, `metadata` — `key` is a slugified-title internal id, `metadata.title` is the display title) — this is a **different, correct** service from the pre-existing `ui-first-tools/service/agent-memory.service.ts`, which is a known-defect stub still targeting the stale `cx_agent_memory` scratchpad shape (`conversation_id`/`value`) against the same table name; do not conflate the two. `organization_id`/`created_by`/`updated_by`/`updated_at` are DB-trigger-stamped, never set client-side (organization_id still passed via `ensureOrgId` to satisfy the generated Insert type). Registered the canonical 5 ways (overlay-ids / catalogue / metadata `ephemeral`+`drawer` / controller block / opener).
- `2026-07-05` — **Run controls are a real window, not a blocking popover.** The Smart Input gear/`+` (`RunControlsMenu`) no longer opens the fullscreen-able tabbed Popover on desktop — it opens the new **`runControlsWindow`** overlay (`features/window-panels/windows/agents/RunControlsWindow.tsx`, opener `useOpenRunControlsWindow`): a non-blocking WindowPanel (860×640, sidebar tab list, minimize-to-tray / maximize / drag / snap) so the chat stays interactive and the panel can always be moved out of the way. **Why:** `PopoverContent` sat at `z-[10001]`, one above the dialog layer (`z-[10000]`), so any dialog launched from inside the menu ("Preview full prompt") rendered BEHIND it — in fullscreen mode, completely buried with no exit. Popover z is now **equal** to the dialog layer (DOM portal order decides; dialog-from-popover and popover-from-dialog both stack correctly), and windows (z≈1000) always sit under dialogs, killing the class. Tab defs, badges, and all tab content moved to the shared core **`RunControlsTabPanel.tsx`** (`useRunControlsState` + `RunControlsTabPanel`) consumed by both presentations, so they cannot drift; non-fill height is one fixed `h-[32rem]` for every tab. Presentation routing in `RunControlsMenu`: **desktop `+` → `PlusAttachMenu`** (`smart-input/PlusAttachMenu.tsx`) — the lightweight anchored attach popover restored (`ResourcePickerMenu` with its own drill-in panels) plus ONE quick row: `QuickRunModelSelect` (compact genuine-delta model override, exported from `run-controls/RunModelPicker.tsx`, shown only when the instance owns an override layer), a Working-doc `Switch` (just on/off — `setConversationDocumentEnabledThunk`; details live elsewhere), and an All-options button → the window; **desktop gear → the window, opening on the Context tab** (`initialTab: "context"`; Attach never rides in the window — it lives in the `+` popover); **mobile → `TabbedBottomSheet`, NEVER a window** (unchanged, Attach still first for `+`); hosted inside a Dialog or popped-out window → the anchored tabbed popover fallback (an overlay window can't stack there). The always-mounted trigger keeps `useConversationDocumentsBridge`; `onOpenDebug` migrated to the `useOpenChatDebugWindow` opener. Registered the canonical 5 ways (overlay-ids / catalogue / metadata ephemeral+drawer / controller block / opener). Consumers (`InputActionButtons`, `SingleRowActionButtons`, `NewChatLandingInput`, cx-chat `ConversationInput`) unchanged.
- `2026-07-02` — **Agent sneak peek: Input + Output tabs.** `AgentSneakPeekContent` (modal + `AgentPeekWindow` + org `AgentPeek`) gains **Input** and **Output** tabs after Summary. Input shows each variable as a compact card (`{{name}}`, component type, required, help text, default) — read-only, builder-inspired. Output uses the same schema-key resolution as page-extraction (`importColumnsFromAgentSchema` / exported `findItemProperties`): free-form agents show **Text**; structured agents list field keys, types, descriptions, and required markers (+ schema name/strict when set). `findItemProperties` exported from `features/page-extraction/utils/columns.ts` for reuse.
- `2026-07-02` — **Generate an orchestrator agent (create-then-build; canonical agent selection; auto-syncing prompt).** Users without an orchestrator get one via a **create-then-build** flow — NOT a modal full of agents. `/agents/sets` → **Generate orchestrator** → `GenerateOrchestratorDialog` is a quick **name + accent** prompt; `useCreateOrchestrator` copies the **Agent Orchestrator** template (`b06689e3…`, via `agx_create_agent_from_template`) — which ships an empty `<available_agents>` placeholder — creates the (empty) set, and opens the builder, where agents are chosen on the **canonical rail** (search / Mine-Shared-All tabs / filters / peek / drag-drop). The builder detects a **template orchestrator** (`useOrchestratorPromptStatus`: prompt has the `<available_agents>` markers) and shows a **Sync agent listings** action — amber-pulsing when the listed agent ids differ from the current members — that runs the builtin **Agent Description Generator** (`62d56534…`) HEADLESSLY via `launchAgentExecution` (ephemeral, `displayMode:"background"`, `autoRun:true`, vars in `runtime.variables`) on a `{id,name,description,output_schema,variable_definitions}` dump of the **current members** → `<agent>` blocks → injects into `<available_agents>` (`syncOrchestratorPrompt`, marker-pre-checked before the LLM). **Every agent list uses the canonical `useAgentConsumer` + `<DesktopFilterPanel>` + filtered selectors** (the generate flow's multi-select modal and CreateSetDialog's alphabetical picker were replaced — an alphabetical dump of hundreds of owned+shared agents is useless); agents load ONCE via `useEnsureAgentsLoaded` (canonical `initializeChatAgents`), never `fetchAgentsList` (which refetches every mount). **Two adversarial-review rounds fixed 11 defects:** function replacer for injection (LLM `$` corrupts a string replace), robust `extractAgentBlocks` (fences/`<agents attr>`/stray-tag strip → idempotent re-sync), stale-closure false-success, orphan recovery on set-creation failure, partial-member/rename surfacing, sync marker pre-check, and a bad org-override I added then removed (the `_stamp_org_default` trigger already backfills the caller's personal org; overriding with a member's org would break on shared/foreign-org members). Module: `features/agents/agent-sets/orchestrator/**` + `hooks/{useEnsureAgentsLoaded,useOrchestratorPromptStatus}`. Docs: `docs/AGENT_SETS.md` + `docs/AGENT_SETS_ROADMAP.md` (future auto-add). Verified: routes 200; DB org/RLS/trigger confirmed. Lint + tsc clean.
- `2026-07-01` — **Multi-attach documents + every surface converges on `DocumentsWorkspace`.** The context drawer/slot-sheet peek (`WorkingDocumentBody`) now renders the same `DocumentsWorkspace` as the Canvas / `workingDocumentPanel` sidebar / floating window — its bespoke `WorkingDocumentTitleActions`/`WorkingDocumentFooter` deleted and unregistered (the workspace owns that chrome; a registered null Footer left an empty drawer footer bar via `hasFooter`). **Multi-attach shipped** (closes the 06-29 pending item): rail rows from other chats attach as persisted tabs — `openWorkspaceDocumentThunk` loads content into the doc's origin slice entry + writes the association edge, `detachWorkspaceDocumentThunk` removes it on tab close, `listAttachedDocumentTabsThunk` restores tabs on mount (`skipOrigin` protects the conversation's own bridge-hydrated drafts). Rail fix: `listRecentUserDocuments` + `rowToCxWorkingDocument` resolve origin from `metadata.origin_conversation_id` when the legacy `conversation_id` column is null, so new-model docs appear. STEP 3 legacy-column drop still DDL-blocked (Supabase MCP de-authed).
- `2026-07-01` — **Custom tools conform to the generated OpenAPI contract end-to-end — closed the final 7 `strictNullChecks` errors with zero casts.** The hand-rolled tool types were already aliased to `components["schemas"]` (the truth); this fixed the code that disagreed. `AgentToolsManager`'s param builder edits typed `ParamRow`s that carry the original `JsonSchemaProperty` as `base` — advanced schema fields (`enum`, `items`, nested `properties`, union types) now **round-trip through the form untouched** instead of being silently destroyed on save, and the type Select only overrides the original when the user actually changes it. `description` is always sent (required on the wire; Pydantic default `""`); the registry `ToolDetailPanel` reuses the generated `JsonSchemaProperty` instead of a lookalike. **Ingress validation replaced the `as unknown as` casts in `converters.ts`:** new `parse-custom-tools.ts` validates `custom_tools` / inline `tool_config` specs at read time; non-conforming entries are excluded + reported via the Error Inspector's new `data-shape` source (never passed through to 422 in Python). Live DB audited (definition/template/definition_version): all 4 stored tools conform, no backfill. `pnpm type-check` = 0 — Wave 5 done.
- `2026-07-01` — **Agent list picker: "System" tab → "Public"; guests and zero-agent users default to Public.** User-facing builtin ownership label centralized in `features/agents/constants/agent-list-labels.ts` (`AGENT_PUBLIC_TAB_LABEL`). `useAgentListCore` preloads agents and switches the default tab from Mine → Public when `selectUserId` is null or owned-agent count is 0 after load.
- `2026-06-30` — **Agent Sets UI pass + runtime roadmap.** (1) Library rail now reuses the **canonical agent filter** (`useAgentConsumer` + `makeSelectFilteredOwned/SharedAgents` + `<DesktopFilterPanel>`) — Mine/Shared/All tabs + category/tag/sort/search — instead of a hand-rolled owned+shared mix. (2) **Auto-arrange** Panel on the canvas: Hierarchy (dagre tree), Radial, Grid — snaps + persists positions + fitView. (3) Member **role title moved to the association `label` column** (only `gap`+`pos` stay in metadata). (4) `AgentRoleCard` redesigned — redundant glyph dropped, name gets the full first row, hover actions float over a backdrop, description expands in place. (5) Agent **peek** reused everywhere via a self-contained `AgentPeekButton` (wraps canonical `AgentSneakPeekModal`) on rail rows + member cards + inspector. (6) `MemberInspector` shows the agent's **I/O** — lazy-loads the full def (`fetchFullAgent`, gated on `selectAgentReadyForBuilder`) to render input variables + output type. New handoff doc [`docs/AGENT_SETS_ROADMAP.md`](./docs/AGENT_SETS_ROADMAP.md) (prioritized path to full runtime delegation: P0 server-side set reader → member-as-tool supervisor → pipelines → hardening → polish). Lint + tsc clean; routes serve 200.
- `2026-06-30` — **Agent Sets (Orchestrators): agent→agent sets on `platform.associations`, no new table, drag-drop builder.** An agent can now preside over a **set** of member agents — each filling a gap in a bigger picture — modeled entirely on the canonical association edge. A set = an orchestrator agent + edges: a `matrx_set` **self-edge** (marks the orchestrator; holds set config `accent`/`tagline`/layout in `metadata`; lets an empty set persist) and `member` edges (orchestrator→member, ordered by `position`, with authored `roleTitle`/`gap` in `metadata`). **Direction fixed: orchestrator=source, member=target** (matches `assoc_set_targets` + the source-org auth gate in `assoc_add`). Writes reuse the chokepoint (`associationsService`) — **no new mutation path**; the only DB addition is one read, `agent_set_list()` (`migrations/agent_sets_list_rpc.sql`, applied+ledgered), enumerating the caller's sets + counts. `agent` added to curated `ASSOCIATION_TARGET_TYPES`. **Surfaces:** `/agents/sets` (savior list), `/agents/sets/[orchestratorId]` (builder — library rail + **React Flow** hub-and-spoke canvas with drag-to-add/reposition + a `@dnd-kit` sortable Grid + member inspector), an **Add to set** action on every agent card, and a **Sets** entry on `/agents/all`. React Flow lives ONLY in `SetBuilderCanvasImpl` behind a `next/dynamic({ ssr:false })` wrapper, guarded by `reactFlowStaticImportBan` in `eslint.config.mjs`. New module `features/agents/agent-sets/**` + `agentSets` slice. Phase 1 = structure + UI; runtime delegation (orchestrator invoking members) is designed-for via `role`/`metadata`, deferred to a later aidream contract. Type-check clean. See [`docs/AGENT_SETS.md`](./docs/AGENT_SETS.md).
- `2026-06-29` — **Working-document end-to-end rebuild: materialize-on-write, M2M associations, versioned, auto-named, one UI — the data-loss class is extinct.** The week-ago regression (an unbound editable doc was offered to the agent as `persist:"client"`, the agent's edit was the client's job to save, and the client dropped it — silently) is structurally impossible now. **DB (STEP 1/1b/2/4, applied+ledgered):** `cx_working_documents`→ a first-class, versioned, owner-scoped entity `working_document` **moved chat→workbench**; `is_versioned=true` + `_history` trigger; entity-variant RLS (was the conversation-deferring component variant); `metadata`+`deleted_at` added; M2M links migrated from the junction into **`platform.associations`** (`working_document`→`conversation`, `{enabled,doc_kind}`); the 96 empty rows (88%) deleted. **aidream (`285eb3f18`):** `_writeback_working_document` is a **materialize-on-write UPSERT** (creates the row at the client-reserved id on the agent's first write) + version-aware conflict signal (`ContextSource.materialize`/`base_version`, `ContextConflictData`). **FE (`45026ba17`, `9f559a8ef`):** `persist:"client"` BANNED; enabling reserves a **deterministic** id (`uuidv5(conversationId:kind)` — multi-tab-safe), the row is created on first content (no empty records); the slice stays current every keystroke and publishes to instanceContext **immediately** (agent sees the exact latest text); the user's commit is **version-aware** (`commitWorkingDocumentContent`) and on a concurrent agent edit raises a **conflict** with a split diff + Keep-mine/Use-agent's (nothing silently lost — both versions in history); realtime is a **ref-counted shared channel**; **auto-naming** via notes' `generateLabelFromContent`; and `WorkingDocumentBody` now **delegates** to the single `WorkingDocumentPanel` (no forked editor). Three adversarial review passes; every real finding fixed. Recovered the user's lost 10,030-char doc. **PENDING:** aidream redeploy (the deployed writeback still points at the moved table); legacy-column drop (`conversation_id`/`user_id`) + junction graveyard (STEP 3, unused litter — see FOUND_DEFECTS); multi-attach beyond the primary slot. [[project_working_document_rebuild]]
- `2026-06-28` — **Working-doc provisioning is now race-safe — killed the `23505` storm on `chat.conversation_documents`.** Using a working document fired a `duplicate key … cx_conversation_documents_conversation_id_kind_key` (409, ×2) the moment a conversation provisioned its first doc. Root cause: the recent DB change dropped `chat.working_documents`' 1:1 `UNIQUE(conversation_id, kind)` (to enable cross-conversation doc sharing), so the **junction** became the _only_ race arbiter — and `getOrCreateConversationDocument` was a read-then-insert with no dedup. The provision effect (`useWorkingDocumentContextSync`) is mounted in 2-3 places at once (always-on SmartInput bridge + every open editor) and stays armed (`binding.kind === "none"`) until the first call resolves, so several calls ran concurrently: each inserted a candidate `working_documents` row and raced the junction insert — one won, the rest threw `23505` (loud error-inspector noise) and their candidates were created-then-deleted (orphan churn). Fix (single chokepoint, `cx-working-document.service.ts`): (1) **module-scoped in-flight dedup** keyed by `${conversationId}:${kind}` collapses the concurrent storm to one clean provision (the canonical duplication fix-shape, [[project_file_fetch_duplication_pattern]]); cleared on settle (in-flight latch, not a result cache). (2) the junction claim is now an **idempotent upsert** (`onConflict: "conversation_id,kind", ignoreDuplicates: true`) so any residual cross-tab race is a silent no-op instead of a `23505`. End state is unchanged (one doc, one junction); the difference is no failed inserts and no orphan churn. No migration, no type change. Verified: live DB shows clean 1-doc/1-junction state with zero orphan junctions.
- `2026-06-27` — **Builtin agents are globally readable + structurally pinned to the Matrx System org.** Two-part fix after the canonical-RLS changeover blanked every system-agent detail page (admin AND user-facing `/agents/[id]`): the direct RLS read (`getAgent`) of a `builtin` returned 0 rows because system agents were `visibility=internal` owned by the **Matrx System** org, which `has_access` granted only to that org's members. (1) **Access model** — added a generic _platform-global tier_ to `iam.has_access` keyed on a new `public.system_orgs.global_readable` flag (set on the Matrx System tenant): any authenticated user gets `viewer` on a global tenant's `internal`+ content, super-admins get full manage; anonymous still denied; no app-code change (direct reads became correct). `migrations/iam_has_access_platform_global_tenant.sql`; doctrine in `docs/db_rebuild/db-canonical-access-model.md` §4. (2) **Creation invariant** — "global = builtin = owned by the System org" is now enforced at the DB edge by `agent._enforce_builtin_system_org` (BEFORE INSERT/UPDATE on `agent.definition`): every `agent_type='builtin'` row is forced to `organization_id = system_org_id('system')` — silent on the null→system fill, **loud WARNING** when it corrects a non-null wrong org (a creation path leaked one), hard EXCEPTION if the System org isn't registered. This made the whole class extinct: all three creation paths (`createSystemAgentFromSeed`, the `convert-from-agent` route, `agx_duplicate_agent/_version` `p_as_system`) and the convert route's snapshot-spread UPDATE all wrote `organization_id: null` (the dead "global = NULL" model) and nothing backfilled it, so newly-created system agents were born invisible. The three TS paths were de-misled (stop writing `organization_id`, comments corrected); 55 builtins stranded on a user's workspace org were re-homed (all 137 now on the System org). `migrations/agent_builtin_system_org_guard.sql`. Verified live: regular user read=true/edit=false, super-admin read+edit=true, anon=false, and the guard snaps a wrong org back. [[project_platform_global_tier]]
- `2026-06-27` — **Working-doc provisioning gated on server-confirmed conversation + real FK.** `ensureConversationDocumentThunk` is now a no-op while the conversation is `cacheOnly` (`selectIsCacheOnly`); it's the single provisioning chokepoint (reactive context-sync effect, enable/disable toggle, note bind/unbind all funnel through it), so a doc toggled on a brand-new chat no longer inserts a `chat.working_documents` row before the conversation persists. DB: `chat.working_documents.conversation_id` + `chat.conversation_documents.conversation_id` gained real FKs → `chat.conversation` (ON DELETE CASCADE, validated) after cleaning 317 leaked orphan rows; future orphan inserts now fail loudly (23503). `cx-working-document.service.ts` surfaces that 23503 explicitly. Table refs corrected (`chat.working_documents` / `chat.conversation_documents`, not `public.cx_*`).
- `2026-06-26` — **Matrx Actions = multi-select repertoire + runtime-only guidance + full-prompt preview.** (1) **Actions list** — an agent lists MANY actions (no cap; canonical `verb:noun`, built-in directives, AND custom free-form types) in `matrx_actions.actions`, with the apply policy in `matrx_actions.apply_policy` (Matrx Actions tab). (2) **Runtime guidance (additive, never edits the authored prompt)** — the action list rides the request (`matrx_actions`); the server's `apply_matrx_actions_guidance` (`aidream/api/utils/matrx_actions.py`, called from `prepare_agent_run` + `run_chat_request`) sets `SystemInstruction.action_types`, and `matrx_ai/instructions/core.py` `_actions_guidance` renders an "## Available Matrx Actions" section for the full list (dropped for non-chat models). The authored system prompt is NEVER mutated — replaced the earlier (harmful) physical-injection + single-action `output_schema` approach. (3) **Full-prompt preview** — run-controls → creator tab → **Preview full prompt** (`features/agents/prompt-preview/`): reuses `assembleManualRequest` + a backend `dry_run` on `/ai/manual` & `/ai/agents/{id}` (`api/utils/preview.serialize_preview`) to run the FULL pre-LLM assembly against an **ephemeral** conversation (`conversation_id:null` + `is_new:false` → `skip_persistence`) and return the rendered system prompt (incl. the actions guidance) + messages + tools + params as JSON — no model call, nothing saved. KNOWN: separate save-on-run path persists dirty builder edits (the draft/`config_overrides` + preview paths do NOT). (Preview moved from a Dialog to the `promptPreviewWindow` WindowPanel — see 2026-07-07.)
- `2026-06-26` — **Agent config storage normalization + Matrx Actions rebrand (both tables).** `settings` is now strictly server-consumed model params; two dedicated jsonb columns added to `agx_agent` AND `agx_version`: **`matrx_actions`** (full rebrand of the retired `settings["output_apply"]` — column + FE `matrxActions` field/selector/action + the new **Matrx Actions** settings tab + aidream `AgentConfig.matrx_actions` / `ctx.metadata["matrx_actions"]`; the `output_apply` name exists nowhere) and **`ui_gates`** (FE-only model-gated UI flags — `file_urls`/`image_urls`/`youtube_videos`/`tools`-bool moved out of `settings`, never sent to the server; one canonical source `lib/redux/slices/agent-settings/ui-gates.ts` replaced 6 duplicate `UI_CAPABILITY_KEYS` sets; flattened into the chat instance via `buildInstanceBaseSettings`; edited via the new **Input capabilities** editor). Backfilled both tables (zero residue); scrubbed `model_id`/`tools`-bool/`internal_tools` from `settings`; converter sanitizer prevents re-pollution. Repaired pre-existing version drift in the same sweep — `tool_config`/`skill_config`/`mcp_servers` now fully versioned; the snapshot triggers + all 5 `agx_*` RPCs project the new + previously-missing columns. Tool-support is a **model capability** the server already enforces (drops tools for non-supporting models); FE gating wired via `supportsTools(normalizedControls)` in the builder + chat tool pickers. Migrations: `agx_config_normalization_matrx_actions_ui_gates.sql` + `_backfill.sql`. Storage doctrine: Layer 1 above.
- `2026-06-26` — **Matrx Actions tab made action-first + tool capability backfilled + dispatcher seam renamed.** The tab now leads with the **action registry** (built-ins + live action catalog) as a first-class always-visible picker — choosing an action wires the agent's `output_schema` to emit that `output_directive` envelope (`actionSchema.ts`, guarded so a custom schema is never clobbered) and shows it by name; `matrx_actions` is policy-only (Default/Auto/Ask/Off). `ai_model.controls.tools` backfilled across all models so the FE tool-support gate is **live** (chat/reasoning = allowed; image/video/TTS/STT/GLiNER/Llama = denied; default-true so new models stay tool-capable) — `tools` is a model capability, dropped from the per-agent `ui_gates` editor (`UI_GATE_EDITABLE_KEYS`) + excluded from the settings catch-all. matrx-ai host seam renamed `output_apply_dispatcher` → `matrx_actions_dispatcher` (`configure()` kwarg + `get_matrx_actions_dispatcher()`), completing the rebrand — `output_apply` exists nowhere in live code.
- `2026-06-26` — **Explicit "Refresh Server Cache" in agent options menu.** Builder (and all agent surfaces using `AgentOptionsMenu`) can now manually call `POST /ai/agents/{id}/invalidate-cache` via `invalidateAgentCache` thunk; success requires the server's `{ cleared: true }` confirmation and surfaces a toast. Shared request helper `agent-cache-bust-request.ts` also powers the automatic post-save middleware. The whole `(admin)` group now renders the shared `AppShell` (`features/shell/components/AppShell.tsx`) — the same sidebar+header `(core)` uses — instead of the legacy `ResponsiveLayout`, so the admin agent routes get the full nav and a real `#shell-header-center`. Consequently the 10 admin agent detail pages (`view/build/run/shortcuts[/new,/batch,/:id]/apps/v/widgets`) were converted from an inline `AgentHeader` bar to the canonical `<PageHeader><AgentHeader basePath=/administration/agents/system-agents/agents/></PageHeader>` portal — identical to core and to the admin Surfaces tab, so the agent header no longer jumps position between sub-tabs. Layout/shell details: CLAUDE.md "Route groups" + `styles/shell.css` admin `.shell-main` rule.
- `2026-06-24` — **Surfaces resizable columns fix.** Panel toggles were incorrectly placed in an in-body `SurfacesPanelToggleBar`, breaking the mac-mail/tasks full-height panel model (double vertical offset + panels not filling the viewport). Restored: toggles in shell header via `SurfacesAgentHeaderControls`, `h-full overflow-hidden` body wrapper, full-height `ClientGroup`. See `features/surfaces/FEATURE.md`.
- `2026-06-24` — **Surfaces tab gets the common agent header.** `/agents/[id]/surfaces` (and the admin twin) was the one sub-route missing the shared `AgentHeader` — it rendered only surface-specific panel toggles, so the agent selector + mode tabs (Build/Run/Surfaces/…) + save/options were unreachable from it. `SurfacesAdminShell` now injects header chrome via `PageSpecificHeader` and takes a `basePath` prop for the mode-tab hrefs.
- `2026-06-23` — **Sent attachment chips: wire-format media normalization + composer hide-on-submit.** Optimistic user messages store API wire blocks (`{ type: "image", file_id, … }`); `normalizeContentBlocks` only mapped cx_message `{ type: "media", kind: "image" }`, so images vanished from user bubbles. Added wire→media coercion in `redux/execution-system/utils/normalize-content-blocks.ts`; `AgentUserMessage#extractBlockFileId` reads `fileId` from `image_output.data`; `SmartAgentResourceChips` hides during `submissionPhase === "pending"` so attachments appear to move with the message (resources stay in Redux until stream success for retry). Chat doc: `components/chat/FEATURE.md`.
- `2026-06-23` — **System-agents route parity (broken Surfaces tab fix).** The admin system-agents detail family (`/administration/agents/system-agents/agents/[id]/*`) was missing two routes its own shared header/buttons linked to, so navigation 404'd: the **Surfaces** mode tab (`AgentModeController` MODES) and the **Batch** button in `AgentShortcutsPanel`. Added `…/[id]/surfaces` (page + layout, mirroring `(core)/agents/[id]/surfaces` with an admin-scoped `backHref`) and `…/[id]/shortcuts/batch`. The surfaces shell needed a real fix to render under the admin layout: `SurfacesAdminShell` injected its header via the shell-only `PageHeader` (target `#shell-header-center`), which the `(admin)` new-layout group never renders — swapped to `PageSpecificHeader` (shell host → `#page-specific-header-content` fallback) so the same shell works under both `(core)` and `(admin)`. `BatchShortcutsEditor` gained an optional `basePath` (default `/agents`) so its in-component nav stays in whichever shell mounted it. Every `${basePath}/${agentId}/…` link the shared agent components emit (view/build/run/shortcuts[/new,/batch,/:id]/apps/surfaces/versions/widgets) now resolves under both families; the legacy `shortcuts/old/*` routes are intentionally NOT duplicated (no button links them). Type-check clean; all eight admin sub-routes verified 200 live with matching titles.
- `2026-06-23` — **Agent Builder variable editor upgrades.** The Edit Variable modal now wraps long option/default-value text instead of letting compact selects/buttons force the dialog wider; Help Text uses `ProTextarea` on `matrx-user/agent-builder` with live full-agent scope plus focused-variable fields (`variable_name`, `variable_help_text`, `variable_json`, editable target metadata); and static option lists can be converted into a new user picklist in-place, seeding each option as both label and injected text, linking the variable to the picklist, and exposing an edit link to `/lists/{id}`.
- `2026-06-23` — **Agent Builder + Chat wired to their agent surfaces (surface-pro-rollout).** Agent Builder: `SystemMessage` / `MessageItem` instruction+message editors → `ProTextarea` (`surfaceName: "matrx-user/agent-builder"` + a plain `getApplicationScope` via the existing `buildAgentBuilderContextData`; object refs + `useLayoutEffect` init), and the existing `UnifiedAgentContextMenu` mounts now pass `getApplicationScope` / `contextData`, so the "…" menu lists the surface's bound agents and runs them with full scope. Chat: the `/chat/new` composer (`NewChatLandingInput`) gained the right-click agent menu for `matrx-user/chat` (`components/chat/agent-context/buildChatContextData`); the shared `AgentTextarea` (`/chat/[id]`) and the transcript `MarkdownContextMenuProvider` still need `surfaceName` threaded — tracked in `components/chat/FEATURE.md`.
- `2026-06-23` — **`CreateWithAiTabs` pane styling hooks.** Added optional outer/header/body/manual/AI/extra pane class hooks while preserving the lazy-mounted, retained-tab behavior. `ProjectCreatePanel` uses them to keep Manual/Paste JSON padded and footer-stable while allowing the Use AI runner to render edge-to-edge inside the create-project window.
- `2026-06-22` — **Smart Input `+` menu: mobile bottom sheet + desktop tab bar fix.** `RunControlsMenu` now uses the reusable `TabbedBottomSheet` (`components/official/bottom-sheet/TabbedBottomSheet.tsx`) on mobile — iOS Settings–style first-level list (Attach, Context, Document, …) with drill-in to each tab's panel via `BottomSheet` + back chevron; desktop keeps the tabbed Popover but hides the horizontal scrollbar (`scrollbar-width:none`) and stops stretching tabs with `flex-1` (the visible "slider" was the scrollbar eating half the tab row). Nested popovers fixed for mobile: `DocumentLinkPicker` → `BottomSheet`; adjacent `ContextDocsMenu` → `BottomSheet`. Internal panels reviewed: `ResourcePickerMenu` / `NotePickerPopover` already drill-in; `ActiveContextPanel` / `RunToolPicker` / `WorkingDocumentControls` work inside the sheet body.
- `2026-06-22` — **Chat scratchpad + kinded/opt-in documents + compact docs-&-context menu.** Generalised the working-document primitive into a kinded, multi-conversation, opt-in one that also backs a new user **scratchpad** (agent read-only). **DB** (`migrations/cx_conversation_documents.sql`, applied + ledgered): `cx_working_documents` gained `kind` (`working`/`scratch`), dropped `UNIQUE(conversation_id)` (now origin, not identity); new junction `cx_conversation_documents` `(conversation_id, kind) → document_id` + persisted `enabled` (owner RLS, realtime). Backfilled 407 rows (content-bearing stayed on, empty became opt-in OFF). **Slice** keyed by `(conversationId, kind)` via `workingDocKey` (`byConversationId` → `byKey`); selectors/actions take optional `kind` (default `working`); **opt-in defaults flipped to OFF**. New thunks: `hydrateConversationDocumentsThunk` (restore persisted opt-in/link on mount), `setConversationDocumentEnabledThunk` (persisted toggle), `ensureConversationDocumentThunk` (race-safe provision via junction unique), `linkConversationDocumentThunk` (cross-conversation A2 link); existing thunks gained `kind`. **Service** generalised for kind + junction (`getOrCreateConversationDocument`, `linkConversationToDocument`, `setConversationDocumentEnabled`, `listUserDocuments`, `getCxWorkingDocumentById`); **realtime now filters by `document_id`** so linked conversations get updates. **Context value:** new `buildUserScratchpadContextValue` (`user_scratchpad`, `mutable:false`, no writeback source). **Hooks** parameterised by `kind`; new `useConversationDocumentsBridge` (hydrate + both context syncs, mounted in `RunControlsMenu`). **UI:** new `ContextDocsMenu` toolbar icon (in `InputActionButtons`, next to `+`; `InputButton` styling + single primary dot when working/scratch/active-context on) with Working Document + My Scratchpad switches, per-doc `DocumentLinkPicker`, and the canonical `ActiveContextPanel`; `WorkingDocumentControls`/`Panel` take `kind` (scratch labelled read-only, no note binding); **removed the intrusive composer chips** (working-doc + context pills) from `SmartAgentResourceChips`. The `+` menu's Document/Context tabs remain (intentional duplicate). Type-check + lint clean.
- `2026-06-22` — **`CreateWithAiTabs` "Use AI" tab no longer renders blank in Dialog/Drawer chrome.** The agent runner (`AgentRunner`) is built entirely from absolutely-positioned children, so it has zero intrinsic height and requires a definite-height ancestor. `CreateWithAiTabs` mounted it inside an `h-full` pane whose `flex-1 min-h-[460px]` body had no _computed_ `height` in auto-height chrome — Dialog/Drawer (`ProjectFormSheet` → `CreateProjectModal`, used by research's `ResearchInitForm`, tasks, etc.) only set `max-h`, and CSS percentage heights ignore `min-height`, so `h-full` collapsed to 0 and the agent showed nothing (Manual tab worked because it's normal-flow content). It only worked in `CreateProjectWindow` because the `WindowPanel` supplies a definite pixel height. Fix: the tab body is now a positioning context (`relative`) and the AI pane fills it via `absolute inset-0` (which honors the rendered `min-h-[460px]` box) instead of `h-full` — so the agent is visible in modal/drawer chrome too, not just the window. Manual/extra tabs stay in flow. `features/agents/components/smart/CreateWithAiTabs.tsx`.
- `2026-06-21` — **Output-schema key order is now preserved (`jsonb` → `json`).** Root cause of "my schema keys keep getting reordered on save" was confirmed live: `output_schema` was stored as `jsonb`, which canonicalizes object keys by (length, then bytewise) and so destroyed the authored `properties` order — fatal for structured outputs, where the model emits fields in schema order (e.g. `DATE` must lead a clinical-timeline row). The write path is innocent: editor (`JSON5.parse` → object), supabase-js, and PostgREST all preserve insertion order — proven by a live round-trip where a `json` column kept order and a `jsonb` column reordered. Fix is purely the column type: `agx_agent` / `agx_version` / `agx_agent_templates`.`output_schema` flipped to `json` (verbatim text storage; no indexes/constraints/views/jsonb-operators depend on it), and `agx_get_version_snapshot` updated to return `output_schema json` so snapshots don't re-`jsonb` the value. No app-code or TS-type change (both `json`/`jsonb` map to `Json`). Existing rows keep their already-canonicalized order (unrecoverable) — re-saving a schema fixes it going forward. `migrations/agx_output_schema_preserve_key_order.sql`. **Required follow-up (`migrations/output_schema_json_comparison_fix.sql`):** `json` has no `=` operator, so the version-snapshot triggers' change-detection guard (`OLD.output_schema IS NOT DISTINCT FROM NEW.output_schema`) raised `operator does not exist: json = json` and blocked EVERY save on `agx_agent` / `prompts` / `prompt_builtins` / `tool_def`; fixed by casting just that comparison to `::text` in all four `trg_*_snapshot_version` triggers (a key-reorder now correctly counts as a change worth versioning). NOTE: the sibling `prompts` / `prompt_builtins` / `tool_def` / `page_extraction_jobs` `output_schema` columns still carry the same latent `jsonb` reorder bug — left untouched (separate features/consumers) pending an explicit decision.
- `2026-06-20` — **Agents gallery drift callout → header indicator.** Removed the dismissible `AgentDriftBanner` from `/agents/all`; weekly-scan drift now tints the existing `AgentsListHeader` "Drift report" link (red = breaking, amber = silent break / stale pin, neutral when info-only or no alerts) with a compact severity badge + agent count.
- `2026-06-20` — **Unified Matrx References (frontend alignment).** Collapsed every internal pointer onto the backend's single canonical structure: a reference item is now PURE FLAT identity ids + optional display hints (no `purpose`/`slot`/`ref`/`display`), across the 7-type taxonomy (`picklist`/`picklist_group`/`picklist_item`/`table`/`table_column`/`table_row`/`table_cell`). **Bookmarks ARE reference items** — the four hand-authored `input_table`/`input_list` bookmark type copies (`message-types.ts`, `user-lists/types.ts`, `prompts/types/data-sources.ts`, `tableReferences.ts`) now re-export / align to the generated wire types; `features/matrx-envelope/bookmarkToReference.ts` maps them onto the taxonomy and `input_table`/`input_list` render as **live reference chips** in the context-item drawer (`context-items/bodies/BookmarkReferenceBody.tsx`, replacing `GenericBody`). HARD CUT: new writes emit FLAT fences; legacy shapes route through the new loud `features/matrx-envelope/legacyTranslate.ts` (one `console.error` per legacy value) — no silent dual-read. `item_label` → `label`. See [`features/matrx-envelope/FEATURE.md`](../matrx-envelope/FEATURE.md); D10 updated.
- `2026-06-19` — **Picklist-bound variables now author the `matrx` reference fence (envelope migration, deliverable b).** `PicklistVariableInput` emits the canonical ` ```matrx ` `kind:"reference"`, `type:"picklist_item"` fence (via the new `features/matrx-envelope/referenceFence.ts#buildPicklistItemFence`) instead of the legacy `picklist_ref` envelope. The persisted value is a fence STRING — single = one item; multi = N items + any "Other" free-text lines after the closing fence — so `componentToValueType` routes picklist to `string`/`value_text`. Round-trip + display read through the dual-read `readPicklistSelection`, which still parses already-saved `picklist_ref` objects/arrays, so nothing breaks during the transition; `variableValueToDisplay` updated accordingly. `PicklistRefEnvelope` / `isPicklistRef` ([agent-definition.types.ts](features/agents/types/agent-definition.types.ts)) are now `@deprecated` read-only back-compat. The FE-controlled direct/override `variables` path resolves end-to-end via aidream `substitute.py#stage_reference_fences`; the **bound scope-cell path is backend-gated** (aidream must resolve a fence-valued bound cell) — tracked in [`FOUND_DEFECTS.md` D10](../../FOUND_DEFECTS.md). Render/detection side (the ` ```matrx ` block + chips) shipped 2026-06-19 — see [`features/matrx-envelope/FEATURE.md`](../matrx-envelope/FEATURE.md).
- `2026-06-19` — **Active context layers are now drawer items behind one "Context" chip + run-controls fullscreen.** The scope-selection system (org / scope(s) / project / task) collapses into a SINGLE differentiated "Context" chip in the Smart Input strip (`SmartAgentResourceChips`); clicking it opens the shared `ContextItemDrawer` navigable across every layer — same group-of-items behavior as the working doc / resources. New normalizer hook `context-items/useActiveContextLayerItems.ts` (read-only mirror of `appContextSlice`, never writes), new registry block types `ctx_org` / `ctx_scope` / `ctx_project` / `ctx_task` with bodies `context-items/bodies/ContextLayerBodies.tsx` (scope → items+values like `ScopeDetailView`; org/project → identity card + manage link; task reuses `TaskBody`/native `TaskEditor`); `ContextItemRefs` gained `orgId` / `scopeId`. The inline `ActiveContextLayersPanel` stays in the Context tab. `RunControlsMenu` gained a **maximize toggle** that expands the whole tabbed control surface to a near-fullscreen sheet (reuses the same body; panel switches `h-96` → `flex-1`). **Both special chips are now removable** (`ResourceAttachmentTile onRemove`): the working-doc X dispatches `setWorkingDocEnabled(false)` — and because that's an explicit `false` on the instance, the default-on fallback never re-adds it for the session; the Context X dispatches `clearContext()` (same as `ClearContextButton`). Nothing auto-restores either.
- `2026-06-19` — **Working document: default-on, user-named, safe note binding, correct unbind.** (1) Enabled by default (`selectWorkingDocEnabled` / slice `ensureEntry` default `true`; toggle-off respected) — no longer a manual add. (2) User-named: Document tab adds a name field; title defaults to `""` (DB default changed `'Working document'` → `''`, `migrations/cx_working_documents.sql`), persisted via new `updateCxWorkingDocumentTitle`, display falls back only when empty. (3) Binding a note over existing content prompts **Append / Replace / Cancel** (`bindWorkingDocumentToNoteThunk({mode})`; append merges note-on-top + current-below and saves back to the note). (4) New `unbindWorkingDocumentThunk` reverts content **and title** to the conversation's own `cx_working_documents` row — fixes the state bug where unbinding kept the note's name/content. Also fixed the drawer/editor full-height layout (`MatrxDynamicPanelHost` content slot needs `h-full`, not `flex-1`).
- `2026-06-22` — Context slot chips: registry of known ambient keys (`user`, `client`, `route_brief`, `conversation`) renders human-readable previews + detail panels instead of raw JSON; detail drawer reads frozen `model_context` snapshot values (ambient keys are not in live `instanceContext`).
- `2026-06-22` — Context slot chips on historical user messages no longer show misleading `0 chars` previews for deferred items (`working_document`, `user_scratchpad`, etc.). Server `size_hint` means "zero inlined at send time", not document length; previews now resolve char count from live instance context and ignore size-hint strings (`contextSlotPreview.ts`, `AgentUserMessage`, `ContextSlotChip*`).
- `2026-06-19` — **Matrx Envelope FE: `matrx` fence rendering, directive receipts, schema-proposal "Apply to an agent".** Canonical client mirror `features/matrx-envelope/envelope.ts` (items-based `{matrx_version,kind,type,items}`: `isMatrxEnvelope`, receipt events + `isDirectiveApplyEvent`, `buildEnvelopeOutputSchema`); `applyDirectives.ts` migrated to it (`DIRECTIVE_ITEM_SCHEMAS` + `buildDirectiveOutputSchema`). Rendering: `"matrx"` fence registered in `content-splitter-v2.ts` → `MatrxEnvelopeBlock` (reference items → chips, fail-safe); `directive_apply.*` stream events → `toast` in `process-stream.ts`. **Schema-proposal flow:** a json block shaped `{name, schema}` is detected (`JSON_BLOCK_PATTERNS.schema_proposal`) → `SchemaProposalBlock` ("Apply to an agent" button) → `ApplySchemaDialog` (searchable picker over `selectAllAgents`/`fetchAgentsListFull`) writes the schema to the chosen agent via the existing `saveAgentField({field:"outputSchema"})` thunk (RLS-gated, optimistic) — so the JSON Schema Generator agent's output becomes a two-click agent update. Block round-trip in `assemble-cx-content-blocks.ts`; client-only types in `missing-types.ts` (since 2026-07-15: `components/mardown-display/markdown-classification/processors/utils/client-blocks.ts`). Backend/protocol: `docs/protocol/MATRX_ENVELOPE.md`, `MATRX_REFERENCES.md`.
- `2026-06-18` — **Chat working document now persists & round-trips agent edits (the Scribe pattern).** Root cause: the chat working doc defaulted to _unbound_ (`{kind:"none"}`, Redux-only), so the agent's `ctx_patch` ran with `persist:"client"` (no DB write) and `syncWorkingDocumentFromAgentThunk` was a no-op — the agent edited an in-request copy that was discarded at end-of-turn. Fix mirrors Scribe end-to-end: new durable table `public.cx_working_documents` (one row per conversation, `user_id=auth.uid()` RLS, in the `supabase_realtime` publication — `migrations/cx_working_documents.sql`); new aidream writeback handler `@register_writeback("cx_working_document")` (`api/utils/context_writeback.py`, ownership-checked); new FE service `cx-working-document.service.ts` + binding kind `cx_working_document`; `ensureWorkingDocumentRowThunk` auto-provisions+binds the row when the doc is enabled (default), so the published context value becomes `persist:"auto"` + `source:{kind:"cx_working_document",…}`. Agent edits return via **Supabase realtime** (subscribed in `useWorkingDocumentContextSync`) plus a **post-turn re-read** fallback in `syncWorkingDocumentFromAgentThunk`. `applyAgentWorkingDocContent` gains a BUG-B empty-remote guard. User inline edits persist via `updateCxWorkingDocumentContent`. `WorkingDocumentControls` now shows "Auto-saved"/"Saving…" for the default cx backing (was "Unbound (not saved)"); note binding remains optional. **Requires the aidream backend carrying the new handler to be running.** Real-time char-by-char diff in a canvas is the next step (not in this change).
- `2026-06-17` — **Output-directive authoring helper** (`components/settings-management/output-schema/applyDirectives.ts`). An agent's final structured output can auto-apply a server side effect (create a project tree / tasks / any `agent_data` row) two ways: by emitting the reserved-key envelope, OR by declaring the directive on the agent (`agx_agent.settings["output_apply"]`) and emitting a plain payload — the aidream dispatcher applies the named directive idempotently after persist, before stream close. FE contract helpers: `buildApplyOutputSchema({ name, directive, payloadSchema })` (strict `output_schema` with `__matrx_apply` + `directive` as `const`, `idempotency_key` required); `buildAgentDeclaredApply({ directive })` (the `settings.output_apply` fragment); `BUILTIN_DIRECTIVE_PAYLOAD_SCHEMAS` for the five built-ins (`create_project_with_tasks` / `create_task` / `create_tasks` / `db_create` / `db_update`); envelope/receipt-event types + `isDirectiveApplyEvent` / `hasApplyKey` guards for the `directive_apply.{started,applied,failed}` DATA events. **Not yet wired into the stream reducer or the Output Schema tab UI** (helper + types only). Backend contract: `aidream/services/output_directives/FEATURE.md`.
- `2026-06-16` — **`features/agents/docs/AGENT_CREATION_CONTEXT.md`** — attachable quick reference for agents authoring agents: `agx_agent` / `ai_model` / `tool_def` shapes, curated model + tool UUIDs, and targeted SQL patterns — replaces full-schema and full-catalog discovery calls.
- `2026-06-23` — **Provider-overload retry UI is wired through the agent stream.** New backend `provider_retry` events regenerate into the frontend stream contract, store latest/history on `activeRequests`, and render in live assistant turns as a compact provider-busy card with retry attempt, countdown, Cancel, and Retry now. The controls POST back to the server action URLs carried by the event using the same conversation-aware backend routing/auth as the stream. `suspended` uses the existing `paused` conversation lifecycle, keeping Anthropic 529 overloads distinct from ordinary failed-turn errors.
- `2026-06-15` — **Agent Service client foundation (`features/agents/services/agentService.ts` + `.types.ts`).** Typed client for AI Dream's new admin-gated REST surface `/agent-service/*` (the same capability behind the new MCP server). Uses the shared `python-client` helpers (`getJson`/`postJson`/`patchJson`), so the Supabase JWT is threaded automatically and validated server-side. **Scope:** this is for the SERVER-ONLY capabilities — the meta-agent `createAgent`/`createStructuredAgent` and the provider-aware schema gate `validateSchema` (object root + `additionalProperties:false` + every prop required; returns `{ok, findings, portable_schema}`) — plus the catalog read model (`listAgents` facet tree, `getAgent`, versions) and reference lookups (`listModels`/`listTools`/`listSkills`). Agent field CRUD stays direct-to-Supabase (unchanged). Running an agent stays on the streaming path. This is the FOUNDATION for context-aware authoring UIs — no slice/components yet (deliberate; build on top). Backend: `aidream/services/agent_service/` + `docs/agents_service/` (`AGENT_DB_STRUCTURE.md`, `USING_THE_MCP.md`).
- `2026-06-14` — **Model Settings modal: Apply now reliably clears the unapplied-edits warning + honest button labels.** Two fixes to `AgentSettingsModal` / `SettingsJsonEditor`. (1) **Bug:** clicking **Apply** in the Output Schema tab (or any same-tab JSON editor) left the modal still warning "your edits will be lost." Root cause: `SettingsJsonEditor` reported its dirty state only through an effect keyed on `text`, but the Apply button's blur pretty-prints the buffer to canonical form _before_ `handleApply` runs, so `setText(formatted)` was a no-op, the effect never re-fired, and the stale `onDirtyChange(true)` was never cleared. The Raw Editable tab masked this because its `onApply` also switches tabs (which reset the flag). Fix: `handleApply`/`handleReset` now call `onDirtyChange(false)` explicitly after adopting the new baseline. (2) **Honest labels (retired the parallel revert):** the modal no longer keeps its own settings/modelId snapshot or "Discard changes?" dialog. Every edit it makes is already written live to the agent record and tracked there as dirty (`applyFieldEdit` → `_dirty`/`_dirtyFields`, with full undo + `resetAllAgentFields`), so a panel-local revert was a redundant, partial parallel system (it reverted settings + modelId but not `outputSchema`/`tools` → half-revert defect). The former **Cancel** (which implied undo but didn't fully undo) is now **Close**, and there's a single close affordance; closing keeps your edits as part of the agent's unsaved changes, saved/discarded at the agent level. The genuinely panel-local concern — an _unapplied JSON editor buffer_ — is the only thing lost on close, and that warning ("Keep editing / Discard & close") is unchanged and accurate.
- `2026-06-13` — **Custom Dictionary** (`features/dictionary/FEATURE.md`): 3 builtin agents seeded (`migrations/dict_system_agents_and_skills.sql`) — Dictionary Assistant (Claude Sonnet), Terminology Curator + Pronunciation Coach (Gemini Flash) — driving the `dictionary` multi-action tool (aidream migration `0102`); 2 attachable skills (`dictionary-management`, `pronunciation-authoring`). The context-slot **inline-policy control was extracted** to `components/context-slots-management/InlinePolicyControl.tsx` (+ `decodeInlinePolicy`/`encodeInlinePolicy`) and is now shared by `AgentContextSlotsManager` and the dictionary manager — one implementation, no fork.
- `2026-06-12` — **Conversation rows on the `ItemRow` primitive; singleton row-menu annihilated.** Every conversation list (chat sidebar, `/code` history, runner sidebars, the 4 agent windows, code-editor history) now renders `@/components/official/item/ItemRow` — full-width label with a deepening edge fade (no truncate), hover/touch kebab, right-click, and inline rename. The legacy singleton (`useConversationRowMenu.ts` + `ConversationRowMenu.tsx` + the `getConversationRowActions` factory + the `AdvancedMenu` dim-backdrop) is **deleted**; `conversationActionRegistry.tsx` now exports one `buildConversationMenu(ctx) => ItemMenuConfig`. Menus are non-modal (no backdrop) and forced `z-[9999]` so they layer above floating windows; Rename is an `intent: "rename"` entry the row turns into inline edit (dispatching `renameConversation`). See the "Conversation row context menu" section above + [`components/official/item/README.md`](../../components/official/item/README.md).
- `2026-06-12` — **Find Usages + Drift Detection (full subsystem).** Replaced the stub "Find Usages" / "Find Usages (Admin)" windows and the legacy `agx_check_drift`/`agx_check_references`/`agx_accept_version` RPCs (dropped) with a real subsystem — see the **Find Usages & Drift** section above. DB (`migrations/agx_usage_001`–`004`): `agx_usage_registry` (code-pinned usages) + `agx_drift_alert` (weekly-scan ledger, kg-suggestions-style lifecycle) + `dm_messages.action_data` (deep-link chips) + the scan engine `agx_usage_scan_core` and its user/admin/report/history/remediation RPCs (severity tiers `breaking`/`silent_breaking`/`warning`/`info`, effective-definition drift, agent→versions→scan); hardened `agx_purge_versions` (added auth gate + preserves every pin holder, not just shortcuts). Frontend: `redux/usages/` (`agentUsages` slice) + `components/usages/` engine/strip/rows/detail/aggregate/notify + `severity.ts` map + real `AgentFindUsagesWindow`/`AgentAdminFindUsagesWindow` (menu admin item now `selectIsSuperAdmin`-gated) + `AgentDriftBanner` on `/agents/all` + messaging `action_data` chips (`messageActionRegistry`, `MessageActionChips`, `sendDirectActionMessage`). New **Reports module** (`features/reports/`, `/reports` + `/reports/agent-drift` + admin variants, sidebar/dashboard nav) with Agent Drift as report #1. Backend (aidream): `matrx_ai/agents/usage_registry.py` declaration collector (~42 hardcoded agent pins across 8 modules converted to `declare_*` — declaration IS the source), `services/agent_usage/` (startup registry sync, weekly drift scan, DM notifier), `/agent-usage/*` super-admin router, weekly `sch_task`. Seeded `/agents/admin` + `/reports/admin` maps. Every RPC live-verified server-side (registry sync 42 rows/0 broken; drift scan 30 groups dedup-clean; report deterministic 3 agents; `agx_usage_update_to_active` repins v1→v3; not-pinnable/code-managed branches graceful). Follow-up `agx_usage_005` fixed `agx_usage_history_counts` (rs_* `agent_id` is TEXT not uuid — was crashing the history expander 100%). **Pending (FOUND_DEFECTS D3 + `docs/handoffs/AGENT_FIND_USAGES_DRIFT_HANDOFF.md`):** never exercised in a browser or on prod — the DM _send_ has never actually run (stubbed in tests), aidream isn't deployed (startup sync / weekly cron / HTTP endpoints unrun), and the windows/banner/chips/remediation-clicks weren't browser-verified (the engine was, via the report drill-in).
- `2026-06-12` — **Reusable working document (extracted from Scribe).** A collaborative editable doc that attaches to any conversation with one `conversationId` prop — see the **Working document** section above. New 11th execution-instance slice `instanceWorkingDocument` (`redux/execution-system/instance-working-document/`, `byConversationId`, cleared on `destroyInstance`) holding `enabled/content/title/binding/saving/agentRevision`. New `hooks/useWorkingDocument.ts` (draft + 800ms debounce, agent-edit merge while not typing, debounced `saveNoteField` when bound, note seed-on-bind, `openAsWindow`) + `useWorkingDocumentContextSync` (effect-only, mounted in `RunControlsMenu` so the live `working_document` `instanceContext` entry tracks the doc whether or not the editor is open). New `utils/workingDocumentContext.ts#buildWorkingDocumentContextValue` is now **the single `working_document` value shape**: `mutable:true`; bound (note / studio_document) → `persist:"auto"` + `source`, unbound → `persist:"client"`. Scribe's `buildWorkingDocumentValue` (`features/transcript-studio/service/assistantContextBuilder.ts`) delegates here (binding `kind:"studio_document"`) — zero behaviour change, one builder. UI: `components/working-document/WorkingDocumentPanel.tsx` (core) + `WorkingDocumentControls.tsx` drive a new **Document** tab in `RunControlsMenu` (enable toggle, `NotePickerPopover` bind/unbind, open-as-window, active dot). Window: `features/window-panels/windows/WorkingDocumentWindow.tsx` + opener `features/overlays/openers/workingDocumentWindow.tsx` (overlay id `workingDocumentWindow`, multi-instance per conversation, ephemeral) wired into `OverlayController`/`catalogue`/`overlay-ids`/`windowRegistryMetadata`. `process-stream.ts` now handles `context_changed` / `context_persisted` (→ `syncWorkingDocumentFromAgentThunk`, re-reads the bound source since the event carries no content) and `context_persist_failed` (logged loudly, non-fatal). **Caveat:** unbound docs can't reflect an agent edit until the backend ships content in the event (see Working document section). Lint clean; live agent-writeback reflection for bound notes pending end-to-end verification.
- `2026-06-12` — **`/agents/all` gallery: owned + shared stacked with independent pagination.** AgentsGrid now mirrors PromptsGrid — default "All" tab shows owned agents first, a "Shared with me" divider, then shared agents; Mine/Shared tabs filter to one side. Fixed `hasShared` (was derived from the filtered list on the Mine tab, so tabs never appeared). Added owned/shared selector factories + `sharedPage` pagination. `fetchAgentsList` now drains all `agx_get_list` RPC pages so shared rows aren't truncated by the SSR seed limit.
- `2026-06-10` — **Cold-launch base model fix: direct-agent launches fetch the FULL execution payload before the instance snapshot.** `/chat/new` seeded its landing instance with an empty `baseSettings.model` (Model tab showed the "Select a model…" placeholder; advanced overrides showed "No model resolved…"): `launchAgentExecution` Step 0.5 only ensured the _minimal_ payload (`agx_get_execution_minimal` — variable defs + context slots, **no `model_id`/`settings`**), so on a cold route with no prior agent fetch `readAgentSnapshot` → `buildInstanceBaseSettings` had nothing to fold in. Step 0.5 now gates on `selectAgentCustomExecutionPayload` and fetches via `fetchAgentExecutionFull` (`agx_get_execution_full` — adds settings/tools/customTools/modelId; no-ops when already loaded), so every direct-agent creation path snapshots with the model present — `/agents/[id]/run` only ever worked because that route happened to load the full record first. Loud guard in `readAgentSnapshot` warns if an agent is snapshotted before `modelId` is a loaded field. Verified live on `/chat/new`: Model tab shows the default agent's model ("Using the agent's model…") and the advanced settings rows render.
- `2026-06-10` — **Run-controls menu unified + per-run overrides rebuilt on the settings catalogue + tools list redesigned.** (1) **One menu:** new [`RunControlsMenu`](components/inputs/smart-input/RunControlsMenu.tsx) (`variant: "gear" | "plus"`) replaces the near-duplicate `InputControlsMenu` (gear — chat rooms, `/agents/[id]/run`) and `LandingPlusMenu` (`+` on `/chat/new`) — both deleted; consumers (`InputActionButtons`, `SingleRowActionButtons`, `NewChatLandingInput`) are one-line swaps. Tabs: Attach (gated `includeAttach`, default on for plus) / Model (gated on the override layer, hidden for manual/builder runs — unchanged) / Tools / Sandbox / Settings. Unified geometry `w-[min(440px,calc(100vw-1rem))]` — the `min()` lives in the WIDTH because a `max-w-*` class on PopoverContent is overridden to `100%` and silently overflows mobile. Attach helpers dedup'd into [`attach-resource.ts`](components/inputs/resources/attach-resource.ts) (`useAttachResource` + `resourceTypeToBlockType` + `resourceLabel`), consumed by both the menu and `SmartAgentResourcePickerButton` (third copy deleted). (2) **Catalogue-driven overrides:** new [`RunConfigOverrides`](components/run-controls/RunConfigOverrides.tsx) replaces the deleted `RunAdvancedSettings` — rows now come from `buildSettingsRows()` (the `settings-catalogue.ts` chokepoint) for the EFFECTIVE model, grouped like the builder, instead of a hand-curated 9-key list with its own renderer. Keeps the genuine-delta contract (set-to-default → `resetOverride`), the `fetchModelById` ensure-full-record effect, and the collapsed disclosure + count badge; adds removed-state rows, an amber caution strip for overrides the (switched) effective model doesn't declare, and honest empty/loading states (never a silent `null`; empty base model reads "No model resolved…"). Controls render via the new shared [`SettingControlInput`](components/settings-management/controls/SettingControlInput.tsx) (every `ControlType` incl. JSON-draft object editor; `response_format` flattened for display, emitted as canonical `{type}`); `NumberInput` moved verbatim out of `AgentSettingsCore` to [`controls/NumberInput.tsx`](components/settings-management/controls/NumberInput.tsx) (import-only change there). Zero Redux changes; `instance-model-overrides` guard tests + catalogue tests green. (3) **Tools tab:** `RunToolPicker` rows are now single-line — centered checkbox + truncated name + right chevron that expands the full description (accordion); row click toggles, chevron is a real button inside a `div[role=button]` row. Verified live: `/agents/[id]/run` (gear: Model default tab, Gemini rows grouped Text/Feature Flags/Input Capabilities, reasoning_effort override → badge + reset → cleared; Tools: 249 single-line rows, expand + toggle + clear footer), `/chat/new` (plus: Attach default + all panels), `/agents/[id]/build` (manual: no Model tab; builder settings modal sliders/inputs intact after the NumberInput move), mobile 375px (popover clamps to 359px). Deferred: fold Attach into the gear everywhere (drop the standalone Database icon); `markRemoved` UI; full validation surface in the popover. (The `AgentSettingsCore` renderer migration onto `SettingControlInput` shipped `2026-08-08`.)
- `2026-06-09` — **Client-tool resume hardening (mirror of the matrx-extend 2026-06-09 incident fixes; server contract aidream `65fa0d4`, live).** New [`resume-claims.ts`](redux/execution-system/thunks/resume-claims.ts): single-flight resume claim per `user_request_id`, taken synchronously before the first `await` in `resumeInstance` (the `hasAbortController` guard alone let two parallel `continuation_needed=true` responses race two model loops onto one conversation — duplicate `cx_message` positions). `resumeInstance` now re-sends fresh `context` (chips + ambient snapshot, cached state only) so the resumed loop isn't context-blind (`ctx_get` → "No context objects available"). `runAiStream` parses the structured 409 `detail.code` on resume: `resume_conflict` → new `ResumeConflictError` → bounded retry in `resumeInstance` (700ms × attempt, max 4); `not_resumable` → terminal benign; `outstanding_delegated_calls` → unchanged; none surface as user-facing errors. Both dispatchers (`dispatch-ui-first-tool`, `dispatch-widget-action`) now send client-measured `duration_ms` on every tool result. Regenerated `types/python-generated/*` from the live backend (new `ResumeRequest.context`/`writable_variables`/`allow_context_create`, `ClientToolResult.duration_ms`). Protocol doc [`CLIENT_TOOL_SUSPEND_RESUME.md`](docs/CLIENT_TOOL_SUSPEND_RESUME.md) updated (§2.5 structured codes, §2.6 single-flight, extension §4 refreshed).
- `2026-06-08` — **Bidirectional user-agent ⇄ system-agent sync (push / pull / personal copy).** Made the user↔system link symmetric — previously a user agent could only be _pushed up_ to a system agent (`convert-from-agent`); now a system agent can be pulled down into a personal copy and the pair stays linked + syncable either way. **DB ([`migrations/agx_agent_bidirectional_sync.sql`](../../migrations/agx_agent_bidirectional_sync.sql)):** (1) fixed `agx_duplicate_agent` — duplicate/convert silently dropped `skill_config`, `default_rag_boost`, `rag_awareness_mode` to defaults; all three now travel as part of the canonical 18-column portable config. (2) New `agx_sync_linked_agents(p_from_id, p_to_id, p_include_identity)` — the single sync engine. SECURITY DEFINER, verifies the two agents are linked via `source_agent_id` (either direction), and gates writes: **builtin target ⇒ `is_super_admin()`** (publishing to the shared catalogue), **user target ⇒ you own it**. `p_include_identity` false (pull) preserves the target copy's name/description/category/tags; true (push) carries them. Stamps `source_snapshot_at` on the derived side = "pair last reconciled at". (3) `agx_update_from_source` reimplemented as a thin delegate over the engine — closes a pre-existing hole where it had **no auth check** and copied only a partial column set. **Frontend:** new thunks in [`agent-definition/thunks.ts`](redux/agent-definition/thunks.ts) — `fetchLinkedCounterpart` (RLS-natural twin resolver), `syncLinkedAgents` (push/pull), `createPersonalCopy` (idempotent — opens an existing copy instead of spawning a second). New `LinkedAgentRef` / `LinkedCounterpartResult` / `PersonalCopyResult` types. New unified [`AgentSyncBody`](components/admin/AgentSyncBody.tsx) (renders inside the repurposed `AgentConvertSystemWindow`, retitled "Linked Agent Sync"): from either side it shows the linked pair + last-synced + Pull/Push (gated, with an identity-overwrite checkbox on pull), "Create my personal copy" when none exists, and falls back to the existing `ConvertAgentToSystemBody` convert-create flow for an unlinked user agent. Menu: the old admin-only "Convert/Update System Agent" item is replaced by a single user-facing "Linked Agent Sync" management item (visible on both user and builtin surfaces). DB engine verified live against a real linked pair: pull preserves identity + carries the 3 previously-dropped fields, push carries identity, and both the ownership and not-linked guards reject. Type-check + lint clean. UI route verification pending (dev server was unresponsive at build time).
- `2026-06-07` — `AgentViewContent` **Copy for AI** dropdown (`SystemAgentCopyForAiMenu` + `buildSystemAgentAiPayload`): three export modes (basics / with messages / full JSON) with xml-ish `system-agent` wrapper, live agent vs frozen version ID wording, `ai_model.name` (not common_name), variable definitions with `defaultValue` stripped.
- `2026-06-07` — `AgentViewContent` hero metadata: resolves `agx_agent.category` UUIDs to `shortcut_categories.label` (Redux first, Supabase fallback); labeled copyable **Agent ID** and **Current Version ID** rows (`agx_get_version_history` match on live `agent.version`); Public/Private badge moved beside category name.
- `2026-06-03` — Per-conversation "Exclude from knowledge graph" toggle (Step 3.2 of the KG activation plan). New `setConversationExcludeFromKg` thunk in `conversation-row-actions.thunks.ts` (Supabase-direct update on `cx_conversation.exclude_from_kg`, optimistic + rollback — mirrors `setConversationFavorite`). New menu entry in `conversationActionRegistry.tsx`, surfaced wherever `<ConversationRowMenu />` is mounted — every sidebar / chat history / agent-run window in the app. Added `excludeFromKg` to `ConversationListItem`, populated from the global sidebar and history-scope selects (the agent-scoped RPC does not yet project the column — rows from that path default to false until the toggle is flipped). The flow is end-to-end through `ConversationRowMenuData` → `ConversationActionContext` → registry icon/label, so the menu reflects the row's current opt-out state.
- `2026-05-25` — **Smart Input advanced settings (Phase 3).** Per-conversation settings overrides, progressively disclosed under the Model tab. New `features/agents/components/run-controls/RunAdvancedSettings.tsx`: a collapsed-by-default "Advanced settings" disclosure showing a CURATED, ordered control set — modern/common first (thinking level, reasoning effort, reasoning summary, verbosity, web search, URL context); the legacy sampling knobs (temperature, max output tokens) deliberately LAST. Only controls the EFFECTIVE (possibly overridden) model declares are shown — it ensures that model's full `controls` are loaded (`fetchModelById` with an `isLoading`-aware retry, since the registry may hold only the lightweight options record). Delta-based + genuine: each control falls back to the model's declared default for display (so a flag whose default is On reads "On"), and setting a value back to that default clears the override (`resetOverride`) instead of storing a base-equal value. Reviewed by subagent (no blockers); applied the effective-default display + clear fix it flagged. Verified live on `/agents/[id]/run`: disclosure appears once controls load; Gemini shows Reasoning Effort/Summary/Web Search/URL Context/Temperature/Max Output Tokens with temp+tokens last; toggling Web Search off creates an override (badge "1" + per-row reset), toggling back to the default clears it.
- `2026-05-25` — **Smart Input model override (Phase 2).** Per-conversation model picker — the first, simplest layer of the Smart Input override UX. New `features/agents/components/run-controls/RunModelPicker.tsx` reads the instance override layer and renders `SmartModelSelect`; picking a model dispatches `setOverrides({ model })`, picking the agent's own model dispatches `resetOverride("model")` (genuine-delta — never stores a base-equal value, backed by the Phase-1 selector re-diff). Surfaced as a prominent **Model** tab (first + default) in the existing `InputControlsMenu` run-controls popover, gated to instances that own an override layer (hidden for manual/builder-test runs, which read the agent live and have their own model selector). Tab strip gained ARIA `tablist`/`tab`/`tabpanel` roles. Reviewed by subagent (correct & safe, no blockers); applied the perf fix (derive the model from the stable override state, not the new-object `selectCurrentSettings`). Verified live on `/agents/[id]/run`: Model tab appears + default-selected; pick a different model → "Overriding…" + Reset + override dot; pick the agent's own model → override cleared; on `/build` (manual) the Model tab is correctly absent. Follow-up (spawned task): converge the cx-chat + agent-comparison model pickers onto this primitive.
- `2026-05-25` — **Picklist-bound agent variables (label → secret description injection).** A variable can now bind to a user picklist (`udt_picklists`): users pick a _label_, and the server injects that item's hidden `description` as the variable value — the description is an absolute secret that never reaches a non-owner client. **Frontend:** `VariableCustomComponent.picklist {listId, groupName?, multiple?}` + `PicklistRefEnvelope`/`isPicklistRef` ([agent-definition.types.ts](features/agents/types/agent-definition.types.ts)); threaded through `buildCustomComponent`/`extractEffectiveValues` so it survives the per-keystroke rebuild ([variable-customcomponent.ts](features/agents/utils/variable-customcomponent.ts)); builder control `PicklistBindingEditor` in `AgentVariableEditor` (hides the static Options editor when bound); runtime adapter `PicklistVariableInput` renders the existing select/radio/buttons/checkbox in _label space_ and emits the envelope (single) or envelope[] (multi), with `allowOther` free-text staying a plain string; one branch in `VariableInputComponent` covers Inline/Stacked/Cards/Wizard; label-only fetch `getPicklistForSelection` + `usePicklistForSelection` (never fetches `description`); display made envelope-aware (`variableValueToDisplay`, `formatVariablesForDisplay`, `AgentVariablesInline` chip) and empty multi-select counts as missing-required. The Redux value flow already carries arbitrary value shapes, so the envelope rides to the wire unchanged. **Server (aidream):** see `aidream/api/utils/picklist_resolution.py` + `matrx_ai/config/picklist_runtime.py` — envelopes resolve to opaque placeholder tokens in the canonical config (persisted/snapshotted/labeled safely) and the real description is materialized only into a throwaway clone at the provider send (executor clone-at-send), with a `replace_variables` tripwire as the backstop. **DB (migration 0064):** `get_picklist_for_selection` (label-only, SECURITY DEFINER); `get_user_list_with_items` owner/editor-gates item descriptions; `udt_picklist_items` SELECT RLS tightened to owner+editor. Verified: DB secrecy via SQL (owner sees descriptions, non-owner 0 rows / null), server resolution + runtime logic via unit tests, frontend types clean. NOTE: live agent-execution injection needs the aidream backend deployed to verify end-to-end (FE/DB are live-verified).
- `2026-05-25` — **Per-conversation overrides foundation (model + defaults-as-override guard).** Toward a Smart Input model/settings override. (1) `model` now travels through the per-instance override layer as the `LLMParams.model` key (sent via `config_overrides.model`). New shared helper `instance-model-overrides/base-settings.ts#buildInstanceBaseSettings(settings, modelId)` folds the agent's model into the instance's `baseSettings`; ALL base-settings write paths route through it — the create-instance snapshot, the builder→instance sync saga (`syncDefinitionToInstances`, now also watching `setAgentField` modelId), and conversation reload (`load-conversation` seeds from `last_model_id`). Invariant documented in the helper. (2) `selectSettingsOverridesForApi` now RE-DIFFS every override against `baseSettings` (JSON.stringify) and drops base-equal values, so a default disguised as an override can never reach the wire regardless of UI — structurally killing the backend "default-as-override" error, for `model` too. (3) `model` added to the catalogue `CATCHALL_EXCLUDED` and the prompts dialog `recognizedKeys` so it's never a stray settings row or a false "unrecognized" warning. Tests: `instance-model-overrides.selectors.test.ts`, `base-settings.test.ts`. Reviewed by subagents (2 rounds); fixed builder-saga + conversation-reload base-model gaps surfaced in review.
- `2026-05-25` — **Settings panel stops silently mutating user choices + unapplied-edits guard.** (1) Picking Response Format = "text" now STORES `{ type: "text" }` instead of dropping/disabling the row — the UI stores exactly what you pick (AgentSettingsCore + prompts ModelSettings, both the select-change and enable-default paths). (2) Removed the `include_thoughts` → `thinking_budget` auto-rewrite in both panels; that cross-field coupling now surfaces as a caution issue with a one-click fix (`thinking-budget-coupling` rule) instead of silently editing a setting you didn't touch. (3) Raw Editable / Output Schema edits are no longer lost silently: `SettingsJsonEditor` reports `onDirtyChange` (cleared on Apply), `AgentSettingsCore` lifts it via `onUnappliedEditsChange`, and the modal turns **Done** red with an "Unapplied edits" hint and, on Done, shows an inline "Keep editing / Discard & close" confirm (editor stays mounted so the buffer survives "Keep editing"). Verified live: text persists as `{type:"text"}`; Done reddens on edit, confirms on close, Apply clears it.
- `2026-06-23` — **CRITICAL: smart-input next-message draft is now structurally protected from stream-driven wipes.** The May-2026 "input clears on send" UX (entry below) clears `entry.text` on two stream events — `markInputPersisted` (on `cx_user_request` reservation, ~1s after submit) and `clearUserInput` (post-stream success finalization). On chat & `agents/run` the composer and the streaming display share **one** `conversationId`, so those clears landed on the user's **next-message draft** — the one they start typing the instant they submit and may build for **many minutes** while a coding agent responds. Result: drafts wiped ~1s after the agent started AND again when it finished (the reported bug; a user lost a 15-minute message twice in one session). **Fix is at the single source of truth — the slice.** New `instance-user-input/input-draft-protection.ts` (`isInputDraftProtected` + `reportInputDraftViolation`): a draft is protected when `text` is non-empty AND no longer equals `lastSubmittedText` (which can only happen because the user typed something new — `setUserInputText` flips `submissionPhase` back to `"idle"` on the first keystroke). `clearUserInput` and `markInputPersisted` now clear **only** the exact just-submitted message; a clear reaching a live draft is **refused** (draft preserved) and **screams** via `reportInputDraftViolation` (loud-recovery doctrine — a fired recovery = a real bug that slipped the proactive layer). Because the guard lives in the reducers, **every** call path is covered (success cleanup, `clearAfterSend`, the `executeInstance` / `executeManualInstance` / `run-ai-stream` error/`clearInputOnError` paths). `agents/build` is unaffected: its autoclear split already moves input to a fresh `conversationId`, so its clears target the OLD streaming id; the guard is a no-op there. **The composer draft is sacred — do not simplify these guards away.** See the SACRED INVARIANT banner in `docs/STREAM_STATUS_LIFECYCLE.md`.
- `2026-05-24` — **Output Schema tab polish + gentle json_schema reminder.** (a) The tab now maximizes space: the description moved into the editor's placeholder (shown only when empty) and the editor fills the dialog. Added an opt-in `fillHeight` to the shared `SettingsJsonEditor` — because the in-house `Textarea` wraps the `<textarea>` in a block focus-ring div, filling required nesting it in a `flex-1` box and using the component's `h-full` auto-stretch (not bare `flex-1`). (b) Gentle, non-blocking reminder on the Settings tab: when `response_format` is `json_schema` but there's no usable output schema (null or fails `validateOutputSchema`), a subtle info banner appears that links straight to the Output Schema tab. Not enforced — purely a nudge. Verified live: editor fills (textarea 58px → 343px), placeholder carries the description, reminder appears/disappears with the response-format value and switches tabs on click.
- `2026-05-24` — **Output Schema tab in the builder's Model Settings.** The agent's structured-output schema (`agx_agent.output_schema` → `AgentDefinition.outputSchema`) was fully wired in the data layer (type `OutputSchema` in `types/json-schema.ts`, `selectAgentOutputSchema`, `setAgentOutputSchema`, converters that preserve it verbatim, load/save thunks) and shown read-only in `AgentViewContent`, but had **no editor**. Added one as a 5th tab ("Output Schema", `Braces` icon) at the end of `AgentSettingsModal` → `AgentSettingsCore`. New `features/agents/components/settings-management/output-schema/`: `OutputSchemaTab.tsx` (self-contained — reads/writes Redux, reuses the forgiving `SettingsJsonEditor` for edit+Apply; an empty buffer clears the schema to `null`) and `validateOutputSchema.ts` — an **advisory** validator (name format, root must be `object`, `additionalProperties:false`, `required` completeness, `strict`, recursive nested-object checks) surfaced via a **Validate** button that reports Errors/Warnings/Suggestions and **never mutates or auto-applies** (per request). Save persists through the existing autosave path. Verified live: tab renders at the end; Validate flags an invalid schema correctly; Apply→switch tab→return reloads the saved schema; clearing sets it back to `null`. 8/8 validator tests pass; no new tsc/lint errors.
- `2026-05-24` — **Model Settings: standard list = only the model's supported keys; set-but-unsupported settings move to the caution layer (flagged + one-click repairable); locked.** Two failure modes, one root: the settings list was selected ad-hoc per model. (1) The original bug — all three panels rendered each group as `group.filter(({key}) => !!getControl(key)).map(...)`, which silently dropped settings the agent had SET when the model didn't support them (and switching models changed the visible set). (2) Correct model: the **standard list shows only the keys the selected model declares in `controls`**; settings the agent has set that the model does NOT support are **never hidden** — they surface in the **caution** layer (the top-of-panel `IssueTable`) as "not valid for this model", with repair. Settings that are neither supported nor set don't appear. Fixes:
  - **One chokepoint.** New `lib/redux/slices/agent-settings/settings-catalogue.ts` owns the grouped catalogue (labels/order, previously copy-pasted as `textModelSettings`/`booleanSettings`/… into three components) + `buildSettingsRows(controls, settings)`, which returns **only the model's supported keys** (plus a catch-all for supported keys the catalogue doesn't name yet). The three panels (`AgentSettingsCore.tsx`, `features/prompts/.../configuration/ModelSettings.tsx`, `features/agent-settings/.../LLMParamsGrid.tsx`) map over it; they no longer filter by model inline.
  - **Caution + repair.** New `unsupported_by_model` validation category + rule (`validation/rules.ts`): a valued, recognized LLM param the model declares no control for is flagged `warning` (excludes UI capability flags; skipped when the model declares zero controls — a data gap, not an incompatibility). Feeds the existing top-of-panel `IssueTable` (caution) and is fixable via `apply-fix.ts` (clear) — per-row **Fix** + **Fix all**. `analyze.ts` maps it to `unsupported-key` so model-switch reconciliation detects it too (previously carried such values over silently).
  - **Lock (won't regress).** Jest: `settings-catalogue.test.ts` asserts the standard list contains ONLY supported keys (never the whole catalogue, never set-but-unsupported); `unsupported-by-model.test.ts` asserts the caution rule fires + clears. ESLint `no-restricted-syntax` (scoped to the three panels + validation dir) errors on any `.filter`/`.some` referencing `getControl`/`controls[key]`/`normalizedControls[key]` — the ad-hoc selection that kept regressing. Verified live: GLM/Gemini/Claude each show only their declared settings in the standard list; a set-but-unsupported value (e.g. `reasoning_summary` on Claude) appears only in the caution table with a working Fix.
- `2026-05-23` — **Chat submit/stream UX fixes: instant input clear, error surfacing, pre-token "alive" indicator.** Three regressions from one screenshot, all fixed in shared code so they cover chat / run / build / widgets and the agents + conversation + manual endpoints. (a) **Input clears on send.** The sent message no longer lingers in the box until the API resolves. The input components (`AgentTextarea`, `CompactAssistantInput`, `NewChatLandingInput`) gate the visible textarea `value` on `submissionPhase === "pending"` — the text stays in Redux (`entry.text`, the non-visual backup `assembleRequest` reads), so nothing sends empty and autoclear's `splitInputIntoNewConversation` (which also reads `entry.text`) is unaffected. On a pre-persistence failure the `executeInstance` / `executeManualInstance` catches dispatch `clearUserInput`, so the box stays clean (the message survives as the optimistic user bubble + the `lastSubmittedText` re-apply backup). `CompactAssistantInput` now submits via `smartExecute` like every other input (it was calling `executeInstance` directly, skipping `markInputSubmitted`). (b) **Errors surfaced.** Restored the deleted virtual streaming entry in `AgentConversationDisplay` so an active request always has a bubble — fixing both the silently-swallowed error (now shows the real `error.message`, e.g. "Failed to fetch", via new `selectRequestError`) and the missing pre-token state. (c) **Pre-token indicator.** New `BreathingOrb.tsx` primitive (ported from matrx-extend); `AgentAssistantMessage` shows a brief "Processing…" on `connecting`, then the orb from `pre_token`→`interstitial`, gone at completion. Detail in `docs/STREAMING_SYSTEM.md` change log. Verified live (input clears ~100ms; "Failed to fetch" rendered; orb DOM-confirmed mid-stream; type-check clean).
- `2026-05-15` — **Logical turn grouping in the transcript.** Multi-iteration agentic flows (each iteration = one server-side `cx_message` reservation) used to render as N adjacent assistant blocks, each with its own `space-y-6` gap and its own AssistantActionBar, fragmenting the answer and scattering controls. Introduced a render-layer grouping pass in `AgentConversationDisplay`: contiguous assistant entries between two user messages collapse into a single `AssistantTurnGroup` that renders sub-messages flush (zero added chrome between iterations) and emits ONE trailing `AssistantActionBar` anchored to the latest member. `AgentAssistantMessage` gained `hideActionBar?: boolean` (suppresses per-member bar + lifts capture-ref to the group). `AssistantActionBar` gained `groupMessageIds?: string[]` — when set, `Copy` and `Speak` aggregate flat text across every iteration; `Edit` / `Like` / `Delete` / overflow stay anchored to the latest `messageId` (the answer). Print / DOM capture now covers the whole logical turn. **Zero data-model changes** — purely a display-layer derivation; `messages.byId` and the streaming-bubble detection are untouched. Single-iteration turns render identically to before. See "Conversation transcript: logical turn grouping" above.

- `2026-05-15` — **Unified conversation row context menu** wired into every conversation list in the app (9 surfaces). Single registry + singleton hook + `ConversationRowMenu` component — see the "Conversation row context menu" section above. DB-backed via new `cx_conversation.is_favorite` column + updated `get_agent_conversations` RPC; new thunks in `features/agents/redux/conversation-list/conversation-row-actions.thunks.ts` apply optimistic updates across both the `conversationList` and `conversationHistory` slices and revert on failure. `softDeleteConversation` was extended to dispatch `removeConversationFromScopes` so deleted rows disappear from every sidebar without a re-fetch. Rename / Pin / Archive / Delete / Duplicate / Share / Open-in-new-tab / Copy-link now behave identically whether triggered from the chat sidebar, runner sidebar, floating widget, quick-chat-history window, per-agent run history window, builder History tab, or the code editor history panel. The CodeEditorHistoryPanel keeps its existing `Trash2` button for DRAFT rows (still local-only, calls `destroyInstanceIfAllowed`) and adds the new menu for real conversations. Z-index discipline: every action calls `onCloseMenu()` **before** opening a dialog so the AdvancedMenu and confirm/text-input dialogs never overlap.
- `2026-05-15` — `atomicRetry` now handles multimodal user turns. The thunk used to call `extractFlatText(triggeringUserMessage)` and reject with "atomic retry can only resubmit text turns" whenever the user message had no `text` part — which happens any time the original input was an image, an attached webpage/notes/table resource, or any other non-text MessagePart. Fixed by splitting the triggering user message's `MessagePart[]` content into a joined text string (every `text` part) and a non-text remainder (everything else, with assistant-only types `tool_call` / `tool_result` / `thinking` filtered out defensively), then seeding **both** `setUserInputText` and `setUserInputMessageParts`. `assembleRequest` already concatenates them onto `user_input` for a brand-new turn, so multimodal retries now produce the same payload the original send did. The only remaining reject path is a user message with literally zero content blocks, which should be unreachable. Both `/agents/[id]/build` and `/agents/[id]/run` share this thunk, so the fix lands in one place.
- `2026-05-15` — **Server-side conversation API: opt-in parallel paths wired.** The Python team shipped a batch of new HTTP endpoints that overlap with existing direct-Supabase RPC thunks (fork, delete, edit-and-fork) and add a whole new compaction capability (replace / hide / restore / turns-compact). Spec lives at [`docs/FE_CONVERSATION_API_CHANGES.md`](../../docs/FE_CONVERSATION_API_CHANGES.md); request/response types are in the auto-generated `types/python-generated/api-types.ts`. We wired every new endpoint **additively** — none of the existing thunks were touched, and no call site was changed:
  - **`lib/api/call-api.ts`** — added 7 typed wrappers: `callConversationFork`, `callConversationForkAndRun` (streaming), `callBatchDeleteMessages`, `callReplaceMessages`, `callHideMessages`, `callRestoreCompaction`, `callCompactTurns`. Body / response shapes are all inferred from the generated OpenAPI types — passing the wrong shape is a compile-time error.
  - **`features/agents/types/conversation-stream-events.ts`** — manual mirror of `ConversationForkedEvent`, the first-event-on-stream payload from `/ai/conversations/{id}/fork-and-run`. Not in `api-types.ts` because stream-event payloads aren't OpenAPI-shaped (same pattern as the scraper's `page_extraction` events).
  - **`features/agents/redux/execution-system/message-crud/server/`** — new directory holding one thunk per endpoint: `forkConversationServer`, `forkAndRunServer`, `batchDeleteMessages`, `replaceMessages`, `hideMessages`, `restoreCompaction`, `compactTurns`. Each calls the matching wrapper, runs `loadConversation` after a successful write so the messages/observability/variables slices mirror the new DB state, and fires the standard cache-bypass + invalidate. `README.md` in the same directory carries the pairing table — which legacy RPC thunk each one parallels, and which are net-new capabilities (compaction has no legacy equivalent).
  - **`forkAndRunServer` integration scope.** The thunk fires the stream, captures the `conversation.forked` first event, optionally navigates the surface via `setFocus`, and hydrates the new conversation via `loadConversation` after the stream completes. It does NOT yet route subsequent stream events into `processStream` for live token-by-token rendering into the fork's slice entries — that's a deliberate follow-up so the wire-up is testable first. The thunk's docstring calls this out explicitly.
  - **`zero-replacement guarantee.`** Existing `forkConversation`, `editMessage`, `deleteMessage`, `overwriteAndResend`, `atomicRetry`, `softDeleteConversation`, `invalidateConversationCache` thunks and their callers are unchanged. Surfaces opt in to the server-backed path one at a time; once a surface is validated end-to-end we consolidate.
- `2026-05-15` — Fork: RPC perf fix + explicit "Stay here / Go to new branch" prompt. Two bugs were ganged together; fixing them in one pass:
  - **Backend.** `cx_fork_conversation` was timing out on real conversations. Two causes: (1) a row-by-row PL/pgSQL loop over `cx_message` that rebuilt the `v_msg_map` JSONB on every iteration — O(N²) memory copies; (2) both `cx_tl_call` queries (the id-map loop and the bulk INSERT) omitted `conversation_id` from their WHERE clause, so the only usable predicate was `v_msg_map ? message_id::text` (a JSONB containment check with no index support), forcing a full table scan. Rewrote the RPC set-based: `jsonb_object_agg` builds both `v_msg_map` and `v_tc_map` in single passes, then single bulk INSERTs copy the rows. Every `cx_tl_call` query now filters by `conversation_id` first so `idx_ctc_conversation` drives the scan. Measured fork-at-position-215 on a 216-message / 89-tool-call conversation: **32 ms end-to-end**, down from statement-timeout (≥8 s). Behavior and return shape are unchanged — still returns the `get_cx_conversation_bundle` payload.
  - **Frontend.** The post-fork "where do you want to go?" affordance was a sonner toast that auto-dismissed after 8 s — easy to miss, made the action feel like a no-op. Replaced with the global imperative `confirm()` modal so the user is explicitly prompted to **Go to new branch** vs **Stay here**, with `requestSurfaceNavigation` firing on confirm. Renamed `ForkOutcomeToast.tsx` → `promptForkOutcome.ts` because the file no longer renders a toast. The other consumer (`UserActionBar` edit-and-resubmit) auto-navigates (the new branch is already streaming a response — user already chose), and its surface-less fallback is now a simple success toast instead of a button that silently no-ops.
- `2026-05-15` — Older-message pagination wired into `AgentConversationColumn` (used by `/agents/[id]/run` and `/chat/[conversationId]`). The bundle RPC always supported `p_before_position`, but no client trigger existed and `hydrateMessages` was clobbering everything. New pieces:
  - `features/agents/redux/execution-system/thunks/conversation-bundle.ts` — extracted the bundle fetcher + row converters out of `load-conversation.thunk.ts` so both initial-load and older-page fetches share one path.
  - `load-older-messages.thunk.ts` — re-entry-guarded, calls `get_cx_conversation_bundle` with the slice's `oldestPosition` cursor, dispatches `prependMessages` + `mergeToolCalls` only. Skips `cx_user_request` / `cx_request` fallback queries on the pagination path.
  - `messages.slice.ts` — `MessagesEntry` now carries `oldestPosition`, `hasMoreOlder`, `isLoadingOlder`. New `prependMessages` reducer is strictly additive (never overwrites existing `byId` entries — preserves references so per-message subscribers don't re-render). `hydrateMessages` seeds the cursor from `bundle.pagination`.
  - `observability.slice.ts` — new `mergeToolCalls` reducer mirrors the additive contract for the tool-call map so older tool turns hydrate without touching streaming tool calls.
  - `OlderMessagesSentinel.tsx` — invisible 1px sentinel at the top of the scroll container. Owns the `IntersectionObserver`, dispatches the older-page thunk with a 200px rootMargin prefetch, and uses a `useLayoutEffect` to restore visual scroll position after each prepend (shift `scrollTop` by the `scrollHeight` delta). Subscribes only to `hasMoreOlder` / `isLoadingOlder` / `firstMessageId` so its re-renders never reach the message tree.
  - `AgentConversationDisplay.tsx` — fixed a pre-existing auto-scroll bug where any length growth (including prepend) yanked the user to the bottom. Now only scrolls when the LAST message id changes AND the count grew.
  - **Stream-safety invariant:** prepend and merge writes touch only NEW ids — existing message and tool-call records keep their object references, so streaming bubbles and previously-rendered messages do not re-render when older history pages in.
- `2026-05-02` — AgentViewContent cleanup: hero block (name, copyable ID, description, status pills, tags), stat strip, reordered sections (settings → variables → context slots → tools → MCP → output schema → separator → messages), per-message MD/Plain toggle, JsonInspector for JSON view and output schema, admin-only Pretty/JSON page toggle. Variable rendering extended into inline code and fenced code blocks (`InlineCodeSnippet.renderVariables`, `BasicMarkdownContent` inline `code` handler). JsonInspector convention added to FEATURE.md.
- `2026-04-29` — Convert-to-system flow (`ConvertAgentToSystemBody`): when existing system agents are listed, each row has an external-link control that opens `/agents/{id}` in a new tab so admins can review before update.
- `2026-04-25` — Barrel import cleanup: external and core callers no longer import `conversation-list`, `message-crud`, `surfaces`, `mcp-client`, or execution-system index folders without a file — use `*.slice`, `*.thunks`, `*.selectors`, `mcp-client/client`, etc. (includes `lib/redux/rootReducer`, `cx-chat/ConversationInput` → `types/instance.types`, MCP API routes, window panels, `packages/matrx-agents` re-exports, and related).
- `2026-04-25` — Variable UI: `VariableInputComponent` now lives in `components/inputs/input-components/VariableInputComponent.tsx` (client); `input-components/index.tsx` re-exports only the leaf inputs (`ToggleInput`, `NumberInput`, etc.) and `useContainerWidth` — importers of the dispatcher use the dedicated file, not the barrel.
- `2026-04-25` — claude: Phase 2 — applied the message UX overhaul to every consumer that shares `AgentConversationColumn` or directly mounts `AgentUserMessage` / `AgentAssistantMessage`. `ChatRoomClient` (chat route) registers `kind: 'page'` with URL `/chat/{conversationId}`. `AgentBuilderRightPanel` registers `kind: 'window'` (test panel switches in place — builder route doesn't carry conversationId). `AgentRunWindow` registers `kind: 'window'`. `AgentChatAssistant` (floating chat-assistant widget) registers `kind: 'widget'` with `customNavigation: true` and on fork spawns a sibling widget for the target conversation while closing itself, preserving the one-widget-per-conversation mental model. `AgentRunnerPage` gained an optional `buildConversationUrl` prop; `CodeWorkspace`'s `ChatPanelSlot` passes one so fork navigation stays at `/code?agentId=X&conversationId=Y` instead of 404'ing on the runner's default `/code/{agentId}/run` pattern. The surfaces thunk now honors a `customNavigation` flag on `window`/`widget` registrations so consumers like the chat-assistant can react to navigation intents without a URL change. `flash-cards/AIChatInterface` is intentionally excluded — it talks to OpenAI directly and does not use the agent system.
- `2026-04-24` — claude: message-level UX overhaul on the runner — fork outcome toast (Go to new branch / Stay here), Edit & Resubmit dialog (Fork vs Overwrite, both auto-submit), per-message Delete dialog (Delete here / Fork without this message, with cascade warning for attached tool calls), and inline Retry button on failed assistant turns. Built on a new central surfaces registry (`features/agents/redux/surfaces/`) so action bars stay surface-agnostic — page consumers register `kind: 'page'` and get a 5-line pendingNavigation effect; window/widget consumers register without one and react to focus updates as before. New thunks (`deleteMessage`, `overwriteAndResend`, `atomicRetry`) are stable; they fall back to per-message soft-delete when the atomic Python RPC `cx_truncate_conversation_after` isn't deployed yet. Python team work captured in [`docs/PYTHON_RESUME_SPEC.md`](./docs/PYTHON_RESUME_SPEC.md). Other consumers (chat, build, AgentRunWindow, widgets) get the new dialogs automatically once they pass `surfaceKey` through `AgentConversationDisplay` and register their surface — separate PR.
- `2026-04-22` — claude: initial FEATURE.md umbrella + new sub-docs (BUILDER, RUNNER, VERSIONING, INVOCATION_LIFECYCLE, ORCHESTRATION, STREAMING_SYSTEM).
- `2026-04-22` — claude: admin surface for system (builtin) agents lives at `app/(authenticated)/(admin-auth)/administration/agents/system-agents/agents/`. Reuses `AgentBuilderPage`, `AgentRunnerPage`, and `AgentCard` from the user-side `(a)/agents/` routes via new `basePath` / `backHref` props on `AgentHeader`, `AgentModeController`, `AgentRunHeader`, `AgentCard`, `AgentListItem`. The admin shell (`ClientAdminLayout`) suppresses its ModuleHeader on builder/runner detail routes so these pages can own the top strip. `SystemAgentsGrid` is a simpler admin-only grid that reads `selectBuiltinAgents` directly. System agent creation goes through `createSystemAgentFromSeed` server action (admin-gated, writes via `createAdminClient` with all scope columns null).

---

> **Keep-docs-live rule:** after any substantive change to agents — especially to thunks, slice shapes, invocation payloads, or stream event types — update this file, the specific sub-doc it touches, and the Change log. A broken mental model cascades across every parallel agent working on top of it.
