# FEATURE.md — `chat route` (the live `/chat` surface)

**Status:** `active`
**Tier:** `1`
**Last updated:** `2026-07-14`

> **This is the authoritative doc for the LIVE chat route.** The chat route lives at `app/(a)/chat/**` and is built on the `features/agents/` execution-system — **not** on the unbuilt `ConversationShell` in `features/conversation/`. If you were sent here by `features/conversation/FEATURE.md` or `phase-07-chat-route.md`, this file supersedes their description of how the route behaves.

---

## Purpose

The user-facing chat surface: pick an agent, start a conversation, stream a reply, browse history. Chat is an agent **runner** where the "agent" is chosen from the user's own / system / community agents. It is the consume end of Build → Test → Consume.

---

## The model — read this before touching anything (load-bearing)

This is the single most-violated invariant in this surface. Two concepts, kept strictly separate:

- **Agent** = the thing that *starts* a conversation. It is a template/launcher. Its only job is the first turn.
- **Conversation** = an independent entity with its own `id`. Once it exists, it stands alone. `cx_conversation.initial_agent_id` records *which agent started it* — that is **read-only provenance**, not a live link back to the agent.

From this, four non-negotiable rules — **never revive a conversation the user didn't explicitly open**:

1. **`/chat/a/[agentId]` ALWAYS starts a brand-new conversation. It NEVER revives.** Selecting an agent (picker, pinned list, `+`, chip) is "start fresh with this agent," full stop.
2. **Switching agents NEVER reopens a past conversation.** Returning to an agent you used earlier in the session must not resurrect that agent's old transcript. (Analogy the user gave: opening your phone shouldn't auto-dial the last person you spoke to.)
3. **`+` (new chat) uses the ACTIVE agent**, falling back to the default (`/chat/new`) only when there is no active agent. **Never** route by "last-used agent."
4. **Bare `/chat` always lands on `/chat/new`. Never resume-last.**

Users reach prior conversations exactly one way: by clicking them in the **history sidebar** (`/chat/[conversationId]`).

---

## Entry points

**Routes** (`app/(a)/chat/`)
- `page.tsx` — bare `/chat`; `redirect("/chat/new")`. Always fresh.
- `new/page.tsx` — `/chat/new`; SSR-resolves the default agent name, mounts `ChatNewClient` (greeting + quick-action chips over the default agent's input). Its Suspense fallback renders the same greeting/input silhouette so first paint never shows a missing composer.
- `a/[agentId]/page.tsx` — `/chat/a/[agentId]`; SSR-resolves the agent name, mounts `ChatRoomClient` with **no** `conversationId` → launcher mints a fresh conversation.
- `[conversationId]/page.tsx` — `/chat/[conversationId]`; SSR-resolves the owning agent from `cx_conversation.initial_agent_id`, mounts `ChatRoomClient` **with** the `conversationId` prop (loads the existing transcript).

**Components** (`features/agents/components/chat/`)
- `ChatRoomClient.tsx` — orchestrates one conversation surface; renders ONLY the `AgentConversationColumn` (like `AgentRunnerPage`). Two mount paths (fresh vs. existing). Owns the fresh-start guard and post-submit URL promotion.
- `ChatRunHeader.tsx` — controls injected into the **app shell header** (`#shell-header-center`) via `<PageHeader>`: a COMPACT agent picker (`AgentListDropdown compact`, never full-width) + `ActiveContextLensChip` (Lens Chip → `ActiveContextTree`) on the **left**, `ChatCanvasButton` on the **right** (`w-full` + `justify-between` at every breakpoint so the cluster never collapses to center on mobile/tablet). Owns agent-select navigation.
- `ChatIncognitoHeaderButton.tsx` — toggles incognito on fresh chat routes (`/chat/new`, `/chat/a/[agentId]`). Portals a `GhostTapButton` into `#shell-header-right`. When active, black out sidebar + avatar and block sandbox binding.
- `ChatIncognitoProvider.tsx` — chat-layout provider; syncs Redux `chatIncognito` slice + `data-chat-incognito` on `.shell-root`.
- `chat-incognito.slice.ts` — Redux source of truth for incognito toggle (read by sandbox resolver, run controls, execute thunks via instance `isEphemeral`).
- `ChatSidebarMenu.tsx` — chat-route actions + conversation history rendered INSIDE the **app shell sidebar** as a Large-Route menu (registered in `features/shell/constants/route-menu-registry.ts`). Mirrors `AgentRunSidebarMenu`. **Chrome rows (always rendered, IDENTICAL DOM in both states) — `New chat` / `Search chats` / `Search agents` / `Voice agent` — use the EXACT same `.shell-nav-item shell-tactile-subtle` markup as `features/shell/components/sidebar/NavItem.tsx`** (canonical 18px Lucide, `.shell-nav-icon` + `.shell-nav-label`). The shell's existing CSS handles label collapse, icon centering, and the collapsed `[title]:hover::after` tooltip — the icons stay at the same x position in both states and the first row stays at the same y. **Do not invent a parallel styling system here.** Search Chats wraps a chrome row in a Popover whose content is `<ChatHistorySidebar initialSearchOpen>` with its own `chat-route-search` scope (so its in-popover searchTerm doesn't leak into the always-on sidebar list). Search Agents reuses the canonical `AgentListDropdown` via `triggerSlot` + `contentSide="right"`. Voice Agent is a real `<Link>` to `/chat/voice` with `.shell-active-pill` applied on match. **Extras** (Pinned + grouped history) render only when the sidebar is expanded — no room in the narrow rail. (`Webhook` matches the app's "Agents" nav glyph; `Bot` is banned by `matrx/no-banned-lucide-icons`.) **Both history lists pass `surfaceId="chat"`** — the ALLOW-list source filter (`source_feature ∈ chat-route / chat-interface / quick-chat`, resolved from `userPreferences.conversationFilters` → `source-registry` `SURFACE_DEFAULTS.chat`). System runs, transcription, server automations, and voice transcripts are hidden by default and reachable via the `ConversationSourceFilterTree` icon, NOT a hard-coded `excludeSourceFeatures` deny-list (removed).
- `ChatHistorySidebar.tsx` — **thin wrapper** over the canonical `ConversationHistorySidebar` (`features/agents/components/conversation-history/`) with `variant="consumer"` + `surfaceId="chat"` defaulted. The list rendering + scoped data pipeline + source filtering all live in that ONE primitive (shared with the dense `/code` + agent-app lists), so the consumer and dense surfaces never drift. New surfaces use `ConversationHistorySidebar` directly.
  - **Row-click navigates by default; self-contained surfaces pass `openInPlace`.** A plain click on a row routes to `/chat/<id>` via the `ItemRow` `<Link>` — correct for the **standalone `/chat` route** (`ChatSidebarMenu`) and the search popover (which navigates AND closes). Any surface that hosts the conversation ITSELF (the `QuickChatSheet` drawer, the `/code` history slot, `ChatHistoryWindow`, agent-app shells) MUST pass `openInPlace` so a click calls `onOpenConversation` and PREVENTS navigation — the `href` is kept so modifier/middle-click still opens a new tab and "Copy link" works. Without it, `onOpenConversation` fires AND the page routes away (the in-place load is shadowed). `openInPlace` is the generic `ItemRow` flag (`components/official/item/`); presence of `onOpenConversation` does NOT imply it.
- `ChatNewClient.tsx` — `/chat/new` landing (default agent + greeting).
- `NewChatGreeting.tsx` — greeting + chips; chip click stashes a draft and pushes to `/chat/a/[chipAgentId]`.
- `agent-context/buildChatContextData.ts` — the `matrx-user/chat` surface emit contract: a PURE `buildChatContextData(args)` mapping live chat state → `createChatScope` (all 20 manifest values; callers pass only what they can source), plus `CHAT_CONTEXT_MENU_PROPS` (`sourceFeature: "chat-route"`, `surfaceName: "matrx-user/chat"`). Shared by every chat region so the composer + display emit one shape.

**Surface agent-context (`matrx-user/chat`)** — the manifest lives at `features/surfaces/manifests/chat.manifest.ts`; the runtime emit + menu props at `agent-context/buildChatContextData.ts`. `NewChatLandingInput` (the `/chat/new` hero) carries the right-click agent menu (`UnifiedAgentContextMenu` + `getApplicationScope` over the draft). The live `/chat/[id]` + `/chat/a/[id]` composer is the shared `AgentTextarea` (`components/inputs/smart-input/`) and the presentational display is `AgentConversationDisplay` (`components/messages-display/`) — both OUTSIDE this folder; their menu/Pro wiring is owned there, not here. The display's `MarkdownContextMenuProvider` currently defaults `surfaceName` to `matrx-user/assistant-message` and must be passed `matrx-user/chat` (tracked — see Change Log 2026-06-23).

**App-shell integration (this is HOW chat gets its sidebar + header — do NOT rebuild a custom one).** The chat route is a "Large Route", exactly like `/agents/[id]/run`:
- Each `page.tsx` renders `<PageHeader><ChatRunHeader …/></PageHeader>` + the content — the header portals into the shell header center slot.
- `route-menu-registry` maps `/^\/chat(?:\/|$)/` → `ChatSidebarMenu`. The shell's `RouteMenuSlot` auto-switches the sidebar to it (defaults to the chat-history "local" menu) and provides the **switch button to the main app menu** — on the desktop sidebar AND in the mobile shell drawer. The shell owns all the chrome (collapse/expand, mobile drawer, switch).

**Hooks**
- `useAgentLauncher(agentId, { surfaceKey, ready, retainOnUnmount })` (`features/agents/hooks/useAgentLauncher.ts`) — managed mode creates + tracks the conversation; returns the focused conversation id for the surface.

**Redux**
- `conversationFocus` slice (`features/agents/redux/execution-system/conversation-focus/`) — `bySurface[surfaceKey].{input,display}`. The surface key is `chat-route:<agentId>`.
- `conversations` / execution-system slices — the live instance.
- `conversationList` / `conversation-history` — sidebar data (history uses `fetchConversationHistory`, a different thunk from the global `conversationList`). The scoped `conversation-history` slice carries the per-scope source filter (`includeSourceApps` / `includeSourceFeatures` / `includeEmptySource`, set by `setScopeSourceFilter`) + the user-wide `sourceFacets` (RPC `get_cx_conversation_source_facets`) that power the filter tree.

---

## Key flows

### Flow 1 — Start fresh with an agent (`/chat/a/[agentId]`)
1. `ChatRoomClient` mounts with `agentId`, no `conversationId`. `surfaceKey = chat-route:<agentId>`.
2. **Fresh-start guard** (runs on every agent/route change, before the launcher): `dispatch(clearFocus(surfaceKey))` — drops any stale per-agent focus so a previously-used agent can't revive its old conversation. NOT ref-guarded: `ChatRoomClient` is reused (not remounted) across chat navigations, so it must re-run on every agent switch / `+` click.
3. `useAgentLauncher` (now `ready`) creates a fresh instance, sets focus to it.
4. User submits → `messageCount >= 2` → **`waitForConversationPersisted` confirms the `chat.conversation` row is committed & readable** → `router.replace('/chat/[conversationId]')`. The launcher's `retainOnUnmount` keeps the live instance alive across this promotion. `record_reserved` only announces reserved UUIDs, NOT a commit — see the promotion invariant below.

### Flow 2 — Open an existing conversation (`/chat/[conversationId]`)
1. SSR resolves `initial_agent_id`; `ChatRoomClient` mounts with both props. Launcher is gated **off** (`ready: false`); fresh-start guard is skipped.
2. If the instance is already live in memory (URL promotion), skip the load to avoid clobbering the stream; otherwise `loadConversation` hydrates from the DB with a bounded initial message page.
3. Cold deep-links keep the transcript-shaped `ChatRoomSkeleton` up until the bundle resolves. The first hydrated transcript window renders only the bottom pair (latest user group + latest assistant group) and defers cold DB markdown for a beat so `MarkdownStream` does not visibly assemble in front of the user.
4. The next four display groups auto-reveal above that pair after the cold markdown settle window. During this cold settle phase the scroll container is pinned to its true bottom (`scrollTop === scrollHeight - clientHeight`), so mounting the older batch never exposes the top of the conversation.
5. Loaded groups older than the initial six remain hidden until the user scrolls upward; the top sentinel reveals two groups at a time, then fetches older DB pages only after the hidden loaded window is exhausted. Live URL-promotion streams bypass this cold-window path entirely.
6. Picker shows the initiating agent (read-only provenance).

### Flow 3 — `+` new chat
- `ChatSidebarMenu` `+` calls `beginFreshChat()` — clears stale surface focus, bumps `chatRoute.freshSessionNonce`, routes to `/chat/a/[activeAgentId]` (or `/chat/new` for the default agent). Both destinations start fresh (Flow 1). `useAgentLauncher` receives `preferFresh` + the nonce so it remints even when the URL stays on `/chat/new`.

### Flow 4 — Failed turn & retry
A failed turn is **kept in history** (never deleted) and recovered with a non-destructive retry. Backend contract: `aidream/.../CONVERSATION_FAILURE_AND_RETRY_FE_GUIDE.md`.

1. A turn fails → it renders as a standalone error bubble (`AssistantError`), NOT folded into any answer's turn group. Detection (`isFailedRecord`, in `messages.selectors.ts`) is unified across **live** failures (`activeRequests[reqId].status==='error'`) and **persisted** ones (`cx_message.status==='failed'` / `metadata.failed`), so a failure looks identical in-session and after reload.
2. The error text resolves `streamError.user_message ?? streamError.message ?? metadata.error ?? content ?? "The response failed."`. Live failures also expose error_type / status_code / technical message behind a **Details** disclosure; reloaded ones show just the friendly line (the message row carries only `metadata.error`).
3. **Retry** (one click, no confirm — it's non-destructive) appears ONLY on the conversation's last, failed turn (`canRetry`, computed in `AgentConversationDisplay`). It dispatches `retryConversationTurn` (`message-crud/retry-turn.thunk.ts`):
   - last user message **persisted** → `executeInstance({ retry: true })` → `POST /ai/conversations/{id}` `{retry:true}` with **no** `user_input`. The failed turn (hidden from the model) stays; the model re-attempts from the user message. Failed attempt + successful retry **share a `position`**, so the transcript orders by `(position, created_at)` (see `messages.slice.ts` `byPositionThenCreatedAt`).
   - last user message **optimistic** (immediate "Failed to fetch", never persisted) → re-send: drop the optimistic bubble, re-seed input, `executeInstance()` (routes turn-1/turn-2+ correctly).
4. **Edit a previous message + resubmit** is a separate, existing path (`UserActionBar` "Edit & resubmit" → fork or `overwriteAndResend`); it re-runs with `user_input`, so it is unaffected by the retry contract.

> **Backend dependency (as of 2026-05-24):** production aidream does NOT yet accept `retry:true` (it 422s `user_input` required) and persists failed turns without `metadata.error` / with `is_visible_to_model=true`. The FE is built to the guide and degrades gracefully; end-to-end retry needs the aidream deploy. See the `project_retry_backend_gap` memory.

### Flow 5 — Client-tool suspend → submit → resume

When the agent calls a client-delegated tool (ask-user, widget action, capability tool), aidream's `_suspend_for_delegation` **hard-suspends the loop and ENDS the stream**. The client must POST the result and then open a fresh stream against `/ai/conversations/{id}/resume` — the original stream is gone. Both the frontend and the matrx-extend extension ship this wiring; the canonical protocol doc is [`features/agents/docs/CLIENT_TOOL_SUSPEND_RESUME.md`](../../docs/CLIENT_TOOL_SUSPEND_RESUME.md).

1. Server emits `tool_event { event: "tool_delegated", call_id, tool_name, … }`, then `phase: "complete"` + `info: "suspended_awaiting_client"`, then ends the stream.
2. `dispatchUiFirstTool` flips the instance to `paused` and awaits the user (e.g., an `<AskCard>` click). End-of-stream guards in [`process-stream.ts`](../../redux/execution-system/thunks/process-stream.ts) keep `paused` from being overwritten by the natural stream-end `complete` transition.
3. User submits → `submitToolResult` (the **single funnel** at [`features/agents/api/submit-tool-results.ts`](../../api/submit-tool-results.ts)) batches into a `POST /tool_results`.
4. Response is `ToolResultsResponse`. When `continuation_needed && user_request_id`, the funnel dispatches `resumeInstance({conversationId, userRequestId})`.
5. [`resumeInstance`](../../redux/execution-system/thunks/resume-instance.thunk.ts) rebuilds the capability envelope via `buildToolInjection`, then drives the SAME [`runAiStream`](../../redux/execution-system/thunks/run-ai-stream.ts) helper as a normal turn — against `/ai/conversations/{id}/resume`. The resumed loop emits events through the same `processStream` reducer; re-entrancy works for free.

**Invariant — never wait on the original stream after a `tool_delegated`.** It has ended. An ESLint chokepoint (`no-restricted-syntax` on `/tool_results`) prevents any callsite outside `submit-tool-results.ts` from POSTing tool results, so the resume handoff cannot be bypassed.

---

## Invariants & gotchas

- **The surface key is `chat-route:<agentId>` (agent-derived).** Focus per surface persists across route changes and is **not** auto-cleared on unmount (the launcher uses `retainOnUnmount`). Therefore the agent route **must** clear stale focus on mount — that is the fresh-start guard in `ChatRoomClient`. Remove it and Bug "switching back to an agent revives its old chat" returns immediately.
- **`createManualInstance` always mints a new conversation id** — it never dedupes by agent. Revival, when it happened, came from stale *focus*, not instance reuse.
- **`conversationFocus` is runtime-only (not persisted).** Revival bugs are same-session; a hard reload hides them. Test within one session.
- **Never reintroduce "last-used agent" or "resume last conversation" routing.** Those are the killed anti-patterns: per-agent focus stickiness, "stateful `+`", resume-last-on-entry. They *feel* helpful and are exactly what the product forbids.
- **`initial_agent_id` is provenance, not a live link.** Show it; never use it to reopen or re-bind a conversation to an agent.
- **First-turn promotion timing (do not weaken):** `messageCount >= 2` is a *readiness* trigger, NOT a persistence signal. `record_reserved` events only announce reserved UUIDs; the backend persists the whole turn (conversation + messages) atomically at stream-end, so the `chat.conversation` row is frequently NOT readable at count==2. The promotion effect therefore gates `router.replace` on `waitForConversationPersisted` (`conversations/conversation-persistence.ts`) — a client read that mirrors `resolveConversationSeed` exactly (schema `chat`, `deleted_at IS NULL`, non-null `initial_agent_id`). Navigating before the row is committed makes the `/chat/[cid]` SSR guard miss and hard-redirect to `/chat/new` — the "can't leave `/chat/new`" bounce. This gate is backend-timing-agnostic: instant when the row commits early, deferred to turn-end when it commits atomically.
- **The "`+`/agent-switch snaps back to the old chat" bug lived in the post-submit promotion effect** — NOT in focus or `loadConversation`. `/chat/[id]` and `/chat/a/[agentId]` share the same `surfaceKey`, so on a `+` click the promotion effect could fire with a STALE-CLOSURE `liveConversationId` (the previous conversation, already at `messageCount >= 2`) and `router.replace` back to it, one render before the launcher swapped focus to the fresh conversation. **Guard (do not remove):** the effect only promotes when `liveConversationId` is STILL the surface's focused input conversation, read live from the store (`store.getState().conversationFocus.bySurface[surfaceKey].input`).
- **Chat does NOT own a sidebar or header — the app shell does, exactly like `/agents/[id]/run`.** History is a Large-Route menu (`ChatSidebarMenu`) in `route-menu-registry`; controls (compact picker + `+`) are injected into `#shell-header-center` via `ChatRunHeader` + `<PageHeader>`. The shell's `RouteMenuSlot` auto-switches the sidebar to the chat-history ("local") view and renders the **switch button to the Main Menu** — on desktop and in the mobile drawer (so the picker/`+` sit between the app hamburger and the avatar, never overlapping). **Never** rebuild a bespoke chat sidebar/header: the old `ChatPageShell` did, which overlapped the global header on mobile and created a 768–1023px dead zone (both gone). To change chat sidebar/header behavior, edit `ChatSidebarMenu` / `ChatRunHeader`, or the shell (`features/shell/components/sidebar/`).
- **The agent picker is COMPACT (`AgentListDropdown compact`), never full-width.**
- **Never send while the mic is recording or finishing transcription.** Mid-voice submit drops the trailing audio and leaves the recorder running. `SmartAgentInput*` + `NewChatLandingInput` disable the send button and Enter while `isRecording || isTranscribing` (`AgentMicrophoneButton.onRecordingStateChange` → `voiceBusy`). Stop recording first; send re-enables when the final transcript lands.
- **A reachable Local engine is not automatically a saved-agent runtime.** Direct local-PC agent routing requires the engine health payload to advertise `agent_execution_v1`; older/incomplete builds fall back to AIDream. This gate prevents picker metadata from ever being mistaken for an executable agent definition. HTTP 422 labels also follow the structured server code: only `invalid_uuid` says “Invalid conversation ID”; agent-definition/model failures say “Agent execution failed.”

---

## Related features

- Depends on: `features/agents/` (execution-system, launcher), `features/agents/components/shared/` (`AgentConversationColumn`), `features/agents/components/agent-listings/` (`AgentListDropdown`).
- Cross-links: [`../../FEATURE.md`](../../FEATURE.md) (agents umbrella), [`../../docs/AGENT_INVOCATION_LIFECYCLE.md`](../../docs/AGENT_INVOCATION_LIFECYCLE.md) (where the agent≠conversation model originates), [`../../../conversation/FEATURE.md`](../../../conversation/FEATURE.md) (the *future* unified shell — not what this route uses), [`../../migration/phases/phase-07-chat-route.md`](../../migration/phases/phase-07-chat-route.md) (build log).

---

## Doctrine compliance

> Required by [PRINCIPLES.md](../../../../PRINCIPLES.md). The artifact is disposable; the platform is the product.

**Primitives reused**
- Redux slices / selectors: `conversationFocus` (`setFocus`, **`clearFocus`**, `selectFocusedConversation`), `conversations`/execution-system, `conversationList`, `conversation-history`, `surfaces`.
- Hooks: `useAgentLauncher`, `useCreatorOwnershipSync`, `useIsMobile`.
- Components: `AgentConversationColumn`, `AgentListDropdown`, `ChatHistorySidebar` (now a thin wrapper over the canonical `ConversationHistorySidebar`), `ConversationSourceFilterTree`, `PinnedAgentsSection`, `GhostTapButton`/`PanelLeftTapButton`, `Drawer`.
- Source filtering: `source-registry` (`SURFACE_DEFAULTS.chat`, `resolveSurfaceFilter`) + `useSurfaceSourceFilter` + `userPreferences.conversationFilters` — the chat surface declares its allow-list, it doesn't invent a filter mechanism.
- Thunks: `launchAgentExecution` → `createManualInstance`, `loadConversation`, `fetchConversationHistory`, `fetchSourceFacets`.

**Primitives introduced**
- None. The revival fix reused the existing `clearFocus` action on the `conversationFocus` slice; the `+` fix reused the existing `activeAgentId` prop. Dead "last-used agent" code (`selectLastUsedAgentId` + a `fetchGlobalConversations` mount fetch) was **removed**, not replaced.

---

## Change log
- `2026-07-17` — claude: **Chat Options densified (ui-dense pass).** Every quickset-style row (QuicksetPanel `Row`, `RunSettingsEditor` `SettingsRow` quickset variant, `SurfaceSimulatorSelect` new `quickset` prop) shares ONE `grid-cols-[9rem_minmax(0,1fr)]` label rail — keep the three in sync. All picker triggers are borderless (match the canonical `ModelListDropdown` trigger: `bg-transparent px-1 text-xs`, no border). **Context tab** now renders `ActiveContextTree` (the canonical compact picker, same as the header lens chip) — `ActiveContextPanel`+layers panel dropped from the tab. **Overrides tab** shows advanced settings expanded (no disclosure click). **Memory tab** renders `AgentMemoryInlinePanel` (new: the Memory window's exact hook + sidebar/body/footer as a master→detail swap — behavior can't drift from the window). **Sandbox tab** rewritten to a one-line bound-state strip (none / verifying / bound+scope tag / stale) + target list + New sandbox + "Only this conversation" checkbox; all wordy copy removed, logic unchanged. Quickset's duplicate Disable Tool Injection row removed (the shared `RunSettingsQuickControls` copy remains) and the empty Active Context row now hosts `ActiveContextLensChip`. **Mobile:** `RunControlsWindow` itself now renders `TabbedBottomSheet` (iOS-style) when `useIsMobile()` — the window overlay can no longer appear as a desktop window on a phone.
- `2026-07-17` — **Composer rail/toolbar de-dupe context + compute.** Removed `ContextLensBar` from `ConversationContextRail` (scopes + preview only in `+` attach menu); rail returns null when no pills. Toolbar `DesktopPresenceIndicator` renders only when a compute target is verified and bound — unbound connect via `PlusAttachMenu` → `ComputeLensBar`.
- `2026-07-17` — **`PlusAttachMenu` → `ComputeLensBar`.** The `+` attach menu now includes a condensed compute row (same pill pattern as `ContextLensBar`): Monitor **Compute** label, up to two inline chips (prefers bound target, local PC, then sandbox), `+N` overflow + chevron opens run-controls **Sandbox** tab. Hidden when sandbox binding is blocked (ephemeral / chat incognito). Shared binding logic: `useComputeTargetActions` (also refactors toolbar `DesktopPresenceIndicator`).
- `2026-07-17` — claude: **live-stream citation rendering.** The typed `citation` stream event (`CitationPayload {block_index, citation}`) is now consumed: process-stream's `isCitationEvent` branch validates each payload through `parseNormalizedCitation` (loud warn on contract violations; raw payload preserved in `rawEvents`), snapshots the last streaming text render block + its content length as the marker anchor, and appends to `ActiveRequest.liveCitations` (`appendCitation`). Derived — never stored — index: `selectLiveCitationIndex` → `buildLiveCitationIndex` (same dedupe/numbering core `addCitationToSources` as the settle-time index). Markers stamp streamed text at render only: `selectAccumulatedTextWithCitationMarkers` (plain streaming branch) and per-block `insertCitationMarkers` in `EnhancedChatMarkdown.renderGroupedSlot` (unified branch); `AgentAssistantMessage` feeds `MessageCitationsProvider`/`MessageSourcesRow` live sources while streaming, persisted index after reload. Stream commit stays CLEAN: `assembleMessageParts` writes marker-free `rawText` + typed `citations` onto committed text parts (server row remains source of truth). `NormalizedCitation` is now ALIASED from the generated `stream-events.ts` wire type (regen landed) — unknown kinds AND providers are contract violations, skipped loudly. Guard: `active-requests/__tests__/citation-live-stream.test.ts`.
- `2026-07-17` — claude: **settle-time citation UI (persisted messages).** Text parts' `citations` arrays (canonical `NormalizedCitation` contract, `docs/handoffs/citations-system.md`) now render as inline numbered superscript chips + a per-message "Sources" footer with click-through (Source Inspector at the exact PDF page for our files via the canonical `useOpenCitation`; new tab for web). Core: `features/agents/redux/execution-system/messages/message-citations.ts` (boundary parse of `TextPart.citations: unknown[]`, dedupe/numbering, marker insert/strip — the ONE marker implementation shared by BOTH content walkers: `extractFlatText({withCitationMarkers})` and `selectMessageInterleavedContent`). Render: markers travel as `<matrxcite n="…" />` in DISPLAY text only, converted inline by `remarkMatrxCite` + `CitationMarkerInline` (`components/mardown-display/chat-markdown/citations/`) — deliberately NOT a splitter block (a block would vertically break Anthropic's mid-sentence citation-split segments, the exact 2026-07-16 regression). Sources ride `MessageCitationsContext` from `AgentAssistantMessage` (which also renders `MessageSourcesRow`). `commitInlineContentEdit` strips markers so they can never persist. Plain flatten (copy/TTS/share) stays marker-free. Live-stream half shipped same day (entry above).

- `2026-07-17` — codex: pasted and selected image attachments now render in every agent composer as the same compact 56×56 thumbnail pattern used by User Feedback (durable `InlineMediaRef`, cover crop, hover-to-open overlay, corner remove control, and touch-visible removal). Non-image resources keep the existing compact `ResourceAttachmentTile`; upload, Redux ownership, send payloads, and post-submit rendering are unchanged.
- `2026-07-16` — **Multi-text-block messages render as one continuous string.** Providers may return an assistant message as MANY `text` blocks split MID-SENTENCE (Anthropic citation spans; first seen live 2026-07-16 — `", and "` rendered on its own line). `extractFlatText` now concatenates consecutive `text` blocks directly instead of joining with `"\n"`; non-text blocks keep the newline. The stored content was always faithful to the provider — this was render-only. Regression test: `messages/__tests__/extract-flat-text.test.ts`. Server side (aidream): `parse_content` now round-trips top-level `citations` into `TextContent.metadata` (was silently dropped on every client resend), and the Anthropic stream handler documents `citations_delta` (SDK accumulates them onto the final message for persistence).
- `2026-07-17` — **Composer rail → `ContextLensBar` + server-truth preview panel.** The rail's Eye/Context button and `ActiveContextLensChip` merged into ONE pill: `ContextLensBar` (`features/scopes/components/active-context/`). Its "Context" segment opens the `contextPreviewPanel` overlay (non-blocking `SidePanelSurface`, singleton) rendering `ContextPreviewPanel` (`features/agents/components/context-preview/`): the aidream `POST /ai/context/preview` response — the EXACT injected context block + tiered variables from the run code path — plus an "Attached this turn" tab (ported client-side entries view). The blocking `AgentSeesSheet` is DELETED; never reintroduce a Sheet for this.
- `2026-07-16` — **Composer rail Scopes pill → Lens Chip.** `ConversationContextRail` no longer opens `ContextItemDrawer` for layers; Eye/Context stays, and the old "Scopes" pill is replaced by `ActiveContextLensChip` (same Popover → `ActiveContextTree` as the chat header — never a drawer). Rail always mounts Eye + Lens Chip.
- `2026-07-16` — **Chat header context control → Lens Chip + ContextTree.** `ChatRunHeader` uses `ActiveContextLensChip` (Lens Chip trigger opens the promoted dense `ActiveContextTree`; Clear in the tree footer).
- `2026-07-15` — claude: **chat↔scope "ask on mismatch" pre-send gate.** A chat carries its scopes durably (its `conversation` tags); when the sidebar's active selection differs from them, `smartExecute` now ALWAYS asks — a 3-way dialog (Use current selection / Combine both / Keep chat's context; dismiss cancels the send, composer intact) instead of silently union-stamping the active selection. An empty sidebar sends the chat's own tags as this turn's `scope_ids` (never strips a chat's context). Gate: `features/scopes/redux/thunks/conversationScopeGate.ts` (runs before `markInputSubmitted`, same placement as the sandbox gate); dialog host: `components/dialogs/scope-mismatch/`; `executeInstance` gained `scopeIdsOverride` (used as the request's `scope_ids`; skips the post-send `syncConversationScopes` union). Skipped for ephemeral chats and manual (Agent Builder) mode. Full model: `features/scopes/FEATURE.md` §"Live delivery path".
- `2026-07-15` — claude (W1-D): **the chat transcript's block routing is now a declarative registry.** `BlockRenderer`'s ~40-case switch moved to `components/mardown-display/chat-markdown/block-registry/block-dispatch.tsx` — dispatch tables organized by content-vocab-crosswalk classification, compile-time exhaustive against the generated block-type unions, loud (`reportUnregisteredBlockType`) on any unregistered type instead of a silent markdown default. Routing stages in order: kind route (`applyIrKindRoute`) → pending-structured skeleton → unified artifact renderer → dispatch table. Rendering behavior and code-splitting unchanged (lazy `BlockComponents` intact). Adding a block type = classify + register in block-dispatch.tsx, never a new switch case.
- `2026-07-14` — codex: **made saved-agent Local routing capability-gated and corrected retry error labels.** A localhost health response must explicitly advertise `agent_execution_v1` before chat sends a saved-agent turn directly to Matrx Local; otherwise the existing AIDream path remains authoritative. Structured 422 codes now distinguish `invalid_uuid`, agent-definition/model failures, tool injection, and generic validation instead of relabeling every preparation failure as “Invalid conversation ID.”
- `2026-07-15` — codex: restored pasted-image and ordinary file attachments after the premature direct-browser `matrx-files` cutover sent authenticated uploads to an endpoint whose CORS preflight returns 405. The canonical `usePasteImageResource` flow is unchanged (upload → durable `file_id` → `image` resource chip); the shared Python client now keeps browser file requests on aidream until `NEXT_PUBLIC_FILES_BROWSER_CUTOVER=true` is explicitly enabled after CORS verification.
- `2026-07-14` — codex: **`/chat/new` first paint includes the composer.** The route fallback now renders the landing greeting, chips, and an input-sized shell while the client bundle resolves; `ChatRoomClient` also accepts landing content as a function of the minted conversation id so the real `NewChatLandingInput` can render during launcher initialization instead of appearing a beat later.
- `2026-07-14` — codex: **cold chat windowing now forces the local bottom window over reused Redux state.** A warm in-memory conversation could keep a previously revealed `visibleGroupLimit`, letting `/chat/[id]` paint from the oldest loaded group after the loader. `AgentConversationDisplay` now lets the cold-route local limit win, and resets its scroll snapshot/anchor bookkeeping per conversation id.
- `2026-07-14` — codex: **cold `/chat/[id]` now opens at the real scroll bottom.** The route keeps a transcript-shaped skeleton visible while `loadConversation` resolves, renders only the latest user group + latest assistant group first, bottom-aligns the cold transcript, and pins the scroll container to `scrollHeight - clientHeight` during the cold settle/reveal window. Cold DB assistant markdown gets a short skeleton settle window so `MarkdownStream` does not visibly assemble. The next four groups mount above while the viewport remains at bottom, and older loaded history is revealed two groups at a time on upward scroll before paging the DB. Live URL-promotion streams skip this path so in-flight responses are not clobbered.
- `2026-07-14` — codex: **Fork at this message no longer 23505s on multi-artifact assistant turns.** The action-menu path still calls `forkConversation` → `cx_fork_conversation`, but the production RPC now preserves `chat.artifact.artifact_index` and stamps the forked discovery row's `source_system/source_id` to the remapped message. This fixes the duplicate `uq_cx_artifact_source_natural_key` failure when one assistant message has multiple artifacts of the same discovery type (for example three `data_table` blocks). Migration `20260714161847_fix_cx_fork_artifact_indices.sql` applied and verified against the reported `/chat/0db57f91-7d03-4a3d-98b6-8adba05d0347` fork-at-position-65 case inside a rollback transaction.
- `2026-07-14` — claude: **attached documents now PERSIST across turns + reloads (durable association edge, not a per-turn request.context pointer).** An attached file/document was an ephemeral per-turn `instanceResources` chip whose pointer was derived into `request.context` and CLEARED on send (`clearSubmittedResources`) — so it vanished after turn 1. It is now a durable `platform.associations` edge to the conversation: `processed_document → conversation` (default when the file has a processed document) or `file → conversation` (the raw file); both registered + active, `container_side=target`. The FE's only jobs: create the edge on attach (`attach-resource.ts` → `useAttachResource`, via the `addAssociation` thunk), remove it on detach, and render the chip from the edge list (new `AttachedDocumentChips.tsx` reads the conversation's incoming edges via `useContainerLinks({ containerType: "conversation" })` — `linksFor("processed_document" | "file")` — and reuses `ResourceAttachmentTile` + `DocumentRepresentationPill`; wired next to `SmartAgentResourceChips` in all four composers: `SmartAgentInputStacked` / `SmartAgentInputSingleRow` / `NewChatLandingInput` / `CompactAssistantInput`). The BACKEND reads the conversation's association edges at call time and injects the context (parallel aidream change) — the FE ships **nothing** in `request.context` for attachments. Chosen Clean/Raw representation + origin `file_id` ride the edge `metadata` (`{ representation, file_id }`); a representation change is an idempotent re-attach (`assoc_add` replaces metadata). **Annihilated** the old per-turn path: `selectResourceContextPayload` + the `attached_document_*` request.context bridge (`instance-resources.selectors.ts`), `selectRequestContextPayload` (`instance-context.selectors.ts` — the three execution thunks now read `selectContextPayload` directly), and the processed_document branch of `SmartAgentResourceChips` + `attachProcessedDocument`/`switchAttachedDocumentToBinary` in `attach-resource.ts`. The binary `document` **bytes** path (vision / native PDF read) is untouched. **Label bug fixed:** the chip name is sourced from the file's real filename via `cleanDocumentLabel` (`attached-documents.ts`), never a signed-URL tail. Grew the shared primitive: `addAssociation` thunk + `useContainerLinks.attach`/`ContainerLink` now carry edge `metadata`. Wire contract: [`../inputs/resources/../../redux/execution-system/instance-resources/RESOURCE_WIRE_SPEC.md`](../../redux/execution-system/instance-resources/RESOURCE_WIRE_SPEC.md).
- `2026-07-12` — **block send while voice is recording/transcribing.** Submitting mid-recording dropped the trailing audio and left the mic/transcription running. Send button + Enter are disabled until the mic is idle across `SmartAgentInputStacked` / `SmartAgentInputSingleRow` / `NewChatLandingInput` / `CompactAssistantInput` (`onRecordingStateChange` → `voiceBusy`).
- `2026-07-11` — claude: **fixed the source-filter popover's "Show all" dead end + the default-stomps-your-selection bug.** The footer button read "Show all" but ran `clearAll` — correct semantics (empty allow-list = no filter = show everything) with a UI that unchecks every box and then disables the button (`activeCount === 0`), so there was no way back. It's now a live two-way toggle ("Select all" ⇄ "Clear filter", never disabled). `FeatureRow` also nested a `<button>` (the `TriCheckbox`) inside the row's own `<button>` — invalid DOM on exactly the leaf rows; the row is the control now, with a presentational `TriBox`. **Load-bearing:** the surface DEFAULT filter is seeded ONCE per default value (`seedScopeSourceFilter` + `scope.seededSourceKey`), not re-asserted on every mount of `ConversationHistorySidebar` — previously any remount of the popover/window overwrote the user's tree selection with `SURFACE_DEFAULTS[surfaceId]`. Two writers, one owner: the default writes on first sight, the tree owns every write after that.
- `2026-07-06` — claude: **`agent-fs` client capability toggle + raw request-override escape hatch in "Chat Options → Settings".** Wired the durable, `code.code_files`-backed agent filesystem (aidream `agent-fs` capability, `payload_model=None`) into the FE capability registry: new `agent-fs.provider.ts` (registered in `register-all.ts`), a `stateless` flag on `ClientCapabilityProvider` so the builder declares it in `client.capabilities[]` with NO `state` entry (matches the verified-good wire shape), and `"agent-fs"` added to `ClientCapabilityName`. Activation is gated **solely** on a new per-conversation `builderAdvancedSettings.agentFs` toggle (default **OFF** — never auto-armed; not rolled out), surfaced admin-only in `RunSettingsEditor` under "Experimental capabilities". Applies to both the chat (`executeInstance`) and Builder (`executeManualInstance`) paths via `buildToolInjection`. Also added a general **request-override escape hatch**: `builderAdvancedSettings.requestOverrides` (raw JSON, admin-only textarea) is shallow-merged onto the outbound body just before POST in both thunks (`resolveRequestOverrides`/`parseRequestOverrides` in `utils/request-overrides.ts`); invalid JSON is skipped loudly (toast + inline editor validation), never partial-merged. Live-verified end-to-end against prod `/ai/chat`: the agent's `echo/cat/ls` wrote `/home/agent/projects/x.md` to `code.code_files` (per-user, folder tree `home→agent→projects`, no uuid in any path). Unit test: `client-capabilities/__tests__/agent-fs-capability.test.ts`.
- `2026-07-16` — Chat Options now opens as a compact 480px WindowPanel inset 72px from the left edge instead of a centered 860px panel, leaving the conversation visible and interactive. Quickset is its first/default tab: its controls are shortcuts into the same per-conversation execution state as the full tabs (no parallel state), and Settings keeps descriptions in a bottom reference section rather than alongside controls. Memory and full-prompt windows open above Chat Options without closing it.
- `2026-07-06` — claude: **Per-conversation scratchpad opt-in + tiny rail pills (canvas toggle + floating X) + 4-row `+` menu.** The scratchpad is no longer auto-attached to every chat: publication is gated on a per-conversation opt-in (default OFF), toggled in `ContextDocsMenu` (ScratchRow now has a real Switch → `setScratchpadGateThunk`) and the rebuilt `PlusAttachMenu` (FOUR rows: model — always shown / Working doc + Scratchpad switches / active context `ActiveScopeChips` + edit / Advanced settings — deep link fixed to the Settings tab). Rail pills compacted to icon + ONE word with a rich shadcn Tooltip (full name, status, click hint) and a hover-reveal floating X: doc pill X = the opt-out switch; generic entry X = `removeContextEntry`; lists/layers have no X. **Doc pill click = canvas visibility toggle** (open/switch/close the `wd:<scope>:<kind>` canvas item, shared with `ChatCanvasButton`) — the doc detail sheet is reachable via ContextDocsMenu → Open instead. Attachment chips use `ResourceAttachmentTile variant="compact"` (same pattern; sent messages keep the two-line tile). Full model: `features/agents/FEATURE.md` → Working document & scratchpad.
- `2026-07-05` — claude: **ConversationContextRail pills toggle their detail panel.** Clicking a context pill (working doc, scratchpad, tasks, layers, ad-hoc slots) now closes the side panel when the same pill is clicked again; opening a different pill closes the previous surface. Active pills get a subtle ring highlight.
- `2026-07-05` — claude: **rail document pills read the editor slice; scratchpad is user-global.** `ConversationContextRail` doc pills no longer derive from `instanceContext` truthiness (an enabled-empty scratchpad phantom-pilled): working pill = `selectWorkingDocEnabled`, scratch pill = active scratchpad has content OR extras attached (`+N` badge); both open `ContextSlotDetailSheet`, which routes every doc key to the EDITABLE workspace via `docKindForContextKey` (the scratch pill's readonly JSON dump is dead). `ContextDocsMenu` scratch row is toggle-less (always on, empty-not-sent); run-controls Document tab = `DocumentsWorkspace`. Scratchpad model: `features/agents/FEATURE.md` → Working document & scratchpad.
- `2026-07-05` — claude: **fixed the "can't leave `/chat/new`" bounce (URL snaps back to `/chat/new`/`/chat/a/[id]` mid-stream, conversation lost until history).** Root cause was NOT a frontend chat change — the backend switched (verified in-DB, ramping 2026-07-05→06) from inserting the `chat.conversation` row up front to persisting the whole turn (conversation + user + assistant message) **atomically at stream-end**. The post-submit promotion fired at `messageCount >= 2` (driven by `record_reserved`, which only *announces* reserved UUIDs — not a commit), `router.replace`'d to `/chat/[cid]`, and the SSR seed lookup (`resolveConversationSeed`) missed the not-yet-committed row → `redirect("/chat/new")`. (`agents/[id]/run` was immune because it never promotes the URL.) Fix: new primitive `waitForConversationPersisted` (`conversations/conversation-persistence.ts`) mirrors the SSR read exactly and gates the promotion `router.replace` on the row being committed & readable — backend-timing-agnostic, makes "promoted-before-committed" structurally impossible. Promotion trigger changed from raw `messageCount` to a `readyToPromote` boolean (no churn on later same-turn messages); `promotionWaitRef` dedupes the async waiter.
- `2026-07-04` — claude: **rationalized the ENTIRE user-message edit/fork/resubmit surface + fixed the "Create a fork → `Invalid conversation ID`" bug + the "fork ends on my own message" dead-end.** Three coordinated fixes:
  - **The fork bug.** "Create a fork" forked (INCLUDING the edited message as the pending turn), edited it, **navigated** to the new `/chat/[id]`, then did `setUserInputText` + `executeInstance`. Navigation remounts the page and resets the input slice, so `assembleRequest` sent no `user_input`; the backend saw neither `user_input` nor `retry` → hard reject. Manual **Retry** worked because it uses `executeInstance({ retry: true })`. Fix: every re-run outcome leaves the edited message as the pending turn and fires `retry: true` — re-RUN, never re-SEND (also kills a latent double-post in the Overwrite path). Race-free: the fork hydrates `messages` in memory, so `ChatRoomClient` skips `loadConversation` and won't clobber the stream.
  - **One canonical surface, no drift.** New shared primitives: `USER_EDIT_ACTIONS` + `routeUserEditAction` (`message-options/userEditActions.ts`), `forkAndResubmitFromMessage` + `editMessageText` thunks. The three genuinely-distinct outcomes of editing a user message — **Save only** (edit in place, no re-run) · **Save & resubmit** (overwrite + re-run here) · **Fork & resubmit** (branch + re-run) — now render as footer buttons in ONE editor (via `FullScreenMarkdownEditor`'s new `primaryActions`/`onPrimaryAction` + the editor callback group's `onAction`), with NO confirmation dialog. There is deliberately no 4th outcome: a fork that doesn't re-run is a dead-end. All THREE entry points share the primitive: the inline **pencil** (Edit) and **paper-plane** (Edit & resubmit) buttons — the paper-plane, wrongly removed earlier in this session, is restored — and the options-menu **Edit & resubmit** item. Deleted `EditResubmitOutcomeDialog`.
  - **Killed the dead-end fork.** The options-menu "Fork at this message" on a USER message forked-and-stopped, leaving a branch ending on an unanswered question. Replaced with **Fork & regenerate from here** (`forkUserMessageItem` → `forkAndResubmitFromMessage`, no edit) so it always produces an answer. Assistant messages keep the plain "Fork at this message" (already ends in a reply). Removed the user menu's old plain "Edit content" (saved silently with no resubmit choice — the "didn't ask me" complaint).
  Backend note: the old "retry backend gap" is closed — the deployed error text advertises `retry=true` and it works.
- `2026-07-02` — claude: **working-document canonicalize STEP 3 applied — legacy columns + junction dropped.** `workbench.working_documents` dropped its legacy `conversation_id`/`user_id` columns (provenance now rides `metadata.origin_conversation_id`; ownership is `created_by`), and the bespoke `chat.conversation_documents` junction was retired to `graveyard` (chat↔doc links already live in `platform.associations` as `working_document → conversation` edges, opt-in flag + doc kind on edge metadata). FE was already on `associationsService` via `cx-working-document.service.ts`; the one runtime break — an explicit `conversation_id` select in `listRecentUserDocuments` — was removed (reads `metadata.origin_conversation_id`). Migration ledgered, `pnpm db-types` + schema snapshot regenerated. Applied in lockstep with the aidream co-requisite (its boot `schema_check` baseline + CRITICAL_TABLES + ORM models updated) so the backend redeploy won't fail its drift gate. See `docs/db_changes/canonicalization_worklog.md` §6.
- `2026-07-01` — claude: **chat sidebar history header + pinned chats.** Consumer `ConversationHistorySidebar` now labels the source-filter row `Filtered Chats` with the filter icon on the right (mobile + desktop). Pinned (favorited) conversations render in a collapsible `Pinned` section above date buckets, with `Show all` after five rows; pinned rows are excluded from the date groups so they don't duplicate.
- `2026-07-01` — claude: **guest `/chat/new` no longer logs working-document auth errors.** `useConversationDocumentsBridge` + `hydrateConversationDocumentsThunk` skip persisted-doc hydration when `selectUserId` is null — guests have no association edges to restore, and the old path hit `requireUserId()` → console `"Not authenticated"` + `"[working-document] hydrate: list links failed"`.
- `2026-07-01` — claude: **ChatRunHeader left/right split at all breakpoints.** Agent picker + icon-only context stay pinned left; Canvas pinned right inside the center inject (`w-full justify-between`, not `lg:w-full` + centered shrink-wrap). Fixes mobile/tablet header collapse into the avatar.
- `2026-06-26` — claude: **moved conversation sharing/ownership onto the canonical `cx_conversation` columns (`is_public`→`visibility`, `user_id`→`created_by`).** The DB canonicalized `cx_conversation`: RLS now enforces the `platform.visibility` enum (`private < internal < link < public`) via `iam.has_access`, `created_by` is the trigger-stamped owner, and `is_public`/`user_id` are DEPRECATED (RLS ignores them, DB will drop them). FE swap: (1) **Ownership reads** now use `created_by` — the load-conversation owner-only artifact-reconcile gate (`thunks/load-conversation.thunk.ts`), the share-modal owner check (`utils/permissions/service.ts#isResourceOwner` for `cx_conversation`), and the "my conversations" history filter (`features/public-chat/services/cx-chat.ts#getUserChatHistory` → `.eq("created_by", …)`). (2) **Sharing read/write** for `cx_conversation` now goes DIRECTLY to the `visibility` column (owner-UPDATE RLS allows it; public-SELECT policy serves anonymous): `getResourceVisibility`/`makePublic`/`makePrivate` special-case the type via `VISIBILITY_ENUM_RESOURCE_TYPES` — the old `make_resource_*` RPCs only set the now-ignored `is_public`, so the public toggle was a silent no-op for access. No DB migration (the shareable-resource registry + RPCs still name `user_id`/`is_public` for other resource types; parity test untouched). (3) Conversation records mirror both `visibility`/`createdBy` (canonical) and the deprecated `isPublic`/`userId` (`instance.types.ts`, `conversation-bundle.ts`, load + fork thunks, `conversations.slice.ts`). Canonical `ConversationVisibility` type + `isConversationShared`/`isConversationPublic` helpers live in `features/cx-chat/types/cx-tables.ts`. (`types/database.types.ts` not yet regenerated for the new `cx_conversation.visibility` column — db-types deferred.)
- `2026-06-25` — claude: **fixed embedded conversation lists routing the whole page away (follow-on to the 2026-06-16 `href` change).** Giving sidebar rows an `href` (for cmd-click new-tab) meant a plain click ALWAYS navigated via the `ItemRow` `<Link>` — so every surface that hosts the conversation itself (the `QuickChatSheet` drawer, the `/code` `ChatHistorySlot`, `ChatHistoryWindow` + its embedded workspace, `AgentAppChatShell`) loaded the conversation in place AND routed the browser to `/chat/<id>`, yanking the user off their page. Added a generic `openInPlace` flag to the `ItemRow` primitive (`components/official/item/`): when set, a plain left-click calls `onOpen` and `preventDefault()`s navigation, while the `href` stays for modifier/middle-click new-tab + "Copy link". Threaded through `ConversationHistorySidebar` (both variants) → `ChatHistorySidebar`; opted in the four self-contained surfaces above. The standalone `/chat` route (`ChatSidebarMenu`) + its Search Chats popover keep navigating (default `openInPlace=false`); the floating-chat widget (`AgentChatHistorySidebar`) was already pure-navigation (no in-place loader). Presence of `onOpenConversation` does NOT imply in-place — the flag is explicit.
- `2026-06-23` — claude: **fixed sent image attachments stuck in the composer + missing from the user bubble.** Two bugs in shared execution code, surfaced on `/chat/new` and every SmartAgentInput. (1) **Input chips lingered after send** — text hides on `submissionPhase === "pending"` but `SmartAgentResourceChips` kept rendering until stream success called `clearAllResources`, so pasted/attached images looked like they never left the box. Chips now hide during `"pending"` (resources stay in Redux for retry/resend). (2) **Bubble showed no attachment chips** — the optimistic user message stores API wire blocks (`{ type: "image", file_id, … }`), but `normalizeContentBlocks` only understood cx_message `{ type: "media", kind: "image" }`, so attachments became `unknown_data_event` and `AgentUserMessage` dropped them. Added wire→media coercion in `normalize-content-blocks.ts`; `extractBlockFileId` now reads `fileId`/`file_id` from normalized `image_output.data` so `FileResourceChip` thumbnails render.
- `2026-06-23` — claude: **fixed pasted images on the `/chat/new` hero input.** `NewChatLandingInput` is a bespoke ChatGPT-style composer (not a thin wrapper around `AgentTextarea`) and had never wired `useClipboardPaste` — clipboard images pasted into the landing textarea did nothing while the standard `SmartAgentInput`/`AgentTextarea` path uploaded via `useFileUpload`, stamped `instanceResources`, and rendered chips. The hero now mirrors `AgentTextarea`'s paste handler (`useClipboardPaste` + `addResource`/`setResourcePreview` + `SmartAgentResourceChips` already pinned at the top), so a pasted image shows as a chip and is sendable with or without prose.
- `2026-06-23` — claude: **fixed user-message collapse: the WHOLE bubble collapses, not just the text.** The collapse logic in `AgentUserMessage` (`components/messages-display/user/`) had drifted so it only measured/clamped the text body — the "top section" (the first-turn **variables** strip, which is usually the *largest* block of text in a user turn, plus context-slot chips and attachment chips) sat OUTSIDE the collapsible region, so a giant variables block rendered fully expanded while only the message text collapsed. Restructured the bubble so the **entire inner stack** (variables → context chips → attachment chips → text) is one measured + clamped unit: `measureRef` now lives on the clamped container itself (`scrollHeight` reports full height through the `max-h` clamp, so the off-screen text-only sizer was dropped), and `useCollapsibleMessageText` now takes an opaque `contentKey` signature fingerprinting every section (variables JSON + context keys + attachment keys + text) rather than text alone — so a change to any section re-evaluates collapse, and async variable/chip hydration is caught by the existing `ResizeObserver`. **Always defaults collapsed on BOTH paths** (live optimistic submit AND DB reload — both render the same component via `selectMessageById`, hook defaults `isCollapsed=true`), and only ever opens on a physical user click (`userToggledRef` blocks auto re-collapse after that). Applies identically in the chat-assistant widget (`AssistantCardStack`, `compact`). **Invariant (documented in the hook + component): everything inside the bubble counts toward sizing and collapses together — do not move any section out of the measured region.**
- `2026-06-23` — claude: **wired the `matrx-user/chat` surface into agent-context (this folder's scope).** Added `agent-context/buildChatContextData.ts` — the surface's pure emit contract (`buildChatContextData` → `createChatScope`, all 20 manifest values; `CHAT_CONTEXT_MENU_PROPS` with `sourceFeature: "chat-route"` + `surfaceName: "matrx-user/chat"`). The `/chat/new` hero composer (`NewChatLandingInput`) now carries the right-click agent menu: the textarea is wrapped in `UnifiedAgentContextMenu` (`asChild` merges handlers onto it — no wrapping DOM, so the grid layout / sticky auto-expand / send-on-enter / `+`-controls / mic / guest auth gate are all untouched), with a live `getApplicationScope` over the draft and `onTextReplace`/insert routed through the bound `setUserInputText` slice. The hero is intentionally NOT swapped to `ProTextarea` (it is a bespoke ChatGPT-style pill with its own voice + controls; ProTextarea would break the layout and duplicate voice). **Out of this folder's scope (reported, not changed):** the live `/chat/[id]` + `/chat/a/[id]` composer (`AgentTextarea`, `components/inputs/smart-input/`) carries no `surfaceName`; the presentational display (`AgentConversationDisplay:589`, `components/messages-display/`) passes no `surfaceName` to `MarkdownContextMenuProvider`, so it defaults to `matrx-user/assistant-message` instead of `matrx-user/chat` — both need wiring in their own folders.
- `2026-06-22` — claude: **unified ConversationContextRail above the composer (everywhere).** Added one compact, modern strip that sits at the top of the SmartAgentInput shell (both stacked + single-row) and surfaces everything attached to the conversation that the user can open — **working document, scratchpad, agent lists (plan/tasks/todos), active context layers (org/scope/project/task), and any other live context entry** — so they're openable without scrolling the transcript to find the message that introduced them (the exact gap left when the composer chips were collapsed into the `ContextDocsMenu` dot). Each pill reuses the existing detail surface (`ContextSlotDetailSheet` → editable `WorkingDocumentPanel` for the working doc; `TaskPanel` for lists; `ContextItemDrawer` for context layers) — no new detail UI. It renders **null** when there's nothing to show (zero footprint), so it's safe in every SmartAgentInput everywhere — not just this column. Mobile-friendly: high-priority pills stay inline, the rest fold into a clean "…" overflow `DropdownMenu` (`maxInline` = 2 mobile / 5 desktop). The standalone `TaskPanelChip` row was **removed** from `AgentConversationColumn` (subsumed by the rail; `PendingAsksZone` stays as-is). New component: `inputs/smart-input/ConversationContextRail.tsx` — the single rail; adding a future source (artifacts, canvas items) is one push into its `items[]`. `TaskPanelChip` is now the rail's hydration owner for `agentLists` (hydrate + realtime subscribe moved into the rail).
- `2026-06-22` — claude: **note drawer view modes + attachment/context dedup on user bubbles.** Sent note chips open with full `NoteViewControls` (Preview, Split, etc.). Stopped rendering `model_context.input_items` in the context strip — attachments show only on the attachment chip row; context strip is ambient/slot context only. See `../context-items/FEATURE.md`.
- `2026-06-22` — claude: **fixed empty drawer when clicking a sent note/task attachment chip.**
- `2026-06-22` — claude: **chat composer gains a compact docs-&-context menu; documents are now opt-in + add a private scratchpad.** New `ContextDocsMenu` toolbar icon sits next to the `+` in `InputActionButtons` (matches `InputButton` styling; one primary dot when the working document, the new **scratchpad**, or active scope context is on). It toggles + cross-conversation-links both docs and embeds the canonical Surface-A `ActiveContextPanel`. The intrusive composer chips (working-doc + scope-context pills) were removed from `SmartAgentResourceChips` — that state is now the single dot. Working document + scratchpad are **opt-in (off by default) and persist across reloads** (new `cx_conversation_documents` junction). Full architecture + DB in [`features/agents/FEATURE.md`](../../FEATURE.md) → "Working document & scratchpad".
- `2026-06-20` — claude: **fixed first-turn variable strip missing on Agent Builder test runs.** `FirstTurnVariables` (display-only prefix on the first user bubble) reads `userValues`, not definition defaults. Run mode stamped resolved variables via `executeInstance` on turn 1; Builder manual mode (`executeManualInstance`) sent them in the payload but never stamped `userValues`, so the strip stayed empty unless the user manually edited every field. Manual path now mirrors the agent path before the optimistic bubble.
- `2026-06-20` — claude: **fixed "giant gaps between tool calls and text" — an agentic turn now renders as ONE unit.** API providers reserve a new `cx_message` per iteration AND a `role:"tool"` row per tool result; in V2 `position` order a turn is `assistant → tool → assistant → …`. `AgentConversationDisplay`'s `displayGroups` pass flushed its turn group on every non-assistant entry, so each interleaved `tool` row **fragmented one logical answer into several stacked `AssistantTurnGroup`s**, each separated by the between-turn `space-y-6` gap (and pre-fix, each with its own action bar) — the "giant gaps" report. Fix: `tool`/`system` rows are filtered from `displayEntries` (they never render standalone — tool results inline onto the preceding assistant's tool_call segments via `selectMessageInterleavedContent`, system rows were already display-skipped). Each turn is now a single group whose members render 4px-flush. Verified live: a 5-iteration turn collapsed from 5 gapped groups to one seamless answer; 6 multi-member turns merged in an 18-message conversation, no regression on single-tool turns. The companion fold-consecutive-tool-calls behavior lives in [`features/tool-call-visualization/FEATURE.md`](../../../tool-call-visualization/FEATURE.md) (`ToolCallBatch`).
- `2026-06-19` — claude: **fixed the "context indicator is lying for past turns/conversations" bug (dangerous).** Each user bubble's `ContextSlotChipStrip` (`AgentUserMessage`) read the **live, conversation-level** `instanceContext`, so every historical user message — across every conversation — displayed the *current* context as if the model had received it. Now `executeInstance` freezes the turn's real context entries onto the optimistic user message's `metadata.context_snapshot` at submit, and `AgentUserMessage` renders **only** that snapshot (`ContextSlotChipStrip entries={...}`) with **no live fallback** — no snapshot → no chips (honest). `ContextSlotChipStrip` gained an optional `entries` prop; passing it pins the strip to a frozen snapshot, omitting it keeps the live-state behavior for "next request" surfaces. Backend follow-up tracked as FOUND_DEFECTS **D11**: persist `context_snapshot` onto `cx_message.metadata` so it survives reload (until then reloaded historical bubbles show no context rather than a fake one).
- `2026-06-16` — claude: **fixed cmd/ctrl+click on chat sidebar navigation.** The `+` (New chat) row, pinned agents, conversation history rows, and agent-search dropdown rows were `<button>`/`router.push` paths that couldn't open in a new tab. New chat + pinned agents are now `<Link href>`; history rows pass `href` into `ItemRow` (modifier clicks use native `<a>` semantics); the search-agents dropdown passes `navigateTo="/chat/a/{id}"` so `AgentRow` cmd+clicks land on the chat agent route, not `/agents/[id]`.
- `2026-06-15` — claude: **conversation source filtering (app → feature) + canonical list consolidation.** Added an ALLOW-list source filter to conversation history so each surface defaults to only its own conversations: `/chat` shows real chats (`source_feature ∈ chat-route / chat-interface / quick-chat`), and the firehose of transcription-cleanup runs, sub-agent spawns, server automations, and voice transcripts is hidden until the user opts in. New: `source-registry.ts` (apps/features/groups + per-surface `SURFACE_DEFAULTS`), positive include filters on the `conversation-history` slice/thunk (`includeSourceApps`/`includeSourceFeatures`/`includeEmptySource` via `setScopeSourceFilter`, server-side `.in()`/`.or()`), `get_cx_conversation_source_facets` RPC + `fetchSourceFacets` (counts powering the tree), the `ConversationSourceFilterTree` popover (app › group › feature checkbox tree, indeterminate parents, reset-to-defaults), a user-editable `userPreferences.conversationFilters` module + `ConversationFiltersTab` settings tab, and the `useSurfaceSourceFilter` hook. **Consolidation:** `ConversationHistorySidebar` is now the ONE canonical list primitive (`variant: "consumer" | "dense"`, built-in `surfaceId` source wiring); `ChatHistorySidebar` collapsed into a thin `variant="consumer"` wrapper; `ChatSidebarMenu` swapped its hard-coded `excludeSourceFeatures=['voice-agent']` deny-list for `surfaceId="chat"`; the cross-agent `ChatHistoryWindow` browser migrated off its ~400-line inline list + global pipeline onto the same primitive (`agentIds: []`, `surfaceId="history-window"` = browse-everything, date/agent grouping + source tree + search built in), keeping its warm-up load sequence and read-only main pane. Agent build/run lists left unfiltered by design. The `ChatHistoryWindow` agent multi-select popover was retired in favor of group-by-agent + the source tree (follow-up: re-add agent-subset filtering to the dense primitive if needed).
- `2026-06-15` — claude: **fixed launch variables: shown on every turn with a stale default, then missing entirely after reload.** The first-turn variable values (e.g. `Topic: …`) were baked into the optimistic user-bubble text by `executeInstance`. That broke two ways: (1) the prefix re-rendered on *every* follow-up bubble showing the agent's stale default the model never received (continuation payloads omit `variables`), and (2) the DB persists only the raw `user_input`, so on reload the first-turn variables vanished. Removed the content baking entirely. Variables are now a display-only strip (`FirstTurnVariables`) rendered once, on the genuine first user message (`selectFirstMessageId` && `!hasMoreOlder`), sourced from the instance variable slice's `userValues`. To make live identical to reload, `executeInstance` now stamps the exact `payload.variables` it sends into `userValues` on turn 1 — the same field `loadConversation` fills from `cx_conversation.variables`. No new primitive: reused `selectUserVariableValues` + `formatVariableDisplayName`/`variableValueToDisplay`.
- `2026-06-15` — claude: **fixed invisible attachments/inclusions on the `/chat/new` hero input.** The landing composer (`NewChatLandingInput`) opened the same `RunControlsMenu` Attach panel as the standard input — resources (notes, tasks, files, webpages, …) wrote to the SAME `instanceResources[conversationId]` Redux state — but the landing input never rendered the resource strip, so anything attached before the first message was silently invisible. Pinned `SmartAgentResourceChips` (the exact component + selector the standard `SmartAgentInputStacked` uses) at the top of the landing pill; renders nothing (zero extra height) when empty. Also made the hero input sendable with attachments alone (no prose) to mirror the standard input's send gate — an attached note with no text is a valid first turn. No new primitive: reused `SmartAgentResourceChips` + `selectInstanceResources`. **Same pass: killed the single↔multi-line flutter** on the hero textarea — collapsing back to the inline layout widened the textarea (narrow `1fr` grid column → full width), un-wrapping the text and shrinking the height, which collapsed it again, oscillating on nearly every keystroke near the wrap boundary. Expansion is now sticky/monotonic (expand on first wrap, only reset to single-line when the field is emptied), and the auto-resize effect re-measures at the new width right after switching (no stale blank line).
- `2026-06-14` — claude: **also fixed "Edit & resubmit → Fork does nothing" for any message past the first turn.** Separate, pre-existing bug surfaced once the editor save worked: `handleResubmitChooseFork` forked at `messagePosition − 1` (EXCLUDING the edited message) then tried to find+edit that message *in* the fork — it's only present when the message is at position 0, so for mid-conversation messages the lookup failed and the flow aborted after silently creating an orphan fork (no navigation). Now forks at `messagePosition` (includes the edited message), edits it in place, and re-runs — mirroring the working Overwrite path. Verified in-browser on a 2-turn conversation: forking the 2nd user message produces a clean branch (`Turn one`/reply preserved, `Turn two` edited + re-run, exactly 2 user bubbles, no duplicate), persisted through reload, original untouched. Also swept the same severed-`onSave`-in-data pattern out of `rich-document` edit/fullscreen-editor/server-api handlers + `PromptSystemMessage` (callback-registry or self-handle); the admin `srv-replace-with-summary` item now uses a callback group.
- `2026-06-14` — claude: **fixed Edit / Edit & resubmit doing nothing + restored the whole message-edit/fork/resubmit family.** Root cause was upstream of chat: the overlay controller hard-coded `fullScreenEditor.onSave={undefined}` (a function can't travel through Redux, but the replacement callback channel was never wired), so the editor opened, the user typed, hit Save — and nothing happened. Same severing silently broke "Edit content" (overflow menu) and "HTML preview → Save". Fix: made `fullScreenEditor` callback-aware (`features/overlays/callbacks/fullScreenEditor.ts` via `callbackManager`); the editor bridge now (1) emits to the caller's callback when present — "Edit & resubmit" uses this to open the fork-vs-overwrite dialog — else (2) self-handles via `editMessage` for any `conversationId`+`messageId` (plain "Edit"/"Edit content"), preserving the message's non-text blocks (`mergeEditedText`). `UserActionBar` now opens the editor through the typed opener (no raw `openOverlay`). Also **created the two RPCs the resubmit/delete paths had referenced for months but that never existed in the DB** — `cx_message_soft_delete` + `cx_truncate_conversation_after` (`migrations/cx_message_soft_delete_and_truncate.sql`, applied + `db-types` regen) — so the **Overwrite** branch and **Delete message** now complete server-side; removed their dead "RPC missing" fallback. Deleted the dead, broken `editAndResubmitItem` menu factory (it carried the exact severed-`onSave`-in-data landmine). See [`../../../overlays/FEATURE.md`](../../../overlays/FEATURE.md) for the severed-callback bug class.
- `2026-06-19` — claude: **attachment/context chips now open ONE interactive drawer (registry-driven).** Every resource/attachment chip — pre-submit (`SmartAgentResourceChips`) and post-submit (`AgentUserMessage`) — routes its click through the new shared `ContextItemDrawer` (`../context-items/`, on `MatrxDynamicPanelHost`, right/resizable). Replaced `AgentUserMessage`'s placeholder `BlockModal` (a `JSON.stringify(block.raw)` dump for every non-image type) entirely. Notes/tasks/working-document are now **editable in place** via their native editors; webpages get a live iframe preview; media renders through `InlineMediaRef`; every other type gets a readable `GenericBody` (no raw JSON as the primary view). Multi-record chips flatten to one drawer item each — prev/next + a bottom thumbnail rail page through them. Edited already-sent attachments surface a "Send updated version" action (re-attaches via `addResource` for the next turn). The live working document is now editable from `ContextSlotDetailSheet` too (renders `WorkingDocumentPanel` for the `working_document` key). New types get custom UIs by adding a `ContextItemTypeDef` in `../context-items/registry.tsx` — see that folder's FEATURE.md.
- `2026-06-12` — claude: **RunControlsMenu Context tab wired** — replaced the placeholder with `ActiveContextPanel` (same `ContextAssignmentField` + live-apply as `ChatRunHeader`'s `ActiveContextButton`; `checkboxVariant="standard"`). Tab shows a primary dot when `selectHasActiveContext`; working context counts toward the gear/plus customization badge.
- `2026-06-10` — claude: **incognito blocks sandbox binding.** Chat incognito now lives in Redux (`chatIncognito` slice). `resolveAgentSandboxRef` skips all bindings on `chat-route` when incognito is active; `RunControlsMenu` hides the Sandbox tab; `SandboxPanel` shows a disabled explainer. `ChatRoomClient` stamps `isEphemeral` on the live instance (and passes it to `useAgentLauncher`) so execute thunks send `store:false`.
- `2026-07-12` — claude: **the sandbox pre-send gate no longer fires on conversations that never had a sandbox.** On `/chat/new` the first message was blocked by `SandboxGateHost` ("This conversation is bound to a sandbox…") for a conversation that had never had one. The gate asked "is this bound?" via a resolver whose *second* level was the persisted `activeAgentSandboxBySurface["chat-route"]` **preference** — set once long ago, pointing at a since-dead box, and never cleared by anything. A default meant for *future* conversations was answering a question about *this* conversation's history. Fixed by making the **conversation record the single source of truth**: `sandboxBinding` (← `cx_conversation.sandbox_instance_id`, the same column the server checks) is hydrated identically for locally-created and fetched conversations, and `getConversationSandboxBinding()` is the ONLY thing the gate reads. The surface preference is now an explicit **seed** (`getSurfaceSeedRef`) that arms an unbound conversation and is **promoted onto the record + written to the DB** on first live use; a dead seed warns loudly and the turn goes out unbound rather than gating. Every user decision (detach / attach different) now **persists before the request goes out** — "send without sandbox" writes `sandbox_instance_id = null` and clears the seed, so the server's own check can't disagree. Also fixed: the metadata mirror dropped `kind`/`name`, so a **local-PC binding lost its `kind` on reload** and became unroutable; and `SandboxPanel` called `useAppSelector` *after* an early return (conditional hook). Guarded by `lib/sandbox/__tests__/sandbox-gate.test.ts`; full write-up in [`docs/sandbox/BINDING_POSTMORTEM.md`](../../../../docs/sandbox/BINDING_POSTMORTEM.md) §4c.
- `2026-06-28` — claude: **fixed sidebar `+` reviving the previous conversation.** Plain `<Link href="/chat/new">` only changed the URL; `useAgentLauncher` minted the conversation id on the first render from stale surface focus (before `clearFocus` ran), so clicking `+` from `/chat/[id]` often showed the old transcript. Added `beginFreshChat()` (clear focus + bump `chatRoute.freshSessionNonce` + route to the active agent's fresh surface), `preferFresh` / `freshSessionKey` on the managed launcher, and wired `ChatSidebarMenu` to the primitive.
- `2026-06-10` — claude: **incognito UI toggle (sidebar + avatar blackout).**
- `2026-06-10` — claude: **removed header `+` (new chat lives in sidebar); added incognito ghost affordance on fresh chat routes.**
- `2026-06-10` — claude: **agent picker tabs (Mine / Shared / All / System).** `AgentListContent` (via `AgentListDropdown` in `ChatRunHeader` + chat sidebar) now shows ownership tabs with badge counts. `makeSelectFilteredAgents` routes the System tab through builtin agents (`fetchAgentsListFull` data already loaded by `initializeChatAgents`); user tabs unchanged. Fav/category/tag filter chips hidden on System tab.
- `2026-06-10` — claude: **fixed draft loss when switching agents via the header picker.** Typing into the `/chat/new` landing input (or any chat input) and then changing agents through the `ChatRunHeader` `AgentListDropdown` destroyed the in-progress text — the picker's `handleAgentSelect` just `router.push`'d to `/chat/a/[id]` with no draft hand-off, while the `/chat/new` quick-action chips already used the `stashChatDraftTransfer` → `consumeChatDraftTransfer` round-trip. `handleAgentSelect` now reuses the SAME primitive: snapshot the source surface's draft (`chat-route:<activeAgentId>` input/display focus → `selectUserInputText` via `store.getState()`, no per-keystroke subscription) and stash it for the destination agent before navigating; `ChatRoomClient`'s existing draft-transfer effect re-applies it on mount. No new primitive introduced — the cross-agent draft carry is the same single-hop sessionStorage transfer the chips use.
- `2026-06-10` — claude: **`LandingPlusMenu` retired — the `/chat/new` `+` and the toolbar gear are now ONE component.** `NewChatLandingInput` mounts [`RunControlsMenu`](../inputs/smart-input/RunControlsMenu.tsx) with `variant="plus"` (Attach default tab + Model/Tools/Sandbox/Settings — the promotion the LandingPlusMenu docstring always promised). Per-run settings overrides under the Model tab are now catalogue-driven (`RunConfigOverrides`), and tool rows are single-line with a chevron-expanded description. Full detail in the agents [FEATURE.md](../../FEATURE.md) 2026-06-10 entry.
- `2026-06-09` — claude: **fixed `/chat/new` landing lingering on top of the live conversation after the first send.** `AgentConversationColumn` swapped the landing (greeting + quick-action chips + hero input) → conversation via an `AnimatePresence` exit. With React Compiler enabled the exiting landing child frequently never completed its exit, so the greeting/hero sat over the streaming chat for ~2.3s until the `/chat/new → /chat/[id]` route promotion remounted the tree (looked broken — two inputs, message + greeting at once). Verified in-browser via fiber inspection: `showLanding` was already `false` (messageCount=2) yet the landing node stayed mounted at opacity 1. Replaced the `AnimatePresence` swap with a plain `showLanding ? landing : conversation` conditional (enter-only fades on each branch) so the landing unmounts the instant the first message lands. Now clears at ~300ms instead of ~2300ms. No input component was touched.
- `2026-05-25` — claude: **aligned chat sidebar chrome with the main app nav (no more parallel styling system).** ChatSidebarMenu was rendering two completely different DOM trees for collapsed vs expanded (a bespoke rail of icon buttons collapsed, ChatHistorySidebar expanded) — so chrome items appeared / disappeared in non-corresponding positions and didn't match the main app nav's look. Replaced with a single render: `New chat · Search chats · Search agents · Voice agent` are now always-rendered rows using the EXACT same `.shell-nav-item shell-tactile-subtle` markup as `features/shell/components/sidebar/NavItem.tsx` (canonical 18px Lucide, `.shell-nav-icon` + `.shell-nav-label`). The shell's existing CSS handles label collapse, icon centering, and the collapsed `[title]:hover::after` tooltip — so the icons stay at the IDENTICAL x position (`itemX=6`, `iconX=12`) in both states and the first item stays at the IDENTICAL y position; subsequent items have only the small y drift that `.shell-nav-item`'s padding switch produces in the main nav too. Pinned + grouped history are expanded-only (no room in the narrow rail). Added a reusable `hideSearchAffordance` prop to `ChatHistorySidebar` so the inner list doesn't ship a duplicate search bar, and gave the Search Chats popover its own Redux scope (`chat-route-search`) so its in-popover searchTerm doesn't silently filter the always-on sidebar list after close. Verified in-browser at 1440 in both states: itemX/iconX identical for all four chrome items, first item itemY identical, Search Chats popover focuses its input on open.
- `2026-05-25` — claude: **fixed "user submits a client-tool answer → nothing happens" (Flow 5).** After aidream made client-tool delegation a hard-suspend (stream ends), the client sat waiting on a closed stream and the agent never resumed. Wired the round-trip on both clients: `submit-tool-results.ts` reads `continuation_needed` and dispatches the new `resumeInstance` thunk; resume goes through a shared `runAiStream` helper extracted from `executeInstance` so both paths share the heartbeat/abort/commit pipeline. `dispatchUiFirstTool` now flips the instance to `paused` honestly (guarded against the end-of-stream `complete` overwrite). Made the failure class structurally impossible: deleted the dead `resume-conversation.ts` and the dead `submitToolResults` (plural) thunk in `execute-instance.thunk.ts`, and added an ESLint `no-restricted-syntax` chokepoint banning any `/tool_results` literal/template outside the funnel. Mirror commit landed in matrx-extend (SW broadcasts `STREAM_CONTINUE` on `continuation_needed`; sidepanel hook subscribes and runs `resumeRun`). Canonical protocol lives at [`features/agents/docs/CLIENT_TOOL_SUSPEND_RESUME.md`](../../docs/CLIENT_TOOL_SUSPEND_RESUME.md) — supersedes the stale `DURABLE_TOOL_CALLS_CLIENT_INTEGRATION.md` (referenced a nonexistent `processStreamEvent` reducer) and clarifies the boundary against `PYTHON_RESUME_SPEC.md` (a different, unbuilt resume) and the extension's cursor-replay scaffold (`STREAM_RESUME_PROTOCOL.md` — also unbuilt). Verified e2e against production aidream: user-tool answer → `POST /tool_results 200 {continuation_needed:true, user_request_id}` → `POST /resume 200` → continuation streams, re-entrancy works.
- `2026-05-25` — claude: **made the collapsed sidebar rail useful + pinned the header controls to the start.** When the shell sidebar is collapsed, `ChatSidebarMenu` no longer renders nothing — it shows an icon rail (`Plus` new chat, `Search` search-chats popover, `Webhook` search-agents popover) so the rail offers the same core actions as the header. Search-chats reuses `ChatHistorySidebar` in a portaled popover (added a reusable `initialSearchOpen` prop); search-agents reuses the canonical `AgentListDropdown` via `triggerSlot` (added a reusable, backward-compatible `contentSide` prop so it opens to the right of the rail instead of over it). `ChatRunHeader` is now `lg:w-full` so on desktop the `[collapse · agent · +]` controls sit at the START of the header (no longer drifting to center as the slot re-centers with sidebar width); mobile is unchanged. `Bot` → `Webhook` to satisfy the banned-icon lint rule and match the Agents nav. Verified in-browser at 1440 (expanded + collapsed; both search popovers open and filter; agent popover opens `side="right"`) and mobile 375 (unchanged). Note: live re-verification was repeatedly disrupted by an unrelated broken untracked file `features/lists-quick/QuickListsManager.tsx` (`LucideIcons` used without a namespace import) that put the dev server in a `/chat`↔`/lists` redirect loop and corrupted HMR.
- `2026-05-24` — claude: **consolidated the Smart Input action icons + added per-run tool selection.** Replaced the standalone sandbox (Box) icon with one `InputControlsMenu` (SlidersHorizontal) opening a tabbed popover: **Tools** (`RunToolPicker` — add registry tools to this run; stored on `builderAdvancedSettings.addedTools`, folded into the request by `buildToolInjection` as additive server specs), **Sandbox** (`SandboxPanel`, extracted from the now-deleted `SandboxAttachControl`), and **Settings** (`RunSettingsEditor` — disable tool injection, Surface Simulator, debug, save-to-DB). Resources (Database) and Variables (`{}`) stay as their own icons. Trigger shows a count badge / active dot when the run is customized. Lives in the shared input so every surface (chat, runner, builder, apps) gets it.
- `2026-05-24` — claude: **built failure-and-retry (Flow 4).** Failed turns are now kept in history and rendered as a standalone `AssistantError` bubble (friendly message + expandable Details for live errors) with a one-click, non-destructive **Retry**. Unified failed-turn detection (`isFailedRecord`/`extractRecordError`) across live + persisted (post-reload) failures; transcript ordering switched to `(position, created_at)` so a failed attempt sits just before its retry (they share a position). Replaced the old truncate-based `atomicRetry` + `RetryConfirmDialog` with `retryConversationTurn` → `executeInstance({retry:true})` (the guide's `POST /ai/conversations/{id}` `{retry:true}` contract; falls back to re-send for never-persisted client failures). Also fixed a latent SSR 500: a static import of `AgentAssistantMessage` in `AgentConversationDisplay` pulled jspdf/fflate into SSR — now `dynamic(ssr:false)` like the rest. Verified in-browser: persisted failed bubble + Retry render on reload; Retry POSTs `{retry:true}` to the right endpoint and keeps the failed turn; happy path (3 turns) unaffected. **Backend not yet on the contract — see Flow 4 note.**
- `2026-05-24` — claude: **rebuilt chat onto the app shell, mirroring `/agents/[id]/run`.** Deleted the bespoke `ChatPageShell` (custom sidebar + header that overlapped the global header on mobile and created a 768–1023px dead zone). History is now a Large-Route menu (`ChatSidebarMenu`) in `route-menu-registry`; controls are a compact picker + `+` in the shell header via `ChatRunHeader`/`<PageHeader>`. The shell sidebar defaults to the chat-history view with a switch to the main app menu (desktop + mobile drawer). `ChatRoomClient` now renders only the conversation column. Verified in-browser at 375 / 1280: history, switch, compact picker, conversation load, and `+`-no-revert all work.
- `2026-05-24` — claude: fixed the real "`+`/agent-switch snaps back to the old conversation" cause — the post-submit promotion effect firing on a stale-closure `liveConversationId` during the shared-`surfaceKey` route transition (added a live-focus freshness guard in `ChatRoomClient`). Reworked the mobile header to inject `[chat-menu][picker][+]` into the app shell's `#shell-header-center` via `PageHeaderPortal` (was a local `<header>` overlapping the app hamburger + avatar); added an "App menu" entry atop the chat drawer; aligned the desktop-sidebar breakpoint to `md:` (768) to kill the 768–1023 dead zone. Verified in-browser at 375 / 768 / 900 / 1280.
- `2026-05-23` — claude: documented the live chat route for the first time. Fixed two revival bugs — agent re-selection reviving the agent's last conversation (stale per-agent focus; added a fresh-start `clearFocus` guard in `ChatRoomClient`) and `+` using the last-used/default agent instead of the active agent (`ChatPageShell.handleNewChat`). Removed the dead `selectLastUsedAgentId` selector. Established the agent≠conversation rules above as the surface's load-bearing invariants.

---

> **Keep-docs-live rule (CLAUDE.md):** after any substantive change to this surface, update this file's status/flows/invariants and append to the Change log. This route had no FEATURE.md for its entire existence, which is why the agent≠conversation model kept getting re-violated. Do not let that recur.
