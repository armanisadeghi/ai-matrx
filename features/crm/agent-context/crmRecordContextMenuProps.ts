/** Shared menu identity for every `matrx-user/crm-record` mount point. */
export const CRM_RECORD_CONTEXT_MENU_PROPS = {
  sourceFeature: "crm" as const,
  surfaceName: "matrx-user/crm-record" as const,
};
