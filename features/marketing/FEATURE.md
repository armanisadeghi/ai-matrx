# Marketing

**Status:** active  
**Tier:** 1  
**Last updated:** 2026-08-11

## Internal Authority Router

`/marketing/brands/[brandId]/sites/[siteId]/authority` is the site-wide
authority-flow workspace. It combines the existing backlink intelligence,
crawler link graph/Link Score, GSC demand and SEO Juice, page roles, keyword
mapping, content plans, and cannibalization evidence. It never computes a
second PageRank or stores a parallel recommendation system.

The server first creates a deterministic allowlist of missing source→target
edges. The versioned `seo.internal_authority_router` agent is constrained to
those exact IDs/URLs and streams `seo_authority_route_analysis` through the
normal server stream. `useAuthorityRouter` adopts that foreign stream into the
canonical Content IR pipeline; `LiveRunDisplay` renders progressively parsed
content while the typed durable `authority.route` result is stored on
`seo.collection_run` and read directly from Supabase after reload.

The workspace provides a graph, ranked route cards, evidence table, explicit
source/target/live-page doors, warnings, confidence and cannibalization risk.
“Add to link plan” updates both existing page-plan directions through
`updatePageDesiredValues`: source `outbound_links` and target `inbound_links`.
The current link compliance UI therefore understands approvals immediately;
the observed `web.link_edge` graph remains immutable until a later crawl.

The reusable `AuthorityRouterDoor` is mounted in the site Backlinks workspace,
the site Links workspace, and each page's Backlinks/Internal links cards so the
underlying evidence never dead-ends. The nested
`matrx-user/marketing-authority` surface exposes the loaded verdict, pages,
candidate allowlist, and recommendations to platform agents without refetching.

## The feature is multi-pillar — websites are ONE pillar

Marketing owns **eight peer pillars**, declared ONCE in
`features/marketing/lib/marketing-nav.ts` (`MARKETING_PILLARS`) and rendered by
the `/marketing` hub, `/marketing/tools`, the shell nav (generated —
`marketingNavChildren()`), and the route metadata. One declaration, four
surfaces; they cannot drift.

| Pillar                         | Live today                                                                                                                                 | Reserved (coming soon)                                                |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| Brands & Websites              | `/marketing/brands`, `/marketing/sites`                                                                                                    | `/marketing/local`                                                    |
| Strategy & Planning            | `/marketing/content-plan`                                                                                                                  | `/marketing/campaigns`, `/calendar`, `/audience`                      |
| Discovery, Search & Visibility | `/marketing/keyword-research`, `/marketing/discovery/youtube`, `/marketing/ranks`, `/marketing/search-console`, `/marketing/ai-visibility` | —                                                                     |
| Content & Channels             | —                                                                                                                                          | `/marketing/content-studio`, `/social`, `/email`, `/ads`, `/outreach` |
| Market Intelligence            | `/marketing/competitors`                                                                                                                   | `/marketing/monitoring`                                               |
| Measurement                    | `/marketing/cost`                                                                                                                          | `/marketing/analytics`, `/marketing/reports`                          |
| SEO Tools                      | `/marketing/tools` → the PUBLIC analyzers on `/seo/*`                                                                                      | —                                                                     |
| Data & Operations              | `/marketing/connections`                                                                                                                   | `/marketing/automations`                                              |

### Rules

- **No marketing surface gets a root-level route.** Content planning and keyword
  research were mounted at `/content-plan` and `/seo/keyword-research`; both moved
  under `/marketing/*` on 2026-07-25 (permanent redirects in `next.config.js`).
- **`/seo/*` is reserved for the `(public)` route group** — anonymous lead-gen
  analyzers that must render without a session. Never add an authed `/seo/*` route.
- **Adding or shipping a Marketing surface means editing `marketing-nav.ts`** —
  never the hub page, the tools page, the sidebar, or the metadata individually.
- **Every site/crawl child URL is a visible mode.** Declare it once in
  `lib/route-sections.ts`; the header, sibling-site switching, crawl subnav, and
  route metadata consume that registry. Root modes are exact-only.
  `route-sections.test.ts` compares both registries to the App Router filesystem,
  so a new child page without navigation fails.
- **Every customer-facing top-level Marketing page is in the Marketing map.**
  `marketing-route-navigation.test.ts` compares the live App Router directories
  with `marketing-nav.ts` in both directions; privileged `/marketing/admin` is
  the sole explicit exception.
- The brands/websites pillar is the largest, not the most important. `/marketing`
  is a list view of all pillars — it must never redirect into one of them.
- **The scraper is NOT part of Marketing.** `/scraper/*` and `features/scraper/`
  are shared platform infrastructure that Marketing's crawler borrows. Do not
  move them in, and do not fork a second crawler here.

### Reserved routes — the coming-soon contract

Each reserved surface is a **real route** rendering
`features/marketing/components/MarketingComingSoon.tsx`, not a dead link. Three
declarations must exist together, or the placeholder throws at render:

1. an entry in `MARKETING_PILLARS` with `status: "coming-soon"` + `comingSoonId`,
2. a `marketing.*` row in `lib/coming-soon/registry.ts` (the user-facing promise —
   see that FEATURE.md: a promise is tracked like a defect), and
3. a builder in `lib/routes.ts` (`marketingRoutes`).

**Shipping one:** build the real page at the SAME URL, delete its registry row,
and drop `status`/`comingSoonId` from its nav entry. The href never changes —
that permanence is the whole point of reserving it.

## AI citation reverse engineer (2026-08-11)

`/marketing/ai-visibility` is the cross-site home; every managed site also has
`.../sites/[siteId]/ai-visibility` in its permanent mode rail. A user supplies the
exact buyer question and chooses ChatGPT, Claude, Gemini, and/or Perplexity. Work
goes directly to aidream's streamed durable command; saved evidence reads directly
from Supabase.

- The stream is adopted with `adoptForeignStream` and rendered only through
  `MarkdownStream`. The server emits balanced `structured_info` `__kind` values for
  provider answers, source captures, specialist verdicts, and synthesis; this surface
  never hand-parses or hand-routes model output.
- Four provider cards reveal answers as soon as they land, independently of the slower
  citation capture and specialist stages. A session-stored run id rejoins after refresh;
  the durable `seo.collection_run.result` snapshot recovers completed work.
- `MatrxDataTable` owns local sort/filter/pagination for claims, cited sources,
  decision signals, and history. “Unverified but influential” is an explicit critical
  claim state, not hidden in prose. Cited URLs always open the real source in a new tab.
- The browser never writes server-owned evidence. It reads
  `seo.ai_visibility_response/claim/citation/signal` under RLS; the Python brain owns
  provider calls, crawl-cache reuse, specialist analysis, cost accounting, and writes.

### Guest vs. authed

`app/(core)/marketing/layout.tsx` branches server-side: guests get
`MarketingLanding` (`features/auth/components/module-landing/landings/`) on ANY
`/marketing/*` URL — never a login wall, never an error. Signed-in users fall
through to the workspace. Registered in `MODULE_LANDING_DIRECTORY` so it appears
on `/features`. See `.claude/skills/module-landing-pages`.

## Purpose

Agency-scale brand operations. The anchor entity is the **Brand** (`web.brand`) — a company an organization manages (their own, or an agency client's). A website is ONE brand property; social accounts and other presences are peers (`web.property`). Machine discovery writes candidates to `web.discovered_item`; humans promote them to confirmed `web.property` / `web.brand_asset` / `web.business_fact` — machine writes never touch confirmed truth. Below the brand layer sit the site verticals: canonical URLs, immutable snapshots, crawl sessions, run URL outcomes, durable run events, prioritized analysis, finding lifecycle evidence, screenshots, links, sharing, batch operations, and cost attribution.

## Entry points

**The URL hierarchy is brand-first.** Canonical entity paths come ONLY from `features/marketing/lib/routes.ts` (`marketingRoutes`) — never hand-built.

- `/marketing/brands` — brand portfolio (the anchor list).
- `/marketing/brands/[brandId]` — brand cockpit: identity, website properties with connection chips, social properties, confirmed business facts, brand asset library, pending-review count.
- `/marketing/brands/[brandId]/sites/[siteId]/**` — every registered site mode
  in `MARKETING_SITE_SECTIONS` lives nested under its brand and appears in the
  shared `EntityModeHeader`. `/socials/...` will join as a sibling.
- `/marketing/sites` — flattened all-sites view (kept deliberately); rows link to the nested canonical URLs.
- `/marketing/sites/[siteId]/**` — LEGACY shim only: client redirect that resolves the brand under the caller's session and replaces the URL. Cross-links built from rows that only carry `site_id` may target it.
- `/marketing/connections` — user/org credential onboarding and site-provider binding guide.
- `/marketing/sites/new` — selected-organization site creation (creates-or-reuses the brand via `web.create_site`).
- `.../pages`, `.../pages/[pageId]`, and `.../snapshots/**` — canonical page registry, user intent, and observed history.
- `.../crawls`, `.../crawls/new`, `.../crawls/[crawlId]`, `.../urls`, and `.../logs` — direct live commands, sessions, reconciliation, encountered URLs, and durable events.
- `.../crawls/[crawlId]/reports` and `.../reports/[reportKey]` — a crawl-report catalogue plus dedicated response-code, title, description, heading, canonical, directive/language, image, content/duplicate, structured-data, and performance pages. Stable identities live only in `lib/crawl-reports.ts`; one shared workspace composes the existing bounded crawl-URL and snapshot queries. **Response-codes carries full redirect-chain evidence** (`web.crawl_url.metadata.redirect_chain`, written by matrx-scraper since 0.1.95): hop-by-hop rendering plus loop / redirect-to-missing / chain findings derived in `lib/crawl-report-row.ts#summarizeRedirectChain`; a crawl that predates hop capture renders an explicit "crawled before chain evidence existed — re-crawl" banner (`crawlHasChainEvidence` probe), never an empty column. **Canonicals resolves canonical CHAINS across the whole session**: `listCrawlCanonicalMap` (session-wide `head_tags->>canonical_url` projection, 5,000-row cap flagged loudly) feeds `buildCanonicalLookup` + `evaluateCanonicalChain` — states `canonicalized | chain | loop | canonical-to-error | target-not-crawled`, with the resolved A → B → C path and the target's HTTP status; an uncaptured target says "target-not-crawled", never a guess. ONE canonical-match rule everywhere: `normalizeUrlForComparison` from `seo/audit/indexability.ts` (the Python-parity normalizer) — never a local trailing-slash compare. The **content report has a Pages | Duplicates toggle**: Duplicates clusters the WHOLE session's capture-time fingerprints (`web.snapshot.extracted.fingerprint`, written by the scraper since matrx-scraper 0.1.94) into exact groups (same `exact_sha256`) and near-duplicate components (64-bit simhash bit-agreement above a visible threshold — Screaming Frog's 90% default, options 80–100%). Clustering is pure and unit-tested (`lib/duplicate-clusters.ts`); the session-wide minimal fetch (`listCrawlFingerprints`, JSON sub-path only, 5,000-row cap flagged loudly) lives in `data/inspection-queries.ts`; the panel is `components/crawls/DuplicateClustersPanel.tsx` with an explicit "this crawl predates fingerprints — re-crawl" empty state. Fingerprint JSON is narrowed ONLY through `parseSnapshotFingerprint` (`lib/snapshot-content.ts`); never compare fingerprints of different `version`s.
- `.../structure` — the site ROUTING TREE over the canonical page registry: every URL path as a node, children sorted by subtree page count ascending, per-node cumulative per-level page totals, level filter, search, and a Finder-style Columns drill view; path prefixes with no recorded page render honestly as "no page" gap nodes. Tree assembly is pure (`lib/route-tree.ts`, unit-tested); the bounded paged fetch is `data/service.ts#fetchSiteStructureRows` (v_page_list identity fields only). The route folds into the site surface (no own agent binding, like access/settings/cost).
- `.../coverage` — source-disagreement coverage matrix over the canonical page registry; every tile deep-links into `.../pages?coverage=<filter>` (filters defined ONLY by `PageCoverageFilter` in `data/service.ts`, copy in `lib/coverage.ts`).
- `.../performance` — site-level PageSpeed health over aidream `GET /seo/sites/{site_id}/performance`: measurable-page coverage (using the server's canonical page eligibility), latest-per-page mobile distribution, mobile/desktop p25/median/p75, and 30-day improvement/regression leaders. The surface renders the live scheduler cadence/status/next run and a ten-page **Next in line** queue ranked by the canonical coverage planner from real 28-day GSC clicks and impressions, then internal-link importance. Every queued page links to the canonical page workspace and has a one-click mobile test through the existing streamed `syncPagespeed` command. The primary fix queue remains the ten lowest measured mobile scores with real search traffic. Page-level AI is deliberately not involved in choosing or running coverage: this is deterministic provider work, so the explicit one-click action is the cheapest and most reliable assist.
- `.../audit` — deterministic site-wide audit rollup over stored per-snapshot metrics (`seo_metrics` + `audit_metrics`): indexability verdict counts, per-section pass rates, top issues with sample-page links, worst pages. **Aggregation happens in Postgres** — `web.site_audit_rollup(uuid)` / `web.site_audit_trend(uuid)` (migration `web_site_audit_rollup_server_side.sql`, SECURITY INVOKER so RLS stays the ceiling), fetched by `data/service.ts` (`fetchSiteAuditRollup` / `fetchSiteAuditTrend`). **There is no row cap on either path**, and the workspace prints its own coverage sentence (`auditCoverageStatement`, surface value `audit_coverage_statement`) rather than silently dropping rows. `lib/audit-rollup.ts` remains the jest-tested SPECIFICATION of the counting semantics the SQL mirrors — change one, change both. Known non-HTML resources (`web.page.content_type_last != 'html'`) are counted transparently but excluded from HTML-only page findings; unknown/not-yet-fetched rows remain eligible. This view only aggregates — it NEVER re-derives a metric. **It also carries the Catalogue-analysis panel** (`components/analysis/CatalogueAnalysisPanel.tsx` over `getSiteAnalysisOverview`): the real `web.analysis_result`/`web.finding` rows the per-page audit workers write (matrx-scraper `analyze_site_pages`, commissioned 2026-08-08) — site score (`v_site_score`), open findings grouped by catalogue item (each drills to the item-filtered findings register), lowest-scoring pages (`v_page_score`, each drills to the page workspace), honest "computed at" freshness, an explicit never-analyzed state, and the **Analyze now** command (`crawler/direct-client.ts#analyzeSite` → `POST /crawler/sites/{id}/analyze`; analysis also runs automatically after every full/list crawl).
- `.../analysis` — open, non-suppressed priority queue from `web.v_priority_queue`, populated by the same workers.
- `.../findings` and `.../findings/[findingId]` — durable finding register, lifecycle detail, and immutable result evidence. **Written for a non-SEO:** rows lead with the catalogue label (never the raw key) and carry the analyzer's own "what's wrong" sentence; detail leads with `components/analysis/FindingRemedyCard.tsx` (the fix, above the evidence) and every row reaches the page workspace, the live URL, and its evidence. See THE FINDING FALLBACK LAW in Invariants before touching either.
- `.../links` — site link graph (default), outbound-links report (`?view=external`), and link-edge table (`?view=table`). The graph (`components/inspection/link-graph/`) reuses the kg-graph cytoscape engine; the External view is the domain-grouped outbound rollup (`ExternalLinksView` over `buildExternalLinkReport`); URL labels are root-stripped everywhere on this page. Visual captures have exactly two homes: the page workspace (current capture per kind + history) and the crawl session — there is deliberately NO site-level screenshot gallery (a 3,000-page site would show 12,000 captures).
- `.../backlinks` — provider discovery plus first-party source-page intelligence as a tabbed workspace (Overview · Backlinks · Referring domains · Anchors · Top pages · Competitors · Insights, `?tab=` URL state). Every stable source-page → target-page row can carry cached page content, page/topic classification, source-target relevance, contextual and anchor quality, editorial nature, controllability, risk, a concrete next action, and separate deterministic/AI/human evidence. The referring-domain tab is the platform's own known-site directory and opinion layer—not a restatement of provider authority. Provider **Refresh** is discovery-only; source-page work is deliberately controlled with a 1/5/10/25 **Analyze next** selector or each row's **Analyze/Re-analyze** action in the Backlinks and Insights tables. Every row opens the same canonical complete-record component in the drawer and WindowPanel: exact source page, containing site, exact target page, link mechanics, provider facts, capture content, errors, assessments, human ruling, lifecycle, and the complete user-facing stored row (internal cache identifiers are deliberately excluded). Starting a single analysis promotes that record into its WindowPanel and streams only that link's run there; independent links can run concurrently and there is no page-shifting global progress slab. The page workspace's Backlinks card follows the same contract: it shows exact source-page records rather than inert domain rollups, can expand from its compact preview to every loaded record in place, each record opens the canonical WindowPanel, and its per-record Analyze/Re-analyze action starts that exact expensive job while streaming inside the opened record. Its View all door preserves context by opening the professional table already filtered to the target page. Analysis, score, relevance, source type, controllability, and recommended action all have direct-database sorting/filtering across the full result set. The Insights lenses include strongest/new/lost/broken plus first-party risk review, high-priority actions, strong relevance, and controllable links. Reads go directly to RLS-protected `seo`; provider/crawler/AI work goes to the selected aidream target at `/seo/sites/...`. Remaining provider expansions (competitor intersections, bulk portfolio metrics, agent-tool actions) live in `aidream/docs/handoffs/backlink-intelligence-backend.md`.
- `.../reputation` — the dedicated Digital PR & Reputation decision workspace.
  It composes enriched backlink/source-page evidence, first-party domain opinions,
  competitor intersections, AI visibility citations and claims, confirmed brand
  facts/assets, and ACL-scoped expert knowledge. The browser reads the latest active
  `digital_pr_reputation_brief_v1` Content IR instance and `seo.reputation_case`
  lifecycle rows directly from Supabase; case decisions use only the guarded
  `seo.update_reputation_case` RPC. Analysis calls aidream because it performs crawl,
  RAG, and AI work. Its normal agent chunks are adopted by the execution system and
  rendered by `MarkdownStream`, so streamed conversation remains canonical Content IR.
  The brief states coverage, exclusions, and limitations; weak or unresolvable AI
  output never appears as a case or outreach recommendation. Cases link to their exact
  source, target page, and backlink record, and every detected problem has a lifecycle
  action. Entry doors exist in the site directory, backlinks toolbar, enrichment detail,
  and referring-domain opportunity detail.
- `.../integrations` — verified Google Search Console/GA4 property bindings, reusable Google/YouTube account connections, app-managed PageSpeed, and custom provider bindings.
- `.../crawls/[crawlId]/snapshots` and `.../links` — run-scoped capture and link evidence.
- `.../access` — site-root sharing.
- `.../settings` — the site's control room: identity + lifecycle, the default
  crawl policy every crawl inherits, GA4 evidence, the strategy interview, and
  **"Where this site's data comes from"** — one `MatrxDataTable` over every
  collection provider. **Connection, health and schedule are three separate
  facts and get three separate columns**; every status is DERIVED live
  (`data/collection-status.ts` — provider bindings + the `seo.collection_run`
  ledger + tracked rank targets), never a stamped string, and every row carries
  its doors (Run now · Connect/Check setup · Open data) declared once in
  `data/collection-providers.ts`. A failing row opens the recorded cause from
  the ledger through `BackendFailureDetails` — the one reader for that ledger is
  `data/collection-runs.ts` (PageSpeed and GA4 failure reads call it too).
  The page is the agent-writable surface `matrx-user/marketing-site-settings`:
  crawl scope patterns, budget, behavior, and lifecycle stage as DRAFTS the user
  still saves (handlers in `components/settings/crawl-policy-writes.ts`);
  visibility and delete are deliberately human-only.
- `/marketing/cost` — provider spend: this month and last month per provider
  against the org/platform monthly ceilings, a 30-day daily-spend chart, and
  recent `seo_budget_exceeded` rejections, all from aidream
  `GET /seo/spend/summary` (`components/operations/SeoSpendPanel.tsx` over
  `data/spend.ts`). **There is no runtime-cost rollup table and no per-site cost
  route** — both read the `web.batch_*` spine and were retired 2026-08-11 (D149,
  resolved). Platform batch execution is monitored at
  `/administration/knowledge/kg-cost` over the canonical `batch.*` tables.
- `/marketing` — the hub: a list view of every pillar (`features/marketing/components/MarketingHub.tsx` over `MARKETING_PILLARS`).
- `/marketing/content-plan` — editorial plan tree; see `features/marketing/content-plan/FEATURE.md`.
- `/marketing/keyword-research` — keyword research workbench (`features/marketing/seo/keyword-research/`).
- `/marketing/discovery/youtube` — authenticated public-video discovery and
  expertise comparison (`features/marketing/discovery/youtube/`); direct
  previews live at `/marketing/discovery/youtube/videos/[videoId]`.
- `/marketing/tools` — in-app index of the public analyzers, which stay on `/seo/*` in the `(public)` group (`features/marketing/seo/public-tools/`, `serp/`, `social/`).
- `/marketing/admin` — feature resource map.
- `features/marketing/lib/route-metadata.ts` — one route classifier supplies every Marketing leaf with a specific tab title, description, social-card metadata, and a unique green favicon badge; client-only legacy redirects are covered at the parent layout.
- `features/marketing/data/hooks.ts` — React Query hooks.
- `features/marketing/data/useSiteCrawlActivity.ts` — one site-scoped crawl heartbeat subscription, reconnect/backoff, durable catch-up, and disconnected polling fallback.
- `features/marketing/data/service.ts` — direct browser Supabase queries and `web.create_site` RPC.
- `features/marketing/data/analysis-service.ts` and `analysis-hooks.ts` — isolated direct-Supabase priority, finding, and result reads.
- `features/marketing/data/inspection-*` and `operations-*` — isolated direct-Supabase media, link, batch, and cost reads.
- `features/marketing/lib/discovery-promotion.ts` — canonical social-URL-to-property-type classifier used by review promotion.
- `features/marketing/crawler/direct-client.ts` — direct authenticated scraper commands and transient NDJSON only; its origin comes from the canonical scraper service target, never a feature-local constant.
- Persisted Marketing reads never use Next.js, Python history routes, or AI Dream intermediaries. Google authorization reuses the canonical `GoogleAPIProvider` and Google Identity Services popup; the one-time code exchange and disconnect are aidream endpoints (`/api/google-integrations/exchange|disconnect`, called directly from the browser with the caller's JWT) because client secrets and refresh tokens cannot run in the browser. There are no Next.js OAuth routes.

## Data model

- `web.brand` — anchor entity and access root for the brand layer (`web_brand` token; canonical `iam.apply_rls`). Its `profile` jsonb holds the human-authored editorial brand profile (`BrandProfile` in `types.ts`; narrow ONLY via `parseBrandProfile`, serialize via `brandProfileToJson`) — the voice-of-the-client document downstream agents rely on; agents propose, humans author.
- `web.property` — one presence of a brand (website / instagram / facebook / x / tiktok / youtube / linkedin / pinterest / google_business_profile); a website property points at its `web.site`.
- `web.brand_asset` / `web.business_fact` — human-confirmed brand truth (logos, social-card imagery, phones, faxes, addresses, homepage identity); `brand_asset.file_id` → canonical `files.files`. Allowed domain types and their UI labels live only in `features/marketing/types.ts`; discovery and manual editors consume those registries rather than maintaining partial lists. The legacy database column is named `kind`, but these are domain row subtypes, not Content-IR Shape kinds; user-facing controls call them **Type**. Kind `portal` is a LINK asset (press kit / brand media portal URL in `source_url`, no file) — managed in the Media workspace Sources view, rendered as a link tile in the Library. **One primary per (brand, kind) is a DB invariant** — the `_single_primary` trigger demotes live siblings before the write lands and a partial unique index backstops races; never demote siblings in app code. A primary logo/favicon/og_image/twitter_image with a public `source_url` syncs the brand identity columns via the `_sync_brand_identity` trigger; file-only assets never write identity URLs (identity URLs are the brand's own public URLs, never our storage).
- `web.discovered_item` — machine-written review inbox (`pending → confirmed | dismissed`), URL-deduped per brand for idempotent re-discovery; `resolved_property_id` / `resolved_asset_id` / `resolved_fact_id` record the promotion target.
- `web.site` — managed website (site-scoped access root). `root_url` is the exact canonical homepage identity. Carries `brand_id`, user-editable identity (`description`, `logo_url`, `favicon_url`, `og_image_url` — the brand's own public URLs, never our storage), and `initialized_at` + `initialization` jsonb written by the scraper initialize command.
- `web.page` — canonical URL plus user-owned intent; never captured content. **The anchor registry: every source (sitemap, GSC, crawl, links, manual) upserts rows here and stamps evidence; source coverage disagreements are the intelligence layer.** Multiple live rows may share a path when scheme/host/query differs, so homepage-only reads match `site_id + url = site.root_url`, never `path = '/'`. **Uniqueness is `page_site_id_url_hash_key` (UNIQUE `(site_id, url_hash)`)** — a duplicate URL is therefore ALWAYS cross-site, never an upsert bug. **The registry deliberately holds non-HTML URLs** (images, json, xml, pdf) because a sitemap listing them is itself a finding; they are classified, not excluded — see the two page-registry invariants below.
- `web.sitemap` — one discovered sitemap document (kind index/urlset, parent, status, counts, freshness, fetch_error). Upsert arbiter: unique (site_id, url) NULLS NOT DISTINCT.
- `web.page_sitemap` — page↔sitemap membership evidence (lastmod/changefreq/priority, first/last_seen). Upsert arbiter: unique (page_id, sitemap_id).
- `web.gsc_page_stat` — per-page per-day Search Console evidence (clicks/impressions/ctr/position; unique (page_id, date)), written by the scraper `sites/{id}/gsc/sync` command, which also stamps `web.site.gsc_synced_at` + `gsc_sync`.
- `web.crawl_session` — one crawl run and its current/terminal state.
- `web.snapshot` — immutable page content observation from one session.
- `web.crawl_url` — one session's encountered URL and outcome; never the canonical registry.
- `web.crawl_event` — ordered durable session history; not the transient live stream.
- `web.finding` — current problem lifecycle state, including suppression and first/latest result pointers.
- `web.analysis_result` — immutable normalized evidence for one item/subject computation.
- `web.link_edge` and `web.screenshot` — immutable link and visual evidence.
- `seo.backlink_snapshot`, `seo.backlink_observation`, and `seo.backlink_dimension_snapshot` — immutable server-owned provider history and snapshot children.
- `seo.backlink` — stable exact source-page → target-page identity plus current provider facts, durable capture/analysis state, cached capture evidence, and separate deterministic/AI/human/resolved assessments. Authenticated clients read through RLS; the only browser write is the narrow human-ruling RPC.
- `seo.referring_domain_profile` — site-scoped known referring-domain directory with provider metrics kept separate from the platform's content-derived score/type/verdict/summary and human ruling. Domain names always open their real site.
- Batch execution state and cost attribution live in the canonical `batch.*`
  subsystem. The retired `web.batch_job` / `web.batch_item` spine — and the
  `web.v_cost_by_client/item/page/run/site` views built on it — must not be
  referenced by Marketing tables, views, functions, triggers, or client reads.
  None of them exist in the live database.
  `web.analysis_result` belongs to its site/page, producing crawl session, and
  analysis catalog item; it has no `web.batch_job` dependency.
- `web.v_priority_queue` — open, non-suppressed findings ranked by weight × severity × confidence.
- `web.v_page_list` — security-invoker, one-row-per-registry-URL query projection for latest snapshot fields, live sitemap count, health verdicts, and rolling 28-day GSC metrics; the Pages table filters/sorts this projection before pagination, then hydrates canonical `web.page` rows by id. **It INNER JOINs live sites and exposes `is_resource`, `is_canonical`, and `has_page_evidence`.** User-facing page totals and the default Pages list require canonical + non-resource + evidence; aliases, machine resources, and unconfirmed crawl candidates remain deliberate audit destinations rather than inflating the number called “Pages.” Prefer this projection over a raw `web.page` count for anything a user reads as "pages".
- `web.v_page_score` and `web.v_site_score` — current score projections.

Generated `Database["web"]` types are authoritative. `utils/supabase/webDb.ts` scopes the normal browser client to the custom schema.

Brand and site visibility is an access grant, not a display flag: `internal` is the default for organization work and lets organization members view and manage the row; `personal` limits access to the owner plus explicit grants; `public` lets anyone read while organization management rights remain unchanged. Canonical share links are separate token records in `platform.share_links`; creating one never changes visibility, so the legacy enum value `link` is not offered as a Marketing visibility choice.

## Key flows

1. Add site: `NewSiteForm` selects an organization and calls `web.create_site`; canonical triggers stamp base fields, then the UI navigates immediately to the site overview. The overview auto-starts site initialize (`?capture=homepage`), which creates the canonical `site.root_url` page, persists `web.snapshot.head_tags`, and captures responsive homepage screenshots (`desktop_fold` preferred for the hero). Legacy bootstrap may still stamp `web.site.homepage_screenshot_id` (`kind='homepage'`); when that pointer is absent the hero resolves the newest canonical-homepage `desktop_fold`/`mobile_fold` capture (never a host/scheme variant or another page's crawl). Only when no capture exists yet does the hero fall back to the site's public `og_image_url`. Observed meta title and description render from the homepage snapshot once initialize completes; a capture failure never rolls back the valid site and can be retried from the thumbnail control.
2. Browse data: the URL owns table search/filter/sort/page state; `MatrxDataTable` emits changes; a feature hook issues a bounded `select(..., { count: "exact" }).range(...)` directly to Supabase with deterministic `id` ordering.
3. Manage a page: `PageWorkspace` reads identity from `web.page` and current content from `latest_snapshot_id`, rendering SERP + social previews, keyword authoring and target-keyword performance, then a compact page-identity card followed by pretty + raw structured-data evidence and the complete typed resource inventory, plus indexability, headings outline, content stats, sitemap memberships, page screenshots, Matrx Analysis/Suggestions placeholders, and page-level provider panels. Page identity renders only captured values, keeps the featured image thumbnail-sized, and replaces an empty legacy payload with one explicit `Fetch now` recrawl explanation instead of placeholder fields. Structured data shows every normalized entity/type plus the raw JSON-LD/microdata/RDFa/microformat payload; resources group every DOM- and structured-metadata-declared image/responsive variant, video/audio item, embed, script, style, font, icon, manifest, track, and document. Older snapshots honestly say when their counts-only capture predates the complete crawler contract. Every evidence section carries Copy, Copy JSON where applicable, and Copy for AI; the same complete `page_identity`, `structured_data`, `resources`, and `images` values enter the marketing-page agent scope (the very large raw evidence values are bindable but deliberately not auto-context). Every section is collapsible and initially open. Captures report desktop/mobile availability explicitly; absence means no matching capture row exists, not that the UI hid it. Extracted Markdown renders in full without an internal height cap. The intent form saves `target_keyword` plus desired metadata to the page (and stamps `web.page.seo_metrics_desired`, contract v1); its copy-current control fills both desired fields from the latest observed metadata without saving. **The target-keyword field is the canonical `KeywordInput`** (`features/marketing/seo/keyword/FEATURE.md` — read it before touching ANY keyword field or building a keyword payload): live library resolution + data chips, GSC-query/Page-Analyzer suggestions, usage chips against observed title/description/H1/URL, the Keyword Intelligence window launcher (`useOpenKeywordWindow`), and the condensed `buildKeywordBrief` riding in the `web-page-intent` copy payload. **Page performance is canonical aidream compute** (`GET /seo/pages/{page_id}/performance`): the full-width panel renders mobile/desktop Lighthouse category rings, clearly separated lab vs real-user Core Web Vitals, the server's regression verdicts, score history, and the same response's 28-day GSC KPIs/daily trend. Its mobile/desktop/both refresh consumes the NDJSON stream with named live stages and elapsed time; missing PSI key and GSC binding states say who can fix them and link to site integrations. GA4 remains a separate persisted page panel. The SERP section renders the CANONICAL `features/marketing/seo/serp` pieces (`SerpResult` with a desktop/mobile toggle, deterministic `SerpFieldChips`, condensed `MetaRecommendations`) and its header launches the floating Search Appearance analyzer (`useOpenSerpAnalyzerWindow`) pre-filled with the page's metadata; the intent form live-validates drafts with the same evaluator. Never hand-roll a SERP preview or re-declare a meta limit — everything imports from `features/marketing/seo/serp` (see its README for the TS↔Python parity contract; the scraper stamps `web.snapshot.seo_metrics` at capture time). All snapshot JSON access goes through the typed narrowers in `lib/head-tags.ts` and `lib/snapshot-content.ts` — components never poke raw snapshot JSON. **The authoring layer** (2026-07-27): every important card has a desired/target twin saved to `web.page.desired_values` through the ONE merge-safe path (`useDesiredValueSlice` + `DesiredSection`, `components/pages/desired/`); authored draft content lives in the 1:1 `web.page_content` table (explicit Save, own version lock); the keyword batch is `seo_keyword → web_page` supporting-role association edges (`data/page-keywords.ts`); tasks/notes/files attach via the canonical association system (bottom `AssociationCardGrid`, header + per-card `PageTaskButton`, per-capture `CaptureAttachments`); image planning + agent generation in `PageImagePlanCard` (`image_producer` surface role, `lib/generate-page-image.ts`). **Internal-link two-plan contract** — three `desired_values` slices, all authored in the Plan-lane `LinksPlan` card (`components/pages/cards/LinksPlanCard.tsx`; `PageLinksCard` is observed-evidence only): `accepted_anchor_texts` (anchors other pages may use linking HERE), `inbound_links` (pages that SHOULD link here: `PlannedLinkEntry[]` of `{id, url, anchor_text?}`), and `outbound_links` (links this page SHOULD carry, same shape). Anchor matching is exact after case/whitespace normalization; URL matching drops fragments and trailing slashes (`normalizePlanUrl`); reports use only each source page's latest accepted snapshot; a source page reads the linked target page's policy directly so mismatches and acceptable replacements appear where the link must be edited. Every planned entry is scored `linked` / `wrong_anchor` / `missing` (`scorePlannedLinks` in `data/page-links.ts`): a planned anchor must match exactly; no planned anchor falls back to the partner page's accepted list; no policy at all means any link satisfies the plan. The SITE rollup is the Links workspace "Plan" view (`.../links?view=plan`, `components/inspection/link-plan/SiteLinkComplianceView.tsx` over `data/site-link-compliance.ts` — two bounded direct reads, caps render loudly): every declared policy/plan scored against every other page's current links, biggest gaps first.
4. Inspect a crawl: summary reads `crawl_session`; URL and log children independently page through `crawl_url` and `crawl_event` by `session_id`. Reports turn the same run URL and immutable snapshot evidence into subject-specific bulk tables at stable child URLs; snapshot JSON is flattened only through `lib/head-tags.ts`, `lib/snapshot-content.ts`, and `lib/crawl-report-row.ts`, never inside components.
5. Run a crawl: `/crawls/new` sends the command directly to the scraper with the caller's Supabase JWT, renders the transient NDJSON feed, supports cancellation, and links to the durable session. `useSiteCrawlActivity` restores the newest queued/running session on every site route, subscribes once to the RLS-authorized `web.crawl_session` heartbeat, and catches up the bounded display-safe event feed from `web.crawl_event` after every update/reconnect; disconnected clients poll until Realtime recovers. The site header always shows **Crawling** plus a route back to the active feed. Stored events and reloads come from Supabase, never Python replay.
6. Triage analysis: `/analysis` pages through `v_priority_queue` and links each projection into a URL-filtered finding register; `/findings/[findingId]` reads lifecycle state, catalog context, first/latest evidence pointers, and paged result history directly from Supabase.
7. Inspect evidence: site/crawl link workspaces and the screenshot gallery read records directly from Supabase; snapshot bodies, markdown, and screenshots are identified only by canonical `files.files` UUIDs and rendered through `@/features/files` (`fileIdToMediaRef` / `InlineMediaRef`).
8. Share and configure: `/access` calls canonical IAM grant/list/revoke RPCs for the `web_site` root; `/settings` uses version-checked direct Supabase updates.
9. Monitor execution: batch execution state lives in the canonical `batch.*`
   subsystem and is monitored at `/administration/knowledge/kg-cost`. Marketing
   does not maintain a parallel `web.batch_*` execution spine, and no Marketing
   route reads one.
10. Configure integrations: `/marketing/connections` and each site's `/integrations` route use the existing Google Identity Services provider in popup authorization-code mode to connect a reusable personal or organization account without a redirect callback. aidream's `/api/google-integrations/exchange` exchanges the one-time code, stores the refresh token as a sealed credential item in the CANONICAL secrets vault (`users.user_secrets`, user- or organization-owned), and discovers Search Console, GA4, and owned YouTube channel resources; the connection row keeps only safe metadata plus the stable `credential_item_id` (`vault_secret_key` is a temporary legacy fallback). The browser reads connection metadata and resource choices directly from Supabase. A site's workspace binds only the selected connection/resource references; automatic provider binds rebase that provider onto the latest site version, preserve sibling providers, and reject same-provider races. PageSpeed uses the application's API key and does not require user OAuth.

## CRUD map — full user control at every level

Arman's doctrine: every editable thing is editable in the UI, every level has proper create/edit/delete. Deletes are ALWAYS soft (`deleted_at`); a parent delete refuses while live children exist with error text that names the fix; confirms use `@/components/ui/confirm-dialog`. Hiding a stored, user-editable field from its editor is a defect.

**Crawler-observed rows are DISMISSED, never deleted (Arman's ruling, 2026-08-08): the crawler represents reality.** The user verb on `web.page` and `web.sitemap` is **Dismiss** — soft `deleted_at` that hides the row from primary views; a future crawl/sitemap/GSC observation revives it server-side, stamping `metadata.dismissals` (`[{dismissed_at, revived_at, revive_reason}]`, scraper-written). Revived rows render the "Previously dismissed ×N" badge (`components/shared/PreviouslyDismissedBadge.tsx`); `metadata.dismissals` is narrowed ONLY via `lib/dismissals.ts`. Dismissed rows live in a deliberate destination, never a default view (THE VIEW LAW) — as do crawled non-HTML resources, on the sibling `?scope=resources` chip. Pages `?scope=dismissed` chip → `DismissedPagesTable` (over `listDismissedPages`, direct `web.page` — `v_page_list` correctly excludes dismissed rows), Sitemaps header "Dismissed" toggle (`listDismissedSitemaps`); both offer Restore (`restorePage`/`restoreSitemap` — sitemap restore reverses the membership cascade). Crawl sessions keep real Delete — they are our own run records, not observed reality.

| Entity                                                                                                          | Create                                                                                                                                | Edit                                                                                                                                                                                                                                                   | Delete                                                                                                     |
| --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------- |
| `web.brand`                                                                                                     | `BrandEditorDialog` (brands list)                                                                                                     | same dialog (list row + cockpit) — every editable field incl. the collapsible "Brand profile" section (audience, voice, positioning, value props, offerings, service area, competitors, keywords, guidelines, notes → `profile` jsonb)                 | list row + cockpit; refused while live sites OR properties remain (DB guard `web.brand_soft_delete_guard`) |
| `web.site`                                                                                                      | `/marketing/sites/new` (`web.create_site`; `?brand=` pre-binds — explicit brand ALWAYS wins over name-match)                          | `SiteEditorDialog` (sites list row + cockpit Websites row) — every editable field incl. status/visibility + brand reassignment via `web.move_site_brand`; root_url/domain read-only (registry migration); `/settings` keeps crawl policy + Danger zone | sites list row, cockpit Websites row, settings Danger zone                                                 |
| `web.page`                                                                                                      | Pages table "Add page" (provenance `manual`; normalization+hash identical to the scraper via `lib/page-url.ts`)                       | `PageWorkspace` intent form                                                                                                                                                                                                                            | **Dismiss** (row action) + Restore from the `?scope=dismissed` view                                        |
| `web.sitemap`                                                                                                   | system-written (sync)                                                                                                                 | is_active toggle (row action)                                                                                                                                                                                                                          | **Dismiss** (row action; cascades to its `page_sitemap` evidence) + Restore from the Dismissed view        |
| `web.page_sitemap`                                                                                              | —                                                                                                                                     | —                                                                                                                                                                                                                                                      | **system-managed evidence — read-only by design** (only the sitemap-delete cascade touches it)             |
| `web.property`                                                                                                  | `PropertyEditorDialog` (cockpit; non-website kinds — website properties are managed via their site)                                   | same dialog, every editable field                                                                                                                                                                                                                      | cockpit row action                                                                                         |
| `web.brand_asset`                                                                                               | `BrandAssetEditorDialog` (cockpit — by URL or canonical file upload via `useFileUpload`) + Media library upload + discovery promotion | same dialog, every editable field incl. replace-file                                                                                                                                                                                                   | cockpit tile action                                                                                        |
| `web.business_fact`                                                                                             | `BusinessFactEditorDialog` (cockpit) + discovery promotion                                                                            | same dialog                                                                                                                                                                                                                                            | cockpit row action                                                                                         |
| `web.discovered_item`                                                                                           | machine-written only                                                                                                                  | review verbs: confirm / dismiss / un-dismiss (restore to pending) — single or bulk via inbox multi-select (row / category / page); bulk type-assign enabled only when the selection shares one type pool                                               | delete (pending + dismissed; confirmed rows are the promotion audit trail and stay) — single or bulk       |
| `web.screenshot`                                                                                                | system-written                                                                                                                        | —                                                                                                                                                                                                                                                      | page-workspace capture-history action (capture record only; the file remains)                              |
| `web.crawl_session`                                                                                             | system-written                                                                                                                        | —                                                                                                                                                                                                                                                      | crawls table row action (URL ledger + events stay attached)                                                |
| `web.snapshot`, `web.crawl_url`, `web.crawl_event`, `web.link_edge`, `web.gsc_page_stat`, `web.analysis_result` | **immutable observed evidence — no CRUD by design**                                                                                   |                                                                                                                                                                                                                                                        |                                                                                                            |
| canonical `batch.*` execution rows                                                                              | out of scope for user CRUD (execution state; monitor-only, at `/administration/knowledge/kg-cost`)                                    |                                                                                                                                                                                                                                                        |                                                                                                            |

Grants: `migrations/web_marketing_crud_grants.sql` added the missing authenticated INSERT/UPDATE grants (page, page_sitemap, screenshot, crawl_session). RLS (canonical `iam.apply_rls` policies) stays the only authorization layer.

**Brand/site/property lifecycle is atomic at the DB** (`migrations/web_brand_asset_primary_and_site_property_lifecycle.sql`): soft-deleting/restoring a site cascades to its `property(kind='website')` row (`_cascade_website_property`; restore matches the exact cascade stamp so an independently-deleted property stays deleted), and `web.brand` refuses soft-delete while ANY live site or property remains (`_soft_delete_guard`). FE deletes only preflight for friendly copy — never re-implement the cascade or the guard in app code.

## Invariants & gotchas

- **A per-page SEO verdict is NEVER re-derived in a component.** Every status/score/reasoning comes from `features/marketing/seo/audit/checks.ts` (`runPageChecks` / `PAGE_CHECKS`), the byte-identical mirror of aidream `matrx_scraper/seo_audit.py`. A surface that compares a word count to 300 in TSX has forked the rule; import the check. Thresholds live in exactly one place per rule — the CAPS constants in `checks.ts`, except the SERP length limits, which are imported from `seo/serp/metrics.ts`. Touching either side means touching both in the same unit of work and regenerating `__fixtures__/page-checks-parity.json`; `checks.parity.test.ts` fails otherwise.
- **Number fields that clamp to a min must never clamp on every keystroke.** Use `components/shared/ClampedNumberInput` (draft string + commit on blur/Enter). `Number("") || min` immediately overwrites a cleared field and makes the input unusable.
- **Single-entity fetchers use `.maybeSingle()` + `assertFound` (`data/service.ts`), never `.single()`.** A row can be soft-deleted out from under an open view; `.single()`'s 0-row PGRST116 leaks a red "Cannot coerce…" error into the inspector. **A zero-row read never asserts deletion** — it is equally a denial, a stale id, or a signed-out session, so it throws through `lib/records/recordUnavailable.ts` and the surface renders `<AccessGate token id/>` ([`features/access-gate/FEATURE.md`](../access-gate/FEATURE.md)), which asks the platform which of the four it actually is. The old "deleted or no longer accessible" sentence was wrong most of the time and is gone. Delete mutations invalidate LIST keys only and never refetch the deleted entity's own subtree (see `useDeleteSite`).

- **The five connection statuses (Init / GSC / GA4 / PSI / CMS) are derived ONLY through `features/marketing/lib/site-status.ts`** — the portfolio list and the site page must never compute them independently. A configured GSC binding with `gsc_synced_at` null is `attention` ("Connected, never synced"), never `connected` — no data has flowed until the sync command stamps the site row.
- **Initialization hydrates progressively, never behind one monolithic spinner.** The initialize stream's granular `initialize_step` events (`step: identity|screenshots|sitemaps|discovered`, `status: started|complete|failed`) drive a live step strip (`InitializeProgress`) and per-step query invalidation: identity → the site row only (exact key — the site key prefixes the whole subtree), screenshots → hero + gallery keys, sitemaps → sitemap list/coverage/coverage-matrix keys, discovered → the brand's discovery lists + pending count. The map lives ONLY in `components/site/initialize-progress.ts` (`queryKeysForInitializeStep`, unit-tested); the event narrower is `initializeStepFromEvent` in `crawler/direct-client.ts`. Against a scraper deploy that predates the contract, the strip stays honestly indeterminate (neutral steps + one spinner) and the run falls back to the end-of-stream refetch — never fake per-step progress.
- **A finished initialize stream is NOT success.** The server records per-step failures in `web.site.initialization.errors`; the overview re-reads the row after every run, toasts the failed steps, renders the "Initialization issues" panel, and captures each step to the Error Inspector (`source: "marketing-crawler"`). A green toast with failed steps hidden is the exact defect this exists to prevent.
- **Every `files.files`-backed artifact (screenshots, captured HTML, extracted Markdown) opens through the canonical file viewer overlay** (`useOpenFilePreviewWindow`; thumbnails via `components/shared/CaptureThumb.tsx`). Bare `<a href="/files/f/...">` links and bespoke lightboxes are platform violations — the canonical viewer provides fullscreen/share/download for free.
- **The live crawl feed never renders per-URL bookkeeping rows.** `page_discovered` / `url_classified` / `page_parsed` present as `null` and feed only the Discovered/Queued/Fetched/Failed/Skipped counters (`summarizeLiveCrawlEvents`); feed rows are reserved for fetches, failures, warnings, and phase milestones, and rendering is capped at the newest 200 rows. Encoded in `live-crawl-event-presenter.test.ts`.
- **Active crawl state is durable, site-wide, and reconnect-safe.** React component memory is never the authority. `web.crawl_session` is the low-frequency Realtime heartbeat; each heartbeat/reconnect refetches the newest 250 display-safe durable events. `web.crawl_event` is deliberately not published because `page_parsed` payloads can contain whole documents. `useSiteCrawlActivity` owns the site's ONE channel, uses exponential reconnect backoff with catch-up, and polls only while disconnected.
- **A streaming command NEVER shows a spinner, and NEVER dies on refresh.** Every command in `crawler/direct-client.ts` runs through `data/useSiteCommandRun.ts` — no surface calls `analyzeSite` / `syncSitemaps` / `syncGsc` / `checkSiteLinks` / `fetchPageNow` bare. The hook streams the run into the floating `SiteCommandRunWindow` (never an inline block — THE FLOATING LAW, [`features/window-panels/FEATURE.md`](../window-panels/FEATURE.md)) and, on mount, rejoins any session of that command still running on the server (`crawlActivity.activeSessions` → `web.crawl_session`), reopening the window and settling from the session row. Buttons read **"Watch progress"** while a run is live and reopen its window — never a disabled spinner. The live buffer is `crawler/command-run-store.ts` (module-scoped, `useSyncExternalStore`): the window renders at the overlay root, outside this feature's provider, and a page-fetch stream emits at page speed, so this is deliberately not Redux and deliberately not component state.
- **`activeCrawl` means the site-wide CRAWL, never a command.** `useSiteCrawlActivity` derives it from `scope.mode` (full / list / initialization / homepage) and exposes every live session separately as `activeSessions`. Taking the newest active row of any mode is what made a GSC sync render as "Crawling" in the site header.
- **Every scraper-boundary failure feeds `captureError`** via the chokepoints in `features/marketing/crawler/direct-client.ts`. Marketing components import `toast` from `@/lib/toast` (the captured sonner wrapper), never from `"sonner"` — bare sonner toasts are invisible to the admin Error Inspector.
- **A scraper stream's `user_message` is a TEMPLATE, never the answer.** The streaming layer emits `"<CanonicalCommand> failed unexpectedly. Please try again or adjust your settings."` for every unclassified crash while the true cause (often a stringified upstream aidream payload) rides in `message`. `direct-client.ts` throws `BackendApiError` built through `describeBackendFailure` (`lib/api/errors.ts`), which unwraps the nesting; the Error Inspector `message` is the SPECIFIC cause and the template goes to `userMessage`. Any surface reporting a command failure must render `headline` + keep `cause`/`code`/`requestId`/`chain` visible (see `GscSyncRow`). This is the class that produced a year of meaningless marketing failures — reintroducing `toast.error(extractErrorMessage(error))` on a stream is a regression.
- **Google connection health is DERIVED, never the stored `status`.** `users.integration_connections` can read `status='connected'`, `last_error=NULL` while its vault credential reference is gone — the exact state that made every GSC sync fail on 2026-07-25 with nothing to see. `google/service.ts` computes `credential_present` / `credential_stable` / `health` from `credential_item_id` + `vault_secret_key` (byte-for-byte the precondition aidream's `resolve_connection_credential` enforces), and `google/health.ts` (`diagnoseGoogleConnection`, `googleConnectionDiagnostics`) is the ONE place that turns a row into a sentence. Every surface that offers Google-backed work states a blocking diagnosis and disables the action BEFORE the call — never "connected" plus a mid-stream crash. aidream now also stamps `status='needs_attention'` + `last_error` on any credential-resolution failure and self-heals on success, so the failure is durable, not experiential.
- **SEO sync failures use the canonical backend-error contract.** GA4 and
  PageSpeed command clients throw `BackendApiError` through `parseHttpError` /
  `parseStreamError`, never plain `Error`. Their cards render the shared
  `BackendFailureDetails` block with the specific cause, machine code, request
  ID, HTTP status, and full service chain; the toast is only the transient
  headline. A stream-provided request ID may live inside structured `details`
  and the parser preserves it. Cards also read the latest durable
  `seo.collection_run` directly under RLS and reconstruct the same failure
  after navigation or refresh; a stored provider error is never stranded only
  in backend logs/raw evidence.
- **Machine discovery writes ONLY `web.discovered_item`.** Confirmed identity (`brand_asset`, `business_fact`, site identity columns) is written by humans (or by explicit promotion code) — never directly by a scraper.
- **A specific discovery guess must stay specific through promotion.** Fax, homepage title/description/site name, Open Graph image, and Twitter card image are first-class confirmed kinds; the review inbox must never demote a recognized `guessed_kind` to `other` because of a partial local options list.
- **Social discovery must be verifiable before confirmation.** Social candidates render the canonical provider icon plus the strongest identity encoded by the URL (handle/channel ID and profile/page/channel type). Every URL-bearing discovery exposes both a clickable URL and an explicit open-in-new-tab action. Remote profile HTML is not treated as browser-readable evidence because major social providers block cross-origin reads and framing; the live target remains the final authority.
- **Promotion targets the model that renders the result.** Social candidates become `web.property` rows; media becomes `web.brand_asset`; facts/identity copy becomes `web.business_fact`. Confirmed logo/favicon/social-card assets also synchronize the brand/site identity URL fields consumed by summary cards. `web.discovered_item.resolved_*_id` records that exact target.
- **Other is labeled, never anonymous.** The review inbox accepts an optional human label for every promoted record and requires one for `other`; manual fact/asset editors enforce the same rule through `business_fact.label` / `brand_asset.title`. A custom label does not mint a new domain type or Content-IR Shape. Bulk confirm therefore SKIPS Other-typed items with an explicit toast — never a silent drop, never a label-less Other row.
- **The discovery inbox never silently caps.** `listDiscoveredItems` is controlled pagination with an exact count (`PagedResult`, id tie-break ordering); the header count and pager show the true total. Bulk confirm routes every item through the ONE canonical per-category promotion function (`confirmDiscoveredAsset`/`Property`/`Fact`) — a second bulk write shape for promotion is a defect; bulk dismiss/restore/delete are single guarded statements (`assertMutated`).
- Site/brand identity URLs (logo, favicon, og image) are the brand's own public URLs. Scraper-produced media (screenshots) goes through the canonical AWS file pipeline and renders via `InlineMediaRef` + `file_id` — never a Supabase Storage URL.
- **Discovered image URLs render through `secureImageUrl`** (`lib/website-url.ts`) — discovery persists `http://` URLs verbatim, and an https page silently blocks them as mixed content (a blank logo, no error). Identity marks render via `SiteIdentityMark` (candidate fallthrough logo↔favicon → neutral globe — never a blank box; `prefer="logo"` for hero sizes) and a brand's social properties render via **`PropertyKindMark`** (`components/shared/PropertyKindMark.tsx` — official platform glyph on the platform's brand color, the one deliberate exception to semantic-token colors; `propertyPublicUrl` derives a canonical profile URL from a bare handle). A generic icon for a known platform is a defect.
- Persisted data always flows browser ↔ Supabase under the caller's JWT and RLS. Never add a Python, AI Dream, or Next.js read proxy.
- Crawler commands/live NDJSON go browser ↔ scraper directly. Durable rows written by the crawler are subsequently read from Supabase.
- A canonical page is not a crawl URL. A page's current content is its latest accepted snapshot, not a page column.
- **A page whose SITE is soft-deleted stays live in `web.page` — every cross-site reader MUST exclude it.** Site soft-delete cascades to `web.property(kind='website')` and deliberately NOT to pages: `web.page` carries `_gc_assoc_softdelete`, which HARD-deletes every `platform.associations` edge, so a cascade would irreversibly destroy page↔keyword/task/note edges that a site restore could not rebuild. The guard is at the read layer instead — `v_page_list` INNER JOINs `web.site … deleted_at IS NULL`, and a direct `web.page` read that is not `site_id`-scoped adds `site!inner(id)` + `.is("site.deleted_at", null)` (`searchPagesForMetaApply` is the one such reader). 817 orphans of one deleted site produced every "the same URL appears twice" report; there were zero same-site duplicates.
- **A registry row is classified, never removed.** Non-HTML URLs and machine endpoints are recorded because coverage disagreement is the product — but they are not pages, so they are segmented, never counted as pages, never offered where a user picks a page, and never scored by the HTML audit. A row is a resource when EITHER signal fires: the crawler's `content_type_last` verdict is non-HTML, OR the URL's shape proves it (asset extension / machine endpoint path — `/wp-json/`, `/wp-content/`, `/feed`, `rest_route=`). ONE rule, three mirrors: `lib/page-content-class.ts` (`isResourceContentType` / `isMachineResourceUrl` / `applyPageOnlyFilters`), `web.is_machine_resource_url` + `v_page_list.is_resource`, and matrx-scraper's `web_crawl/page_class.py` — change one, change all three. **URL shape is load-bearing, not belt-and-braces:** `content_type_last` is NULL for 8.7k of 10.8k rows (declared by sitemap/GSC, never fetched), so the verdict alone is silent for most of the registry — that gap is how 717 `/wp-json/...` endpoints became datadestruction.com's worst-ranked pages, each faulted for missing `og:title` and `<h1>`. The shape set stays narrow: extensionless, `.php`, `.html` and `.aspx` are NOT matched, because that ambiguity is what the verdict is for. Resources are a deliberate destination (`.../pages?scope=resources`), exactly like `?scope=dismissed`.
- **Evidence, identity, and content kind are separate page-count gates.** `v_page_list.is_canonical` removes live alias spellings; `is_resource` removes positively classified non-HTML responses; `has_page_evidence` requires a retained snapshot, live sitemap membership, live GSC row, or a manual assertion. The Overview and Coverage “Pages” number applies all three. A crawl-discovered URL that never produced any of that evidence remains an **Unconfirmed candidate**, linked at `.../pages?coverage=unconfirmed`; it is never silently erased and never presented as a confirmed page. Coverage reports the overlapping current evidence totals (crawl / sitemap / Google) beside first-source provenance, whose non-overlapping buckets add back to the confirmed total.
- Every child query scopes both its resource id and `site_id`; cross-site ids must not resolve under the wrong shell.
- Deep tables always use controlled Supabase filtering, sorting, exact counts, and bounded ranges. The canonical table never imports Supabase.
- Analysis tables use `table.queryState` for debounced database queries and `table.state` for immediate controlled-table feedback.
- `v_priority_queue` deliberately has no finding ID. Priority rows open the findings register with canonical item/page filters; only `web.finding.id` opens finding detail.
- 🚨 **THE FINDING FALLBACK LAW — a new `item_key` must render correctly with NO frontend change.** The server's check registry (aidream `matrx_scraper/seo_audit.py::PAGE_CHECKS`) grows on its own schedule. **Never key a map, filter, icon, copy string, or `switch` off a hardcoded list of item keys.** `features/marketing/lib/finding-remedies.ts` is the ONE registry: `resolveFindingRemedy` never returns null and never throws — an unregistered key gets `humanizeItemKey`, the DB's own `metadata.reasoning`, a visible "New check" badge, and the GENERIC remedy (open `seo.page_analyzer` with the finding briefed), which is a real action, not a placeholder. `metadata.reasoning` is written on EVERY result and is the guaranteed floor every surface falls back to; the register carries it per row (`FindingListRow.reasoning`, joined from `last_result_id`).
- **Every finding ships its fix, or an honest instruction — never neither.** A remedy is `ai` (a one-click `AssistChip` launching the SEO agent with a prepared brief — the assists primitive, so THE INTENTIONAL-ACTION LAW applies) or `manual` (a copy-able instruction naming the exact change and where, for things only the site owner can do on their own site). Never invent a button that does nothing. Every finding also gets an honest exit: `suppressFinding` / `acknowledgeFinding` (`data/finding-mutations.ts`) — the analyzer owns detection and never touches suppression; only a passing re-analysis resolves a finding, never a click.
- Finding detail evidence is scoped by `site_id + subject_type + subject_id + item_id`; a same-item result from another subject must never appear.
- `crawl_event.sequence` defaults ascending; every sort adds `id` as a deterministic tie-breaker.
- Snapshot body and screenshot fields are direct UUID FKs to `files.files`. The browser never sees a bucket/path/native URI and never constructs a storage URL; private bytes and signed URLs resolve through the canonical Files pipeline in AI Dream.
- `body_ref`, `markdown_ref`, `storage_bucket`, and `storage_path` are forbidden crawler contracts. There is no compatibility reader because all pre-cutover crawl data was disposable and wiped.
- Crawl artifact access fails closed: immutable metadata plus the direct snapshot/screenshot file FK classify the file, and the database requires an exact tenant match plus current site-viewer access. No `platform.associations` row exists for this relationship. Missing, forged, cross-tenant, or soft-deleted references never fall back to file ownership.
- No legacy crawler data is migrated or read.
- **Google OAuth refresh tokens live ONLY in the canonical secrets vault** (`features/secrets`; aidream is the sole encryption owner). `users.integration_connections` holds safe metadata plus the stable `credential_item_id`; `vault_secret_key` is only a loud legacy fallback until its removal — never ciphertext. One Google connection requests the combined read-only Search Console, Analytics, and YouTube scope set. YouTube channels are safe `youtube_channel` resource rows; access tokens remain server-side and ephemeral. A parallel encryption pathway for any credential is a defect (the bespoke AES-256-GCM control plane was annihilated 2026-07-20). Site JSON contains only connection/resource references, never tokens or client secrets.
- **Automatic provider binds use provider-scoped CAS rebasing** (`updateBuiltInProviderIntegration`). Retry unrelated `web.site.version` bumps once; preserve every sibling provider; reject a concurrent change to the same provider. Manual whole-form saves remain strict CAS updates.
- Exchange/disconnect are aidream endpoints; disconnect revokes at Google AND deletes the vault item. Connection lists, discovered resources, site bindings, and all crawler/analysis history are read directly from Supabase under RLS. The scraper's GSC sync resolves the credential through aidream's service-token internal endpoint — the scraper deployment needs `AIDREAM_URL` + `AIDREAM_SERVICE_TOKEN`, no shared encryption env.

- **The Marketing surface fleet is the ONE agent-context system for this feature.** Tree: `matrx-user/marketing` (hub) · `marketing-brand` → `marketing-site` → its verticals (`site-pages`, `page`, `crawls`, `crawl`, `audit`, `analysis`, `findings`, `links`, `backlinks`, `coverage`, `sitemaps`, `discovery`, `integrations`, `site-settings`, `site-keywords`, `site-media`) · `marketing-ranks-hub` (cross-site, standalone — deliberately NOT inheriting brand/site; `marketing-batches` was retired with its route on 2026-08-11). Manifests in `features/surfaces/manifests/marketing*.manifest.ts`; snapshots/settings/access/cost routes deliberately fold into their parent surface (route→surface logic in `features/surfaces/utils/route-to-surface.ts` `resolveMarketingSurface`, unit-tested). Parent context flows as XML: `brand_context`/`site_context` built ONLY by `lib/surface-context.ts` and supplied through `lib/scopes/site-surface-base.tsx` (`useMarketingSiteSurfaceBase().getBaseValues()`) — every workspace's `SurfaceRuntimeProvider` spreads the base first, then its own manifest-declared keys, built strictly from already-loaded query data (getScope never fetches). Adding a value? Declare it in the manifest, emit it in the workspace's provider, re-sync the DB (`surface-authoring` skill) — never pass an undeclared key.
- **Every Marketing data surface carries the Copy + Copy-for-AI pair** (agent-copy doctrine). Tables get it via the canonical `MatrxDataTable` `copy` config (toolbar whole-view pair + per-row pair, filter/sort context included by the primitive); non-table sections use `SectionCard`'s `copy` prop and page/record headers use `<CopyButtons>` directly. All payloads build through `features/marketing/lib/copy-payloads.ts` (`webCopy` / `humanLines` / `webLocation`) with stable `web-*` kinds — never a hand-rolled envelope, never a hand-picked agent field list (agent data is the full row/list).

## Related features

- Depends on `components/official/matrx-data-table`, `features/shell`, `features/organizations`, and the browser Supabase client.
- The Marketing command-only transport lives in `features/marketing/crawler`; persisted Marketing reads never depend on it.
- CMS and later marketing workspaces build on the same `web.site` / `web.page` identities.
- Architecture: `docs/MARKETING_SITE_PLATFORM_PLAN.md` and `docs/MARKETING_SITE_ROUTE_ARCHITECTURE.md`.
- Cross-repo Google OAuth verification inventory, scope decisions, cleanup register, and submission gates: `../common-docs/projects/google-oauth-verification/PLAN.md`. Read it before changing Google scopes, clients, consent-screen configuration, or reviewer-facing flows.

## Doctrine compliance

- Reused: `MatrxDataTable`, `EntityModeHeader`, `RouteHeader`, official UI primitives, React Query, active-organization picker, generated database types, and `webDb`.
- Introduced: feature-local typed services/hooks and resource-specific dense workspaces. They compose platform primitives and do not fork the table, header, organization, file, or Supabase client systems.
- No Redux slice is introduced; durable state is Supabase-backed and table view state is URL-backed.

## Current work

The site/page/crawl foundation, direct live-crawl controls, dedicated technical-SEO crawl reports, analysis/finding workspaces, link/screenshot inspection, backlinks, persisted 28-day GSC keyword performance, reusable personal/org Google OAuth, GSC property binding/synchronization, app-managed PageSpeed with per-page synchronization/history/regression UI, site access/settings, and provider spend rollups are live in code. The RLS-protected `seo` schema is exposed read-only to authenticated browser clients and included in generated database types; product SEO workspaces read ordinary persisted facts directly through Supabase, while the canonical combined page-performance read and collection work run in aidream. Remaining verticals include automatic GSC keyword-market enrichment, target-keyword analysis, broader GA4 history, connection health/sync history, cross-site analysis, catalog/configuration UI, crawl scheduling UI/worker, analysis and AI-batch execution workers, actionable reconciliation/finding mutations, current-link projections, and CMS task/change/publish workflows.

## Change log

- 2026-08-11 — Claude: **Image generation floats instead of hiding behind a button spinner.** The shared marketing headless shell `runHeadlessAgent` (`lib/generate-page-image.ts` — consumed by the page image-plan card, the site media desk, and `generate-video-metadata.ts`) took a `live: {instanceId, label}` option, so every generation path opens the canonical `LiveRunWindow`. The two-step default pipeline drives **ONE** window across both runs: "Writing the image prompt" → "Rendering the image", and the finished image renders inside it. Two traps handled explicitly: the run's instance is **held past completion** (the next live run releases it) because destroying it on the way out blanks the window at the exact moment the image lands, and a launch that dies before a stream exists **closes** its own window rather than leaving it stuck on "pending" — the caller's step-attributed toast still carries the failure. Callers pass a per-subject `liveInstanceId` (`page-image:${entry.id}`, `site-media-image:${site.id}`) so two orders give two windows. Live-verified on `…/sites/[siteId]/media`: both steps re-bound one window, image rendered in it, saved to the brand library. `generate-video-metadata.ts` inherits the option and has not opted in yet.

- 2026-08-11 — Claude: **The whole crawler command family stopped showing a spinner, and stopped dying on refresh.** `analyzeSite`, `syncSitemaps`, `syncGsc`, `checkSiteLinks` and `fetchPageNow` have always streamed NDJSON progress (`*_progress` events carrying the server's own sentence plus a counter object) and always written a durable `web.crawl_session` row — and six callers passed no callbacks at all, `await`ed, and rendered `Loader2`. A reload aborted the stream and left a blank panel over work that kept running server-side. Now every one of them goes through **`useSiteCommandRun`** (`data/useSiteCommandRun.ts`): the run streams into the floating **`SiteCommandRunWindow`** (`features/window-panels/windows/marketing/`, opened by `features/overlays/openers/siteCommandRunWindow.tsx` — THE FLOATING LAW: no page shift, the user keeps working underneath), and on mount the hook looks for a live session of that command and **rejoins it**, reopening the window with a "Rejoined" badge and settling from the session row when it goes terminal. The generalization is of the existing crawl primitive, not a second one: `getActiveCrawl` became `listActiveCrawlSessions` (a LIST — several commands run at once), `useSiteCrawlActivity` now exposes `activeSessions` alongside `activeCrawl`, which is finally *crawl-shaped only* (`scope.mode` in full/list/initialization/homepage) so a GSC sync no longer reads as "Crawling" in the site header — it says "Syncing Search Console". `SiteCommandFeed` renders both event shapes through the ONE existing `presentLiveCrawlEvent` (extended with the `*_progress` cases), shows only counters that have actually moved, links its session id to the durable session page, and strips ANSI/terminal decoration off a raw upstream failure without dropping a word of it. Buttons no longer disable into a spinner: while a run is live they read **"Watch progress"** and reopen its window. Live-verified end to end against datadestruction.com — analysis (rejoined across a full reload at 3m49s), sitemap sync (progress → real server failure, surfaced verbatim), link check, page fetch, GSC sync. Store: `crawler/command-run-store.ts` (module-scoped `useSyncExternalStore` buffer — the window renders at the overlay root, outside this provider, and a page-fetch stream emits at page speed); command vocabulary: `crawler/site-commands.ts`.

- 2026-08-11 — Codex: **PageSpeed coverage became automatic, transparent, and manually steerable.** The site Performance workspace now reports the live scheduler cadence, next run, and failure/retry state from aidream instead of implying that the coverage meter advances by magic. An incomplete site gets a ranked ten-page **Next in line** queue from the same canonical coverage planner the scheduler uses—real 28-day GSC clicks/impressions first, then internal importance—with page doors and a per-row Test now action through the existing streamed `syncPagespeed` command. The scheduler itself moved from one cancellation-prone 1,500-request sequential run to resumable ten-request batches every ten minutes with bounded concurrency and least-covered-site rotation.
- 2026-08-11 — Claude: **The site audit stopped shipping the database to the browser, and stopped being 469 rows from a blank page.** `fetchSiteAuditRows` paged every `web.page` row plus every latest snapshot's full `seo_metrics`/`audit_metrics` jsonb into the client (6.9 MB for allgreenrecycling.com's 4,531 pages) and grouped them in JS behind a hard 5,000-row ceiling that **threw** rather than truncating — allgreenrecycling was 469 crawls from rendering nothing at all, and `fetchSiteAuditTrendRows` had the same shape over 20,000 snapshots. Both are now ONE RPC: `web.site_audit_rollup(uuid)` / `web.site_audit_trend(uuid)` (`web_site_audit_rollup_server_side.sql`, SECURITY INVOKER + STABLE, so RLS is the ceiling exactly as for `v_page_list`), **no cap on either path** — 605 KB and ~0.8 s for allgreenrecycling. Machine resources are excluded by CALLING the shared rule — both halves: `web.is_machine_resource_url` AND `web.is_resource_content_type`. The first cut of these RPCs called the URL half but **inlined** the content-type half under a comment asserting the copy was identical; it was not, and it re-hid the 97 `other`-typed pages the shared function had just restored. A copy is a future outage: aidream's `scripts/check_page_class_mirrors.py` now diffs the LIVE catalog against `matrx_utils.web_page_class` on every release and screams on any inlined copy (it found a third, `web.v_site_kpis`, on its first run). Alias URLs — the same document under a second URL, 47 live, mostly `http://` twins — are skipped too, matching `v_page_list.is_canonical` and matrx-scraper's `pages_skipped_alias`; the one thing Postgres had to compute is URL quality, added as `web.url_quality_metrics(text)` returning the `StoredUrlQualityMetrics` shape verbatim — the third mirror of `evaluateUrlQuality`, byte-identical over all 10,437 distinct registry URLs. `lib/audit-rollup.ts` stays as the jest-tested SPECIFICATION of the counting semantics (URL issues always counted, SERP issues always warnings, other sections by their own severity), and the cutover was gated on a diff of both implementations across 5 live sites — every count, verdict, pass, issue, sample, and trend point identical. The RPC's `jsonb` is narrowed by `parseSiteAuditRollup` / `parseSiteAuditTrend`, which throw loudly rather than hand a half-shaped rollup to the workspace. Finally, the surface now **states its coverage** instead of leaving it implied: `auditCoverageStatement` prints "Audited all N pages … Nothing is capped or sampled." on screen, in every copy/export, and as the new `audit_coverage_statement` surface value.
- 2026-08-11 — Claude: **Site settings: the data-source table stopped lying, and the page became agent-writable.** The collection panel printed the server's `next_due_reason` into a "Next due" column, so a FAILED backlink run explained itself with "scheduled DataForSEO backlink refresh is not enabled for this site" — an answer to a question nobody asked, standing where the real failure should have been. It also wore a floating "not connected" chip on Backlinks while that provider held 274 rows and a successful run an hour earlier (the site's *schedule* flag was off, not its credential), reported rank tracking as "not yet built" after WS-10 shipped it with a live route on the same site, and offered no action on any state it named. **Connection, health and schedule are three different facts and now occupy three columns** of a real `MatrxDataTable` (sort + filter + row detail + copy/export), every status DERIVED live in `data/collection-status.ts` from provider bindings, the `seo.collection_run` ledger, and tracked rank targets — never a stamped string; schedule is evidence-derived too (a real due date, the site's own backlink cadence, or proof from the ledger that the platform has started a run itself). Every row carries its doors — Run now (GSC/GA4 through the existing site-level syncs), Connect/Check setup, Open data — declared once per provider in `data/collection-providers.ts` along with what it collects in plain English. Clicking a failing row shows the recorded cause through `BackendFailureDetails`, which is how the live DataForSEO defect ("live endpoints accept exactly one task") became visible at all. New `data/collection-runs.ts` is the ONE reader for that ledger; `getLatestPagespeedFailure` and `getLatestAnalyticsFailure` were two private copies of its query and now call it. **New surface `matrx-user/marketing-site-settings`** (30 values, 9 roles, DB-synced) exposes identity, the full crawl-policy draft, pattern issues, `unsaved_changes`, and the same status rows the panel renders — one React Query entry, not a second request — and declares four `ask`/`draft` write targets: `crawl_scope_patterns`, `crawl_budget`, `crawl_behavior`, `site_lifecycle`. Crawl policy is exactly the expertise our user does not have ("exclude the tag and author archives", "this site is 30k pages, cap it"), so handlers (`components/settings/crawl-policy-writes.ts`) stage into the SAME setters the user's typing drives and throw on a bad shape instead of coercing. Visibility, delete, and credentials stay human-only. `/settings` left the folded-route list in `route-to-surface.ts` — an agent-writable page cannot borrow its parent's surface.

- 2026-08-11 — Reputation and AI Visibility now watch their own runs. Both
  surfaces mount the canonical `LiveRunDisplay` on their adopted `requestId`,
  so the model's output streams into the page from the first token instead of
  sitting behind a spinner until a chunk-gated block appeared. Keyword research
  already rendered its adopted stream through the canonical pipeline (with a
  durable-artifact fallback for rejoined runs) and was left alone.
- 2026-08-11 — Codex: **Backlink screenshots and rank semantics are reviewable.**
  The canonical backlink drawer/WindowPanel renders the stored link-evidence image
  through `CaptureThumb`, so the standard file viewer supplies full-size, share,
  and download controls; the backlinks table adds a direct “View captured link”
  door. The record header now has explicit source and target page buttons, cache
  identifiers remain absent, screenshot failures are visible, and provider rank
  zero renders as “Not ranked” rather than looking like missing or contradictory
  data.
- 2026-08-11 — Codex: **The canonical backlink record is section-copyable and no longer leaks cache internals.** `BacklinkEnrichmentDetail` now uses the shared Marketing `SectionCard` + agent-copy path for Link identity, provider facts, redirects, analysis errors, assessment, recommended action, captured evidence, human ruling, and lifecycle, with focused human and Copy-for-AI payloads at each section. `source_capture.cache_key` / legacy `cacheKey` are stripped by the tested `backlinkCaptureForUi` boundary before any fact card, raw inspector, or section payload can render them. The raw-record slab is now a collapsed **Technical details** section with the canonical `JsonInspector` AI action, internal page/reputation doors open in a new tab so the backlink workspace is preserved, and the last-error card carries the existing one-click Re-analyze action beside the problem.
- 2026-08-11 — Claude: **The site AI Visibility route joined the mode rail, and the retired site cost mode left it.** `.../sites/[siteId]/ai-visibility` existed on disk with no `MARKETING_SITE_SECTIONS` entry, so the permanent rail this doc already promised never showed it and its metadata fell back to Overview's; it is now registered (letter `Av`, `Radar` icon) and the filesystem drift test passes. The same test was also red the other way: D149 deleted `.../sites/[siteId]/cost` but left its section and icon behind, i.e. a rail link to a 404 — both are gone, and the marketing admin map lost its dead `site cost` / `batches` / `batches/[batchId]` rows and gained the AI Visibility route. **The three streamed `unknown → Record<string, unknown>` casts in `useAiVisibility` / `useCompetitorAutopsy` are gone, not moved:** `streamData` narrows with `isJsonObject`, and the completed/recovered analysis payload passes through `readAiVisibilityResult` — a field-by-field reader the compiler checks against the generated `AiVisibilityResult`, so a payload that cannot be a result now reports a stream defect to the user instead of being asserted into the type. The workspace-level `/marketing/cost` page is untouched.
- 2026-08-11 — Claude: **AI Visibility and Competitor Autopsy are live end to end — the half-deployed state is closed.** Both surfaces shipped calling aidream endpoints production did not serve, because the aidream half was never committed: the service modules existed only as untracked files on one laptop and **no FastAPI router registered either one**. Registered `POST /seo/sites/{site_id}/ai-visibility/analyze` and `POST /seo/sites/{site_id}/competitor-autopsy` in `seo_collections.py` (typed bodies `AiVisibilityAnalyzeBody` / `CompetitorAutopsyBody`, `run_streamed_command` final events `seo.ai_visibility_completed` / `seo.competitor_autopsy_completed` — exactly the kinds the hooks already listened for), and added `AiVisibilityResult` + `CompetitorAutopsyResult` to the `SeoCommandResult` union so the result documents are real OpenAPI contracts rather than opaque objects. **Both routes are live in production and `pnpm sync-types` regenerated `types/python-generated/*` from the deployed contract** — three of the four type errors were the missing schemas and vanished on regeneration; the fourth (`hasAnalysis`) was a genuine defect: it demanded `Record<string, unknown>` while the persisted column is `Json`, so it now takes `unknown` and narrows, which also stops an array or scalar from reading as "Analyzed". No cast, no suppression. **Two built surfaces were unreachable:** `CompetitorAutopsyWorkspace` had no route at all and `AiVisibilityHub` was unmounted, while `/marketing/competitors` and `/marketing/ai-visibility` both rendered `MarketingComingSoon` — both routes now render their real workspace and both `lib/coming-soon/registry.ts` entries are deleted. Verified against production, not localhost: a real AI Visibility run on prpinjectionmd.com completed with live ChatGPT/Claude/Gemini/Perplexity answers, **29 cited pages captured** and claims persisted. The first live autopsy exposed a real backend defect — competitor discovery requested the `COMPETITORS` capability, which makes the DataForSEO adapter demand a canonical normalizer `labs.google.competitors_domain` has never had, killing every run before its first provider call; it now uses `RAW_PROVIDER`, matching how the service actually consumes the payload and what the relevant-pages call in the same file already did.

- 2026-08-11 — Claude: **D149 resolved by RETIRING the `web.batch_*` surface — it was never a working feature, it was scaffolding that never had a producer.** Evidence, not opinion: of 16,236 `web.analysis_result` rows, **zero** carry a `batch_id`; `runtime.global_execution` has **never** recorded a single `web_batch_item` link (the sole input to every `v_cost_by_*` view, so all five projected $0 for their entire existence); and no code in aidream or this repo ever wrote a `web.batch_job` / `web.batch_item` row. The relations were applied (`web_cost_view_and_index_hardening.sql`, 2026-07-19) and later dropped from the live database when execution moved to the canonical `batch.*` subsystem (matrx-batch, 2026-08-10) — so as of today these four routes queried relations that do not exist and would 404/PGRST205 for every user. Deleted: `/marketing/batches`, `/marketing/batches/[batchId]`, `.../sites/[siteId]/cost`, `BatchesTable`, `BatchDetailWorkspace`, `SiteCostWorkspace`, `CostModeButtons`, `data/operations-{types,queries,hooks}.ts`, and the `matrx-user/marketing-batches` surface (manifest + registry + route resolver + its `ui.ui_surface*` rows). **`/marketing/cost` survives and is now honest**: its "Provider spend" half reads live aidream `GET /seo/spend/summary`, so that panel IS the page (the dead "Runtime cost" toggle is gone), and the hub surface dropped `cost_rollup_mode` / `cost_rollups` / `cost_rollups_total` in code and in the DB. Nav, route metadata, route sections, and the Site Overview tile all lost their dead entries; the two retired paths now 404 rather than rendering a broken table. Batch execution is monitored where it actually lives, `/administration/knowledge/kg-cost` over `batch.*`. **Not fabricated:** `batch.provider_batch` / `batch.work_item` carry a generic purpose/link model with no site, analysis-item, or subject dimension, so there is no honest field-for-field repoint — per D149, a Marketing projection over `batch.*` is a product decision, not a repair. `pnpm type-check` on `features/marketing/data/*` is clean (~65 errors gone); the remaining marketing errors are the untouched pre-existing `seo/ai-visibility` + `competitors` drift.
- 2026-08-11 — Codex: **Marketing child routes cannot masquerade as Overview.** Site and crawl modes now come from `lib/route-sections.ts`, which also drives sibling switching and route metadata; Overview/Summary match exactly, the shared matcher is segment-safe, `/capabilities` and cross-site `/ranks` are visibly registered, and filesystem-backed tests fail on any future route/navigation drift.
- 2026-08-11 — Codex: **Backlinks now use one complete canonical record window.** Drawer and WindowPanel show every identity and evidence layer without truncating the source or target URL; Analyze opens that window, streams per-link progress inside it, and no longer shifts or blocks the workspace. Multiple exact links can run concurrently. Intelligence columns use generated `seo.backlink` query fields for real full-dataset sorting and filtering, and the item window refreshes its exact durable row independently of table order.
- 2026-08-11 — Codex: **Backlink analysis became individually controllable and
  visibly live.** The old toolbar action hard-coded 25 items, discarded every
  intermediate NDJSON event, and exposed only a spinner until the terminal
  receipt. It now offers bounded 1/5/10/25 batches, and every backlink row in
  both the Backlinks table and curated Insights lenses has Analyze/Re-analyze.
  One persistent run panel consumes command, capture, AI, success, and failure
  events; displays run id and exact counts; and invalidates the existing direct
  Supabase reads throughout the run. The table names each row's durable
  analysis state and attempt count. Provider Refresh no longer hides an
  automatic 25-link enrichment drain—discovery and AI analysis are separate,
  user-controlled jobs.
- 2026-08-11 — Claude: **The Discovery inbox is agent-writable — an agent can now CLASSIFY a review queue, but still cannot decide it.** `matrx-user/marketing-discovery` declares ONE `draft`/`ask` write target, `item_classifications`: an array of `{item_id, kind, label?}` staging the per-item type a reviewer confirms with, handled on `DiscoveryInbox`'s own provider through the same `setKindOverrides` / `setLabelOverrides` the row's dropdown and label input drive. Array-valued because triage is a queue: one call can type 20 rows behind ONE confirm, mirroring the existing bulk "Set type for selected" control. **The kind vocabulary varies per ROW**, because each category promotes into a different table — media → `web.brand_asset`, social → `web.property`, everything else (identity/fact/link) → `web.business_fact` — so the handler picks the pool from the live item and refuses a cross-pool kind naming the right list, rather than staging a value `confirmDiscovered*` would bounce at the DB. That vocabulary moved out of the component into the new React-free `features/marketing/discovery-classification.ts` (pools, `kindPoolOf`, `isKindInPool`, `defaultKind`, `LABEL_REQUIRED_KIND`), which the manifest prose, the handler, and the row Selects all import — one list, no drift. `label` travels inside the same object because the two controls commit on one Confirm click and are coupled: `other` REQUIRES a non-empty label (the user's Confirm button is disabled without one), so staging a labelless `other` is refused. The whole array validates before any state moves, so a bad entry never leaves a half-applied triage; new read value `staged_classifications` lets an agent read back what is staged. **Two fixes this forced:** `customLabel` was per-row local state invisible to the parent, so it was lifted to `labelOverrides`; and BULK Confirm now sends each row's staged label instead of the hardcoded `null` it always sent — previously a label the user typed was silently dropped on the bulk path, and a labelled `other` row was skipped for want of a label it actually had. **Deliberately NOT writable: Confirm, Dismiss and Delete.** The rule this feature is built on — the machine writes only `web.discovered_item` candidates, humans promote to confirmed truth — is exactly the line the write half stops at; dismiss commits a decision just as confirm does. Selection is excluded too, because the same selection drives Bulk delete. Live-verified with a real agent run on prpinjectionmd.com: three classifications staged across two pools in one dialog, a decline left the row untouched, cross-pool / unknown-id / missing-label calls all came back as the handler's own errors, and a request to confirm-and-delete produced no tool call at all.
- 2026-08-10 — Claude: **The Site Media workspace is agent-writable — and making it so lifted the image-order form out of the view that owned it.** `matrx-user/marketing-site-media` declares 3 `draft`/`ask` write targets: `media_order` (one partial patch of `type` / `brief` / `style` / `width` / `height`) plus `media_standards_slots` / `media_standards_notes`, so "draft me a hero for this medspa and set our standard sizes" becomes a staged order and a staged standards table the user reviews. **The order form had to MOVE to be writable.** Its five fields were `useState` inside `GenerateMediaView`, which only mounts on `?view=generate` — a handler registered there would have disappeared on six of the workspace's seven views, and applying it would have had to switch views mid-run and unmount whatever else was registered. The draft now lives in `SiteMediaWorkspace` as a controlled `MediaOrderDraft` (which subsumes the old one-shot `pendingBrief` handoff and its consume-once effect, so "Order replacement" and "Use as brief" feed the same state an agent writes), `SiteMediaWriteTargets` registers `media_order` workspace-wide, and the surface now EMITS that draft as `media_order_draft` with resolved dimensions — closing the readiness gap the manifest had recorded against itself. The standards targets went the other way on purpose: that draft genuinely belongs to `MediaStandardsView`, so they register there and are offered only while the Standards view is open. `media-order-presets.ts` is now a real vocabulary (`as const satisfies` → `MediaOrderPresetId`, `MEDIA_ORDER_PRESET_IDS`, `isMediaOrderPresetId`, all derived from the array the menu renders), interpolated into the manifest's agent-facing contract and validated against in the handler. Pure validation core with 26 unit tests in `lib/site-media-write-targets.ts`. **Deliberately not writable:** generating the image (it spends money and mints a `web.brand_asset` row — the user presses "Order this image"), and promoting / uploading / deleting assets or flipping `is_primary`. Crawled alt text is observed evidence with no editable field here; authoring alt text remains `marketing-page`'s `page_image_alts`. Live-verified with a real agent run on Cosmetic Injectables Medspa (apply, decline, undeclared-target refusal, and a bad preset id coming back as the handler's own error).
- 2026-08-10 — Claude: **All eight marketing version-guarded writes converged onto the shared `utils/supabase/guardedUpdate.ts` helper** (platform optimistic-concurrency contract, `common-docs/systems/optimistic-concurrency/FEATURE.md`): `updatePageIntent`, `updatePageDesiredValues`, `savePageContent`, `updateSiteIdentity`, `updateBrand`, `updateProperty`, `updateBrandAsset`, `updateBusinessFact`. Behavior unchanged — each site keeps its exact "changed in another session" copy (a local `guardedUpdateOrThrow` maps both conflict and row-gone to it, as before), and `updatePageDesiredValues` keeps its read-merge-write + one silent retry, now keyed off the typed `conflict` result instead of a blind 0-rows recursion.
- 2026-08-10 — Codex: **Backlinks became source-page intelligence, not a provider spreadsheet.** The workspace now reads stable `seo.backlink` rows and first-party `seo.referring_domain_profile` rows directly from Supabase; exposes capture/analysis progress; triggers bounded enrichment independently or after refresh; shows page type, content summary/topics, source-target relevance, contextual/anchor/editorial quality, controllability, risk, and the recommended action in the main table/detail, Insights lenses, referring-domain directory, per-page backlink card, Copy-for-AI payload, and surface context. Source, target page, and referring domain all have doors. Human rulings use narrow RPCs and override resolved interpretation without erasing deterministic or AI evidence.
- 2026-08-10 — Claude: **The Keyword Research workbench is agent-writable — the seed is staged, the run is never fired.** `matrx-user/keyword-research` declares 2 `ask` write targets. `research_seed_keyword` (`draft`, `{ keyword: string }`) stages the primary keyword into the Research input, so "what should I research next?" ends with the phrase already in the box; the handler lives in `KeywordResearchLauncher.tsx` — the component that OWNS the input — registered through the `useSurfaceWriteHandlers` child seam, landing via `setPrimaryInput` + `onKeywordChange`, the exact pair the input's `onChange` calls. `keyword_selection` (`ui`, `{ keyword_ids, mode?: "replace" | "add" }`) moves the explorer's row checkboxes through the workbench's own `setSelectedIds` — the same setter `toggleSelected` uses — so a strategist agent can answer "which of these are worth acting on?" by selecting them, and the toolbar's bulk bar reacts as if the user had clicked. Ids are validated against the rows VISIBLE RIGHT NOW (the same `sorted` projection `visible_keywords` emits) and one unknown id rejects the whole write, mirroring the invariant the explorer already enforces on its own selection: it never acts blind. **Running the research is deliberately NOT a target** — it is an outbound, billable third-party pipeline (LSI agent → ingestion → paid volume batch), so the Research button stays the user's press, exactly as the crawler leaves Start crawl and the scraper leaves Scrape alone. The bulk archive stays human because destructive stays human, and the explorer filter was considered and rejected as a mechanical toggle that earns no target. Both handlers throw while a run is in flight rather than mutating state the running request owns. Because the launcher is SHARED with the floating `KeywordResearchWindow` (a different surface), it registers handlers only when a host opts in via `writeSurfaceName`. Live-verified with a real Badass Agent run: one message, one ask dialog per target carrying the contract prose, Apply staged the seed (Research armed but unpressed) and selected the two rows named by phrase, decline returned a clean `declined` result with no error, and a "run it / archive them" request reached no tool at all.
- 2026-08-10 — Claude: **The New Crawl workspace is agent-writable, and the crawl-command vocabulary now has ONE home.** `matrx-user/marketing-crawls` declares 3 `draft`/`ask` write targets — `crawl_options` (partial patch: `max_pages`, `concurrency`, `render_mode`, and the four toggles) plus `crawl_include_patterns` / `crawl_exclude_patterns` (full-list replacements) — so "only crawl the blog, skip tag pages, cap it at 200" becomes a staged launch form the user reviews. Handlers live on `NewCrawlWorkspace`'s own provider and land through the same setters the user's typing uses; pattern lists run the SAME `invalidCrawlPatterns` regex gate as the form (the gate that exists because the server used to accept broken regexes and silently widen a constrained crawl), and every handler throws rather than coercing. **Starting the crawl is deliberately not a target** — it spends real time and budget against the client's own server, so the human presses Start crawl; the sessions table registers no handlers at all (immutable records, pure query state). New `crawler/crawl-options.ts` is THE vocabulary: `CRAWL_RENDER_MODES` (which `CrawlStartOptions["render_mode"]` now derives from, and which `crawl-defaults.ts` validates through `isCrawlRenderMode` instead of its own re-typed list), `CRAWL_COMMAND_TOGGLES`, the page-limit/concurrency bounds, and the canonical labels — the launch form renders from it, the manifest interpolates it into the model-facing contract, and the handler validates against it, so a promise made to an agent cannot drift from what the form accepts. Live-verified with a real agent run on vasaro.com (apply, decline, undeclared-target refusal, and an out-of-bounds value coming back as the handler's own error).
- 2026-08-09 — Claude: **The per-page VERDICT layer now has a browser mirror.** `features/marketing/seo/audit/checks.ts` is the byte-identical TypeScript twin of the check section of aidream `matrx_scraper/seo_audit.py` — `PageEvidence`, `CheckOutcome`, `Remediation`, every `check_*`, and the `PAGE_CHECKS` registry (41 checks, same keys in the same order). The browser could already compute METRICS (title pixel width, social card, indexability) but not VERDICTS, so any UI wanting to say "this title is too long — warn, score 60" had to round-trip to the server or re-invent the rules in TSX; that re-invention is the three-way drift the Python consolidation had just eliminated. Statuses, scores, issue counts, evidence payloads AND the reasoning strings all match character for character — a reasoning string is a user-facing expert sentence, so two implementations disagreeing means one page gets two explanations. **Thresholds are declared once**: the CAPS constants mirror `seo_audit.py`, and the SERP length limits are IMPORTED from `seo/serp/metrics.ts` (`TITLE_LIMITS` / `DESCRIPTION_LIMITS`), never re-declared. Parity is locked by `checks.parity.test.ts` against `__fixtures__/page-checks-parity.json`, which is GENERATED — never hand-maintained — by `packages/matrx-scraper/scripts/generate_page_checks_parity_fixture.py` (94 evidence cases × 41 checks; healthy page, each single defect, and an all-evidence-missing page, and every check is scored in at least one case rather than only reaching its `n_a` branch). Two Python semantics the mirror had to port rather than approximate, both pinned by their own fixture case: `round()` is half-to-EVEN (`Math.round(46.5)` = 47, Python 46) and so is `f"{x:.1f}"` (`(2.25).toFixed(1)` = "2.3", Python "2.2"). Change a rule on either side → change both in the same unit of work and regenerate; the check-section header in `seo_audit.py` and the header of `checks.ts` both say so, and the test fails if you forget.
- 2026-08-09 — Claude: **Brand cockpit agent-writable.** `matrx-user/marketing-brand` exposes verified ask-policy `brand_profile` and `brand_identity` targets through canonical version-guarded `updateBrand`; brand name and confirmed business facts remain human-owned.

- 2026-08-09 — Claude: **Findings became actionable for a non-SEO, and unknown checks became a non-event.** New pure registry `lib/finding-remedies.ts` (THE FINDING FALLBACK LAW + THE REMEDY RULE, both in Invariants): 18 registered remedies over the server's `PAGE_CHECKS` — AI one-clicks for titles/descriptions/H1/thin content/alt text (`AssistChip` + `makeEphemeralAssist` → `seo.page_analyzer`, reusing the assists primitive the GSC producer already uses), copy-able owner instructions for the indexability and transport checks (robots/canonical/4xx/5xx/redirect chain+loop/mixed content/page weight/TTFB/pagination) — plus a GENERIC remedy so a key added server-side today renders complete tomorrow with no deploy here (35 unit tests, incl. all eight incoming keys and a fabricated one; verified live by unregistering a key and watching the fallback render). New `components/analysis/FindingRemedyCard.tsx` leads the detail page with the analyzer's own sentence + the fix + the doors. New `data/finding-mutations.ts` closes the missing write half (`suppressFinding` with a required reason / `unsuppressFinding` / `acknowledgeFinding` / `reopenFinding`, direct-to-Supabase). `listSiteFindings` now joins the latest result's `metadata.reasoning` and the catalogue label (`FindingListRow.reasoning` / `item_label`) so the REGISTER reads as English without opening a row. Doors added everywhere a page is named (findings register, priority queue, audit worst-pages): page workspace + live URL in a new tab, and the finding title is a real link (D112 `href`).
- 2026-08-09 — Claude: **The public analyzers' "Fetch from URL" works signed out** (FOUND_DEFECTS D137 closed). `/seo/metadata` and `/seo/social-preview` called `POST /scraper/quick-scrape`, and aidream mounts the whole `/scraper` router behind `require_authenticated` — every anonymous visitor got a 401 and a "please sign in" toast on a page that never offers sign-in. Both analyzers now read meta tags through the guest-friendly `/seo/public/page-audit` route (`require_guest_or_above`), whose canonical `matrx_scraper.seo_audit` payload already carries `title` / `meta_description` / `canonical` / the full `og` + `twitter` maps: ONE new consumer `features/marketing/seo/public-tools/usePublicPageMetadata.ts` (fetch + `toSeoMetaFields` / `toSocialMetaFields` projections), and the now-dead `seo/serp/extract-seo-from-scrape.ts` deleted. Verified signed-out in the browser. Server half (aidream `services/seo/command_runs.py`): `run_streamed_command` emitted its terminal `final_event` only on the completed-REPLAY branch, so a first-ever run streamed stage events and just ended — `/seo/page-audit`, `/seo/structured-data` and `/seo/robots-tester` rendered nothing until the same URL was submitted twice. Fresh claims now emit it too.
- 2026-08-09 — Claude: **Backlinks Insights lens tables scroll again.** `BacklinkInsightsTab`'s view wrapper was a plain block `div` (`min-h-0 flex-1`), so `BacklinkObservationTable`'s root (`flex min-h-0 flex-1 flex-col`) and the `h-full` `MatrxDataTable` inside it never got a bounded height: on every lens (`?insight=toxic|best|new|lost|broken`) the table grew to full row height and the workspace's `overflow-hidden` clipped it — no scrollbar, rows past the fold unreachable. The wrapper is now `flex min-h-0 flex-1 flex-col`. The Links tab and the Anchor-profile view were unaffected (already flex / self-bounding with `h-full`), which is why the break looked lens-specific. The defect CLASS is now guarded at two layers: `pnpm check:scroll-chain` (static, both in-file and across component boundaries) and `useClippedContentGuard` on every `MatrxDataTable` (runtime → Error Inspector `layout-scroll-chain`).
- 2026-08-08 — Claude: **Graded Copy-for-AI variants + copy/export sweep.** New shared helpers: `buildGroomerPresetPayload`/`groomerPresetVariants` (`components/agent-copy/groomer-types.ts` — a page's ONE groomer section list now also feeds Balanced/Minimal `AiCopyMenu` variants, Everything staying the `CopyButtons` `agent` escape hatch) and `keyFieldsAiVariant` (`lib/copy-payloads.ts` — the table-toolbar "Key fields" projection variant). Page-level variants on the medium/massive tabs (keywords, ranks, findings, analysis, audit, links, crawls, discovery, cost); bounded tabs (coverage, crawl summary, sitemaps list) deliberately keep the plain pair; raw-row tables (findings, analysis, links) carry table-level `copy.aiVariants` key-fields projections. Gaps closed: `/marketing` hub header pair (pillar/entry map), Site overview "Needs attention" card copy, Access + Brands list converted from hand-rolled row copy to `MatrxDataTable` `copy` configs (view copy + JSON/CSV export now free), GA4 analytics settings card pair, brand-cockpit per-item pairs (websites/properties/facts/assets), `CrawlSubnav` hover session-record pair (covers URLs/Reports/Snapshots/Links/Logs sub-routes), crawl-report header pair, audit ExportMenu, FindingDetail/SitemapDetail JSON flavor.
- 2026-08-08 — Claude: **Per-page audit analysis is LIVE end-to-end** (Arman's ruling: commission the `web.analysis_result` workers — empty platform-wide since design). Server half: matrx-scraper 0.1.96 `web_crawl/analysis.py` runs 15 deterministic catalogue checks per page (titles/metas presence+length+duplication, H1, thin content, image alt, robots conflicts, canonical presence/conflicts, broken+redirecting internal links, exact duplicates) from stored evidence, writing immutable results with visible reasoning and reconciling `web.finding`; runs post-crawl automatically and standalone at `POST /crawler/sites/{id}/analyze`. FE half: the Sites portfolio **Health column is unhidden** (weighted `v_site_score`, server-served sort via the `health_score` branch in `listSites` — scored sites ranked, unscored appended, never dropped), the Audit tab gained the **Catalogue-analysis panel** (`CatalogueAnalysisPanel` over `getSiteAnalysisOverview` + `useSiteAnalysisOverview`; Analyze-now command via `analyzeSite`), findings rows' Page cell and Finding detail both drill to the **page workspace**, and the analysis/findings/priority-queue workspaces now show real rows. Verified against a real crawled site's evidence (44 pages × 15 checks → 660 results, 28 findings, idempotent re-run; score views populate).
- 2026-08-08 — Claude: **`matrx-user/marketing-site-media` surface declared** for the site Media workspace (previously fell back to `matrx-user/marketing-site`). Manifest `features/surfaces/manifests/marketing-site-media.manifest.ts` (5 own values: `media_view`, `media_inventory_summary`, `brand_library_assets`, `media_standards`, `research_images_summary`; 5 curated groups; `media_auditor` + `art_director` roles; readiness `partial` pending the live binding test), runtime builder `lib/scopes/site-media-scope.ts` (pure summarizers over the views' cached query data — getScope never fetches), nested `SurfaceRuntimeProvider` in `SiteMediaWorkspace` spreading `getBaseValues()`, `media` vertical added to `resolveMarketingSurface`, DB row + sync applied and verified live (27 values / 9 roles). `GenerateMediaView` now launches its two-step image pipeline with the new surface name.
- 2026-08-08 — Claude: **Media workspace gained the Sources view: free stock supply + brand portals.** New `StockSourcesView` (`?view=sources`, between Research and Generate) with two halves: **Brand portals** — the brand's press-kit / media-portal URLs as `web.brand_asset` kind `portal` rows (add/remove inline; rendered as clickable link tiles in the Library view too); **Stock search** — Unsplash via the canonical `/api/unsplash` proxy (`hooks/images/unsplashClient.ts`, which gained `photos.trackDownload`), orientation filter + paging, one-click save into the brand library. Saves are DURABLE-first: bytes ingest through `useFileUpload({kind:"external_url"})` → `createBrandAsset({fileId, source:"stock"})` with photographer attribution + Unsplash License recorded in `notes` and provenance in file metadata; byte-fetch failure falls back to the permanent Unsplash CDN URL as `source_url`. The Unsplash `photos.trackDownload` guideline event fires on every save. DB: `brand_asset_kind_check` gained `portal`, `brand_asset_source_check` gained `stock` (`migrations/web_brand_asset_portal_kind_stock_source.sql`, applied live + ledgered).
- 2026-08-08 — Claude: **D130 fixed — headless image-gen promise always settles on a terminal run.** Root cause was platform-level (`process-stream.ts` only exited when the transport closed; a server that reached terminal but held the socket open with heartbeats hung `executeInstance` forever) — fixed there with the 30s terminal-settlement guard (see `features/agents/docs/STREAMING_SYSTEM.md`). `lib/generate-page-image.ts` hardened: `TERMINAL_STATUSES` now the real `RequestStatus` values (old set had nonexistent "completed"/"failed", missed "timeout"/"cancelled"), and `extractMatrxFileId` accepts `fileId` OR the drifted `file_id` spelling, plus terminal-status blocks whose `status` never flipped to "complete". GenerateMediaView's 5-minute `Promise.race` stays as a loud backstop.
- 2026-08-08 — Claude: **Redirect & canonical CHAIN evidence in the crawl reports** (Screaming Frog parity item 6.a). Response-codes renders every persisted hop (`web.crawl_url.metadata.redirect_chain`, matrx-scraper ≥ 0.1.95, written on success AND failure paths so redirect-to-404/410 survives) with loop / redirect-to-missing / chain findings via `summarizeRedirectChain`; pre-hop-capture crawls get an explicit re-crawl banner from the `crawlHasChainEvidence` probe. Canonicals resolves session-wide canonical chains (`listCrawlCanonicalMap` → `buildCanonicalLookup` / `evaluateCanonicalChain`): chain / loop / canonical-to-error / target-not-crawled states, resolved A → B → C paths, target HTTP status. Canonical matching unified on the parity `normalizeUrlForComparison` (killed the local trailing-slash-only compare). Shared hop parser `parseRedirectChain` in `lib/snapshot-content.ts`.
- 2026-08-08 — Claude: **Duplicate-content clusters in the crawl content report** (Screaming Frog parity item 6.b). Scraper (matrx-scraper v0.1.94) writes versioned capture-time fingerprints to `web.snapshot.extracted.fingerprint` (exact sha256 + 64-bit shingle simhash over the audited visible text); the content report gained a Pages | Duplicates toggle rendering exact-duplicate groups and near-duplicate connected components above a visible 80–100% similarity threshold (default 90%), each cluster listing pages with word counts and page-workspace links. Pure clustering in `lib/duplicate-clusters.ts` (unit-tested union-find over one representative per exact group; versions never cross-compared); session-wide minimal fetch `listCrawlFingerprints` (JSON sub-path select, 5,000-row cap, loud truncation); fingerprint narrowing only via `parseSnapshotFingerprint`. Pre-fingerprint crawls get an explicit "re-crawl to populate" empty state — no backfill worker by design.
- 2026-08-08 — Claude: **Dismiss, not Delete, on crawler-observed rows** (Arman's ruling: the crawler represents reality). Pages + sitemaps user verb renamed Delete→Dismiss end-to-end (service `dismissPage`/`dismissSitemap`, hooks, EyeOff row actions, revive-aware confirm copy); new `restorePage`/`restoreSitemap` + deliberate Dismissed destinations (Pages `?scope=dismissed` → `DismissedPagesTable`; Sitemaps header toggle); server-stamped `metadata.dismissals` narrowed via `lib/dismissals.ts` (unit-tested) and rendered as the `PreviouslyDismissedBadge` on revived rows. Crawl sessions keep real Delete.
- 2026-08-08 — Claude: **Discovery inbox scales: controlled pagination + bulk review** (board bullet closed; browser-verified dismiss/restore round-trip). `listDiscoveredItems` returns `PagedResult` with an exact count and bounded `.range()` (the silent `.limit(500)` is gone); the inbox has a true pager (50/100/250, "Showing X–Y of Z", auto-walks back when the last page empties) and multi-select (per-row / per-category / whole-page) with bulk confirm (sequential through the canonical per-item promotion functions, per-item failures named; Other-typed items skipped loudly), bulk dismiss, bulk restore, bulk type-assign (single-pool selections only), and confirm-gated bulk delete. New service verbs `bulkConfirmDiscoveredItems` / `bulkDismissDiscoveredItems` / `bulkUndismissDiscoveredItems` / `bulkDeleteDiscoveredItems` + matching hooks.
- 2026-08-08 — Claude: **Brand asset authority + brand/site lifecycle atomicity** (both board bullets closed; `migrations/web_brand_asset_primary_and_site_property_lifecycle.sql` live + trigger-tested in a rolled-back transaction). `BrandAssetEditorDialog` uploads through the canonical file handler (staged preview, replace-file on edit, `source='uploaded'`); cockpit asset tiles render `file_id` assets via `CaptureThumb` (canonical Files pipeline + platform file viewer). DB: one atomic primary per (brand, kind) (`_single_primary` + partial unique index), primary identity-URL sync to `web.brand` (`_sync_brand_identity`), site↔website-property soft-delete/restore cascade (`_cascade_website_property`), brand soft-delete guard (`_soft_delete_guard`); 7 drifted live properties on deleted sites/brands backfilled. FE `deleteBrand` preflights live properties as well as sites.
- 2026-08-08 — Claude: **Crawler robustness (FE half).** ONE `crawl_defaults` round-trip: `features/marketing/crawler/crawl-defaults.ts` (full `CrawlStartOptions` contract, merge-not-replace, all 4 render modes) replaces the two lossy partial mappers in Site settings / launch form — advanced fields (depth, patterns, throttle, seed URLs, list mode) now survive a settings save, and `browser_with_screenshot` is no longer silently downgraded. Site settings gained the full default-crawl-policy editor; launch form + settings both take include/exclude regexes with inline validation and refuse to start/save on an invalid pattern (server historically failed open). `/crawls` is active-crawl aware: running rows tick their duration live and carry Cancel; Start becomes "Open live crawl" while one runs; deleting an active session (or a site with one) cancels-to-terminal first. Crawl summary gained Cancel + Watch live + a 5s poll fallback. Live event vocabulary now includes `urls_classified`/`page_captured` (server emitted them; FE dropped them). Warning feed rejects dump-shaped messages (ANSI/query/args/traceback) per the presenter contract. Server half (one-active-crawl guard, regex 422, tldextract scope, transient-failure page-state fix, deleted_at-reviving upserts) lands in aidream `packages/matrx-scraper`.
- 2026-08-06 — Codex: consolidated the direct-to-Google OAuth verification campaign into `common-docs/projects/google-oauth-verification/PLAN.md`; the two stale repo-local Google OAuth plans now point there. No runtime scope or OAuth configuration changed in this documentation pass.
- 2026-08-04 — Claude: **Search Console v2 — Dig Here / Watchlist / New Pages** (doc: `features/marketing/search-console/FEATURE.md` § "The three method tabs"). Dig Here: a working rules engine (seeded adoptable templates + user/org rules in `seo.gsc_dig_rule`; stateless whitelist-validated `seo.gsc_perf_dig` — no dynamic SQL; drafts preview through the same RPC). Watchlist: watch any query/page from any table (canonical `user_entity_state` favorites; `seo.fn_upsert_keyword` bridge; anchored `seo.gsc_perf_watch` keeps zero-impression rows visible). New Pages: manual launch tracker on `web.page.launch_tracking` with the first-GSC-impression milestone from `seo.gsc_perf_page_first_dates`. Shared metric columns extracted to `search-console/lib/columns.tsx`.
- 2026-07-30 — Claude: **shipped `/marketing/search-console` — the GSC data dashboard** (sub-feature doc: `features/marketing/search-console/FEATURE.md`). Cross-site portfolio landing + per-site deep view: KPI tiles that toggle chart series, recharts performance chart with prev/YoY compare overlay, generic dimension tables (queries/pages/countries/devices/appearance) on MatrxDataTable controlled mode over the new `seo.gsc_perf_*` INVOKER RPCs (narrowest-profile accuracy contract, latest-fact dedup, weighted position — see `migrations/seo_gsc_perf_rpcs.sql`), GSC-style filter chips, queries↔pages cross-drills, multi-instance `gscDrilldownWindow` floating panels (right-click any row; panels re-drill into panels), Copy/JSON/Copy-for-AI on every element, on-demand canonical sync button. Data spine (aidream): new `POST /seo/sites/{id}/gsc/search-performance/sync` + nightly `seo_gsc_sync` scheduler with derived watermark + 16-month backfill into `seo.search_performance_daily`. MatrxDataTable rows now stamp `data-row-id` (the context-menu-v3 join point). Legacy `web.gsc_page_stat` surfaces untouched (retirement handoff: `docs/handoffs/gsc-page-stat-retirement.md`).
- 2026-07-30 — Claude: **brand cockpit stays balanced under bulk facts.** Business facts now group by kind (`groupFactsByKind` + `FACT_KIND_ORDER`: identity/editorial kinds first, bulk contact kinds last); a kind with ≥3 facts collapses to a bounded group — header (icon + label + count) + 4-fact preview + "Show all N" toggle — with phones/faxes/emails two-up on wide cards and addresses one-up. Per-fact edit/delete stays on every row (`BusinessFactRow`, compact variant inside groups). Hero protections: description `line-clamp-3`, social icon strip caps at 8 (+N overflow). Verified against the All Green Recycling shape (16 addresses + 14 phones): the top section keeps its shape, everything remains reachable below.
- 2026-07-29 — Codex: **Visual page feedback now uses the canonical image
  annotation window and association system.** The page Plan lane has a
  one-click Visual feedback composer for upload, clipboard, current-page
  capture, or screen capture. Every current and historical crawl screenshot
  also has a Mark up action. Saving creates a flattened PNG through the
  universal file handler and writes the existing `file → web_page`
  `platform.associations` edge with source provenance; no feedback table,
  screenshot variant, or parallel persistence path was added. Attached markup
  can be previewed, edited in place, or detached from the page.
- 2026-07-29 — Codex: canonicalized every active frontend SEO title/description
  counter, recommendation, scraper analysis, SERP preview, CMS/page editor,
  research output, image metadata hint, and flashcard truncation onto
  `seo/serp/metrics.ts`. Mobile now uses the same product allowance as desktop:
  title 600px, description 920px; character windows remain 15–60 and 70–160.
  The TypeScript/Python parity fixture enforces the cross-codebase contract.
- 2026-07-30 — Claude: **brand cockpit display upgrade.** New shared `PropertyKindMark` (platform glyph + official brand color tile; `propertyPublicUrl` handle→URL derivation; `Google` glyph added to `components/icons/brand-icons.tsx`) replaces the generic `AtSign` on cockpit social rows and adds a clickable social icon strip to the brand hero. Hero identity is logo-first at 72px (`SiteIdentityMark` gained `prefer="logo"` + failed-candidate fallthrough) and falls back to the confirmed asset library (`is_primary`-first) when the brand row has no logo/favicon URL; the og/hero share image renders as a hero cover thumb. Business facts get kind-specific icons + canonical labels (new `isBusinessFactKind`/`isBrandAssetKind` guards in `types.ts`); asset tiles sort primary-first, show canonical Type labels, a primary star, color swatches for `color` assets, and click through to the source URL. **Root-cause fix:** `secureImageUrl` upgrades persisted `http://` image URLs at render time — mixed-content blocking is why brand logos silently never rendered.
- 2026-07-29 — Claude: **Studio split is now PAIR-ALIGNED (Arman's side-by-side ruling).** `PageWorkspace` no longer renders two independent lanes — it renders 16 paired rows (`rows: {key, current, plan}[]`) in ONE grid (`xl:grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)]` in Studio): for every "thing", the observed CURRENT card and its PLAN counterpart share one grid row, always side by side and always the same height (the shorter side stretches). The middle column is the current→plan connector (vertical primary-gradient thread + ArrowRight node) per row. All cells stay mounted in every mode (CSS hidden — mode switches never unmount, caches/edits survive); each cell is its own `@container` root. Pairings: SERP↔Intent, target-perf+queries↔keyword batch, identity↔identity notes, structured-data+resources↔structured-data notes, social↔social plan, indexability↔indexability plan, media↔image plan, analyzer↔strategy notes, GSC/PSI/GA4↔performance goals, headings↔heading plan, content stats↔content targets, page content↔draft content, findings↔tasks, links↔links plan, backlinks↔backlink plan, sitemaps+captures↔Push-to-CMS (both `LinksPlan` and `PushToCmsCard` are now mounted — the two "not wired yet" notes below are resolved). Sides with no structured counterpart get the NEW generic `PagePlanNoteCard` (`components/pages/cards/PagePlanNoteCard.tsx` — freeform notes persisted to ONE string key of `desired_values` via `useDesiredValueSlice`; new keys `identity_notes`/`strategy_notes`/`performance_goals`/`backlink_plan`, plus first consumers for `structured_data_notes`/`additional_content_notes`) or a dashed `PairStub` (missing snapshot). "Attached to this page" (associations) moved OUT of the Plan lane to a full-width strip at the bottom — it is neither current nor plan. `PageMediaCard` gained `refetchAction` and `MediaEmptyState` an `action` slot: a counts-only/pre-inventory snapshot (crawler persists per-image inventory only since ~07-29) now offers Fetch-now right inside the media empty state.
- 2026-07-29 — Claude: **Sites portfolio is now a KPI cockpit.** New `web.v_site_kpis` view (per-site canonical page count, pages-in-GSC, 28d clicks/impressions/weighted position, prior-28d clicks/impressions + day-coverage columns so trend deltas are suppressed while GSC history is partial) and two SECURITY INVOKER rollup RPCs `web.site_gsc_daily(site, days)` / `web.site_gsc_top_pages(site, days, limit)` (migration `marketing_site_kpis_view_and_gsc_rollups.sql`). `listSites` merges the KPIs into `SiteListRow` and serves KPI-column sorts through the view (id-prefilter → view order/page → hydrate). Table columns are now Site / Pages (+in Google) / Clicks·28d / Impressions·28d / Pos. / Connections / Status — Description, Access, and Updated-at are gone, default sort is clicks desc, and the per-row button strip collapsed into one canonical `ItemMenu` "…" (workspace / quick view / live site / copy pair / edit / delete). Hovering any GSC cell opens a `HoverCard` peek (`SiteKpiPeeks.tsx`: hand-rolled SVG trend chart — deliberately no charting lib — 30/90-day toggle, top-10 pages, data-through date); Pages hovers to an inventory breakdown; the row menu's Quick view opens the non-blocking draggable `SitePeekWindow` (WindowPanel behind a `dynamic()` edge, AgentPeekButton pattern) with KPI tiles + metric-switchable 90d chart + top pages. Health column deliberately NOT rendered: `web.v_site_score` is empty platform-wide (page scoring has never produced rows) — re-add it when scoring ships rather than showing a column of dashes. Date-only GSC dates format via `formatStatDate` (a `formatCompactDate` on a date-only string renders the prior evening in negative-offset timezones).

- 2026-07-29 — Claude: **Push to CMS shipped for the page workspace.** New `PushToCmsCard` (`components/pages/cards/PushToCmsCard.tsx`, props `{page: MarketingPage, site: MarketingSite}`, anchor `push_to_cms`) pushes a page's authored plan — `web.page_content.content` (markdown → HTML via `marked`, already in the dep graph) + `meta_title_desired`/`meta_description_desired` — into the CMS as a DRAFT via the ONE canonical write path (`CmsPageService.createPage`/`saveDraft` over `/api/cms/*`; never the aidream bridge, never auto-publish). Site mapping reuses `resolveCmsLink` (unlinked → deep-link to `?view=setup`, linking NOT reimplemented); page mapping is by ROUTE (`web.page.path` vs `client_pages.route`; found → saveDraft, missing → create as unpublished draft; THE 301 LAW — this push never moves a route; deep routes require the parent to exist, `/` only maps onto an existing homepage). Pure planning/execution core in `lib/push-to-cms.ts` (`resolvePushTarget`, `buildPushPayload`, `executeCmsPush`); provenance `{source:"page-workspace", web_page_id, pushed_at}` stamped into the CMS activity log (`changes.metadata` — `client_pages` has no metadata jsonb, verified live); `plan_node_id` surfaced read-only when set. Mounted in the PageWorkspace pair grid (publication row) since 2026-07-29.
- 2026-07-29 — Claude: **Internal-link TWO-PLAN system (Arman's inbound+outbound ruling).** `desired_values` gained `inbound_links` / `outbound_links` (`PlannedLinkEntry[]`, jsonb plans — deliberately NOT association edges). New `LinksPlan` (`components/pages/cards/LinksPlanCard.tsx`, props `{page: MarketingPage}`, anchor `links_plan`) carries all three link-plan editors (accepted anchors moved out of `PageLinksCard`, which is now observed-only) with live per-entry scoring (linked / wrong anchor / not linked), site-page URL datalist, and target-policy fallback from observed edges + the bounded site directory. Scoring core `scorePlannedLinks` + normalizers in `data/page-links.ts`; site aggregation `data/site-link-compliance.ts` (pages cap 2000 / edges cap 5000, truncation rendered loudly, latest-snapshot-only, self-links excluded); site Links workspace gained the "Plan" view (`?view=plan`, hidden on crawl scope) — `SiteLinkComplianceView` with health chips, wrong-anchor offenders per target, planned-inbound and planned-outbound gap lists, Copy-for-AI. `DesiredSection.title` widened to `ReactNode`. `LinksPlan` mounted in the PageWorkspace pair grid (links row) since 2026-07-29.
- 2026-07-29 — Claude: **Capture observations layer.** Every page capture gained an "Observations" affordance (`components/pages/CaptureObservations.tsx`, mounted by `PageCapturesCard` beside `CaptureAttachments`): a dialog showing the screenshot LARGE (canonical `InlineMediaRef`, one-click into the platform file viewer) beside a write-while-looking panel. Persistence is ZERO bespoke — each observation is a real `workbench.notes` row (`createNote`, tags `observation`/`needs-fix`, folder "Page Observations", org-internal, capture provenance in note metadata) attached through the ONE association path (`useContainerLinks` note → `web_screenshot` edge), so observations also show in the paperclip picker, `/notes`, and every association surface. "Broken — needs fix" flags the note AND opens the task quick-create window pre-populated (source `web_screenshot` — canonical entity→task direction, the window owns the edge); every listed observation carries timestamps, a needs-fix badge, open-in-note (`useOpenNoteInfoWindow`), and a create-fix-task shortcut. A failed attach after a successful note create screams the exact recovery path (never silent). New canonical batch read `fetchNotesByIds` added to `features/notes/service/notesService.ts`.
- 2026-07-29 — Claude: **Current | Plan | Studio split shipped (ratified names).** `PageWorkspace` now renders two always-mounted lanes — CURRENT (every observed/measured card: SERP preview, target performance, queries, identity/structured/resources, social + indexability observed halves, media, analyzer, GSC/PSI/GA4, headings, content stats, page content, findings, links, backlinks, sitemaps, captures) and PLAN (intent, social-card plan, indexability plan, heading plan, keyword batch, draft content, tasks, image plan, attachments) — controlled by the header `WorkspaceViewToggle` (`components/pages/WorkspaceViewToggle.tsx`; local style-only pref, desktop defaults Studio split, mobile defaults Current). Lanes hide via CSS (never unmount — in-progress edits and query caches survive switching) and are `@container` roots so card pairs go two-up only when a lane is wide. The mixed cards were split: `SocialCardPlan`, `IndexabilityPlan`, `HeadingsPlan` are the extracted desired halves (same `useDesiredValueSlice` keys — one consumer per key preserved) and work WITHOUT a snapshot (planning never waits for a crawl).
- 2026-07-29 — Claude: **Media surfaces shipped.** (1) `PageMediaCard` (`components/pages/cards/PageMediaCard.tsx`, props `{page, snapshot}`, anchor `media_inventory`) — size-separated gallery over the latest snapshot's per-image inventory + OG/twitter share images + video/embed resources, honest counts-only empty state when the crawler hasn't persisted the inventory. (2) Site-level `/marketing/brands/[brandId]/sites/[siteId]/media` route (`SiteMediaWorkspace`, nav-registered in `MarketingSiteLayoutClient`) — aggregates the inventory across ALL pages' latest snapshots (service `fetchSiteMediaRows` selects only `images`+`head_tags`), dedupes by src, filters (tier / missing-alt / page / search), shows the pages each asset appears on. The photo/graphic/icon + photo-grade + aspect heuristics were EXTRACTED to the shared core `lib/media/categorization.ts` (research now delegates — never fork these heuristics); the marketing adapter + dedupe is `lib/snapshot-media.ts`, the shared gallery renderer is `components/media/SnapshotMediaGallery.tsx`. Conscious exception: crawl assets are third-party URLs with no `file_id`, rendered via plain `<img loading="lazy">` (InlineMediaRef does not apply).
- 2026-07-28 — Claude: **shipped `/marketing/ranks` (cross-site rank hub)** — the first reserved route to go live, at its permanent URL. `CrossSiteRanksHub` + `cross-site-data.ts` (direct-Supabase bounded reads over `seo.rank_target`/`rank_observation` + batched `web.site` enrichment; loud caps), MatrxDataTable local mode with full Copy/Copy-for-AI, row click → the per-site Ranks workspace. Registry row `marketing.rank-tracking` deleted; nav entry live. OPEN: `seo.rank_target` rows can be readable where their `web.site` row is not (UUID shown as site name — access asymmetry, see brand-coverage handoff).
- 2026-07-28 — Claude: **`matrx-user/marketing-ranks-hub` surface registered** for `/marketing/ranks` — standalone manifest (no brand/site inheritance; 4 values in one `cross_site_portfolio` group + `portfolio_rank_analyst` role, readiness `partial` pending the live binding test), route resolver special-case ahead of the hub fallback, `SurfaceRuntimeProvider` emitter in `CrossSiteRanksHub` with `data-surface-value` anchors, DB row + sync applied and verified live (9 values, contract honored, live portfolio in Surface Context).
- 2026-07-28 — Claude: **public SEO tool suite single-sourced.** `MARKETING_PUBLIC_TOOL_CATEGORIES` in `marketing-nav.ts` now declares the full public suite (live + planned, categorized); `MARKETING_PUBLIC_TOOLS` is derived (live only) and `app/(public)/seo/page.tsx` renders the shared declaration instead of its own hand-written list. The 11 advertised-but-untracked "coming soon" analyzers are now registered `marketing.tools.*` promises in `lib/coming-soon/registry.ts`.
- 2026-07-28 — Codex: completed page-observation evidence end to end. The
  crawler now persists every raw/parsed JSON-LD script, microdata, RDFa,
  microformats, per-image details, all DOM- and structured-metadata-declared
  resources, CMS/page identity signals, safe WordPress/Shopify identifiers,
  related API/shortlink URLs, and featured-image provenance in the existing
  immutable snapshot JSONB fields. The page workspace surfaces a compact
  thumbnail and only non-empty identity fields below Target Keyword
  Performance, followed by normalized schema entities/types, grouped
  resource/media inventories, raw evidence, Copy JSON, Copy, and Copy-for-AI.
  Legacy snapshots show one explicit recrawl explanation instead of empty
  placeholders. No table, migration, proxy, or extra fetch was added.
- 2026-07-28 — Codex: initialization now distinguishes root failures from
  dependent skips. The progress contract accepts `skipped`, renders it
  neutrally, and preserves the prerequisite message; one homepage 429 no
  longer appears as three failed application steps. The parser also reads
  counts from the backend's canonical nested `counts` object instead of only
  undocumented top-level fields. Non-blocking screenshot-retention and
  partial-sitemap notices render in a separate amber panel and never make a
  successful initialization report failed.
- 2026-07-27 — Codex: **anchor-text policy and cross-page internal-link compliance.** The page Internal Links evidence card now switches between URL rollups and collapsed anchor-text trees (anchor → pages → link counts), authors a normalized list of acceptable inbound anchors through the existing merge-safe `web.page.desired_values.accepted_anchor_texts` slice, reports acceptable/unacceptable percentages and exact source pages, and resolves each internal target's policy on outbound edges so source pages highlight anchor mismatches and show acceptable replacements. Link evidence is current-snapshot only rather than inflated by historical crawl rows. Export now offers condensed anchor/page CSV, issues-only CSV, a simple text issue list, and full analysis JSON alongside the existing Copy/Copy-for-AI pair. Reused `TextArrayInput`, `DesiredSection`, `SegmentedControl`, `ExportMenu`, React Query invalidation, and the canonical `web.link_edge`/`web.page` direct-Supabase path; no table or migration was added.
- 2026-07-27 — Claude: **module build-out.** Marketing now covers eight pillars with 16 RESERVED routes (campaigns, calendar, audience, local, ranks, ai-visibility, content-studio, social, email, ads, outreach, competitors, monitoring, analytics, reports, automations) rendering the new shared `MarketingComingSoon`, each tracked in `lib/coming-soon/registry.ts`. Added the public `MarketingLanding` + server-side guest/authed branch in the module layout (guests never hit a login wall) and registered it in `MODULE_LANDING_DIRECTORY`. The shell sidebar is now GENERATED from `MARKETING_PILLARS` (`marketingNavChildren()`), as is reserved-route metadata — hub, menu, and metadata can no longer drift. Admin map covers all 16.
- 2026-07-27 — Codex: **site-overview context-menu reference wiring.** The canonical site cockpit now mounts the v3 `NonEditableContextMenu` over the whole read-only workspace with the complete live `matrx-user/marketing-site` scope, raw-document Export/Download, and the real `web_site` entity for Attach To + Share. Opening the inline identity editor suppresses that outer menu and gives each controlled text field its own `EditableContextMenu`, so selection, Cut/Paste, Save, content insertion, and inline-agent edits target the exact field without nested menus double-opening. This is the reference pattern for the remaining Marketing rollout: one read-only surface wrapper, field-specific editable wrappers only while an editor is active.
- 2026-07-27 — Claude: **page workspace authoring layer — desired values, keyword batch, tasks, draft content, image plan, associations.** Every important card now has a desired/target twin: ONE `web.page.desired_values` jsonb (one key per card, merge-safe single write path `updatePageDesiredValues` + shared `useDesiredValueSlice`/`DesiredSection` primitives in `components/pages/desired/` — no remount wipes; PageIntentCard's remount key narrowed to intent fields) with embedded editors in SocialCardPreview (share title/description), IndexabilitySection (canonical/robots), HeadingsOutline (`DesiredOutlineEditor` — the header-structure plan: add/indent/reorder/seed-from-observed). Authored draft content lives in the NEW 1:1 `web.page_content` table (own version lock so draft saves never bump `web.page.version`), edited by `PageDraftContentCard` (BasicContentEditor behind one dynamic edge, explicit Save + beforeunload guard). `PageKeywordsCard` is the keyword batch on the real keyword plane: `seo_keyword → web_page` supporting-role edges (typed wrappers `data/page-keywords.ts`; unknown phrases ensured via `seo.fn_upsert_keyword`, now authenticated-executable) with promote-to-primary writing `target_keyword`. Tasks are first-class: `PageTasksCard` + header/per-card `PageTaskButton` → task quick-create window pre-linked (`web_page` source; canonical edge direction is entity→task). `PageImagePlanCard` plans images in `desired_values.image_plan` and generates them headlessly via the surface's new `image_producer` agent role (`lib/generate-page-image.ts`, imageGrading pattern; file_id lands on the entry). Attachability: `web_page` token gained `title_column='url'` + registry overlay + `/marketing/pages/[pageId]` short-link resolver; bottom `AssociationCardGrid` under `PrimaryEntityProvider`; per-capture `CaptureAttachments` (TaskChipRow + note/file picker on `web_screenshot`). Surface: `authoring`/`attachments` groups + 12 values (draft_content, desired_values, keyword_batch, image_plan, findings, gsc_queries, in/outbound_links, backlinks, structured_data, perf, images, page_tasks, attached_items), all emitted by `buildMarketingPageScope`; DB synced (74 values / 12 roles live). **Platform class fix:** the association read RPCs (`assoc_for_entity`/`assoc_for_targets`/`assoc_for_sources`) now honor the `iam.system_orgs.global_readable` tier via new `iam.org_readable` — system-org edges (ALL keyword edges) were previously invisible to every client. Known gap: the scraper stores only image counts, so per-image observed inventory/alt editing waits on an aidream scraper change.
- 2026-07-27 — Claude: **keyword intelligence wave 2 + page evidence cards.** The Keyword Intelligence window is now a registered agent surface (`matrx-user/keyword-intelligence` — 17 values incl. `keyword_brief`, 3 unbound roles, emitter inside `KeywordIntelPanel`; DB-synced live) and the marketing-page surface emits `target_keyword_data` (condensed market brief for the saved target keyword). Page workspace gained four evidence cards with full Copy/Copy-for-AI: Queries reaching this page (per-page GSC evidence with one-click Adopt-as-target), inline Open findings, Internal links (in/outbound `web.link_edge` rollups), and per-page Backlinks; snapshot history gained a two-snapshot Compare diff (canonical narrowers only). Keyword adoption sweep: ranks add-target uses `KeywordInput`, ranks/site-keywords rows and the content-plan KeywordPicker carry Keyword Intelligence launchers, `ranks/types.ts` re-aliased onto generated OpenAPI schemas. Intent save nudges a one-click market fetch for unknown keywords.
- 2026-07-26 — Claude: **canonical keyword primitive** (`features/marketing/seo/keyword/` + its FEATURE.md): `KeywordInput` (library resolution, condensed data chips, GSC/analyzer/library suggestions, intelligence launcher), the new `keywordWindow` window panel (Keyword Intelligence — Overview/Relationships/Site/Rankings/SERP/Research tabs, all reusing keyword-research + ranks data layers), `buildKeywordBrief`, `KeywordUsageChips`. Page-workspace intent form adopted it: scope-bound input, real GSC-query + Page-Analyzer suggestions, usage checks, keyword market brief in the intent copy payload.
- 2026-07-26 — Claude: **site overview redesign.** `SiteOverview` rebuilt as a fluid, adaptive command center: max-width-capped container (no more fixed-layout breakage at odd widths), hero card (screenshot + identity + inline last-crawl/connections/initialized meta links), a 6-KPI linked grid (score→audit, pages→pages, findings→findings, discovery→discovery, target keywords→pages, in-Google→pages?coverage=in_gsc — KPI cards ARE the navigation), a "Needs attention" queue (discovery/findings/blocked/SERP/missing-keyword/connection-attention rows, green all-clear state), Quick work, a 12-entry Workspace directory with live counts, and the old giant Connections section collapsed to a compact status-chip strip (chips link to `/integrations`; Re-initialize retained). `getSiteOverview` gained six cheap head-count signals (`targetKeywordPages`, `pagesInGsc`, `blockedPages`, `serpIssues`, `sitemaps`, `crawlSessions`); shared `MetricCell` gained an optional `href` prop (whole-cell link with hover arrow) instead of a forked stat-card component.
- 2026-07-25 — Claude: **marketing consolidation.** Root-level `/content-plan` and `/seo/keyword-research` moved under `/marketing/*` (permanent redirects added); `features/content-plan` → `features/marketing/content-plan` and `features/seo` → `features/marketing/seo` (all ~50 import sites updated). `/marketing` no longer redirects to `/brands` — it is now a real multi-pillar hub over the new single-source `lib/marketing-nav.ts`, joined by `/marketing/tools` indexing the public `/seo/*` analyzers. Shell nav rebuilt to mirror the pillars (websites demoted from "the feature" to one pillar); admin map + route metadata updated.
- 2026-07-25 — Codex: made crawl activity survive refresh and navigation: the site layout restores the active durable session, owns one reconnecting Supabase heartbeat with event catch-up/poll fallback, resumes the live feed and cancellation controls on `/crawls/new`, and shows a responsive site-wide **Crawling** status plus live fetched count. Published `web.crawl_session` to Realtime; kept large `crawl_event` payloads off the websocket.
- 2026-07-24 — Codex: made homepage metadata and hero-capture reads resolve the exact `web.site.root_url` page, preserving alternate host/scheme rows as coverage evidence without triggering multi-row PGRST116 errors.
- 2026-07-24 — Claude (surface completeness backfill): the `matrx-user/marketing-page`
  surface is the exemplar of the COMPLETENESS + NAMING laws. The manifest
  (`features/surfaces/manifests/marketing-page.manifest.ts`) now declares 7 value
  groups and 20 new values (identity fields, composite `page_intent`, indexability/
  social/url-quality evaluations, content stats + file ids, captures, Page Analyzer
  artifact, page score/failed checks, PageSpeed/GA4/sitemap evidence);
  `buildMarketingPageScope` emits ALL of it and exports the shared deterministic
  helpers (`evaluatePageIndexability`, `evaluatePageSocialCard`, `pageCaptureRows`,
  `captureAvailability`, `latestPagespeedByStrategy`, `webAnalyticsTotals`) that the
  PageWorkspace UI renders from — one evaluation path, two consumers. PageWorkspace/
  PageContentCard render section titles and field labels via `surfaceValueLabels`/
  `surfaceGroupLabels` (byte-identical to the manifest) and tag every major card and
  metric with `data-surface-value` locate-on-page anchors (SectionCard/MetricCell
  grew an `anchor` prop). Pagespeed + GA4 page rows moved to shared react-query
  hooks (`usePagePerformance`/`usePageWebAnalytics` in `data/hooks.ts`); the Page
  Analyzer hook is lifted to PageWorkspace so the surface scope emits the same
  artifact the card shows.

- 2026-07-24 — Claude (M-9 SEO cost UI): `/marketing/cost` (the pre-existing
  runtime/AI-execution cost rollup page — a DIFFERENT concept from SEO
  provider spend) gained a third "Provider spend" mode
  (`SeoSpendPanel.tsx`/`SeoSpendChart.tsx`/`data/spend.ts`) reading the new
  aidream `GET /seo/spend/summary` route: provider-by-provider breakdown vs
  the WS-7 monthly ceiling (spent/ceiling, % used, progress bars), a 30-day
  daily spend chart, and any recent `seo_budget_exceeded` rejections. No new
  nav entry — `/marketing/cost` was already in `MarketingWorkspaceNav`. Not
  exercised in a live browser this pass (no dev server per task rules); the
  route is deploy-gated like every other 2026-07-23+ SEO route (`as unknown
as keyof paths` cast pending the OpenAPI type sync).

- 2026-07-23 — Claude (WS-15): D74/DEF-15 CLOSED — the External Links unchecked-status
  notice from 2026-07-20 is resolved, and it was never actually a scraper gap: the
  link-check pipeline (`matrx_scraper.web_crawl.link_check`, built 2026-07-21) had been
  a silent no-op the whole time because a matrx-frontend migration one day earlier
  (`web_crawl_artifact_immutable_trigger_security.sql` → `web.reject_immutable_fact_mutation`)
  blanket-rejects every UPDATE on `web.link_edge`. Fixed at the DB
  (`db/migrations/0240_web_link_edge_http_status_mutable.sql` in aidream, narrows the
  trigger to allow an `http_status`-only UPDATE, everything else stays immutable).
  `ExternalLinksView.tsx` gained a "Check link status" button (streams
  `POST /crawler/sites/{id}/links/check`), per-target HTTP-status badges, and a broken-
  link stat chip — live-verified against `allgreenrecycling.com` (120,240 edges, all
  NULL before → tens of thousands populated after). Also shipped: the new/lost backlink
  trend chart at `.../backlinks` (M-61, `BacklinkTrendChart.tsx` — reads the
  `timeseries_new_lost_summary`/`timeseries_summary` datasets already stored on
  `seo.backlink_snapshot`, no refresh needed to see real history) and a site-audit score
  trend sparkline at `.../audit` (M-55, `AuditScoreTrendChart.tsx` — aggregates
  `buildSiteAuditRollup` per historical `web.snapshot` capture day instead of only the
  latest, reusing the same pure rollup function, zero forked scoring logic). Found but
  NOT fixed (filed in aidream `FOUND_DEFECTS.md`): the same immutability trigger still
  blocks `link_resolution.py`'s `target_page_id` backfill (tiny blast radius), and
  `web.analysis_result` is completely empty platform-wide — `v_page_score`/`v_site_score`
  have zero real rows despite M-46 claiming WORKING.

- 2026-07-22 — Claude: Marketing surface fleet — 16 new surfaces (+`marketing-page` gaining `inheritsFrom`) with brand→site→vertical inheritance, purpose-driven intros, 36 agent-role slots, and honest value declarations; DB fully mirrored (rows, values, roles, intros, url_patterns, parents — verified live); nested-dynamic route→surface resolver + tests; runtime emitters (`SurfaceRuntimeProvider` + manifest scope helpers over loaded query data) wired across the brand cockpit, portfolios, batches, page workspace (with v3 context menu on page content), site layout, and the site verticals.

- 2026-07-22 — Claude: brand editorial profile + surface parent-context builders — `BrandProfile` type with `parseBrandProfile`/`brandProfileToJson` narrowers over the new `web.brand.profile` jsonb; collapsible "Brand profile" section in `BrandEditorDialog` (create + edit, list fields one-per-line); brand create/update paths carry `profile`; new pure `lib/surface-context.ts` (`buildBrandContextXml` / `buildSiteContextXml`, unit-tested) emitting the compact `brand_context` / `site_context` XML the Marketing surface fleet inherits.
- 2026-07-22 — Codex: added the brand/site Backlinks workspace with direct RLS-filtered Supabase reads, canonical SEO-server manual refresh, exact receipt inspection, weekly/monthly/bootstrap profiles, and per-site weekly/monthly schedule configuration. Registered the route beside Links and regenerated database types for the new backlink tables.
- 2026-07-22 — Codex: added the site Keywords workspace with direct Supabase reads of the persisted 28-day GSC query projection, strongest-page navigation, DataForSEO market enrichment, and controlled search/filter/sort/pagination.
- 2026-07-22 — Codex: exposed the existing `seo` schema through the Supabase Data API for authenticated, RLS-filtered `SELECT` access; preserved server-owned writes, added `seo` to the canonical type-generation command, regenerated types, applied and ledger-recorded the migration, and verified a real authenticated `seo.collection_run` read.
- 2026-07-22 — Codex: repaired the shared responsive route header used by every Marketing workspace and site route. Wide layouts render every icon + label, constrained layouts keep the active label while collapsing inactive items to tooltip-backed icons, narrower desktop layouts use one dropdown, and mobile uses the canonical bottom sheet; portal-mount measurement can no longer lock the header in its compact menu state.
- 2026-07-22 — Codex: upgraded the page workspace with collapsible sections, a copy-current observed-metadata control, uncapped Markdown, explicit desktop/mobile capture availability, Matrx Analysis/Suggestions placeholders, and stored per-page GSC metrics; added honest disabled PageSpeed/GA4 page-refresh states pending supported backend contracts.
- 2026-07-22 — Codex: made the canonical Pages table default to global 28-day clicks descending with impressions descending as its tie-breaker; added the RLS-preserving `web.v_page_list` projection so sitemap, word, health, clicks, impressions, and position columns sort/filter before pagination instead of only enriching an already-bounded page.
- 2026-07-22 — THE VIEW LAW: `listSites`/`listSiteOptions` documented as a deliberate org-browse surface (`MARKETING_SITES_IS_DELIBERATE_ORG_BROWSE` marker + comment on `SITE_COLUMNS`) rather than left as an undocumented bare-RLS list — site management is intentionally cross-org, unlike a personal content list.

- 2026-07-22 — Capture / HTML / Markdown open-in-viewer no longer lands on an empty "File not found" panel. Marketing already passed only the canonical `files.files` UUID into `useOpenFilePreviewWindow`; the Files viewer now hydrates that off-tree crawl artifact via `useEnsureCloudFile` (viewable under `files.has_access_for`, never discoverable in the Files tree). No marketing-side identifier change — still `file_id` / `body_file_id` / `markdown_file_id` only.
- 2026-07-21 — Fixed the controlled number-input trap on crawl defaults (site settings + start crawl): clearing Maximum pages / Concurrency no longer snaps back mid-keystroke. Shared `ClampedNumberInput` keeps a draft string and only clamps/restores on blur or Enter.
- 2026-07-21 — Link graph made readable at real site scale + two prod timeouts fixed. **Sections view is now the default**: the map aggregates pages by URL directory (`buildSectionGraph`) — folders are one node sized by page count, links inside a folder become a node stat instead of edges, and the long tail of individual pages at a level collapses into one "N more pages" node (flat sites were the killer case: 212 root-level pages became 212 nodes). Breadcrumb drill-down; Pages (page-per-node) is opt-in and only offered under 60 pages in focus. Labels are single path segments, truncated, width-bounded, and hidden on small nodes at page level — full URLs live in the side panel only. **Perf:** `web.count_link_edges` RPC (checks access once, then counts) replaces PostgREST `count=exact`, which ran `iam.has_access` per row and timed out (57014) on a 103k-edge site; the site-scoped table read dropped its `snapshot!inner` embed (8.1s → fast; the crawl-scoped read keeps it since that is how a session is filtered); added `link_edge_site_created_idx`.

- 2026-07-20 — Claude: outbound-links (External) view — `.../links?view=external` renders the domain-grouped outbound report (`ExternalLinksView` + pure `buildExternalLinkReport` in link-graph `model.ts`; Screaming Frog External tab / Ahrefs Linked Domains): destination domains sorted by link count with target drill-down (exact outbound URL — query kept, fragment dropped — link occurrences, distinct linking pages with clickable root-stripped samples, anchor rollup, nofollow), stat chips, filter, Copy-for-AI (`web-external-links`), and the graph's "external targets" stat jumps to it. Reuses the same capped edge fetch as the graph (no new query). Filed D74: the scraper never populates `web.link_edge.http_status` (ALL rows null) — broken-link detection is blocked on a backend link-check pass; the External view shows an honest unchecked-status notice until then.
- 2026-07-20 — Claude: site link GRAPH view — `.../links` (site + crawl scope) now defaults to an interactive link graph (`components/inspection/link-graph/`: pure `model.ts` aggregation, directed stylesheet, layouts) REUSING the kg-graph cytoscape engine (lifecycle hook, camera/focus/search ops, theme chrome); Graph/Table toggle persisted at `?view=`. Nodes = unique pages (inlink-sized, colored by click depth from home or by HTTP status), directed weighted edges (dashed nofollow, red broken), external targets as toggleable diamonds, radial/tree site-map layouts rooted at the homepage, node panel (status/depth/in-out links/anchor rollup/query variants/page-workspace link), Copy-for-AI on the toolbar. URL policy: fragments + tracking params (`utm_*`, gclid, fbclid, msclkid, mc_eid) never create identity; other query strings group into the base page by default with a Split-params toggle; non-http(s) targets and self-loops excluded and counted. Table + graph both render root-stripped URLs (`displayUrl`). Data: `listLinkGraphEdges` pages past PostgREST's 1k cap (5k row cap, truncation flagged). Fixed in kg engine: camera ops clear stuck core-animation queues; StrictMode mount-guard refs reset on unmount (redundant animated relayout); layouts fit explicitly on `layoutstop` (a 0×0-container layout leaves a garbage camera, refit on first resize).
- 2026-07-21 — Codex: added the crawl-report catalogue and ten stable dedicated report pages under `.../crawls/[crawlId]/reports/**` (response codes, titles, meta descriptions, headings, canonicals, directives/language, images, content/duplicate hashes, structured data, performance). The shared workspace reuses the existing controlled URL/snapshot queries, canonical JSON narrowers, SEO metric contract, `MatrxDataTable`, and Copy/Copy-for-AI primitives; report navigation, metadata/favicon identities, admin-map coverage, parsers, and real-data browser behavior are verified.
- 2026-07-20 — Redesigned the site-overview hero to use its right column for prominent score, canonical-page, findings, discovery-review, and crawl-freshness displays; added generous trailing scroll space so the final content can move comfortably above the viewport edge.

- 2026-07-21 — Claude: on-demand page fetch + rendered content. New scraper command `POST sites/{id}/pages/fetch` (aidream `prepare_page_fetch`, mode `page_fetch`) wired as `fetchPageNow` + `FetchPageButton`: page-workspace header, never-captured empty state, pages-table row action, and auto-first-capture after manual Add page. Extracted Markdown renders as a real "Page content" card (`PageContentCard`, code-split MarkdownPreview) instead of only a file link. The same round's `web_public_view_visibility.sql` public-default experiment was reverted on 2026-07-22 after it proved to be a cross-tenant read grant, not a harmless display setting; canonical RLS and the `internal` default are current.
- 2026-07-20 — Claude: rolled the Copy + Copy-for-AI doctrine across every data/table surface (pages, snapshots + snapshot detail sections, sitemaps list/detail, coverage matrix, crawl sessions/summary/URLs/logs, crawl snapshots, link edges, batches + batch detail, site/workspace cost, priority queue, findings register + finding detail): tables via the `MatrxDataTable` `copy` config, sections via `SectionCard copy`, record headers via `CopyButtons`; kinds are stable `web-*` slugs from `lib/copy-payloads.ts` (new `webLocation` helper).
- 2026-07-20 — Fixed the site hero to resolve `site.homepage_screenshot_id` exactly when present (legacy bootstrap); initialize sites without that pointer now resolve the homepage `desktop_fold` capture instead of falling through to `og_image_url`.
- 2026-07-21 — Fixed hero regression for initialized sites: `useSiteHeroScreenshot` now runs when `homepage_screenshot_id` is null, and `getSiteHeroScreenshot` prefers homepage `desktop_fold`/`mobile_fold` before legacy `homepage`/`viewport` kinds.

- 2026-07-20 — Codex: connected discovery promotion end to end: social candidates now become typed brand properties, confirmed identity assets hydrate brand/site summary fields, existing mis-promoted rows are repaired, `resolved_property_id` records social provenance, Other requires a custom label, and user-facing domain selectors now say Type rather than Kind.
- 2026-07-24 — Codex: made social discoveries verifiable at a glance with canonical provider icons, URL-derived handle/channel/profile details, clickable URLs, and an explicit open-in-new-tab action on every URL-bearing discovery.
- 2026-07-20 — Codex: made discovery promotion consume the canonical asset/fact kind registries; added first-class fax, homepage identity, Open Graph image, and Twitter card image kinds; fixed the Add Fact selector being clipped by its dialog scroll container; and extended the scraper to recognize Schema.org and labeled-text fax numbers without duplicating them as phones.
- 2026-07-20 — Codex: registered the Marketing favicon family and added route-aware metadata for every current Marketing leaf, including unique tab badges and client-only legacy redirects.
- 2026-07-20 — Codex: made automatic integration binds provider-scoped CAS rebases so unrelated site version bumps no longer strand a successful Google connection while same-provider races still fail loudly.
- 2026-07-20 — Codex: cut Marketing artifacts over to canonical Files UUIDs, added complete per-snapshot body/markdown/screenshot rendering, and removed all parallel file-backend URL construction and legacy crawler reference fields.
- 2026-07-18 — Codex: added the first production vertical with direct Supabase portfolio, site creation/shell, pages/snapshots, crawl sessions, URL ledger, durable event log, and admin map.
- 2026-07-18 — Codex: connected site bootstrap and crawl start/cancel/live progress directly to the standalone scraper; no persisted-data proxy or replay endpoint was added.
- 2026-07-18 — Codex: added site-scoped priority analysis, the finding lifecycle register, and immutable result evidence detail with URL-controlled direct-Supabase tables.
- 2026-07-18 — Codex: added link and screenshot inspection, site access and settings, batch operations, and site/workspace cost rollups.
- 2026-07-18 — Codex: added secret-free site integration references for GSC, GA4, PageSpeed, and extensible providers; verified connection authority remains intentionally separate.
- 2026-07-19 — Codex: wired the main Marketing Hub to the production workspace and added user/org vault onboarding plus site binding entry points without using the legacy browser-token Google page.
- 2026-07-19 — Codex: added a user-facing live crawl event presenter that keeps exception, ORM query/argument, stack, and ANSI details out of the primary feed.
- 2026-07-19 — Codex: added production Google OAuth with encrypted reusable personal/org connections, automatic Search Console and GA4 discovery, direct-Supabase connection/resource reads, site property selectors, and app-managed PageSpeed configuration.
- 2026-07-19 — Codex: simplified the site overview header to a compact thumbnail plus site identity row, removed the browser-chrome preview workspace and duplicate Site identity card, and kept homepage capture available via a hover refresh control on the thumbnail.
- 2026-07-19 — Codex: replaced the unused parallel Google redirect flow with the codebase's canonical Google Identity Services provider, using its popup code model for personal/org connections and durable offline synchronization without a new callback URI.
- 2026-07-19 — Codex: corrected the Google code-exchange deployment lookup to use the server-side client-ID alias and replaced ambiguous raw membership reads with the canonical organization-admin authority.
- 2026-07-19 — Codex: made Search Console the focused default connection flow, automatically persisted an exact domain match, added explicit provider-level completion actions, made PageSpeed one-click enablement, and stopped optional Analytics discovery failures or raw Google API errors from poisoning the user-visible Google connection.
- 2026-07-19 — Codex: site overview hero now top-aligns site identity, reads observed homepage meta title/description from the bootstrap snapshot's `head_tags`, and refreshes that metadata after homepage capture retries.
- 2026-07-19 — Codex: crawl new/summary workspaces now fill the available viewport height; live feed scrolls in-panel, and session scope/stats/metadata render as condensed field grids instead of raw JSON.
- 2026-07-20 — Claude: shipped the Brand layer (`web.brand` / `property` / `brand_asset` / `business_fact` / `discovered_item`, migration `web_brand_layer.sql`, canonical RLS, entity registry, one-brand-per-site backfill; test crawl data wiped). Sites portfolio now leads with identity (favicon/logo mark) plus the five connection chips derived solely from `lib/site-status.ts`; site overview rebuilt around pre/post-initialization states with an Initialize command, connections board, edit-in-place identity, and hero screenshot via `InlineMediaRef`; new `/discovery` review inbox promotes discovered candidates to confirmed assets/facts (verified end-to-end). CMS provider slot added to the integrations schema. Scraper `sites/{id}/initialize` command is being built in aidream.
- 2026-07-20 — Claude: brand-first routing — moved every site vertical under `/marketing/brands/[brandId]/sites/[siteId]`, added the `marketingRoutes` builders, the brands portfolio + brand cockpit, a client-side legacy redirect for all flat `/marketing/sites/[siteId]` URLs, and made `web.create_site` create-or-reuse the brand + website property (migration `web_brand_layer_fixes.sql`, which also fixed the discovered_item dedup index to a NULLS NOT DISTINCT unique so the scraper's ON CONFLICT works). Initialization failures are now loud end-to-end: per-step server errors render on the overview, toast, and land in the Error Inspector; marketing toasts route through the captured `@/lib/toast` wrapper.
- 2026-07-20 — Claude: Wave 1 of the coverage model — `web.sitemap` + `web.page_sitemap` live (migration `web_sitemaps.sql`), sitemap sync command contract dispatched to aidream (standalone `sites/{id}/sitemaps/sync` + inside initialize; upserts canonical pages provenance 'sitemap'); new Sitemaps workspace per site (document list with status/counts/freshness, per-sitemap page listing with crawled/membership-count/never-crawled filters, coverage stat row, Sync button). Roadmap ratified: Wave 2 GSC sync → Wave 3 Coverage matrix → Wave 4 link reconciliation → Wave 5 page-workspace rebuild.
- 2026-07-20 — Claude: Wave 2 GSC sync surface (FE) — `web.gsc_page_stat` + `site.gsc_synced_at`/`gsc_sync` landed in generated types; `syncGsc` direct-client command (`sites/{id}/gsc/sync`); Integrations GSC card gained a Sync-now row with last-synced freshness; `site-status.ts` now marks a configured-but-never-synced GSC binding `attention` ("Connected, never synced") everywhere chips render (unit-tested); pages table gained 28-day Clicks/Impr/Pos columns via a truncation-proof paginated batch rollup; coverage matrix gained the Google Search cells (in GSC / in Google not in sitemap / in sitemaps invisible to Google) as three new `?coverage=` filters, with a never-synced call-to-action when `gsc_synced_at` is null.
- 2026-07-20 — Claude: Wave 5 page workspace rebuild — killed both raw JsonPreview dumps; PageWorkspace now renders a SERP preview with observed-vs-desired editorial diff beside the intent form, an Open Graph/Twitter social card preview, indexability (canonical/robots/redirect chain/final URL), an H1-flagging headings outline, content stats (words/sentences/Flesch/links/images + artifact links), sitemap memberships, and page screenshots via `InlineMediaRef`. Pages table lead column is the observed snapshot `<title>` (batched one-query snapshot enrichment) with a word-count column; snapshot JSON narrowing centralized in `lib/head-tags.ts` (full og/twitter/indexability parse) + new `lib/snapshot-content.ts`, both unit-tested.
- 2026-07-20 — Claude: Wave 3 coverage matrix — new `.../coverage` route (`CoverageWorkspace`) rendering the sitemap/crawl/provenance disagreement tiles over `web.page`, each deep-linking to the pages table; `listPages` gained a `coverage` filter (PostgREST `page_sitemap!left` embed with `is null` / `not is null` membership semantics) plus a per-row live `sitemap_count`; pages table gained URL-owned `?coverage=` chips and dropped the permanently-empty score column; GSC cells stubbed pending `web.gsc_page_stat`.
- 2026-07-20 — Claude: full-CRUD program — SiteEditorDialog (every editable site field + brand reassignment via new `web.move_site_brand`), site edit/delete on the portfolio and cockpit rows, Property/BrandAsset/BusinessFact editor dialogs with cockpit add/edit/delete, manual page creation (scraper-identical URL hashing in `lib/page-url.ts`) + page delete, sitemap activate/deactivate + delete (cascading its membership evidence), discovery delete + un-dismiss, screenshot + crawl-session delete. Grants migration `web_marketing_crud_grants.sql` (page INSERT/UPDATE — the page-intent editor previously 42501'd — page_sitemap/screenshot/crawl_session UPDATE). `web.create_site` now takes `p_brand_id` (explicit brand always wins; cockpit "Add site" passes `?brand=`, NewSiteForm locks the org and shows the target brand). PGRST116 class fix: every single-entity fetcher across service/analysis/inspection/operations converted to `.maybeSingle()` + `assertFound`; delete flows stop refetching the dead row. Verified in-browser end-to-end: brand→add-site-under-brand (no duplicate brand)→delete site→delete brand; property/fact/asset create+edit+delete; manual page add (hash matches scraper) + delete.
- 2026-07-20 — Claude: annihilated the bespoke Google-credential encryption. The exchange/disconnect control plane moved to aidream (`/api/google-integrations/*`, browser-direct with JWT); refresh tokens now live only in the canonical secrets vault with `users.integration_connections.vault_secret_key` as the reference (migration `20260720210000_google_connection_vault_ref.sql` dropped the credential columns). Deleted `google/server.ts`, `google/connection-server.ts`, `google/discovery-health.ts`, and both Next.js OAuth routes; the pre-existing connection was marked `needs_attention` (reconnect heals it into the vault).
- 2026-07-20 — Claude: progressive initialize experience — `runInitialize` consumes granular `initialize_step` stream events (identity/screenshots/sitemaps/discovered) with per-step query invalidation so site identity renders seconds into the run; new `InitializeProgress` step strip in the InitializeCard and as a summary section for re-initialize; honest indeterminate fallback against pre-contract scraper deploys (verified live); event→invalidation map + reducer unit-tested in `components/site/initialize-progress.test.ts`.
- 2026-07-20 — Claude: brand management round — Brands list now speaks brand (identity + industry/description, Websites chips, Socials/Assets/Facts counts, Review, Status; no website-as-subtitle), full brand CRUD (BrandEditorDialog exposes EVERY user-editable field for create and edit; row + cockpit delete with confirm, blocked while sites remain), one correct "Add brand" button, site Danger-zone delete in settings, `/marketing` redirects to `/brands`, and the hover-underline name-link pattern annihilated feature-wide in favor of whole-row click.
- 2026-07-20 — Claude: screenshots restructured out of the site level — deleted the `/screenshots` route + gallery (screenshots are page evidence, not a site surface); page workspace gained a Captures section (current capture per kind, per-page history "as of <date>", delete); every capture/HTML/Markdown artifact now opens the canonical file viewer overlay instead of bare `/files/f/` links (`CaptureThumb`); dead duplicate site-screenshot fetchers removed. Live crawl feed redesigned: discovery/classification events are counters (Discovered/Queued/Fetched/Failed/Skipped) never rows, feed rows reserved for fetches/failures/warnings/milestones, rendered feed capped at 200. Manual page re-add of a soft-deleted URL now restores the row instead of failing 23505.
- 2026-07-20 — Claude: SERP section canonicalized (the `section-canonicalization` reference build) — PageWorkspace search-result preview now renders the canonical `features/marketing/seo/serp` pieces (SerpResult desktop/mobile toggle, deterministic pixel/char chips, condensed MetaRecommendations); section header launches the new floating Search Appearance analyzer (`serpAnalyzerWindow`) pre-filled with page metadata; intent form live-validates drafts and every intent save stamps `web.page.seo_metrics_desired` (contract v1); scraper stamps `web.snapshot.seo_metrics` on every capture (matrx_scraper.meta_metrics, byte-identical to the TS evaluator, parity-tested); `matrx-user/marketing-page` surface declared + synced (20 values, intro, 4 placeholder agent roles) with runtime scope builder `lib/marketing-page-scope.ts`; PAGE_COLUMNS/SNAPSHOT_COLUMNS dedup killed 5 drifting column lists in `data/service.ts`.
- 2026-07-20 — Claude: agent-copy rollout across brand and site surfaces — Copy + Copy-for-AI pairs (`webCopy` from `lib/copy-payloads.ts`, `<CopyButtons>`) on the brands portfolio (whole-list header + per-brand row), brand cockpit (whole-brand hero + Websites/Socials/Facts/Assets cards), discovery inbox (whole-inbox + per-item), sites list (whole-list + per-row), site overview (whole-site hero, connections board, initialization-issues panel), site integrations (whole-integrations + per-provider card), Google connections inventory, site settings, and site access (whole-page + per-grant row). Kinds: `web-brand`, `web-brands-list`, `web-brand-websites`, `web-brand-properties`, `web-business-facts`, `web-brand-assets`, `web-discovery-inbox`, `web-discovered-item`, `web-site`, `web-sites-list`, `web-site-connections`, `web-site-initialization`, `web-site-integrations`, `web-site-integration-provider`, `web-google-connections`, `web-site-settings`, `web-site-access`, `web-site-access-grant`.
- 2026-07-20 — Claude: page-audit canonicalization (second `section-canonicalization` build) — deterministic social-card / heading-structure / indexability evaluators (`features/marketing/seo/audit/`, TS↔Python parity-tested against `matrx_scraper/audit_metrics.py`); scraper stamps `web.snapshot.audit_metrics` (contract v1) on every capture. PageWorkspace: social section now renders platform-faithful X/Facebook/LinkedIn cards (`features/marketing/seo/social/SocialCard`) with an X/FB/LI toggle + Social Cards window launcher and observed-tag checks; Indexability gained the verdict banner (Indexable / Needs review / Blocked) + signal list; Headings outline gained evaluator issues + inline skipped-level badges and now shows empty headings. New Social Cards window panel (`socialCardAnalyzerWindow`) + public `/seo/social-preview` tool; `observed_audit_metrics` added to the marketing-page surface.
- 2026-07-21 — Claude: pages table gained the Health column — three deterministic verdict glyphs per page (SERP metadata / social card / indexability) read from the stored `web.snapshot` metrics via the existing batched snapshot enrichment; muted glyph = latest snapshot predates stamping. All 350 pre-existing snapshots backfilled with `seo_metrics` + `audit_metrics` (same TS evaluators, `source: "client"`; scraper-stamped values never overwritten).
- 2026-07-27 — Codex: audit eligibility now follows the crawler's response-driven `web.page.content_type_last`. Known JSON/XML/PDF/image/text resources remain transparent crawl evidence but cannot generate HTML-only SERP/social/heading/URL findings; unusual URLs returning actual HTML remain fully audited. The audit summary reports the excluded non-HTML resource count.
- 2026-07-21 — Claude: URL-quality evaluator (4th, warnings-only `audit_metrics` section; TS↔Python parity; excluded from overall_ok; rendered live in the page-workspace Indexability card) and the new site `.../audit` rollup dashboard (verdict tiles, pass-rate bars, top issues, worst pages; Audit mode in the site nav).
- 2026-07-22 — Claude: site overview hero right column now surfaces Review inbox + Quick work directly under the KPI strip (removed the duplicate cards from the page bottom).
- 2026-07-22 — Codex: corrected Add Brand visibility to the live access model: Internal is the default, Private maps to owner-scoped `personal`, Public grants outside read access, and canonical share links remain a separate mechanism rather than a visibility option.
- 2026-07-22 — Codex: moved every direct scraper command onto the canonical multi-service API target so the shell Production/Localhost switch and an individual scraper pin control crawling without a page-local URL.
- 2026-07-25 — Claude: annihilated the "CanonicalGscSync failed unexpectedly" class. `lib/api/errors.ts` gained `describeBackendFailure`/`unwrapUpstreamError`/`isGenericUserMessage` (unwraps a nested upstream payload, promotes the deepest specific sentence, keeps code/request-id/chain); `crawler/direct-client.ts` now throws `BackendApiError` through it on BOTH the HTTP and in-stream error paths and captures the real cause as the Error Inspector `message`. Google connection health is derived (`credential_present`/`credential_stable`/`health` from `credential_item_id` + `vault_secret_key`, parity with aidream's resolver precondition) and explained in the new `google/health.ts`; the connections hub shows the exact reason + a per-connection Diagnostics block + Reconnect, and the GSC card blocks Sync with the reason up front and renders the full failure (cause, code, request id, service chain) after a failure. aidream's `resolve_connection_credential` now stamps `needs_attention` + `last_error` on every credential failure and self-heals on success (deploy pending). Root cause of the incident: connection `7223fed4…` sat `status='connected'` with no vault credential from 2026-07-19 until a reconnect at 02:39 on 2026-07-25, after which the same sync succeeded.
- 2026-07-25 — Codex: extended the one canonical Google popup connection with `youtube.readonly`, owned-channel discovery, first-class `youtube_channel` inventory rows, and visible per-account YouTube channel counts/names. No browser token store, Next.js OAuth route, or YouTube-specific credential system was added.
- 2026-07-26 — Claude: killed the rank-check error/spinner class on `/marketing/.../ranks`. Backend: matrx-seo's Brave adapter was a recreated, unpaced Brave client (fresh instance per run, `min_interval_seconds=0`, 3 fast retries → raw 429 surfaced to users); it now rides matrx_scraper's shared process-wide Brave rate limiter in aidream (standalone gets internal adaptive pacing tuned from `x-ratelimit-limit`), retries 6x honoring Retry-After — a rate limit queues, never errors. Frontend: `useRunRankCheck` captures in-band stream `error` events and always forces a terminal done/error state after the stream ends (rows previously spun forever because `result.error` stays null on in-band failures), surfacing failures as a toast.
- 2026-07-26 — Codex: promoted YouTube Discovery from the dev demo route
  into its permanent Marketing home at `/marketing/discovery/youtube`, moved
  the full feature to `features/marketing/discovery/youtube`, registered it in
  the hub, shell nav, admin map, route builders, and metadata, and retained the
  old demo URLs as redirects. Modal previews and direct video pages now share
  one preview component and link to each other.
- 2026-07-27 — Claude: **GSC page totals read path fix** — `getPageSearchTotals` now aggregates from `web.gsc_page_stat` (the scraper sync target, same as v_page_list/KPI strip) instead of empty `seo.search_performance_daily` page-profile rows; per-query breakdown still reads `search_performance_daily` query_page rows when that pipeline has populated them.
- 2026-07-29 — Claude: marketing-page surface 360 additions — new `page_draft_content` write target (mode `draft`, `applyPolicy: ask`, value `{ markdown, mode?: "replace" | "append" }`): an agent's proposed page body is STAGED into PageDraftContentCard's unsaved-draft editor state (resetKey bumped so the rich panes show it); the user still clicks Save draft — nothing persists on apply. MarketingPageWriteTargets now also PUBLISHES the `page_keywords` UI-state key (`{ target, supporting[] }` — primary target keyword + attached supporting phrases) for keyword kind-components, unpublished on unmount; the keyword-board queryFn was extracted to `data/page-keywords.ts` (`fetchPageKeywordBoard` + `PageKeywordBoardEntry`) so the publisher and PageKeywordsCard share ONE fetch definition on `pageKeywordsQueryKey`.
- 2026-07-30 — Claude: new site **Structure** vertical (`.../structure`) — the routing tree over the canonical page registry: pure `lib/route-tree.ts` builder (ascending subtree-size sort, per-node cumulative level counts, virtual "no page" gap nodes; unit-tested), bounded `fetchSiteStructureRows` + `useSiteStructure`, Tree + Columns views with level filter/search, nav mode, route metadata, admin map row.
- 2026-08-08 — Claude: **Backlinks workspace rebuilt to GSC-grade.** The single-scroll page (in-body h1 + always-open schedule band + axis-less hand-rolled SVG chart + top-8 truncated cards) became a tabbed workspace on the GSC architecture: `BacklinksGate` (the route's one `next/dynamic` edge), tab pills + compact toolbar, `BacklinkKpiBand` (canonical `MetricCell`; spam/dofollow-share/new-lost-domain KPIs surfaced for the first time), recharts `BacklinkTrendChart` (diverging new/lost bars + running-total lines, real axes/tooltip), `BacklinkObservationTable` (page rank, spam, placement, rel chips, broken status, first/last seen — previously fetched-but-hidden), parameterized `BacklinkDimensionTable` with true pagination, and `BacklinkInsightsTab` (anchor-profile classifier with over-optimization warnings + five server-filtered lenses). Foundation: `lib/vocab.ts` / `lib/extras.ts` (jsonb narrowers) / `lib/anchors.ts` (15 unit tests). Data layer gained lens-aware observation reads + paginated dimension reads. Backend gaps handed off in `aidream/docs/handoffs/backlink-intelligence-backend.md`.
- 2026-08-08 — Claude: **Site overview hero + crawl UX humanized.** Hero image restored (root cause was the platform-wide matrx-files FileRecord contract drift — see `features/files/handler/FEATURE.md` 2026-08-08). Crawl defaults: `max_pages` 500→9,999 (was silently truncating; backend cap 50,000), settings fallbacks now import `defaultCrawlOptions` instead of a drifted copy. Launch page labels are plain language ("Page limit" / "Parallel fetches") with repercussion helper text. Live feed: "Checkpoint" → "Progress — N pages scraped · M waiting", `crawl_warning` rows show the scraper's real message (was a fixed string), consecutive progress checkpoints collapse to the newest. `useCrawl` polls every 3s while a session is running/queued so the crawl detail page is live. "In Google" KPI shows "Connect Search Console" + routes to `/integrations` when GSC is not connected.
- 2026-08-08 — Claude: **Site Media workspace rebuilt as a five-view canvas.** `SiteMediaWorkspace` (route `.../media`, `?view=`) now carries: **Crawled** (the existing deduped inventory + click-to-drill `MarketingMediaAssetWindow` — full metadata, page doors, standards check, copy/open, promote-to-library, order-replacement), **Library** (this brand's `web.brand_asset` rows grouped by kind: upload via `useFileUpload` → `createBrandAsset({fileId, source:"uploaded"})`, add-by-URL via the reused `BrandAssetEditorDialog`, star/edit/delete), **Research** (`research.rs_media` org-scoped via `data/media-library.ts#fetchResearchImages` — topic/origin/search filters, own-domain images promote to the library (`source:"research"`), external ones become Generate briefs), **Generate** (order-off-the-menu: `lib/media-order-presets.ts` presets resolve dimensions from the site's media standards, spec built by `buildSiteImageSpec`, runs the SAME two-step pipeline as the page card via `generatePageImageTwoStep`, result persisted as `brand_asset {source:"generated"}` and shown in a Recently-generated strip), and **Standards** (`web.site.settings.media_standards` slot editor — `parseSiteMediaStandards`/`saveSiteMediaStandards`, read-merge-write, version-checked). `createBrandAsset` accepts `fileId` + `source` (`brand_asset_source_check` widened live to include `generated`/`research` — `migrations/web_brand_asset_source_widen.sql`, ledgered). `SnapshotMediaGallery` tiles take optional `onSelect`. `sectionSuffix` gained the missing `intake` slug. Verified live: standards save round-trip (SQL-confirmed), all five views on real site data.
- 2026-08-11 — Codex: **Crawled-media detail is a real window, not a fixed drawer.** Every gallery tile opens the registered multi-instance `MarketingMediaAssetWindow` through its typed callback-aware opener. The canonical `ImageViewer` fills the resizable main canvas (zoom/pan/rotate/flip); metadata, standards verdict, page `EntityRef` doors, and library/edit/replacement actions live in WindowPanel's resizable secondary inspector. The overlay carries only `siteId` + `assetSrc` and rebuilds the asset through `useSite` / `useSiteMedia`; callbacks never enter Redux.
- 2026-08-11 — **Video inventory now requires positive video evidence.** `buildSiteVideoAssets` no longer upgrades every generic crawler `embed`/`iframe` resource into a video. It accepts known YouTube/Vimeo URLs, direct video files/MIME types, or resources explicitly classified as video (including the crawler's provider hint), keeping Google Maps, forms, calendars, documents, and other embedded tools out of the Videos view. The exact Google Maps embed regression is pinned by the pure aggregation test.
- 2026-08-11 — Codex: **Crawled and Videos inventories no longer exceed the Supabase REST request-line limit on large sites.** The shared media service still pages `web.page` at 1,000 rows, but batches the follow-up `web.snapshot.id IN (…)` filters at 150 UUIDs — response paging and GET-URL sizing are separate boundaries. A 1,073-current-snapshot regression covers both image and video projections. No RLS or schema change was needed: the authenticated caller could read every row; the prior 1,000-UUID filters were ~39 KB and the edge returned a body-less 400 before PostgREST/SQL executed.
- 2026-08-08 — Claude: **Media workspace video pillar (six views) + the first video agent flow.** New **Videos** view (`?view=videos`, `SiteVideosView`) joins the media canvas: site-level crawled video evidence — `fetchSiteVideoResourceRows` pages ONLY the `extracted->resources` JSON sub-path (never full `extracted` blobs) and pure `lib/snapshot-video.ts#buildSiteVideoAssets` dedupes by provider+id (YouTube via `lib/media/youtube`, Vimeo via the new shared `lib/media/vimeo` — hoisted from research's `mediaEmbed`, which now re-exports; generic iframes require the crawler's explicit video/provider signal and tracking scaffolding remains excluded via `NON_MEDIA_EMBED_HOSTS`) — plus the brand's owned `brand_asset kind='video'` rows (uploads render via `InlineMediaRef as="video"`; provider URLs show derived posters). Crawled videos promote to the library (`source:"discovered"`). **Agent flow:** "Write metadata" runs the system agent _Marketing Video Metadata Writer_ (`VIDEO_METADATA_AGENT_ID` in `lib/generate-video-metadata.ts`) through the SAME headless shell (`runHeadlessAgent`/`waitForAnswerText`, now exported from `generate-page-image.ts`) with a strict `<video_metadata>` JSON answer wrapper, persisting title/description/keywords/schema.org VideoObject to `brand_asset.title/notes/data.video_metadata` (UpdateBrandAssetInput patch gained `data`). The media-resource kind vocabulary is now shared (`MEDIA_RESOURCE_KINDS`/`isMediaResourceKind` in `lib/snapshot-content.ts`; PageMediaCard consumes it). Fix-on-sight: BrandLibraryView video tiles no longer render broken (InlineMediaRef/poster/FileVideo instead of CircleOff), MarketingMediaAssetWindow's image-promote dropdown no longer offers `video`. Promo-clip GENERATION is a registered Coming Soon (`marketing.generate-video`, blocked on a durable video-job path — aidream has five video providers reachable only via video-model agents / `ai.generate_video` workflow nodes).
- 2026-08-08 — Claude: **Page workspace: CMS push v2 + surface completeness.** A successful Push to CMS now advances the linked `plan.node` status to `in-production` (forward-only from unset/idea/planned/briefed; `bumpPlanNodeStatusAfterPush` in `lib/push-to-cms.ts`; bump failures scream but never roll back the push). The marketing-page surface gained a `publication` group + `cms_push` value (Plan→CMS bridge summary read from the Push card's react-query cache at trigger time), `link_plan` (authored link plan live-scored against observed edges via the same pure helpers LinksPlanCard renders from), and `media_inventory` (categorized snapshot media via the shared categorization core, capped 60 assets); `desired_values` description now names the link-plan slices and plan-note keys. DB mirror synced (80 value rows). LinksPlan card anchor corrected to `link_plan` for Locate.
- 2026-08-08 — Claude: **Media workspace image-editing suite.** Library assets with a `file_id` (and crawled assets via a new "Import & edit" that materializes the URL through `fileHandler.upload({kind:"external_url"})` → `createBrandAsset({source:"discovered"})`) open `AssetImageEditorDialog` (`components/media/AssetImageEditorDialog.tsx`): the canonical Image Studio `EditModeShell` (Filerobot + AI ops: adjust/auto-color/sharpen/denoise/Remove BG/Inpaint-with-mask/upscale via the typed `/images/*` Python client) in a near-fullscreen dialog, in NEW `preserveSource` mode — the source file is never mutated; every save/op creates a new `files` row and lands back as a new `web.brand_asset {source:"manual"}`. A "Fit to standards" strip renders one-click cover-crops for each `media_standards` slot (`POST /images/edit` `op:"resize"`, slot format, `visibility:"public"`) and saves each as its own library asset. `BrandLibraryView` now takes `standards` and shows a Crop tile action; `MarketingMediaAssetWindow` returns imported assets through its typed callback channel; `preserveSource` lives on `ModeShellProps` (see `features/image-studio/FEATURE.md`).
- 2026-08-08 — Claude: SEO results became actionable — `ApplyMetaToPage` (seo/serp) writes a generated title/description to a page's desired metadata via `useUpdatePageIntent`, `AddKeywordsToPage` (seo/keyword-research) attaches researched keywords via `addPageSupportingKeywords`, both through the shared `components/pages/PagePickerDialog`; `searchPagesForMetaApply` added (cross-site, filters crawled assets out via `content_type_last`).
- 2026-08-08 — Claude: **Page-registry integrity — duplicate URLs and asset inflation, both fixed at the read layer.** The reported "two live rows for one URL" was NOT an upsert bug: `page_site_id_url_hash_key` (UNIQUE `(site_id, url_hash)`) has always held, the scraper upserts through it, and there are ZERO same-site duplicates and zero duplicate live sites per domain. All 810 duplicate URL groups were pages of a SOFT-DELETED site (817 rows) that the site-delete cascade never touched. Fixed by INNER JOINing live sites in `v_page_list` and adding `site!inner(id)` + `.is("site.deleted_at", null)` to `searchPagesForMetaApply` (the one cross-site page reader) — deliberately NOT by cascading `deleted_at` onto pages, because `_gc_assoc_softdelete` hard-deletes association edges and a restore could not rebuild them (filed as D-assoc-gc in FOUND_DEFECTS.md). Assets-as-pages (597 of 10,608 live rows) are FLAGGED, not deleted and not moved to a second table: `v_page_list.is_resource` is the server verdict, `lib/page-content-class.ts` is the ONE TS mirror (replacing the hand-rolled copies in `service.ts` and `audit-rollup.ts`), the site KPI page count now reads the projection with `is_resource=false`, and `.../pages?scope=resources` is the deliberate destination. Evidence: zero non-HTML rows carry authored intent; 103 of them are sitemap/GSC-declared, so excluding at crawl time would destroy real findings. Migration `web_page_list_live_site_and_resource_class.sql` applied + verified live (duplicate URL groups 810 → 0).
- 2026-08-09 — Claude: **No-dead-ends sweep (THE DOOR LAW) on the three portfolio tables.** `BrandsPortfolio`: the Brand name cell is a real anchor via `MatrxColumnDef.href` → `marketingRoutes.brand(id)`, and the **Websites** column — which enumerated each brand's sites by domain with `site.id` AND `row.id` in scope and linked none of them — now renders each chip as a `next/link` to `marketingRoutes.site(row.id, site.id)`. `SitesPortfolio`: the Site name cell gained `href` → `marketingRoutes.site(row.brand_id, row.id)` (previously navigation existed only as `onRowOpen`, i.e. mouse-only). `PagesTable`: the canonical-page cell gained `href` → `${sitePath}/pages/${row.id}`. All three keep their whole-row click as the mouse convenience; `MatrxDataTable` already suppresses row-open for clicks that land on an anchor.
- 2026-08-09 — Codex: **Canonical page-performance panel shipped.** `PageWorkspace` now consumes aidream `GET /seo/pages/{page_id}/performance` through generated live-OpenAPI types instead of rebuilding the read from raw `seo.page_performance` JSONB. `PagePerformanceCard` renders Google-threshold mobile/desktop Lighthouse score rings, separately labelled Lighthouse lab metrics and CrUX real-user data, a prominent human regression verdict, two-line performance history, and the response's GSC 28-day KPI + daily sparkline block. `POST .../pagespeed/sync` exposes mobile/desktop/both and streams named provider→persist→receipt progress with elapsed time for slow runs. No-data runs are actionable; missing platform PSI key names the operator fix; unbound GSC shows the server reason and links directly to site integrations. The existing SVG score ring moved to reusable `components/official/ScoreRing.tsx` with caller-supplied threshold tiers (study behavior unchanged). No `seo.collection_run` door exists in the registry/routes, so the panel deliberately does not print a dead UUID.
- 2026-08-09 — Codex: **Site-level performance dashboard shipped.** New site mode `.../performance` consumes the typed aidream site rollup and answers both questions the page-only surface could not: how the site performs overall and how much has actually been tested. It renders coverage progress/cycle estimate, a reusable-chart mobile distribution, mobile/desktop percentiles, improvement/regression leaders, and leads with the traffic-qualified slow-page fix queue; every page row resolves through `marketingRoutes.sitePage`. The zero-measurement state runs the existing streamed PageSpeed sync for the server-suggested page and refreshes in place.
- 2026-08-11 — Codex: **Page analysis writes repaired after the retired web
  batch spine was dropped.** `web.validate_cross_pointers()` still referenced
  `web.batch_job` from its `analysis_result` branch, so every analyzer result
  insert raised `42P01` and rolled back the run. Migration
  `web_remove_retired_batch_cross_pointers.sql` removes that dependency and the
  dead `batch_item` branch while preserving every live site/page/session/item
  integrity check; a rollback-only clone insert verified the trigger on the
  live database.
- 2026-08-11 — Codex: **Page totals now mean evidence-backed pages, not every
  durable URL candidate.** `v_page_list` exposes canonical identity and retained
  page-evidence verdicts; Overview, Pages, and Coverage exclude aliases,
  resources, and unconfirmed candidates from the headline while preserving
  linked destinations for each. Coverage now accounts for the registry total,
  overlapping crawl/sitemap/GSC evidence, and non-overlapping first source.
- 2026-08-10 — Codex: **Page Studio comparison rows now fold and align as one.**
  Each Current/Plan pair has one compact Studio disclosure, nested card toggles
  are suppressed only in Studio, and paired cards stretch to the taller lane.
  Draft content expands with observed page content instead of leaving a short
  editor beside a long document. The remaining current-vs-plan data gaps are
  tracked in `docs/handoffs/marketing-page-studio-alignment.md`.
- 2026-08-11 — Claude: **`lib/markdown/plain-text.ts` — markdown → plain text, for surfaces that show WORDS.** AI Visibility rendered provider answers as raw text, putting literal `**asterisks**` in front of the user; the answer cards now render through `BasicMarkdownContent` (already in that route's graph via `LiveRunDisplay`, so no added bundle weight), bounded and faded rather than line-clamped — `line-clamp` cannot clamp the block children markdown produces. The claims table's "Exact influential wording" column quoted the same markers, and a table cell shows words rather than a rendered document, so it goes through the new primitive. That primitive replaces a private `stripMarkdown` buried in `presentation-export.ts` that only handled bold; it is deliberately conservative (lone `*` and `snake_case` identifiers survive untouched — mangling prose is worse than a stray marker) and covered by 6 tests. **Reach for it only when a single line of text IS the product** — a table cell, tooltip, aria-label, or exported slide. Anywhere formatting fits, render through the canonical markdown path instead.
- 2026-08-11 — Claude: **backlinks correctness + doors pass, and one new shared primitive.** Fixed four real defects (deep link built `?search=` where the table hook reads `?q=`, so it always landed unfiltered; the Domains tab matched before `isDimensionTab()` so the provider's referring-domain aggregates were collected-but-unreachable; the dimension drawer had a title and no body; a long analyze batch rendered no progress and its error panel offered no retry), and turned the inert KPI/review counts into doors onto their lens (the hand-rolled second KPI strip is now the canonical `MetricCell`). **New:** `lib/records/recordUnavailable.ts` + `components/shared/RecordUnavailableNotice.tsx` — a zero-row single-record read is soft-deleted OR alive-but-no-access OR a stale id, and `assertFound` asserted deletion for all three (two agent-review items were rejected as "site deleted or inaccessible" while brand and site were both live; the null was RLS). Deletion is now claimed only when a probe proves it, the ambiguous case says both possibilities and offers doors, and every construction reports to the Error Inspector. Audit remainder (plain-language pass, verdict-first Overview, mobile, assists producer, agent-role binding, disavow, drilldowns, movers, anchor footprint, watchlist) is chipped in `.matrx/AGENT_TASKS.md` with context in `docs/handoffs/backlink-intelligence-frontend.md`.
- 2026-08-11 — Claude: **the backlinks workspace now speaks English, not
  DataForSEO and not our pipeline.** Every user-visible string under
  `components/backlinks/**` was rewritten for the non-technical owner (root
  CLAUDE.md, THE MISMATCH RULE), with the vocabulary living in
  `components/backlinks/lib/vocab.ts` rather than per component. Refresh depths
  are `Quick check (weekly)` / `Full detail (monthly)` / `Complete history
  (first run)`; the eleven empty states that repeated "run a Monthly detail or
  Full bootstrap refresh" verbatim now call ONE `backlinkEmptyHint(what)`.
  Pipeline states stopped being shown as status (`Dead letter` → `Gave up —
  needs help`, `Capturing` → `Reading the page`), the `3 trys` attempt badge is
  gone from the table, `PR`/`DR`/`Rel` became `Page authority` / `Site
  authority` / `Counts for SEO?` (values `Passes credit` / `No credit` / `Paid
  placement` / `User-posted`), and `r412` renders as `Authority 412`. The
  vendor name, "the aidream scheduler", "AI Dream work target", "provider
  observations" and "resolved assessment … in their exact persisted form" are
  out of user copy; `Your ruling` / `Confirm assessment` became `Do you agree?`
  / `Yes, this looks right`. `StatusBadge` gained an optional `label` so tone
  still keys on the machine value while the words are human. Layout, tabs, and
  components were deliberately untouched — that is the Overview/IA chip.
