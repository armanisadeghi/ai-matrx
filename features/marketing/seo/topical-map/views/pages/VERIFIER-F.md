# VERIFIER-F — the zero-authorship verifier brief for Lane F

Dispatched by the coordinator in-process (model `claude-sonnet-5`, lane `quick`) once the three Lane F builders have landed. The lane owner wrote this; the builders never saw it.

LANE: quick, model claude-sonnet-5 (verifier, ZERO authorship). You did not build any of this and must not fix it. You were dispatched by the owner of Lane F (pages convergence workspace) of the topical-map UI build to attack what three builders produced, against the OWNER'S WORDS below — not against the builders' file lists or summaries. Return what is missing, wrong or dishonest. Work autonomously; nobody answers questions.

REPO. https://github.com/armanisadeghi/ai-matrx, branch `claude/tender-gates-5qcxnd` (checked out). NO dev server. Supabase MCP reads (project brsgrqvjdzwihsvnfqkf) are allowed if you want to compare a rendered number against the live function; never a write. You may run: `pnpm install --frozen-lockfile`, `pnpm type-check`, `pnpm check:parse`, `pnpm check:dead-ends`, `pnpm check:agent-disclosure`, `pnpm check:mandate-keys`, `pnpm check:scroll-chain`, `pnpm jest features/marketing/seo/topical-map --no-coverage`, and any jest test YOU write under `features/marketing/seo/topical-map/views/pages/__tests__/verify-*.test.tsx` (commit those by path with trailer `Co-Authored-By: Claude Sonnet <noreply@anthropic.com>` and push; edit NOTHING else).

THE OWNER'S WORDS (the bar). Vision §1.2: a per-page intent — keep here, move to that topic, merge into that page, redirect to that page (live or planned), rewrite in place, delete. Vision §2.7 step 7: "The person marks pages in bulk — select fifteen, two clicks, 'move these', 'delete these', 'redirect these to that' — and creates planned pages where needed." Vision U5: "a table of a site's pages filterable by topic, current-vs-intended topic, disposition, state, traffic (zero-click pages, thin pages), with multi-select and two-click actions … accept/reject proposed intents in every mode; progress counts per topic and per site. Writes through set_page_intents. Settings: intent_review_mode, bulk_action_confirm (always | above N; default above 20). Done means: on a 4,000-page site, marking 200 zero-traffic pages as redirect-to-a-planned-page takes under a minute of clicks." Acceptance §6.5: "Person opens the bulk workspace, filters zero-click pages under 'electronics recycling', selects 200, redirects them to a planned page created from the topic in two clicks." §6.6: "table view shows progress 0/200 done."
Plan §6 F (the coordinator's brief to the lane): MatrxDataTable in controlled query mode over `usePageIntents` (server filters topicSlug/disposition/state, paged ≤1000) plus an "on no topic" tab over `usePagesWithoutTopic`; columns page (record door + live URL) / current topics / destination (IntentDot + topic) / disposition / state / source / clicks / impressions (absent ≠ 0, hidden refs carry no numbers) / region / note; filters bound to `setPageFilters`; multi-select bulk bar (keep, move, merge, redirect live or planned, rewrite, delete, mark done) each a two-click flow (target picker → `useMapDryRun` preview → consequence sentence naming counts, gated by `bulkActionNeedsConfirmation`) writing `useSetPageIntents` with source "human"; result UI showing set / kept ("a person already decided this page") / failed honestly; ReviewDeck in `intent_review_mode`; "make a planned page here" as a redirect target; per-topic and per-site progress strip; header run controls "Map the pages" / "Find the regions" / "Propose destinations" each stating consequence and spend first, floating into LiveRunWindow, rejoining on reload, `dry_run` + `limit` first-class; every knob via `useTopicalMapKnobs`; errors verbatim; `readOnly` removes writes (absent, not disabled). Round 22/23 rules: `current_topics: []` is a real state; an intent with no `topic` key is a real state; kept ≠ failed and kept ≠ silently set.

WHAT YOU DO. (1) Read `features/marketing/seo/topical-map/CONTRACTS.md`, `FEATURE.md`, `views/pages/VERIFY-F.md` (the browser walk — you cannot run it; use it as the list of claims), then the code under `views/PagesWorkspace.tsx` and `views/pages/**` cold, as a hostile reader. (2) Run every gate above; paste each result line verbatim. (3) Prove or refute, with a jest test you write over the REAL recorded fixture `redux/__fixtures__/listPageIntentsRound22.ts` (mock only hook boundaries): (a) the 200-page redirect flow is TWO CLICKS from a selection to the confirm dialog (count the clicks in your test and name them); (b) a recorded `current_topics: []` row and a recorded no-`topic` intent both render honest words, never blank; (c) an absent `clicks` renders as absent, not 0; (d) a `set_page_intents` result carrying `kept: 1` and a `kept_existing` row (shape: `{ ok:true, page_id, kept_existing:{source:"human", state:"accepted"} }`, from aidream migration 0805) renders as kept, never failed, never silently set; (e) `readOnly` yields no checkbox, no bulk bar, no run controls, no review toggle; (f) every colour on the destination dot comes from the `intent_colors` knob (change the mocked knob and the class changes). (4) Hunt for: hard-coded thresholds/colours/modes that should be knobs; a sort control that fakes a client sort over one page; a blank cell where a state should be named; a disabled-looking control without a sentence; a browser dialog; a `dynamic()` import in the feature; a barrel `index.ts`; an RPC sentence reworded; a spinner as the answer while a run works; a place where `kept` is folded into failed; anything the plan asked for that is simply absent (list each). (5) Say what the walk in VERIFY-F.md cannot prove without a browser.

REPORT (final message): one status line (`DONE` = you verified; `DONE_WITH_CONCERNS`; `ESCALATE`), then ≤25 lines: each gate's verbatim result; each of (a)–(f) PROVED / REFUTED with the test name; the missing/wrong list ranked by how badly it breaks the owner's words, each with file:line; the SHA you verified against. Never soften a finding.
