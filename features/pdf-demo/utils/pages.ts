/**
 * Compatibility shim — the canonical page-range parser moved to
 * `features/pdf/utils/pages.ts` in the 2026-06 PDF domain consolidation.
 * Import from there in new code; this path survives only until the demo
 * pages are repointed.
 */
export * from "@/features/pdf/utils/pages";
