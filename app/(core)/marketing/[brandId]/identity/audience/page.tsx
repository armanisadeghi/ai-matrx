import { MarketingComingSoon } from "@/features/marketing/components/MarketingComingSoon";

/** Segments, ICPs, and personas for this client — reserved, not yet built. */
export default function BrandAudiencePage() {
  return <MarketingComingSoon comingSoonId="marketing.audience" />;
}
