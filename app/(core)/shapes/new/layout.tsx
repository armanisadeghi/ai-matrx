import { newShapeMetadata } from "@/features/content-ir/studio/shape-studio-metadata";

export const metadata = newShapeMetadata;

export default function NewShapeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
