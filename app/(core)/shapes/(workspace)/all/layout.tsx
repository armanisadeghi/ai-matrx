import { shapesLibraryMetadata } from "@/features/content-ir/studio/shape-studio-metadata";

export const metadata = shapesLibraryMetadata;

export default function ShapesAllLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
