"use client";

/**
 * /administration/ui/lite-window — the lightweight window, for approval in
 * practice. Same component as `/demos/lite-window`; this is the copy that ships
 * in the admin build, which is the one reachable today.
 */

import { LiteWindowExamples } from "@/features/window-panels/lite-window/LiteWindowExamples";

export default function LiteWindowAdminPage() {
  return <LiteWindowExamples />;
}
