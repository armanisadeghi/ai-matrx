import { OneBindingUi } from "./OneBindingUi";

export const metadata = {
  title: "The One Binding UI (preview) | Administration",
  description:
    "Non-functional mockup of the one flow that binds a job to a place at any rung: two-sides-and-a-middle mapping, AI auto-map, scope as a first-class step, the narrated auto-run promise, and the batch mode. Mock data only.",
};

export default function Page() {
  return <OneBindingUi />;
}
