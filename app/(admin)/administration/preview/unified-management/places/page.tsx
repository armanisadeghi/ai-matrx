import { PlacesWorkspace } from "./PlacesWorkspace";

export const metadata = {
  title: "Places Workspace (preview) | Unified Management | Administration",
  description:
    "Non-functional mockup of the post-merge PLACES WORKSPACE: one place's manifest with known-value alignment, its bindings with the user-owned auto-run control, a discovered-mandates preview with the exclusion valve, the completeness strip (readiness, drift report, counted sync receipt), and the admin ⇄ user-hub scope switcher. Mock data only.",
};

export default function Page() {
  return <PlacesWorkspace />;
}
