// Users & Access › Invitations — canonical table over access requests.
// Thin shell over the feature client.

import { InvitationsTableClient } from "@/features/admin/users/components/InvitationsTableClient";

export default function UsersInvitationsPage() {
  return <InvitationsTableClient />;
}
