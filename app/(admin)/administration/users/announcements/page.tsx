// Users & Access › Announcements — system-wide broadcasts (not feedback).
// Thin shell over the feature client.

import { AnnouncementsTableClient } from "@/features/admin/users/components/AnnouncementsTableClient";

export default function UsersAnnouncementsPage() {
  return <AnnouncementsTableClient />;
}
