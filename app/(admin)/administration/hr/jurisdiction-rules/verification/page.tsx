// HR & Employment Law › Verification (SPEC-UI-IA §3.12 route 85b).
//
// The JUR-SEED board plus every rule past its verification date.

import { JurisdictionVerificationClient } from "@/features/admin/hr/jurisdiction-rules/components/JurisdictionVerificationClient";

export default function JurisdictionVerificationPage() {
  return <JurisdictionVerificationClient />;
}
