// HR & Employment Law › Overview (section home).
//
// Rule counts by status, the overdue board's size, and doors into the rule
// library and the JUR-SEED verification board. Thin shell over the client.

import { HrAdminOverviewClient } from "@/features/admin/hr/jurisdiction-rules/components/HrAdminOverviewClient";

export default function HrAdminOverviewPage() {
  return <HrAdminOverviewClient />;
}
