import { WindowPreviewPage } from "@/features/mandates/record-next/WindowPreviewPage";

export const metadata = {
  title: "Mandate window",
  description: "Open the new mandate window",
};

export default function IntelligenceMandateWindowRoute() {
  return <WindowPreviewPage />;
}
