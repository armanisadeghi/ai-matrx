// Users & Access › Entitlements — canonical table over the capability registry
// + 30-day usage. Thin shell over the feature client.

import { EntitlementsTableClient } from "@/features/admin/users/components/EntitlementsTableClient";

export default function UsersEntitlementsPage() {
  return <EntitlementsTableClient />;
}
