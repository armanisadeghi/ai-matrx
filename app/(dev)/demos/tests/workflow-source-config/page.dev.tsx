"use client";

export default function SourceConfigPage() {
  return (
    <div className="container mx-auto p-4 max-w-2xl">
      <h1 className="text-lg font-semibold">
        Workflow source config (removed)
      </h1>
      <p className="mt-2 text-sm text-muted-foreground">
        The xyflow SourceConfigBuilder demo referenced a deleted module. The
        workflows-xyflow UI is being preserved separately — this test page is a
        placeholder until that surface is rewired.
      </p>
    </div>
  );
}
