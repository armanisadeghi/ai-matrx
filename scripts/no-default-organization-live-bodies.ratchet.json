{
  "public._provision_new_user_personal_org()": {
    "lane": "DEFAULT-ORG-4",
    "note": "Signup provisioning CREATION site. Chair ruling 2026-09-22: creating a person's own personal organization at provisioning is legitimate — the organization being created is the answer. To be declared in its own body with `-- personal-organization-creation:`, after verifying the body only CREATES and never reads a default to route a write elsewhere."
  },
  "public._provision_new_user_profile()": {
    "lane": "DEFAULT-ORG-4",
    "note": "Signup provisioning CREATION site. Chair ruling 2026-09-22: creating a person's own personal organization at provisioning is legitimate — the organization being created is the answer. To be declared in its own body with `-- personal-organization-creation:`, after verifying the body only CREATES and never reads a default to route a write elsewhere."
  },
  "public.access_request_create(text, uuid, text, text)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.ctx_projects_add_creator_membership()": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.dm_get_or_create_direct_conversation(uuid, uuid, uuid)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.fork_processed_document(uuid)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.fork_shared_conversation(uuid, text)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.fork_shared_flashcard_set(uuid, text)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.fork_shared_quiz(uuid, text)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.get_user_email_preferences(uuid)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.handle_new_dm_user()": {
    "lane": "DEFAULT-ORG-4",
    "note": "Signup provisioning CREATION site. Chair ruling 2026-09-22: creating a person's own personal organization at provisioning is legitimate — the organization being created is the answer. To be declared in its own body with `-- personal-organization-creation:`, after verifying the body only CREATES and never reads a default to route a write elsewhere."
  },
  "public.setting_access_request_create(uuid, text, text, text, text, jsonb, text)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.transfer_guest_data_to_user(uuid, uuid, text)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.user_form_profile_append_to_array(uuid, text, jsonb)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.user_form_profile_set_custom_field(uuid, text, jsonb)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.wsp_resolve_system_task(text, text, uuid)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  },
  "public.wsp_upsert_system_task(text, text, text, text, text, text, text, text, date, text, uuid, uuid, uuid, jsonb)": {
    "lane": "DEFAULT-ORG-4",
    "note": "Non-creation body: it substitutes the caller's personal workspace for an organization the call, the record or the ladder already knows. To be replaced so the acting organization is passed explicitly and a call that cannot answer is refused."
  }
}
