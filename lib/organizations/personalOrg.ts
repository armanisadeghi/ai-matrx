// Transitional re-export: `ensureOrgId` / `ensureOrgIdServer` now live in
// `@/lib/organizations/ensureOrgId`. Import from there; this path is removed
// once no importer remains.
export { ensureOrgId, ensureOrgIdServer } from "@/lib/organizations/ensureOrgId";
