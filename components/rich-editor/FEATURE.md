# components/rich-editor — THE ONE EDITOR (Tiptap 3 visual + CodeMirror 6 source + shared-renderer preview)

Rich-content PLAN decisions 5, 6, 11 (`../../../common-docs/projects/rich-content-unification/PLAN.md`), register row RC-B4.
Verified against code 2026-09-25. Built beside Toast UI — **no surface uses it yet except `/markdown-studio` → Editor mode**; the flip is a later row.

## The contract — the stored text is the truth

- **No edit → the stored bytes.** Open, switch Visual/Source/Preview, save: byte-identical (`serializeVisualDocument` writes every unchanged block as its stored bytes).
- **An edit changes bytes only where the person typed.** Only changed blocks are serialized (`core/markdown-serialize.ts`) in the author's own spelling (`md*` attrs: `*`/`-`, `**`/`__`, `1)`, heading style, quote prefix, link tail, table row bytes). **No escapes are ever added** (a literal `|` typed into a table cell is the one exception — it would split the cell).
- **The fidelity gate.** A construct is editable rich text only if the serializer reproduces its stored bytes exactly at load (`core/markdown-parse.ts`); otherwise it is a `sourceLocked` atom — rendered, edited as source, written back verbatim.
- **Islands are bytes.** Kinds / `__kind` JSON, XML sections, fences, math, HTML, comments, anchors, `{{vars}}`, citations, tags are atoms (`islandBlock`, `inlineIsland`) edited only in their own editor (`islands/IslandCodeEditor.tsx`, `replaceIslandRaw`). Typing over a selected island starts a paragraph after it.
- **Moves and pastes rewrite nothing they did not bring.** A block move (⌘⇧↑/↓, the grip menu, a grip drag) changes only the order: every other block stays its baseline node and is written back as its stored bytes. There are **no Tiptap paste rules** in the schema (`createRichEditorExtensions` strips `addPasteRules` from every extension): Tiptap runs them over the whole changed range of a paste or a DROP, and a drag's range spans every block it passed, so the link rule once re-marked an untouched bare `ops@example.com` into `[ops@example.com](mailto:…)`. Plain-text paste (a text editor, a terminal, ⌘⇧V) is parsed by the ONE parse path (`core/paste-markdown.ts` → `buildVisualDocument`), source identity stripped — a pasted list is a list, a pasted `<section>` one island, a `{{var}}` a chip. HTML paste goes through the schema's HTML rules (`html-to-markdown.ts`).
- **Callouts, directives, footnote definitions and wikilinks are islands** (content-ir 0.14): stored ones change only through their island editor; `setCallout` on new prose writes a fresh `> [!TYPE]`.
- **Author escapes read as the character.** `\*`, `\_`, `\|`… load as the bare character under the non-inclusive `mdEscape` mark; the serializer writes the backslash back before each ASCII punctuation character in it. Text typed beside it is plain — the editor never adds an escape.
- **Undo is the person's own act** (`core/history-approval.ts`): the island bytes an undo/redo takes away (Visual: prosemirror-history meta; Source: CodeMirror `undo`/`redo` user events) are approved, so reversing an island edit — even across a save — never asks for consent.
- **Tables — THE writer is `core/table-source.ts`.** `rewriteTableSource(original, grid)` is the one way any edit path writes a table back (the rich editor's serializer AND the in-body answer table editors MarkdownTable / TableWithSeparatedControls / StreamingTableRenderer): an untouched cell is its source bytes (padding, escapes, `\|`), only an edited cell changes, a row added/removed changes only its line. The ONE parser (`parseMarkdownTable`) reads `\|` as part of a cell. Guard `pnpm check:table-writers` (shrink-only). A re-aligned column rewrites only its own delimiter cell (same width, same spaces); a column added or removed rewrites the delimiter row.
- **The save gate (`core/save-plan.ts`).** Every view saves through `planSave(stored, current)`: block-LCS regions, island accounting (removed / changed / swallowed need consent unless changed through the island's own editor; moved never asks), then content-ir 0.13's strict splice in two proven steps — `islandEdit` for island changes, block edits for prose. Validation offers, never blocks: the consent dialog always has **Save anyway**.

## Where things are

| Path | What |
|---|---|
| `RichEditor.tsx` | Front door — the ONE `dynamic(ssr:false)` edge; props type re-exported |
| `RichEditorImpl.tsx` | Shell: views, toolbar (tools scroll; host actions + Cancel + Save never scroll), find, outline, focus mode (floating Exit + Save; Escape exits), dictation (`useMicField`), right-click AI menu (`EditableContextMenu` — its trigger slots onto a host `<div>`, never onto the editor component), save + consent, status bar. In-place hosts (RC-B5): `onCancel`/`cancelLabel`, `defaultOutlineOpen={false}` for a narrow column, `onNothingToSave` (Save with no change → the host returns to its reading view) |
| `core/` | React-free, headless-testable: schema (`extensions.ts`), parse/serialize, `visual-document.ts`, `save-plan.ts`, `commands.ts` (every verb), `find-replace.ts` + `visual-find.ts`, `outline.ts` (github-slugger = renderer anchors), `text-metrics.ts`, `variables.ts` (HighlightedText's rule), `shortcuts.ts` (THE key table), `source-format.ts`, `html-to-markdown.ts` (the paste converter = the schema + serializer) |
| `visual/` | Tiptap view: node views, slash + `{{` menus, selection & table toolbars, block handle (drag/move), decorations (anchors, task boxes, footnotes, find), `shortcut-handlers.ts` |
| `source/` | CodeMirror live preview: marks hide off the cursor line, `{{var}}` chips, islands rendered through `MarkdownStream` via portals, find highlights, `{{` completion |
| `islands/` | Island labels/icons, `IslandPreview` (shared renderer; print package page-break divider), `IslandCodeEditor` |
| `panels/` | Find & replace, outline, shortcuts sheet (⌘/), kind picker (`content_ir.kind_definition` + canonical `kind_example`) |

Page breaks use `@ai-matrx/print/directives` (`PAGE_BREAK_MARKDOWN`, `isPageBreakLine`) — one grammar. `marked` is used ONLY in `core/markdown-parse.ts`, as a byte-mapped lexer (lawful site in `scripts/rich-content-inventory/registry.ts`).

## Gates

- `npx jest components/rich-editor` — adapter, features, strict splice, shortcuts (mutations via `plant.py` go red: naive serializer, escaping serializer, consent-less gate, table-row reuse, skipped island step).
- `npx tsx scripts/check-rich-editor-roundtrip-corpus.ts` — every stored row (shared reader `scripts/lib/rich-content-corpus.ts`) through a real headless Editor: no-edit, view switch, exact one-paragraph edit, islands intact, no escapes, and `move_blocks` (a drop-tagged drag of the first block through the editor's own plugins: every unmoved block keeps its source identity; dragging it back returns the stored bytes). Read-only; ids only.
- `table-source.test.ts`, `pasted-tables.test.ts` — the table writer and real-world pasted tables (Wikipedia, Docs, Notion, Sheets, Excel, Word) through both paste paths.
- `block-moves.test.ts` — every block spelling (bare URLs/emails, autolinks, both emphasis spellings, reference links, HTML, escapes, islands) moved by ⌘⇧↑/↓ and by a drop-tagged drag; plain-text paste structure.

## Change Log

- 2026-09-25 — Round 4 (verify R4-1…R4-4): THE splitter follows GFM's escape rules (backslash-run parity); the writer escapes pipes by parity, pads a trailing escape, adds edge pipes for empty edge cells, and reads every row back — `TableWriteRefused` keeps the draft instead of writing a wrong table; typed backslashes before punctuation doubled in Visual; `\|` in code inside a table cell shows `|` (core rule). Judged by an independent GFM oracle (`scripts/lib/gfm-table-oracle.ts`): the verifier's hostile-table harness is a permanent test, and the corpus table gates judge by the oracle. Read-only damage census: `scripts/scan-table-edit-damage.ts`.
- 2026-09-25 — Round 5 (verify R5-1…R5-3): the writer re-reads the WHOLE written table with `marked` (`assertTableReadsBack`) — a row that splits right but ENDS the table (pipe-less `- | x`, `> 90%`, `# 3`, `1.`) is refused, relative to the stored table's own extent; a rewritten first cell of a pipe-less row gets a leading pipe (`| - x`). A code span never wraps another mark's delimiters (`` `- `[`x`](u) ``, not `` `- [x](u)` ``). One GFM cell-pipe rule for every surface: `components/markdown-core/syntax/gfm-cell-pipes.ts` (Studio remark plugin, chat/artifact `StreamingTableRenderer`, the writer). Chat's splitter and live stream draw pipe-less GFM tables (`processors/utils/gfm-table-lines.ts`). Corpus gates type block syntax into first cells on both paths; `--ids` re-judges named rows. The verifier's round-5 probes live in `__tests__/verifier/`.
- 2026-09-25 — Round 3 (verify R3-1…R3-6): THE table writer (`core/table-source.ts`) for the editor and all in-body answer tables, `\|` kept in the one parser, `check:table-writers` guard, corpus `table_cells` + `answer_tables` gates; escaped brackets literal unless TeX (content-ir rule); node-view controls stop PM events (first pencil click threw); edge whitespace moved outside emphasis; pasted literal `*`/`` ` `` escaped; phone Find input full width.
- 2026-09-25 — Minor findings F8c–f: author escapes read as the character (`mdEscape` mark), only the re-aligned delimiter cell changes, one dictation error toast (useMicField owns it; guard `features/audio/__tests__/mic-field-one-toast.test.ts`), undo/redo of an island edit never asks for consent.
- 2026-09-25 — Verify round (RC-B4 F1–F8): paste rules removed (a drag rewrote an untouched autolink), plain-text paste parses as markdown, `{{`/slash menus own Enter in list items, `{{` offers the document's own variables (both views), slash filters past a space, outline keeps `{{vars}}`/in-word underscores, focus mode has an Exit + Save bar and Escape, phone Save never scrolls away, grip opens its menu on click (a Radix trigger's pointerdown killed the drag), right-click menu slots onto a host div; RC-B5 in-place props. Callout tests moved to the content-ir 0.14 island contract.
- 2026-09-25 — Built (RC-B4): three views, save gate on content-ir 0.13, slash/variable menus, tables, callouts, checklists, page breaks, footnotes, block handle, find & replace, outline, metrics, focus mode, shortcut table, paste converter, image upload, math/code/kind islands; mounted on `/markdown-studio` Editor mode saving only to a disposable note.
