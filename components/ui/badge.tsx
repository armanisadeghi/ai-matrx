import {
  Badge as DesignSystemBadge,
  badgeVariants as designSystemBadgeVariants,
  type BadgeProps as DesignSystemBadgeProps,
} from "@ai-matrx/design-system";

export const Badge = DesignSystemBadge;
export const badgeVariants = designSystemBadgeVariants;
export type BadgeProps = DesignSystemBadgeProps;
