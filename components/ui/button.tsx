"use client";

import {
  Button as DesignSystemButton,
  buttonVariants as designSystemButtonVariants,
  type ButtonProps as DesignSystemButtonProps,
} from "@ai-matrx/design-system";

export const Button = DesignSystemButton;
export const buttonVariants = designSystemButtonVariants;
export type ButtonProps = DesignSystemButtonProps;
