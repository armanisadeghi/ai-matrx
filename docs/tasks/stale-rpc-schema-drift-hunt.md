Focused hunting session: find every Postgres function/RPC in our live database that references a column or table that no longer exists, before users find them for us. Today a user hit an error on /agents/all because project_id had been removed from agents but the RPCs behind that page were never updated — this exact class (schema changed, dependent SQL functions not updated) has bitten us repeatedly, and it's invisible until a user calls the broken function.

Context: ONE live Supabase database, project txzxabzwovsujtloxrus (Matrx Main), accessed via the Supabase MCP. The 2026 schema-reorg moved many tables out of public into custom schemas (iam, platform, admin, chat, plan, users, ...) and removed columns (the project-FK removal from feature tables is doctrine — see the aidream CLAUDE.md 'Forbidden relationship shortcuts' section), so stale references are likely widespread.

Do this:
1. SWEEP: pull every function's source from pg_proc (all non-system schemas, including SECURITY DEFINER RPCs, trigger functions, and views/materialized views too — same failure class). Statically extract table and column references and diff them against the live information_schema. Flag: references to dropped columns (e.g. project_id on tables that lost it), references to tables under their old public.X name after the schema move, and references to functions that no longer exist. Beware false positives from dynamic SQL and plpgsql variables — verify each hit by reading the function body before flagging.
2. VERIFY & CLASSIFY each confirmed-broken function: is it still called? Check frontend call sites (supabase.rpc('...') across matrx-frontend and matrx-extend), aidream code, triggers, and cron. Classify: BROKEN-AND-CALLED (critical — users hit it), BROKEN-AND-DEAD (candidate to drop), SUSPICIOUS.
3. FIX the BROKEN-AND-CALLED set directly in the live DB via the Supabase MCP (no migration files — apply live, verify by calling the function, per repo doctrine). For BROKEN-AND-DEAD, list them for my approval before dropping anything.
4. BUILD THE PERMANENT GUARD: this repo's doctrine is 'compare TRUTH to code' — add a check that pulls all live function sources and diffs their references against the live schema, screaming on any stale reference. Follow the reference implementation pattern in db/schema_analysis/ (a new @register_check in checks/, runs loud + non-blocking in release.sh). The goal: the next time a column is dropped with a stale RPC left behind, the very next release run names it — no user ever finds it first.
5. Report: full findings table, what you fixed and verified, what needs my decision, and confirm the guard fires correctly (test it by temporarily pointing it at a known-stale fixture if needed).