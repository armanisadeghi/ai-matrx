---
status: active
updated: 2026-07-20
repos: [matrx-frontend, aidream]
vision: []
---

# Marketing — Brand-first platform + coverage program

## Vision — Arman's words

- "a website is only one of many assets a company has that a marketing company has to manage… they will also have an Instagram account, accounts on Facebook, an x, TikTok, and YouTube… Let's build this correctly right now… Let's think about putting the structure in place today that we will be able to grow into tomorrow."
- Brand is the anchor name: "the name 'Brand' makes a lot more sense" — built for marketing companies, but "for 95% of our orgs, it's actually their own brand or brands they're managing."
- Discovery review: "having this page that is this highly dynamic UI that allows a user who's a marketing expert to go through everything we have found and essentially tell us what these things are… oh, wow. That's the company's logo."
- Routing: "marketing/brands/[id]/sites/[id]… That would then leave room for… marketing/resources, marketing/tools/meta-tile-checker… marketing/brands/[id]/socials/instagram/[id]".
- Core-first: "I'm not focused on the AI integration parts because we still need to just get the core working where we can connect a site… properly reads the sitemap and that you can click to see the sitemaps and we can actually manage them… if we're connecting to Google Search Console, where is that data going?… we need to have our canonical pages act as somewhat of our anchors so that then everything attaches to those… an exhaustive list of external links and then also internal links and that they all eventually reconcile somewhere."
- Editing doctrine: "give me FULL AND COMPLETE access to edit ALL editable things at that level. No hiding data from the user… easy and direct to edit either directly in the table or via a window panel component or both."
- Errors: nothing silent, ever; everything lands in the admin Error Inspector. Bar: "BETTER be better than" Botify/Screaming Frog-class tools.

## Resources

- [features/marketing/FEATURE.md](../../features/marketing/FEATURE.md) — authoritative; read before any code. Invariants: routes only via `lib/routes.ts`, statuses only via `lib/site-status.ts`, toast only via `@/lib/toast`, machine writes → `discovered_item` only, no hover-underline name-links (whole-row click).
- Data layer: `features/marketing/data/service.ts` (+hooks.ts) — follow its exact patterns (column-list constants, bounded ranges, id tie-breaks, batched `.in()` enrichment).
- Scraper commands: `features/marketing/crawler/direct-client.ts` (streamCommand; auto-captures failures to the Error Inspector, source `marketing-crawler`).
- Server twin: aidream `packages/matrx-scraper` — `web_crawl/sitemap_sync.py`, `web_crawl/service.py`, `web_crawl/FEATURE.md`. All logic in importable service functions (future workflow nodes).
- DB: schema `web` on project `txzxabzwovsujtloxrus`. New-table recipe (registry rows in `platform.entity_types`/`entity_relationships` → `iam.apply_rls` → grants → base triggers) is modeled in [migrations/web_sitemaps.sql](../../migrations/web_sitemaps.sql). Migrations: apply via Supabase MCP + upsert `public._schema_migrations` (source matrx-frontend, sha256 checksum, duration_ms 0) + `pnpm db-types`. ON CONFLICT arbiters must be plain (NULLS NOT DISTINCT) uniques — partial/expression indexes broke the scraper twice.
- Test login: `http://localhost:<port>/api/dev-login?token=<DEV_LOGIN_TOKEN>&next=/marketing/brands` (session hook prints the URL) or `/login` admin@admin.com / Password1234#. Turbopack trap: new/moved route dirs sometimes need a dev-server restart (stale route-graph cache reports phantom conflicts).

## Remaining work

1. **GSC unblock — two env vars + reconnect (deploy itself is DONE).** matrx-scraper 0.1.41 published to PyPI 2026-07-21 02:23 and the service rebuilt from it; that build contains the vault-based GSC path (aidream e7422f47b) and the www/apex scope fix (0447e2f38). The scraper is a standalone service and does NOT inherit aidream's env: `gsc_sync.py` reads **`AIDREAM_URL`** (`https://server.app.matrxserver.com`) and **`AIDREAM_SERVICE_TOKEN`** (same value as aidream's `AIDREAM_SANDBOX_SERVICE_TOKEN`) from its OWN process env, failing loudly with "GSC credential resolution is not configured…" if unset — confirm they're set on the scraper service (they are NOT in `packages/matrx-scraper/.env.example` — add them there too). Then Arman reconnects Google at `/marketing/connections` (row `7223fed4…` is `needs_attention`, no vault ref; requires his Google login) and runs Sync. aidream prod is verified live with the `/google-integrations/*` endpoints. `web.gsc_page_stat` has 0 rows ever.
2. **Post-deploy E2E** on All Green Recycling (arman@ account): initialize (expect 4 screenshots + discovered items + zero `initialization.errors`), sitemaps sync (~33 docs / ~3.7k pages), discovery-inbox review, GSC sync (expect `gsc_page_stat` rows + pages Clicks/Impr/Pos columns + coverage Google cells + "last synced" freshness), `links/resolve` backfill. Never-run-verified paths: GSC sync end-to-end; GSC/enrichment columns with real data in-browser.
3. **Direct single-page fetch (Arman ask, 2026-07-20).** "When you go to a page, there is no button to just go get the most updated version of that page directly… This also applies to pages that have never been captured before." Build a scraper command `POST /crawler/sites/{site_id}/pages/fetch` (body: page_id or url) that reuses the single-URL capture pipeline (snapshot + `seo_metrics` + screenshot + head_tags, restore-on-conflict per item below) WITHOUT the crawl-session scope machinery — the existing homepage `bootstrap` command is the model to generalize. FE: "Fetch now" on the PageWorkspace header + pages-table row action + fetch-after-create on manual "Add page". Must not disturb crawl sessions or scope classification.
4. **Everyone-can-view access (Arman ruling, 2026-07-20).** "Considering these are public websites and the content is public things we've scraped, then EVERYONE gets full access to view." Wire universal VIEW through the canonical access architecture (`web.brand`/`web.site` already carry the base-entity `visibility` enum; children hang off the access roots) — writes stay owner/org-scoped. Boundary still needing his veto: connections/credentials, cost, and GSC stats are NOT scraped-public content — keep those restricted unless he says otherwise. This supersedes the old org-wide-visibility decision (deleted from Decisions needed) and shrinks the `/access` UUID-picker pain to edit-grants only.
5. **Markdown must render as a real page (Arman, 2026-07-20).** "the way markdown is handled is terrible. It links to a file instead of showing you a normal, proper page in the ui." Today PageWorkspace's "Open extracted Markdown" opens the file-viewer overlay on the raw .md file; `SnapshotArtifacts.tsx` already has an inline `MarkdownPreview` behind a `showMarkdown` flag nobody sets. Build a proper rendered Content view on the page workspace (canonical markdown renderer, styled like any other page) with the file link demoted to a secondary artifact action.
6. **Copy + Copy-for-AI rollout — feature-wide gap (Arman, 2026-07-20).** Platform rule: every page/section/card gets a Copy icon and an intelligent "Copy for AI" (xml payload). The primitive is `components/agent-copy/` (`<CopyButtons>` human+agent pair, `buildAgentPayload` xml envelope) — invoke the `agent-copy` skill; exemplars: `features/tasks/components/TaskCopyForAiButton.tsx` + `serializeProjectTaskForAi.ts`. Marketing has ZERO callsites. Wire it across: page-workspace cards (SERP, social preview, indexability, headings, content stats, sitemap memberships, captures), every table (per-row icon + whole-list in header), brand cockpit cards (facts/assets/properties), coverage tiles, crawl session summary/feed. Stable kinds (`web-page`, `web-page-serp`, `web-brand`, …); pass `human`/`agent` as functions.
7. **Crawl persistence failure, 2026-07-21 00:30** — `canonical_crawl_persistence_error` "We could not save one of the captured pages" on the titaniumsuccess site crawl; and 01:39 a crawl stream died mid-run ("Stream ended before its completion event", client saw Failed-to-fetch — coincides with the aidream redeploy window). Diagnose from scraper logs after the fresh deploy; suspect the soft-delete/upsert class (next item).
8. **Social properties routes** — discovery promotion now creates/attaches the typed `web.property` row and repairs legacy fact promotions. Remaining scope is the dedicated `brands/[id]/socials/...` route family from Arman's URL sketch.
9. **`/marketing` overview page** — currently a redirect to `/brands` (deliberate stopgap). Build the real workspace overview when the coverage data exists to populate it.
10. **Repo-wide sonner→`@/lib/toast` migration** — running as separate user-started task (task chip); if it stalls, the recipe is in `lib/toast.ts`'s header + ESLint ban.
11. **One human click-through of the brand-move select** — SiteEditorDialog's Brand dropdown (drives `web.move_site_brand`) could not be operated by the automation harness (Radix Select ignores its synthetic clicks); options load correctly and the RPC is live + typed. Move a site between two same-org brands and watch both cockpits update.
12. **Soft-delete vs upsert-arbiter rule (server side).** The unique arbiters the scraper upserts against (`page(site_id,url_hash)`, `sitemap(site_id,url)`, `page_sitemap(page_id,sitemap_id)`, `discovered_item` dedup, `gsc_page_stat(page_id,date)`) are deliberately PLAIN uniques (partial indexes broke ON CONFLICT twice), so they also match soft-deleted rows. Writer rule: **every server upsert's ON CONFLICT DO UPDATE must also set `deleted_at = null`** when the business meaning is "this thing exists again" (sitemap re-discovered after user delete, page re-advertised). Today a deleted sitemap that re-syncs silently updates a row that stays invisible forever. FE side is fixed for manual page re-add (restore-on-23505 in `features/marketing/data/service.ts#createManualPage`, live-verified). Remaining sweep note: `analysis_item(org,key)`, `provider(org,key)`, `site_item_config(site,item)` uniques also ignore `deleted_at` — no delete UI exists for them yet, but any future delete ships with the same restore-on-conflict rule.
13. **FENCED-FILE dead link — `SiteOverview.tsx` line ~412** links to `${sitePath}/screenshots`, which no longer exists (site-level gallery deleted per Arman's ruling; captures live on the page workspace + crawl session). The overview's screenshot affordance should open the homepage page's Captures (or the canonical file viewer directly via the hero `file_id`). Fenced for the current session's agent — fix there.
14. **EntityModeHeader / RouteModeNav overlap at mid-wide viewports.** With Marketing's 13 modes, at ~1500-1700px the centered mode pill renders over the left entity label (site name under the Overview tab). The collapse-to-dropdown logic doesn't account for left/right cluster widths. Shared shell primitive (`features/shell/components/header/RouteModeNav.tsx` / `EntityModeHeader.tsx`) — fix there, test with the longest mode list (marketing site shell).
15. **Access page grants by raw UUID** (`/access` — "Enter UUID manually", grantee ID input). Works, but an agency admin can't be expected to paste UUIDs; needs the platform user/org picker.
16. **Backlog (post-coverage):** GSC-submitted-sitemaps vs ours diff UI; external-link domain rollup UI (Wave 4 FE); analysis/finding workers (score columns intentionally hidden until then); brand-level discovery inbox aggregation; duplicate "Titanium Success"/"AI Matrx" test sites+brands in other orgs — Arman deletes via UI.

## Done

- www↔apex scope bug fixed — scraper treats www/apex as the same site for crawl scope (aidream `0447e2f38`, shipped in matrx-scraper ≥0.1.39; was the "hundreds go by, only 4 fetched" cause). Re-verify with one real crawl on aimatrx.com during E2E.
- Discovery promotion integration: the frontend now promotes fax, homepage title/description/site name, Open Graph images, and Twitter card images without falling back to `other`; social discoveries become typed brand properties; confirmed identity assets hydrate brand/site summary fields; Other requires a custom label; the Add Fact dropdown is no longer clipped; and the scraper recognizes Schema.org/labeled fax numbers without duplicating them as phones.
- Adversarial UX audit + screenshot restructure (2026-07-20, Claude): site-level Screenshots surface deleted (screenshots are page/crawl evidence — page workspace "Captures" = current per kind + as-of history + delete; crawl snapshots keep run scope); every `files.files`-backed artifact opens the canonical file viewer overlay (`CaptureThumb` + `useOpenFilePreviewWindow`; verified live — viewer opens with share/download/fullscreen; image bytes blocked only by the known access-association bug being fixed in aidream). Live crawl feed redesigned and live-verified: discovery/classification are counters only (Discovered/Queued/Fetched/Failed/Skipped + in-place ticker), rows only for fetches/failures/warnings/milestones, capped at 200; presenter tests encode the contract. Soft-delete constraint sweep done (results in the soft-delete/upsert-arbiter item); manual page re-add restores soft-deleted row (verified in-browser). Legacy flat-URL redirect, discovery, sitemaps, coverage, integrations, settings, access, cost, links, batches, connections, sites list all walked and rendering sanely.

- Full-CRUD program (2026-07-20): every marketing entity has its appropriate create/edit/delete (CRUD map in features/marketing/FEATURE.md); SiteEditorDialog + row actions everywhere; property/asset/fact editors; manual page create/delete; sitemap toggle/delete; discovery delete/un-dismiss; screenshot + crawl-session delete; grants migration web_marketing_crud_grants.sql; PGRST116 class fix (maybeSingle + assertFound + list-only delete invalidation); create_site p_brand_id + move_site_brand wired (cockpit "Add site" pre-binds the brand — the duplicate-brand mint bug is dead). Verified E2E: brand→site-under-brand→delete site→delete brand.
- Brand layer live — `web.brand/property/brand_asset/business_fact/discovered_item`, canonical RLS, `create_site` creates-or-reuses brand; see migrations/web_brand_layer*.sql.
- Brand-first routing — `/marketing/brands/[brandId]/sites/[siteId]/**`, legacy flat URLs client-redirect; see `lib/routes.ts`, `app/(core)/marketing/`.
- Brands hub + cockpit + full brand CRUD (all-fields editor dialog, delete guarded by owned sites), site Danger-zone delete — `components/brands/`.
- Discovery inbox with confirm→asset/fact promotion, verified E2E — `components/discovery/`.
- Sitemaps vertical (tables + workspace + per-sitemap page lists + sync command) — `components/sitemaps/`, server `sitemap_sync.py`.
- Site overview rebuilt (initialize flow, connections board, identity edit-in-place, loud per-step failure panel).
- Error capture: `marketing-crawler` red source at scraper chokepoints; captured sonner wrapper `lib/toast.ts` (marketing migrated).
- Constraint fixes from first prod run (screenshot kinds, discovered value_hash dedup) — migrations/web_initialize_constraint_fixes.sql; scraper upsert retargeted + regression-tested (aidream 3b8ef547c).
- Wave 2 (GSC sync server+FE), Wave 3 (Coverage matrix at `/coverage` + `?coverage=` pages filters), Wave 4 (link resolution + `links/resolve`), Wave 5 (page workspace rebuild — SERP/social/indexability/headings/stats) — see components/coverage/, components/pages/, aidream web_crawl/gsc_sync.py + link_resolution.py.

## Decisions needed

- **Captured-HTML retention.** Situation: every snapshot stores the full raw HTML AND extracted Markdown as `files.files` objects (`body_file_id` / `markdown_file_id`, written by `body_persister_orm.py` with no unchanged-content dedupe — a re-crawl of an identical page stores fresh copies). Today nothing re-reads the HTML bytes except the human file viewer; Markdown is about to be genuinely reused (rendered Content view). The planned analysis workers are the intended HTML consumer (re-analyze without refetching). Decide: (a) keep storing raw HTML as-is (analysis workers land on it), (b) store Markdown + head_tags + seo_metrics only and refetch when workers need HTML, or (c) keep HTML but add content-hash dedupe so unchanged pages share one stored file (cheapest long-term, small scraper change). Recommendation: (c).
- **Public-view boundary.** Situation: Arman ruled everyone gets view access because the content is scraped from public websites. But three data families are NOT scraped public content: Google Search Console stats (`gsc_page_stat` — the org's private Search Console data), cost rollups, and integration/connection metadata. Decide: do those three stay restricted to the owning org while everything scraped is universally viewable (recommended), or does "everyone views everything" include them?
