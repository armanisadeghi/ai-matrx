# Patrol Sightings — matrx-frontend

One line per spotted violation of a REGISTERED patrol (see
`/Users/armanisadeghi/code/common-docs/systems/improvement/pattern-patrols/PATROL_REGISTRY.md`;
protocol in the `pattern-patrol` skill). Agents on other missions log here and
move on; patrol runs verify each line themselves and check it off with a
one-word outcome. Sightings are hints, not facts.

Format: `- [ ] <P#> | <file-or-route> | <one line> | <date>`

## Open

- [ ] P13 | `features/marketing/seo/keyword-workbench/components/cells.tsx:143` | the dimension-value cell picker on the Keyword Workbench assignment table lists values with no "+ Add" — the exact "assign a tier and get forced into the existing options" class. NOT fixed here: the Keyword Workbench chip is actively rewriting that tree (`5e6fd1f061`), so it is noted in HANDOFF.md instead of edited | 2026-08-23
- [ ] P13 | `features/scopes/components/quick-assign/ScopeContextTargetPicker.tsx` (4 mapped choice lists) | a four-level cascade — organization → scope type → scope → context item. Each level is a record with its OWN creation home, so this needs four doors rather than one inline create; not a mechanical fix. | 2026-08-23
- [x] P13 | `features/scopes/components/CategorySelect.tsx` | FIXED AT THE ROOT 2026-08-24 — this is THE category control for the whole app (one component, `dimension` argument), so fixing it closed the same dead end on all twelve consuming surfaces at once. Rebuilt on `components/ui/creatable-picker`, creating through the canonical `useCategories().create`. Live-verified on the content plan. | 2026-08-23
- [ ] P13 + FORK | `features/agent-shortcuts/components/CategorySelect.tsx` | **CORRECTED 2026-08-24 after research** — NOT a second table. Shortcut categories are `platform.categories` rows with `dimension='shortcut'` (66 rows, migration already done); the duplication is a legacy REST API + row shape + Redux slice + this forked picker on top of the SAME data. Research, the four real differences, and a five-phase merge: `docs/handoffs/agent-shortcut-categories-merge.md`. Do not add "+ Add" here — it would entrench the fork. One product decision is open. | 2026-08-24
- [ ] P13 | `features/marketing/content-plan/components/ResearchTopicSelect.tsx` (3) | closed topic picker; topics are user vocabulary | 2026-08-23
- [ ] P13 | `features/marketing/competitors/CompetitorAutopsyWorkspace.tsx` (3) + `CompetitorIdentification.tsx` (1) | closed choice lists in the competitor surfaces | 2026-08-23
- [ ] P13 | `features/agents/components/run-controls/SurfaceSimulatorSelect.tsx` (2) | picks a registered surface — likely a legitimate "existing record created elsewhere" exemption, but the exemption must be recorded in the detector's allowlist WITH its reason by a human, not assumed | 2026-08-23
- [ ] P13 | `features/agents/components/inputs/input-components/SelectInput.tsx` (2) | the generic agent-input Select renders broker-supplied options with no add path; whether P23 applies depends on whether the option set is the author's vocabulary — needs a ruling, not a guess | 2026-08-23
- [ ] P13 | `features/marketing/seo/capabilities/SeoCapabilitiesWorkspace.tsx` (1), `features/marketing/front-doors/FrontDoorSiteSelect.tsx` (1), `features/marketing/discovery/youtube/YouTubeDiscovery.tsx` (1) | remaining detector findings from the first `pnpm check:picker-add` run | 2026-08-23
- [ ] CANDIDATE | live RLS / aidream `iam.apply_rls` | ambient platform-admin access on personal rows: 94 authenticated-readable `user_id` tables retain `platform_admin_all`; suggestion queue fixed, credential/secret and personal-state remainder tracked as D252; proposed Tier R after every RLS-generator/backfill change, using count-only admin-vs-owner simulation | 2026-08-22
- [ ] P5 | `features/admin/unwired/UnwiredConsole.tsx:184` | table needs canonical row/view Copy, but its existing per-row “Copy brief” must first move into a row-level Copy-for-AI variant; adding the pair now would create a forbidden third copy control | missing-machinery | 2026-08-19
- [x] P5 | `features/assists/manager/AssistsManager.tsx` + `features/marketing/seo/keyword-research/components/KeywordResearchWorkbench.tsx` | canonical row/view Copy and Copy-for-AI added; two adversarial payload rejections repaired; exact candidate `8196a478e` certified | fixed-certified | 2026-08-19
- [ ] P1 | `features/masterwork/components/masterworks/AuditionDialog.tsx` (findings list) | backtest cites a specific Rulebook `rule_id` as inert text; build one canonical rule-level route/anchor/opener on the Rulebook detail surface, then consume it here and for every rule citation | missing-machinery | 2026-08-17
- [x] P3 | `features/marketing/content-plan/**` (`/marketing/content-plan`, `/marketing/content-plan/[siteId]`) | whole feature was below the touch floor and clipped at 390px (~120 h-6/h-7/h-8 controls, no `<h1>`, one `useIsMobile()` caller); fixed as a class with the new `.matrx-touch-targets` subtree floor + `MatrxColumnDef.mobileHidden`, measured 0 controls under 44px across all six views | fixed-measured | 2026-08-16
- [ ] P3 | hover-only controls, see `.matrx/patrol-reports/mobile-friendly-ui.md` | current detector returns 225 files; the two real structurally new actions in `OpenQuestionsPanel` are repaired in the 2026-08-17 candidate, while decorative-icon matches remain non-findings and the broader class still needs control-vs-decoration triage | partial-fixed; detector-task-open | 2026-08-16
- [x] P10 | see `.matrx/patrol-reports/type-suppression-debt.md` | candidate `a4a7c03ea` removes two `as any` casts and one stale ESLint suppression; resumed focused state/size proof and independent certification closed the escaped-delivery run | fixed-certified-delivered | 2026-08-18
- [ ] P9 | see `.matrx/patrol-reports/coming-soon-compliance.md` | first/full pass found 12 verified promise cases: corrected 10-case/14-handler Tier-M batch is preserved after two concrete rejections were fixed, but final interaction certification is infrastructure-blocked by a foreign preview lease; two manual proposals and 99 detector candidates remain routed | infrastructure-blocked | 2026-08-13
- [ ] P2 | see `.matrx/patrol-reports/inventory-law.md` | 2026-08-18 full detector/novelty pass fixed and independently certified two canonical agent-door gaps; 30 truthful catalogue units remain (2 detector-routed + 28 conversion tasks) after removing the invalid Notes and delete-unwired items | fixed-certified; backlog-open | 2026-08-18
- [x] P3 | see `.matrx/patrol-reports/mobile-friendly-ui.md` | exact candidate `e8a5e8428` repairs all 10 remaining live viewport-unit tokens, four structurally new touch-floor surfaces, and two hover-hidden actions; independently certified after two evidence-record corrections and integrated to main with exact ancestry preserved | fixed-certified-integrated; release-queued | 2026-08-17
- [ ] P3 | `app/(admin)/administration/ai/ai-models/provider-sync/page.tsx` (`/administration/ai/ai-models/provider-sync`) | header is compressed/overlapping at 375×812 in dark mode; observed during independent P8 certification and left off-mission | 2026-08-14
- [ ] P6 | see `.matrx/patrol-reports/no-emojis-in-ui.md` | 2026-08-15 full pass auto-fixed 78 verified lines in a 15-file batch; first candidate was rejected for premature projection claims, corrected candidate `441071746` is CERTIFIED, and 116 verified lines remain open in 55 files | fixed-certified; delivery-controller-ready | 2026-08-15
- [x] P5 | see `.matrx/patrol-reports/copy-everywhere.md` | six certain table gaps repaired with canonical `MatrxDataTable.copy`; certified by clean baseline delta plus focused rendered-component fallback after the direct preview exceeded the fleet memory cap | 2026-08-12
- [x] P10 | `features/tasks/hooks/useTaskManager.ts:150` | former `TEMP-RT-DEBUG` cast was removed by its owning task before the approved P10 batch; re-verification found no remaining harness symbol or cast | resolved-before-batch | 2026-08-12
- [ ] P4 | `.matrx/patrol-reports/light-dark-integrity-exception-review.md` | 48 active files / 102 raw-token lines remain proposed fixed-palette exceptions after the 2026-08-18 reconciliation; none is approved, old PromptExecutionDebugPanel resolved by deletion, and three newly reviewable surfaces were added; exact candidate `86fe855c6` fixes parked/stale Next build-copy scanning and is independently certified | detector-fixed-certified; verified-open | 2026-08-18
- [x] P4 | `features/applet/**` (ModernGlass.tsx, StyledComponents.tsx) | moot — the whole applet/app-builder feature was deleted 2026-08-13 (Arman); both files are gone | resolved-by-deletion | 2026-08-13
- [x] P7 | see `.matrx/patrol-reports/no-browser-dialogs.md` | prior manual findings were resolved before the 2026-08-13 patrol by canonical `confirm({...})` and `TextInputDialog` replacements in `460ff2dcc`; the full scope-aware detector is clean in `v0.4.561` | resolved-before-run | 2026-08-13
- [ ] P8 | see `.matrx/patrol-reports/real-loading-states.md` | structural-novelty pass repaired 19 callsites across 17 files in two independently certified batches; 68 mapping/skeleton cases remain open with exact missing evidence | fixed-certified-integrated; backlog-open; no-human-approval | 2026-08-17

## Cleared

- [x] P4 | `features/agents/components/context-items/bodies/WebpageBody.tsx` | former fixed-white live-page iframe was replaced before this patrol by the canonical saved-snapshot renderer; the raw token and proposed exception no longer exist | resolved-before-run | 2026-08-14
- [x] P1 | `app/(admin)/administration/ai/ai-tasks/page.tsx`, `app/(admin)/administration/agents/agent-apps/{analytics,executions}/page.tsx`, `app/(core)/organizations/page.tsx` | six registered-token dead ends repaired with canonical EntityRef doors; adversarial heading-semantics regression fixed; final verdict CERTIFIED | fixed-certified | 2026-08-12
- [x] P2 | `features/agent-comparison/components/MasterInputWindow.tsx` | configured agent labels were inert inside the mapping window | fixed-certified (canonical new-tab `EntityRef`; type PASS→PASS; detector 11→10; adversarial verdict CERTIFIED) | 2026-08-12
- [x] P3 | see `.matrx/patrol-reports/mobile-friendly-ui.md` | prior infrastructure-based rejection superseded: recovered 16-token literal `vh`→`dvh` batch across 15 files passed baseline-delta gates and independent adversarial certification, then reached READY production in `v0.4.550` | fixed-certified-shipped | 2026-08-12
- [x] P4 | `components/mardown-display/markdown-classification/custom-views/view-components/CandidateProfileView.tsx:209,211` | pale light-theme header skeletons now use theme-relative `bg-foreground/{20,10}` | fixed-certified | 2026-08-12
- [x] P4 | `features/war-room/components/room/RoomHeader.tsx:657` | mobile action-sheet row now uses semantic `active:bg-accent/50 border-border` | fixed-certified | 2026-08-12
- [x] P7 | see `.matrx/patrol-reports/no-browser-dialogs.md` | attempted 16-alert/9-file automatic batch was REJECTED because the managed preview became runaway before the complete browser matrix; all product changes fully reverted | rejected-reverted | 2026-08-12
- [x] P7 | see `.matrx/patrol-reports/no-browser-dialogs.md` | recovered the same approved 16-alert/9-file batch in isolation; baseline-delta gates and risk-based adversarial proof passed; shipped in v0.4.550 and verified live | fixed-certified-shipped | 2026-08-12
- [x] P3 | `components/ui/drawer.tsx:87` | canonical `DrawerContent` was fixed to the mobile viewport bottom without default `pb-safe` | 2026-08-12 | fixed-certified (added the approved primitive-level `pb-safe` default and regression assertion; mobile/desktop plus light/dark adversarial verdict CERTIFIED)
- [x] P4 | `components/errors/ErrorBoundaryView.tsx:121` | shared CopyButton uses `hover:bg-white/10` on both dark code and neutral light surfaces; light-mode hover is effectively invisible | 2026-08-11 | fixed-certified (theme surfaces now use `hover:bg-accent`; the fixed dark stack surface explicitly opts into stable on-dark chrome; adversarial verdict CERTIFIED)
- [x] P4 | 282 total `bg-white`/`text-black` files still need per-line triage | 2026-08-08 | superseded-and-reopened (the corrected property-specific 2026-08-11 pass found 150 unpaired lines; its earlier agent-cleared exception decisions were invalid under the new human-owned exception contract, and the unresolved set is now tracked under Open)
- [x] P7 | (2 files, grep `window.confirm\|window.alert\|window.prompt` in features/components/app) | last 2 browser-dialog files in the repo — finish the eradication | 2026-08-08 — STALE (the two-file count was wrong; superseded by the full patrol report + the 2026-08-09 batch above)
- [x] P3 | 6 files via grep `h-screen\|100vh` | banned viewport units still present | 2026-08-08 | fixed (2026-08-09 sweep: 4 youtube-discovery files min-h-screen→min-h-dvh, YouTubeSearchHistory + WarRoomShell + RunControlsMenu calc(100vh…)→calc(100dvh…); WarRoomShell got unit swap only — full header conformance stays with docs/handoffs/war-room-list-and-room-conformance.md)
- [x] P3 | 9 files: `fixed bottom-0` without `pb-safe` | fixed bottom bars missing safe-area padding | 2026-08-08 | fixed (2026-08-09 sweep: safe-area padding added to EditTabLayout bottom bar, ui/toast viewport, OrchestraBuilder mobile slide-over, MessagingSideSheet, MobileLayout sidebar nav; NotesFilterSheet/BottomSheet/CanvasShareSheet/MatrxDynamicPanel already compliant; dev resizable panels + MatrxPanel have zero consumers — skipped as dead code)
- 2026-08-09 · loud-recovery/dead-end · /marketing/.../keywords Performance tab renders 'No search queries stored yet' empty state while v_site_keyword_performance returns HTTP 500 — error laundered into fake empty state (spawn chip filed) · found during brand-alias drawer fix
- 2026-08-09 — **Patrol candidate (nominated):** 14 `components/ui/*` shadcn wrappers gate their Radix Root on `useIsMounted` and `return null`, blanking always-visible triggers/tabs/nav until hydration. Grep: `useIsMounted` + `return null` under `components/ui/`. Evidence + why the stated justification is false: FOUND_DEFECTS D144. Found while fixing the context-menu instance during the Inventory Law sweep (PR #72).
- [x] P4 | 44-file no-`dark:` batch (re-grep found 63) | historical code-fix batch completed, but its agent-declared “legitimate” exceptions are superseded and reopened under the human-owned exception contract; no old skip is trusted as approval | 2026-08-09
- 2026-08-10 — **Patrol candidate (nominated): spinner-while-AI-works.** ~50 client agent-run call sites show only a spinner/disabled button while the model streams (server always streams; clients discard it). Grep: `useHeadlessAgentJson`/`runHeadlessAgentJson` consumers that never read `activeRequestId`/`conversationId`; `useRunAgent` without `onChunk`. Fix recipe + full worklist: docs/handoffs/live-stream-everywhere.md (primitives `useLiveAgentRun` + `<LiveRunDisplay>` shipped 2026-08-10). Found during the platform-wide live-streaming gap analysis.
- 2026-08-11 · light-dark (P4) · hardcoded chart series hex escapes the token system: `#4285f4`/`#5e35b1` in `features/marketing/components/backlinks/BacklinkTrendChart.tsx:33-34` (+ axis tick hex at :210), same pattern in `features/marketing/search-console/components/PerformanceChart.tsx:214` and `features/marketing/components/pages/PagePerformanceCharts.tsx:18-21`. Not a backlinks-only fix — recharts series colors need `--chart-1..5` tokens applied across all chart consumers, or dark mode keeps getting hand-picked blues. Found during the backlink-system UI audit.
- 2026-08-11 · dead-end · `assertFound` (`features/marketing/data/service.ts:171`) reports EVERY zero-row read as "This <entity> was deleted or is no longer accessible", so an access/org-resolution miss reads as a deletion and offers no door. Confirmed live: two agent-review items were rejected as "site deleted or inaccessible" while brand + site were both alive and un-deleted in the DB. Chip filed as TASK-BL-7 (platform-wide, not backlinks-only).
- 2026-08-11 — **Patrol candidate (nominated): DURABILITY MISMATCH.** A stored URL whose lifetime is shorter than its consumer's contract — an expiring signed S3 URL persisted where the reader must resolve it LATER, or for ANYONE (anon share pages, published episodes, OG images, CDN/blog surfaces). Recurring class, third+ occurrence: FOUND_DEFECTS D1 (episode covers), the 2026-08-11 `pc_studio_runs.image_urls/.video_urls` sample, and this sweep's `workbench.notes.content`. NOT the same as "an expiring URL exists" — time-boxed share links, TTL caches, and audit rows recording the issued URL are all correct and must stay. Detection already built and repeatable: `pnpm check:media-durability` (+ `:full` patrol sweep) diffs the live DB against the committed classification in `scripts/media-durability/allowlist.json`; contract + limits in `scripts/media-durability/FEATURE.md`. Live counts at nomination: 734 tables scanned in Matrx Main → 61 columns holding signed URLs, of which exactly **1** is a real mismatch, 4 are mitigated-but-open, and 56 are correct by design; the CMS project `viyklljfdhtidwecakwx` was clean across all 11 tables / 4,960 rows. Found during the platform-wide durability-mismatch eradication.
- [x] P6 | `features/matrx-envelope/directives/planTree/PlanTreePreview.tsx:123` | literal key replaced by `KeyRound`; Batch A passed accessible-state, visual, and full type gates and shipped in `v0.4.527` | fixed-certified | 2026-08-11
- 2026-08-12 · stale-closure-loop · `features/agents/orchestras/components/AgentLibraryRail.tsx` + `CreateOrchestraDialog.tsx` each hold a duplicate `applyArrayViaToggle(current, next, toggle)` that fires `toggle` N times in one tick; `useAgentConsumer.toggleCategory`/`toggleTag` each recompute from the SAME render-closure snapshot, so only the LAST call survives (a whole-set clear removes one item). Canonical `setIncludedCats`/`setIncludedTags` now exist on the hook — these two callsites should consume them and the duplicate helper should die. Fixed in `AgentsGrid` only; left off-mission here. Found while wiring the agents-hub write target.
- [ ] CANDIDATE (disappearing-run class) | features/marketing/components/reputation/useReputationAnalysis.ts:79 + features/marketing/competitors/useCompetitorAutopsy.ts:97 | adopted-stream hooks reap removeRequest without aborting the in-flight stream (chip task_0c80bcb4 open); class doctrine /Users/armanisadeghi/code/common-docs/systems/agents/execution-runtime/LIVE-RUN-RETENTION.md | 2026-08-12
- 2026-08-13 · mobile-breakage (P3) + headings · Content Plan has **18 agent-review rows, 0 approved, 12 `changes_requested`**, and Arman's rejections repeat ONE class: no semantic page heading (9 of 12), controls under 44px (7), 390px clipping / no mobile drill-down (7). Verified in main @ v0.4.547: the entire `features/marketing/content-plan/` tree contains **zero `<h1>`**, the shell `PageHeader` supplies none (consumers pass their own, per `features/shell/components/header/variants/USAGE.md`), and exactly **one** file in the feature calls `useIsMobile()` (`ContentPlanWorkbench`; Setup uses none). This is a whole-feature presentation pass, not 12 separate bugs — chip `content_plan.review_queue.mobile_headings_pass`. Found while status-checking the content-plan handoff.
- 2026-08-14 — **Patrol candidate (nominated): `transition-all` on a motion-driven element.** A Tailwind `transition-all` on an element whose `transform` Framer Motion writes every frame makes the browser transition `transform` too; the CSS transition interpolates from `none` and fights motion, so the computed transform can sit at the identity matrix while motion's `x`/`y` hold correct values — the translate is silently discarded and the element pins to its origin. This is the confirmed root cause of **D195** (`components/ui/transformable-card.tsx`, fixed 2026-08-14), where two cards with distinct `initialPosition` stacked on one origin for four releases and the wrapper CSS was blamed instead. Detection is cheap and precise: match `<motion.*>` blocks whose className contains `transition-all`. Live count at nomination: **172 files** import `motion/react`/`framer-motion`, 14 of those also contain `transition-all`, and exactly **1** currently puts it on a motion element — `features/agents/resources/ResourceChips.tsx:133` (`whileHover={{ scale: 1.05 }}` + `transition-all`; left off-mission). Low count today is the argument FOR a cheap standing check: the pattern is invisible in review, costs nothing to detect, and destroyed a primitive's core feature the one time it landed. Found while fixing D195.
- 2026-08-15 — **Patrol candidate (nominated): modal dialogs freeze agent assistance on authored fields.** Repo grep found **314** TSX files importing the shared Dialog and **46 files / 55 Dialog roots** that also host form controls; **0/55** explicitly use `modal={false}`, and only **2/46** field-hosting files carry an outside-interaction unsaved-edit guard. Confirmed blocker: `FeedbackDetailDialog` made the body non-interactive and trapped focus away from the Agents header / floating `LiveRunWindow`; fixed in-mission with the existing non-modal prop + guard and browser-proved. Proposed **Tier C, weekly** until the shared default/policy is decided: audit the 55 authored roots, move agent-authorable Dialogs to the safe shared default/pattern, preserve deliberate blocking/alert dialogs as explicit modal opt-ins, and certify focus, outside-click, Escape, and unsaved-state behavior by risk class.
- [x] P8 | `components/mardown-display/chat-markdown/internal-handlers/SafeBlockRenderer.tsx` | shared dynamic fallback now uses the canonical contextual `Loading content…` status without changing the boundary | fixed-certified-delivered | 2026-08-17
- [x] P1 | `features/workflow-runtime/components/ReadoutView.tsx` | reported child-run id is no longer rendered as text; the current fallback names “Sub-workflow” and expands the canonical `WorkflowRunBoard` for that exact child run | resolved-before-run | 2026-08-17
- 2026-08-16 · mobile-breakage (P3) · Ambient fixed chips collide with bottom-anchored work surfaces on mobile: `ErrorInspectorBadge` (`fixed bottom-4 left-4`) covered a composer's controls and `AssistsDock` (`fixed bottom-14 right-3`) sat on top of the composer input in the vision-interview room (Arman's screenshots). Both got a mobile-only lift (`bottom-24`/`bottom-32`, `sm:` restores desktop) in-mission, but the CLASS needs a shared answer: any `fixed bottom-*` ambient chip vs any route whose bottom edge is an input/action bar — candidates include `AgentChatBubble`, `WindowTray`, `MobileActionBar` interplay. A shared bottom-clearance primitive (CSS var the shell/composer sets, chips consume) would end the per-chip guessing. Found while fixing the vision-interview mobile breakage.
- 2026-08-17 — **Patrol candidate (nominated): guided intake flows follow the house pattern.** Any create/intake wizard renders as a dedicated full page (never a cramped dialog) of big default-filled option buttons — icon in a tinted square, label + one-line helper, color-coded per question, min 44px, a sensible default PRE-SELECTED on every question so the user can click straight through — **never small chip "bubbles"**. Exemplars: `/research/topics/new` (`features/research/components/init/ResearchInitForm.tsx`), the podcast builder (`features/podcasts/generator/components/GeneratorForm.tsx`), the app builder (`features/agent-apps/`). Confirmed violation, fixed in-mission 2026-08-17: the Masterwork `NewRulebookDialog` intake (chip-bubble `BandPicker` in a dialog — Arman: "those stupid little small bubbles are horrible… completely goes against everything we have done") deleted and rebuilt as `/masterwork/new` (`features/masterwork/intake/NewRulebookFlow.tsx`). Detection: grep for `rounded-full border px-` option pickers inside `Dialog` content, and any new `Dialog` whose body asks 3+ questions. Found while rebuilding the Rulebook creation intake.
