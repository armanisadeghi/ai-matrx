-- expect: branch=accept production=accept autocommit=yes
-- chair-step: §11.0b, the campaign's one deliberately irreversible act; the chair is awake
--
-- ATTACK-7 finding 1's own probe: header-less + `-- chair-step:` + ALTER TYPE ... ADD VALUE.
-- The JUDGE accepts it — that is the chair-step contract — and BOTH runners then refuse a
-- non-TTY stdin and demand the filename typed back before a byte executes. `excused` counts
-- the non-additive reasons the chair step carried; it must be the same number in both.
--
alter type public.zz_judgment_enum add value 'commenter';
