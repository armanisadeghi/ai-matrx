-- retired: chair ruled KEEP (2026-09-26): the custom_fields grant files.files received on regeneration is the platform-standard declared state (authenticated holds it on every governed table; row security decides rows). Never applied; superseded by that ruling — see common-docs projects/rich-content-unification/evidence/read-lane-v2-queue.md.
-- (was chair-step): REVOKES the signed-in column grant on files.files.custom_fields that the read-lane v2 regeneration of files.files added at 2026-09-26 02:05 PT. Zero-delta requires it: before that regeneration authenticated held no privilege on custom_fields (the column was added platform-wide by the entity_type_gets_custom_fields trigger after files.files' grants were last generated); iam.apply_table_grants grants every live column not in client_excluded_columns, so the regeneration caught the grants up. Run this to restore the exact prior grants — OR, if the column is meant to be client-readable/writable (the declared generator state), leave the grant and retire this file. Rows read are unchanged either way (6 seats, 0 deltas).
revoke select, insert, update (custom_fields) on files.files from authenticated;
